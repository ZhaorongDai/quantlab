---
phase: quick-260909-174
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - example/acquisition.md
  - example/pageledger.md
  - quantlab/base/acquisition.py
  - tests/test_acquisition_progress.py
  - tests/test_tiingo_quota.py
  - tests/test_atomic_sidecars.py
  - .planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/manifest_semantics_scan.py
  - .planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/scan-allowlist.txt
  - .planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/prose_only_check.py
autonomous: true
requirements: [D-18]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-18 语义在仓库里只剩一个版本，且与 HEAD 的行为一致：`_failures.json` 是**跨 run 累积**的耐久记录（03.4-08 把写盘前的合并变成无条件、位于续跑循环之外、紧邻写入的唯一一次调用），空的 `{}` 只说明「本轮有消息的符号都干净、且盘上也没有别的遗留条目」，非空也不等于上一轮有失败；`AcquisitionResult.failures` 与盘上清单是**两个独立值**（REVIEW CR-01 `c0e392f`：`_run` 只往 `manifest = dict(failures)` 这个副本上合并），成立的关系是 `set(result.failures)` 包含于 `set(manifest)`（共有 key 上消息一致）与 `set(result.failures)` 包含于 `set(result.requested)`。任何一句仍在流通的陈述都不得与这段相反。"
    - "验收不是一次字面量 grep，而是一个能红的命令：`manifest_semantics_scan.py` 按 11 条语义变体（中英双语）扫描全仓 `*.md`/`*.py`，扫描前把每份文件的空白折叠成单空格（换行不再能把一句话藏起来），并额外构造一份把 Python 相邻字符串字面量拼接缝合掉的变体、取两者较大值（字符串断行不再能把一句话藏起来）。gate 是「扫描输出 == 预先写死的 allowlist」，双向可失败：漏掉一个站点会多出一行，误改一个本来正确的站点会少一行。"
    - "扫描的排除面是路径级且写在明处：只排除 `.planning/phases/` 与 `.planning/quick/`（都是带日期的历史记录，允许描述写下时为真的东西；本 PLAN、其 SUMMARY 与扫描器脚本都在其中，因此不会自己命中自己）。`.planning/ROADMAP.md` 与 `.planning/STATE.md` 不排除 —— 站点 1 和站点 3 就住在那里。`.planning/WINDOWS.md` 的两行也不排除，而是进 allowlist 并写明理由，让豁免可见而不是藏在过滤器里。"
    - "每处新写下的行为描述都是从**当前源码**重新推导的，不是从任何兄弟文档抄来的：写清单读者集合前重跑 `grep -rn read_failure_manifest\\(\\) quantlab/`，写合并覆盖面前重跑 `_merge_unattempted_failures` 的调用点枚举，两次输出都记进 SUMMARY。VERIFICATION gap 2 就是「继承」造出来的 —— 上一份文档说什么，下一份就照抄什么。"
    - "引用链被从根上拆掉，而不是换一根新绳子：`_write_failure_manifest` 的 docstring 不再是另外五处（`_run` 两处注释、`_merge_unattempted_failures` docstring、三个测试文件）定义危害的权威出处。每一处改为用自己的话陈述危害（把清单整份写成 `{}` 会抹掉运维手上唯一一份「这些符号还在失败」的记录），因此下一次语义变化不会再一改就烂五处。"
    - "三个 `.py` 文件的改动是 prose-only，由 `prose_only_check.py` 用 AST 证明：注释根本不进 AST，所有字符串常量被替换成占位符，f-string 只保留插值表达式 —— 任何条件、调用、控制流或插值的改动都会让它红。这条 gate 自己也要被 mutation 证伪一次（临时改一个可执行表达式看它转红，再还原），遵循本仓库自己的规矩：a lock that passes on arrival is mutation-verified rather than accepted。"
    - "`uv run pytest -q` 仍然是 734 passed / 0 failed。本仓库有若干扫描源码的结构性测试（`tests/test_source_inspector.py` 扫 `_failures.json` 字面量的重复声明、`tests/test_acquisition_progress.py` 的 `Parallel(` AST 计数、`tests/test_acquisition_batching.py` 的 AST 首句守卫），所以一次 docstring 改动不是自动测试中性的。"
  artifacts:
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - example/acquisition.md
    - example/pageledger.md
    - quantlab/base/acquisition.py
    - tests/test_acquisition_progress.py
    - tests/test_tiingo_quota.py
    - tests/test_atomic_sidecars.py
    - .planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/manifest_semantics_scan.py
    - .planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/scan-allowlist.txt
    - .planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/prose_only_check.py
  key_links:
    - "`manifest_semantics_scan.py` 的输出 -> `scan-allowlist.txt`：allowlist 在任何编辑之前（Task 1）就写死并提交，所以 Task 2/3 的编辑是被一份早于它们的文件裁决的。执行者无法通过「把改完的扫描结果倒进 allowlist」把 gate 变绿。"
    - "`prose_only_check.py` 的 base SHA -> `$(git rev-parse --git-dir)/quick-260909-174-base`：GSD 逐任务提交，所以只看工作区的比较是一个不可能失败的证明（03.1 D-06 先例）。SHA 在 Task 1 第一步、任何编辑之前钉进 `.git/`。"
    - "`_write_failure_manifest` 的 docstring -> 另外五处引用它的站点：这条链必须在同一个提交里一起断开。只改根（站点 2）会让五处引用悬空，只改引用会让根继续说假话 —— 两种半吊子都是同一个缺陷换个字号。"
    - "`example/acquisition.md` 的核心概念段（站点 4）-> 同文件「常见坑 5」：坑 5 是已经正确的那一版表述，是站点 4 与站点 5 的语气参照物；但内容仍要对着源码复核，参照的是写法不是事实。"
---

<objective>
把六处仍在断言已作废语义的文档/docstring 改成与 HEAD 行为一致的表述，并连带拆掉以
`_write_failure_manifest` docstring 为根的那条引用链（另外六处站点，其中
`tests/test_atomic_sidecars.py` 那一处是本次用「跨行 + 跨字符串拼接」归一化扫描新发现的 ——
任何按本阶段建议的字面量做的行级 grep 都扫不到它）。

被作废的两条语义：

1. `set(result.failures)` 与盘上清单相等 —— REVIEW CR-01（`c0e392f`）**主动作废**。`_run`
   现在往 `manifest = dict(failures)` 的副本上合并，把未被污染的 `failures` 交给
   `AcquisitionResult`。
2. 「清单是某一轮的快照 / 空的 `{}` 说明上一轮跑得干净」—— 03.4-08 之后清单是**跨 run 累积**
   的耐久记录。

Purpose: `.planning/ROADMAP.md` 是仓库外 `quantlab-console` 要照着写断言的契约文件，它现在
声称的那条等式在本仓库里有一个**正绿着的测试**在断言其反面
（`test_a_disjoint_rerun_does_not_inherit_earlier_failures`）；而 `_write_failure_manifest`
的 docstring 是另外五处（含一个测试的失败消息）引用的权威出处，引用链的根现在是一句假话。

Output: 8 个文件的有界文本编辑（其中 `.py` 一律 prose-only），外加三份验收器产物 ——
一个语义多变体扫描器、一份预先写死的 allowlist、一个 AST prose-only 校验器。

**无行为变更。可执行代码的改动是零。**
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03.4-data-source-registry/03.4-VALIDATION.md
@.planning/phases/03.4-data-source-registry/03.4-VERIFICATION.md

`03.4-VALIDATION.md` 的 D-18 行与其下 "Why the `result_and_manifest_agree` row was AMENDED
rather than deleted" 三段，是**唯一**一份已经正确的契约陈述。六个站点要向它看齐，
不是向彼此看齐。
</context>

<!-- planner-discipline-allow: last run was clean | 最近一次 run | 每次 run 覆盖重写 | 永远描述 | 上次跑干净 | 干净收尾 | does hold on every exit path -->
<!-- 理由：这些字面量以「扫描器要搜的正则」身份出现在 action 里，而扫描器按路径排除
     `.planning/quick/`，本 PLAN.md 与扫描器脚本本身都在其中，因此不可能自我命中。
     gate 也不是 grep -c LITERAL == 0 形式，而是「扫描输出 == allowlist」的双向比对。 -->

<tasks>

<task type="tracer">
  <name>Task 1: 建立能红的验收器，并在 example/pageledger.md 上跑通一次红到绿</name>
  <files>.planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/manifest_semantics_scan.py, .planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/scan-allowlist.txt, .planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/prose_only_check.py, example/pageledger.md</files>
  <precondition>八个目标文件在工作区里都是干净的。仓库当前有 `cal.py`、`pyproject.toml`、`test.py`、`uv.lock` 四处与本任务无关的未提交改动，它们绝不入暂存区；若 `git status --porcelain` 显示本计划的任何目标文件已 dirty，停下并报告。</precondition>
  <read_first>
    - `example/pageledger.md`：用 `grep -n "_failures.json" example/pageledger.md` 定位那段
      「关于那个 `_failures.json`」的说明，整段读完再动手 —— 同一句里有**必须存活**的三个
      正确子句（不是续跑输入 / 仓库内两个读者 / 都经由唯一那个容错读取器）。
    - `example/acquisition.md` 的「常见坑 5」（`grep -n "跨 run 累积的运维记录" example/acquisition.md`）：
      同一概念已经改对的那一版，作为语气参照。
  </read_first>
  <action>
第一步，在任何编辑之前钉住基线 SHA：把 `git rev-parse HEAD` 的输出写进
`$(git rev-parse --git-dir)/quick-260909-174-base`。理由是本仓库自己的 03.1 D-06 先例 ——
GSD 逐任务提交，所以到 Task 3 时只比较工作区的检查会是一个不可能失败的证明。

第二步，写 `manifest_semantics_scan.py`（纯 stdlib，只用 `re`、`subprocess`、`pathlib`、`sys`；
不 import 任何 `quantlab` 模块；始终以 0 退出 —— 它是报告器，不是 gate）。行为完全确定：

- 待扫文件来自 `git ls-files "*.md" "*.py"`，剔除前缀为 `.planning/phases/` 与
  `.planning/quick/` 的路径。这两个前缀是带日期的历史记录，允许描述写下时为真的东西；本
  PLAN、其 SUMMARY 和扫描器脚本自己都在 `.planning/quick/` 下，所以扫描器不会自我命中。
  `.planning/ROADMAP.md`、`.planning/STATE.md`、`.planning/WINDOWS.md` 一律不排除。
- 每份文件读成严格 utf-8（不吞解码错误 —— 真出现就是一个发现）。先做 `flat`：把 `\s+`
  全部折叠成单个空格。再做 `joined`：在 `flat` 基础上用 `re.sub(r"(['\"])\s*\1", "", flat)`
  缝合 Python 相邻字符串字面量的拼接缝。**这两步就是本扫描器存在的理由** —— 站点 2 的那句话
  在源码里跨行断开，`tests/test_atomic_sidecars.py` 那一处也是跨行断开，
  `tests/test_acquisition_progress.py` 里还有一处是跨字符串拼接断开的；三者都躲得过行级 grep。
- 11 条语义变体正则，顺序固定（索引就是输出里的 `Pnn`，不可重排）：
  P00 是 `set\(result\.failures\)\s*==\s*set`，
  P01 是 `does hold on every exit path`，
  P02 是 `always describes the LATEST run`，
  P03 是 `an empty one is a meaningful statement`，
  P04 是 `last run was clean`，
  P05 是 `describing the latest run`，
  P06 是 `最近一次\s*run`，
  P07 是 `每次\s*run\s*覆盖重写`，
  P08 是 `永远描述`，
  P09 是 `上次跑干净`，
  P10 是 `干净收尾`。
- 每个（文件, 正则）组合取 `flat` 与 `joined` 两份文本中 `re.findall` 命中数的**较大值**；
  非零才输出一行 `路径|Pnn|次数`。整体按路径升序（`sorted(files)`），同一路径内按正则索引升序。

第三步，跑一次扫描，把完整输出存进本任务目录下的 `scan-before.txt` 并记进 SUMMARY。
编写本计划时实测为 **22 行**；若数字不同，先停下弄清为什么再继续。

第四步，写死 `scan-allowlist.txt` —— 全部编辑完成之后应当恰好剩下的 **7 行**，一字不差、
顺序如下（每行的存活理由写进提交信息与 SUMMARY，文件本身只放机器可读的行）：
`.planning/WINDOWS.md|P00|2` 与 `.planning/WINDOWS.md|P04|2`（带时间戳的偏差台账，记录的是
一个**已修复**的偏差在被发现时的样子，与 `.planning/phases/` 同类；不用路径过滤器藏起来，
而是让豁免留在明处）；`example/acquisition.md|P00|1`（「旧的那条等式已经作废，别再恢复」
那一句 —— 一个概念留**一处**带标签的退役说明就够，这就是那一处）；
`example/acquisition.md|P07|1`（「常见坑 5」那句本身为真：整份重写是机制，跨 run 累积是语义，
两句连在一起才不误导）；`example/registry.md|P00|1`（同样带退役标签）；
`quantlab/base/acquisition.py|P00|1`（`_run` 里那条 "old ... equality is GONE" 注释，带标签，
是给未来读者的防复活说明）；`tests/test_acquisition_progress.py|P00|2`（两处都写着
"used to mean" / "used to pin"）。

注意 `example/acquisition.md|P07` 在 allowlist 里是 **1**，而扫描前是 **2**：站点 4 那一处
必须消失、坑 5 那一处必须留下。这一格是双向的 —— 少改会剩 2，多改会剩 0，两种都红。

第五步，证明 gate 现在是红的：跑本任务 verify 里那条命令，确认它打印出**八个**待改文件路径。
把这份清单记进 SUMMARY。

第六步，写 `prose_only_check.py`（纯 stdlib：`ast`、`subprocess`、`sys`）。用法是
`prose_only_check.py BASE_SHA FILE...`。对每个文件取 `git show BASE_SHA:FILE` 与工作区当前
内容，各自 `ast.parse` 后经一个 `ast.NodeTransformer` 归一化：所有 `str` 型 `Constant`
替换成同一个占位常量；`JoinedStr` 只保留其 `FormattedValue` 子节点（丢掉字面部分，留下
插值表达式）。比较两侧 `ast.dump`。不同则打印文件名并以 1 退出，相同则打印确认并以 0 退出。
注释根本不进 AST，所以注释改动天然通过；而条件、调用、控制流、f-string 插值的任何改动都会红。

第七步，修站点 6（`example/pageledger.md`）。只替换那句把清单称作某一轮整体快照的从句，
换成：它是**跨 run 累积**的耐久记录（`{symbol: 错误消息}`，自 03.4-08 起，写盘前会把盘上
本轮没轮到的旧条目折回来）。同句里必须原样存活的三件事：它**不是续跑输入**（续跑只看
水位边车在不在）、仓库内读它的有**两处**、两处都经由唯一那个容错读取器
`CoverageLedger.read_failure_manifest`。前面「页台账是批内断点、失败清单是整体记录」的
粒度对比保留，对比本身仍然成立。

**写之前重跑 `grep -rn "read_failure_manifest()" quantlab/` 并把输出记进 SUMMARY**，读者集合
按那次输出写 —— 不许从 `example/acquisition.md` 或 ROADMAP 抄。VERIFICATION gap 2 就是
「继承」造出来的：上一份文档说什么，下一份就照抄什么，而它已经被同一阶段证伪了。

**禁止复述被退役的句子**，即使加上「以前说过」的标签也不行。理由有两条：一，这条引用链
本身就是缺陷机制 —— 五处站点靠引用第六处的一句话来定义危害，那句话一变五处全烂；二，
复述会让 allowlist 不再可以预先写死，gate 就不确定了。已经带标签的退役说明有
`example/acquisition.md` 的那一句、`example/registry.md`、`03.4-VALIDATION.md` 的 D-18 行，
一个概念有这些就够了。新句子一律**正面**表达新语义（跨 run 累积 / 耐久记录 / 本轮加盘上遗留），
不要用「不再是某一轮的快照」这种靠否定旧措辞的写法 —— 那会把被扫的字面量重新写回文件。
  </action>
  <verify>
    <automated>cd /Users/daizhaorong/projects/quantlab; D=".planning/quick/260909-174-fix-six-stale-documentation-and-docstrin"; uv run python "$D/manifest_semantics_scan.py" | grep -Fxv -f "$D/scan-allowlist.txt" | sed -E 's/\|.*//' | sort -u</automated>
  </verify>
  <done>
上面这条命令打印的文件集合恰好是下面**七行**，一个不多一个不少 —— `example/pageledger.md`
已经不在其中（证明它对站点 6 的修复变绿了），其余七个待改文件都还在（证明它确实能红）：
`.planning/ROADMAP.md`、`.planning/STATE.md`、`example/acquisition.md`、
`quantlab/base/acquisition.py`、`tests/test_acquisition_progress.py`、
`tests/test_atomic_sidecars.py`、`tests/test_tiingo_quota.py`。

`scan-before.txt`（22 行）与 `grep -rn "read_failure_manifest()" quantlab/` 的输出都已记进
SUMMARY。`$(git rev-parse --git-dir)/quick-260909-174-base` 里存着编辑前的 HEAD SHA。
`uv run pytest tests/test_source_inspector.py -q` 仍绿（本任务未碰 `.py` 源码，这是廉价的
早期背书）。

  </done>
</task>

<task type="auto">
  <name>Task 2: 修四处散文站点 —— ROADMAP、STATE、example/acquisition.md 两处</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, example/acquisition.md</files>
  <read_first>
    - 用 `grep -n "does hold on every exit path" .planning/ROADMAP.md` 定位站点 1，向上读到该
      bullet 开头、向下读到下一个 bullet（`**Retire the hardcoded vendor dispatch.**`）。
    - 用 `grep -n "holds on EVERY exit path" .planning/STATE.md` 定位站点 3。
    - 用 `grep -n "FAILURE_MANIFEST_NAME" example/acquisition.md` 定位站点 4 所在的核心概念
      bullet，把它连同其下的「合并」段与两条包含关系一起读完；用
      `grep -n "的实际内容" example/acquisition.md` 定位站点 5 所在的走查段。
    - `03.4-VALIDATION.md` 的 D-18 行与其下三段解释 —— 这是要向之看齐的那一份。
  </read_first>
  <action>
**站点 1（`.planning/ROADMAP.md`，英文）。** 只替换从那条等式开头到
"...still in the manifest, read back from disk." 为止的整句群。前面 2026-09-08 的 D-18
FACTUAL CORRECTION（清单不是续跑输入）与 2026-09-09 plan 03.4-09 的读者集合更正
（以 "...not a count carried forward.)" 结束的那个括号）**必须一字不动地存活**，后面的
bullet 也不动。

替换内容必须交代五件事，措辞自拟：(a) 清单是**跨 run 的耐久记录** —— 03.4-08 把写盘前的
合并变成无条件、位于续跑循环之外、紧邻写入的唯一一次调用，所以五条退出路径是按构造覆盖的，
不是逐条审计出来的；(b) `AcquisitionResult.failures` 与盘上清单是两个**独立值**
（REVIEW CR-01 / `c0e392f`：`_run` 只往 `manifest = dict(failures)` 的副本上合并）；
(c) console 可以照着写断言的三条 —— 结果的失败集合**包含于**盘上清单的键集合、共有 key 上
两边消息一致、结果的失败集合**包含于** `result.requested`（用 `⊆` 或 `<=` 写，绝不要写成等式）；
(d) 空的 `{}` 的正确读法 —— 本轮有消息的符号都干净、且盘上没有别的遗留条目，反过来非空也不
等于上一轮有失败；(e) 真正被钉住的运维保护是
`tests/test_acquisition_progress.py::test_the_manifest_survives_a_quota_abort_on_the_default_path`
（清单内容在默认路径的配额中止后仍在盘上）与
`::test_a_disjoint_rerun_does_not_inherit_earlier_failures`（结果不被别的 roster 的旧条目膨胀）。
末尾按本文件既有风格加一条日期标注 `(FACTUAL CORRECTION 2026-09-09, quick 260909-174: ...)`，
说明本 bullet 此前断言的那条等式已被 REVIEW CR-01 主动作废、原因见 `03.4-VALIDATION.md`
的 D-18 行 —— 但**不要把那条等式本身复述出来**（Task 1 第七步的禁令，全任务通用）。

**站点 3（`.planning/STATE.md`，英文）。决定：就地修正并加日期标注，不追加新条目。**
这条决策记录里历史上为真的部分（03.4-05 选择了 MERGE 而不是跳过写入，RESEARCH Pitfall 4
留了这个选择）保留；只把被证伪的尾句换成真正的后果 —— 保留上一轮未重试的条目，使清单成为
**跨 run 的耐久记录**；并注明 03.4-08 把这次调用移出续跑循环从而按构造覆盖全部退出路径、
REVIEW CR-01 又把结果与清单拆成两个值（2026-09-09 更正，quick 260909-174）。
**为什么就地改而不是追加**：这条目下方已经有两条后继条目在推翻它（一条说那条等式是「回执」
不是「检查」，一条说读者集合是从源码数出来的）。再追一条会变成同一主题三条并列，而最老的
那条仍在文件靠前处断言假话 —— 正是本任务要消灭的「一句陈述和它自己的反面并排站着」。
这一步完全可逆：一次 `git revert` 就回来了。

**站点 4（`example/acquisition.md` 核心概念的失败清单 bullet，中文）。** 只替换第二句。新句子
说：它是**跨 run 累积**的耐久记录 —— 自 03.4-08 起，写盘前会把盘上「本轮从没轮到」的旧条目
折回来（机制见下面「合并」那段）；空的 `{}` 只说明「本轮有消息的符号都干净、且盘上也没有
别的遗留条目」，反过来非空也不等于上一轮有失败。`Acquisition._FAILURE_LOG_SAMPLE = 5` 那一句
原样保留。这一句**不要**再复述整份重写的机制 —— 下面的「合并」段已经把它讲完了，而正是
「重写机制」孤零零站在核心概念开头，才让读者推出了那个错误结论。

**站点 5（`example/acquisition.md` 走查段里 `{}` 那份实测输出下面的一句，中文）。** 贴出来的
JSON `{}` **一个字节都不能动** —— `example/README.md` 规定这个目录里的输出都是真跑出来的。
只改解读那一句：空的 `{}` 的意思是「本轮有消息的符号都干净、盘上也没有别的遗留条目」，
不是「还没跑过」。语气与内容参照同文件已经改对的「常见坑 5」。

四处都遵守 Task 1 第七步的两条规矩：行为描述从当前源码重新推导（不从兄弟文档继承）、
禁止复述被退役的句子。`example/` 两处用中文写，`.planning/` 两个文件用英文写。
  </action>
  <verify>
    <automated>cd /Users/daizhaorong/projects/quantlab; D=".planning/quick/260909-174-fix-six-stale-documentation-and-docstrin"; uv run python "$D/manifest_semantics_scan.py" | grep -Fxv -f "$D/scan-allowlist.txt" | sed -E 's/\|.*//' | sort -u; echo "--- reverse ---"; uv run python "$D/manifest_semantics_scan.py" | grep -Fxv -f - "$D/scan-allowlist.txt"</automated>
  </verify>
  <done>
第一段输出恰好是四行，即只剩 Task 3 的 Python 面：`quantlab/base/acquisition.py`、
`tests/test_acquisition_progress.py`、`tests/test_atomic_sidecars.py`、
`tests/test_tiingo_quota.py`；`.planning/` 与 `example/` 全部出清。

`--- reverse ---` 之后**空**，说明 `example/acquisition.md|P07|1` 这一格落在了正确的一侧：
站点 4 那一处已消失、「常见坑 5」那一处仍在。少改会剩 2、多改会剩 0，两种都会在这里现形。

`git diff --stat` 只显示这三个文件；`cal.py`/`pyproject.toml`/`test.py`/`uv.lock` 未被暂存。
  </done>
</task>

<task type="auto">
  <name>Task 3: 从根上拆掉引用链 —— 站点 2 的 docstring 与引用它的五处，并证明 prose-only</name>
  <files>quantlab/base/acquisition.py, tests/test_acquisition_progress.py, tests/test_tiingo_quota.py, tests/test_atomic_sidecars.py</files>
  <read_first>
    - `grep -n "def _write_failure_manifest" quantlab/base/acquisition.py`，整个 docstring
      读完（三段）。
    - `grep -n "MERGED, not replaced" quantlab/base/acquisition.py`（`_run` 里逐 pass 合并
      上方的注释）与 `grep -n "UNCONDITIONAL, and outside" quantlab/base/acquisition.py`
      （写盘前那次无条件合并上方的注释）。
    - `grep -n "Why this exists at all" quantlab/base/acquisition.py`
      （`_merge_unattempted_failures` docstring 的第二段）。
    - `grep -n "own docstring" tests/test_acquisition_progress.py tests/test_tiingo_quota.py tests/test_atomic_sidecars.py`
      与 `grep -n "empty dict" tests/test_acquisition_progress.py`。
    - `sed -n '/manifest = dict(failures)/,/self._write_failure_manifest(manifest)/p' quantlab/base/acquisition.py`
      —— 这段是新表述的**事实来源**，先看代码再动笔。
  </read_first>
  <action>
先重跑两条枚举并把输出记进 SUMMARY，新写下的每一句行为描述都按它们写，不从任何兄弟文档
继承：`grep -rn "read_failure_manifest()" quantlab/`（读者集合），以及
`grep -rn "_merge_unattempted_failures(" quantlab/ tests/`（合并的调用点 —— 期望是
`quantlab/base/acquisition.py` 里一处定义加一处调用，且那处调用位于 `_run` 顶层语句、
不在任何 `if`/`while`/`for`/`try` 之内）。

**站点 2 —— `_write_failure_manifest` 的 docstring。这是引用链的根，必须先写它。**
新 docstring 要说清四件事：

1. 这个文件每次都被整份重写，**但** `_run` 在调用本方法之前已经把盘上「本轮没有消息」的
   条目合并进了它要写的那份副本（`_merge_unattempted_failures`，自 03.4-08 起无条件、
   位于续跑循环之外、紧邻本调用）。所以盘上这个文件是**跨 run 的耐久记录** ——「当前已知
   仍在失败的全部符号」，不是某一轮的快照。
2. 因此空的 `{}` 的意思是：本轮有消息的符号都干净，**且**盘上也没有别的遗留条目；反过来，
   非空也不意味着上一轮有失败 —— 忙 store 上大多数条目是折回来的
   （`_merge_unattempted_failures` 的 docstring 已经把这条后果写全了，指过去即可，不要重述）。
3. 结果对象是**另一个值**：`_run` 传进来的是 `manifest = dict(failures)` 的副本，
   `AcquisitionResult.failures` 拿的是本轮自己的 `failures`（REVIEW CR-01）。成立的关系是
   结果的失败集合**包含于**这个文件的键集合、共有 key 上消息一致。
4. 保留原有的追溯标记，但挂到还成立的东西上：`(T-0iy-07, as amended by 03.4-08 and
   REVIEW CR-01)`。并指向真正钉住运维保护的那个测试
   `tests/test_acquisition_progress.py::test_the_manifest_survives_a_quota_abort_on_the_default_path`。

第二段（跨 pass 累积、不被每一遍重新赋值、`(WR-03)`）的**主张保留**，只把段首那句依赖旧
框架的措辞改掉 —— 这一段讲的是**一次 run 内部多个 pass** 之间的关系，与「这个文件描述哪一轮」
无关。第三段（原子写、`(D-20)`）保留，只把结尾那个「与某句已退役的陈述同类」的从句换成直接
的危害：一份被截断、比记录实际持有的符号更少的清单，会**静默删掉耐久条目**，而这正是 WR-03
要防的同一种抹除。

**站点 7-8 —— `_run` 里的两处注释。** 一处在逐 pass 合并上方（"MERGED, not replaced"），
一处在写盘前那次无条件合并上方。两处现在都靠「本方法的 docstring 把空清单称作某句话」来定义
危害。改成用自己的话说危害：一次没轮到旧失败符号的 run 把清单整份写成 `{}`，会抹掉运维手上
唯一一份「这些符号还在失败」的记录。两处的其余内容（合并位置为什么在循环外、
`attempted` 为什么从 `failures` 而不是 `manifest` 推导、成功符号为什么在同一次更新里被丢弃、
被 abort 跳过的符号为什么不动）全部原样保留。

**站点 9 —— `_merge_unattempted_failures` docstring 的 "Why this exists at all." 段。**
同样把「本方法之所以必要，是因为另一处 docstring 把空清单称作某句话」改成直接陈述危害。
这一段其余部分（历史、五条出口、两种折回情形、Stale entries 的知情接受、
merge-rather-than-skip 的取舍、`attempted` 的含义）**一字不动**。

**站点 10-12 —— 三个测试文件。只改 docstring 与断言的失败消息文本，不碰任何断言本身。**
`tests/test_acquisition_progress.py` 三处：`test_result_and_manifest_agree` docstring 里
Cancelled 那一条、"overwrite the manifest with an empty dict ..." 那条断言消息、以及
"per `_write_failure_manifest`'s own docstring ..." 那条断言消息 —— 三处一律改为直接说
危害（抹掉上一轮记录在盘上的 404，那是运维唯一的一份），并保留各自的括注
（`RESEARCH Pitfall 4` 等）。`tests/test_tiingo_quota.py` 里
`test_a_later_aborting_pass_does_not_erase_an_earlier_passs_failures` 的 docstring 同理，
WR-03 的编号与「只影响上报、符号仍会被重试」的括注保留。
`tests/test_atomic_sidecars.py` 里那段解释为什么连空清单也要断言：把理由换成 —— 空的 `{}`
本身就是一个有意义的盘上状态（「当前没有任何已知仍在失败的符号」），所以它必须真的落成一个
文件，而不能被优化成「文件不存在」；两种状态必须可区分。这个测试原本的行为主张不变。

**三个硬性约束。** (1) 禁止复述被退役的句子（Task 1 第七步），哪怕加「以前说过」的标签；
(2) 新写的任何一行都**不得**出现「等号加 `_failures.json` 字面量」的形状 ——
`tests/test_source_inspector.py` 会用正则扫 `quantlab/base/acquisition.py` 的**整份源码**
找这个字面量的重复声明，一行 docstring 也会被算进去；(3) 不增删任何可执行结构，
包括 `Parallel(` 的出现次数（`tests/test_acquisition_progress.py` 用 AST 数它，而它自己的
docstring 记着「按子串数会得到 3」这件事）。

最后按顺序跑三件事并把输出记进 SUMMARY：`prose_only_check.py` 对四个 `.py` 文件（含
Task 1 未改的那三个测试文件也一并传入，用 `$(git rev-parse --git-dir)/quick-260909-174-base`
里的 SHA）；**证明这条 gate 能红** —— 用 `b1e6d31^` 当 base 再跑一次
`quantlab/base/acquisition.py`（那是 03.4-08 合并重定位之前的树，结构确有不同），必须以 1
退出并点名该文件（这一步不碰工作区，遵循本仓库「a lock that passes on arrival is
mutation-verified rather than accepted」的规矩）；然后是全量 `uv run pytest -q`。
  </action>
  <verify>
    <automated>cd /Users/daizhaorong/projects/quantlab; D=".planning/quick/260909-174-fix-six-stale-documentation-and-docstrin"; GD="$(git rev-parse --git-dir)"; test -s "$GD/quick-260909-174-base" || { echo "FAIL: base SHA file missing"; exit 1; }; B="$(cat "$GD/quick-260909-174-base")"; uv run python "$D/prose_only_check.py" "$B" quantlab/base/acquisition.py tests/test_acquisition_progress.py tests/test_tiingo_quota.py tests/test_atomic_sidecars.py; echo "prose-only exit=$?"; uv run python "$D/manifest_semantics_scan.py" | grep -Fxv -f "$D/scan-allowlist.txt"; echo "forward-empty-exit=$?"; uv run python "$D/manifest_semantics_scan.py" | grep -Fxv -f - "$D/scan-allowlist.txt"; echo "reverse-empty-exit=$?"; uv run pytest -q</automated>
  </verify>
  <done>
`prose-only exit=0`（四个 `.py` 文件相对基线 SHA 只改了字符串字面量与注释）；
`forward-empty-exit=1` 且其上无输出（扫描里没有任何 allowlist 之外的行）；
`reverse-empty-exit=1` 且其上无输出（allowlist 的七行一行不少地仍在扫描里）——
两个方向都空，gate 全绿。`uv run pytest -q` 报 **734 passed, 0 failed**。

另外，`prose_only_check.py` 以 `b1e6d31^` 为 base 跑 `quantlab/base/acquisition.py` 时
**以 1 退出并点名该文件**，输出已记进 SUMMARY —— 这条 gate 被证明能红，不是到达即绿。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 本仓库 -> 仓库外 `quantlab-console` | `.planning/ROADMAP.md` 是 console 照着写断言的契约文本；跨仓库、无编译期检查 |
| 文档/docstring -> 未来的读者与执行者 | 一句已作废的陈述会被当作现行契约照做，且会被下一份文档继承 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-174-01 | Repudiation | `.planning/ROADMAP.md` 的 D-18 段 | high | mitigate | 契约文件当前陈述一条**可执行地为假**的不变式，console 若照写会得到一个永远红的断言。Task 2 用 `03.4-VALIDATION.md` 已修正的三条包含关系替换，并指名两个真正钉住保护的测试 |
| T-174-02 | Tampering | `tests/test_acquisition_progress.py` / `test_tiingo_quota.py` / `test_atomic_sidecars.py` | high | mitigate | 「只改文案」的编辑可能顺手弱化一条断言且不被察觉。`prose_only_check.py` 用 AST 证明只有字符串常量改动（并以 `b1e6d31^` 为 base 证明它能红），外加全量 734 passed 背书 |
| T-174-03 | Information disclosure | 四份 `example/*.md` 的实测输出 | low | accept | 本任务不重跑任何示例、不新增输出；贴出的 JSON 一个字节都不动（`example/README.md` 的目录约定）。风险为零，记录在案以免执行者「顺手刷新」输出 |
| T-174-04 | Denial of service | 验收扫描器自身 | medium | mitigate | 一个只覆盖多数实例的验收检查是本阶段第四次复发的直接成因。扫描器做空白折叠与字符串拼接缝合（两种断行都藏不住一句话），gate 是与预先写死的 allowlist 双向比对，Task 1 先证明它是红的 |
</threat_model>

<verification>
1. `uv run python .../manifest_semantics_scan.py` 与 `scan-allowlist.txt` 双向比对全空。
2. `uv run python .../prose_only_check.py "$BASE" <四个 .py>` 以 0 退出；同一脚本以 `b1e6d31^`
   为 base 时以 1 退出（能红的证据）。
3. `uv run pytest -q` = 734 passed, 0 failed。
4. `git diff --stat "$BASE"..HEAD` 只列出本计划 `files_modified` 里的路径；
   `cal.py`、`pyproject.toml`、`test.py`、`uv.lock` 自始至终未被暂存。
5. SUMMARY 里记着三份从源码现取的枚举输出：`read_failure_manifest()` 的调用点、
   `_merge_unattempted_failures` 的调用点、以及 `scan-before.txt` 的 22 行。
</verification>

<success_criteria>
- 十二处站点（六处 VERIFICATION 点名的 + 六处引用链）在仓库里都与 HEAD 行为一致，
  同一份文件里不再有一句陈述与它自己的反面并排存在。
- 验收是一条能红的命令，不是一次字面量 grep；它的排除面是路径级的、写在明处的，
  而 `.planning/ROADMAP.md` 与 `.planning/STATE.md` 在扫描范围之内。
- `_write_failure_manifest` 的 docstring 不再是任何其他站点定义危害的权威出处。
- 零行为变更：`.py` 改动经 AST 证明为 prose-only，全量套件 734 passed 不变。
</success_criteria>

<output>
Create `.planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/260909-174-SUMMARY.md` when done
</output>

