---
phase: quick-260907-sm2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - quantlab/__init__.py
  - quantlab/vecbt/__init__.py
  - quantlab/config/__init__.py
  - quantlab/utils/module.py
  - quantlab/utils/nautilus.py
  - quantlab/enums/data.py
  - quantlab/base/acquisition.py
  - quantlab/acquisition/
  - quantlab/base/
  - quantlab/dataset/
  - quantlab/dl_model/
  - quantlab/factor/
  - quantlab/label/
  - quantlab/ml_model/
  - quantlab/my_ops/
  - quantlab/utils/
  - quantlab/vecbt/
  - ingest_alpaca.py
  - ingest_binance_spot.py
  - ingest_tiingo.py
  - ingest_us_equity.py
  - refresh_us_equity_universe.py
  - cal.py
  - train_model.py
  - get_binance_instruments.py
  - tests/conftest.py
  - tests/test_config_paths.py
  - tests/test_data_dir_cli.py
  - tests/test_volume_guard.py
  - tests/test_extensibility_contract.py
  - tests/test_factor_hierarchy.py
  - tests/test_universe.py
  - tests/test_constituent_panel.py
  - tests/test_factor_polars.py
  - tests/test_acquisition_batching.py
  - tests/test_ticker_pattern_reconciliation.py
  - tests/test_cleaning.py
  - tests/test_dataset_hierarchy.py
  - tests/test_spot_dataset.py
  - tests/test_ingest_tiingo_universe_wiring.py
  - README.md
  - example/
autonomous: true
requirements: [NS-01, NS-02, NS-03, NS-04, NS-05, NS-06]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "NS-01: `import quantlab.base.config` (and every other layer) resolves under the single `quantlab.` prefix; the twelve former top-level package names no longer exist as importable roots."
    - "NS-02: The built wheel contains exactly one top-level directory, `quantlab`, plus its `.dist-info`. The repo-root `data/` directory -- real market data -- is absent from it, and so is every other non-package directory setuptools' flat-layout discovery was reaching for."
    - "NS-03: Zero residual first-party imports of the bare package names remain in `quantlab/`, `tests/`, `scripts/`, `example/` or the repo-root scripts. `.planning/` is a historical archive and is NOT rewritten."
    - "NS-04: `uv run pytest tests/ -q` reports 537 passed, 0 failed, 0 errors -- the same count as before the move. No test passes because its guard stopped looking at anything."
    - "NS-04a: `get_data_root()` with neither override nor env set still resolves to the REPO-root `data/`, not to `quantlab/data/` -- and the two tests that assert this derive the repo root independently, so they cannot agree on a wrong answer."
    - "NS-04b: The volume guard's structural arm still goes RED for all four spellings of a forbidden import that the new single-package layout makes possible, including the relative ones that did not exist before. Proven by running it red, not asserted."
    - "NS-05: The `get_cls_from_path` behaviour change for previously-persisted `config.name` values is written down as a decision with its evidence, not left implicit."
    - "NS-06: In `/Users/daizhaorong/projects/quantlab-console`, `uv sync` installs `quantlab` as an editable dependency and `import quantlab` there resolves to this repo's source tree."
  artifacts:
    - quantlab/__init__.py
    - quantlab/vecbt/__init__.py
    - quantlab/config/instruments.yaml
    - pyproject.toml
    - quantlab/utils/module.py
  key_links:
    - "`quantlab/config/__init__.py:get_data_root()` -> `tests/test_data_dir_cli.py:REPO_ROOT` AND `tests/test_config_paths.py:_REPO_ROOT`. The second one derives the root from `config.__file__`, so it moves WITH the bug and would keep passing; only the first, derived from `tests/__file__`, is an independent witness. Both must be correct, and they must not share a derivation."
    - "`tests/test_data_dir_cli.py:_path_consuming_names()` -> the root scripts' `from quantlab.config import ...` module prefix. The function derives its deny set by matching an import module name; a stale prefix empties the set and the guard asserts nothing (it has a built-in tripwire for exactly this, which must be seen to hold)."
    - "`tests/test_volume_guard.py`'s structural arm -> `quantlab/acquisition/universe.py`'s imports. Namespacing newly enables relative spellings (`from ..base.acquisition import X`, `from ..base import acquisition`) that a substring scan over source text cannot see."
    - "Residual top-level package directories (left by `git mv`, holding only a bytecode cache) -> `tests/conftest.py`'s `find_spec` probe. A surviving directory resolves as an implicit namespace package, so the probe returns None instead of raising and the acquisition-client patch it gates is skipped in silence. Verified failure mode, not a supposition."
    - "`quantlab/utils/module.py:get_cls_from_path()` -> `import_path` on `BaseDataset`/`Factor`/`BaseModel`/`Acquisition` -> the `config.name` string persisted into saved configs."
    - "`[tool.setuptools.packages.find] include` -> the repo-root `data/` directory. This is the only thing standing between a `uv build` and a wheel full of market data."
    - "`quantlab/utils/nautilus.py:_load_instrument_config()` -> `quantlab/config/instruments.yaml`, resolved `__file__`-relative through `..`. The yaml moves with its package, so the relative hop stays correct by construction -- but that is a claim to verify, not to assume."
---

<objective>
Move this repo's twelve flat top-level packages under a single `quantlab/`
package so `import quantlab.base.config` works, `uv` can install the project,
and the new sibling repo `quantlab-console` can consume it as an editable path
dependency.

Purpose: `uv sync` in `/Users/daizhaorong/projects/quantlab-console` fails
outright today -- setuptools' flat-layout discovery finds twelve top-level
packages and refuses. Behind that is a second problem: names like `base`,
`config`, `data`, `dataset` and `utils` are generic enough that installing them
into a shared environment makes `import config` ambiguous. Exactly one consumer
exists right now, so namespacing is at its cheapest.

Output: a `quantlab/` package containing all twelve former roots, a
`pyproject.toml` that declares a build backend and an explicit package set,
every first-party import rewritten, every structural test guard re-rooted and
re-proven, and a green `uv sync` in the console repo.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@pyproject.toml
@utils/module.py
@tests/test_config_paths.py
@tests/test_data_dir_cli.py
@tests/test_volume_guard.py
</context>

<scope_observed_at_planning_time>
Everything below was enumerated live against the working tree, not taken from
the task description. Re-derive it rather than trusting these counts if the
tree has moved on.

**The twelve packages** (tracked-file counts): `acquisition` 4, `base` 11,
`config` 2, `dataset` 7, `dl_model` 4, `enums` 3, `factor` 4, `label` 2,
`ml_model` 2, `my_ops` 2, `utils` 8, `vecbt` 1. Total 50 tracked files.
`vecbt/` has NO `__init__.py`. `config/__init__.py` is a real 20 KB module, not
an empty marker; the other ten are empty. `config/instruments.yaml` is the only
non-`.py` file inside any of them.

**`data/` is a data directory, not a package.** It holds `data/data/` and
`data/downloads/` (reference tables and us_equity market data). It has no
`__init__.py`. `.gitignore` excludes its contents. It must not move and must
not be packaged.

**`utils/` is already invisible to setuptools.** setuptools' flat-layout finder
excludes `utils`, `tests`, `example` and `scripts` by name -- which is why the
error message the developer saw lists twelve entries but not `utils`. An
explicit package declaration is required regardless of layout.

**Import sites:** 70 `.py`/`.ipynb` files carry a first-party import
(35 under `tests/`, 8 at the repo root, 27 inside the packages).
`README.md` carries 2 and `example/*.md` carry 39 more.
`main.py`, `read_mock_data_sink.py`, `test.py`, `test_nt.ipynb` and
`scripts/download_stock_data_from_tiingo.py` carry NONE.

**All root scripts use the `from config import ...` (ImportFrom) form.** No
root script uses bare `import config`. The bare form appears only in
`tests/conftest.py:52`, `tests/test_data_dir_cli.py:21`,
`tests/test_config_paths.py:10` and, for submodules,
`tests/test_ticker_pattern_reconciliation.py:30-31`,
`tests/test_spot_dataset.py:135`, `tests/test_acquisition_batching.py:309`,
`tests/test_cleaning.py:218`, `tests/test_dataset_hierarchy.py:292`,
`tests/test_volume_guard.py:723`, `tests/test_ingest_tiingo_universe_wiring.py`
(five sites).

**String-form / structural sites** (the ones that do not move themselves):
- `tests/conftest.py:189` `"acquisition.tiingo.TiingoClient"` (with
  `raising=False`), `:544` `"acquisition.universe.requests.get"`,
  `:1013` `importlib.util.find_spec("acquisition.alpaca")`,
  `:1015` `"acquisition.alpaca._AlpacaMarketDataClient"`.
- `tests/test_universe.py` lines 78, 329, 499, 527, 565, 610, 679, 824, 965,
  1853 -- all `"acquisition.universe.requests.get"`.
- `tests/test_constituent_panel.py:393` same target.
- `tests/test_factor_polars.py:186` `"base.data.BaseDataset.head"`.
- `tests/test_volume_guard.py:740` the forbidden-literal tuple.
- `tests/test_extensibility_contract.py:32-35` `CORE_LAYER_FILES` -- CWD-relative
  paths read with `Path(rel).read_text()`.
- `tests/test_factor_hierarchy.py:55` `CONSUMER_FILE = "base/model.py"`, read at
  line 130.
- `tests/test_data_dir_cli.py:181` `REPO_ROOT / "utils" / "cli.py"`, and
  `:322-355` `_path_consuming_names()` matching `node.module.split(".")[0]`.
- `tests/test_config_paths.py:24` `_REPO_ROOT = _CONFIG_SOURCE.parent.parent`,
  consumed at lines 215 and 366.
- `config/__init__.py:85` `Path(__file__).resolve().parent.parent / "data"`.
- `utils/nautilus.py:39` `dirname(__file__), "..", "config", "instruments.yaml"`.

**Persisted-artifact census for the `import_path` decision.** `find` over the
whole repo excluding `.git`/`.venv` returns zero `*.pth`, zero `*.joblib`, and
exactly one `config.json` -- `.planning/config.json`, which is GSD's own
settings file (`model_profile`, `commit_docs`, ...), not a model config. No
`model_ckpt/` or `models/` directory exists, and `.gitignore` excludes both, so
none is in history either. There is nothing to migrate.

**Console repo state.** `/Users/daizhaorong/projects/quantlab-console` has a
`uv.lock` that already resolved `quantlab` as
`source = { editable = "../quantlab" }` -- resolution reads metadata and
succeeds. Its `.venv/lib/python*/site-packages/` contains NO `quantlab`: the
failure is at BUILD/INSTALL time, which is the gate that must flip.

**Baseline:** `uv run pytest tests/ -q --collect-only` reports 537 tests.
</scope_observed_at_planning_time>

<tasks>

<task type="tracer">
  <name>Task 1: Move the twelve packages under quantlab/, rewrite every first-party import, and declare the package to setuptools</name>
  <files>pyproject.toml, quantlab/__init__.py, quantlab/vecbt/__init__.py, quantlab/config/__init__.py, quantlab/** (all 50 moved files), ingest_alpaca.py, ingest_binance_spot.py, ingest_tiingo.py, ingest_us_equity.py, refresh_us_equity_universe.py, cal.py, train_model.py, get_binance_instruments.py, tests/**/*.py</files>
  <reversibility rating="costly">A rename of 50 tracked files plus an import rewrite across 70 more. Fully revertible with git, but it invalidates any in-flight branch or working-tree diff that touches the old paths, and the working tree currently carries a modified `test.py`.</reversibility>
  <action>
Re-enumerate the twelve package directories and the import sites yourself before
touching anything; the census in `scope_observed_at_planning_time` is a
planning-time snapshot, not authority.

Create `quantlab/` and `git mv` each of `acquisition`, `base`, `config`,
`dataset`, `dl_model`, `enums`, `factor`, `label`, `ml_model`, `my_ops`,
`utils`, `vecbt` into it. Use `git mv` on the directory so the rename is visible
as a rename in history. `git mv` moves only tracked files, so each source
directory will be left behind holding an untracked `__pycache__`; remove those
leftovers, and sweep every `__pycache__` in the tree afterwards. This is not
tidiness. A leftover top-level directory holding nothing but a bytecode cache
still satisfies Python as an implicit namespace package under the old name, and
that is what turns Task 2's `find_spec` probe from a raise into a None -- the
probe is guarded by an is-not-None branch, so the acquisition-client patch it
gates would be skipped in silence and the test would reach a real vendor. The
residual-directory check in this task's verify is the gate for that.

Add an empty `quantlab/__init__.py`. Add an empty `quantlab/vecbt/__init__.py`:
`vecbt` has never had one, and the package declaration below uses
`find_packages` semantics, which will silently skip any directory without one.

Rewrite every first-party import to the `quantlab.` prefix across
`quantlab/**`, `tests/**`, the repo-root scripts, `scripts/` and any notebook.
PRESERVE THE IMPORT FORM, changing only the module path:
`from config import X` becomes `from quantlab.config import X`;
`import config` becomes `import quantlab.config as config`;
`import utils.cli as cli` becomes `import quantlab.utils.cli as cli`;
`import acquisition` becomes `import quantlab.acquisition as acquisition`.
Keeping the bound name identical is what keeps the ~200 downstream call sites
untouched, and it keeps `acquisition.__path__` / `acquisition.__name__` working
in the pkgutil walk at `tests/test_acquisition_batching.py:309-315`.
Do NOT rewrite anything under `.planning/` -- those are historical records of
what the code looked like at the time, and editing them falsifies the archive.

Do NOT introduce relative imports while doing this. Absolute `quantlab.`-prefixed
imports everywhere keeps one spelling in the codebase and keeps the structural
guards in Task 2 reading one grammar.

Fix the storage-root depth in `quantlab/config/__init__.py:get_data_root()`.
It resolves the default root as `Path(__file__).resolve().parent.parent /
"data"`, which meant repo-root `data/` when the file was `config/__init__.py`
and now means `quantlab/data/` -- a directory that does not exist. Every
acquisition run would report one root and write to another, silently. Add the
one extra `.parent` hop, and leave the override and env-var branches alone.

Verify by reasoning AND by execution that
`quantlab/utils/nautilus.py:_load_instrument_config()` still finds the yaml: it
joins `dirname(__file__)` with `..` and `config/instruments.yaml`, and since
`config/` moved alongside `utils/` the hop still lands. This one is correct by
construction, which is precisely the kind of claim that deserves a runtime
check rather than a nod.

In `pyproject.toml`, add a `[build-system]` table naming
`setuptools.build_meta` with a `setuptools` requirement, and declare the
package set explicitly with a `[tool.setuptools.packages.find]` table:
`where` the repo root, `include` restricted to the `quantlab` prefix with a
wildcard, and `namespaces` disabled. The `include` filter is what makes it
impossible for the repo-root data directory to be swept into a distribution,
and disabling namespaces is what makes the `__init__.py` requirement
load-bearing rather than decorative. Add a `[tool.setuptools.package-data]`
entry mapping the `quantlab.config` package to its yaml, because setuptools
ships no non-Python file inside a package without being told to. Leave
`[project]`, `[dependency-groups]` and `[tool.pytest.ini_options]` unchanged --
`pythonpath` stays as it is, and `tests/` stays at the repo root.

Adding setuptools as an explicit build requirement is not a new dependency: it
is already the implicit build backend for this project, which is why the error
the developer hit is setuptools' own flat-layout message. This task makes the
existing choice explicit rather than introducing a package.
  </action>
  <verify>
    <automated>
uv run python -c "import quantlab, quantlab.base.config, quantlab.base.model, quantlab.acquisition.universe, quantlab.utils.module, quantlab.utils.nautilus, quantlab.config, quantlab.dataset.stock, quantlab.factor.alpha101, quantlab.vecbt.bt; print('IMPORTS OK', quantlab.__file__)" \
&& uv run python -c "
import os, pathlib
os.environ.pop('QUANTLAB_DATA_DIR', None)
from quantlab import config
config.set_data_root(None)
expected = pathlib.Path.cwd().resolve() / 'data'
got = config.get_data_root().resolve()
assert got == expected, ('data root moved with the package', got, expected)
assert expected.is_dir(), expected
print('DATA ROOT OK', got)
" \
&& uv run python -c "
from quantlab.utils.nautilus import _load_instrument_config
import quantlab.utils.nautilus as n, pathlib
p = pathlib.Path(n.__file__).parent.parent / 'config' / 'instruments.yaml'
assert p.is_file(), p
print('INSTRUMENTS YAML OK', p)
" \
&& test -z "$(grep -rInE '^[[:space:]]*(from|import)[[:space:]]+(acquisition|base|config|dataset|dl_model|enums|factor|label|ml_model|my_ops|utils|vecbt)([.[:space:]]|$)' --include='*.py' --include='*.ipynb' quantlab tests scripts ./*.py ./*.ipynb 2>/dev/null | grep -v '__pycache__')" \
&& test -z "$(for p in acquisition base config dataset dl_model enums factor label ml_model my_ops utils vecbt; do [ -e "$p" ] && echo "RESIDUAL $p"; done)" \
&& W="$(mktemp -d)" && uv build --wheel --out-dir "$W" && uv run python -c "
import glob, zipfile
names = zipfile.ZipFile(sorted(glob.glob('$W/quantlab-*.whl'))[-1]).namelist()
tops = {n.split('/')[0] for n in names}
extra = sorted(t for t in tops if t != 'quantlab' and not t.endswith('.dist-info'))
assert not extra, ('non-quantlab top level in wheel', extra)
for required in ('quantlab/__init__.py','quantlab/utils/module.py','quantlab/utils/cli.py','quantlab/vecbt/__init__.py','quantlab/vecbt/bt.py','quantlab/config/__init__.py','quantlab/config/instruments.yaml'):
    assert required in names, ('missing from wheel', required)
print('WHEEL OK', len(names))
"
    </automated>
  </verify>
  <done>
Every layer imports under `quantlab.`; the twelve former roots no longer exist
as directories; `get_data_root()` still resolves the repo-root data directory
and that directory exists; the instruments yaml is still reachable through the
`__file__`-relative hop; the residual-import sweep over `quantlab/`, `tests/`,
`scripts/`, root scripts and notebooks returns nothing; a built wheel contains
exactly one top-level package and carries the three files most likely to be
silently dropped (the package-data yaml, the formerly `__init__`-less `vecbt`,
and the `utils` package setuptools' flat layout excludes by name).
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-root and re-prove every structural test guard, then hold the suite at 537</name>
  <files>tests/conftest.py, tests/test_universe.py, tests/test_constituent_panel.py, tests/test_factor_polars.py, tests/test_volume_guard.py, tests/test_extensibility_contract.py, tests/test_factor_hierarchy.py, tests/test_config_paths.py, tests/test_data_dir_cli.py, tests/test_acquisition_batching.py, tests/test_ticker_pattern_reconciliation.py</files>
  <action>
This task exists because Task 1's rewrite cannot reach a module path that is
written as a string. Sweep for all of them rather than working from a list;
search the test tree for quoted dotted paths whose first segment is one of the
twelve package names, for CWD-relative or `REPO_ROOT`-relative paths naming a
package directory, and for every `read_text`, `inspect.getsource`,
`inspect.getfile`, `ast.parse`, `find_spec` and `pkgutil` call site.

Rewrite the monkeypatch string targets and the `find_spec` probe to the
`quantlab.` prefix. Measured failure modes, so you know which of these the
suite catches for you and which it does not: a monkeypatch string target whose
MODULE no longer exists raises at fixture setup even with `raising=False` --
that flag only suppresses the missing-ATTRIBUTE arm -- so those sites fail
loudly and the 537 gate catches them. The `find_spec` probe in `conftest` is
the one that can go quiet, and only in combination with Task 1's cleanup: if a
residual root-level package directory survives holding nothing but a bytecode
cache, the old name resolves as an implicit namespace package and `find_spec`
on its submodule returns None rather than raising. The probe is guarded by an
is-not-None branch, so the Alpaca client patch would simply be skipped and any
test relying on it would reach the real client. Confirm Task 1's residual-
directory sweep came back empty before trusting this one.

Re-root the hardcoded relative source paths: the core-layer file tuple in
`test_extensibility_contract.py`, the consumer-file constant in
`test_factor_hierarchy.py`, and the `utils/cli.py` path in
`test_data_dir_cli.py` all now live one directory deeper.

Fix the deny-set derivation in `test_data_dir_cli.py:_path_consuming_names()`.
It selects imports by taking the first dot-separated segment of the import's
module name and comparing it to the config package's old top-level name; after
the rewrite that first segment is the umbrella package for every first-party
import, so the comparison matches either nothing or everything. Match the FULL
dotted module name of the config package by equality instead of matching a
prefix segment, so a sibling package cannot fall into the deny set. The test
already carries a tripwire that fails loudly when the deny set matches no call
in a script's main block; confirm that tripwire is silent for all five scripts
for the right reason -- because the set is populated -- rather than assuming.

Fix the repo-root derivation in `test_config_paths.py`. It derives its root
from the config module's own `__file__` by walking up two levels, which is the
SAME expression the storage-root default uses. After the move both are wrong by
the same directory, so this test agrees with the bug and stays green -- it is
the one guard in the suite that will pass for the wrong reason. Re-derive it
from this test file's own location, the way `test_data_dir_cli.py` does, so the
two assertions on the default storage root come from two independent witnesses.
Prove the new derivation actually bites: temporarily restore the shallower
`.parent` walk in the storage-root default, confirm BOTH tests fail, then
restore.

Replace the structural arm of the volume guard's "no acquisition client can be
constructed here" test with an AST-based import check. Today it reads the
universe module's source and asserts three dotted literals are absent as
substrings. Under the old flat layout that was airtight, because every import
of those modules had to be spelled absolutely. Now that all twelve packages
share one parent, a relative import can reach an acquisition client without
producing any of those substrings -- and a naive re-prefixing of the literals
makes it worse, because a prefixed literal no longer matches the relative
spelling at all. Parse the universe module with `ast`, resolve each `Import`
and `ImportFrom` -- including the relative ones, using the node's level against
the module's own package -- to a fully qualified dotted name, and assert that
none of them is an acquisition-client module or the acquisition base module.
Leave the test's other three arms untouched: the socket ban, the credential
removal, and the scan of the module's namespace for bound `Acquisition`
subclasses. They answer different questions and all four together are the
property.

Now RED-prove that guard rather than assuming it. For each of the four
spellings the new layout admits -- an absolute import of the acquisition base
module, a relative import of the same reaching up through the parent package, a
relative import that binds the submodule by name from the parent package
instead of by dotted path, and an absolute import of a concrete vendor client --
insert that one line at the top of the universe module, run only that test,
require a NON-ZERO exit, and restore the file. A spelling that leaves the test
green is a hole; fix the resolver and re-run all four. Finish with a clean
`git diff` on the universe module so no probe survives.

Where a comment in the moved code names a module by its old file path, update
the path. The literals those comments quote are dotted MODULE names, and the
guard that reads them is now an AST check over import nodes, so a comment
cannot satisfy or defeat it either way -- update prose for accuracy without
quoting a retired name beside its replacement. One constraint still binds:
`test_ticker_pattern_reconciliation.py` scans the acquisition base module's
source for uncommented regex compilation, excluding lines whose stripped form
begins with a hash, so any comment you touch there must keep starting with one.
  </action>
  <verify>
    <automated>
U=quantlab/acquisition/universe.py; T='tests/test_volume_guard.py::test_the_guard_constructs_no_acquisition_client_and_needs_no_credentials'; FAILED=0
for SPELL in 'from quantlab.base.acquisition import Acquisition' 'from ..base.acquisition import Acquisition' 'from ..base import acquisition' 'import quantlab.acquisition.tiingo'; do
  cp "$U" "$U.redbak"
  { printf '%s\n' "$SPELL"; cat "$U.redbak"; } > "$U"
  if uv run pytest "$T" -q >/dev/null 2>&1; then echo "GUARD STAYED GREEN FOR: $SPELL"; FAILED=1; fi
  mv "$U.redbak" "$U"
done
test "$FAILED" -eq 0 && git diff --quiet -- "$U" && echo 'RED PROOF OK 4/4' \
&& X="$(mktemp -d)/junit.xml" && uv run pytest tests/ -q --junit-xml="$X" && uv run python -c "
import sys, xml.etree.ElementTree as ET
root = ET.parse(sys.argv[1]).getroot()
suite = root.find('testsuite')
suite = root if suite is None else suite
tests = int(suite.get('tests')); fails = int(suite.get('failures')); errs = int(suite.get('errors')); skips = int(suite.get('skipped'))
assert (fails, errs) == (0, 0), ('failures/errors', fails, errs)
assert tests - skips == 537, ('passed count drifted from the 537 baseline', tests, skips)
print('SUITE OK', tests, 'collected,', skips, 'skipped')
" "$X"
    </automated>
  </verify>
  <done>
All four forbidden-import spellings drive the volume guard's structural arm RED
and the universe module is left byte-identical afterwards; the two storage-root
assertions derive the repo root independently and both fail when the depth bug
is reintroduced; `uv run pytest tests/ -q` exits zero with 537 passing, zero
failures and zero errors.
  </done>
</task>

<task type="auto">
  <name>Task 3: Record the persisted-config break, update the docs, and prove the console install</name>
  <files>quantlab/utils/module.py, README.md, example/*.md</files>
  <precondition>The sibling repository `/Users/daizhaorong/projects/quantlab-console` exists at that path and its `pyproject.toml` declares `[tool.uv.sources] quantlab = { path = "../quantlab", editable = true }`. Halt and report if it does not -- the acceptance gate is unverifiable without it.</precondition>
  <reversibility rating="reversible">Choosing the hard break now does not foreclose adding a compatibility mapping later; the mapping can be introduced the first time an artifact needs it.</reversibility>
  <action>
Record the decision on persisted `config.name` values in the docstring of
`get_cls_from_path` in `quantlab/utils/module.py` (and, if the module has one,
its module docstring). The four base classes each expose an `import_path`
property built from the class's own module and qualified name; that string is
assigned to `config.name` in every config setter and serialized into the JSON
saved beside a model checkpoint, and `get_cls_from_path` reconstructs the
object by importing that dotted path. Namespacing changes every one of those
strings, so a config persisted before this change no longer resolves.

Take the hard break rather than adding a legacy alias table, and write the
EVIDENCE for that choice into the docstring, not just the choice: a search of
the whole repository excluding version-control and virtualenv directories
returns zero model checkpoints of either persistence format, and the only
`config.json` in the tree belongs to the planning tooling, not to a model. The
two directories such artifacts would live in do not exist and are excluded from
version control, so none is recoverable from history either. There is nothing
to migrate, and an alias table would be permanent maintenance owed to a set
that is empty. Note that the mapping remains available if an artifact ever
turns up. Anchor the record with the identifier `260907-sm2` so the decision is
traceable back to this task.

Rewrite the first-party imports inside the fenced code blocks of `README.md`
and `example/*.md` to the `quantlab.` prefix, using the same form-preserving
rule as Task 1. These files are this repo's documented API surface -- an
example a reader copies must run. Leave `.planning/` untouched.

Then prove the whole point of the task: run `uv sync` in the console repo and
confirm `quantlab` actually lands in its environment, resolving to this repo's
source tree rather than to a copied build. Drive it with `uv --directory`
rather than changing shell directory.
  </action>
  <verify>
    <automated>
uv run python -c "
from quantlab.utils import module
doc = (module.get_cls_from_path.__doc__ or '') + (module.__doc__ or '')
assert '260907-sm2' in doc, 'the persisted-config decision is not recorded against this task'
assert len(doc) > 300, ('the record states a choice without its evidence', len(doc))
print('DECISION RECORDED', len(doc), 'chars')
" \
&& test -z "$(grep -rInE '^[[:space:]]*(from|import)[[:space:]]+(acquisition|base|config|dataset|dl_model|enums|factor|label|ml_model|my_ops|utils|vecbt)([.[:space:]]|$)' README.md example/*.md)" \
&& uv --directory /Users/daizhaorong/projects/quantlab-console sync \
&& uv --directory /Users/daizhaorong/projects/quantlab-console run python -c "
import pathlib, quantlab, quantlab.base.config, quantlab.acquisition.universe
p = pathlib.Path(quantlab.__file__).resolve()
expected = pathlib.Path('/Users/daizhaorong/projects/quantlab/quantlab/__init__.py')
assert p == expected, ('console resolved a different quantlab', p, expected)
print('CONSOLE EDITABLE OK', p)
"
    </automated>
  </verify>
  <done>
`get_cls_from_path`'s docstring carries the break, its evidence and this task's
identifier; no stale first-party import remains in `README.md` or `example/*.md`;
`uv sync` in the console repo succeeds and `import quantlab` there resolves to
`/Users/daizhaorong/projects/quantlab/quantlab/__init__.py`, proving the
editable path dependency installs.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo source tree -> built distribution | Whatever the package declaration admits is shipped. Repo-root `data/` holds real market data. |
| test process -> vendor HTTP APIs | Crossed only through monkeypatch string targets. A target that patches nothing lets a test reach a real vendor with real credentials. |
| quantlab -> quantlab-console | One-way editable path dependency. The console consumes `quantlab.*`; quantlab never imports the console. |
| persisted `config.name` -> `importlib.import_module` | A dotted path read from disk is imported. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-sm2-01 | Information disclosure | wheel build / `[tool.setuptools.packages.find]` | high | mitigate | `include` restricted to the `quantlab` prefix and `namespaces` disabled, so the repo-root data directory cannot be discovered; Task 1's verify builds a wheel and asserts its top-level set is exactly the package plus `.dist-info`. |
| T-sm2-02 | Spoofing | `tests/conftest.py` `find_spec` probe gating the Alpaca client patch | high | mitigate | Measured: a stale monkeypatch string target raises loudly, but a stale `find_spec` returns None if a residual top-level directory survives as an implicit namespace package, silently skipping the patch and letting a test reach a real vendor. Task 1's verify asserts zero residual directories; Task 2 sweeps every quoted dotted path and probe; the 537 gate holds the count so a skipped fixture cannot hide behind a shrinking suite. |
| T-sm2-03 | Tampering | `tests/test_volume_guard.py` structural arm over `quantlab/acquisition/universe.py` | high | mitigate | Substring scan replaced with an AST import resolver that handles relative levels, then RED-proven against all four spellings the single-package layout admits; the universe module is left byte-identical after the probes. |
| T-sm2-04 | Tampering | `quantlab/config/__init__.py:get_data_root()` default depth | high | mitigate | Extra `.parent` hop, plus `tests/test_config_paths.py`'s repo-root derivation moved off the config module's `__file__` so the two storage-root assertions stop sharing the buggy expression; both proven to fail when the depth is reverted. |
| T-sm2-05 | Repudiation | `quantlab/utils/module.py:get_cls_from_path()` vs. previously persisted `config.name` | low | accept | Hard break, taken deliberately: a whole-repo census found zero checkpoints of either format, zero model `config.json` files, and no checkpoint directory in the tree or in version control. Recorded with its evidence and this task's identifier so the choice is auditable, and the alias table stays available if an artifact ever appears. |
| T-sm2-SC | Tampering | build-time `setuptools` requirement added to `[build-system]` | low | accept | Not a new package. setuptools is already this project's implicit build backend -- the flat-layout error that motivates the task is setuptools' own message -- so declaring it makes an in-use dependency explicit rather than introducing one. No package-manager install task is added. |
</threat_model>

<verification>
- `uv run pytest tests/ -q` exits zero: 537 passed, 0 failed, 0 errors.
- `uv run python -c "import quantlab.base.config"` succeeds; none of the twelve
  former top-level names remains importable or present as a directory.
- A built wheel's top-level set is exactly `quantlab` plus `.dist-info`, and it
  carries `quantlab/config/instruments.yaml`, `quantlab/vecbt/__init__.py` and
  `quantlab/utils/module.py`.
- With neither override nor env var set, `get_data_root()` returns the
  repo-root data directory, asserted by two tests with independent derivations.
- The volume guard's structural arm goes red for all four forbidden-import
  spellings, demonstrated by running it, not by inspection.
- `uv --directory /Users/daizhaorong/projects/quantlab-console sync` succeeds
  and `quantlab.__file__` there points into this repo's source tree.
- The residual-import sweep returns nothing for `quantlab/`, `tests/`,
  `scripts/`, the repo-root scripts and notebooks, `README.md` and `example/*.md`.
  `.planning/` is deliberately excluded.
</verification>

<success_criteria>
- NS-01 through NS-06 in `must_haves.truths` all hold.
- Root-level CLI scripts remain at the repo root; no `console_scripts` entry
  point is introduced.
- `tests/` remains at the repo root and `pythonpath` is unchanged.
- The rename appears in git history as a rename, not as delete-plus-add.
- Nothing under `.planning/` is rewritten.
</success_criteria>

<output>
Create `.planning/quick/260907-sm2-make-quantlab-an-installable-quantlab-na/260907-sm2-SUMMARY.md` when done.
</output>
