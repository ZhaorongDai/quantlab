---
phase: "03.10"
plan: "10"
type: execute
wave: 5
depends_on: ["03.10-03", "03.10-04", "03.10-08", "03.10-09"]
files_modified:
  - scripts/ingest_wrds_crsp.py
  - tests/test_ingest_wrds_crsp.py
  - quantlab/acquisition/sql_volume.py
  - quantlab/utils/cli.py
  - tests/test_sql_volume_guard.py
autonomous: true
requirements: [D-01, D-03, D-05, D-12, D-14, D-15, D-17]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "scripts/ingest_wrds_crsp.py resolves SOURCE = DataSourceRegistry.get('wrds'), builds its acquisition config through SOURCE.config_factory_for('us_equity', '1d', 'crsp_daily') and reads provider constants through SOURCE.acquisition_cls_for(...); it never names the CRSP acquisition class and calls no quantlab/config factory (D-12)"
    - "The roster is --universe crsp_sp500 | comp_nasdaq100 (CrspMembership.permnos_in_range over the window, after the reference pull) plus --permnos and --qqq (PERMNO 86755); both dates are required (D-05, D-14, D-15)"
    - "An --end-date past the CRSP product end prints `clipped end <requested> -> <product end> (crsp_a_stock annual product end)` and pulls through the product end; a --start-date past it exits non-zero naming the product end with zero daily COPY (D-01)"
    - "Entitlement (crsp_a_stock; plus crsp_a_indexes for crsp_sp500; plus comp and crsp_a_ccm for comp_nasdaq100) is checked first; the per-year count probe and SqlVolumeGuard run before run(); a refusal exits non-zero with zero daily COPY and --force-volume overrides it visibly (D-03)"
    - "The probe, the reference pull, the daily pull and the conversions share ONE WrdsSession: an end-to-end run against the fake records exactly one connection, and close_shared runs in a finally (D-03)"
    - "--to-zarr converts the equity store (default filter equity_common, --security-filter selectable, collision_universe = --universe), the QQQ benchmark store when --qqq is given, and the chosen universe's membership panel, and prints every store and sidecar path (D-12, D-15, D-17)"
    - "The printed volume estimate labels its buckets as years for CRSP; the guard's refusal text for WRDS TAQ is unchanged (D-03)"
    - "The script accepts no credential argument and prints no credential; WRDS_USERNAME comes from the environment and the password from ~/.pgpass (D-03)"
  artifacts:
    - path: "scripts/ingest_wrds_crsp.py"
      provides: "WRDS CRSP ingest CLI"
      contains: "assert_acquisition_volume_fits"
    - path: "tests/test_ingest_wrds_crsp.py"
      provides: "end-to-end CLI runs against FakeCrspSession + structural locks"
    - path: "quantlab/acquisition/sql_volume.py"
      provides: "optional unit label on estimate / assert_acquisition_volume_fits"
    - path: "quantlab/utils/cli.py"
      provides: "print_sql_volume_estimate renders the estimate's unit"
  key_links:
    - from: "scripts/ingest_wrds_crsp.py"
      to: "quantlab/acquisition/sql_volume.py::SqlVolumeGuard.assert_acquisition_volume_fits"
      via: "guard call placed after CrspVolumeProbe.count_rows_by_year and before run(SOURCE, ...)"
    - from: "scripts/ingest_wrds_crsp.py"
      to: "quantlab/acquisition/wrds_crsp_reference.py::CrspReferenceTables.pull -> quantlab/dataset/crsp_membership.py::CrspMembership.permnos_in_range"
      via: "reference tables pulled in the same session, then the roster resolved from them"
    - from: "scripts/ingest_wrds_crsp.py"
      to: "quantlab/acquisition/registry.py::convert(..., data_type='crsp_daily')"
      via: "--to-zarr"
  prohibitions:
    - requirement_id: "D-03"
      category: "safety"
      status: "resolved"
      verification: "test"
      resolution: null
      reason: null
      statement: "MUST NOT accept, print or log a WRDS username or password; MUST NOT open a second WRDS session in one run"
    - requirement_id: "D-12"
      category: "scope"
      status: "resolved"
      verification: "test"
      resolution: null
      reason: null
      statement: "MUST NOT call a quantlab/config factory function from the CRSP CLI; configs come from the provider classmethods and direct construction"
---

<objective>
Give the user one command that does the whole CRSP job safely: `scripts/ingest_wrds_crsp.py`
checks entitlement, clips the window to the annual product end, pulls the reference tables,
resolves the point-in-time roster, prices the pull per year and refuses above the volume
ceilings, pulls the daily raw tier, and optionally converts the equity panel, the QQQ
benchmark store and the universe membership panel — all on one WRDS connection (one Duo
push). It follows `scripts/ingest_wrds_taq.py` (03.9-07).

**Sequencing:** execute only after phase 03.9 is fully merged (its CLI is the analog) and
plans 03.10-03, -04, -08 and -09 are merged.

Purpose: D-12's operator surface for the new capability.

Output: the CLI, its tests, and a backward-compatible unit label on the SQL volume guard's
estimate (research OQ-4).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-CONTEXT.md
@.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-RESEARCH.md
@.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-PATTERNS.md
@.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-03-SUMMARY.md
@.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-04-SUMMARY.md
@.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-08-SUMMARY.md
@.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-09-SUMMARY.md
</context>

<environment_notes>
- Run from YOUR working-tree root: `uv run pytest -q tests/test_ingest_wrds_crsp.py`. Never `cd` into the main checkout. `-k` matching nothing exits 5 = failure. Never `git stash`.
- Drive the script in-process like `tests/test_ingest_wrds_taq.py` (03.9-07) does (runpy / `main(argv)` with `--data-dir <tmp>`), with `mock_crsp_session` active and `write_reference_tables`-style reference rows put into `FakeCrspSession.reference_rows` so the script's own reference pull serves them.
- `tests/crsp_fixtures.py` is read-only here.
</environment_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: A unit label on the SQL volume estimate (TAQ text unchanged)</name>

  <files>quantlab/acquisition/sql_volume.py, quantlab/utils/cli.py, tests/test_sql_volume_guard.py</files>

  <read_first>
    - quantlab/acquisition/sql_volume.py (whole file)
    - quantlab/utils/cli.py `print_sql_volume_estimate` (~815-857)
    - tests/test_sql_volume_guard.py (whole file: existing text assertions must stay green)
    - scripts/ingest_wrds_taq.py and tests/test_ingest_wrds_taq.py (03.9-07: the existing caller of `assert_acquisition_volume_fits` / `print_sql_volume_estimate`; its output must not change)
  </read_first>

  <behavior>
    - `SqlVolumeGuard().estimate(rows, symbols=..., start_date=..., end_date=...)` returns `unit == "trading day"` (default) and every existing test passes unchanged.
    - `estimate(..., unit="year bucket")` returns `unit == "year bucket"`; a refusal with that unit says "2 year bucket(s)" where TAQ says "2 trading day(s)".
    - `print_sql_volume_estimate` prints a line starting `  trading days:` for a TAQ estimate (unchanged) and `  year buckets:` for a CRSP one; an estimate dict without a `unit` key (older caller) prints `trading days:`.
  </behavior>

  <action>
Tests first (new tests beside the existing ones). Add a keyword-only `unit: str = "trading day"` to `SqlVolumeGuard.estimate` and `assert_acquisition_volume_fits`; store it in the estimate dict as `"unit"`; use it in the refusal message where the day count is rendered. In `print_sql_volume_estimate` render `estimate.get("unit", "trading day") + "s:"` for that line with the same column alignment. The module stays standard-library only and imports no acquisition class. Docstrings: CRSP counts per calendar-year bucket (plan 03's `CrspVolumeProbe`), so "day" is the wrong word there.
  </action>

  <verify>
    <automated>uv run pytest -q tests/test_sql_volume_guard.py</automated>
    <fails_when>non-zero exit, or any failed test in the summary line</fails_when>
    <automated>uv run pytest -q tests/test_sql_volume_guard.py tests/test_ingest_wrds_taq.py</automated>
    <fails_when>non-zero exit, or any failed/errored test in the summary line — the 03.9 callers of SqlVolumeGuard and print_sql_volume_estimate (the TAQ CLI) must stay green after the unit keyword is added</fails_when>
  </verify>

  <acceptance_criteria>
    - `uv run pytest -q tests/test_sql_volume_guard.py -k unit` exits 0 with at least 2 passed
    - `uv run pytest -q tests/test_sql_volume_guard.py` exits 0 (all pre-existing tests still pass)
    - `uv run pytest -q tests/test_sql_volume_guard.py tests/test_ingest_wrds_taq.py` exits 0 (the 03.9 TAQ CLI still renders and refuses exactly as before)
  </acceptance_criteria>

  <done>The same guard prices TAQ days and CRSP years and says which it is counting.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: scripts/ingest_wrds_crsp.py and its end-to-end runs against the fake session</name>

  <files>scripts/ingest_wrds_crsp.py, tests/test_ingest_wrds_crsp.py</files>

  <read_first>
    - scripts/ingest_wrds_taq.py (03.9-07, whole file: docstring sections, SOURCE resolution, parser, `__main__` order, error exits, session try/finally)
    - tests/test_ingest_wrds_taq.py (how the script is driven in-process and asserted)
    - quantlab/utils/cli.py: `add_window_args`, `add_volume_guard_args`, `add_data_dir_arg`, `apply_data_dir`, `add_to_zarr_arg`, `add_chunk_args(default=...)`, `refuse_conversion_without_raw_data`, `print_conversion_result`, `print_sql_volume_estimate`
    - quantlab/acquisition/wrds_crsp.py (`CrspQueries.assert_entitled`, `resolve_window`, `reference_dir_for`, `CrspVolumeProbe`), quantlab/acquisition/wrds_crsp_reference.py (`CrspReferenceTables.pull`), quantlab/dataset/crsp_membership.py (`CrspMembership.permnos_in_range`, `INDEXES`), quantlab/base/config.py (`CrspDatasetConfig`, `qqq_benchmark`, `QQQ_PERMNO`, `ConstituentDatasetConfig`), quantlab/dataset/constituent.py (the two CRSP constituent classes), quantlab/dataset/crsp.py (sidecar suffixes)
    - scripts/ingest_tiingo.py lines 225-272 (the `--to-zarr` tail)
  </read_first>

  <behavior>
    With `mock_crsp_session` (default daily and reference rows):
    - `--permnos 14593 --start-date 2020-08-01 --end-date 2020-08-31 --data-dir <tmp> --to-zarr` exits 0; stdout contains the volume estimate with a `year buckets:` line, "succeeded", the equity store path `<tmp>/data/us_equity/1d/wrds_crsp_custom_1d.zarr` and the three sidecar paths (`.crsp_adjustment.json`, `.crsp_filter_report.json`, `.crsp_symbology_report.json`); the store's symbols are ["AAPL"]; `<tmp>/downloads/us_equity/1d/wrds_crsp/_reference/manifest.json` exists; `FakeWrdsSession.connections == 1`.
    - `--universe crsp_sp500 --start-date 2020-08-01 --end-date 2020-08-31 --data-dir <tmp>` resolves the roster from the reference `dsp500list_v2` rows (["14593"] with the default rows), counts only that roster, and stops at raw ("Skipping Zarr conversion").
    - `--universe comp_nasdaq100 ...` pulls `idxcst_his` and `ccmxpf_lnkhist` and resolves the Alphabet PERMNOs ["14542", "90319"] for a 2015 window; with an unlinked synthetic spell it exits non-zero naming `--allow-unlinked-ndx`, and succeeds with that flag.
    - `--end-date 2026-06-30` prints exactly `clipped end 2026-06-30 -> 2025-12-31 (crsp_a_stock annual product end)` and the last daily COPY's upper bound is '2025-12-31'.
    - `--start-date 2026-01-05 --end-date 2026-06-30` exits non-zero naming 2025-12-31; no dsf_v2 COPY recorded.
    - `SqlVolumeGuard.MAX_RAW_ROWS` monkeypatched to 1 -> non-zero exit, message names `--force-volume`, no dsf_v2 COPY; with `--force-volume` the run proceeds and prints the forced line.
    - `usable_schemas = {"crsp_a_stock"}` with `--universe crsp_sp500` -> non-zero exit naming `crsp_a_indexes`, zero COPY of any table.
    - `WRDS_USERNAME` unset -> non-zero exit naming the variable; with a planted `WRDS_USERNAME` value a successful run's stdout/stderr never contains it.
    - `--qqq --permnos 14593 --start-date 1999-01-01 --end-date 2025-12-31 --to-zarr` writes `wrds_crsp_custom_1d.zarr` (no QQQ symbol) and `wrds_crsp_qqq_1d.zarr` (symbols ["QQQ"]).
    - `--universe crsp_sp500 ... --to-zarr` also writes `wrds_crsp_sp500_membership.zarr` with an `is_member` variable.
    - Parser errors (exit 2): no --universe/--permnos/--qqq; `--permnos AAPL` (message says PERMNO); missing `--end-date`; `--rows-per-symbol-day 10` (message says CRSP rows are counted server-side); `--universe sp500` (not a choice); `--security-filter bogus`.
  </behavior>

  <action>
Tests first (RED: the script does not exist). Then write `scripts/ingest_wrds_crsp.py`:
- Module docstring in the ingest_wrds_taq style: what it does (CRSP Stock v2 daily from `crsp_a_stock.dsf_v2`, reference tables, point-in-time roster, optional Zarr stores); Credentials (WRDS_USERNAME from env, password only in ~/.pgpass via libpq, no credential flag, nothing printed); One Duo push per run; Annual product (end clipped to the product end with a printed line; a new vintage needs a fresh raw tier); Volume (count(*) per year and PERMNO batch before the pull; 20 GiB ceiling; bytes/row 150 is an assumption until the live smoke measures it); Stores and sidecars; Usage examples (`uv run python scripts/ingest_wrds_crsp.py --universe crsp_sp500 --start-date 2000-01-01 --end-date 2025-12-31 --to-zarr`, `--permnos 14593,13407 ...`, `--qqq ...`).
- Module constants: `SOURCE = DataSourceRegistry.get("wrds")`; `CAPABILITY = ("us_equity", "1d", "crsp_daily")`; `ACQ = SOURCE.acquisition_cls_for(*CAPABILITY)`; `UNIVERSES = CrspMembership.INDEXES`; `STORE_TEMPLATE = "wrds_crsp_{name}_1d.zarr"` (name sp500 / nasdaq100 / custom), `QQQ_STORE = "wrds_crsp_qqq_1d.zarr"`, `MEMBERSHIP_TEMPLATE = "wrds_crsp_{name}_membership.zarr"`.
- Parser: `--universe` (choices UNIVERSES), `--permnos` (comma list), `--qqq`, `add_window_args(parser)`, `add_volume_guard_args(parser)`, `add_data_dir_arg(parser)`, `--batch-size` (default `ACQ.DEFAULT_BATCH_SIZE`), `--refresh`, `--refresh-reference`, `--allow-unlinked-ndx`, `--security-filter` (choices = the preset names, default `equity_common`), `add_to_zarr_arg(parser)`, `add_chunk_args(parser, default="year")`. No argument whose flag mentions user, password, passwd or pgpass.
- Validation with `parser.error`: at least one of --universe/--permnos/--qqq; every PERMNO all digits; both dates present; `--rows-per-symbol-day` given -> error saying CRSP rows are counted server-side.
- `__main__` order: parse; `apply_data_dir(args)`; validate; kwargs = {"batch_size": ..., "clip_to_product_end": True}; `session = WrdsSession.shared()`; inside `try/finally: WrdsSession.close_shared()`:
  1. `CrspQueries.assert_entitled(session, schemas)` for exactly the schemas this run needs;
  2. `start, end, clipped = ACQ.resolve_window(session, start, end, clip=True)`; when clipped print the exact clip line;
  3. `acq_config = SOURCE.config_factory_for(*CAPABILITY)(symbols=(), start_date=..., end_date=..., kwargs=kwargs)` (the ONE factory call); `reference_dir = ACQ.reference_dir_for(acq_config)`;
  4. `CrspReferenceTables(session, reference_dir).pull(product_end=..., include_sp500=..., include_nasdaq100=..., refresh=args.refresh_reference)`; print rows per table;
  5. roster = universe PERMNOs (`CrspMembership(CrspReference(reference_dir)).permnos_in_range(..., allow_unlinked=args.allow_unlinked_ndx)`) + `--permnos` + (`QQQ_PERMNO` with --qqq), de-duplicated, sorted numerically; `acq_config = dataclasses.replace(acq_config, symbols=tuple(roster))`;
  6. `counts = CrspVolumeProbe(session, batch_size=...).count_rows_by_year(roster, start, end)`; `print_sql_volume_estimate(SqlVolumeGuard(acq_config.kwargs).assert_acquisition_volume_fits(counts, symbols=len(roster), start_date=..., end_date=..., force=args.force_volume, unit="year bucket"), forced=args.force_volume)`; print the bytes/row assumption note;
  7. `result = run(SOURCE, acq_config, refresh=args.refresh)`; print succeeded/failed and the raw root;
  8. with `--to-zarr`: build the equity `CrspDatasetConfig` directly (zarr path from `STORE_TEMPLATE` under `get_data_root() / "data" / "us_equity" / "1d"`, `raw_data_dir_path=acq_config.raw_data_dir_path`, `catalog_path=str(get_data_root() / "data" / "catalog")`, `reference_dir`, the window, `permnos` = roster without the --qqq-added PERMNO, `security_filter=args.security_filter`, `collision_universe=args.universe`); `refuse_conversion_without_raw_data(CrspStockDataset(replace(ds_config, symbols=None)), result)`; `convert(SOURCE, ds_config, data_type="crsp_daily", granularity=args.chunk, on_new_listing=args.on_new_listing)`; `print_conversion_result`; print the three sidecar paths; with --qqq the same through `CrspDatasetConfig.qqq_benchmark(...)`; with --universe build the matching CRSP constituent dataset with `ConstituentDatasetConfig(zarr_file_path=..., cache_dir=str(reference_dir), start_date=..., end_date=..., kwargs={"allow_unlinked": args.allow_unlinked_ndx})`, `from_raw_data()`, `save()`, and print its path; without --to-zarr print that the run stops at raw.
- Entitlement, product-end, vintage, guard and membership errors exit non-zero with their message (the 03.9 CLI's exit idiom); the traceback is not the interface.
  </action>

  <verify>
    <automated>uv run pytest -q tests/test_ingest_wrds_crsp.py</automated>
    <fails_when>non-zero exit, or fewer than 14 passed in the summary line</fails_when>
  </verify>

  <acceptance_criteria>
    - `uv run python scripts/ingest_wrds_crsp.py --help` exits 0 and lists `--universe`, `--permnos`, `--qqq`, `--force-volume`, `--to-zarr`, `--chunk`, `--security-filter`, `--allow-unlinked-ndx`
    - `uv run pytest -q tests/test_ingest_wrds_crsp.py -k "one_connection or clip or refus or entitle or credential"` exits 0 with at least 5 passed
    - The end-to-end test asserts `FakeWrdsSession.connections == 1`
  </acceptance_criteria>

  <done>One command entitles, clips, pulls references, resolves the roster, prices, pulls and converts, on one session, and refuses before any daily data query when entitlement, the product end or the volume says no.</done>
</task>

<task type="auto">
  <name>Task 3: Structural locks on the CRSP CLI (guard placement, one session, no vendor class, no factories, no credentials)</name>

  <files>tests/test_ingest_wrds_crsp.py</files>

  <read_first>
    - tests/test_ingest_wrds_taq.py (the 03.9-07 structural tests: `_main_body`, call-line helpers) — re-implement the helpers locally, do not import from that module
    - scripts/ingest_wrds_crsp.py (Task 2)
    - quantlab/config/__init__.py (function names, read only)
  </read_first>

  <action>
Add AST/source tests over `scripts/ingest_wrds_crsp.py`, each failing with a message that names the line numbers it compared:
`test_the_crsp_guard_is_called_by_name_in_main` (an `assert_acquisition_volume_fits` call in the `__main__` body);
`test_the_crsp_guard_follows_the_probe_and_precedes_run` (guard line > `count_rows_by_year` line, guard line < every `run(` call and every `.acquisition_cls(` / `ACQ(` construction);
`test_the_reference_pull_precedes_the_roster_and_the_probe` (`.pull(` line < `permnos_in_range` line < `count_rows_by_year` line);
`test_force_volume_is_a_typed_flag_only` (`force=args.force_volume` and `add_volume_guard_args` present; no environment read of a FORCE flag);
`test_the_crsp_cli_names_no_acquisition_class_and_calls_one_factory` (the CRSP acquisition class name is absent from the source; exactly one `.config_factory_for(` call; `acquisition_cls_for` present);
`test_the_crsp_cli_calls_no_quantlab_config_factory` (no call to a function defined in `quantlab/config/__init__.py` other than `get_data_root` / `set_data_root`, found by parsing that module);
`test_apply_data_dir_precedes_every_path_derivation`;
`test_no_credential_argument` (no `add_argument` flag containing "password", "passwd", "user" or "pgpass");
`test_one_session_per_run` (exactly one `WrdsSession.shared(` call; `close_shared` inside a `finally`).
Mutation-check one lock: delete the guard call in a scratch copy (never committed) and confirm `test_the_crsp_guard_is_called_by_name_in_main` fails; record the result in the SUMMARY.
  </action>

  <verify>
    <automated>uv run pytest -q tests/test_ingest_wrds_crsp.py -k "guard or reference_pull or force or acquisition_class or factory or apply_data_dir or credential or session"</automated>
    <fails_when>non-zero exit, exit 5 with "deselected", or fewer than 9 passed in the summary line</fails_when>
  </verify>

  <acceptance_criteria>
    - The selector above reports at least 9 passed and 0 failed
    - The SUMMARY records the guard-deletion mutation result
  </acceptance_criteria>

  <done>The CLI's protective ordering, single session and no-credential/no-factory rules are structural, mutation-checked locks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator argv -> SQL / paths | PERMNOs, universes, dates and filter names from the command line |
| process env -> session | WRDS_USERNAME read from the environment |
| CLI -> WRDS | the only place a live connection is opened by this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.10-33 | Information Disclosure | CLI output / arguments | high | mitigate | no credential flag (structural test); username only from env; planted-username test on stdout/stderr |
| T-03.10-34 | Denial of Service (self) | Duo push storm / connection exhaustion | high | mitigate | one `WrdsSession.shared()` per run, `close_shared` in finally, `max_workers=1`; one-connection e2e test |
| T-03.10-35 | Denial of Service (self) | oversized pull | high | mitigate | per-year count probe + SqlVolumeGuard before `run()`; refusal exits with zero daily COPY; `--force-volume` explicit only |
| T-03.10-36 | Tampering | argv PERMNOs / dates into SQL | high | mitigate | parser validates digits and ISO dates; SQL values only via `sql.Literal` in `CrspQueries` |
| T-03.10-37 | Tampering | silent window truncation | medium | mitigate | the product-end clip is printed verbatim; a start past the product end exits non-zero |
</threat_model>

<verification>
- `uv run pytest -q tests/test_ingest_wrds_crsp.py tests/test_sql_volume_guard.py tests/test_ingest_wrds_taq.py` passes (the TAQ CLI is unaffected by the unit label).
- `uv run python scripts/ingest_wrds_crsp.py --help` exits 0.
</verification>

<success_criteria>
An operator can pull and convert CRSP daily data for a CRSP point-in-time universe with one command and one Duo push, and every protective check fires before any daily data moves.
</success_criteria>

## Artifacts this phase produces (this plan's share)
- `scripts/ingest_wrds_crsp.py` (`SOURCE`, `CAPABILITY`, `ACQ`, `UNIVERSES`, `STORE_TEMPLATE`, `QQQ_STORE`, `MEMBERSHIP_TEMPLATE`)
- `quantlab/acquisition/sql_volume.py`: `unit` keyword on `estimate` / `assert_acquisition_volume_fits`
- `quantlab/utils/cli.py`: `print_sql_volume_estimate` renders the unit
- `tests/test_ingest_wrds_crsp.py`, new unit tests in `tests/test_sql_volume_guard.py`
- On disk (runtime): `data/us_equity/1d/wrds_crsp_{sp500|nasdaq100|custom}_1d.zarr`, `wrds_crsp_qqq_1d.zarr`, `wrds_crsp_{sp500|nasdaq100}_membership.zarr` + sidecars

<output>
Create `.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-10-SUMMARY.md` when done
</output>
