---
phase: "03.10"
plan: "10"
type: execute
wave: 5
depends_on: ["03.10-09"]
files_modified:
  - example/wrds_crsp.md
  - example/README.md
  - .planning/todos/pending/2026-09-07-no-ticker-rename-mapping-between-membership-history-and-pric.md
autonomous: false
requirements: [D-01, D-04, D-05, D-08, D-12, D-13, D-14, D-15, D-17, D-18]
user_setup:
  - service: wrds
    why: "Live WRDS CRSP pull for the final smoke (one Duo push per command)"
    env_vars:
      - name: WRDS_USERNAME
        source: "Your WRDS account username; the password stays in ~/.pgpass (mode 0600, line wrds-pgdata.wharton.upenn.edu:9737:wrds:<username>:<password>)"

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "example/wrds_crsp.md (Chinese, the example/acquisition.md section shape) documents the pipeline, credentials and Duo behaviour, the annual product end / clipping / vintage rule, PERMNO-keyed raw and ticker symbology (renames, share classes, collisions, seams), the security-filter presets with the D-17 reading, the total-return adjustment and its anchor refusal, delisting returns, the QQQ store, both universes and their coverage limits, and every sidecar (D-01, D-04, D-08, D-14, D-15, D-17, D-18)"
    - "The pending ticker-rename todo records how the CRSP vendor sidesteps the problem (one PERMNO-keyed symbology for prices and universes) without closing it for the other vendors (D-04)"
    - "In the MAIN tree after every merge, the new CRSP test files pass and the full suite's failing ids are a subset of 03.9-BASELINE-FAILURES.txt (D-13)"
    - "A user-run live smoke confirms, with one Duo push per command: AAPL has 253 daily rows in 2020, a re-run skips via watermarks, the AAPL adjClose ratio 2020-08-28 -> 2020-08-31 is about 1.033912, FB ends 2022-06-08 and META starts 2022-06-09, the S&P and Nasdaq-100 rosters resolve, the clip line prints for a 2026 end, and a 2026 start is refused (D-01, D-04, D-05, D-08, D-12, D-14, D-15)"
  artifacts:
    - path: "example/wrds_crsp.md"
      provides: "CRSP vendor contract doc"
      contains: "crsp_a_stock"
  key_links:
    - from: "scripts/ingest_wrds_crsp.py"
      to: "WRDS PostgreSQL (crsp_a_stock.dsf_v2, reference tables)"
      via: "the one live session opened by the user's run"
  prohibitions:
    - requirement_id: "D-13"
      category: "safety"
      status: "resolved"
      verification: "manual"
      resolution: null
      reason: null
      statement: "MUST NOT let the agent open a WRDS connection; the live smoke is run by the user only"
---

<objective>
Close the phase: document the CRSP vendor for users (Chinese, like the other example
docs), note in the open rename todo how this vendor sidesteps the problem, run the
no-new-failures gate in the MAIN tree after every merge (D-13), and have the user run the
live smoke with one Duo push per command.

**Sequencing:** execute only after phase 03.9 is fully merged and plan 03.10-09 is merged.
This plan is `autonomous: false` only because of the final human-verify checkpoint.

Purpose: offline tests prove the contract; the live smoke proves it matches WRDS's real
data and access behaviour.

Output: `example/wrds_crsp.md`, the README index line, the todo note, the gate record and
the user's live numbers in the SUMMARY.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-CONTEXT.md
@.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-RESEARCH.md
@.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-VALIDATION.md
@.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-09-SUMMARY.md
</context>

<environment_notes>
- The gate (Task 2) runs from the MAIN tree after all merges (user memory: a worktree verify can test unmerged code and report a false green). Commands are relative to the repository root.
- The agent never connects to WRDS. Task 3 is run by the user.
- Never `git stash`.
</environment_notes>

<tasks>

<task type="auto">
  <name>Task 1: example/wrds_crsp.md, the README index line and the rename-todo note</name>

  <files>example/wrds_crsp.md, example/README.md, .planning/todos/pending/2026-09-07-no-ticker-rename-mapping-between-membership-history-and-pric.md</files>

  <read_first>
    - example/acquisition.md lines 1-140 (section shape: 一句话 / 不用它会怎样 / 核心概念 / 它是怎么工作的 / 完整例子 / 常见坑)
    - example/wrds_taq.md (03.9-07: the WRDS credential, Duo and volume sections to mirror, not copy)
    - example/README.md (index format)
    - scripts/ingest_wrds_crsp.py docstring and `--help`
    - quantlab/dataset/crsp.py (presets, variables, sidecar suffixes), quantlab/dataset/crsp_symbology.py, quantlab/dataset/crsp_membership.py (docstrings)
    - .planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-CONTEXT.md D-01..D-19 and the LIVE-CHECK JSON numbers to quote
    - .planning/todos/pending/2026-09-07-no-ticker-rename-mapping-between-membership-history-and-pric.md
  </read_first>

  <action>
Write `example/wrds_crsp.md` in Chinese with the acquisition.md section shape:
- 一句话.
- 不用它会怎样: the failure modes this vendor prevents — survivorship bias from missing delisting returns, a fabricated cross-company return where a ticker is reused, `BRK.B` spelled two ways between the universe and the prices, a CRSP annual release silently mixed with an older one, a request past 2025-12-31 silently returning nothing.
- 核心概念:
  - why `dsf_v2`: unique on `(permno, dlycaldt)`, while `wrds_dsfv2_query` duplicates rows;
  - raw keyed by PERMNO, ticker assigned at conversion from `stksecurityinfohist`, with the rules for renames (FB→META), share classes (`BRK.B`, `BF.A`/`BF.B`/`BF`), NULL tickers and collisions (the three rules);
  - PERMNO seams (D-18) and the opt-out;
  - the filter presets, and the D-17 reading: the two-column predicate alone keeps ADRs and units, so the sharetype allow-list is added;
  - the variable table: the Tiingo 12 plus 15 extras, each with its CRSP source and unit;
  - the adjustment formula, the anchor at the last close in the window, and why an in-place extension is refused;
  - delisting returns already sit in `dlyret` (the Lehman example) and are never added twice;
  - events on their ex-dates;
  - the annual product end, clipping and the vintage stamp;
  - QQQ in its own store as data only, not wired into the backtester;
  - the two universes, CRSP S&P 500 from 1925-12-31 and the Compustat Nasdaq-100 via CCM from 1995-01-01 (left-censored), both clipped at the product end.
- 它是怎么工作的: a flow diagram — CLI → one WrdsSession (one Duo push) → entitlement → product end → reference tables → roster → per-year count → SqlVolumeGuard → registry.run → raw → registry.convert → CrspStockDataset → Zarr + three sidecars; plus the membership panel.
- 凭证: WRDS_USERNAME, the ~/.pgpass line with chmod 600, no credential flag.
- 体量: live row estimates and the bytes/row assumption until the smoke measures it.
- 完整例子: commands, marked as not executed live unless Task 3 recorded them.
- 常见坑:
  - a rename ends one symbol column and starts another, which a backtest sees as a delisting;
  - pre-1992 Nasdaq rows have no OHLC;
  - the anchor refusal and the vintage refusal, and how to rebuild;
  - unlinked Nasdaq-100 spells and `--allow-unlinked-ndx`.
Add one index line for `wrds_crsp.md` to `example/README.md` in its existing format. Append a dated section to the rename todo: for the CRSP vendor, prices and universes share one PERMNO-keyed symbology, so the vocabularies agree by construction. The todo stays open for Tiingo/Wikipedia. No username or password value appears anywhere (placeholders only).
  </action>

  <verify>
    <automated>test -f example/wrds_crsp.md && grep -c "dsf_v2" example/wrds_crsp.md && grep -n "wrds_crsp.md" example/README.md</automated>
    <fails_when>non-zero exit, a count of 0, or no README line</fails_when>
  </verify>

  <acceptance_criteria>
    - `example/wrds_crsp.md` contains `crsp_a_stock`, `dsf_v2`, `PERMNO`, `BRK.B`, `equity_common`, `shrcd_10_11`, `crsp_adjustment.json`, `crsp_filter_report.json`, `crsp_symbology_report.json`, `WRDS_USERNAME`, `.pgpass`, `--force-volume`, `2025-12-31`, `1995-01-01`
    - `example/README.md` has a line linking `wrds_crsp.md`
    - the todo file contains the string `03.10`
  </acceptance_criteria>

  <done>A reader can run the CRSP pipeline safely and understand every data-semantic choice from one doc.</done>
</task>

<task type="auto">
  <name>Task 2: Main-tree no-new-failures gate and new-suite check</name>

  <precondition>Plans 03.10-01 through 03.10-09 are merged into the branch this task runs on (their SUMMARY files exist), and the task runs in the MAIN tree, not a worktree.</precondition>

  <files>(none — verification only)</files>

  <read_first>
    - .planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-BASELINE-FAILURES.txt
    - .planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-VALIDATION.md
  </read_first>

  <action>
From the repository root of the main tree, run the CRSP suites, then the full suite with `-p no:cacheprovider`. Extract the sorted `FAILED` ids (`grep '^FAILED' | sed 's/ - .*//' | sort`) into the session scratchpad and compare them with `comm -13` against 03.9-BASELINE-FAILURES.txt. Record the pass/fail counts, the final summary line and the comm output in the SUMMARY. Any new failing id blocks Task 3 and is reported as a gap; it is not fixed inside this plan.
  </action>

  <verify>
    <automated>uv run pytest -q tests/test_crsp_tracer.py tests/test_wrds_crsp_acquisition.py tests/test_crsp_reference_tables.py tests/test_crsp_symbology.py tests/test_crsp_dataset.py tests/test_crsp_membership.py tests/test_crsp_identity.py tests/test_crsp_constituent.py tests/test_ingest_wrds_crsp.py tests/test_sql_volume_guard.py</automated>
    <fails_when>non-zero exit, or any failed/errored test in the summary line</fails_when>
    <automated>uv run pytest -q --ignore=tests/test_factor_hierarchy.py -p no:cacheprovider</automated>
    <fails_when>`comm -13 .planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-BASELINE-FAILURES.txt <sorted FAILED ids>` prints any line, or any ERROR line appears</fails_when>
  </verify>

  <acceptance_criteria>
    - The ten CRSP-related test files pass in the main tree
    - The comm comparison prints nothing; the SUMMARY quotes the final "N failed, M passed" line
  </acceptance_criteria>

  <done>The merged main tree fails only baseline tests, and every CRSP suite passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: User-run live WRDS CRSP smoke (one Duo push per command)</name>
  <what-built>The full CRSP pipeline: `scripts/ingest_wrds_crsp.py` → WrdsSession (psycopg2, one connection) → reference tables → roster → per-year count and guard → raw PERMNO shards → CrspStockDataset → Zarr + sidecars, plus the QQQ store and the membership panels.</what-built>
  <how-to-verify>
    Run these yourself; the agent never connects. `export WRDS_USERNAME=<your username>` first, and approve the Duo push once per command.
    1. `uv run python scripts/ingest_wrds_crsp.py --permnos 14593,13407,83443 --start-date 2019-01-01 --end-date 2023-12-31`
       - Check the estimate (with `year buckets:`) and that only one Duo push arrives.
       - Check the shards under `data/downloads/us_equity/1d/wrds_crsp/wrds/month=.../`.
       - `uv run python -c "import polars as pl; print(pl.scan_parquet('data/downloads/us_equity/1d/wrds_crsp/wrds/**/*.pqt').filter(pl.col('permno')==14593, pl.col('timestamp').dt.year()==2020).select(pl.len()).collect())"` should print 253.
       - Note the shard bytes divided by rows: the measured bytes/row.
    2. Re-run the same command: every PERMNO is skipped as covered.
    3. Add `--to-zarr`, then run `uv run python -c "import xarray as xr; d=xr.open_zarr('data/data/us_equity/1d/wrds_crsp_custom_1d.zarr'); a=d.adjClose.sel(symbol='AAPL'); print(float(a.sel(timestamp='2020-08-31')/a.sel(timestamp='2020-08-28')), sorted(map(str,d.symbol.values)))"`.
       - The ratio is about 1.033912.
       - The symbols include AAPL, BRK.B, FB and META.
       - FB's last row is 2022-06-08 and META's first is 2022-06-09.
       - Open the three sidecars beside the store.
    4. `--universe crsp_sp500 --start-date 2024-01-01 --end-date 2024-12-31 --to-zarr`
       - Report the roster size and the estimate.
       - Report `crsp_filter_report.json`'s dropped counts by type and any seams or collisions in `crsp_symbology_report.json`.
       - `wrds_crsp_sp500_membership.zarr` exists.
    5. `--universe comp_nasdaq100 --start-date 2024-01-01 --end-date 2024-12-31`
       - Report the roster size.
       - If it stops on unlinked spells, re-run with `--allow-unlinked-ndx` and report the listed spells.
    6. `--qqq --start-date 2024-01-01 --end-date 2026-06-30 --to-zarr`
       - The line `clipped end 2026-06-30 -> 2025-12-31 (crsp_a_stock annual product end)` prints.
       - In `wrds_crsp_qqq_1d.zarr`, close on 2025-12-31 is 614.31.
    7. `--permnos 14593 --start-date 2026-01-05 --end-date 2026-06-30`: refused, naming 2025-12-31, before any pull.
    Report the numbers from 1, 3, 4, 5 and 6 and anything unexpected.
  </how-to-verify>
  <resume-signal>Type "approved" with the measured AAPL 2020 row count, bytes/row, the adjClose ratio, the roster sizes and the filter drop counts, or describe the discrepancy.</resume-signal>
  <verify>
    <human-check>The user ran steps 1-7 and reported: 253 AAPL rows in 2020, one Duo push per command, a skipping re-run, a ratio of about 1.033912, FB/META split at 2022-06-09, both rosters resolved (or the unlinked list recorded), the clip line, and the 2026 refusal.</human-check>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the user's reported numbers (row count, bytes/row, ratio, roster sizes, filter drops, unlinked spells if any)
    - A discrepancy is recorded as a gap for /gsd-verify-work, not fixed inside this plan; a measured bytes/row far from 150 is recorded as a follow-up to update `WrdsCrspDailyAcquisition.DEFAULT_BYTES_PER_ROW`
  </acceptance_criteria>
  <done>The user confirmed the live pipeline matches the offline contract, or the discrepancies are recorded as gaps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user shell -> WRDS | the only live connection in this phase, opened by the user |
| docs -> readers | documentation must not carry credential values |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.10-38 | Information Disclosure | example/wrds_crsp.md | medium | mitigate | placeholders only for username/password; acceptance criterion checks the doc |
| T-03.10-39 | Denial of Service (self) | live smoke | low | accept | one Duo push per command by design; the user runs it deliberately |
| T-03.10-40 | Repudiation | unverified gate | medium | mitigate | the gate runs in the main tree after merges; the SUMMARY quotes the comm output |
</threat_model>

<verification>
- The main-tree gate is recorded with an empty comm output.
- The user's live numbers are in the SUMMARY or recorded as gaps.
</verification>

<success_criteria>
Phase 03.10 is documented, gated by "no new failures" in the main tree, and confirmed against real WRDS CRSP data by the user.
</success_criteria>

## Artifacts this phase produces (this plan's share)
- `example/wrds_crsp.md`, the `example/README.md` index line
- A phase-03.10 note in `.planning/todos/pending/2026-09-07-no-ticker-rename-mapping-between-membership-history-and-pric.md`
- The gate record and live-smoke numbers in `03.10-10-SUMMARY.md`

<output>
Create `.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-10-SUMMARY.md` when done
</output>
