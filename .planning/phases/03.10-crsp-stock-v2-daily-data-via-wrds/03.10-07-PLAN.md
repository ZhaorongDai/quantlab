---
phase: "03.10"
plan: "07"
type: execute
wave: 3
depends_on: ["03.10-02"]
files_modified:
  - quantlab/dataset/crsp_membership.py
  - tests/test_crsp_membership.py
autonomous: true
requirements: [D-05, D-14, D-19]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "CrspMembership.permno_intervals('crsp_sp500') returns CRSP's own point-in-time S&P 500 membership from dsp500list_v2 as closed (permno, start_date, end_date) intervals with an EXPLICIT end never later than the reference product end (AAPL 14593: 1982-11-18..2025-12-31) (D-05, D-19)"
    - "CrspMembership.permno_intervals('comp_nasdaq100') links comp.idxcst_his (gvkeyx 000208) to PERMNOs through crsp_a_ccm.ccmxpf_lnkhist on gvkey AND iid = liid with linktype in (LC, LU, LS), intersecting the membership spell with the link's date range (NULL thru / linkenddt = open): Alphabet gives GOOGL 90319 from 2005-12-21 and GOOG 14542 from 2014-04-03, both ending 2025-12-31 (D-14, D-19)"
    - "Nasdaq-100 spells are clipped to the CRSP product end; spells starting after it are dropped and counted in the report; the universe's coverage starts 1995-01-01 because Compustat's history is left-censored there (D-14, D-19)"
    - "A membership day no valid link covers (beyond a 4-calendar-day tolerance) is never dropped silently: by default ValueError lists the unlinked spells and the uncovered ranges; with allow_unlinked=True they are listed in the report instead. One (gvkey, iid) mapping to two PERMNOs on the same date raises (D-14)"
    - "permnos_in_range(index, start, end) returns the sorted PERMNO strings whose membership overlaps [start, end] — the roster the CLI pulls (D-05, D-14)"
  artifacts:
    - path: "quantlab/dataset/crsp_membership.py"
      provides: "CrspMembership (SP500, NASDAQ100, INDEXES, PIT_COVERAGE_START, SP500_INDNO, NDX_GVKEYX, LINK_TYPES, LINK_GAP_TOLERANCE_DAYS, permno_intervals, permnos_in_range, report)"
      contains: "class CrspMembership"
    - path: "tests/test_crsp_membership.py"
      provides: "S&P and Nasdaq-100/CCM interval tests (verbatim Alphabet rows + synthetic edge cases)"
  key_links:
    - from: "quantlab/dataset/crsp_membership.py::CrspMembership"
      to: "quantlab/dataset/crsp_reference.py::CrspReference"
      via: "dsp500list_v2 / idxcst_his / ccmxpf_lnkhist parquet and the manifest product_end"
  prohibitions:
    - requirement_id: "D-05"
      category: "scope"
      status: "resolved"
      verification: "test"
      resolution: null
      reason: null
      statement: "MUST NOT modify quantlab/acquisition/universe.py or add a UniverseCatalog category; the CRSP universes live beside the existing ones"
    - requirement_id: "D-14"
      category: "correctness"
      status: "resolved"
      verification: "test"
      resolution: null
      reason: null
      statement: "MUST NOT silently drop a Nasdaq-100 membership spell that has no CCM link, and MUST NOT emit a membership end later than the CRSP product end or a NULL end"
---

<objective>
Build the two point-in-time index universes this vendor offers, at the PERMNO level:
CRSP's own S&P 500 membership (D-05) and the Compustat Nasdaq-100 constituent history
linked to PERMNOs through the CRSP/Compustat Merged link table (D-14). The CLI resolves
its pull roster from these PERMNO intervals (no tickers needed), plan 08 uses them to
break same-ticker collisions, and plan 09 turns them into symbol-level membership panels.

**Sequencing:** execute only after phase 03.9 is fully merged and plan 03.10-02 is merged.
Pure module over the reference parquet files; no WRDS access.

Purpose: a universe that is point-in-time and survivorship-free by construction, keyed by
the identifier that does not change on renames.

Output: `quantlab/dataset/crsp_membership.py` and `tests/test_crsp_membership.py`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-CONTEXT.md
@.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-RESEARCH.md
@.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-02-SUMMARY.md
</context>

<environment_notes>
- Run from YOUR working-tree root: `uv run pytest -q tests/test_crsp_membership.py`. Never `cd` into the main checkout. `-k` matching nothing exits 5 = failure. Never `git stash`.
- Write reference sets with `tests/crsp_fixtures.py::write_reference_tables` (verbatim `DSP500_ROWS`, `IDXCST_ROWS`, `CCM_ROWS`); do NOT edit `tests/crsp_fixtures.py`. Synthetic rows live in the test module and carry `# SYNTHETIC`.
- Live facts (LIVE-CHECK-2): `dsp500list_v2` open membership is `mbrenddt = 2025-12-31` (503 rows), no NULLs; idxcst_his 000208 has 100 spells starting 1995-01-01 (left censor) and 10 starting after 2025-12-31; CCM `lpermno` is double precision; Alphabet gvkey 160329 links iid 01 -> 90319 (LC from 2004-08-19) and iid 03 -> 14542 (LC from 2014-04-03, NR 2014-03-31..04-02).
</environment_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CRSP S&P 500 PERMNO intervals and the roster query</name>

  <files>quantlab/dataset/crsp_membership.py, tests/test_crsp_membership.py</files>

  <read_first>
    - quantlab/dataset/crsp_reference.py (plan 02: `CrspReference.table`, `product_end`)
    - quantlab/base/constituent.py lines 25-68 and 130-170 (closed intervals; `_densify` extends an OPEN interval to wall-clock today — why ends must be explicit)
    - quantlab/acquisition/universe.py around `get_symbols_in_range` (~1519): the interval-overlap roster semantics to mirror
    - .planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-RESEARCH.md § Q5
  </read_first>

  <behavior>
    - Verbatim AAPL row -> (14593, 1982-11-18, 2025-12-31).
    - SYNTHETIC: a PERMNO with two spells (1990-01-02..1995-06-30 and 2001-03-01..2025-12-31) keeps both intervals; overlapping or touching spells of one PERMNO are merged into one.
    - SYNTHETIC row with a NULL mbrenddt -> end == product end (2025-12-31); a row with mbrenddt after the product end -> clipped to it; a row starting after the product end -> dropped and counted in `report["dropped_after_product_end"]`.
    - SYNTHETIC row with another `indno` -> excluded and counted in `report["other_indno_rows"]`.
    - `permnos_in_range("crsp_sp500", "2000-01-01", "2000-12-31")` returns the PERMNOs overlapping that year as sorted digit strings (numeric order); a PERMNO whose only spell ended 1995-06-30 is absent.
    - `permno_intervals("sp500")` (unknown name) raises ValueError listing `INDEXES`.
    - Every end_date is non-null and <= `CrspReference.product_end`.
  </behavior>

  <action>
Tests first. Create `quantlab/dataset/crsp_membership.py`:
- Module docstring: two universes, PERMNO-keyed; closed intervals on both ends (the constituent convention); ends are always explicit because `IndexConstituentDataset._densify` would extend an open interval to the wall clock, past CRSP coverage; `quantlab/acquisition/universe.py` is untouched (its structural no-acquisition-import rule).
- `class CrspMembership`: `__init__(self, reference: CrspReference)`; constants `SP500 = "crsp_sp500"`, `NASDAQ100 = "comp_nasdaq100"`, `INDEXES = (SP500, NASDAQ100)`, `PIT_COVERAGE_START = {SP500: "1925-12-31", NASDAQ100: "1995-01-01"}`, `SP500_INDNO = 1000500`, `NDX_GVKEYX = "000208"`, `LINK_TYPES = ("LC", "LU", "LS")`, `LINK_GAP_TOLERANCE_DAYS = 4`; `self.report: dict`.
- `permno_intervals(index, *, allow_unlinked=False) -> pl.DataFrame` (permno Int64, start_date Date, end_date Date; sorted by permno, start) dispatching on `index` (ValueError for anything not in `INDEXES`); the S&P branch reads `dsp500list_v2`, keeps `indno == SP500_INDNO`, fills a NULL end with the product end, clips ends to it, drops starts after it, merges overlapping/touching intervals per PERMNO, and records counts in `report`.
- `permnos_in_range(index, start_date, end_date, *, allow_unlinked=False) -> list[str]`: intervals with `start_date <= end` and `end_date >= start`, distinct PERMNOs sorted numerically, as strings.
  </action>

  <verify>
    <automated>uv run pytest -q tests/test_crsp_membership.py -k "sp500 or roster or unknown or product_end"</automated>
    <fails_when>non-zero exit, exit 5 with "deselected", or fewer than 5 passed in the summary line</fails_when>
  </verify>

  <acceptance_criteria>
    - The selector above reports at least 5 passed and 0 failed
    - `git diff --stat $(git merge-base HEAD main)..HEAD -- quantlab/acquisition/universe.py` prints nothing (the file is untouched)
  </acceptance_criteria>

  <done>CRSP's own S&P 500 history is available as explicit-ended PERMNO intervals and as an overlap roster.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Nasdaq-100 via Compustat idxcst_his and the CCM link table — iid-exact join, clipping, unlinked and ambiguous spells</name>

  <files>quantlab/dataset/crsp_membership.py, tests/test_crsp_membership.py</files>

  <read_first>
    - quantlab/dataset/crsp_membership.py (Task 1)
    - .planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-LIVE-CHECK-2.json keys `L7_3` (link type counts), `L7_4`, `L8_2`, `L8_3`, `L8_4`
    - .planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-LIVE-CHECK-NDX-QQQ.json keys `B3_comp_ndx_index`, `B4_comp_ndx_members`
    - .planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-RESEARCH.md § D-14
    - .planning/todos/pending/2026-09-07-no-ticker-rename-mapping-between-membership-history-and-pric.md ("fail loudly" requirement)
  </read_first>

  <behavior>
    - Verbatim Alphabet (IDXCST_ROWS + CCM_ROWS): intervals (90319, 2005-12-21, 2025-12-31) and (14542, 2014-04-03, 2025-12-31); the NR links contribute nothing.
    - SYNTHETIC spell 2003-01-02..2010-06-30 fully covered by an LU link -> one interval with that end.
    - SYNTHETIC spell starting 2026-01-05 -> dropped, `report["dropped_after_product_end"] == 1`; an open spell whose link is open -> end 2025-12-31 and counted in `report["clipped_to_product_end"]`.
    - SYNTHETIC spell with no link at all -> ValueError whose message names the gvkey, iid, the spell dates and `allow_unlinked`; with `allow_unlinked=True` it returns the linked intervals and `report["unlinked"]` holds {gvkey, iid, from, thru, uncovered: [[start, end], ...]}.
    - SYNTHETIC open spell whose only link ends 2020-06-30 -> unlinked range 2020-07-01..2025-12-31 reported (or raised by default).
    - SYNTHETIC link gap of 3 calendar days (link A ends Friday, link B starts Tuesday, same PERMNO) -> no error; `report["tolerated_gaps"]` lists it.
    - SYNTHETIC two LC links for one (gvkey, iid) with different lpermno overlapping 2012-01-03..2012-03-30 -> ValueError naming both PERMNOs and the overlap.
    - `report["left_censored_spells"]` counts spells whose `from` is 1995-01-01; `PIT_COVERAGE_START["comp_nasdaq100"] == "1995-01-01"`.
    - `lpermno` 90319.0 (Float64) becomes Int64 90319; a non-integral lpermno raises ValueError.
  </behavior>

  <action>
Tests first. Add the Nasdaq-100 branch to `permno_intervals`:
- Read `idxcst_his` (keep `gvkeyx == NDX_GVKEYX`) and `ccmxpf_lnkhist` (keep `linktype in LINK_TYPES` and non-null `lpermno`; cast lpermno to Int64 after checking it is integral).
- Join on `gvkey` and `iid == liid` (security-exact; both Alphabet classes are members under one gvkey, so filtering on `linkprim` would lose one).
- For each spell, clip `[from, thru or product_end]` to the product end (drop spells starting after it; count), intersect with each matched link `[linkdt, linkenddt or product_end]`; keep non-empty pieces as (permno, start, end).
- Coverage: subtract the pieces from the clipped spell; remaining calendar-day ranges of length <= `LINK_GAP_TOLERANCE_DAYS` go to `report["tolerated_gaps"]`, longer ones are unlinked. Unlinked spells -> ValueError listing up to 20 (gvkey, iid, from, thru, uncovered ranges), the total, and the remedy (`allow_unlinked=True`, surfaced by the CLI as `--allow-unlinked-ndx`), unless `allow_unlinked`, in which case they go to `report["unlinked"]` and a loguru warning prints the count.
- Ambiguity: a (gvkey, iid) whose pieces map to two PERMNOs on overlapping dates -> ValueError naming both PERMNOs and the overlap.
- Merge overlapping/touching pieces per PERMNO; record `report["left_censored_spells"]` and state the 1995-01-01 left censor in the docstring (the D-19 fact: 100 spells start that day).
  </action>

  <verify>
    <automated>uv run pytest -q tests/test_crsp_membership.py</automated>
    <fails_when>non-zero exit, or fewer than 13 passed in the summary line</fails_when>
  </verify>

  <acceptance_criteria>
    - `uv run pytest -q tests/test_crsp_membership.py` exits 0 with at least 13 passed
    - `uv run pytest -q tests/test_crsp_membership.py -k "nasdaq or ccm or unlinked or ambiguous or gap"` exits 0 with at least 6 passed
  </acceptance_criteria>

  <done>The Nasdaq-100 universe is PERMNO-exact through CCM, bounded by CRSP coverage, and loud about every day it cannot link.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reference parquet -> universe roster | membership decides which PERMNOs are pulled and which symbols a backtest may hold |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.10-23 | Tampering | survivorship bias via silently dropped spells | high | mitigate | unlinked spells raise by default; the opt-in path records every one in the report |
| T-03.10-24 | Tampering | future membership past price coverage | medium | mitigate | every end clipped to the manifest product end; post-end spells dropped with a count |
| T-03.10-25 | Tampering | wrong security for a multi-class issuer | medium | mitigate | iid = liid join (never linkprim); two PERMNOs for one (gvkey, iid) on one date raise |
</threat_model>

<verification>
- `uv run pytest -q tests/test_crsp_membership.py` passes.
- `quantlab/acquisition/universe.py` unchanged.
</verification>

<success_criteria>
Both CRSP-vendor universes exist as PERMNO-level point-in-time intervals with explicit ends and loud failure on anything unlinked.
</success_criteria>

## Artifacts this phase produces (this plan's share)
- `quantlab/dataset/crsp_membership.py`: `CrspMembership` (`SP500`, `NASDAQ100`, `INDEXES`, `PIT_COVERAGE_START`, `SP500_INDNO`, `NDX_GVKEYX`, `LINK_TYPES`, `LINK_GAP_TOLERANCE_DAYS`, `permno_intervals()`, `permnos_in_range()`, `report`)
- `tests/test_crsp_membership.py`

<output>
Create `.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-07-SUMMARY.md` when done
</output>
