---
phase: "03.10"
plan: "16"
type: execute
wave: 5
depends_on: ["03.10-15"]
gap_closure: true
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [D-01, D-12, D-14]

estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "ROADMAP.md's Phase 03.10 block names the REQUIREMENTS.md ids the phase advances instead of a placeholder (GAP-E)"
    - "REQUIREMENTS.md's traceability table records Phase 03.10 against DATA-03 and DATA-05, with a note saying what the phase contributed to each (GAP-E)"
    - "The v1 requirement set is unchanged in size and membership: no id is added, removed or renamed, and the Coverage block's arithmetic (28 ever defined, 1 withdrawn, 27 active, 0 unmapped) still reads true (GAP-E)"
    - "DATA-03's status is not falsely advanced to complete — Phase 2 still owns its delivery, and 03.10 is recorded as advancing it (GAP-E)"
    - "The D-01..D-19 accounting in the eleven executed plans is left exactly as it is; this plan closes the REQUIREMENTS.md end only, not the CONTEXT-decision end (GAP-E)"
    - "The DATA-03 annotation names the concrete extensibility this phase exercised: a second us_equity/1d vendor registered as a SourceDescriptor with its own capability resolver, reached without a quantlab/config factory (D-12)"
    - "The DATA-05 annotation names both CRSP-native point-in-time universes this phase added — the S&P 500 membership from dsp500list_v2 and the Compustat Nasdaq-100 universe via the CCM gvkey-to-PERMNO link (D-14), and the CRSP annual product end that bounds both (D-01)"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "the resolved Requirements line for Phase 03.10"
      contains: "DATA-03"
    - path: ".planning/REQUIREMENTS.md"
      provides: "the Phase 03.10 traceability rows"
      contains: "03.10"
  key_links:
    - from: ".planning/ROADMAP.md Phase 03.10 Requirements line"
      to: ".planning/REQUIREMENTS.md Traceability table rows for DATA-03 and DATA-05"
      via: "the requirement ids, which must name the same two rows in both files"
  prohibitions:
    - requirement_id: "D-13"
      category: "safety"
      statement: "MUST NOT open a live WRDS connection from an agent, an executor or a test"
    - requirement_id: "D-16"
      category: "scope"
      statement: "MUST NOT wire any CRSP or QQQ store into BacktestConfig.benchmark_dataset"
    - requirement_id: "SEC-01"
      category: "security"
      statement: "MUST NOT hardcode a credential value in any planning document"
    - requirement_id: "CLEAN-03"
      category: "process"
      statement: "MUST NOT invent a new requirement id, retire an existing one, or flip a checkbox this phase did not deliver"
---

<objective>
**GAP-E — the unclosed other end of this phase's requirement accounting.** Documentation only. It
blocks nothing and falsifies no delivered behaviour, and it is the last thing this gap set closes.

Every one of the eleven executed plans declared `requirements: [D-xx]`, which are this phase's own
CONTEXT decision ids, not REQUIREMENTS.md ids (that file uses `DATA-*`/`SEC-*`/`CLEAN-*`). That was
handled correctly at the executors' end: `requirements.mark-complete` returned `not_found` for the
D-xx ids, four SUMMARYs said so explicitly rather than skipping quietly, and REQUIREMENTS.md was
correctly left untouched rather than polluted with ids it does not own. Plan 03 correctly left
`requirements-completed: []`.

What was never closed is the other direction: `ROADMAP.md`'s Phase 03.10 block still reads
`**Requirements**: TBD`, and no REQUIREMENTS.md row mentions the phase at all — so the largest
single data-vendor integration in the project is invisible in the traceability table, while
`DATA-03` (a market/frequency/vendor can be added without changing the upper layers) and `DATA-05`
(point-in-time S&P 500 and Nasdaq-100 panels) are both materially advanced by it.

**Map, do not manufacture.** The smaller and truer change is to map 03.10 onto the two requirements
it advances. Writing a new CRSP requirement id would perturb a count this file deliberately keeps
honest (28 ever defined / 1 withdrawn / 27 active / 0 unmapped) and would assert a requirement the
project never wrote.

Purpose: an eleven-plan vendor integration that appears nowhere in the traceability table cannot be
audited by anyone who did not live through it.

Output: the resolved ROADMAP Requirements line, and two annotated traceability rows.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-VERIFICATION.md
@.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-15-SUMMARY.md
</context>

<environment_notes>
- These are planning documents, not source. Use exact, scoped replacements.
- Never use a `sed` range delete on a planning document: a missed end-match deletes to end of file.
  Replace an exact string, and compare `wc -l` before and after — the line delta must be explainable
  by the lines this plan adds.
- Never `git stash`. Never open a WRDS connection.
- `ROADMAP.md` holds every phase of the project. Edit ONLY the Phase 03.10 block; a whole-file
  rewrite destroys the other phase entries.
</environment_notes>

<tasks>

<task type="auto">
  <name>Task 1: Resolve the ROADMAP Phase 03.10 Requirements placeholder</name>

  <files>.planning/ROADMAP.md</files>

  <read_first>
    - .planning/ROADMAP.md the Phase 03.10 block in full (the Goal paragraph, the Requirements placeholder line, Depends on, Plans, the user-decisions list and the wave list) — and the two neighbouring phase blocks, to copy the exact formatting other resolved phases use for their Requirements line
    - .planning/REQUIREMENTS.md the DATA-03 and DATA-05 bullets in the Data section, so the ROADMAP line names ids that exist verbatim
    - .planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-VERIFICATION.md the Requirements Coverage section and its judgement paragraph — the reasoning this edit implements
  </read_first>

  <action>
Replace the Phase 03.10 block's placeholder Requirements line with the two ids the phase advances,
in the exact formatting the other resolved phase blocks in this file use. Follow the ids with a
short parenthetical stating the contribution in each direction: DATA-03 gains a second production
vendor for the same market and frequency, exercised through the registry without any change to the
factor, model or backtest layers; DATA-05 gains CRSP-native point-in-time S&P 500 membership from
`dsp500list_v2` and a Compustat Nasdaq-100 universe via the CCM link, beside the existing
Wikipedia-derived panels.

State in the same line, or immediately under it, that neither requirement is COMPLETED by this
phase: DATA-03's delivery is Phase 2's, and DATA-05 was already complete in Phase 03.1. Phase
03.10 advances both. That sentence is what stops a later reader from concluding the phase
over-claimed.

Add one line recording that this phase's plans declare D-01 through D-19 — the 03.10-CONTEXT
decision ids — in their own `requirements:` frontmatter, and that those are phase-local decision
ids rather than REQUIREMENTS.md ids, so a reader comparing the two vocabularies is not left
guessing. Do not rewrite any plan's frontmatter.

Touch nothing else in the file. Every other phase block, every checkbox and every wave heading stay
byte-identical.
  </action>

  <verify>
    <automated>uv run python -c "import pathlib; s=pathlib.Path('.planning/ROADMAP.md').read_text(encoding='utf-8'); b=s.split('Phase 03.10:')[1].split('Phase 03.9:')[0]; print('PLACEHOLDER', b.count('Requirements**: TBD')); print('DATA03', b.count('DATA-03')); print('DATA05', b.count('DATA-05')); print('DXX', b.count('D-19'))"</automated>
    <fails_when>non-zero exit, a printed PLACEHOLDER that is not 0, a printed DATA03 of 0, a printed DATA05 of 0, or a printed DXX of 0</fails_when>
    <automated>uv run python -c "import pathlib; s=pathlib.Path('.planning/ROADMAP.md').read_text(encoding='utf-8'); print('PHASES', s.count('### Phase ')); print('PLANS0310', s.count('03.10-') )"</automated>
    <fails_when>non-zero exit, or a printed PHASES below the count recorded before this task ran (record it first with the same command on the pre-edit file via git show HEAD:.planning/ROADMAP.md)</fails_when>
  </verify>

  <acceptance_criteria>
    - The Phase 03.10 block contains no Requirements placeholder and names both `DATA-03` and `DATA-05`
    - The block states explicitly that neither requirement is completed by this phase
    - The block records that the plans' own `requirements:` entries are 03.10-CONTEXT decision ids
    - Before committing, `git diff HEAD --stat -- .planning/ROADMAP.md` shows only additions and the one replaced line, and `git diff HEAD -- .planning/ROADMAP.md` names no phase block other than 03.10
    - The total `### Phase ` heading count in the file is unchanged from `git show HEAD:.planning/ROADMAP.md`
  </acceptance_criteria>

  <done>A reader of the roadmap can see which project requirements this phase serves, and can see that it advances rather than completes them.</done>
</task>

<task type="auto">
  <name>Task 2: Record Phase 03.10 in the REQUIREMENTS traceability table</name>

  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
    - .planning/REQUIREMENTS.md the Traceability table in full, and in particular the `DATA-03 | Phase 2 | Pending` and `DATA-05 | Phase 03.1 | Complete` rows, plus the rows that already carry a multi-phase or annotated status (BT-01, CFG-01, FACTOR-01, DATA-08) — those are the in-file precedents for how an annotated row is written
    - .planning/REQUIREMENTS.md the Coverage block and its 2026-09-12 arithmetic note — the counts this task must leave true
    - .planning/REQUIREMENTS.md the DATA-03 and DATA-05 bullets in the Data section
    - .planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-VERIFICATION.md the Requirements Coverage section
  </read_first>

  <action>
Annotate the two existing traceability rows rather than adding new ones, following the shape BT-01
and CFG-01 already use for a requirement served by more than one phase.

- The DATA-03 row: keep its owning phase and its Pending status, and append a note that Phase 03.10
  added a second `us_equity`/`1d` vendor (WRDS CRSP Stock v2) through the registry's
  per-capability resolver, consumed by the factor, label and backtest layers without a change in
  any of them — the extensibility this requirement asks for, exercised but not yet signed off by
  its owning phase.
- The DATA-05 row: keep its Complete status and Phase 03.1 ownership, and append a note that Phase
  03.10 added CRSP-native point-in-time S&P 500 membership (`dsp500list_v2`, by PERMNO) and a
  Compustat Nasdaq-100 universe via the CCM link, in the price panel's own period-correct tickers.

Also add a sentence to the DATA-05 bullet in the Data section naming the CRSP-native source beside
the existing one, since that bullet is where a reader looks for what the requirement means. Leave
the DATA-03 bullet's wording alone — its acceptance condition is unchanged.

Do NOT add a requirement id, do NOT remove one, do NOT change any checkbox marker, and do NOT
recompute the Coverage block: no id is added or retired here, so 28 ever defined / 1 withdrawn /
27 active / 28 mapped / 0 unmapped all stay true. Verify that by counting rather than by assuming,
and record the counted numbers in the SUMMARY.
  </action>

  <verify>
    <automated>uv run python -c "import pathlib,re; s=pathlib.Path('.planning/REQUIREMENTS.md').read_text(encoding='utf-8'); t=s.split('## Traceability')[1]; rows=[l for l in t.splitlines() if l.startswith('| ') and not l.startswith('| Requirement') and not l.startswith('|---')]; print('ROWS', len(rows)); print('D03_0310', sum(1 for l in rows if l.startswith('| DATA-03') and '03.10' in l)); print('D05_0310', sum(1 for l in rows if l.startswith('| DATA-05') and '03.10' in l))"</automated>
    <fails_when>non-zero exit, a printed ROWS that differs from the count on `git show HEAD:.planning/REQUIREMENTS.md`, a printed D03_0310 that is not 1, or a printed D05_0310 that is not 1</fails_when>
    <automated>uv run python -c "import pathlib,re; s=pathlib.Path('.planning/REQUIREMENTS.md').read_text(encoding='utf-8'); ids=sorted(set(re.findall(r'\*\*([A-Z]+-[0-9]+)\*\*', s))); print('IDS', len(ids)); print(' '.join(ids))"</automated>
    <fails_when>non-zero exit, or a printed IDS count that differs from the same command run against `git show HEAD:.planning/REQUIREMENTS.md`</fails_when>
  </verify>

  <acceptance_criteria>
    - The DATA-03 traceability row mentions Phase 03.10 and still reads Pending
    - The DATA-05 traceability row mentions Phase 03.10 and still reads Complete
    - The traceability row count is identical to `git show HEAD:.planning/REQUIREMENTS.md`
    - The set of bolded requirement ids in the file is identical to `git show HEAD:.planning/REQUIREMENTS.md`
    - The Coverage block's four numbers are unchanged, and the SUMMARY records them as counted rather than assumed
    - Before committing, `git diff HEAD -- .planning/REQUIREMENTS.md` shows no deleted requirement bullet and no changed checkbox marker
  </acceptance_criteria>

  <done>An auditor reading the traceability table sees the CRSP integration and what it contributed, with no requirement invented and no count disturbed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning documents -> future auditors | a traceability table is the audit surface; a wrong status is a false assurance |
| bulk text edit -> the rest of the file | a range delete or a whole-file write on these documents destroys unrelated content |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.10-56 | Repudiation | REQUIREMENTS.md status annotation | medium | mitigate | DATA-03 keeps Pending and DATA-05 keeps Complete; the acceptance criteria assert both statuses and the unchanged id set, so the edit cannot quietly claim delivery |
| T-03.10-57 | Tampering | scoped edit to ROADMAP.md / REQUIREMENTS.md | medium | mitigate | exact-string replacement only, never a `sed` range delete; row counts and id sets are diffed against `git show HEAD:` and the `git diff` is asserted to touch one block |
| T-03.10-58 | Information Disclosure | planning documents | low | accept | these two files carry no credential and this plan adds none; the phase's credential handling is unchanged |
| T-03.10-SC | Tampering | npm/pip/cargo installs | high | accept | this plan installs no package; no new dependency enters `pyproject.toml` |
</threat_model>

<verification>
- The Phase 03.10 ROADMAP block names DATA-03 and DATA-05 and carries no placeholder.
- The DATA-03 and DATA-05 traceability rows each mention Phase 03.10, with their statuses unchanged.
- The traceability row count and the bolded-id set are identical to HEAD.
- `git diff HEAD --` before the commit touches only the Phase 03.10 ROADMAP block and the two annotated rows plus the DATA-05 bullet.
</verification>

<success_criteria>
Phase 03.10 is traceable from ROADMAP.md to REQUIREMENTS.md and back, no requirement id was
invented or retired, and no status was advanced beyond what the phase delivered.
</success_criteria>

## Artifacts this phase produces (this plan's share)

New content created here (no new code symbols — this plan is documentation only):

- `.planning/ROADMAP.md` — the resolved Phase 03.10 Requirements line naming `DATA-03` and
  `DATA-05`, the advances-not-completes sentence, and the note that the plans' own `requirements:`
  entries are 03.10-CONTEXT decision ids
- `.planning/REQUIREMENTS.md` — the Phase 03.10 annotation on the `DATA-03` and `DATA-05`
  traceability rows, and the CRSP-native sentence on the `DATA-05` bullet
- No new requirement id, no new file, no new dependency

<output>
Create `.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-16-SUMMARY.md` when done
</output>
