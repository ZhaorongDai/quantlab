---
phase: "03.10-crsp-stock-v2-daily-data-via-wrds"
plan: "16"
subsystem: infra
tags: [planning, traceability, requirements, roadmap, documentation, gap-closure]

# Dependency graph
requires:
  - phase: "03.10 plan 15"
    provides: "the last code-side gap closure (GAP-D / WR-01); GAP-E is the accounting end that closes after it"
  - phase: "03.10-VERIFICATION.md"
    provides: "the Requirements Coverage section and its judgement paragraph — the reasoning this plan implements verbatim"
provides:
  - ".planning/ROADMAP.md — the Phase 03.10 Requirements line now names DATA-03 and DATA-05 with the concrete contribution made to each, replacing `**Requirements**: TBD`"
  - ".planning/ROADMAP.md — an explicit advances-not-completes sentence: DATA-03 stays Pending under Phase 2, DATA-05 was already Complete in Phase 03.1"
  - ".planning/ROADMAP.md — a note that the plans' own `requirements: [D-01 … D-19]` are 03.10-CONTEXT decision ids, not REQUIREMENTS.md ids, which is why `requirements.mark-complete` reported them `not_found`"
  - ".planning/REQUIREMENTS.md — the DATA-03 traceability row annotated with Phase 03.10 while keeping Phase 2 / Pending"
  - ".planning/REQUIREMENTS.md — the DATA-05 traceability row annotated with Phase 03.10 while keeping Phase 03.1 / Complete"
  - ".planning/REQUIREMENTS.md — the DATA-05 bullet names the CRSP-native source (dsp500list_v2 by PERMNO, Nasdaq-100 via the CCM gvkey→PERMNO link) beside the existing Wikipedia one"
affects: ["03.10-17 (the closing gate)", "any later audit reading the traceability table for the CRSP integration"]

# Actuals (#2632) — chars/4 over the realized diff of this plan's commits
# (git diff 68dd395..HEAD | grep '^+' | wc -c == 2784).
actuals:
  tokens: 696
  tasks: 2
  commits: 3
plan_head_before: 68dd39562d7192801f61857300297b7ef7b0bb23

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Map, do not manufacture: an unmapped phase is recorded against the requirements it advances, rather than given a new requirement id that would perturb a deliberately honest count"
    - "Advanced is not Complete: an annotation that names a contribution must also name what it does NOT claim, or a later reader reads the annotation as delivery"
    - "Two vocabularies are joined, not merged: phase-local CONTEXT decision ids (D-xx) and project requirement ids (DATA-xx) stay in their own files, with a cross-reference explaining why mark-complete reported not_found"
    - "Counted, not assumed: the invariant counts (phase headings, traceability rows, bolded ids, checkbox markers, the Coverage arithmetic) are diffed against `git show HEAD:` rather than reasoned about"

key-files:
  created:
    - ".planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-16-SUMMARY.md"
  modified:
    - ".planning/ROADMAP.md"
    - ".planning/REQUIREMENTS.md"

key-decisions:
  - "Task 1's own verify recipe cannot detect the defect it targets, on two of its four printed values. `DATA03` and `DATA05` are counted over the WHOLE Phase 03.10 block, and that block already contained the plan-16 bullet line `03.10-16-PLAN.md — GAP-E: ... record Phase 03.10 against DATA-03 and DATA-05 ...`. Measured at HEAD before any edit: `BASE DATA03 1`, `BASE DATA05 1`. Both `fails_when` conditions are `a printed DATA03 of 0` / `a printed DATA05 of 0`, so both were already satisfied by a line the plan does not touch — a completely unedited file passes them. Only `PLACEHOLDER` (1 → 0) and `DXX` (0 → 1) discriminate. A third check was written that reads the `**Requirements**:` LINE itself rather than the block, and it is the one that actually proves Task 1: REQ_LINE_HAS_DATA03 True, REQ_LINE_HAS_DATA05 True, REQ_LINE_HAS_TBD False."
  - "Task 2's verify recipe IS discriminating, and was checked rather than assumed. `'03.10' in l` on the DATA-05 row could have been satisfied spuriously by its own `Phase 03.1` cell; it is not — `03.1 ` has no trailing zero — so both `D03_0310` and `D05_0310` read 0 at HEAD and 1 after the edit. Confirmed by running the same command against `git show HEAD:.planning/REQUIREMENTS.md` first."
  - "The two rows were annotated in place rather than duplicated, following the BT-01 / CFG-01 / DATA-08 precedent already in the file (status column carries the prose). Adding rows would have broken the 28-row count the Coverage block asserts."
  - "The statuses were left in the first position of each status cell (`Pending — advanced by …`, `Complete — also advanced by …`) so the row still reads Pending / Complete to a human scanning the column, not only to the regex."
  - "The new ROADMAP prose mentions `Phase 2` and `Phase 03.1` (where DATA-03 and DATA-05 are owned). A naive grep over the diff therefore names three phases; the diff is a SINGLE hunk at `@@ -143,7 +143,20 @@`, wholly inside the Phase 03.10 block, and no `### Phase ` heading line is added, removed or modified (`git diff | grep -c '^+.*### Phase '` → 0)."
  - "The DATA-05 bullet sentence is written in Chinese, matching every other requirement bullet in that section; the traceability-row annotations are in English, matching every other annotated row in that table. Language follows the surrounding convention in each location rather than one global choice."
  - "No `sed` was used on either document. All three changes are exact-string Edit replacements of one existing line each."

patterns-established:
  - "An annotated traceability row states the owning phase's status first and the advancing phase's contribution second — the status stays scannable and the contribution stays auditable"
  - "When a plan's verify recipe is pre-satisfied at HEAD, record the measured baseline and add a check with discriminating power rather than reporting the green"

requirements-completed: []  # The plan declares `requirements: [D-01, D-12, D-14]` — 03.10-CONTEXT decision ids, NOT REQUIREMENTS.md ids. Recording them here would assert project requirements this plan did not complete; the D-id accounting lives in the phase's own CONTEXT file. This is the same distinction the plan itself documents into ROADMAP.md.

coverage:
  - id: D1
    description: "ROADMAP.md's Phase 03.10 block names the REQUIREMENTS.md ids the phase advances (DATA-03, DATA-05) instead of the `TBD` placeholder"
    requirement: "DATA-03"
    verification:
      - kind: other
        ref: "uv run python -c \"... b.count('Requirements**: TBD') ...\" → PLACEHOLDER 0 (was 1 at HEAD)"
        status: pass
      - kind: other
        ref: "discriminating check on the `**Requirements**:` line itself → REQ_LINES 1, REQ_LINE_HAS_DATA03 True, REQ_LINE_HAS_DATA05 True, REQ_LINE_HAS_TBD False"
        status: pass
    human_judgment: false
  - id: D2
    description: "The ROADMAP block states that neither requirement is completed by this phase, and records that the plans' own requirements: ids are 03.10-CONTEXT decision ids"
    verification:
      - kind: other
        ref: "ADVANCES_SENTENCE True, PENDING_KEPT True, CONTEXT_IDS_NOTE True; DXX 1 (was 0 at HEAD)"
        status: pass
    human_judgment: false
  - id: D3
    description: "REQUIREMENTS.md's traceability table records Phase 03.10 against DATA-03 (still Pending) and DATA-05 (still Complete), with a note saying what the phase contributed to each"
    requirement: "DATA-05"
    verification:
      - kind: other
        ref: "uv run python -c \"... rows ...\" → ROWS 28, D03_0310 1, D05_0310 1 (both 0 at HEAD)"
        status: pass
    human_judgment: false
  - id: D4
    description: "The v1 requirement set is unchanged in size and membership, and the Coverage arithmetic still reads true"
    verification:
      - kind: other
        ref: "IDS 34 with an identical sorted id list to `git show HEAD:.planning/REQUIREMENTS.md`; ROWS 28 == 28; CHECKBOXES_IDENTICAL True (28 markers); COVERAGE_IDENTICAL True (28 / 1 / 27 / 28 / 0); BULLET_COUNT 28 == 28"
        status: pass
    human_judgment: false
  - id: D5
    description: "The edit is scoped: only the Phase 03.10 ROADMAP block, the two traceability rows and the DATA-05 bullet changed"
    verification:
      - kind: other
        ref: "PHASES 17 == 17; one ROADMAP diff hunk at @@ -143,7 +143,20 @@; grep -c '^+.*### Phase ' → 0; REQUIREMENTS diff is 3 insertions / 3 deletions, no deleted bullet, no changed checkbox"
        status: pass
    human_judgment: false

# Metrics
duration: 4 min
completed: 2026-09-20
status: complete
---

# Phase 03.10 Plan 16: GAP-E — Phase 03.10 mapped onto DATA-03 and DATA-05 Summary

**The eleven-plan CRSP integration is now visible in the traceability table: ROADMAP's `**Requirements**: TBD` is replaced by DATA-03 and DATA-05 with the contribution made to each, both rows annotated in place, and an explicit advances-not-completes sentence — no requirement id invented, no count disturbed.**

## Performance

- **Duration:** 4 min
- **Started:** 2026-09-20T22:09:36Z
- **Completed:** 2026-09-20T22:13:18Z
- **Tasks:** 2
- **Files modified:** 2 (plus this SUMMARY)

## Accomplishments

- **ROADMAP.md Phase 03.10 no longer reads `TBD`.** The Requirements line names `DATA-03 (advanced)` and `DATA-05 (advanced)`, each with the concrete contribution: for DATA-03, a second production `us_equity`/`1d` vendor registered as a `SourceDescriptor` with its own per-capability resolver, reached without a `quantlab/config` factory and consumed by the factor, label and backtest layers unchanged (D-12); for DATA-05, CRSP-native point-in-time S&P 500 membership from `dsp500list_v2` (by PERMNO) and a Compustat Nasdaq-100 universe via the CCM gvkey→PERMNO link, in the price panel's own period-correct tickers, both bounded by the CRSP annual-update product end (D-14, D-01).
- **The phase does not over-claim.** A bolded sentence states that neither requirement is COMPLETED here: DATA-03's delivery belongs to Phase 2 and stays Pending, DATA-05 was already Complete in Phase 03.1.
- **The two id vocabularies are joined rather than merged.** A third paragraph records that every plan declares `requirements: [D-01 … D-19]` — `03.10-CONTEXT.md` decision ids, not REQUIREMENTS.md ids — which is why `requirements.mark-complete` correctly reported them `not_found` and REQUIREMENTS.md was rightly left untouched by the eleven executed plans.
- **REQUIREMENTS.md records the phase on both rows, in place.** DATA-03 keeps `Phase 2` / `Pending`; DATA-05 keeps `Phase 03.1` / `Complete`. The DATA-05 bullet in the Data section additionally names the CRSP-native source beside the existing Wikipedia one, because that bullet is where a reader looks for what the requirement means.
- **Every invariant count was measured, not assumed** (see the counted numbers below), and the pre-edit baselines were taken from `git show HEAD:` with the plan's own commands before anything was edited.

## Counted numbers (measured, not assumed)

Baselines taken from `git show HEAD:` at base commit `68dd395` BEFORE any edit, then re-run after.

| Quantity | Before | After | Required |
|---|---|---|---|
| `.planning/ROADMAP.md` lines | 1184 | 1197 | delta fully explained by 14 insertions − 1 deletion |
| `### Phase ` headings in ROADMAP.md | 17 | 17 | unchanged |
| `03.10-` occurrences in ROADMAP.md | 19 | 20 | +1, from the added `03.10-CONTEXT.md` reference |
| `Requirements**: TBD` in the 03.10 block | 1 | 0 | must become 0 |
| `D-19` in the 03.10 block | 0 | 1 | must become non-zero |
| `.planning/REQUIREMENTS.md` lines | 181 | 181 | unchanged (three in-place line replacements) |
| Traceability rows | 28 | 28 | unchanged |
| Bolded requirement ids | 34 | 34 | unchanged, and the sorted id list is byte-identical |
| Requirement checkbox markers | 28 | 28 | identical `(marker, id)` pairs |
| Requirement bullets (`^- [`) | 28 | 28 | unchanged |
| Coverage block | 28 / 1 / 27 / 28 / 0 | 28 / 1 / 27 / 28 / 0 | unchanged, line-for-line identical |

The full sorted id list, identical before and after: `ARCH-01 ARCH-02 BT-01 BT-02 CFG-01 CLEAN-01 CLEAN-02 CLEAN-03 CLEAN-04 DATA-01 DATA-02 DATA-03 DATA-04 DATA-05 DATA-06 DATA-07 DATA-08 FACTOR-01 FACTOR-02 FACTOR-03 FACTOR-04 MODEL-01 MODEL-02 PLAT-01 PLAT-02 PLAT-03 PLAT-04 PLAT-05 PLAT-06 PORT-01 PORT-02 QUAL-01 QUAL-02 SEC-01`.

## Verify output, verbatim

**Task 1, plan verify block 1** (`uv run python -c "... b=s.split('Phase 03.10:')[1].split('Phase 03.9:')[0] ..."`):

```
PLACEHOLDER 0
DATA03 4
DATA05 4
DXX 1
```

Same command against `git show HEAD:.planning/ROADMAP.md`, run BEFORE the edit:

```
BASE PLACEHOLDER 1
BASE DATA03 1
BASE DATA05 1
BASE DXX 0
```

**Task 1, plan verify block 2:**

```
PHASES 17
PLANS0310 20
```

against a pre-edit baseline of:

```
BASE PHASES 17
BASE PLANS0310 19
```

`PHASES` is not below its baseline (the block's `fails_when`), and `PLANS0310` rose by the one added `03.10-CONTEXT.md` reference.

**Task 1, added discriminating check** (reads the `**Requirements**:` line rather than the whole block):

```
REQ_LINES 1
REQ_LINE_HAS_DATA03 True
REQ_LINE_HAS_DATA05 True
REQ_LINE_HAS_TBD False
ADVANCES_SENTENCE True
PENDING_KEPT True
CONTEXT_IDS_NOTE True
```

**Task 2, plan verify block 1:**

```
ROWS 28
D03_0310 1
D05_0310 1
```

pre-edit baseline: `BASE ROWS 28`, and both `D03_0310` / `D05_0310` were 0 (neither row mentioned the phase).

**Task 2, plan verify block 2:**

```
IDS 34
ARCH-01 ARCH-02 BT-01 BT-02 CFG-01 CLEAN-01 CLEAN-02 CLEAN-03 CLEAN-04 DATA-01 DATA-02 DATA-03 DATA-04 DATA-05 DATA-06 DATA-07 DATA-08 FACTOR-01 FACTOR-02 FACTOR-03 FACTOR-04 MODEL-01 MODEL-02 PLAT-01 PLAT-02 PLAT-03 PLAT-04 PLAT-05 PLAT-06 PORT-01 PORT-02 QUAL-01 QUAL-02 SEC-01
```

pre-edit baseline: `BASE IDS 34` with the identical list.

**Task 2, added no-collateral-damage check:**

```
CHECKBOXES_IDENTICAL True 28
COVERAGE_IDENTICAL True
   - v1 requirements ever defined: 28 total
   - Withdrawn: 1 (DATA-08 — phase 03.6 SC-3 deleted the capability)
   - Active: 27
   - Mapped to phases: 28 (every v1 requirement, the withdrawn one included, still has a row above)
   - Unmapped: 0
LINES old/new 181 181
BULLET_COUNT old/new 28 28
```

**Plan-level scope check:**

```
.planning/REQUIREMENTS.md |  6 +++---
.planning/ROADMAP.md      | 15 ++++++++++++++-
2 files changed, 17 insertions(+), 4 deletions(-)

ROADMAP diff hunks: 1
@@ -143,7 +143,20 @@ https://wrds-www.wharton.upenn.edu/pages/get-data/center-research-security-price
added/removed `### Phase ` heading lines: 0
```

**Key-link check (ROADMAP line ↔ REQUIREMENTS rows):**

```
KEY_LINK ids on the ROADMAP line: ['DATA-03', 'DATA-05']
  -> DATA-03 row annotated with 03.10: ['DATA-03']
  -> DATA-05 row annotated with 03.10: ['DATA-05']
ROADMAP lines 1197 | REQUIREMENTS lines 181
```

## A verify block that could not catch what it claimed

The brief asked for this to be stated if found, and it was found.

**Task 1's `DATA03` and `DATA05` checks are pre-satisfied at HEAD and therefore prove nothing.** They count occurrences over the entire Phase 03.10 block, and that block already contains, at line 219 of the pre-edit file, the plan-16 roadmap bullet:

> `- [ ] 03.10-16-PLAN.md — GAP-E: resolve the ROADMAP Requirements placeholder and record Phase 03.10 against DATA-03 and DATA-05 in REQUIREMENTS.md, with no id invented and no count disturbed`

Measured before any edit: `BASE DATA03 1`, `BASE DATA05 1`. The block's `fails_when` reads *"a printed DATA03 of 0, a printed DATA05 of 0"* — so a completely unedited ROADMAP.md passes both. An executor that made no change at all, and only failed the `PLACEHOLDER` condition, would still see two greens. The two checks with real discriminating power in that block are `PLACEHOLDER` (1 → 0) and `DXX` (0 → 1); the added line-scoped check above is what actually pins the deliverable.

**Task 2's checks are sound, and that was verified rather than assumed.** The near-miss worth naming: `'03.10' in l` on the DATA-05 row could have been satisfied by that row's own `Phase 03.1` cell. It cannot — `03.1 ` carries no trailing zero — and the pre-edit run confirmed both counts were 0. Had the owning phase been `03.10` instead of `03.1`, that check would have been as hollow as Task 1's.

## Task Commits

1. **Task 1: Resolve the ROADMAP Phase 03.10 Requirements placeholder** — `b218b2b` (docs)
2. **Task 2: Record Phase 03.10 in the REQUIREMENTS traceability table** — `343fc51` (docs)

**Plan metadata:** the third and final commit on this branch (`docs(03.10-16): complete the GAP-E requirement-accounting plan`) — its own hash is deliberately not quoted here, since this file is inside that commit.

Measured, not narrated: `git rev-list --count 68dd395..HEAD` → 3, which is the number recorded in `actuals.commits` (the two task commits plus this SUMMARY commit). At the moment the SUMMARY body was written the count was 2; the frontmatter was corrected to the post-commit measurement by amending this commit, so the recorded figure and the repository agree.

## Files Created/Modified

- `.planning/ROADMAP.md` — Phase 03.10 block only: the resolved Requirements line, the advances-not-completes sentence, and the 03.10-CONTEXT decision-id note. One hunk, 14 insertions and 1 deletion.
- `.planning/REQUIREMENTS.md` — three in-place line replacements: the DATA-03 traceability row, the DATA-05 traceability row, and the DATA-05 bullet in the Data section.
- `.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-16-SUMMARY.md` — this file.

## Decisions Made

See `key-decisions` in the frontmatter. The two that matter most to a later reader:

1. **The rows were annotated, not duplicated, and the status stayed in front.** `Pending — advanced by Phase 03.10, …` and `Complete — also advanced by Phase 03.10, …` keep the column scannable by a human, and keep the 28-row count the Coverage block asserts.
2. **`requirements-completed: []`.** This plan's frontmatter declares `requirements: [D-01, D-12, D-14]`, which are 03.10-CONTEXT decision ids, not REQUIREMENTS.md ids. Copying them into `requirements-completed` would assert project requirements this plan did not complete — the exact confusion this plan documents away. The prior plans in this phase handled it the same way, and four of their SUMMARYs said so explicitly.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Task 1's verify recipe needed a check with discriminating power**

- **Found during:** Task 1
- **Issue:** The plan's `DATA03` / `DATA05` counts are taken over the whole Phase 03.10 block, which already mentions both ids in its own plan-16 bullet. Measured at HEAD: 1 and 1. Both `fails_when` conditions were satisfied before any edit, so the recipe could not detect an unresolved Requirements line.
- **Fix:** Added a check that reads the `**Requirements**:` line itself (`REQ_LINES` / `REQ_LINE_HAS_DATA03` / `REQ_LINE_HAS_DATA05` / `REQ_LINE_HAS_TBD`), plus assertions for the advances-not-completes sentence and the CONTEXT-id note. The plan's own commands were still run, and their output is recorded verbatim above alongside the pre-edit baselines that expose the problem.
- **Files modified:** none — verification only.
- **Verification:** pre-edit baseline run against `git show HEAD:.planning/ROADMAP.md`, then the post-edit run; both printed above.
- **Committed in:** no code change; documented here and in `key-decisions`.

**2. [Rule 2 - Missing Critical] Added a collateral-damage check on REQUIREMENTS.md that the plan asserts but does not automate**

- **Found during:** Task 2
- **Issue:** The plan's acceptance criteria require that no checkbox marker changes and that the Coverage block's four numbers stay true, but its two verify blocks check only row count, the two annotations and the bolded-id set. A checkbox flip or a mangled Coverage line would have passed.
- **Fix:** Added a check that diffs the `(marker, id)` pairs and the five Coverage lines against `git show HEAD:.planning/REQUIREMENTS.md`, plus the bullet count and line count. Output printed above.
- **Files modified:** none — verification only.
- **Verification:** `CHECKBOXES_IDENTICAL True 28`, `COVERAGE_IDENTICAL True`, `LINES old/new 181 181`, `BULLET_COUNT old/new 28 28`.
- **Committed in:** no code change; documented here.

---

**Total deviations:** 2 auto-fixed (both Rule 2 — verification gaps, no behaviour change)
**Impact on plan:** Neither changed the deliverable; both changed what is PROVEN about it. No scope creep: no file outside the plan's `files_modified` was touched, no requirement id was invented or retired, and no status was advanced.

## Issues Encountered

None beyond the verification gap recorded above. Both documents were edited with exact-string replacements; no `sed` was used, and the line deltas are fully explained (ROADMAP +13 net from 14 insertions and 1 deletion; REQUIREMENTS unchanged at 181 lines).

## Known Stubs

None. This plan is documentation only; it wrote no code, no placeholder and no TODO. The `**Requirements**: TBD` placeholder it removed was the phase's last planning-document debt marker (03.10-VERIFICATION.md's debt-marker gate had already recorded it as GAP-E rather than a source debt marker).

## Prohibitions honoured

| Prohibition | Honoured |
|---|---|
| D-13 — no live WRDS connection | No connection opened; nothing in this plan touches `quantlab/acquisition/` |
| D-16 — no CRSP/QQQ store in `BacktestConfig.benchmark_dataset` | No code touched at all |
| SEC-01 — no hardcoded credential in a planning document | Neither edited file contains a credential value; `WRDS_USERNAME` is not even named |
| CLEAN-03 — no invented/retired id, no flipped checkbox | 34 bolded ids and 28 `(marker, id)` pairs identical to HEAD, counted above |

## User Setup Required

None.

## Next Phase Readiness

- GAP-E is closed. The phase's five gaps are now all addressed: GAP-0 (plan 12), GAP-A/GAP-B (13), GAP-C (14), GAP-D + WR-01 (15), GAP-E (16).
- `03.10-17-PLAN.md` remains — the closing gate: a main-tree no-new-failures run, the operator-run OFFLINE re-conversion proving CTLT/MRO/PXD/WRK recover on the real raw tier, and a recorded human decision on WR-05 and WR-06.
- One thing for 03.10-17 or the verifier to carry forward: DATA-03 is still `Pending`, and correctly so. If a later phase wants it Complete, the second market/frequency combination it asks for is now demonstrably in place — what is missing is Phase 2's own sign-off, not the extensibility evidence.
- Tracking writes (STATE.md, ROADMAP.md's progress table and plan counts) were deliberately NOT made; the orchestrator owns them for this phase.

## Self-Check: PASSED

- `.planning/ROADMAP.md` — present, modified, and its change is IN commit `b218b2b` (`git show --stat` confirms `.planning/ROADMAP.md | 15 ++++++++++++++-`), not stranded in the working tree.
- `.planning/REQUIREMENTS.md` — present, modified, and its change is IN commit `343fc51` (`git show --stat` confirms `.planning/REQUIREMENTS.md | 6 +++---`).
- `.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-16-SUMMARY.md` — exists on disk and is committed in the third commit on this branch.
- `git log --oneline` shows all three `docs(03.10-16):` commits on `worktree-agent-a6af468b0294293da`, based on `68dd395`.
- Working tree clean after each commit; `git diff --diff-filter=D` reports no deletions in either task commit.
- No tracking write was made: `.planning/STATE.md` is untouched, and ROADMAP.md's progress table and plan counts are unchanged (the only ROADMAP hunk is the Phase 03.10 Requirements prose).

---
*Phase: 03.10-crsp-stock-v2-daily-data-via-wrds*
*Completed: 2026-09-20*
