---
phase: "03.10"
plan: "12"
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - tests/crsp_fixtures.py
  - tests/test_crsp_dataset.py
autonomous: true
requirements: [D-08, D-09, D-10, D-19]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "tests/crsp_fixtures.py carries a MODERN CIZ delisting fixture (PERMNO 21186, 2024-07-08, dlydelflg='Y', the no-price price flag, dlyprc 0.000000, dlyret -0.005630, NULL dlyclose/dlyvol/dlycumfacpr/dlycumfacshr and NULL type columns) beside the historical Lehman row, preceded by two real-priced rows so the pre-delisting adjusted level has a hand-checkable expected value (GAP-0, D-19)"
    - "An end-to-end test builds a panel from that fixture and asserts adjClose on 2024-07-05 equals 51.51, adjClose on 2024-07-03 equals 51.51/1.035377, adjVolume is finite on both priced days, and raw close on the delisting row is NaN rather than 0.0 — and that test is RED against the current quantlab/dataset/crsp.py (GAP-0 -> GAP-A/GAP-B, D-08, D-09)"
    - "A PERMNO whose only priced rows are no-price sentinels has a RED test demanding a ValueError that names the PERMNO, instead of an all-zero adjusted column (GAP-A)"
    - "A PERMNO whose priced rows all lack dlycumfacshr has a RED test demanding a ValueError that names the PERMNO, instead of an all-NaN adjVolume column (GAP-B)"
    - "A PERMNO whose cumulative return chain reaches exactly 0.0 (a dlyret of -1.0) has a RED test demanding a ValueError instead of an infinite adjusted series (GAP-A)"
    - "A corpus-coverage test fails if the delisting fixture set ever loses either CIZ delisting shape, or loses its NULL dlycumfacshr row (GAP-0)"
    - "The rest of the CRSP offline suite (identity, symbology, membership, constituent, tracer, acquisition, ingest CLI, vendor seam) stays green while tests/test_crsp_dataset.py is deliberately red (D-13)"
  artifacts:
    - path: "tests/crsp_fixtures.py"
      provides: "the modern CIZ delisting fixture rows and their security-info intervals"
      contains: "21186"
    - path: "tests/test_crsp_dataset.py"
      provides: "the four RED tests and the corpus-coverage guard"
      contains: "21186"
  key_links:
    - from: "tests/crsp_fixtures.py modern delisting rows"
      to: "quantlab/dataset/crsp.py _derivation() close + anchor selection"
      via: "FakeCrspSession -> run_crsp_pull -> registry.convert -> the panel the test opens"
  prohibitions:
    - requirement_id: "D-13"
      category: "safety"
      statement: "MUST NOT open a live WRDS connection from an agent, an executor or a test; every fixture row is served by FakeCrspSession"
    - requirement_id: "D-16"
      category: "scope"
      statement: "MUST NOT wire any CRSP or QQQ store into BacktestConfig.benchmark_dataset"
    - requirement_id: "SEC-01"
      category: "security"
      statement: "MUST NOT hardcode a credential value; WRDS_USERNAME and ~/.pgpass are the only credential sources"
    - requirement_id: "FACTOR-04"
      category: "architecture"
      statement: "MUST NOT introduce a DataFrame as an inter-layer transport format; the panel crossing a layer boundary is an xarray.Dataset"
---

<objective>
**GAP-0, the tracer for this whole gap-closure set.** Put the delisting shape CRSP actually
writes into the fixture corpus, and hold GAP-A and GAP-B RED with it end to end, before any
line of `quantlab/dataset/crsp.py` is touched.

`tests/crsp_fixtures.py` encodes exactly one delisting shape — Lehman 2008-09-18, price flag
`DP`, `dlyprc = 0.052`, a REAL delisting price. That shape is **0 of 5** delisting rows in the
raw tier this phase actually pulled. The shape that is **5 of 5** — the delisting-AMOUNT row,
with `dlyprc = 0.0` as a no-price sentinel and `dlyclose`/`dlyvol`/`dlycumfacpr`/`dlycumfacshr`
all NULL — appears nowhere under `tests/` or `quantlab/`. That is why 179 CRSP tests pass green
over two blockers.

**This plan writes no production code and fixes nothing.** Its deliverable is a RED run whose
failure messages are the specification plan 03.10-13 implements against. A green run at the end
of this plan means the assertions do not pin the defect and the plan has failed.

Purpose: a fix that cannot be held red cannot be proved. The tracer is the one path — fixture
row -> fake session -> raw shard -> `registry.convert` -> panel — wired end to end on real
verified CRSP values, so the repair in plan 13 is measured against arithmetic a human can check
by hand rather than against a test that was written to agree with the code.

Output: the modern-delisting fixture rows and their security-info intervals in
`tests/crsp_fixtures.py`; four RED tests and one green corpus-coverage guard in
`tests/test_crsp_dataset.py`; the verbatim RED pytest output recorded in the SUMMARY.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-VERIFICATION.md
@.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-REVIEW.md
@.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-CONTEXT.md
</context>

<environment_notes>
- Python >= 3.13 under `uv`. Every command is `uv run ...` from the repository root.
- `tests/conftest.py` sets `OMP_NUM_THREADS=1` on darwin before any import. Do not undo it.
- Never `git stash`. Never open a WRDS connection: `tests/conftest.py` carries a tripwire and
  `FakeCrspSession` is the only session any test may see.
- The tests this plan adds to `tests/test_crsp_dataset.py` are RED ON PURPOSE. Do NOT mark them
  `xfail`, `skip` or `raises`-wrapped to make the file green — that would erase the evidence the
  next plan is measured against. Commit them red.
- This project's red evidence is a real `pytest` run, not node TAP. Paste the verbatim summary
  line and the failing test ids into the SUMMARY.
</environment_notes>

<tasks>

<task type="tracer">
  <name>Task 1: The modern CIZ delisting fixture, wired end to end and RED</name>

  <files>tests/crsp_fixtures.py, tests/test_crsp_dataset.py</files>

  <read_first>
    - tests/crsp_fixtures.py lines 142-310 (`_row`, `dsf_row` and its per-column defaults, `AAPL_AUG_2020_ROWS`, `LEHMAN_2008_ROWS`) — the fixture idiom, and the fact that `None` is the NULL spelling while every other default is a live-observed value
    - tests/crsp_fixtures.py lines 388-500 (`secinfo_row`, `SECINFO_ROWS`) — in particular the Lehman block's final `secinfo_row(80599, "2008-09-18", "2008-09-18", None, None, None)`, the NULL-ticker interval that makes the delisting carry fire
    - tests/test_crsp_dataset.py lines 29-200 (`_synthetic_secinfo`, `_reference_rows`, `_pull`, `_dataset_config`, `_convert`, `_panel`, `_build_store`, `_build`, `_at`) — the helper chain every test in the file uses
    - tests/test_crsp_dataset.py lines 399-470 (`test_lehman_delisting_loss_is_counted_exactly_once`) — the shape this new test mirrors for the OTHER delisting shape
    - .planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-REVIEW.md lines 110-140 (CR-01's reproduced WRK raw rows and the panel they produce) — the source of every number below
    - .planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-RESEARCH.md lines 217-240 (Q2, CIZ return and price semantics)
  </read_first>

  <action>
Add to `tests/crsp_fixtures.py`, beside `LEHMAN_2008_ROWS`, a module constant
`WESTROCK_2024_ROWS` holding WestRock (PERMNO 21186) across its 2024 delisting, built with
`dsf_row` and documented as VERIFIED against the raw tier this phase pulled (the evidence is
03.10-REVIEW.md CR-01's table), with `dlyvol` values marked as the live ones rather than the
builder's synthetic default:

- 2024-07-03 — `dlydelflg="N"`, `dlyprc="49.750000"`, `dlyprcflg="TR"`, `dlyret="0.019676"`,
  `dlyopen="49.550000"`, `dlyvol="4435075"`, `dlycumfacpr="1.000000000000"`,
  `dlycumfacshr="1.000000000000"`, `ticker="WRK"`.
- 2024-07-05 — `dlydelflg="N"`, `dlyprc="51.510000"`, `dlyprcflg="TR"`, `dlyret="0.035377"`,
  `dlyopen="50.780000"`, `dlyvol="11862010"`, `dlycumfacpr="1.000000000000"`,
  `dlycumfacshr="1.000000000000"`, `ticker="WRK"`.
- 2024-07-08 — the modern delisting row: `dlydelflg="Y"`, `dlyprc="0.000000"`,
  `dlyprcflg="DA"`, `dlyret="-0.005630"`, `dlyretmissflg="NA"`, and `None` for every one of
  `dlyclose`, `dlyvol`, `dlyopen`, `dlycumfacpr`, `dlycumfacshr`, `ticker`, `sharetype`,
  `securitytype`, `securitysubtype`, `usincflg`, `issuertype`. The NULL type columns are not
  decoration: they are what makes the delisting row exercise the D-10 filter carry, exactly as
  Lehman's row does.

Write a comment above the constant stating plainly what distinguishes the two shapes: the
delisting-PRICE flag `DP` carries a real price (Lehman, 0.052), while the delisting-AMOUNT flag
`DA` writes `dlyprc = 0.0` as a NO-PRICE sentinel with the factor and volume columns NULL, and
that the second is 5 of 5 delisting rows in the pulled tier.

Add WestRock's two security-info intervals to `SECINFO_ROWS`, mirroring the Lehman block so the
carry fires: `secinfo_row(21186, "2015-07-01", "2024-07-05", "WRK", "WRK", None)` and the
NULL-ticker delisting-day interval `secinfo_row(21186, "2024-07-08", "2024-07-08", None, None,
None)`. Mark the 2015 start as SYNTHETIC if the live projection did not sample it, following the
file's existing SYNTHETIC-annotation convention.

Then add to `tests/test_crsp_dataset.py`, in the delisting section beside the Lehman test, a
module constant `WESTROCK_PERMNO = "21186"` and a test named
`test_a_modern_delisting_keeps_a_real_adjusted_level` that builds the panel over
2024-07-01..2024-07-31 for that PERMNO alone through `_build`, and asserts:

- `adjClose` at 2024-07-05 for symbol `WRK` is approximately 51.51 — the anchor is the last
  REAL positive close, so the last priced day's adjusted level equals its raw close;
- `adjClose` at 2024-07-03 is approximately `51.51 / 1.035377`;
- neither of those two values is 0.0, asserted explicitly so a zeroed column cannot satisfy the
  approximations by accident;
- `adjOpen` at 2024-07-05 is approximately `50.78 * (51.51 / 51.51)`, i.e. 50.78, and is not 0.0;
- `adjVolume` is finite at 2024-07-03 and at 2024-07-05 (`np.isfinite`), and approximately equal
  to the raw `volume` on those days, because `dlycumfacshr` is 1.0 on both;
- raw `close` at 2024-07-08 is NaN, not 0.0 — the sentinel is not a trade;
- `ret` at 2024-07-08 is approximately -0.005630, so the fix changes the LEVEL the chain anchors
  to and never the chain itself;
- `is_delisting` at 2024-07-08 is 1.0, and the symbol column is `WRK` on that day (the carry).

Run it. It MUST fail. Record which assertion fails first and the verbatim message.
  </action>

  <verify>
    <automated>uv run pytest -q tests/test_crsp_dataset.py -k modern_delisting 2>&1 | tail -25</automated>
    <fails_when>the summary line reports "1 passed" or "0 failed", or reports "no tests ran" / a collection error — this test MUST be RED here, and a green or uncollected run means it does not pin GAP-A/GAP-B</fails_when>
    <automated>uv run python -c "import tests.crsp_fixtures as f; rows=f.WESTROCK_2024_ROWS; d=rows[-1]; print(len(rows), d['dlyprcflg'], d['dlyprc'], d['dlycumfacshr'], d['dlyvol'], d['ticker'])"</automated>
    <fails_when>non-zero exit, an AttributeError naming WESTROCK_2024_ROWS, a printed row count other than 3, or a printed dlycumfacshr/dlyvol/ticker that is not None</fails_when>
  </verify>

  <acceptance_criteria>
    - `tests/crsp_fixtures.py` defines `WESTROCK_2024_ROWS` with exactly 3 rows; the last row's `dlyprcflg` is the delisting-AMOUNT flag, its `dlyprc` is `"0.000000"`, and its `dlycumfacshr`, `dlycumfacpr`, `dlyvol`, `dlyclose` and `ticker` are all `None`
    - `tests/crsp_fixtures.py` `SECINFO_ROWS` contains two intervals for permno `21186`, the later one with a `None` ticker on 2024-07-08
    - `uv run python -c "import tests.crsp_fixtures as f; print(sum(1 for r in f.SECINFO_ROWS if r['permno']=='21186'))"` prints `2`
    - `tests/test_crsp_dataset.py` contains `def test_a_modern_delisting_keeps_a_real_adjusted_level`
    - That test is RED: the pytest summary for `-k modern_delisting` reports 1 failed, 0 passed
    - The SUMMARY quotes the verbatim first failing assertion and the pytest summary line
  </acceptance_criteria>

  <done>The delisting shape that is 5 of 5 in the real data is in the corpus, wired from fixture row to converted panel, and it fails against the shipped conversion with a message that names the wrong value.</done>
</task>

<task type="auto">
  <name>Task 2: Three RED refusal tests — no usable anchor must raise, not publish</name>

  <files>tests/test_crsp_dataset.py</files>

  <read_first>
    - tests/test_crsp_dataset.py lines 947-990 (`test_the_anchor_is_the_last_non_null_close`) — the existing anchor test, whose "non-null" wording is the exact loophole GAP-A walks through; these three tests sit beside it
    - tests/test_crsp_dataset.py lines 29-200 (the helper chain, again — `_build_store` raises from `_convert`, so a refusal test wraps `_build_store`, not `_panel`)
    - quantlab/dataset/crsp.py lines 386-460 (`_derivation`: the `close` definition, the anchor `group_by`, the `_factor` / `_volume_factor` columns) — the code these tests describe the required behaviour of
    - .planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-VERIFICATION.md gaps GAP-A and GAP-B, `missing:` lists — each bullet there is one assertion here
  </read_first>

  <action>
Add three tests to `tests/test_crsp_dataset.py`, all using `SYNTHETIC_PERMNO` / `SYNTHETIC_SYMBOL`
and `extra_secinfo=_synthetic_secinfo()`, each wrapping `_build_store(...)` in
`pytest.raises(ValueError)` and asserting the PERMNO string appears in the raised message. Each
must state in its docstring WHY a refusal is the right outcome rather than a NaN column.

1. `test_a_permno_with_only_sentinel_prices_is_refused_by_name` — two rows in 2020-03, both with
   `dlyprcflg="DA"`, `dlyprc="0.000000"`, a small non-null `dlyret`, and `dlycumfacshr` left at
   the builder default. No row carries a real price, so there is no level to anchor on; the
   conversion must refuse naming the PERMNO rather than publish an adjusted column of 0.0.

2. `test_a_permno_whose_priced_rows_lack_cumfacshr_is_refused_by_name` — two rows in 2020-03 with
   genuine positive `dlyprc` (100.00 then 102.00) and `dlycumfacshr=None` on both. Every quantity
   the anchor row is read for must be present on that row; `adjVolume` for the security's whole
   history is otherwise NaN while raw `volume` is fully populated, which is survivorship bias
   through the volume column. Also assert the message mentions the share-adjustment factor by its
   CRSP column name, so a reader of the refusal knows which column is missing.

3. `test_a_total_loss_return_chain_is_refused_rather_than_made_infinite` — three rows in 2020-03
   where the middle row carries `dlyret="-1.000000"` with a positive price, so the cumulative
   product is exactly 0.0 from that row onward and the anchor's `_G_anchor` is 0.0. Dividing by it
   yields an infinite adjusted series for the whole PERMNO under IEEE semantics. Assert the
   refusal names the PERMNO. Document in the docstring that no such row exists in the pulled data
   and that nothing in the code prevents one.

All three are RED now: the shipped conversion publishes zeros, NaN and inf respectively, and
raises nothing. Run them and record which of the three produce `DID NOT RAISE` versus another
error, because the distinction tells plan 13 whether the failure is a missing guard or a
different one.
  </action>

  <verify>
    <automated>uv run pytest -q tests/test_crsp_dataset.py -k "only_sentinel_prices or lack_cumfacshr or total_loss_return_chain" 2>&1 | tail -30</automated>
    <fails_when>the summary line reports fewer than 3 failed, or reports any passed, or reports "no tests ran" — all three MUST be RED here</fails_when>
  </verify>

  <acceptance_criteria>
    - `tests/test_crsp_dataset.py` contains `def test_a_permno_with_only_sentinel_prices_is_refused_by_name`, `def test_a_permno_whose_priced_rows_lack_cumfacshr_is_refused_by_name` and `def test_a_total_loss_return_chain_is_refused_rather_than_made_infinite`
    - Each asserts `pytest.raises(ValueError)` and that `SYNTHETIC_PERMNO` appears in `str(excinfo.value)`
    - `uv run pytest -q tests/test_crsp_dataset.py -k "only_sentinel_prices or lack_cumfacshr or total_loss_return_chain"` reports 3 failed, 0 passed
    - The SUMMARY records, per test, whether the failure was `DID NOT RAISE` or another error
  </acceptance_criteria>

  <done>Every way an adjustment anchor can be unusable now has a test demanding a named refusal, and all three are red against the shipped code.</done>
</task>

<task type="auto">
  <name>Task 3: The corpus-coverage guard, and the RED evidence record</name>

  <files>tests/test_crsp_dataset.py</files>

  <read_first>
    - tests/crsp_fixtures.py lines 142-310 (the fixture constants this guard reads back)
    - .planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-VERIFICATION.md `### Anti-Patterns Found` row for tests/crsp_fixtures.py — "fixture corpus covers the historical shape, not the current one" is the regression this guard exists to catch
    - .planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-BASELINE-FAILURES.txt — the failing-id baseline the rest of the suite is measured against
  </read_first>

  <action>
Add `test_the_delisting_fixture_corpus_covers_both_ciz_shapes` to `tests/test_crsp_dataset.py`.
It imports `LEHMAN_2008_ROWS` and `WESTROCK_2024_ROWS` from `tests.crsp_fixtures`, collects every
row whose `dlydelflg` is `"Y"`, and asserts:

- the set of `dlyprcflg` values across those rows contains both the delisting-PRICE flag and the
  delisting-AMOUNT flag, so neither shape can be dropped without a failure here;
- at least one delisting row has `dlyprc` equal to `"0.000000"`;
- at least one delisting row has `dlycumfacshr` of `None`;
- at least one delisting row has a `dlyprc` that parses to a strictly positive float.

Its docstring states the fact this guard encodes: the corpus previously carried one shape, that
shape was 0 of 5 delisting rows in the pulled tier, and two blockers shipped green because of it.
This test is GREEN as soon as Task 1 lands — it is the durable guard, not part of the red set.

Then run the rest of the CRSP offline suite EXCLUDING `tests/test_crsp_dataset.py` (which is red
by design in this plan) and confirm it is unchanged, so the fixture additions have no collateral
blast radius. Record in the SUMMARY: the verbatim pytest summary line for the red file, the
verbatim summary line for the rest of the CRSP suite, and the four RED test ids listed in the
order plan 03.10-13 must turn them green.
  </action>

  <verify>
    <automated>uv run pytest -q tests/test_crsp_dataset.py -k fixture_corpus_covers</automated>
    <fails_when>non-zero exit, "no tests ran" in the output, or a summary line that does not report 1 passed</fails_when>
    <automated>uv run pytest -q tests/test_crsp_identity.py tests/test_crsp_symbology.py tests/test_crsp_membership.py tests/test_crsp_constituent.py tests/test_crsp_tracer.py tests/test_crsp_reference_tables.py tests/test_wrds_crsp_acquisition.py tests/test_ingest_wrds_crsp.py tests/test_wrds_vendor_seam.py</automated>
    <fails_when>non-zero exit, or any failed/errored count in the summary line</fails_when>
    <automated>uv run pytest -q tests/test_crsp_dataset.py 2>&1 | tail -15</automated>
    <fails_when>the summary line reports 0 failed, or reports more than 4 failed — exactly the four tests this plan authored are expected red, and a fifth means the fixture additions broke something that was passing</fails_when>
  </verify>

  <acceptance_criteria>
    - `tests/test_crsp_dataset.py` contains `def test_the_delisting_fixture_corpus_covers_both_ciz_shapes` and it passes
    - The nine other CRSP suites pass with 0 failed and 0 errors
    - `uv run pytest -q tests/test_crsp_dataset.py` reports exactly 4 failed
    - The SUMMARY lists the four RED test ids verbatim, plus both pytest summary lines
    - No test in the four is marked `xfail`, `skip` or otherwise neutralised — `grep -c "xfail\|@pytest.mark.skip" tests/test_crsp_dataset.py` returns the same count as before this plan
  </acceptance_criteria>

  <done>The corpus can never silently lose a delisting shape again, the rest of the suite is proven unaffected, and the four red ids plan 13 must close are written down.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process -> WRDS PostgreSQL | MUST NOT be crossed; `FakeCrspSession` serves every row and `tests/conftest.py` carries the tripwire |
| fixture file -> repository | fixture rows are committed data and must carry no credential value |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.10-41 | Spoofing | tests/crsp_fixtures.py | low | mitigate | new rows go through `dsf_row`/`secinfo_row`, which raise `KeyError` on an unknown column, so a fabricated column cannot enter the corpus unnoticed |
| T-03.10-42 | Information Disclosure | tests/crsp_fixtures.py | medium | mitigate | the added rows are market data only; no username, password or `~/.pgpass` content appears, and the WRDS tripwire in `tests/conftest.py` stays untouched |
| T-03.10-43 | Repudiation | the RED evidence | medium | mitigate | the SUMMARY quotes the verbatim pytest summary line and the four failing ids, so "it was red" is a recorded fact rather than a claim |
| T-03.10-SC | Tampering | npm/pip/cargo installs | high | accept | this plan installs no package; RESEARCH.md's Package Legitimacy Audit is unchanged and no new dependency enters `pyproject.toml` |
</threat_model>

<verification>
- `tests/test_crsp_dataset.py` reports exactly 4 failed, and those four are the ids this plan authored.
- `tests/test_crsp_dataset.py -k fixture_corpus_covers` reports 1 passed.
- The nine other CRSP suites report 0 failed, 0 errors.
- No WRDS connection was opened; `FakeCrspSession` served every row.
</verification>

<success_criteria>
The CIZ delisting-AMOUNT shape is in the fixture corpus with hand-checkable values, four tests
demand the behaviour GAP-A and GAP-B violate, all four are red, the guard against losing either
shape is green, and the rest of the CRSP suite is unaffected.
</success_criteria>

## Artifacts this phase produces (this plan's share)

New symbols and paths created here:

- `tests/crsp_fixtures.py` — `WESTROCK_2024_ROWS` (module constant, 3 `dsf_row` records); two new
  `secinfo_row` entries for permno `21186` inside `SECINFO_ROWS`
- `tests/test_crsp_dataset.py` — `WESTROCK_PERMNO` (module constant);
  `test_a_modern_delisting_keeps_a_real_adjusted_level`;
  `test_a_permno_with_only_sentinel_prices_is_refused_by_name`;
  `test_a_permno_whose_priced_rows_lack_cumfacshr_is_refused_by_name`;
  `test_a_total_loss_return_chain_is_refused_rather_than_made_infinite`;
  `test_the_delisting_fixture_corpus_covers_both_ciz_shapes`
- No new production symbol, no new config field, no new CLI flag, no new dependency

<output>
Create `.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-12-SUMMARY.md` when done
</output>
