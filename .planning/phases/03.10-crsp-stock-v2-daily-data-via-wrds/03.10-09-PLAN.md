---
phase: "03.10"
plan: "09"
type: execute
wave: 4
depends_on: ["03.10-05", "03.10-06", "03.10-07"]
files_modified:
  - quantlab/dataset/crsp_membership.py
  - quantlab/dataset/constituent.py
  - tests/test_crsp_constituent.py
autonomous: true
requirements: [D-04, D-05, D-14]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "CrspMembership.symbol_intervals(index) maps PERMNO membership onto the SAME CrspSymbology intervals the price panel uses, so a member PERMNO's symbol is period-correct: 13407 is a member as FB through 2022-06-08 and as META from 2022-06-09; 83443 as BRK.B (D-04, D-05)"
    - "CrspSP500ConstituentDataset and CompustatNasdaq100ConstituentDataset are IndexConstituentDataset subclasses implementing exactly the two hooks: coverage starts 1925-12-31 and 1995-01-01; intervals come from the reference directory given as ConstituentDatasetConfig.cache_dir (D-05, D-14)"
    - "Their is_member panels end at the CRSP product end (2025-12-31), never at the wall clock, because every interval end is explicit (D-05, D-14)"
    - "For the same reference set and PERMNOs, every symbol in a CRSP membership panel is a symbol the CRSP price panel produces for that PERMNO on those dates (D-04, D-05)"
    - "Unlinked Nasdaq-100 spells stop the membership panel build unless ConstituentDatasetConfig.kwargs['allow_unlinked'] is True (D-14)"
  artifacts:
    - path: "quantlab/dataset/crsp_membership.py"
      provides: "CrspMembership.symbol_intervals"
      contains: "def symbol_intervals"
    - path: "quantlab/dataset/constituent.py"
      provides: "CrspSP500ConstituentDataset, CompustatNasdaq100ConstituentDataset"
      contains: "class CompustatNasdaq100ConstituentDataset"
    - path: "tests/test_crsp_constituent.py"
      provides: "symbol-interval, panel-edge, price/membership agreement, unlinked and round-trip tests"
  key_links:
    - from: "quantlab/dataset/constituent.py::CrspSP500ConstituentDataset._build_intervals"
      to: "quantlab/dataset/crsp_membership.py::CrspMembership.symbol_intervals"
      via: "CrspReference(config.cache_dir)"
    - from: "quantlab/dataset/crsp_membership.py::CrspMembership.symbol_intervals"
      to: "quantlab/dataset/crsp_symbology.py::CrspSymbology.symbol_intervals"
      via: "the one interval rule shared with the price panel"
  prohibitions:
    - requirement_id: "D-05"
      category: "scope"
      status: "resolved"
      verification: "test"
      resolution: null
      reason: null
      statement: "MUST NOT modify quantlab/acquisition/universe.py, quantlab/base/constituent.py or the existing Wikipedia-based constituent datasets"
---

<objective>
Expose the two CRSP-vendor universes as symbol-level point-in-time membership panels —
the same `is_member` Zarr shape the existing Wikipedia-based universes produce — so the
existing universe mask machinery can be pointed at them, and prove their symbols agree
with the CRSP price panel's symbols.

**Sequencing:** execute only after phase 03.9 is fully merged and plans 03.10-05, -06 and
-07 are merged. Runs in the same wave as plan 08 (disjoint files).

Purpose: D-05/D-14 universes must be usable beside the CRSP prices; a mask whose tickers
differ from the price panel's would silently hold nothing.

Output: `CrspMembership.symbol_intervals`, the two constituent dataset classes, and
`tests/test_crsp_constituent.py`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-CONTEXT.md
@.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-RESEARCH.md
@.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-05-SUMMARY.md
@.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-07-SUMMARY.md
</context>

<environment_notes>
- Run from YOUR working-tree root: `uv run pytest -q tests/test_crsp_constituent.py`. Never `cd` into the main checkout. `-k` matching nothing exits 5 = failure. Never `git stash`.
- Reference sets via `tests/crsp_fixtures.py::write_reference_tables` (read-only here); synthetic rows (e.g. 13407 S&P membership from 2013-12-23, GOOG class C 14542 secinfo) live in the test module with `# SYNTHETIC`.
- The price-panel agreement test builds a CRSP price store with `mock_crsp_session` + `run_crsp_pull` + `registry.convert(..., data_type="crsp_daily")`.
</environment_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Symbol-level membership intervals from the shared symbology</name>

  <files>quantlab/dataset/crsp_membership.py, tests/test_crsp_constituent.py</files>

  <read_first>
    - quantlab/dataset/crsp_membership.py (plan 07)
    - quantlab/dataset/crsp_symbology.py (plan 05: `symbol_intervals`, `report`)
    - quantlab/base/constituent.py lines 130-283 (`_densify` input contract: `(symbol, start_date, end_date)`, closed, null end = open)
  </read_first>

  <behavior>
    - SYNTHETIC S&P membership of 13407 2013-12-23..2025-12-31 with verbatim FB/META secinfo -> rows (FB, 2013-12-23, 2022-06-08) and (META, 2022-06-09, 2025-12-31).
    - Verbatim 83443 secinfo + SYNTHETIC S&P membership 2010-02-16..2025-12-31 -> (BRK.B, 2010-02-16, 2025-12-31).
    - Nasdaq-100 Alphabet (verbatim IDXCST/CCM rows, verbatim 90319 secinfo, SYNTHETIC 14542 secinfo GOOG class C from 2014-04-03) -> GOOG 2005-12-21..2025-12-31 (90319 then 14542, merged per symbol because they are contiguous) and GOOGL 2014-04-03..2025-12-31.
    - A member PERMNO with no symbol on part of its membership -> that piece is dropped and listed in `report["unlabelled_members"]` (permno, start, end), with a loguru warning.
    - Every end_date is explicit and <= the product end.
    - Agreement: for a raw tier with daily rows of 13407 around 2022-06-08/09 and 83443 in 2020, the set of (date, symbol) pairs where the membership intervals say "member" and the price panel has a row are identical for those PERMNOs (no member symbol absent from the price panel on a trading day it has data).
  </behavior>

  <action>
Tests first. Add `symbol_intervals(index, *, allow_unlinked=False) -> pl.DataFrame` to `CrspMembership`: PERMNO intervals from `permno_intervals(...)` intersected per PERMNO with `CrspSymbology(self.reference.table("stksecurityinfohist")).symbol_intervals()` (no overrides — overrides are a price-store option, not a universe fact); keep non-empty overlaps as (symbol, start_date, end_date); record pieces with a null symbol in `report["unlabelled_members"]` and drop them with a warning; merge overlapping or touching intervals per symbol; return sorted by (symbol, start_date) with Date dtypes and explicit ends. Docstring: symbol-level membership is what the mask needs; a ticker held by two PERMNOs in turn stays one continuous membership, and the price panel marks the PERMNO switch as a seam (D-18).
  </action>

  <verify>
    <automated>uv run pytest -q tests/test_crsp_constituent.py -k "symbol_interval or rename or share_class or googl or unlabelled or agree"</automated>
    <fails_when>non-zero exit, exit 5 with "deselected", or fewer than 5 passed in the summary line</fails_when>
  </verify>

  <acceptance_criteria>
    - The selector above reports at least 5 passed and 0 failed
    - `quantlab/dataset/crsp_membership.py` contains `def symbol_intervals` and `unlabelled_members`
  </acceptance_criteria>

  <done>Membership is expressed in the price panel's own tickers, renames and share classes included.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CrspSP500ConstituentDataset and CompustatNasdaq100ConstituentDataset</name>

  <files>quantlab/dataset/constituent.py, tests/test_crsp_constituent.py</files>

  <read_first>
    - quantlab/dataset/constituent.py (whole file: the two-hook pattern)
    - quantlab/base/constituent.py lines 70-128 (config setter clamp to `_pit_coverage_start`, `_clean`)
    - quantlab/base/config.py (`ConstituentDatasetConfig`: `cache_dir`, `as_of`, `kwargs`)
    - quantlab/utils/module.py lines 47-75 (`load_dataset_from_config`)
    - tests/test_ndx_constituent.py (how an existing constituent panel is tested end to end)
  </read_first>

  <behavior>
    - `CrspSP500ConstituentDataset(ConstituentDatasetConfig(zarr_file_path=..., cache_dir=<reference dir>, start_date="2022-06-01", end_date="2022-06-30"))`; `from_raw_data()` gives `is_member` with FB True on 2022-06-08 and False on 2022-06-09, META the reverse.
    - With the default start/end, the panel's first timestamp is 1925-12-31 (clamp) and its last is 2025-12-31 (the product end, not today) — assert against `pd.Timestamp("2025-12-31")` without depending on the wall clock.
    - `CompustatNasdaq100ConstituentDataset` clamps a requested start of 1990-01-01 to 1995-01-01 and has GOOG and GOOGL members on 2015-01-02.
    - A reference set with an unlinked Nasdaq-100 spell -> `from_raw_data()` raises ValueError naming the spell; with `kwargs={"allow_unlinked": True}` it builds.
    - `load_dataset_from_config(json.loads(json.dumps(ds.get_config())))` rebuilds each class with an equal config.
    - The two Wikipedia-based classes in the module are unchanged (their existing tests still pass).
  </behavior>

  <action>
Tests first. Append to `quantlab/dataset/constituent.py` two classes following the existing two-hook pattern exactly:
- `CrspSP500ConstituentDataset(IndexConstituentDataset)`: `INDEX = CrspMembership.SP500`; `_pit_coverage_start()` returns `CrspMembership.PIT_COVERAGE_START[INDEX]`; `_build_intervals()` returns `CrspMembership(CrspReference(self.config.cache_dir)).symbol_intervals(INDEX, allow_unlinked=bool((self.config.kwargs or {}).get("allow_unlinked", False)))`. Docstring: CRSP's own point-in-time S&P 500 membership (dsp500list_v2), coverage from 1925-12-31, ends bounded by the CRSP annual product end; `cache_dir` is the CRSP reference directory; tickers are the CRSP price panel's.
- `CompustatNasdaq100ConstituentDataset(IndexConstituentDataset)`: `INDEX = CrspMembership.NASDAQ100`, same hooks; docstring: Compustat idxcst_his gvkeyx 000208 linked through CCM on gvkey + iid, left-censored at 1995-01-01 (12 years earlier than the Wikipedia Nasdaq-100 panel's 2007-02-01), GOOG/GOOGL both members.
Import `CrspMembership` and `CrspReference` at the module top (both are dataset-layer pure modules, so no acquisition client is imported). Do not touch the existing classes.
  </action>

  <verify>
    <automated>uv run pytest -q tests/test_crsp_constituent.py tests/test_constituent_panel.py tests/test_ndx_constituent.py tests/test_config_roundtrip.py tests/test_dataset_hierarchy.py</automated>
    <fails_when>non-zero exit, or any failed/errored test in the summary line, or fewer than 10 passed in tests/test_crsp_constituent.py</fails_when>
  </verify>

  <acceptance_criteria>
    - `uv run pytest -q tests/test_crsp_constituent.py` exits 0 with at least 10 passed
    - `uv run python -c "from quantlab.dataset.constituent import CrspSP500ConstituentDataset as S, CompustatNasdaq100ConstituentDataset as N; from quantlab.base.constituent import IndexConstituentDataset as B; print(issubclass(S,B), issubclass(N,B))"` prints `True True`
  </acceptance_criteria>

  <done>Both CRSP-vendor universes exist as ordinary constituent panels, bounded by CRSP coverage, in the price panel's tickers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| membership panel -> universe mask -> model/backtest | a wrong or future-extended mask biases every downstream result |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.10-30 | Tampering | look-ahead via membership extended to today | medium | mitigate | every interval end explicit and <= product end; test pins the last timestamp to 2025-12-31 |
| T-03.10-31 | Tampering | mask/price ticker mismatch | medium | mitigate | one shared CrspSymbology rule; agreement test over the same reference and raw tier |
| T-03.10-32 | Tampering | unlinked NDX spells silently missing | medium | mitigate | membership build raises unless allow_unlinked is set explicitly in config kwargs |
</threat_model>

<verification>
- `uv run pytest -q tests/test_crsp_constituent.py tests/test_constituent_panel.py tests/test_ndx_constituent.py` passes.
</verification>

<success_criteria>
The CRSP S&P 500 and Compustat Nasdaq-100 universes are available as is_member panels that line up symbol for symbol with CRSP prices.
</success_criteria>

## Artifacts this phase produces (this plan's share)
- `quantlab/dataset/crsp_membership.py`: `CrspMembership.symbol_intervals()`, `report["unlabelled_members"]`
- `quantlab/dataset/constituent.py`: `CrspSP500ConstituentDataset`, `CompustatNasdaq100ConstituentDataset`
- `tests/test_crsp_constituent.py`

<output>
Create `.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-09-SUMMARY.md` when done
</output>
