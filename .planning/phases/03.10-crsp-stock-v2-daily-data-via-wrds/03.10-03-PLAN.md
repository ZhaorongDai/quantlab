---
phase: "03.10"
plan: "03"
type: execute
wave: 2
depends_on: ["03.10-01"]
files_modified:
  - quantlab/acquisition/wrds_crsp_reference.py
  - tests/test_crsp_reference_tables.py
autonomous: true
requirements: [D-02, D-03, D-11, D-14, D-19]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "CrspReferenceTables pulls the whole small CRSP tables crsp_a_stock.stksecurityinfohist, stkdelists, stkdistributions and crsp_a_indexes.dsp500list_v2 into downloads/us_equity/1d/wrds_crsp/_reference/{name}.parquet — a sibling of the raw root, never under it — with the pinned live-check columns and dtypes, and a manifest.json recording product_end, pulled_at and per-table rows (D-02, D-11)"
    - "Delisting and distribution events are stored raw and complete: the Lehman stkdelists row (delret -0.6, deldlydt 2008-09-18) and AAPL's 1987 distributions round-trip through CrspReference unchanged (D-11, D-19)"
    - "With include_nasdaq100, comp.idxcst_his is pulled for gvkeyx '000208' with the reserved column quoted as an identifier, and crsp_a_ccm.ccmxpf_lnkhist is pulled for exactly the gvkeys that idxcst_his returned (D-14, D-19)"
    - "Entitlement for every schema the pull needs (crsp_a_stock; crsp_a_indexes for S&P; comp and crsp_a_ccm for Nasdaq-100) is checked before any count or COPY; an S&P-only pull never probes comp or crsp_a_ccm (D-03, D-14)"
    - "Every table is counted first, refused above MAX_REFERENCE_ROWS before its COPY, and its COPY must return exactly that many rows; each parquet file lands atomically and the manifest is written last, so a pull interrupted mid-way leaves no manifest claiming tables it did not finish (D-03)"
    - "A second pull for the same product end with every requested file present issues no query; a different product end or refresh=True re-pulls (D-02)"
    - "Every reference query is COPY (SELECT ... FROM <schema>.<table> [WHERE ...]) with no ORDER BY / GROUP BY / DISTINCT / LIMIT (D-03)"
  artifacts:
    - path: "quantlab/acquisition/wrds_crsp_reference.py"
      provides: "CrspReferenceTables.pull, MAX_REFERENCE_ROWS, NDX_GVKEYX"
      contains: "class CrspReferenceTables"
    - path: "tests/test_crsp_reference_tables.py"
      provides: "whole-table, vintage-skip, atomicity, ceiling, NDX/CCM, entitlement and SQL-shape tests"
  key_links:
    - from: "quantlab/acquisition/wrds_crsp_reference.py::CrspReferenceTables"
      to: "quantlab/dataset/crsp_reference.py::REFERENCE_TABLES / MANIFEST_NAME"
      via: "the same pinned spec (columns, dtypes, cast) the reader and the test fixture use"
    - from: "quantlab/acquisition/wrds_crsp_reference.py::CrspReferenceTables"
      to: "quantlab/acquisition/wrds_crsp.py::CrspQueries"
      via: "copy_query / count_query / assert_entitled over the one shared WrdsSession"
  prohibitions:
    - requirement_id: "D-10"
      category: "correctness"
      status: "resolved"
      verification: "test"
      resolution: null
      reason: null
      statement: "MUST NOT derive or merge anything from stkdelists into returns here; the table is stored raw as event data only"
    - requirement_id: "D-03"
      category: "safety"
      status: "resolved"
      verification: "test"
      resolution: null
      reason: null
      statement: "MUST NOT write any file under the daily raw root (a stray non-shard file there breaks the raw scan)"
---

<objective>
Acquire the small CRSP / Compustat / CCM reference tables the rest of the phase reads:
security-info history (the PERMNO -> ticker/share-class source for D-04), delisting and
distribution events (D-11), CRSP's S&P 500 membership (D-05), and, on request, the
Compustat Nasdaq-100 constituent history plus its CCM links (D-14). Each is a whole-table
(or one-predicate) COPY into a fixed-name `_reference/` directory beside the raw root,
with a vintage manifest.

**Sequencing:** execute only after phase 03.9 is fully merged and plan 03.10-01 is merged.
Uses `CrspQueries` from `quantlab/acquisition/wrds_crsp.py` without changing it (plan 02
edits that file in the same wave).

Purpose: the symbology, universes and dataset read these files; the CLI pulls them once
per vintage before resolving its roster.

Output: `quantlab/acquisition/wrds_crsp_reference.py` and its tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-CONTEXT.md
@.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-RESEARCH.md
@.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-01-SUMMARY.md
</context>

<environment_notes>
- Run from YOUR working-tree root: `uv run pytest -q tests/<file>.py`. Never `cd` into the main checkout.
- A `-k` selector matching nothing exits 5: treat as failure. Never `git stash`.
- Use `mock_crsp_session` and `FakeCrspSession.reference_rows` / `usable_schemas` / `raise_on_copy` / `crsp_copy_calls` / `crsp_count_calls` from `tests/crsp_fixtures.py`; do NOT edit that file (other wave-2 plans read it). Scenario rows go in the test module.
- Live facts: row estimates stksecurityinfohist 191,048; stkdelists 29,833; stkdistributions 1,101,681; dsp500list_v2 2,084; idxcst_his for 000208 has 523 spells; CCM `lpermno`/`lpermco` are double precision (03.10-LIVE-CHECK*.json).
</environment_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Whole-table pulls into _reference/ with manifest, ceiling, atomic files and vintage skip</name>

  <files>quantlab/acquisition/wrds_crsp_reference.py, tests/test_crsp_reference_tables.py</files>

  <read_first>
    - quantlab/dataset/crsp_reference.py (plan 01: `ReferenceTableSpec`, `REFERENCE_TABLES`, `MANIFEST_NAME`, `CrspReference`)
    - quantlab/acquisition/wrds_crsp.py (plan 01: `CrspQueries.copy_query/count_query/assert_entitled/count/copy`, `WrdsCrspDailyAcquisition.reference_dir_for`)
    - quantlab/utils/atomic.py (`write_json_atomically`, the tmp-in-same-dir + os.replace idiom)
    - quantlab/dataset/stock.py lines 384-430 (the raw scan globs `**/*.pqt` under the raw root; why nothing else may live there)
    - tests/crsp_fixtures.py (`DELISTS_ROWS`, `DISTRIBUTION_ROWS`, `DSP500_ROWS`, `SECINFO_ROWS`, `write_reference_tables` — the manifest shape to match)
    - .planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-RESEARCH.md § Pattern 3 (reference tables are not an Acquisition)
  </read_first>

  <behavior>
    - `CrspReferenceTables(session, ref_dir).pull(product_end=date(2025, 12, 31))` (S&P default, no Nasdaq-100) writes exactly `stksecurityinfohist.parquet`, `stkdelists.parquet`, `stkdistributions.parquet`, `dsp500list_v2.parquet` and `manifest.json`; `ref_dir == WrdsCrspDailyAcquisition.reference_dir_for(<a build_config config>)`, and nothing appears under the raw root.
    - Dtypes follow the spec: `permno` Int64, `secinfostartdt` Date, `delret` Float64, `deldlydt` Date, `disdivamt` Float64, `mbrenddt` Date; row counts equal the fake's rows.
    - `CrspReference(ref_dir).table("stkdelists")` holds the Lehman row with `delret == -0.6` and `deldlydt == 2008-09-18`; `table("stkdistributions")` holds the three AAPL 1987 rows unchanged (one with a null `disdivamt`).
    - `manifest.json` == {"product_end": "2025-12-31", "pulled_at": <ISO UTC>, "tables": {name: {"schema", "table", "rows", "where"}}} with the four names.
    - A second `pull` with the same product end issues zero count and zero COPY calls; `refresh=True` re-pulls all four; `product_end=date(2026, 12, 31)` re-pulls all four and the manifest's product end becomes 2026-12-31.
    - `monkeypatch.setattr(CrspReferenceTables, "MAX_REFERENCE_ROWS", 2)` -> ValueError naming the first table over 2 rows, its count and the ceiling, raised before that table's COPY.
    - `raise_on_copy = {2: RuntimeError("boom")}` (third table) -> the error propagates; `manifest.json` is absent (fresh dir) or still the previous one; the next `pull` re-pulls every table.
  </behavior>

  <action>
Tests first. Create `quantlab/acquisition/wrds_crsp_reference.py`:
- Module docstring: why these are not an `Acquisition` (whole small tables, not symbol-keyed time series; RESEARCH Pattern 3), where they live (`reference_dir_for`, sibling of the raw root because the raw scan walks every file under it), and that `stkdelists` is stored as event data only — delisting returns are already in `dlyret` (D-10/D-19).
- `class CrspReferenceTables`: `__init__(self, session, reference_dir)`; class constants `MAX_REFERENCE_ROWS = 5_000_000`, `STOCK_TABLES = ("stksecurityinfohist", "stkdelists", "stkdistributions")`, `SP500_TABLES = ("dsp500list_v2",)`, `NASDAQ100_TABLES = ("idxcst_his", "ccmxpf_lnkhist")`, `NDX_GVKEYX = "000208"` (comp.idx_index "Nasdaq 100", D-14).
- `pull(self, *, product_end: date, include_sp500: bool = True, include_nasdaq100: bool = False, refresh: bool = False) -> dict`:
  1. requested = STOCK_TABLES + (SP500_TABLES if include_sp500) + (NASDAQ100_TABLES if include_nasdaq100);
  2. read the existing manifest (if any); when its `product_end` equals `product_end.isoformat()`, every requested table is listed in it and its file exists, and not `refresh` -> return it without any query;
  3. `CrspQueries.assert_entitled` over the schemas the requested tables live in (from their specs);
  4. when the old manifest's product end differs, start from an empty table map (old files are overwritten or no longer listed); otherwise keep the old entries;
  5. for each requested table in order: build its WHERE (none for the stock and S&P tables; this task leaves the two Nasdaq-100 tables to Task 2), `CrspQueries.count` -> refuse above `MAX_REFERENCE_ROWS` with a ValueError naming schema.table, rows and the ceiling; `CrspQueries.copy` with `spec.columns`; `pl.read_csv(infer_schema=False)`; header must equal `spec.columns` and row count must equal the count (ValueError naming both); `spec.cast`; write to a temp file in `reference_dir` then `os.replace` onto `{name}.parquet`;
  6. finally write the manifest with `write_json_atomically` — LAST, so a crash never leaves a manifest listing an unfinished table.
- The `where` recorded in the manifest is a human-readable description (e.g. `"gvkeyx = '000208'"`), never a rendered SQL string that needs a connection.
  </action>

  <verify>
    <automated>uv run pytest -q tests/test_crsp_reference_tables.py -k "whole or dtype or events or manifest or skip or refresh or vintage or ceiling or atomic or raw_root"</automated>
    <fails_when>non-zero exit, exit 5 with "deselected", or fewer than 7 passed in the summary line</fails_when>
  </verify>

  <acceptance_criteria>
    - The selector above reports at least 7 passed and 0 failed
    - `quantlab/acquisition/wrds_crsp_reference.py` contains `MAX_REFERENCE_ROWS` and `write_json_atomically`
  </acceptance_criteria>

  <done>The four stock/S&P reference tables land complete, typed and atomically beside the raw tier, once per vintage, with a manifest that never over-claims.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Nasdaq-100 constituents (comp.idxcst_his) and CCM links, entitlement scope and SQL shape</name>

  <files>quantlab/acquisition/wrds_crsp_reference.py, tests/test_crsp_reference_tables.py</files>

  <read_first>
    - quantlab/acquisition/wrds_crsp_reference.py (Task 1)
    - .planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-LIVE-CHECK-2.json keys `L7_2`, `L7_4`, `L8_1`, `L8_2`, `L8_3`, `L8_4` (columns and Alphabet rows; `from` is a reserved word)
    - .planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-RESEARCH.md § D-14 and Pitfall 8
    - tests/crsp_fixtures.py (`IDXCST_ROWS`, `CCM_ROWS`, the fake's WHERE parser)
  </read_first>

  <behavior>
    - `pull(product_end=..., include_nasdaq100=True)` also writes `idxcst_his.parquet` (Alphabet rows: gvkey 160329, iid 01 from 2005-12-21 and iid 03 from 2014-04-03, `thru` null) and `ccmxpf_lnkhist.parquet` (the five L7_4 rows, `lpermno` Float64, `linkenddt` null where open).
    - The rendered idxcst_his COPY contains `"gvkeyx" = '000208'` and selects `"from"` as a quoted identifier; the rendered CCM COPY contains `"gvkey" = ANY(ARRAY['160329'])`, i.e. exactly the gvkeys idxcst_his returned.
    - With `usable_schemas = {"crsp_a_stock", "crsp_a_indexes"}`, an S&P-only pull succeeds and `schema_probes` contains neither "comp" nor "crsp_a_ccm"; the same knob with `include_nasdaq100=True` raises `WrdsEntitlementError` naming `comp` and `crsp_a_ccm` before any COPY.
    - `reference_rows` with no idxcst_his rows for 000208 -> ValueError naming gvkeyx 000208, and no CCM query is issued.
    - No rendered reference COPY or count contains `ORDER BY`, `GROUP BY`, `DISTINCT` or `LIMIT`; each count's WHERE equals its COPY's WHERE.
    - An S&P-only manifest followed by a Nasdaq-100 pull for the same product end pulls only the two new tables and the manifest lists all six.
  </behavior>

  <action>
Tests first. Extend `CrspReferenceTables.pull`:
- idxcst_his WHERE: `sql.Identifier("gvkeyx") = sql.Literal(NDX_GVKEYX)`; the `from` column is only ever selected through `sql.Identifier` (the spec's column list), which quotes it (Pitfall 8).
- After idxcst_his is written, collect its distinct `gvkey` values (sorted); an empty set raises ValueError naming gvkeyx 000208 and stops before any CCM query; the CCM WHERE is `sql.Identifier("gvkey") = ANY(sql.Literal(<list of str>))`.
- Entitlement is computed from exactly the requested tables' schemas, so an S&P-only pull never asks about comp/crsp_a_ccm.
- The incremental case (same product end, new tables requested) keeps the existing entries and pulls only the missing ones.
  </action>

  <verify>
    <automated>uv run pytest -q tests/test_crsp_reference_tables.py</automated>
    <fails_when>non-zero exit, or fewer than 13 passed in the summary line</fails_when>
  </verify>

  <acceptance_criteria>
    - `uv run pytest -q tests/test_crsp_reference_tables.py` exits 0 with at least 13 passed
    - `uv run pytest -q tests/test_crsp_reference_tables.py -k "nasdaq or ccm or entitle or sql"` exits 0 with at least 4 passed
  </acceptance_criteria>

  <done>The Nasdaq-100 constituent history and its CCM links are pulled only when asked for, only for the linked gvkeys, with the reserved column quoted and entitlement checked first.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WRDS -> `_reference/` | whole-table COPY output becomes parquet files the dataset trusts |
| idxcst_his gvkeys -> CCM SQL | server-returned values are fed back into a query |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.10-12 | Tampering | partially written reference set | medium | mitigate | per-file tmp + os.replace; manifest written last; count check per table |
| T-03.10-13 | Tampering | gvkey values reused in SQL | high | mitigate | passed only as `sql.Literal` list; identifiers via `sql.Identifier`; reserved `from` quoted by Identifier |
| T-03.10-14 | Denial of Service (self) | unexpectedly large reference table | medium | mitigate | count(*) first, refuse above MAX_REFERENCE_ROWS before the COPY |
| T-03.10-15 | Information Disclosure | driver errors | high | mitigate | every query through `WrdsSession._query` (username scrub); no credential in the manifest |
</threat_model>

<verification>
- `uv run pytest -q tests/test_crsp_reference_tables.py tests/test_crsp_tracer.py` passes.
- No file under the raw root after any reference pull (asserted in the tests).
</verification>

<success_criteria>
The six reference tables are available as typed parquet with a vintage manifest, pulled safely and only as far as the requested universes need.
</success_criteria>

## Artifacts this phase produces (this plan's share)
- `quantlab/acquisition/wrds_crsp_reference.py`: `CrspReferenceTables` (`MAX_REFERENCE_ROWS`, `STOCK_TABLES`, `SP500_TABLES`, `NASDAQ100_TABLES`, `NDX_GVKEYX`, `pull()`)
- `tests/test_crsp_reference_tables.py`
- On disk (runtime): `downloads/us_equity/1d/wrds_crsp/_reference/{stksecurityinfohist,stkdelists,stkdistributions,dsp500list_v2,idxcst_his,ccmxpf_lnkhist}.parquet` + `manifest.json`

<output>
Create `.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-03-SUMMARY.md` when done
</output>
