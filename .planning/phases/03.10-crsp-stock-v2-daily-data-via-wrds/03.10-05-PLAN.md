---
phase: "03.10"
plan: "05"
type: execute
wave: 2
depends_on: ["03.10-01"]
files_modified:
  - quantlab/dataset/crsp.py
  - tests/test_crsp_dataset.py
autonomous: true
requirements: [D-07, D-08, D-09, D-10, D-11, D-19]

estimate:
  tokens: 105000
  raw_tokens: 105000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The CRSP panel carries Tiingo's twelve variables plus the CRSP extras permno, permco, ret, retx, shrout (shares), market_cap (USD), bid, ask, prc_is_bidask, is_delisting, numtrd, cumfacpr, cumfacshr, facprc and close_trade, every one float64; close = abs(dlyprc) and close_trade = dlyclose (D-07, D-09)"
    - "A bid/ask-midpoint day (dlyprcflg 'BA', AAPL 1980-12-12) has close 28.8125 and prc_is_bidask 1.0; a missing return (dlyret NULL with dlyretmissflg NS or MP) is NaN in ret, never 0, and contributes a factor of 1 to the adjustment chain (D-09)"
    - "Delisting losses are in the series exactly once: for Lehman (80599) is_delisting is 1.0 only on 2008-09-18, ret there is -0.6, and adjClose(2008-09-18)/adjClose(2008-09-15) == (1+0.428571)(1-0.566667)(1-0.6) — stkdelists.delret is never applied on top (D-10, D-19)"
    - "Events surface on their dates: divCash = dlyorddivamt + dlynonorddivamt on the ex-date (AAPL 2020-08-07 = 0.82), splitFactor = previous dlycumfacpr / dlycumfacpr (AAPL 2020-08-31 = 4.0, 1.0 on ordinary days), facprc = dlyfacprc (D-11)"
    - "The adjustment anchor is the PERMNO's last non-null close inside the configured window, computed once over the whole window: stores converted with granularity 'year' and 'month' are identical (D-08)"
    - "Converting into an existing store whose recorded anchor ({zarr}.crsp_adjustment.json: start_date, end_date, product_end) differs is refused before any write, naming both anchors; a store without that sidecar is refused too (D-08)"
    - "Alpha158Stock and the Return label build unchanged over a CRSP panel; Return(t) equals the next day's CRSP ret, including -0.6 into Lehman's delisting day (D-07, D-10)"
  artifacts:
    - path: "quantlab/dataset/crsp.py"
      provides: "CRSP_EXTRA_VARIABLES, full variable construction, anchor sidecar (ADJUSTMENT_SIDECAR_SUFFIX) and refusal"
      contains: "ADJUSTMENT_SIDECAR_SUFFIX"
    - path: "tests/test_crsp_dataset.py"
      provides: "price/return, delisting, events, drop-in factor/label, granularity identity and anchor tests"
  key_links:
    - from: "quantlab/dataset/crsp.py::CrspStockDataset._derivation"
      to: "quantlab/base/data.py::from_raw_data_chunked"
      via: "one whole-window computation cached per instance, sliced per chunk window"
    - from: "quantlab/dataset/crsp.py (adjOpen/adjHigh/adjLow/adjClose/adjVolume)"
      to: "quantlab/factor/alpha158.py::Alpha158Stock, quantlab/label/fret.py::Return"
      via: "the Tiingo variable names these consumers read"
  prohibitions:
    - requirement_id: "D-10"
      category: "correctness"
      status: "resolved"
      verification: "test"
      resolution: null
      reason: null
      statement: "MUST NOT compound stkdelists.delret into returns or adjusted prices; CIZ dlyret already carries the delisting return on its own row"
    - requirement_id: "D-09"
      category: "correctness"
      status: "resolved"
      verification: "test"
      resolution: null
      reason: null
      statement: "MUST NOT fill a missing CRSP return with 0 in the ret variable"
    - requirement_id: "D-08"
      category: "correctness"
      status: "resolved"
      verification: "test"
      resolution: null
      reason: null
      statement: "MUST NOT compute the adjustment anchor per chunk window, and MUST NOT append a window adjusted to a different anchor onto an existing store"
---

<objective>
Complete the price, return and event semantics of `CrspStockDataset` that the tracer
started: every Tiingo variable and the CRSP extras (D-07), the bid/ask and missing-return
rules (D-09), delisting losses exactly once (D-10), events on their ex-dates (D-11), and a
total-return adjustment whose anchor is global to the configured window and protected
against being extended in place (D-08).

**Sequencing:** execute only after phase 03.9 is fully merged and plan 03.10-01 is merged.
The security filter, collision rules and PERMNO seams arrive in plan 07 (wave 3) in this
same file; this plan's test scenarios use single-PERMNO, common-stock rows so they stay
valid after plan 07's default filter.

Purpose: factors, labels and the backtester read only the adjusted columns; this plan is
what makes a CRSP panel a drop-in replacement for a Tiingo panel.

Output: `quantlab/dataset/crsp.py` (semantics + anchor sidecar) and
`tests/test_crsp_dataset.py`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-CONTEXT.md
@.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-RESEARCH.md
@.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-01-SUMMARY.md
</context>

<environment_notes>
- Run from YOUR working-tree root: `uv run pytest -q tests/test_crsp_dataset.py`. Never `cd` into the main checkout. `-k` matching nothing exits 5 = failure. Never `git stash`.
- Build raw tiers the way production does: `mock_crsp_session` + `tests/crsp_fixtures.py::run_crsp_pull` (set `FakeCrspSession.daily_rows` first) and `write_reference_tables`; convert with `registry.convert(WRDS_SOURCE, CrspDatasetConfig(...), data_type="crsp_daily", granularity=...)`. Do NOT edit `tests/crsp_fixtures.py`; scenario rows (built with `dsf_row`, marked `# SYNTHETIC`) live in the test module. Keep every scenario row NS/EQTY/COM (the `dsf_row` default) so plan 07's default filter keeps them.
- macOS: tests that compile KunQuant and also import torch/xgboost rely on `tests/conftest.py` setting `OMP_NUM_THREADS=1`; do not import torch here.
</environment_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Price and return semantics — extras, bid/ask midpoint, missing returns, delisting exactly once</name>

  <files>quantlab/dataset/crsp.py, tests/test_crsp_dataset.py</files>

  <read_first>
    - quantlab/dataset/crsp.py (plan 01: `_derivation`, `_raw_axes_in_range`, `_raw_data_to_xr_window`, `TIINGO_VARIABLES`)
    - quantlab/dataset/cleaning.py lines 79-140 and 283-298 (`flag_anomalies`, `clean_market_data`: OHLCV required, anomaly_flag added)
    - quantlab/base/data.py lines 1084-1093 (`_pin_append_dtypes`)
    - .planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-RESEARCH.md § Q2 (field table), Q6 (variables), Pitfall 7 (float64 flags)
    - .planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-LIVE-CHECK-2.json keys `L3_1`, `L3_2`, `L10_1`
    - tests/crsp_fixtures.py (`AAPL_1980_ROWS`, `LEHMAN_2008_ROWS`, `dsf_row`, `run_crsp_pull`, `write_reference_tables`)
  </read_first>

  <behavior>
    - AAPL_1980_ROWS (verbatim): close(1980-12-12) == 28.8125, prc_is_bidask == 1.0, bid 28.75, ask 28.875, ret NaN (dlyretmissflg NS), ret(1980-12-15) == -0.052061, market_cap(1980-12-12) == 1588606.0 * 1000, shrout == 55136 * 1000, open/high/low/volume NaN (not collected before 1992; expected, not an error), cumfacpr 224.0.
    - SYNTHETIC common stock 10107 over five days where day 3 has dlyret NULL with dlyretmissflg MP and dlyprc NULL: ret(day3) is NaN; close(day3) NaN; adjClose(day4)/adjClose(day2) == 1 + ret(day4) (the day-4 return spans the gap).
    - SYNTHETIC row with dlyprc -10.5 and dlyprcflg BA: close == 10.5, prc_is_bidask == 1.0.
    - LEHMAN_2008_ROWS with the default reference tables (stkdelists holds delret -0.6): symbol LEH; is_delisting == [0,0,0,0,1]; ret(2008-09-18) == -0.6; close(2008-09-18) == 0.052; adjClose(09-18)/adjClose(09-17) == pytest.approx(0.4); adjClose(09-18)/adjClose(09-15) == pytest.approx(1.428571 * 0.433333 * 0.4, rel=1e-9); adjClose(09-18) == 0.052 (the delisting row is the last close, i.e. the anchor).
    - The variable set == TIINGO_VARIABLES + CRSP_EXTRA_VARIABLES + ("anomaly_flag",) after `_clean`; every variable except anomaly_flag is float64.
  </behavior>

  <action>
Tests first. In `quantlab/dataset/crsp.py`:
- Add `CRSP_EXTRA_VARIABLES = ("permno", "permco", "ret", "retx", "shrout", "market_cap", "bid", "ask", "prc_is_bidask", "is_delisting", "numtrd", "cumfacpr", "cumfacshr", "facprc", "close_trade")` with a comment per name giving its CRSP source and unit: `ret`=dlyret, `retx`=dlyretx, `shrout`=shrout x 1000 (CRSP stores thousands), `market_cap`=dlycap x 1000 (thousands of USD -> USD), `bid`/`ask`=dlybid/dlyask, `prc_is_bidask`=1.0 when dlyprcflg == "BA" else 0.0 (NaN when the flag is null) — the "no trade" indicator per D-19, `is_delisting`=1.0 when dlydelflg == "Y" else 0.0, `numtrd`=dlynumtrd, `cumfacpr`/`cumfacshr`, `facprc`=dlyfacprc, `close_trade`=dlyclose (null on bid/ask and delisting days, which is why `close` is abs(dlyprc)).
- Build every variable in `_raw_data_to_xr_window` from the cached derivation, cast Float64, null -> NaN. `ret` stays NaN where dlyret is null (the chain's `fill_null(0)` applies only inside the cumulative product, never to the stored variable).
- Module docstring: the D-09 abs() is a no-op guard on CIZ (live: zero negative prices in 2000), the BA flag is the no-trade signal, and D-10: the delisting return is already a `dlyret` row, so `stkdelists` stays event data and is never compounded.
  </action>

  <verify>
    <automated>uv run pytest -q tests/test_crsp_dataset.py -k "variables or bidask or missing_ret or negative or delisting or lehman"</automated>
    <fails_when>non-zero exit, exit 5 with "deselected", or fewer than 5 passed in the summary line</fails_when>
  </verify>

  <acceptance_criteria>
    - The selector above reports at least 5 passed and 0 failed
    - `uv run python -c "from quantlab.dataset.crsp import CRSP_EXTRA_VARIABLES as V; print(len(V), 'is_delisting' in V, 'prc_is_bidask' in V)"` prints `15 True True`
  </acceptance_criteria>

  <done>Prices, flags and returns follow CIZ semantics: positive prices with a bid/ask flag, NaN for missing returns, and Lehman's delisting loss counted once.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Events on ex-dates and the drop-in check through Alpha158Stock and the Return label</name>

  <files>quantlab/dataset/crsp.py, tests/test_crsp_dataset.py</files>

  <read_first>
    - quantlab/factor/alpha158.py lines 91-130 (`Alpha158Stock`: adjusted inputs only)
    - quantlab/label/fret.py lines 1-60 (`Return` reads `Input("adjClose")`)
    - tests/test_factor_kunquant.py lines 30-66 and 226-276 (`_factor_config`, `_ADJUSTED_STOCK_COLUMNS`, how a stock factor is built over a store)
    - tests/universe_fixtures.py lines 315-335 (`make_return_label`)
    - .planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-LIVE-CHECK-2.json key `L4_1`
  </read_first>

  <behavior>
    - AAPL_AUG_2020_ROWS: divCash(2020-08-07) == 0.82, 0.0 on the other three days; splitFactor(2020-08-31) == 4.0, 1.0 elsewhere (1.0 on the PERMNO's first row); facprc(2020-08-31) == 4.0.
    - SYNTHETIC day with dlyorddivamt 0.2 and dlynonorddivamt 1.5 -> divCash 1.7; with both NULL -> 0.0.
    - SYNTHETIC 40 consecutive business days for PERMNO 10107 (prices and returns consistent, one 2:1 split in the middle): `Alpha158Stock` with `data_columns` = the five adjusted names and `factor_names=["KMID", "ROC5", "STD5"]` computes over `CrspStockDataset(<the converted config>)` and returns finite values for most bars; the `Return` label (`n_forward_periods=1`) equals the next day's `ret` for every consecutive pair (rtol 1e-5, KunQuant is float32), including across the split day.
    - Over LEHMAN_2008_ROWS the Return label on 2008-09-17 == pytest.approx(-0.6, rel=1e-5).
  </behavior>

  <action>
Tests first. Confirm/adjust the event variables in `quantlab/dataset/crsp.py`: `divCash` = `dlyorddivamt.fill_null(0) + dlynonorddivamt.fill_null(0)` (unadjusted cash per share on the ex-date, the Tiingo convention); `splitFactor` = the PERMNO's previous `dlycumfacpr` divided by today's (1.0 on its first row inside the window, documented); `facprc` = `dlyfacprc`. Distributions and delistings also stay available raw in `_reference/` (plan 03) for anything finer. Build the factor/label in the test through the real classes and `FactorConfig`; do not add any CRSP-specific branch to `quantlab/factor` or `quantlab/label`.
  </action>

  <verify>
    <automated>uv run pytest -q tests/test_crsp_dataset.py -k "events or divcash or split or drop_in or alpha158 or return_label"</automated>
    <fails_when>non-zero exit, exit 5 with "deselected", or fewer than 4 passed in the summary line</fails_when>
  </verify>

  <acceptance_criteria>
    - The selector above reports at least 4 passed and 0 failed
    - `git diff --stat $(git merge-base HEAD main)..HEAD -- quantlab/factor quantlab/label` prints nothing (no consumer change)
  </acceptance_criteria>

  <done>Distributions and splits appear on their ex-dates, and Tiingo-shaped consumers compute on a CRSP panel with no code change, delisting return included.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Global adjustment anchor — granularity identity, last-non-null anchor, anchor sidecar and refusal</name>

  <files>quantlab/dataset/crsp.py, tests/test_crsp_dataset.py</files>

  <read_first>
    - quantlab/base/data.py lines 358-560 (`from_raw_data_chunked`: `_raw_axes_in_range` runs once, before `ChunkLedger.assert_consistent` and before any append)
    - quantlab/base/chunking.py (`TimeChunkPlanner` granularities, `ChunkLedger.default_path`)
    - quantlab/utils/atomic.py (`write_json_atomically`)
    - quantlab/dataset/crsp_reference.py (`CrspReference.product_end`)
    - .planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-RESEARCH.md § Q6 "Anchor movement and incremental refresh", Pattern 4, Pitfalls 1-2
  </read_first>

  <behavior>
    - SYNTHETIC 10107 daily rows 2019-11-01..2020-09-30 with a 4:1 split (cumfacpr 4 -> 1) on 2020-08-31 and a dividend: stores converted with granularity "year" and with "month" into two paths are identical (`xr.testing.assert_identical` on the loaded datasets).
    - SYNTHETIC PERMNO whose last row inside the window has dlyprc NULL (MP): adjClose equals close on the previous row (the last non-null close is the anchor) and adjClose is NaN on the null row.
    - After a convert, `{zarr}.crsp_adjustment.json` exists beside the store (not inside it) with {"start_date": "2019-11-01", "end_date": "2020-09-30", "product_end": "2025-12-31", "rule": "total_return_backward_from_last_close"}.
    - A second convert of the same config resumes (every window skipped) without error.
    - The same zarr path with `end_date="2020-12-31"` raises ValueError naming "2020-09-30" and "2020-12-31" and telling the user to choose a new zarr_file_path or delete the store and its `.crsp_*.json` sidecars; the store's ledger and data are unchanged.
    - A store directory present without the sidecar raises ValueError naming the missing sidecar.
  </behavior>

  <action>
Tests first. In `quantlab/dataset/crsp.py`:
- Class constant `ADJUSTMENT_SIDECAR_SUFFIX = ".crsp_adjustment.json"` and a helper returning `Path(str(config.zarr_file_path) + ADJUSTMENT_SIDECAR_SUFFIX)` (a sibling of the store).
- Anchor = the PERMNO's last row with a non-null close inside `[config.start_date, config.end_date]`; the derivation (plan 01) already computes once per instance — verify it is never recomputed per window and never filtered to the window before the cumulative product.
- In `_raw_axes_in_range()` (runs once, before any write in `from_raw_data_chunked`): build the anchor record {start_date, end_date, product_end from `CrspReference(config.reference_dir).product_end`, rule}; if the store path exists: a missing sidecar or a sidecar different from the record raises ValueError naming both anchors (or the missing file) and the remedy; if the store does not exist, write the sidecar with `write_json_atomically` before returning (so a store never exists without it).
- Docstring: extending `end_date` rescales every earlier adjusted value of a still-listed PERMNO (same as Tiingo); the chunk ledger appends windows and never rewrites, so an in-place extension would splice two anchors into one column — hence the refusal; CRSP updates yearly and a full rebuild of the daily S&P panel is cheap.
  </action>

  <verify>
    <automated>uv run pytest -q tests/test_crsp_dataset.py</automated>
    <fails_when>non-zero exit, or fewer than 14 passed in the summary line</fails_when>
    <automated>uv run pytest -q tests/test_crsp_tracer.py tests/test_stock_dataset.py tests/test_chunked_ingest.py</automated>
    <fails_when>non-zero exit, or any failed/errored test in the summary line</fails_when>
  </verify>

  <acceptance_criteria>
    - `uv run pytest -q tests/test_crsp_dataset.py` exits 0 with at least 14 passed
    - `uv run pytest -q tests/test_crsp_dataset.py -k "granularity or anchor or sidecar"` exits 0 with at least 4 passed
  </acceptance_criteria>

  <done>The adjustment has one anchor per configured window whatever the chunking, and a store can never silently mix two anchors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw shards + reference parquet -> Zarr | derived adjusted prices are what every model and backtest trusts |
| existing store -> new conversion | a second run appends into a store written earlier |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.10-19 | Tampering | anchor splice on extension | high | mitigate | anchor sidecar written before the first append and compared on every conversion; mismatch or absence refuses before any write |
| T-03.10-20 | Tampering | double-counted delisting return | high | mitigate | returns come only from dlyret; stkdelists never enters the computation; Lehman test pins the product |
| T-03.10-21 | Tampering | zero-filled missing returns | medium | mitigate | stored `ret` keeps NaN; only the internal cumulative product treats null as factor 1, by CIZ's gap-spanning return definition |
| T-03.10-22 | Denial of Service (self) | whole-window derivation memory | low | accept | derivation keeps only the per-row factor columns (~6M rows x ~12 cols for the S&P set); documented |
</threat_model>

<verification>
- `uv run pytest -q tests/test_crsp_dataset.py tests/test_crsp_tracer.py` passes.
- `git diff --stat $(git merge-base HEAD main)..HEAD -- quantlab/factor quantlab/label` is empty.
</verification>

<success_criteria>
A CRSP panel is a numerically faithful, Tiingo-shaped, total-return-adjusted daily panel with delisting losses included once and a protected anchor.
</success_criteria>

## Artifacts this phase produces (this plan's share)
- `quantlab/dataset/crsp.py`: `CRSP_EXTRA_VARIABLES`, `ADJUSTMENT_SIDECAR_SUFFIX`, full variable construction, anchor record/refusal in `_raw_axes_in_range`
- `tests/test_crsp_dataset.py`
- On disk (runtime): `{zarr_file_path}.crsp_adjustment.json`

<output>
Create `.planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-05-SUMMARY.md` when done
</output>
