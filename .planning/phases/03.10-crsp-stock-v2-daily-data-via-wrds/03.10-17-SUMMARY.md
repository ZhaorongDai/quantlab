---
phase: "03.10-crsp-stock-v2-daily-data-via-wrds"
plan: "17"
subsystem: infra
tags: [regression-gate, verification, decision-record, gap-closure, crsp, closing]

# Dependency graph
requires:
  - phase: "03.10 plans 12-16"
    provides: "the whole gap-closure set, merged into main — this plan is the gate that runs after all of them and the decision record that closes the books"
  - phase: "03.9-BASELINE-FAILURES.txt"
    provides: "the 55-id failure baseline both comm directions are taken against"
  - phase: "03.10-VERIFICATION.md"
    provides: "the GAP-A/GAP-B read-only reproduction on the shipped store, and the 1618-passed figure the growth check is measured against"
  - phase: "03.10-REVIEW.md"
    provides: "WR-04, WR-05, WR-06 and IN-01..IN-06 — the warnings this plan either decides or hands forward in writing"
provides:
  - "A main-tree regression verdict for the whole gap set: 0 new failures against the 03.9 baseline, CRSP suites green, suite grown by exactly the tests the gap set added"
  - "Proof on REAL vendor rows (not fixtures) that GAP-A/GAP-B are repaired: CTLT/MRO/PXD/WRK all recover a finite positive adjClose and a sub-1.0 adjVolume NaN fraction"
  - "An operator decision on record for WR-05 and for WR-06, each with rationale, neither resolved by a silent patch"
  - "A written brief for the next planner: WR-04, IN-01..IN-05 still open, plus a NEWLY FOUND defect class that is larger than WR-05 (see key-decisions)"
affects: ["the new PERMNO-symbol-axis phase (which now owns WR-05 and the newly found seam class)", "any later phase reading this phase's regression verdict"]

# Actuals (#2632)
actuals:
  tokens: 0
  tasks: 3
  commits: 1
plan_head_before: 076633f7efaf07bc2a639a5ff5f94201ae1c9864

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "A decision recorded with its rationale is not the same artifact as a patch: 'we looked and kept it' and 'nobody looked' are indistinguishable a year later, and only one of them is a decision"
    - "A gate's own extraction format is part of the gate: a comm against a baseline file is only meaningful when both sides are normalized identically, and the failure mode is silent in BOTH directions"
    - "Measure the population before choosing a mitigation: the same warning justifies different options at 6 occurrences and at 3095"

key-files:
  created:
    - ".planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-17-SUMMARY.md"
  modified: []

key-decisions:
  - "WR-05 — DECISION: defer to the new PERMNO-symbol-axis phase. Rationale, in the operator's words: the symbol axis is being changed from ticker to PERMNO, so `resolve_collisions` / `active_over_delisting` lose their reason to exist entirely — two companies cannot contend for one symbol when the symbol IS the permanent security id. Adding a `rows_dropped_carrying_delisting` counter (option a) would be building a feature onto code the next phase deletes, which is exactly the repo bloat the operator asked to avoid. The decision was initially taken as option (a) on the measured evidence below, then REVISED to defer once the axis change was decided; both the original choice and the revision are recorded here rather than only the final state."
  - "WR-06 — DECISION: option (b), narrow the tolerance to gaps containing no weekday. Rationale: it preserves what the code comment actually intended (only a genuine long weekend is tolerated) and removes the case the comment never covered, and the cost measured out at ZERO — see the allow_unlinked finding below. Implementation is NOT in this plan (see the acceptance criterion that forbids it); it is handed forward as a follow-up naming `quantlab/dataset/crsp_membership.py:163`."
  - "A DEFECT CLASS LARGER THAN WR-05 WAS FOUND while sizing WR-05, and it is the most consequential thing in this SUMMARY. `resolve_collisions` detects contention with `group_by(['timestamp','symbol'])` — it can only see two PERMNOs claiming one ticker ON THE SAME DAY. A ticker reused SEQUENTIALLY (company A delists, years later company B takes the same ticker) has exactly one PERMNO on every single day, is never `crowded`, and is therefore concatenated into one column with no check, no report entry and no warning. Measured on the full reference tier: in a 2000-2024 window, 3205 tickers are used by >=2 PERMNOs, of which only 110 (3.4%) overlap in time and are seen by the existing guard — 3095 (96.6%) are pure sequential succession and pass silently. Even a 2015-2024 window has 748. Full history: 8719 of 36990 tickers (23.6%) reused by >=2 PERMNOs, touching 18850 of 39506 PERMNOs (47.7%); the worst ticker (EAGL) was used by 10 different companies. The module's own docstring states the stake: 'two companies' prices in one series ... every return across the seam would be fabricated' (T-03.10-16). The guard implements that intent for 3.4% of the population. This is WORSE than WR-05 in kind, not just in count: WR-05 DROPS a row (missing data), this FABRICATES a return at the seam (invented data), which then propagates through every look-back factor in Alpha101/Alpha158 (delay, ts_rank, stddev, correlation, decay_linear) and is learned by the model as a stable in- and out-of-sample signal."
  - "WR-05's measured population, recorded because it is what the (a) decision was originally based on and it stays the reference if the axis change is ever reconsidered: 98 genuine occurrences over 1962-2025 (96 tickers, 0.26% of all tickers) where one PERMNO delists inside a same-ticker overlap while the other survives. A further 69 (ticker, date) groups had BOTH PERMNOs delisting the same day — 138 hit rows — where the 'keep the one still trading' rule does not apply at all; those are a separate case and were excluded from the 98. Of the 98, 86 carry a non-null delret: median +0.001089, mean -0.032563, only 6 below -20% and 4 below -50% (three -1.0 total losses: SIM 1962-11-15, CNL 1974-03-01, ELAN 1984-02-02; one -0.68: I 1993-01-11). By decade: 10/7/7/26/35/9/4 for the 1960s..2020s, so the severe cases are concentrated before 1993."
  - "WR-06's measured population AND the finding that makes option (b) free: there are exactly 6 tolerated_gaps on the Nasdaq-100 branch, of which 4 contain a trading day (2, 1, 1 and 1 trading days — the worst is gvkey 001164, 2006-01-07..01-10, whose 4 calendar days hold 2 trading days) and 2 are pure weekends (the comment's intended case). The plan's prose warns of 'up to four TRADING days'; the real worst case in this data is two. Critically: `permno_intervals('comp_nasdaq100')` with the DEFAULT `allow_unlinked=False` ALREADY RAISES, because 6 separate spells are genuinely unlinked (e.g. gvkey 012884 uncovered 2007-02-01..02-05). Any working Nasdaq-100 pull today must therefore already be passing `allow_unlinked=True`. So option (b)'s stated cost — 'will start refusing pulls that succeed today' — does not exist: it moves 4 silent holes from `tolerated_gaps` into `unlinked` (6 -> 10), changing visibility, not behaviour."
  - "Task 1's comm comparison was run WRONG on the first attempt and printed all 55 ids in BOTH directions, which reads as a catastrophic regression. Cause: the baseline file stores ids WITH their leading `FAILED ` token, and the first extraction stripped it, so comm compared two disjoint sets. The plan's own `sed 's/ - .*//'` preserves the prefix and is correct — the deviation was the operator's. Recorded because the INVERSE mistake (a normalization difference that makes comm print NOTHING) is a false green that no amount of reading the exit status would catch, and because this gate is the phase's last line of defence. Both directions were re-run after normalizing identically, and `diff -q` confirms the two id lists are byte-identical."
  - "Tasks 1 and 2 were executed inline by the orchestrator in the main tree rather than dispatched to an executor, because Task 1's `<precondition>` explicitly requires 'the working directory is the main tree rather than an executor worktree' and Task 2's requires the gitignored `data/downloads/...` tier, which no worktree carries. Task 2 was run rather than handed to the operator as a command because its own `<how-to-verify>` states no WRDS connection is opened and no Duo push arrives — it reads parquet already on disk and writes to a scratch path — so the orchestrator could produce the evidence instead of asking for it."
  - "Task 2's prescribed command in the plan is WRONG and was corrected. It passes `raw_data_dir_path='data/downloads/us_equity/1d/wrds_crsp'`, which `_assert_vendor_root` refuses: the raw path must TERMINATE at the vendor segment (D-11), so it must be `.../wrds_crsp/wrds`. The corrected value was taken from `scripts/ingest_wrds_crsp.py:494` (`raw_data_dir_path=acq_config.raw_data_dir_path`), which the plan's own `<read_first>` names as the authoritative shape for exactly this contingency."

patterns-established:
  - "When a plan asks for a mitigation, size the population first: WR-05 and WR-06 each looked like a judgement call in the abstract and each turned out to have a cost or a frequency that decided it"
  - "A guard's DETECTION predicate is as much a part of it as its resolution rule — `group_by(timestamp, symbol)` encoded an assumption (contention is same-day) that silently excluded 96.6% of the population the guard's own docstring says it exists to protect"

requirements-completed: []  # The plan declares `requirements: [D-08, D-10, D-13]` — 03.10-CONTEXT decision ids, NOT REQUIREMENTS.md ids, exactly as plans 12-16 recorded. `requirements.mark-complete` reports `not_found` for D-ids; recording them here would assert project requirements this plan did not complete.

coverage:
  - id: T1
    description: "The merged main tree fails only baseline tests, the CRSP suites are green, and the suite grew by the tests this gap set added"
    verification:
      - kind: test
        ref: "ten CRSP suites in the main tree → `199 passed, 158 warnings in 27.01s`, 0 failed / 0 errors"
        status: pass
      - kind: test
        ref: "full suite `uv run pytest -q --ignore=tests/test_factor_hierarchy.py -p no:cacheprovider` → `55 failed, 1638 passed, 1 skipped, 1239 warnings in 247.81s`; `grep -c '^ERROR'` → 0"
        status: pass
      - kind: other
        ref: "`comm -13 base.txt now.txt` → EMPTY (no new failures); `comm -23` → EMPTY (none vanished); `diff -q` reports the id lists identical. Taken at HEAD 076633f7efaf07bc2a639a5ff5f94201ae1c9864."
        status: pass
      - kind: other
        ref: "growth check: 1618 → 1638 passed (+20); `+def test_` = 19, `-def test_` = 0, one added `@pytest.mark.parametrize` with 2 cases ⇒ 19-1+2 = 20 collected; `xfail`/`skip` introduced = 0; test-file numstat shows 0 deletions in all four touched files"
        status: pass
    human_judgment: false
  - id: T2
    description: "Re-converting the shipped raw tier proves GAP-A/GAP-B repaired on real vendor rows: the four damaged PERMNOs recover a real adjusted level and a finite adjusted volume"
    verification:
      - kind: other
        ref: "scratch re-conversion of PERMNOs 14763/15069/21186/75241 over 2024 → CTLT adjClose_max 63.4800 / min_positive 42.5602 / adjVolume_nan_frac 0.0041; MRO 29.3374 / 21.7834 / 0.0738; PXD 275.5200 / 214.1795 / 0.6516; WRK 54.7400 / 37.8766 / 0.4754. VERIFICATION had recorded `np.unique(adjClose) == [0.]` and a NaN fraction of 1.000 for every one of them."
        status: pass
      - kind: other
        ref: "WRK hits the hand-checkable arithmetic plan 03.10-12 fixed in advance: adjClose 2024-07-05 = 51.510000000000005 (expected 51.51); adjClose 2024-07-03 = 49.749994446467326, and 51.51/1.035377 = 49.749994446467326 (exact); the 2024-07-08 delisting row is close=NaN / adjClose=NaN, NOT 0.0"
        status: pass
      - kind: other
        ref: "nothing disturbed: `ls data/data/us_equity/1d/` lists the same 14 entries as before; `git status --porcelain data/` is empty (the tier is gitignored); the scratch store was written to /tmp/crsp_gap_recheck.zarr"
        status: pass
    human_judgment: true
  - id: T3
    description: "WR-05 and WR-06 each have an operator decision on record with a rationale, and neither was resolved by a silent patch inside this plan"
    verification:
      - kind: other
        ref: "see key-decisions: WR-05 → defer to the PERMNO-symbol-axis phase; WR-06 → option (b). Both carry the operator's rationale."
        status: pass
      - kind: other
        ref: "`git show --name-only --format= HEAD` on this plan's commit names ONLY .planning/phases/03.10-crsp-stock-v2-daily-data-via-wrds/03.10-17-SUMMARY.md — neither quantlab/dataset/crsp_symbology.py nor quantlab/dataset/crsp_membership.py appears, as the acceptance criterion requires"
        status: pass
    human_judgment: true

# Metrics
duration: 1h 5m
completed: 2026-09-20
status: complete
---

# Phase 03.10 Plan 17: Closing the gap set — regression verdict, real-data proof, two decisions Summary

**The whole gap set introduces zero new failures against the 03.9 baseline, the four symbols whose adjusted columns were entirely `0.0`/`NaN` recover real prices on actual CRSP rows (WRK to 12 decimal places of a hand-computed 51.51), and WR-05 / WR-06 each leave with a recorded decision — plus one newly found defect class that is larger than either of them.**

## Performance

- **Duration:** ~1h 5m (dominated by three full-suite runs at ~4m each plus the population measurements)
- **Tasks:** 3 (1 automated gate, 2 human checkpoints)
- **Files modified:** 0 (this SUMMARY only — Task 3's acceptance criterion forbids touching the two modules)
- **Run at HEAD:** `076633f7efaf07bc2a639a5ff5f94201ae1c9864`

## Task 1 — the regression gate

Both verify blocks passed. Verbatim summary lines:

```
199 passed, 158 warnings in 27.01s
55 failed, 1638 passed, 1 skipped, 1239 warnings in 247.81s (0:04:07)
```

`comm -13` (new failures) and `comm -23` (baseline failures that disappeared) both printed nothing;
`diff -q` confirms the sorted id lists are byte-identical to the 55-id 03.9 baseline. No `ERROR` line.

The growth check is the one a subset comparison cannot make, and it reconciles exactly:

| quantity | value |
|---|---|
| passed at VERIFICATION | 1618 |
| passed now | **1638** (+20) |
| `+def test_` / `-def test_` | 19 / **0** |
| added `parametrize` | 1, with 2 cases |
| collected cases from 19 functions | 19 − 1 + 2 = **20** |
| `xfail` / `skip` introduced | **0** |

See `key-decisions` for the first-attempt comm normalization error and why it is recorded.

## Task 2 — the four symbols recover, on real vendor rows

| symbol | adjClose_max | adjClose_min_positive | adjVolume_nan_frac |
|---|---|---|---|
| CTLT | 63.4800 | 42.5602 | 0.0041 |
| MRO | 29.3374 | 21.7834 | 0.0738 |
| PXD | 275.5200 | 214.1795 | 0.6516 |
| WRK | 54.7400 | 37.8766 | 0.4754 |

VERIFICATION recorded all four as `np.unique(adjClose) == [0.]` with a NaN fraction of 1.000.

```
2024-07-03  close=49.75  adjClose=49.749994446467326   ← 51.51/1.035377 = 49.749994446467326
2024-07-05  close=51.51  adjClose=51.510000000000005   ← expected 51.51
2024-07-08  close=nan    adjClose=nan                  ← the delisting row: NaN, not 0.0
```

The fixture arithmetic plan 03.10-12 fixed in advance is reproduced exactly on the real tier. No
WRDS connection was opened; the shipped stores are untouched.

## Task 3 — the two decisions

- **WR-05 → defer to the PERMNO-symbol-axis phase.** The axis change deletes
  `resolve_collisions` / `active_over_delisting` outright, so adding a counter to them first is
  work the next phase throws away.
- **WR-06 → option (b)**, narrow the tolerance to gaps containing no weekday. Measured cost: zero,
  because the default `allow_unlinked=False` path already raises.

Rationale and measured populations for both are in `key-decisions`.

## Not planned in this gap set — the written brief for the next planner

| id | File | Why it was left |
|---|---|---|
| **SEAM-01 (NEW)** | `quantlab/dataset/crsp_symbology.py` `resolve_collisions` | **Found while sizing WR-05, and larger than it.** Contention is detected per `(timestamp, symbol)`, so a SEQUENTIALLY reused ticker is concatenated across two companies with no check at all. 3095 of 3205 reused tickers (96.6%) in a 2000-2024 window are this case. Fabricates a return at the seam, which propagates through every look-back factor. Owned by the new PERMNO-axis phase. |
| **WR-05** | `quantlab/dataset/crsp_symbology.py` `active_over_delisting` | Decided: defer. Subsumed by the PERMNO-axis phase. |
| **WR-06** | `quantlab/dataset/crsp_membership.py:163` `LINK_GAP_TOLERANCE_DAYS` | Decided: option (b). Implementation deliberately not in this plan. |
| **WR-04** | `quantlab/acquisition/wrds_crsp_reference.py:169-224` | An interrupted cross-vintage reference pull leaves a mixed-vintage `_reference/` whose manifest still reports the OLD vintage, which `_adjustment_record` stamps into `.crsp_adjustment.json`. Fix: unlink the manifest before the first write of a different vintage. Left because it is not a gap and is the only warning needing a module and a test suite no gap plan touches. |
| **IN-01** | `quantlab/acquisition/wrds_crsp.py:447` | `self._data_type` as a bare expression statement used as eager validation. |
| **IN-02** | `quantlab/dataset/crsp.py:796-804` | Dead local `columns` and a redundant second `.select()`. |
| **IN-03** | `quantlab/dataset/crsp.py:851` | Function-scope `import json` in `_assert_anchor_unchanged`. |
| **IN-04** | `quantlab/dataset/crsp_membership.py:618-622` | Five placeholder-less f-strings (`ruff F541`) in the module's most-read refusal message. |
| **IN-05** | `quantlab/dataset/crsp.py:882` (`if recorded != record:`) | Whole-dict anchor comparison — adding one key to `_adjustment_record()` invalidates every existing store. The most consequential of the six. |
| **IN-06** | `scripts/ingest_wrds_crsp.py:491,511-512` | **TAKEN** in plan 03.10-15; the over-constructed `probe_dataset` and the `replace(ds_config, symbols=None)` no-op are gone. |

## Rebuild debt carried out of this phase

Plans 03.10-13 and 03.10-14 both require a re-conversion of any store built before their fixes
(zeroed `adj*` columns; full constituent history under `--universe`/`--permnos`). One re-conversion
settles both. Task 2 proved the repair on a scratch store but deliberately did NOT rebuild
`data/data/us_equity/1d/wrds_crsp_sp500_1d.zarr`, which still holds the zeroed columns. The
PERMNO-axis phase will rebuild these stores anyway, so the rebuild is best done once, there.

Two live confirmations remain unrun, both needing a WRDS connection an agent may not open: the
Blackstone roster-override case (PERMNO 92108, 2007-2019, `--universe crsp_sp500`) and the QQQ
close of 614.31 on 2025-12-31.

## Self-Check: PASSED
