---
phase: 01-codebase-cleanup-security-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
autonomous: true
requirements: [CLEAN-04]

must_haves:
  truths:
    - "uv sync produces a working environment with no ModuleNotFoundError/ImportError for any first-party module in the repo"
    - "pyproject.toml accurately declares the packages actually imported by the codebase (not the stale 3-package crypto-quant lock)"
  artifacts:
    - path: "pyproject.toml"
      provides: "dependency declarations matching actual imports"
      contains: "torch"
    - path: "uv.lock"
      provides: "resolved, current lockfile for the quantlab project"
      contains: "name = \"quantlab\""
  key_links:
    - from: "pyproject.toml"
      to: "uv.lock"
      via: "uv sync dependency resolution"
      pattern: "name = \"quantlab\""
---

<objective>
Reconcile `pyproject.toml`/`uv.lock` with the ~20 third-party packages actually imported throughout the codebase (currently `dependencies = []` and a stale lockfile for a different, old project name), so `uv sync` produces a working environment (CLEAN-04) — a hard precondition for every later phase.

Purpose: Nothing in Phase 2+ can run until the dependency environment is resolvable. This is pure environment/tooling work, independent of the other cleanup plans in this phase.
Output: `pyproject.toml` with a correct `dependencies` list; `uv.lock` regenerated against it; confirmed via a full first-party-module import smoke test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare and lock actual dependencies via uv add</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>pyproject.toml, uv.lock, .planning/codebase/STACK.md</read_first>
  <action>
  Run `uv add &lt;package&gt;` for each of the following PyPI package names (import names differ for a few -- noted in parens) so `uv` resolves compatible version constraints into `pyproject.toml`'s `dependencies` array and regenerates `uv.lock` against this project (`quantlab`, not the stale `crypto-quant`): numpy, pandas, polars, xarray, torch, KunQuant, nautilus-trader (import name `nautilus_trader`), vectorbt, wandb, loguru, joblib, tqdm, Bottleneck (import name `bottleneck`), requests, PyYAML (import name `yaml`), tiingo, psutil, plotly, scikit-learn (import name `sklearn`), zarr (implicit runtime dependency of `dataset/backend.py:XrBackend`'s `xr.Dataset.to_zarr()`/`xr.open_dataset(engine="zarr")` calls -- not in STACK.md's explicit list but required for the Zarr storage backend to function; xarray does not vendor it). For `KunQuant` specifically: run `uv add KunQuant` first -- it is published on PyPI (verified present, version 0.1.11, by the same Menooker/KunQuant GitHub project referenced by the local dev clone's git remote). Only if `uv add KunQuant` fails to resolve, or the package installed from it fails Task 2's import check, fall back to adding it as a git dependency pointing at `https://github.com/Menooker/KunQuant` and note this fallback explicitly in the plan SUMMARY -- do not silently swap in a different/local path. After all `uv add` calls, run `uv sync` and iterate on any resolution or build failures (e.g. platform-specific wheel availability for `torch`/`nautilus-trader`/`KunQuant` on this machine) until it exits 0.
  </action>
  <acceptance_criteria>
    - `pyproject.toml`'s `[project] dependencies` array is non-empty and includes each package name above (spot-checkable via grep).
    - `uv.lock` no longer references the old project name `crypto-quant`.
    - `uv.lock` locks at least 19 top-level third-party packages (`grep -c '^name = ' uv.lock` &gt;= 19; this count includes transitive deps too, so treat it as a floor, not exact).
    - `uv sync` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>uv sync && ! grep -q "crypto-quant" uv.lock && [ "$(grep -c '^name = ' uv.lock)" -ge 19 ]</automated>
  </verify>
  <done>`uv sync` completes with exit code 0 and produces a `.venv` with all ~20 packages installed; `pyproject.toml`'s dependencies array reflects actual imports, not the stale 3-package crypto-quant lock.</done>
</task>

<task type="auto">
  <name>Task 2: Verify every first-party module imports cleanly</name>
  <files>pyproject.toml, uv.lock</files>
  <read_first>.planning/codebase/STRUCTURE.md</read_first>
  <action>
  Run a single `uv run python -c "..."` command importing every first-party top-level module: `base.backend`, `base.config`, `base.data`, `base.factor`, `base.model`, `dataset.backend`, `dataset.spot`, `dataset.stock`, `factor.alpha101`, `factor.alpha158`, `label.spot`, `my_ops.preprocess`, `dl_model.mlp`, `dl_model.rnn`, `dl_model.rnn_classification`, `ml_model.backend`, `backtest.test_strategy`, `vecbt.bt`, `utils.timer`, `utils.file`, `utils.binance`, `utils.nautilus`, `utils.module`, `utils.asdict`, `enums.constant`, `enums.data`, `config`. If any `ModuleNotFoundError`/`ImportError` surfaces, add the missing package to `pyproject.toml` via `uv add` and re-run `uv sync`, repeating until the import command exits 0 with no exceptions raised.
  </action>
  <acceptance_criteria>
    - The combined import command exits 0.
    - Command output contains no `ModuleNotFoundError` or `ImportError` traceback.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "import base.backend, base.config, base.data, base.factor, base.model, dataset.backend, dataset.spot, dataset.stock, factor.alpha101, factor.alpha158, label.spot, my_ops.preprocess, dl_model.mlp, dl_model.rnn, dl_model.rnn_classification, ml_model.backend, backtest.test_strategy, vecbt.bt, utils.timer, utils.file, utils.binance, utils.nautilus, utils.module, utils.asdict, enums.constant, enums.data, config; print('OK')"</automated>
  </verify>
  <done>All 27 first-party modules import successfully under the uv-managed environment with exit code 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `pyproject.toml`/`uv.lock` &lt;-&gt; PyPI package registry | Any package name declared here is fetched and executed (at install and import time) from a third-party registry; a typosquatted or malicious package name would run arbitrary code locally |

## Package Legitimacy Audit

No RESEARCH.md exists for this phase (skipped by user), so per the package-legitimacy fallback policy the planner performed direct verification against `pypi.org` for every package this plan adds (2026-09-04). All 20 packages are already imported by the existing prototype code (STACK.md) -- this plan is reconciling declarations to match existing, already-chosen dependencies, not introducing new ones.

| Package | PyPI name | Verified on PyPI | Version seen | Disposition |
|---------|-----------|-------------------|---------------|-------------|
| numpy | numpy | yes | 2.5.2 | OK -- ubiquitous, high-adoption |
| pandas | pandas | yes | 3.0.5 | OK -- ubiquitous, high-adoption |
| polars | polars | yes | 1.44.1 | OK -- well-established |
| xarray | xarray | yes | 2026.7.0 | OK -- well-established |
| torch | torch | yes | 2.14.0 | OK -- ubiquitous, high-adoption |
| KunQuant | KunQuant | yes | 0.1.11 | OK -- niche but confirmed: PyPI project matches the Menooker/KunQuant GitHub org the developer's own local clone (`~/KunQuant`) points at as `origin` |
| nautilus-trader | nautilus-trader | yes | 1.231.0 | OK -- well-established trading-engine package |
| vectorbt | vectorbt | yes | 1.1.0 | OK -- well-established |
| wandb | wandb | yes | 0.29.0 | OK -- ubiquitous MLOps tool |
| loguru | loguru | yes | 0.7.3 | OK -- well-established |
| joblib | joblib | yes | 1.6.0 | OK -- well-established |
| tqdm | tqdm | yes | 4.70.0 | OK -- ubiquitous |
| Bottleneck | Bottleneck | yes | 1.6.0 | OK -- well-established numpy accelerator |
| requests | requests | yes | 2.34.2 | OK -- ubiquitous |
| PyYAML | PyYAML | yes | 6.0.3 | OK -- ubiquitous |
| tiingo | tiingo | yes | 0.16.1 | OK -- official Tiingo SDK, already in use |
| psutil | psutil | yes | 7.2.2 | OK -- well-established |
| plotly | plotly | yes | 7.0.0 | OK -- well-established |
| scikit-learn | scikit-learn | yes | 1.9.0 | OK -- ubiquitous |
| zarr | zarr | yes | 3.3.0 | OK -- well-established, required by xarray's zarr backend already in use in `dataset/backend.py` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-SC | Tampering (supply chain) | `pyproject.toml`/`uv add` installs | mitigate | All 20 packages verified live against `pypi.org` by the planner (table above) prior to this plan; no [ASSUMED]/[SUS] packages present, so no blocking checkpoint is required. Executor should still sanity-check `uv add` resolves to the exact package name listed here (not a similarly-named typosquat) before proceeding if any install prompt looks unexpected. |
| T-01-02-02 | Denial of Service (build failure) | `torch`/`nautilus-trader`/`KunQuant` native builds on this machine | accept | These packages may require platform-specific wheels or a Rust/C++ toolchain; if `uv sync` fails to build one of them, that is a legitimate environment constraint to resolve interactively during execution (not a security issue) -- Task 1's action explicitly calls for iterating on build failures. |
</threat_model>

<verification>
Run both tasks' `<verify><automated>` commands. Confirm `uv.lock`'s `[[package]]` entry for `name = "quantlab"` lists dependencies matching `pyproject.toml`, and that `.venv/` (gitignored) now exists with installed packages.
</verification>

<success_criteria>
- `uv sync` exits 0 from a clean state and installs all ~20 actually-imported third-party packages.
- Every first-party module in the repo imports without error under the resulting environment.
</success_criteria>

<output>
Create `.planning/phases/01-codebase-cleanup-security-hardening/01-02-SUMMARY.md` when done
</output>
