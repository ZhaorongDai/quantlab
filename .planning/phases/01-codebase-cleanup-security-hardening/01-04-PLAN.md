---
phase: 01-codebase-cleanup-security-hardening
plan: 04
type: execute
wave: 2
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - README.md
autonomous: true
requirements: [CLEAN-03]

must_haves:
  truths:
    - "A new contributor reading README.md can correctly identify the actual module layout, entry points, and how to install/run the pipeline without hitting a non-existent file reference"
  artifacts:
    - path: "README.md"
      provides: "accurate project description, module layout, and uv-based setup instructions"
      contains: "uv sync"
  key_links: []
---

<objective>
Rewrite `README.md` so it accurately describes the current repository (module layout, entry points, install/setup, required env vars), replacing its currently outdated "Crypto Quant Trading Models" content, which references files and classes that do not exist (`models/lstm_model.py`, `examples/train_models.py`, `ModelConfig`, `XGBoostModel`, `pip install -r requirements.txt`).

Purpose: CLEAN-03 requires README.md to match reality; this plan runs after the other Wave 1 cleanup plans (01-01, 01-02, 01-03) so the README can accurately describe the *post-cleanup* state — env-var-based config, `uv sync` install, the fixed/deduped scripts — rather than needing a second pass.
Output: An accurate `README.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STACK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md to match the actual codebase</name>
  <files>README.md</files>
  <read_first>README.md, .planning/codebase/STRUCTURE.md, .planning/codebase/ARCHITECTURE.md, config/__init__.py, pyproject.toml</read_first>
  <action>
  Replace README.md's entire content with accurate documentation covering: (1) Project name/description matching PROJECT.md's stated purpose — a config-driven quant research pipeline (data -&gt; factor -&gt; return model -&gt; portfolio optimization -&gt; target holdings -&gt; backtest), xarray/Zarr as the canonical in-memory/on-disk data format; (2) Actual top-level package layout with a one-line purpose each: `base/` (abstract base classes: DataBackend, Dataset, FactorKunQuant, BaseModel), `dataset/` (concrete Dataset/DataBackend implementations: SpotKlineDataset, StockDataset, XrBackend, PlBackend), `factor/` (Alpha101SpotKline, Alpha101Stock, Alpha158SpotKline via KunQuant), `label/` (SpotReturn, SpotBinaryReturn), `my_ops/` (custom KunQuant ops), `dl_model/` (MLPRegressor, RNNRegressor, RNNClassifier torch models) and `ml_model/` (joblib-based persistence, no concrete model yet), `backtest/` (Nautilus Trader live/backtest Strategy) and `vecbt/` (vectorbt signal backtest helper), `config/` (config factory functions + instruments.yaml), `enums/` (shared constants), `utils/` (timer/file/binance/nautilus/module/asdict helpers), `scripts/` (Tiingo downloader); (3) Actual entry-point scripts instead of the fictitious `examples/train_models.py`: `cal.py`, `train_model.py`, `test.py`, `get_binance_instruments.py`, `read_mock_data_sink.py`, `scripts/download_stock_data_from_tiingo.py`; note there is no single unified CLI — each script builds its own configs; (4) Installation via `uv sync` (Python &gt;=3.13, dependency-managed via `uv`), not `pip install -r requirements.txt`; (5) Required environment variables: `TIINGO_API_KEY` (required to run `scripts/download_stock_data_from_tiingo.py`), `WANDB_API_KEY` (required for W&amp;B experiment tracking during model training), `QUANTLAB_DATA_DIR` (optional — overrides the default `./data` root used by `config/__init__.py`'s factory functions; if unset, data defaults to a `data/` directory at the repo root); (6) Actual config dataclasses from `base/config.py`: `DatasetConfig`, `FactorConfig`, `DLConfig`, `MLConfig` (not the fictitious `ModelConfig`); (7) Actual model classes from `dl_model/`: `MLPRegressor`, `RNNRegressor`, `RNNClassifier` (not `LSTMModel`/`XGBoostModel`, which do not exist in this codebase). Do not reference any file path, class name, or command that does not exist in the current repo tree as of this plan.
  </action>
  <acceptance_criteria>
    - README.md contains none of: `models/lstm_model.py`, `examples/train_models.py`, `ModelConfig`, `XGBoostModel`, `requirements.txt`.
    - README.md mentions `uv sync` and `TIINGO_API_KEY`.
    - README.md's described top-level directories (`base/`, `dataset/`, `factor/`, `label/`, `dl_model/`, `ml_model/`, `backtest/`, `vecbt/`, `config/`, `enums/`, `utils/`, `scripts/`) each correspond to an existing directory in the repo.
  </acceptance_criteria>
  <verify>
    <automated>! grep -qE "models/lstm_model\.py|examples/train_models\.py|ModelConfig|XGBoostModel|requirements\.txt" README.md && grep -q "uv sync" README.md && grep -q "TIINGO_API_KEY" README.md</automated>
  </verify>
  <done>README.md's description, structure, and usage sections describe the actual current module layout, entry points, and setup process, with no reference to non-existent files or classes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None new — documentation-only change with no code execution path.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | n/a | README.md | accept | Pure documentation edit; no trust boundary, no executable code, no credential handling. |
</threat_model>

<verification>
Run Task 1's `<verify><automated>` command. Manually skim the rewritten README against `.planning/codebase/STRUCTURE.md`'s Directory Layout to confirm no drift.
</verification>

<success_criteria>
- README.md accurately describes the current module layout and how to install/run the pipeline, with zero references to non-existent files.
</success_criteria>

<output>
Create `.planning/phases/01-codebase-cleanup-security-hardening/01-04-SUMMARY.md` when done
</output>
