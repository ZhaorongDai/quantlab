---
phase: 01-codebase-cleanup-security-hardening
plan: 05
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03", "01-04"]
files_modified: []
autonomous: false
requirements: [CLEAN-01]

must_haves:
  truths:
    - "git log --all shows no commit anywhere in local history containing the leaked Tiingo API key literal"
    - "The user has been explicitly prompted, in this plan, to rotate the leaked Tiingo key out-of-band in the Tiingo dashboard (a step this plan cannot automate)"
    - "The origin remote (github.com/ZhaorongDai/quantlab.git) is untouched — nothing was pushed or force-pushed as part of this plan"
  artifacts:
    - path: ".git"
      provides: "single fresh commit history with no leaked-credential trace in any commit or reflog entry"
  key_links: []
---

<objective>
Reset local git history to a single fresh commit containing no commit that includes the leaked Tiingo API key (CLEAN-01), per the user's own logged decision in PROJECT.md's Key Decisions table ("Git 历史重置为全新仓库...将在清理阶段执行,执行前会再次与用户确认"). This MUST run last, after every other Wave 1/2 cleanup plan in this phase has landed and been verified — it captures the final, cleaned working-tree state into the one fresh commit.

Purpose: The current `Initial commit` (64200da) is already pushed to the public GitHub remote and contains the hardcoded Tiingo key literal. Local history must no longer contain it anywhere (any commit, any reflog entry) going forward.
Output: A single-commit local git history with no trace of the leaked key. The `origin` remote is explicitly NOT modified by this plan — no push, no force-push. The already-leaked key itself is separately, manually rotated by the user in the Tiingo dashboard (Task 2) since a local history rewrite cannot retroactively un-leak a key that is already public on GitHub.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-reset verification — confirm clean tree and no remaining leaked-secret trace</name>
  <files>none (verification only)</files>
  <read_first>scripts/download_stock_data_from_tiingo.py, config/__init__.py</read_first>
  <action>
  Before touching git history, confirm the repository is in the expected post-cleanup state. Run `git status --porcelain` and confirm it is empty (all changes from plans 01-01 through 01-04 are committed on the current branch — if not, stop and report rather than proceeding). Run `git grep` (or `grep -r`, excluding `.git/` and `.venv/`) across all tracked files for the literal leaked key string `<REDACTED_LEAKED_TIINGO_KEY>` and confirm zero matches (this literal must already be gone per plan 01-01's Task 1). Run a broader secret-shaped scan across tracked `*.py` files (pattern like `(api[_-]?key|secret|token|password)\s*=\s*['"][A-Za-z0-9]{16,}`) and manually review any hits — the only known finding (the Tiingo key) must already be resolved; report anything else found rather than silently proceeding. Record the current commit count and log for reference in the SUMMARY by running `git log --oneline --all | wc -l` and `git log --oneline --all` directly (do not assume or hardcode a specific expected count — the count grows by one with every plan/commit executed before this one runs, so derive it live rather than trusting any number written into this plan). Confirm the oldest commit is still `64200da Initial commit` (`git log --oneline --all | tail -1`).
  </action>
  <acceptance_criteria>
    - `git status --porcelain` output is empty.
    - Zero matches for the leaked key literal across tracked files.
    - Secret-shaped grep scan reviewed with no unresolved hardcoded-credential hits reported.
    - SUMMARY records the live-derived commit count (via `git log --oneline --all | wc -l`), not a hardcoded literal from this plan.
  </acceptance_criteria>
  <verify>
    <automated>test -z "$(git status --porcelain)" && ! git grep -q "<REDACTED_LEAKED_TIINGO_KEY>"</automated>
  </verify>
  <done>Working tree is clean and confirmed free of the leaked key literal and any other unresolved hardcoded-credential pattern before history is rewritten.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Manual Tiingo API key rotation reminder (non-automatable)</name>
  <action>Present the Tiingo key rotation reminder below to the user and wait for their acknowledgment before continuing to Task 3 — this step cannot be automated because Claude has no Tiingo account credentials or session.</action>
  <what-built>
  The leaked Tiingo API key (`<REDACTED_LEAKED_TIINGO_KEY>`) was pushed to the public GitHub remote `github.com/ZhaorongDai/quantlab.git` in the repo's `Initial commit`. Task 4 of this plan will rewrite *local* git history so this key no longer appears in any local commit or reflog — but that does NOT retroactively revoke the key or scrub it from GitHub's server-side history, caches, or any fork/clone made before this reset. The key must be treated as permanently compromised regardless of the local history reset. Rotating/revoking it requires logging into your own Tiingo account — Claude has no Tiingo credentials or session and cannot perform this step via any CLI/API available in this environment.
  </what-built>
  <how-to-verify>
  1. Log into the Tiingo dashboard: https://api.tiingo.com/account/api/token
  2. Revoke the leaked key and/or generate a new one.
  3. Export the new key as `TIINGO_API_KEY` in your local shell environment (e.g. your `.bash_profile`/`.zshrc`) — never commit it to any file. `scripts/download_stock_data_from_tiingo.py` (fixed in plan 01-01) now reads it exclusively from this env var.
  This step is independent of the git history reset in Task 4 below — you can do it before, after, or defer it, and either way the reset in Task 4 will proceed once you resume.
  </how-to-verify>
  <resume-signal>Type "rotated" once you've revoked/rotated the key in the Tiingo dashboard, or "will-do-later" to proceed with the git history reset now and rotate the key separately afterward.</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Final go/no-go for the destructive git history reset</name>
  <action>Present the decision below to the user and wait for their explicit selection before Task 4 (the actual destructive git history rewrite) is allowed to run.</action>
  <decision>Proceed with resetting local git history to a single fresh commit (discarding all existing commits, including the `Initial commit` that contains the leaked Tiingo key), or abort and keep the current history for now?</decision>
  <context>
  This is CLEAN-01 and matches the user's own already-logged decision in PROJECT.md's Key Decisions table ("Git 历史重置为全新仓库,不保留当前含泄露 Key 的提交" — outcome explicitly marked "将在清理阶段执行,执行前会再次与用户确认", i.e. "will be executed during cleanup, confirmed with the user again before execution"). This confirmation IS that re-confirmation.

  This is a destructive, git-history-rewriting operation:
  - After Task 4 runs, `git log --all` will show exactly ONE commit. The current multi-commit history (`64200da` Initial commit ... through whatever the tip is by the time this plan runs, per Task 1's live-derived count) will be gone from this local repository (old commit objects become unreachable and are pruned).
  - The `origin` remote (`https://github.com/ZhaorongDai/quantlab.git`) will NOT be touched by Task 4 — no `git push` or `git push --force` is performed. The current history, including the leaked key, remains on GitHub exactly as it is today until you separately decide to force-push the new history or delete/recreate the remote repository — that decision and action is yours to make outside this plan.
  - This does not depend on your answer to the previous Tiingo-key-rotation checkpoint; the two are independent security actions.
  </context>
  <options>
    <option id="proceed">
      <name>Proceed with the reset now</name>
      <pros>Closes CLEAN-01; local history will contain no trace of the leaked key going forward, matching the user's already-stated decision</pros>
      <cons>Irreversible once old refs are pruned in this local clone (though the commits still exist on GitHub's `origin` until you separately act on that)</cons>
    </option>
    <option id="abort">
      <name>Abort — keep current history for now</name>
      <pros>No destructive action taken; can be re-run later</pros>
      <cons>CLEAN-01 remains unmet; leaked key stays in local history (already public on GitHub regardless)</cons>
    </option>
  </options>
  <resume-signal>Select: proceed or abort</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Execute the local git history reset (only if Task 3 resumed with "proceed")</name>
  <files>none (git internals only — no tracked file content changes)</files>
  <read_first>none</read_first>
  <action>
  Run, in order, from the repo root: `git checkout --orphan _fresh_history_tmp`; `git add -A`; `git commit -m "chore: fresh repository history (no leaked credentials)"`; `git branch -D main`; `git branch -m main`; `git reflog expire --expire=now --all`; `git gc --prune=now --aggressive`. Do NOT run `git push`, `git push --force`, `git push --force-with-lease`, or any command that writes to the `origin` remote — this plan is local-history-only. After completion, confirm the current branch is `main` (`git branch --show-current`) and that `git remote -v` still shows the original `origin` URL unchanged (do not add, remove, or repoint any remote).
  </action>
  <acceptance_criteria>
    - `git log --all --oneline` shows exactly 1 commit.
    - `git log --all -p` contains zero occurrences of the leaked key literal `<REDACTED_LEAKED_TIINGO_KEY>`.
    - `git reflog --all` shows no entry referencing the old `64200da` commit after expiry.
    - `git remote -v` still shows `origin https://github.com/ZhaorongDai/quantlab.git` unchanged.
    - `git branch --show-current` returns `main`.
  </acceptance_criteria>
  <verify>
    <automated>test "$(git log --all --oneline | wc -l | tr -d ' ')" = "1" && ! git log --all -p | grep -q "<REDACTED_LEAKED_TIINGO_KEY>" && [ "$(git branch --show-current)" = "main" ]</automated>
  </verify>
  <done>Local git history contains a single commit with no trace of the leaked Tiingo API key anywhere in history or reflog; the origin remote is unmodified and nothing has been pushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local `.git` history &lt;-&gt; public GitHub remote (`origin` = github.com/ZhaorongDai/quantlab.git) | The already-leaked credential crossed this boundary the moment `Initial commit` was pushed; a local-only history reset cannot retract what is already public on the remote/any fork/clone |
| Tiingo account dashboard &lt;-&gt; this agent | Out-of-band, human-only boundary — Claude has no Tiingo session/credentials and cannot rotate the key itself |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Information Disclosure | `scripts/download_stock_data_from_tiingo.py` @ commit `64200da` (historical) | mitigate | Local history squashed to a single fresh commit with no reference to the leaked key literal in any commit or reflog entry (Task 4). Explicitly does NOT retract the already-public copy on GitHub — that residual risk is handled by the out-of-band key rotation called out in Task 2, since the key must be treated as compromised regardless of what happens to local history. |
| T-01-05-02 | Repudiation / Tampering | `origin` remote (github.com/ZhaorongDai/quantlab.git) | accept | This plan intentionally does not push or force-push to `origin`; the stale, key-containing history remains on GitHub until the user separately force-pushes the new history or recreates the remote repository — explicitly out of scope for this plan per the user's own phase-context instruction. |
| T-01-05-03 | Tampering (accidental data loss) | local `.git` history reset | mitigate | Task 1 requires a fully clean working tree (all prior plans' work committed) before proceeding; Task 3 requires explicit human confirmation before Task 4 executes; Task 4 never touches `origin`, so even a mistaken local reset does not propagate. |
</threat_model>

<verification>
Run Task 1 and Task 4's `<verify><automated>` commands (Task 4 only after the Task 3 checkpoint is resumed with "proceed"). Confirm `git remote -v` output is identical before and after this plan runs.
</verification>

<success_criteria>
- `git log --all` shows a fresh history with no commit containing the leaked Tiingo API key.
- The user has been explicitly, unambiguously prompted to rotate the key out-of-band, independent of the git reset.
- `origin` remains untouched — no push or force-push occurred.
</success_criteria>

<output>
Create `.planning/phases/01-codebase-cleanup-security-hardening/01-05-SUMMARY.md` when done
</output>
</output>
