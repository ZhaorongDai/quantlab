---
phase: 01-codebase-cleanup-security-hardening
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - vecbt/bt.py
  - get_binance_instruments.py
autonomous: true
requirements: [CLEAN-02]

must_haves:
  truths:
    - "Calling vecbt.bt.backtest_from_signals with valid Series arguments returns a working Portfolio object instead of raising a runtime error"
    - "get_binance_instruments.py no longer maintains its own duplicate copy of Binance exchange-info fetch/parse logic"
  artifacts:
    - path: "vecbt/bt.py"
      provides: "working backtest_from_signals that passes its parameters through to vbt.Portfolio.from_signals"
      contains: "short_entries=short_entries"
    - path: "get_binance_instruments.py"
      provides: "single-source Binance parsing via utils.binance, no duplicate definitions"
      contains: "from utils.binance import"
  key_links:
    - from: "vecbt/bt.py:backtest_from_signals"
      to: "vectorbt.Portfolio.from_signals"
      via: "explicit close/entries/exits/short_entries/short_exits arguments"
      pattern: "vbt\\.Portfolio\\.from_signals\\(\\s*close"
    - from: "get_binance_instruments.py"
      to: "utils/binance.py"
      via: "import _get_binance_exchange_info, _parse_symbol_info"
      pattern: "from utils\\.binance import"
---

<objective>
Fix the two concrete broken/duplicate-code findings from the codebase review (CLEAN-02): `vecbt/bt.py:backtest_from_signals` calls `vbt.Portfolio.from_signals()` with no arguments (guaranteed runtime error), and `get_binance_instruments.py` maintains its own near-identical copy of `utils/binance.py`'s exchange-info fetch/parse logic.

Purpose: Both are the explicit dead/broken/duplicate-code examples named in ROADMAP.md's Phase 1 success criteria; leaving them means CLEAN-02 is not met and the vectorbt backtest path (needed again in Phase 6) stays unusable.
Output: A working `backtest_from_signals`; a single source of truth for Binance instrument parsing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
</context>

<interfaces>
<!-- utils/binance.py — the module get_binance_instruments.py will import from. Both underscore-prefixed
helpers are internal-by-convention but already used this way as the module's public surface for
reuse within this single-owner repo. -->

From utils/binance.py:
```python
def get_instrument_info(symbols: list[str]) -> dict: ...
def _get_binance_exchange_info() -> Dict[str, Any]: ...
def _parse_symbol_info(symbol_data: Dict[str, Any]) -> Dict[str, Any]: ...
```

<!-- The working vbt.Portfolio.from_signals call pattern to mirror, from train_model.py:130-138 -->
```python
p = vbt.Portfolio.from_signals(
    data["price"],
    entries=data["long"] == 1,
    exits=data["short"] == 1,
    short_entries=data["short"] == 1,
    short_exits=data["long"] == 1,
    fees=0.0,
)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix vecbt/bt.py:backtest_from_signals to pass its own parameters through</name>
  <files>vecbt/bt.py</files>
  <read_first>vecbt/bt.py, train_model.py</read_first>
  <behavior>
    - Given close/long_entries/long_exits/short_entries/short_exits pandas Series aligned to a shared DatetimeIndex, calling `backtest_from_signals(close, long_entries, long_exits, short_entries, short_exits, index)` returns a `vectorbt.portfolio.base.Portfolio` instance (does not raise).
    - The returned Portfolio reflects the reindexed `close` and the passed-through entries/exits (i.e. it is not constructed from empty/default arguments).
  </behavior>
  <action>
  In `backtest_from_signals`, after the existing `close = close.reindex(index)` line, replace the bare `vbt.Portfolio.from_signals()` call (currently invoked with zero arguments — guaranteed to raise) with `vbt.Portfolio.from_signals(close, entries=long_entries, exits=long_exits, short_entries=short_entries, short_exits=short_exits)`, mirroring the working pattern at `train_model.py:130-138`. Keep the function's existing signature (`close, long_entries, long_exits, short_entries, short_exits, index`) unchanged. Add `return p` at the end of the function so callers can use `.stats()`/`.plot()` on the result (currently the Portfolio object is built and discarded).
  </action>
  <acceptance_criteria>
    - `vbt.Portfolio.from_signals()` (zero-argument call) no longer appears in the file.
    - `backtest_from_signals` contains a `return` statement returning the constructed Portfolio.
    - Calling the function with synthetic 5-row boolean Series does not raise and returns an object whose type name contains `Portfolio`.
  </acceptance_criteria>
  <verify>
    <automated>uv run python -c "import pandas as pd; from vecbt.bt import backtest_from_signals; idx = pd.date_range('2024-01-01', periods=5, freq='D'); close = pd.Series([1,2,3,2,1], index=idx); long_e = pd.Series([True,False,False,False,False], index=idx); long_x = pd.Series([False,False,True,False,False], index=idx); short_e = pd.Series([False]*5, index=idx); short_x = pd.Series([False]*5, index=idx); p = backtest_from_signals(close, long_e, long_x, short_e, short_x, idx); assert 'Portfolio' in type(p).__name__, type(p); print('OK')"</automated>
  </verify>
  <done>backtest_from_signals passes its own close/entries/exits/short_entries/short_exits through to vbt.Portfolio.from_signals and returns a usable Portfolio object; a synthetic smoke-test call succeeds without raising.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate duplicate Binance exchange-info parsing into utils/binance.py</name>
  <files>get_binance_instruments.py</files>
  <read_first>get_binance_instruments.py, utils/binance.py</read_first>
  <action>
  Remove the `get_binance_exchange_info()` and `parse_symbol_info()` function definitions from `get_binance_instruments.py` — they near-identically duplicate `utils/binance.py:_get_binance_exchange_info`/`_parse_symbol_info`. Add `from utils.binance import _get_binance_exchange_info, _parse_symbol_info` near the top of `get_binance_instruments.py` (alongside the existing `requests`/`yaml`/`json`/`pathlib` imports). Update every call site inside `update_instruments_config()` and `get_all_usdt_pairs()` that currently calls the now-removed local `get_binance_exchange_info()`/`parse_symbol_info()` to call the imported `_get_binance_exchange_info()`/`_parse_symbol_info()` instead. Leave `update_instruments_config`, `get_all_usdt_pairs`, and the `argparse`-based `if __name__ == "__main__":` CLI block otherwise unchanged — their config-merging, fees/margin-default, and CLI behavior stays exactly as-is.
  </action>
  <acceptance_criteria>
    - No `def get_binance_exchange_info` or `def parse_symbol_info` definitions remain in the file.
    - `from utils.binance import` appears exactly once.
    - `update_instruments_config` and `get_all_usdt_pairs` call the imported `_get_binance_exchange_info`/`_parse_symbol_info` (not undefined names).
    - `import get_binance_instruments` succeeds with no `NameError`/`ImportError` (module-level code does not trigger a live network call — only the `if __name__ == "__main__"` block does).
  </acceptance_criteria>
  <verify>
    <automated>[ "$(grep -cE "^def (get_binance_exchange_info|parse_symbol_info)\(" get_binance_instruments.py)" = "0" ] && grep -q "from utils.binance import" get_binance_instruments.py && uv run python -c "import get_binance_instruments; print('OK')"</automated>
  </verify>
  <done>get_binance_instruments.py no longer redefines Binance exchange-info fetch/parse logic; it imports and calls utils.binance's implementation; update_instruments_config/get_all_usdt_pairs/argparse CLI behavior is unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local process &lt;-&gt; Binance public REST API | `utils/binance.py`'s exchange-info calls are unauthenticated, public endpoints; no credential crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering (integrity/drift) | `get_binance_instruments.py` duplicate parsing logic | mitigate | Consolidating to a single implementation (Task 2) removes the risk of the two copies silently drifting apart after a future filter-type change on Binance's side. |
| T-01-03-02 | n/a (correctness, not security) | `vecbt/bt.py:backtest_from_signals` | accept | This is a code-correctness fix, not a security-relevant change; no new trust boundary is crossed (still local, in-process pandas/vectorbt computation on already-loaded data). |
</threat_model>

<verification>
Run both tasks' `<verify><automated>` commands. Confirm `git diff vecbt/bt.py get_binance_instruments.py` touches only the intended functions and imports, with no behavior change to `update_instruments_config`'s config-merge logic.
</verification>

<success_criteria>
- `backtest_from_signals` runs without error when called with valid arguments and returns a Portfolio object.
- `get_binance_instruments.py` and `utils/binance.py` share one implementation of Binance exchange-info fetch/parse logic.
</success_criteria>

<output>
Create `.planning/phases/01-codebase-cleanup-security-hardening/01-03-SUMMARY.md` when done
</output>
