# Phase 03.12: CRSP 复权锚点改为 store 首个可用行 - Pattern Map

**Mapped:** 2026-09-22
**Files analyzed:** 8（1 新建 / 5 改测试 / 1 改实现 / 1 改文档）
**Analogs found:** 8 / 8

> 文件清单来自 `03.12-RESEARCH.md` **§19.5**（删除 13 / 保留 8 / 改动 4），**不是** §1 的原始三张清单。
> `quantlab/base/config.py` 一个字节不改；store 保持 27 个 `data_vars`；五个 `adj*` 仍是落盘列。
> 所有 analog 路径均已确认为 git-tracked 源码（主仓库工作树，无 `.gsd/capabilities` 镜像）。

---

## File Classification

| 新建/修改文件 | Role | Data Flow | 最接近的既有类比 | 匹配度 |
|---|---|---|---|---|
| `tests/test_crsp_first_anchor.py`（**新建**，I-1..I-4） | test | batch / file-I/O | `tests/test_crsp_dataset.py`（同一 fixture 套路），尤其 `:1049-1087` | exact |
| ↳ I-2（全量 vs 增量 `assert_identical`） | test | batch | `tests/test_crsp_dataset.py:1049-1087` + `tests/test_dataset_update.py:182-201` | exact（两半拼合） |
| `tests/test_crsp_rebuild_measurements.py`（加 I-1/I-5 真实数据门 + 改 `EXPECTED_WRITTEN_SIDECARS`） | test | integration / file-I/O | 本文件自身的 `rebuilt` fixture + `test_sp500_2024_rebuild_matches_post_fix_measurements` | exact（同文件内） |
| `tests/test_crsp_dataset.py:1090-1130` → `test_the_anchor_is_the_FIRST_usable_row` | test | batch | 原用例自身（只搬 fixture 行序 + 改名） | exact |
| `tests/test_crsp_tracer.py:200/218/219`、`tests/test_crsp_identity.py:1066-1069`、`tests/test_crsp_dataset.py:461` | test | batch | `tests/test_crsp_tracer.py:193-225` 的 `at()` 断言块 | exact |
| 「锚点机械零残留」gate（可新建，或并入既有 residue 文件） | test | static scan | **`tests/test_no_identity_residue.py`（03.11 的现成范本）** | exact |
| `quantlab/dataset/crsp/__init__.py`（M-A/M-C + 13 条删除） | dataset（transform core） | batch transform | 本文件自身 `_derivation()`；删除类比见 `rebuild.py:49-53` 的 R-05 判例 | exact |
| `example/wrds_crsp.md`（若干段落） | doc | — | `quantlab/dataset/crsp/rebuild.py:49-53` 的「不再生成但留在清单」措辞 | role-match |

---

## Pattern Assignments

### 1. `tests/test_crsp_first_anchor.py`（新建，test / batch）

**Analog:** `tests/test_crsp_dataset.py`

#### 1.1 模块 docstring + import 约定（`tests/test_crsp_dataset.py:1-38`，逐字）

```python
"""CRSP panel semantics: prices, returns, events and the adjustment anchor.
...
**Every quantlab import is INSIDE a test or helper body.** That is not style:
these tests are written before the names they assert on exist, and a
module-scope import would turn the RED run into a collection error -- zero
tests discovered, which proves nothing about the behaviour (TDD gate #3770).

**Provenance rule, inherited from `tests/crsp_fixtures.py`.** A row reused from
that module is VERBATIM live data; a row built here with `dsf_row` is invented
and carries a `# SYNTHETIC` comment. Every synthetic row keeps `dsf_row`'s
NS/EQTY/COM defaults, so plan 08's common-stock filter keeps it.
"""

from __future__ import annotations

from datetime import date

import pytest

SYNTHETIC_PERMNO = "10107"
SYNTHETIC_SYMBOL = "MSFT"
SYNTHETIC_AXIS = int(SYNTHETIC_PERMNO)
```

⚠️ 这两条规则（**quantlab import 一律在函数体内**；**合成行必须带 `# SYNTHETIC` 注释**）在新文件里必须复刻。

#### 1.2 fixture 构造五件套（`tests/test_crsp_dataset.py:66-207`，逐字骨架）

新文件要么 `from tests.test_crsp_dataset import _pull, _dataset_config, _convert, _panel, _build, _at`，
要么复刻。**推荐复刻**（既有 CRSP 测试文件之间互不 import helper，各自持有一份；
`tests/test_crsp_tracer.py` 甚至把 `at()` 定义成测试体内的闭包）。骨架：

```python
def _synthetic_secinfo():
    """A `stksecurityinfohist` interval for `SYNTHETIC_PERMNO`."""
    from tests.crsp_fixtures import secinfo_row
    return [secinfo_row(int(SYNTHETIC_PERMNO), "1986-03-13", "2025-12-31",
                        SYNTHETIC_SYMBOL, SYNTHETIC_SYMBOL, None)]


def _reference_rows(extra_secinfo=()):
    from tests.crsp_fixtures import (CCM_ROWS, DELISTS_ROWS, DISTRIBUTION_ROWS,
                                     DSP500_ROWS, IDXCST_ROWS, SECINFO_ROWS)
    return {
        "crsp_a_stock.stksecurityinfohist": list(SECINFO_ROWS) + list(extra_secinfo),
        "crsp_a_stock.stkdelists": list(DELISTS_ROWS),
        "crsp_a_stock.stkdistributions": list(DISTRIBUTION_ROWS),
        "crsp_a_indexes.dsp500list_v2": list(DSP500_ROWS),
        "comp.idxcst_his": list(IDXCST_ROWS),
        "crsp_a_ccm.ccmxpf_lnkhist": list(CCM_ROWS),
    }


def _pull(tmp_path, rows, permnos, *, start, end,
          product_end="2025-12-31", extra_secinfo=()):
    """Serve `rows` through the fake session, land the raw + reference tiers."""
    from quantlab.acquisition.wrds.crsp import WrdsCrspDailyAcquisition
    from tests.crsp_fixtures import (FakeCrspSession, run_crsp_pull,
                                     write_reference_tables)

    FakeCrspSession.daily_rows = list(rows)
    FakeCrspSession.product_end = date.fromisoformat(product_end)

    cfg, result = run_crsp_pull(tmp_path, permnos, start_date=start, end_date=end)
    assert result.failures == {}, result.failures

    reference_dir = WrdsCrspDailyAcquisition.reference_dir_for(cfg)
    write_reference_tables(reference_dir, _reference_rows(extra_secinfo),
                           product_end=product_end)
    return cfg, str(reference_dir)


def _dataset_config(tmp_path, cfg, reference_dir, *, start, end, store="crsp.zarr"):
    from quantlab.base.config import CrspDatasetConfig
    return CrspDatasetConfig(
        zarr_file_path=str(tmp_path / store),
        raw_data_dir_path=cfg.raw_data_dir_path,
        catalog_path=str(tmp_path / "catalog"),
        reference_dir=str(reference_dir),
        start_date=start,
        end_date=end,
    )


def _convert(dataset_config, granularity="year"):
    from quantlab import registry
    from quantlab.acquisition.wrds import WRDS_SOURCE
    return registry.convert(WRDS_SOURCE, dataset_config,
                            data_type="crsp_daily", granularity=granularity)


def _panel(dataset_config):
    import xarray as xr
    return xr.open_zarr(dataset_config.zarr_file_path).load()


def _at(panel, variable, day, symbol):
    return float(panel[variable].sel(timestamp=day, symbol=symbol).values)
```

**fixture 参数**：每个用例签名第一个参数是 `mock_crsp_session`（`tests/conftest.py:1246`），
第二个是 `tmp_path`。`mock_crsp_session` 会 reset `FakeCrspSession` 并把它 patch 到
`quantlab.acquisition.wrds.taq.WrdsSession`。

#### 1.3 合成行的写法（`tests/test_crsp_dataset.py:1097-1110`，逐字）

```python
    from tests.crsp_fixtures import dsf_row

    rows = [  # SYNTHETIC: the window's LAST row has no price.
        dsf_row(SYNTHETIC_PERMNO, "2020-03-02", dlyprc="100.000000", dlyret="0.010000"),
        dsf_row(SYNTHETIC_PERMNO, "2020-03-03", dlyprc="102.000000", dlyret="0.020000"),
        dsf_row(
            SYNTHETIC_PERMNO,
            "2020-03-04",
            dlyprc=None,
            dlyprcflg=None,
            dlyret=None,
            dlyretmissflg="MP",
        ),
    ]
```

`dsf_row(permno, day, **overrides)`（`tests/crsp_fixtures.py:162`）铺满 50 列，默认是
NS/EQTY/COM/`dlyprcflg="TR"`/`dlyvol=1_000_000`/单位因子的「普通交易日」。
**I-1 的 fixture 就是把上面那个无价格行从末尾搬到开头**（`2020-03-01` 无价格，
`03-02`/`03-03` 有价），断言锚落在第二行。

#### 1.4 I-2 的直接姐妹：`test_year_and_month_granularity_produce_identical_stores`（`tests/test_crsp_dataset.py:1049-1087`，**逐字**）

```python
def test_year_and_month_granularity_produce_identical_stores(
    mock_crsp_session, tmp_path
):
    """D-08: the anchor is GLOBAL to the configured window, so chunking is
    invisible in the output.

    Two converts of the SAME raw tier into two paths, one planning two
    year-windows and one planning eleven month-windows. `assert_identical`
    compares every variable, coordinate and name -- a per-window anchor would
    put a fabricated return at each of the ten extra seams, so this is the
    test Pitfall 1's warning sign was written for.
    """
    import xarray as xr

    cfg, reference_dir = _pull(
        tmp_path,
        _anchor_series_rows(),
        [SYNTHETIC_PERMNO],
        start=ANCHOR_START,
        end=ANCHOR_END,
        extra_secinfo=_synthetic_secinfo(),
    )

    stores = {}
    for granularity in ("year", "month"):
        dataset_config = _dataset_config(
            tmp_path,
            cfg,
            reference_dir,
            start=ANCHOR_START,
            end=ANCHOR_END,
            store=f"crsp_{granularity}.zarr",
        )
        result = _convert(dataset_config, granularity=granularity)
        stores[granularity] = _panel(dataset_config)
        assert result.granularity == granularity

    xr.testing.assert_identical(stores["year"], stores["month"])
    assert stores["year"].sizes["timestamp"] > 200, stores["year"].sizes
```

**I-2 照抄这个形状，只换两处**：(a) 两个 store 的差别从 `granularity` 换成
「一次性 `[A_start, B_end]`」 vs 「先 `[A_start, A_end]` 再 `update()` 到 `B_end`」；
(b) `_pull` 一次、`_dataset_config` 两个不同 `store=` 路径。

增量那一半的既有写法来自 `tests/test_dataset_update.py:170-201`：

```python
    StockDataset(config).from_raw_data_chunked(granularity="year")
    ...
    StockDataset(config).update(granularity="year")
```

⚠️ `BaseDataset.update()`（`quantlab/base/data.py:380-393`）就是
`from_raw_data_chunked(..., on_new_listing=self._AUTOMATIC)`。CRSP 侧要改 `end_date` 再 `update()`，
`CrspDatasetConfig` 的 `end_date` 变更会触发 config setter 清 `_derivation_cache`
（`crsp/__init__.py:494-497`，保留清单 K8）。

#### 1.5 `_anchor_series_rows()` 生成器（`tests/test_crsp_dataset.py:972-1017`）

I-2 需要一条跨两个 year 窗口的合成序列，`_anchor_series_rows()` 是现成的（
`ANCHOR_START="2019-11-01"` / `ANCHOR_END="2020-09-30"`，含 `2020-08-31` 的 4:1 拆股与
`2020-08-07` 的分红），跨 2019/2020 两年 ⇒ **I-2 可直接复用它**，把 A 窗口切在 `2019-12-31`。

#### 1.6 I-3 / I-4 的断言形状（`tests/test_crsp_tracer.py:204-215`，逐字）

```python
    assert at("adjClose", "2020-08-28") / at("adjClose", "2020-08-31") == (
        pytest.approx(1 / 1.033912, rel=1e-9)
    )
```

比值断言一律带显式 `rel=1e-9`（§19.4.2：两锚下比值最大相对差 `4.900e-16`）。
I-1 的锚行等式按 §19.4.5 **必须 `rtol`**：`pytest.approx(close_value, rel=1e-12)`，
**不能用 `==`**（真实 store 上精确 `==` 只有 514/520）。

---

### 2. `tests/test_crsp_rebuild_measurements.py`（改，test / integration）

**Analog:** 文件自身。

#### 2.1 真实数据根的取法（`:179-224`，逐字关键段）

```python
def _data_root() -> Path:
    """The main repository root, from `QUANTLAB_DATA_ROOT`.

    `pytest.fail` rather than `pytest.skip` on every failure path, and no
    conditional-skip marker appears anywhere in this module. A skipped data
    gate reads as a pass in every summary line an operator will ever look at,
    which makes it worse than no gate at all.
    """
    raw = os.environ.get("QUANTLAB_DATA_ROOT")
    if not raw:
        pytest.fail(
            "QUANTLAB_DATA_ROOT is not set. This gate rebuilds the REAL CRSP "
            "store, which lives in the main repository (data/ is gitignored "
            "and therefore absent from any worktree). Run this file as:\n"
            '  QUANTLAB_DATA_ROOT="$(dirname "$(git rev-parse '
            '--path-format=absolute --git-common-dir)")" \\\n'
            "    uv run pytest -q -s tests/test_crsp_rebuild_measurements.py "
            "-p no:cacheprovider\n"
            "Failing rather than skipping on purpose: a skip here is "
            "indistinguishable from a pass."
        )
```

⚠️ **本机无真实数据时它不 skip，而是 fail**——这与 `project_gsd_worktree_verify_path` 一致：
数据根由 `--git-common-dir` 推导，**绝不写死绝对路径**。新用例沿用同一 `rebuilt` fixture，不得引入
`skipif`（模块 docstring `:29-35` 说明：`grep -c` 该 marker 必须为 0）。

#### 2.2 新用例挂到 `rebuilt` fixture 上（`:339-388` + `:429-440`）

```python
@pytest.fixture(scope="module")
def rebuilt():
    """Rebuild the real store ONCE and hand both tests the measurement."""
    root = _data_root()
    backup_dir = _fresh_backup_dir(root)
    assert not backup_dir.exists(), (...)
    print(f"backup generation: {backup_dir}")
    started_at = time.time()
    config = _config(root)
    rebuilder = CrspStoreRebuilder(config, data_root=root)
    measurement = rebuilder.rebuild(backup_dir=backup_dir)
    return measurement, started_at, config


def test_sp500_2024_rebuild_matches_post_fix_measurements(rebuilt):
    """The W0 acceptance gate: five numbers off the freshly-rebuilt panel."""
    measurement, _started_at, _config_used = rebuilt
    print(f"data_root:    {measurement.data_root}")
    print(f"store_path:   {measurement.store_path}")
    print(f"dims:         {measurement.dims}")
    print(f"metrics:      {measurement.metrics}")
```

⚠️ **module-scoped、破坏性、只跑一次**。新的
`test_the_real_store_reproduces_its_adjusted_columns`（I-1 + I-5）必须
**复用同一个 `rebuilt` fixture**，绝不自己再 `rebuild()` 一次。
I-5 的四个不变量走 `measurement.metrics`（§19.6.2 实测 `.first()` 下 **0 / 0 / 764 / 764** 不变）；
I-1 需要 `xr.open_zarr(measurement.store_path)` 自己打开面板做锚行比对。
`print(...)` 是本文件的既定证据形式——SUMMARY 逐行抄录。

#### 2.3 `EXPECTED_WRITTEN_SIDECARS`（`:114-127`，逐字）

```python
#: The sidecars a rebuild must WRITE, each newer than the moment it started.
#:
#: This is deliberately NOT `CRSP_SIDECAR_SUFFIXES`. That tuple is the CLEARING
#: list -- what must be deleted before a rebuild -- and it still carries
#: `.crsp_symbology_report.json` precisely because that file is no longer
#: generated and may be lying around from an older store. The two lists answer
#: different questions, and conflating them would either stop clearing a stale
#: file or demand a file the current tree has no code to write.
EXPECTED_WRITTEN_SIDECARS: tuple[str, ...] = (
    ".chunks.json",
    ".crsp_adjustment.json",
    ".crsp_filter_report.json",
    ".crsp_tickers.json",
)
```

**改动**：删掉 `".crsp_adjustment.json"` 一行（`:124`）。这段注释已经把 R-05 的判例写好了——
不必重写，`.crsp_adjustment.json` 会加入 `.crsp_symbology_report.json` 的行列，成为
「清理清单里有、必写清单里没有」的第二个例子。

---

### 3. R-05 判例本体：`quantlab/dataset/crsp/rebuild.py:25-60`（逐字）

**这是「删除机械但保留清理条目」的既有措辞模板，M-D 照它改写。**

```python
#: Every sidecar that belongs to a CRSP store, as a suffix on the store path.
#:
#: - `.chunks.json` -- `base/chunking.py:ChunkLedger`'s record of which windows
#:   were written, which the next append reconciles against.
#: - `.crsp_adjustment.json` -- the adjustment anchor the adjusted series was
#:   built from. THE reason this phase rebuilds at all: the shipped stores were
#:   written before the no-price-sentinel anchor fix.
...
#: `.crsp_symbology_report.json` stays on this list even though later plans in
#: this phase stop GENERATING it. A suffix dropped from here would leave a file
#: describing a deleted mechanism sitting beside a store it never described --
#: the exact confidently-wrong audit trail the clearing rule exists to prevent.
CRSP_SIDECAR_SUFFIXES: tuple[str, ...] = (
    ".chunks.json",
    ".crsp_adjustment.json",
    ".crsp_filter_report.json",
    ".crsp_symbology_report.json",
    ".crsp_tickers.json",
)
```

**M-D 的改法**：`:29-32` 那三行 `.crsp_adjustment.json` 的说明改写成
「不再生成；留在这张清单上是为了清掉旧 store 旁的遗留」——句式直接套 `:49-53` 的
`.crsp_symbology_report.json` 段。元组本身**一行不动**。

---

### 4. 「锚点机械零残留」gate —— `tests/test_no_identity_residue.py`（03.11 范本，逐字）

**Analog:** `tests/test_no_identity_residue.py`（整文件）。本 phase 新增的 gate 可以是
**同文件加第二张清单**，也可以是平行新文件；03.11 的判例是「一张清单，一个家」。

核心三段（逐字）：

```python
#: The shipping package. The sibling of `tests/`, resolved from this file so
#: the gate works from any working directory.
PACKAGE_ROOT = Path(__file__).resolve().parent.parent / "quantlab"

#: Deleted in 03.11-07. Must be zero NOW.
DELETED_IN_03_11_07: tuple[str, ...] = (
    "_resolve_identity",
    "symbology_report_path",
    "SYMBOLOGY_REPORT_SUFFIX",
    ...
)


def _sources() -> list[Path]:
    """Every `.py` file in the shipping package, sorted."""
    assert PACKAGE_ROOT.is_dir(), f"expected the package at {PACKAGE_ROOT}"
    sources = sorted(PACKAGE_ROOT.rglob("*.py"))
    assert len(sources) > 20, (
        f"only {len(sources)} module(s) walked -- the scan must not be "
        f"vacuous, or a name reintroduced tomorrow would pass unseen"
    )
    return sources


def _hits(names: tuple[str, ...]) -> list[str]:
    """`['path:line: text', ...]` for every occurrence of every name."""
    pattern = re.compile(
        r"(?<![A-Za-z0-9_])(?:" + "|".join(re.escape(n) for n in names) + r")"
        r"(?![A-Za-z0-9_])"
    )
    found: list[str] = []
    for source in _sources():
        relative = source.relative_to(PACKAGE_ROOT.parent)
        for number, line in enumerate(
            source.read_text(encoding="utf-8").splitlines(), start=1
        ):
            if pattern.search(line):
                found.append(f"{relative}:{number}: {line.strip()}")
    return found


def test_the_deleted_identity_machinery_leaves_no_residue_in_quantlab():
    """Zero hits in `quantlab/`, comments included."""
    residue = _hits(DELETED_IN_03_11_07)
    assert residue == [], (
        "phase 03.11 deleted the ticker-identity machinery, but "
        f"{len(residue)} reference(s) to it survive in quantlab/. A reference "
        "in a COMMENT counts and is listed here on purpose: ..."
        + "\n".join(residue)
    )
```

**三条必须继承的设计约束**（模块 docstring `:23-49`）：

1. **注释与 docstring 计入命中**，不做 `grep -v '^#'`、不做丢弃 docstring 的 AST 扫描——
   「注释就是设计记录，指向已删机械的注释是关于系统如何工作的假陈述」。
   这一条对本 phase 尤其要紧：`crsp/__init__.py:10-31 / :51-53 / :516-528 / :596-606 / :706`
   全是 docstring/注释里的 `last`/anchor 叙述。
2. **只扫 `quantlab/`**，`.planning/`、`example/`、`tests/` 故意出范围。
3. **成员规则**：只有**专属于**被删机械的名字才上清单（`_as_date` 因为三个模块各有同名私有 helper 而
   **不上**，「会叫狼的 gate 会被人学会跳过」）。

⇒ 本 phase 的清单候选（§19.5.1 D4/D5/D11-D16）：
`ADJUSTMENT_SIDECAR_SUFFIX`、`ADJUSTMENT_RULE`、`adjustment_sidecar_path`、
`_adjustment_record`、`_assert_anchor_unchanged`、`_write_adjustment_record`、
`total_return_backward_from_last_close`。
⚠️ **`.crsp_adjustment.json` 这个字面量不能上清单**——它必须继续活在
`rebuild.py:CRSP_SIDECAR_SUFFIXES` 里（R-05）。这正是「成员规则」要挡的那类误报。
⚠️ 配套 `find . -name __pycache__ -type d -prune -exec rm -rf {} +`（R-09，stale `.pyc`）。

---

### 5. `_derivation()` 本体：改动前的真实形状（`quantlab/dataset/crsp/__init__.py`）

#### 5.1 缓存早返回 + 锚点 gate（`:513-535`，逐字）

```python
    def _derivation(self) -> pl.DataFrame:
        """The labelled, adjusted frame for the WHOLE configured window.

        Computed once per instance and cached. Every window densifier slices
        this, so every chunk of one conversion shares one anchor per PERMNO.

        **Also the ANCHOR GATE** (WR-02). It sits here, on the FIRST derivation
        of an instance, because this is the one method BOTH conversion entry
        points call: `from_raw_data_chunked` reaches it through
        `_raw_axes_in_range`, and `from_raw_data` through `_raw_data_to_xr`.
        While the gate lived in `_raw_axes_in_range` only the chunked path was
        gated, so `from_raw_data().save()` could extend a store in place onto a
        second anchor -- and, having written no sidecar, latched that store into
        a permanent refusal for every later chunked run. The cache check above
        keeps it once per instance: a re-dated config resets the cache in the
        `config` setter, which is exactly when the anchor must be re-checked.
        """
        cached = getattr(self, "_derivation_cache", None)
        if cached is not None:
            return cached

        self._assert_anchor_unchanged(self._adjustment_record())
```

**改动**：`:519-528` 的 ANCHOR GATE 段删除（D7，但 WR-02 事实要在别处保留）；
`:534` 整行删除（D6）。`:530-532` 的缓存早返回**保留**（K8），但 `:516-517`
「缓存保证每个 chunk 共享一个锚」的因果要改写成「全窗口取数 + 派生后切片保证同一个锚；
缓存只省重算」（19.1.1）。

#### 5.2 `:570` 的 sort —— **M-A 的前置条件，必须留着**（逐字）

```python
        frame = frame.sort(["permno", "timestamp"])
```

polars 的组内聚合按帧内行序取值，这一行是 `.first()` 语义的**唯一依据**。

#### 5.3 哨兵派生 `close` 与 `_G`（`:571-594`，保留清单 K3/K5，一个字不改）

```python
        derived = frame.with_columns(
            # The SENTINEL is excluded BEFORE the abs, not after: ...
            pl.when(
                pl.col("dlyprcflg")
                .str.strip_chars()
                .str.to_uppercase()
                .is_in(list(_NO_PRICE_FLAGS))
                .fill_null(False)
                | (pl.col("dlyprc") == 0.0).fill_null(False)
            )
            .then(None)
            .otherwise(pl.col("dlyprc").abs())
            .alias("close"),
            (1.0 + pl.col("dlyret").fill_null(0.0))
            .cum_prod()
            .over("permno")
            .alias("_G"),
        )
```

#### 5.4 选锚（`:596-620`）—— **M-A 就是这里的三行**（逐字）

```python
        # The anchor is the PERMNO's last row carrying a USABLE LEVEL -- a
        # strictly positive close AND the share factor every adjusted volume is
        # scaled by -- not simply its last row, and not merely its last
        # non-null one. "Non-null" was the loophole: CRSP's no-price sentinel is
        # the number 0.0, which passes `is_not_null()` and then makes
        # `adjClose = 0.0 * _G / _G_anchor` exactly 0.0 on every day of that
        # security's history (CR-01). `dlycumfacshr` is in the SAME predicate so
        # that every quantity read off the anchor comes from ONE row that
        # carries all of them; selecting on `close` alone and then reading a
        # NULL `dlycumfacshr` off that row is what made `adjVolume` NaN for a
        # whole delisted history while raw `volume` was fully populated (CR-02).
        anchor = (
            derived.filter(
                pl.col("close").is_not_null()
                & (pl.col("close") > 0.0)
                & pl.col("dlycumfacshr").is_not_null()
            )
            .group_by("permno")
            .agg(
                pl.col("close").last().alias("_close_anchor"),        # :615
                pl.col("_G").last().alias("_G_anchor"),               # :616
                pl.col("dlycumfacshr").last().alias("_cumfacshr_anchor"),  # :617
            )
        )
        derived = derived.join(anchor, on="permno", how="left")
        self._assert_anchor_usable(derived)
```

**M-A = `:615-617` 三个 `.last()` → `.first()`。`:607-612` 的三条谓词一个字不改（K2）。**
`:596-606` 的注释里 `PERMNO's last row` → `first row`（M-C）。

#### 5.5 `_assert_anchor_usable` 的错误消息（`:706`，保留但改一词）

```python
                f"adjustment anchor in [{self.config.start_date}, "
                f"{self.config.end_date}]: {missing[:10]}. The anchor is a "
                f"PERMNO's last row inside that window carrying BOTH a strictly "
```

⇒ `last row` → `first row`（K1）。整个方法（`:665-741`）其余部分保留（D-05）。

#### 5.6 `_finalise` 的 `select(columns)`（`:1456-1466`，逐字）—— **一个字不动**

```python
        columns = ["timestamp", "symbol", *self.TIINGO_VARIABLES, *self.EXTRA_VARIABLES]
        frame = frame.select(
            pl.col("timestamp"),
            pl.col("symbol"),
            *[
                pl.col(name).cast(pl.Float64).alias(name)
                for name in (*self.TIINGO_VARIABLES, *self.EXTRA_VARIABLES)
            ],
        ).select(columns)
        self._derivation_cache = frame
        return frame
```

五个 `adj*` 仍在 `TIINGO_VARIABLES` 里 ⇒ store 保持 **27 个 data_vars**（§19.5 全局前提）。

#### 5.7 要删的三个方法的当前形状（D11/D12/D13/D14）

```python
    @classmethod
    def adjustment_sidecar_path(cls, config: CrspDatasetConfig) -> Path:      # :1471
        """`{zarr_file_path}.crsp_adjustment.json`, a SIBLING of the store."""
        return Path(str(config.zarr_file_path) + cls.ADJUSTMENT_SIDECAR_SUFFIX)

    def _adjustment_record(self) -> dict:                                     # :1475
        reference = CrspReference(self.config.reference_dir)
        return {
            "start_date": str(self.config.start_date),
            "end_date": str(self.config.end_date),
            "product_end": str(reference.product_end),
            "rule": self.ADJUSTMENT_RULE,
        }

    def _assert_anchor_unchanged(self, record: dict) -> None:                 # :1492

    def _write_adjustment_record(self, record: dict) -> None:                 # :1558
        if Path(str(self.config.zarr_file_path)).exists():
            return
        write_json_atomically(
            self.adjustment_sidecar_path(self.config), record, indent=2, sort_keys=True,
        )
```

调用点成对删除：`:1596` / `:1633`（`_raw_axes_in_range`）与 `:1760` / `:1765`（`_raw_data_to_xr`），
外加 `:385` / `:390` 两个类常量、`:1749-1752` 的 docstring 段。

---

### 6. 期望值 churn 的改法（6 条）

**Analog:** `tests/test_crsp_tracer.py:193-225` 的 `at()` 断言块（§1.6 已逐字给出）。
改法是**就地换数字**，形状不动。唯一需要多写一句注释的是第 6 条：

```python
# 当前（tests/test_crsp_dataset.py:461）
assert _at(panel, "adjClose", "2008-09-18", LEHMAN_AXIS) == pytest.approx(0.052)

# 推荐改法（VALIDATION.md 采纳）
# 锚不再是这一行，所以 adjClose 只是 APPROXIMATELY 等于自己的 close（rel 5.4e-06）。
assert _at(panel, "adjClose", "2008-09-18", LEHMAN_AXIS) == pytest.approx(
    0.052, rel=1e-4
)
```

---

## Shared Patterns

### 每个 CRSP 测试文件自带一套 helper，不跨文件 import
**Source:** `tests/test_crsp_dataset.py:66-210`（`_pull`/`_dataset_config`/`_convert`/`_panel`/`_at`）
vs `tests/test_crsp_tracer.py:193-196`（测试体内定义 `at()` 闭包）
**Apply to:** `tests/test_crsp_first_anchor.py`
⇒ 新文件复刻 helper，不从兄弟测试文件 import。

### 所有 quantlab import 在函数体内
**Source:** `tests/test_crsp_dataset.py:18-21`、`tests/test_crsp_tracer.py:23-26`（逐字理由见 §1.1）
**Apply to:** 新测试文件的每一个 helper 与用例
例外：`tests/test_crsp_rebuild_measurements.py:73-80` 是**模块级** import（它不是 TDD-red 文件，
是重建门），新增用例沿用该文件既有风格。

### 合成 vs 逐字实盘数据的 provenance 注释
**Source:** `tests/test_crsp_dataset.py:23-26`
**Apply to:** 新文件每一条 `dsf_row(...)` 构造的行，必须带 `# SYNTHETIC` 说明。

### 数值断言一律显式 `rel=`
**Source:** `tests/test_crsp_tracer.py:204-215`
**Apply to:** I-1（`rel=1e-12`，禁 `==`）、I-3/I-4（`rel=1e-9`）、churn 第 6 条（`rel=1e-4`）。

### 「两张清单答不同问题」
**Source:** `quantlab/dataset/crsp/rebuild.py:49-53` + `tests/test_crsp_rebuild_measurements.py:114-121`
**Apply to:** `CRSP_SIDECAR_SUFFIXES`（保留 `.crsp_adjustment.json`）vs
`EXPECTED_WRITTEN_SIDECARS`（移除它）。两处都已有现成注释，**不必重新论证，引用即可**。

### 残留 gate 计注释、只扫 `quantlab/`、成员必须专属
**Source:** `tests/test_no_identity_residue.py:23-49`
**Apply to:** 本 phase 的锚点机械零残留 gate。

### 真实数据门：fail 不 skip、根由 `--git-common-dir` 推导、module-scoped 只重建一次
**Source:** `tests/test_crsp_rebuild_measurements.py:29-62` / `:179-204` / `:339-345`
**Apply to:** 新增的 `test_the_real_store_reproduces_its_adjusted_columns`。

---

## No Analog Found

无。本 phase 每一个新建/修改文件都在仓库内有直接类比。

> 参考：`quantlab/base/config.py`（不改）、`quantlab/backtest/*`（D-04 零改动）、
> `quantlab/base/backtest.py:1216 _compare_fingerprints`（D-11 不改行为，只在 UAT 抄 warning）
> 均**不进本清单**，因为它们不是本 phase 的改动面。

---

## Metadata

**Analog search scope:** `quantlab/dataset/crsp/`、`quantlab/base/`、`tests/`
**Files scanned:** 9（`quantlab/dataset/crsp/__init__.py`、`quantlab/dataset/crsp/rebuild.py`、
`quantlab/base/data.py`、`tests/test_crsp_dataset.py`、`tests/test_crsp_tracer.py`、
`tests/test_crsp_rebuild_measurements.py`、`tests/test_no_identity_residue.py`、
`tests/test_dataset_update.py`、`tests/conftest.py`）
**Tracked-source check:** 全部 analog 路径均在主仓库工作树内，`git ls-files` 可见；无 gitignored 镜像路径
**Pattern extraction date:** 2026-09-22
