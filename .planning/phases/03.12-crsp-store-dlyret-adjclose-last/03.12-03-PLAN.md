---
phase: "03.12"
plan: 03
type: execute
wave: 2
depends_on: ["03.12-01"]
files_modified:
  - example/wrds_crsp.md
autonomous: true
requirements: [DATA-03]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`example/wrds_crsp.md` 里不再有任何一句把当前行为描述成「锚点在窗口最后一个收盘」「锚点被旁车记录并保护」「改窗口会被旁车文件拒绝」—— 删除机械与文档是**成对**的，留一句描述已删机制的散文就是留一份关于系统如何工作的假陈述"
    - "复权那一节改写后说的是：锚 = 每个 PERMNO 在 store 内**第一个**同时满足「`close` 严格为正」与「`dlycumfacshr` 非空」的行（后复权），锚在只向前 append 时永不移动 ⇒ store 不可变"
    - "**D-05** 的两条拒绝在文档里**原样保留**：没有任何合格行 ⇒ 按 PERMNO 拒绝；`G_anchor` 为 `0.0` 或非有限 ⇒ 按 PERMNO 拒绝。CR-01（哨兵是数字 `0.0`）与 CR-02（只按 close 选锚再回读 NULL `dlycumfacshr`）的解释一个字不改，只把「最后」改成「第一」"
    - "**D-10** 的两条已知限制以具名条目进文档，带实测后果数字（`start_date` 后移 112,359 行 / max rel 0.75；raw tier 向前回填 756/756 行 / max rel 0.76），并写明代码不检查它们（`quantlab/base/chunking.py:307-372` 的 `ChunkLedger.assert_consistent` 不看 `start_date`）"
    - "**D-12** 以一条**重建 checklist** 的形式进文档：重建 CRSP store 之后**必须**一并重算因子库与标签库，否则 `factor_store[i]` 的 digest 会保持不变而底层数据已变 —— 一个看起来没问题的旧因子库"
    - "`:584` 那句「复权锚点在一次转换里只算一次，按整个配置窗口算」**保留** —— RESEARCH §19.1.1 证明这句仍然正确"
    - "GAP-2 一节（`:891-937`）里描述已删闸门当前行为的两句被处理掉，而该 GAP 的**结论**（CLI 在空名册时跳过权益转换、`permnos=()` 不再被读成「全部」）原样保留 —— 那两条交付与锚点无关"
  artifacts:
    - example/wrds_crsp.md
  key_links:
    - "`example/wrds_crsp.md` 的复权节 ↔ `quantlab/dataset/crsp/__init__.py:596-620` 的选锚三行与 `:665-741` 的 `_assert_anchor_usable` —— 文档描述的就是这两处，行为改了文档必须跟着改"
    - "文档的重建 checklist ↔ `quantlab/dataset/crsp/rebuild.py:CrspStoreRebuilder` 与 `quantlab/base/backtest.py:1143-1160` 的 `factor_store[i]` 指纹口径 —— checklist 存在的唯一理由是后者覆盖的是因子库**自己**的变量"
  prohibitions:
    - "不得删除或弱化 D-05 的两条拒绝的说明（CR-01 / CR-02 的整段解释保留，只改锚的方向词）"
    - "不得删除 `:584` 那句「复权锚点在一次转换里只算一次，按整个配置窗口算」"
    - "不得把 D-10 的两条失效场景写成「不会发生」「实践中不成立」或任何弱化措辞 —— 它们是实测会发生的，文档的职责是写下边界"
    - "不得改动 `example/wrds_crsp.md` 以外的任何文件（`quantlab/` 与 `tests/` 属于 plan 01 / 02 / 04）"
    - "禁止用 `sed` 范围删除（`feedback_no_sed_range_delete_planning_docs`）；每处改动用带唯一锚点的精确替换，改完核对 `wc -l`"
    - "不得在文档里承诺任何本 phase 不做的东西：不承诺锚点参数化、不承诺 `\"listing\"` 锚、不承诺锚点钉旁车或反解比对（D-10 裁定 1-A 纯重算）"
---

<objective>
把 `example/wrds_crsp.md` 里关于复权锚点的散文改成它现在真正的样子，并把 D-10 的两条已知限制
与 D-12 的重建 checklist 写进去。

删除机械与文档是成对的：plan 02 删掉了窗口四元组旁车与跨运行闸门，如果这份文档还写着
「锚点被记录，而且被保护」「改窗口会被旁车文件拒绝」，下一个读者会去找一个不存在的机制。
这不是杂物，是**关于系统如何工作的假陈述**。

Purpose: 让唯一一份面向操作者的 CRSP 散文文档与代码同步，并把「历史值不被改写」的边界
写在操作者看得到的地方 —— 因为 D-10 之后，代码不检查它。
Output: 复权节整节重写 + 两条具名已知限制 + 一条重建 checklist + 四处零散措辞修正。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-CONTEXT.md
@.planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-RESEARCH.md
@.planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-PATTERNS.md
@.planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-01-SUMMARY.md
</context>

## 读 CONTEXT.md 与 RESEARCH.md 的必读前提

- **CONTEXT.md 的 D-01 / D-02 / D-03 / D-06 已作废。** 权威是 `## ⚠ 2026-09-22 决策修订`
  一节与 **D-10 / D-11 / D-12**。**D-04 / D-05 / D-07 / D-08 / D-09 仍然有效。**
- **RESEARCH.md 的 §2.1 / §2.2 / §2.3 / §2.4 / §5 / §6.2 / §6.3 / §6.4 / §11 / §14 已失效。**
  权威是 **§19**；本 plan 的文档处置表以 **§19.4.3 的「成对要删/改的文档位置」**为准。
- 本 plan 与 plan 02 **同 wave 并行**，文件不重叠。文档描述的是本 phase 结束后的状态，
  不依赖读取 plan 02 的产物。

## Probe notes（显式记录，不是静默丢弃）

- **Spec-less probe fallback：可见跳过。** 本 phase 无 `*-SPEC.md`，ROADMAP 的 `Requirements`
  是 `TBD`，`phase_req_ids` 为空 ⇒ 无可探测需求文本，本 plan 不含探针派生的 `must_haves` 谓词、
  不含 Edge Coverage / Prohibitions 探针条目。
- **API Coverage 探针 `detected: false`** ⇒ 不产出 `COVERAGE.md`。
- **Assumption-Delta 探针 `detected: false`**；**Schema Push Gate** 不适用（无 ORM schema）。
- **Package Legitimacy Gate 不适用** —— 本 plan 无任何包安装任务（纯文档）。

## Artifacts this phase produces（全 phase 清单）

| 符号 | 位置 | 类型 | 产出 plan |
|---|---|---|---|
| `tests/test_crsp_first_anchor.py` | 仓库根 `tests/` | 新文件 | 01 |
| `test_the_anchor_is_first_usable_row_and_its_adjclose_equals_its_close` | 同上 | 用例（I-1） | 01 |
| `test_switching_the_anchor_is_a_constant_rescale_per_permno` | 同上 | 用例（I-3） | 01 |
| `test_the_day_over_day_ratios_unchanged_by_the_anchor_switch` | 同上 | 用例（I-4） | 01 |
| `test_the_anchor_is_the_FIRST_usable_row` | `tests/test_crsp_dataset.py` | 用例（改名） | 01 |
| `test_a_full_and_incremental_build_agree_bit_for_bit` | `tests/test_crsp_first_anchor.py` | 用例（I-2） | 02 |
| `tests/test_no_anchor_machinery_residue.py` | 仓库根 `tests/` | 新文件 | 02 |
| `PACKAGE_ROOT` / `DELETED_IN_03_12_02` / `_sources` / `_hits` | 同上 | 模块常量 / helper | 02 |
| `test_the_deleted_window_anchor_machinery_leaves_no_residue_in_quantlab` | 同上 | 用例 | 02 |
| `test_the_real_store_reproduces_its_adjusted_columns` | `tests/test_crsp_rebuild_measurements.py` | 用例（I-1 + I-5） | 04 |

**本 plan 不产出任何符号** —— 它产出的是 `example/wrds_crsp.md` 里的两个新小节
（已知限制、重建 checklist）与一节重写。**本 phase 不新增任何生产侧符号。**

<tasks>

<task type="auto">
  <name>Task 1: 复权那一节整节重写 —— 锚从「窗口最后一个收盘」改成「store 内第一个可用行」</name>
  <files>example/wrds_crsp.md</files>
  <read_first>
    - `example/wrds_crsp.md:64-76` —— 退市哨兵那一段，`:70` 那句讲「原来的判据」的措辞
    - `example/wrds_crsp.md:430-480` —— 复权整节全文：`:439` 的小节标题、`:441-446` 的语义总述、`:448-460` 的四条实现讲究（含 D-05 的两条拒绝与 CR-01 / CR-02 的解释）、`:462-470` 的「锚点被记录，而且被保护」段
    - `quantlab/dataset/crsp/__init__.py:596-620`（plan 01 改过的选锚三行与它上方的注释）与 `:665-741`（`_assert_anchor_usable` 的两条拒绝）—— 文档描述的就是这两处，措辞要与代码里的说法对齐
    - `03.12-CONTEXT.md` 的 `## ⚠ 2026-09-22 决策修订` 一节（为什么是 `.first()`、为什么建库时算严格优于读侧派生）
    - `03.12-RESEARCH.md` §19.4.3 的「成对要删/改的文档位置」表（本节涉及 `:70-71` / `:439` / `:441-460` / `:462`）
    - `03.12-RESEARCH.md` §19.2.1 与 §19.2.4（后复权之下 `adjClose` 的量级方向：长期赢家的近期 `adjClose` **高于**名义价）
  </read_first>
  <action>
⚠️ **禁止 `sed` 范围删除**。每处改动用带唯一锚点的精确替换，全部改完后核对 `wc -l`，
把改前/改后行数记进 SUMMARY。

1. `:70` 那句现在说「原来的判据是『最后一个非空收盘』，而 `0.0` 不是空值」。
   把「最后一个非空收盘」改成「第一个非空收盘」（CR-01 的事实**完全不变** ——
   哨兵是数字 `0.0`、它不是空值、于是 `dlyprcflg='DA'` 的哨兵行成了锚、
   `adjClose` 在该证券整段历史上恒为 0.0 —— 只有锚的方向词变了）。
   同段 `:73` 那句「动的只是这条链被锚定在哪个**水平**上（WestRock：从 0.0 变回它最后一个
   真实收盘 51.51）」里的「最后一个真实收盘」也要跟着改成「第一个真实收盘」，
   并核对那个数字在新锚下是否仍然成立；若不确定就把括号里的具体例子换成不带数字的说法，
   **不要**留一个未经核对的数字。

2. `:439` 的小节标题改写。新标题要说清两件事：锚在 store 内**第一个**可用收盘（后复权），
   以及**为什么不再拒绝原地扩展**。旧标题后半句「以及为什么拒绝原地扩展」必须换掉 ——
   那个拒绝不存在了。

3. `:441-446` 的语义总述改写：`adj*` 仍然是**总收益复权**（拆股 + 分红），
   但连乘方向反过来 —— 从该 PERMNO 在 store 内**第一个**可用收盘往**后**连乘，
   所以**最早**那天的 `adjClose` 等于它的原始 `close`，越晚的日子 `adjClose` 相对名义价越高
   （长期赢家尤其明显）。`adjOpen/High/Low` 仍按同一个日因子缩放，`adjVolume` 仍用
   `dlycumfacshr` 派生的股数因子（**D-07**：三种复权因子分开，总收益 / 仅拆股 / 成交量不是同一个数）。
   再加一句说清这次换锚买到了什么：**锚只是每个 PERMNO 首行的函数**，在只向前 append
   （固定 `start_date`、延长 `end_date`）的前提下**永不移动** ⇒ store 在这种形态下不可变
   ⇒ 不再需要任何窗口四元组旁车或跨运行闸门。

4. `:448-460` 的四条实现讲究：
   - 第一条（锚是哪一行）把「最后一行」改成「第一行」，**两个条件写在同一个谓词里**的整段
     理由（五个 `adj*` 全部从同一行读出、只按收盘挑锚再读 NULL `dlycumfacshr` 就是 GAP-B）
     **一个字不改**；
   - 第二条（两条拒绝）**一个字不改** —— 没有合格行、`G_anchor` 为 `0.0` 或非有限值，
     都按 PERMNO 拒绝。补一句方向性说明：`.first()` 之下第二条**更难触发**，
     因为 `G_A` 是首行处的累积值，要零化它得有一个 `dlyret == -1.0` 发生在锚**之前**，
     而锚就是首个可用行；
   - 第三条（空 `dlyret` 贡献因子 1）与第四条（无价格那天 `adjClose` 是 NaN）**一个字不改**。

5. `:462-470` 的「**锚点被记录，而且被保护。**」整段：**删除**。它描述的旁车、
   「先比对、后写入」、「锚点不一致或旁车文件缺失就拒绝」三件事全部不存在了。
   ⚠️ 紧随其后那段讲「两个旁车文件（`crsp_filter_report.json` / `crsp_tickers.json`）
   在 store 已存在时不再重写」的内容**保留** —— 那条 store-exists 守卫还在
   （`quantlab/dataset/crsp/__init__.py` 的 `_write_identity_reports`），
   只要把「和锚点旁车一样」这个比较对象换掉。

6. 在这一节末尾**新增一个具名小节**「已知限制：历史值在什么情况下会被静默改写」，
   写两个条目（**陈述它会发生，给出实测后果，不要写成「不会发生」**）：
   - **`start_date` 后移**：重叠区 112,359 / 112,934 行改变，max rel **0.75**；
   - **raw tier 向前回填**（`start_date` 一个字没改，但 raw 里出现了比原锚更早的行）：
     实测 **756 / 756 行**全部改写，max rel **0.76**。这是 `.first()` 锚引入的**新**风险方向 ——
     `.last()` 锚怕 `end_date` 变，`.first()` 锚怕 `start_date` 变。
   并写明：**代码不检查其中任何一条**。`ChunkLedger.assert_consistent`
   （`quantlab/base/chunking.py:307-372`）检查符号轴指纹、store 与 ledger 的空/非空、
   store 末尾是否等于最后一个窗口的 end —— 它不看 `start_date`。
   最后写明这是一次**明示的取舍**（D-10）：逐位相等是「实测是 0」而不是「结构上不可能不是 0」，
   它依赖「start 不动 + raw 不向前长 + vintage 不改写锚行」三个外部事实；
   被支持的 append 形态只有一种（固定 `start_date`、延长 `end_date`），
   它由 `tests/test_crsp_first_anchor.py::test_a_full_and_incremental_build_agree_bit_for_bit` 钉住。
   ⚠️ **不要**在这里承诺锚点钉旁车或反解比对 —— D-10 明确否决了它们。
  </action>
  <!-- 探针会把下面 verify 里的 `$(git rev-parse --show-toplevel)` 标成
       verify_command_path_resolvability=unresolvable。这是**预期的**，不是疏漏：项目规则
       `project_gsd_worktree_verify_path` 明令 verify 块禁止写死主仓库绝对路径 —— 隔离执行时
       绝对路径会指向未改动的主树，测出假绿。动态解析仓库根是**正确**写法；探针只是无法静态
       求值它。命令保持原样，不要替换成字面路径。 -->
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && A=$(grep -c '锚点在窗口最后一个收盘' example/wrds_crsp.md || true); B=$(grep -c '锚点被记录，而且被保护' example/wrds_crsp.md || true); C=$(grep -c '已知限制' example/wrds_crsp.md || true); D=$(grep -c '0\.76' example/wrds_crsp.md || true); E=$(grep -c 'assert_consistent' example/wrds_crsp.md || true); echo "A=$A B=$B C=$C D=$D E=$E"; test "$A" = 0 && test "$B" = 0 && test "$C" != 0 && test "$D" != 0 && test "$E" != 0 && echo ADJUSTMENT_SECTION_OK</automated>
    <fails_when>non-zero exit, or the line `ADJUSTMENT_SECTION_OK` is absent from stdout, or the echoed counter line shows `A=` / `B=` as anything other than 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '锚点在窗口最后一个收盘' example/wrds_crsp.md` 返回 `0`
    - `grep -c '锚点被记录，而且被保护' example/wrds_crsp.md` 返回 `0`
    - `grep -c '第一个非空收盘' example/wrds_crsp.md` ≥ `1`
    - `grep -c '已知限制' example/wrds_crsp.md` ≥ `1`
    - `grep -c '0\.75' example/wrds_crsp.md` ≥ `1` 且 `grep -c '0\.76' example/wrds_crsp.md` ≥ `1`（两条限制带实测后果）
    - `grep -c '756' example/wrds_crsp.md` ≥ `1`
    - `grep -c 'assert_consistent' example/wrds_crsp.md` ≥ `1`（点名了那个不检查 `start_date` 的函数）
    - `grep -c 'test_a_full_and_incremental_build_agree_bit_for_bit' example/wrds_crsp.md` ≥ `1`（被支持的 append 形态有一条可执行的钉子）
    - `grep -c '不会发生\|实践中不成立\|几乎不可能' example/wrds_crsp.md` 返回 `0`（限制不得被弱化）
    - `grep -c 'GAP-B' example/wrds_crsp.md` ≥ `1` 且 `grep -c 'G_anchor' example/wrds_crsp.md` ≥ `1`（D-05 的两条拒绝与 CR-02 解释原样保留）
    - **两段式范围门 (a)**（`example/` 里没碰别的文档）：`PHASE_BASE="$(git log --diff-filter=A --format=%H -n1 -- .planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-01-PLAN.md)^"; git diff --name-only "$PHASE_BASE" -- example/` 只输出 `example/wrds_crsp.md` 一行
    - **两段式范围门 (b)**（`example/` 之外也没碰东西：`scripts/`、`config/`、仓库根文件全在这条的扫描面里）：`PHASE_BASE="$(git log --diff-filter=A --format=%H -n1 -- .planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-01-PLAN.md)^"; git diff --name-only "$PHASE_BASE" -- . ':(exclude).planning/' ':(exclude)quantlab/' ':(exclude)tests/'` 只输出 `example/wrds_crsp.md` 一行
    - ⚠️ **上面两条的 pathspec 都不能写成裸 `-- .`。** 两类噪声会让裸 `-- .` 在任何执行点上都不可能只输出一行：(1) 基线 `$PHASE_BASE` 是 `docs(03.12): create phase plan` 的**父**提交，而那个提交本身新增了 `.planning/ROADMAP.md` 与四个 `03.12-*-PLAN.md`；(2) 本 plan 是 wave 2、`depends_on: ["03.12-01"]`，执行时树里已经带着 plan 01 改过的 `quantlab/dataset/crsp/__init__.py` 与四个 `tests/` 文件，外加 `03.12-01-SUMMARY.md`。所以 `.planning/` / `quantlab/` / `tests/` 三个前缀被显式排除 —— 它们**不归本 plan 管**（prohibitions 已禁止本 plan 改 `quantlab/` 与 `tests/`，那条由 plan 02 的零残留 gate 与 plan 04 的全量回归兜底）。实测（2026-09-22，本 plan 执行前的 HEAD）：(a) 与 (b) 均输出 0 行，而裸 `-- .` 输出 5 行 `.planning/` 路径
    - SUMMARY 记录了 `example/wrds_crsp.md` 改前/改后的 `wc -l` 两个数字
  </acceptance_criteria>
  <done>复权节描述的是 `.first()` 锚；D-05 的两条拒绝与 CR-01 / CR-02 的解释原样保留；两条已知限制带实测数字并点名了不检查它们的那个函数。</done>
  <reversibility rating="reversible">纯文档改写。</reversibility>
</task>

<task type="auto">
  <name>Task 2: 四处零散措辞 + 一条重建 checklist（D-12：因子库 / 标签库一并重算）</name>
  <files>example/wrds_crsp.md</files>
  <read_first>
    - `example/wrds_crsp.md:555-590` —— 「它是怎么工作的」流程图与其下的四个要点：`:565` 的
      「全局复权（每 PERMNO 一个锚点，缓存在实例上）」、`:569` 的旁车列表、
      `:584` 的「复权锚点在一次转换里只算一次」（**保留**）
    - `example/wrds_crsp.md:760-770` —— 例 2 结尾那段讲「不需要连 WRDS 重新转换」的话，`:766` 是要改的那句
    - `example/wrds_crsp.md:891-937` —— GAP-2 整节。要处理的是 `:902` 与 `:932` 两句；
      该 GAP 的两条**交付结论**（CLI 空名册时跳过权益转换；`permnos=()` 不再被读成「全部」）与
      `:936-937` 的「旧 store 不需要因为这个改动重建」**保留**
    - `quantlab/dataset/crsp/rebuild.py:25-60` —— `CRSP_SIDECAR_SUFFIXES` 与它的清理规则（重建 checklist 的第一步）
    - `quantlab/base/backtest.py:1143-1160` —— `factor_store[i]` / `train_factor_store[i]` / `train_label_store[i]` 三个指纹键覆盖的是**因子库自己的变量**
    - `03.12-CONTEXT.md` 的 **D-12** 与 **D-11** 两节
    - `03.12-RESEARCH.md` §19.3.1（四个记录点逐条翻转的表，特别是第四行的「digest 保持不变而底层已变」）、§19.6.1 / §19.6.2（`BaseStoreRebuilder` 的四步与本机实测产物）
  </read_first>
  <action>
⚠️ **禁止 `sed` 范围删除**。逐处精确替换，改完核对 `wc -l`。

1. `:565` 流程图里的「全局复权（每 PERMNO 一个锚点，缓存在实例上）」：
   改成说明锚是**每 PERMNO 在 store 内的首个可用行**。括号里的「缓存在实例上」要改口径 ——
   一个 store 一个锚的原因是**全窗口取数 + 派生后切片**，缓存省的是重算次数，不是正确性
   （RESEARCH §19.1.1）。
2. `:569` 旁车列表里去掉 `.crsp_adjustment.json`，只留 `.crsp_filter_report.json` /
   `.crsp_tickers.json` / `.chunks.json`。图里的对齐（竖线位置）要跟着调，**不要**留下
   一条错位的 ASCII 框线。
3. `:584` 那句「**复权锚点在一次转换里只算一次**，按整个配置窗口算，然后切片给每个分块窗口用。
   按 `year` 和按 `month` 转出来的 store 是 `assert_identical` 相等的」**保留** ——
   RESEARCH §19.1.1 证明它仍然正确。在它后面补一句：跨**运行**的 append
   （固定 `start_date`、延长 `end_date`）同样得到逐位相同的 store，由
   `tests/test_crsp_first_anchor.py::test_a_full_and_incremental_build_agree_bit_for_bit` 钉住。
4. `:766` 那句「注意复权锚点由 `start_date`/`end_date` 决定，改窗口会被旁车文件拒绝」：
   **改写**。新说法：锚只由 `start_date` 一侧决定（它是每个 PERMNO 在窗口内的首个可用行），
   所以延长 `end_date` 重新转换是安全的、逐位相等的；而**改动 `start_date` 等于重建**，
   不是 append —— 并指向 Task 1 新增的「已知限制」小节。
5. GAP-2 一节：
   - `:902` 那句现在把「闸门抛 `ValueError`」描述成**当前**行为。改写成历史陈述：
     在 03.12 之前，撞上一个用别的窗口建好的同名 store 会被跨运行闸门拒绝；那道闸门在 03.12
     随锚点换向一起删除了，**但这个 GAP 的两条交付与它无关**（CLI 在空名册时跳过权益转换、
     `permnos=()` 不再被读成「全部」），两条仍然有效。同段 `:904` 那句「锚点守卫的行为是
     **正确**的；错的是 CLI」也要相应改成过去式，别让读者以为那道守卫还在。
   - `:932` 那句描述配套测试「先把那个旧 store 和一份窗口不一致的旁车种下去」：
     改成只描述种 store（plan 02 把种旁车那段从 `tests/test_ingest_wrds_crsp.py` 的
     `_plant_stale_equity_store()` 里去掉了），结论「用字节证明没写权益 store」保留。
6. **新增重建 checklist**（D-12）。位置放在「常见坑」之前、或紧跟第 4 步改写的那段之后，
   作为一个具名小节「重建 CRSP store 的完整步骤」。至少四步，第 3 步是 D-12 的本体：
   1. **先清旁车再清 store**：`CRSP_SIDECAR_SUFFIXES` 里的五个后缀全部删掉，否则
      `_write_identity_reports` 的 store-exists 守卫会让新 store 旁边留着描述上一个面板的审计文件；
      `CrspStoreRebuilder`（`quantlab/dataset/crsp/rebuild.py`）已经把这四步（assert inputs →
      backup → clear → convert）编排好了，**不要自己写 backup/clear/convert**。
   2. **备份代数只增不覆盖**：每次重建取一个自己的名字，既有的每一代都是只读的 ——
      每一代都是当前代码已经无法再产出的那个面板的唯一记录。
   3. **必须一并重算因子库与标签库**（**D-12，不是可选项**）。理由写清楚：
      `quantlab/base/backtest.py` 的 `factor_store[i]` / `train_factor_store[i]` /
      `train_label_store[i]` 三个指纹键覆盖的是**因子库自己的变量**，所以只重建 dataset store
      而不重算因子库，那个 digest 会**保持不变而底层数据已变** —— 一个看起来没问题的旧因子库，
      而这个键本来就是为抓这种情形设的，却偏偏抓不到。
   4. **既有 backtest run 会失效，而且只会得到一条 warning**（**D-11**）。
      `_compare_fingerprints`（`quantlab/base/backtest.py:1216`）**故意**只发 warning 而不 raise：
      数据集本来就会被追加、复权本来就会回溯重算，变了的数据仍然可以回测 —— 那是一条长期真理，
      不是为这次重建找的借口。所以重建之后**重放一个既有 run 会跑完并打出指纹 warning**，
      操作者应当把 warning 的条数与键名记下来，而不是把它当噪声划掉。
   ⚠️ 这一节**不得**承诺自动化的因子库重算 —— 本 phase 不实现它，checklist 是给操作者的步骤。
  </action>
  <!-- 与 Task 1 同：下面 verify 里的 `$(git rev-parse --show-toplevel)` 被探针标成
       unresolvable 是预期的。`project_gsd_worktree_verify_path` 禁止在 verify 里写死主仓库
       绝对路径（隔离执行会测到未改动代码而假绿），动态解析仓库根才是正确写法。命令不改。 -->
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && A=$(grep -c '改窗口会被旁车文件拒绝' example/wrds_crsp.md || true); B=$(grep -c 'crsp_adjustment' example/wrds_crsp.md || true); C=$(grep -c '复权锚点在一次转换里只算一次' example/wrds_crsp.md || true); D=$(grep -c '重建 CRSP store 的完整步骤' example/wrds_crsp.md || true); E=$(grep -c 'factor_store' example/wrds_crsp.md || true); F=$(grep -c '_compare_fingerprints' example/wrds_crsp.md || true); echo "A=$A B=$B C=$C D=$D E=$E F=$F"; test "$A" = 0 && test "$B" = 0 && test "$C" != 0 && test "$D" != 0 && test "$E" != 0 && test "$F" != 0 && echo REBUILD_CHECKLIST_OK</automated>
    <fails_when>non-zero exit, or the line `REBUILD_CHECKLIST_OK` is absent from stdout, or the echoed counter line shows `A=` or `B=` as anything other than 0, or shows `C=0` (the still-true sentence was deleted)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '改窗口会被旁车文件拒绝' example/wrds_crsp.md` 返回 `0`
    - `grep -c 'crsp_adjustment' example/wrds_crsp.md` 返回 `0`
    - `grep -c '复权锚点在一次转换里只算一次' example/wrds_crsp.md` 返回 `1`（**仍然为真的那句被保留**）
    - `grep -c '重建 CRSP store 的完整步骤' example/wrds_crsp.md` 返回 `1`
    - `grep -c 'factor_store' example/wrds_crsp.md` ≥ `1` 且 `grep -c '标签库' example/wrds_crsp.md` ≥ `1`（D-12 进文档）
    - `grep -c '_compare_fingerprints' example/wrds_crsp.md` ≥ `1` 且 `grep -c 'warning' example/wrds_crsp.md` ≥ `1`（D-11 进文档）
    - `grep -c 'CrspStoreRebuilder' example/wrds_crsp.md` ≥ `1`（checklist 指向现成编排器而不是教人手写 backup/clear）
    - `grep -c 'permnos=()' example/wrds_crsp.md` ≥ `1`（GAP-2 的两条交付结论未被误删）
    - `uv run python -c "s=open('example/wrds_crsp.md').read(); assert s.count('│') % 1 == 0; import re; box=[l for l in s.splitlines() if '│' in l]; assert len(box) > 5, len(box)"` 退出码 `0`（流程图框线仍成组存在）
    - **两段式范围门 (a)**（与 Task 1 同形）：`PHASE_BASE="$(git log --diff-filter=A --format=%H -n1 -- .planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-01-PLAN.md)^"; git diff --name-only "$PHASE_BASE" -- example/` 只输出 `example/wrds_crsp.md` 一行
    - **两段式范围门 (b)**：`PHASE_BASE="$(git log --diff-filter=A --format=%H -n1 -- .planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-01-PLAN.md)^"; git diff --name-only "$PHASE_BASE" -- . ':(exclude).planning/' ':(exclude)quantlab/' ':(exclude)tests/'` 只输出 `example/wrds_crsp.md` 一行。**不得写成裸 `-- .`** —— 理由与实测见 Task 1 同条判据下的 ⚠️ 说明（`.planning/` 是基线自带的噪声，`quantlab/` 与 `tests/` 是 plan 01 在本 wave 之前留下的、不归本 plan 管的改动）
    - SUMMARY 记录了本 plan 两个 task 合计的 `wc -l` 改前/改后数字
  </acceptance_criteria>
  <done>四处零散措辞与代码同步；`:584` 仍然为真的那句被保留；一条四步重建 checklist 存在，其中 D-12（因子库 / 标签库一并重算）与 D-11（指纹只发 warning）各有自己的段落与理由。</done>
  <reversibility rating="reversible">纯文档改写。</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 文档 → 操作者的心智模型 | 这是本 plan 唯一的边界。`example/wrds_crsp.md` 是唯一一份面向操作者的 CRSP 散文文档；操作者按它决定「这次改动要不要重建」「append 安不安全」 |

**不存在的边界，如实记录**：本 plan 只改一个 Markdown 文件。无代码改动、无网络调用、
无凭证、无输入解析、无依赖变化。

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.12-10 | Repudiation | `example/wrds_crsp.md` 里描述已删机制的句子 | medium | mitigate | 留一句「锚点被记录，而且被保护」，操作者会以为有一道闸门在拦不一致的窗口 —— 而它已经不存在了。这不是过时的注释，是一份**会让人放松警惕**的假陈述。缓解：Task 1 / Task 2 的 verify 用具名句子片段做计数归零门，并要求 `A=` / `B=` 计数进 stdout 以便肉眼复核 |
| T-03.12-11 | Information Disclosure | 文档里被弱化的已知限制 | high | mitigate | D-10 把「历史值不被改写」交给了三个代码不检查的外部事实，其中「raw tier 向前回填」实测能静默改写 756/756 行。若文档把它写成「不会发生」，一次合法的历史回填就会在无人察觉的情况下改写全部历史价，而所有测试仍然全绿。缓解：prohibitions 明令禁止弱化措辞；acceptance 用 `grep -c '不会发生\|实践中不成立\|几乎不可能'` 归零门 + 要求实测数字（`0.75` / `0.76` / `756`）出现在文中 |
| T-03.12-12 | Tampering | 因子库 digest 与底层 store 脱节（D-12 未进文档时） | high | mitigate | `factor_store[i]` 指纹覆盖的是因子库自己的变量，只重建 dataset store 会让 digest 保持不变而底层已变 —— 这个键本来就是为抓这种情形设的却抓不到。缓解：重建 checklist 第 3 步以「不是可选项」的措辞写进文档，并给出理由；acceptance 用 `grep -c 'factor_store'` 与 `grep -c '标签库'` 钉住 |
| T-03.12-13 | Denial of Service | `sed` 范围删除把文档截断 | medium | mitigate | `feedback_no_sed_range_delete_planning_docs`：范围删除漏匹配会一路删到 EOF。本 plan 的删除量集中在 `:439-470` 与 GAP-2 两片。缓解：prohibitions 禁止；每 task 后核对 `wc -l`，两个数字进 SUMMARY；ASCII 框线的成组存在有一条独立断言 |
| T-03.12-SC | Tampering | npm/pip/cargo installs | low | accept | **本 plan 无任何包安装任务**（纯文档）。RESEARCH `## Package Legitimacy Audit` 逐字：本 phase 不安装任何外部包 |
</threat_model>

<verification>
1. 两段式范围门，两条都只输出 `example/wrds_crsp.md` 一行（**不得用裸 `-- .`**：基线自带五个 `.planning/` 路径，且本 plan 在 wave 2 执行时树里已有 plan 01 的 `quantlab/` + `tests/` 改动，裸 `-- .` 的「只输出一行」永远不可能为真）：
   (a) `PHASE_BASE="$(git log --diff-filter=A --format=%H -n1 -- .planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-01-PLAN.md)^"; git diff --name-only "$PHASE_BASE" -- example/`
   (b) `PHASE_BASE="$(git log --diff-filter=A --format=%H -n1 -- .planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-01-PLAN.md)^"; git diff --name-only "$PHASE_BASE" -- . ':(exclude).planning/' ':(exclude)quantlab/' ':(exclude)tests/'`
2. Task 1 的计数门输出 `ADJUSTMENT_SECTION_OK`
3. Task 2 的计数门输出 `REBUILD_CHECKLIST_OK`
4. `grep -c '不会发生\|实践中不成立\|几乎不可能' example/wrds_crsp.md` 返回 `0`
5. 人工复核（记进 SUMMARY）：`:555-590` 的 ASCII 流程图框线没有错位

⚠️ 本 plan 与 plan 02 **同 wave 并行**。两者文件不重叠（本 plan 只碰 `example/wrds_crsp.md`），
合并后不需要额外的冲突处理；但合并后的全量回归由 plan 04 的 `<verification>` 承担。
</verification>

<success_criteria>
- [ ] 复权节描述的是 `.first()` 锚，标题不再承诺「拒绝原地扩展」
- [ ] D-05 的两条拒绝与 CR-01 / CR-02 的解释原样保留（只改方向词）
- [ ] `:584` 那句「复权锚点在一次转换里只算一次」被保留
- [ ] 「已知限制」小节存在，带 `0.75` / `0.76` / `756` 三个实测数字，点名 `assert_consistent` 不看 `start_date`，且无任何弱化措辞
- [ ] 「重建 CRSP store 的完整步骤」小节存在，第 3 步是 D-12（因子库 / 标签库一并重算，不是可选项），第 4 步是 D-11（指纹只发 warning 及其理由）
- [ ] `crsp_adjustment` 在全文出现 0 次
- [ ] GAP-2 的两条交付结论未被误删
</success_criteria>

<output>
Create `.planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-03-SUMMARY.md` when done.
SUMMARY 必须包含：`example/wrds_crsp.md` 改前/改后 `wc -l`、两个计数门的完整 stdout
（含 `A=… B=…` 那一行）、以及改写后复权节标题与「已知限制」小节标题的逐字文本。
</output>
