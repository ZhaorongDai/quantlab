---
phase: "03.12"
plan: 02
type: execute
wave: 2
depends_on: ["03.12-01"]
files_modified:
  - quantlab/dataset/crsp/__init__.py
  - quantlab/dataset/crsp/rebuild.py
  - scripts/ingest_wrds_crsp.py
  - tests/test_crsp_first_anchor.py
  - tests/test_crsp_dataset.py
  - tests/test_ingest_wrds_crsp.py
  - tests/test_crsp_ticker_sidecar.py
  - tests/test_crsp_rebuild_measurements.py
  - tests/test_no_anchor_machinery_residue.py
autonomous: true
requirements: [DATA-03]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "**I-2**（**D-10** 点名的验收锚点）：先全量建 `[A_start, B_end]`，与先建 `[A_start, A_end]` 再 `update()` 到 `B_end`，两个 store 在 `xr.testing.assert_identical` 下相同 —— 逐位 + 变量名 + 坐标。派生层实测重叠 66,356 行 `max rel 0.000e+00`"
    - "「延长 `end_date` 再 append」从**被拒绝**变成**合法且逐位相等**：`_assert_anchor_unchanged` 及其旁车不再存在，取代它的是 I-2 用例"
    - "**D-10** 的两个失效场景以**具名已知限制**的形式活在 `_derivation()` 的注释里，而不是被写成「不会发生」：(1) `start_date` 后移 → 112,359 行改变 max rel 0.75；(2) **raw tier 向前回填**（`start_date` 一个字没改）→ 756/756 行静默改写 max rel 0.76。两者代码都不检查（`quantlab/base/chunking.py:307-372` 的 `ChunkLedger.assert_consistent` 不看 `start_date`）"
    - "`quantlab/**/*.py` 里对这七个名字的出现次数为 0，**注释与 docstring 计入命中**：`ADJUSTMENT_SIDECAR_SUFFIX` / `ADJUSTMENT_RULE` / `adjustment_sidecar_path` / `_adjustment_record` / `_assert_anchor_unchanged` / `_write_adjustment_record` / `total_return_backward_from_last_close`"
    - "`.crsp_adjustment.json` 这个**字面量**仍然活在 `quantlab/dataset/crsp/rebuild.py:CRSP_SIDECAR_SUFFIXES`（**清理**清单）里，且它**不在**零残留清单上 —— R-05 判例：从清理清单删掉 suffix，会让一个描述已删机制的文件留在它从未描述过的 store 旁边"
    - "`tests/test_crsp_rebuild_measurements.py` 的 `EXPECTED_WRITTEN_SIDECARS`（**必写**清单）里不再有 `.crsp_adjustment.json`，它加入 `.crsp_symbology_report.json` 的行列，成为「清理清单里有、必写清单里没有」的第二个例子"
    - "**D-04** 兑现：`quantlab/base/`、`quantlab/backtest/`、`quantlab/factor/`、`quantlab/label/` 零改动；**D-11** 兑现：`quantlab/base/backtest.py:1216 _compare_fingerprints` 一行不改，仍然只发 warning"
    - "store 仍是 27 个 `data_vars`；`quantlab/base/config.py` 一个字节不改"
  artifacts:
    - tests/test_no_anchor_machinery_residue.py
    - quantlab/dataset/crsp/__init__.py
    - quantlab/dataset/crsp/rebuild.py
  key_links:
    - "零残留 gate 的三条设计约束 → `tests/test_no_identity_residue.py:23-49` 的逐字范本：注释与 docstring **计入**命中（不 `grep -v '^#'`、不做丢弃 docstring 的 AST 扫描）、**只扫 `quantlab/`**、成员必须**专属于**被删机械"
    - "`_derivation()`（`quantlab/dataset/crsp/__init__.py:513`）→ `_scan_raw()`（`:536`）→ `quantlab/dataset/stock.py:433/436` 的 config 两端 —— 派生**一次**、跨整个 `[start_date, end_date]`，`_raw_data_to_xr_window`（`:1703-1741`）**在派生之后**才切日期。这是「一个 store 一个锚」的真正原因；`_derivation_cache` 省的是 N 次重算，不是正确性"
    - "`quantlab/dataset/crsp/rebuild.py:CRSP_SIDECAR_SUFFIXES`（清理）↔ `tests/test_crsp_rebuild_measurements.py:EXPECTED_WRITTEN_SIDECARS`（必写）—— 两张清单答不同问题，`rebuild.py:49-53` 与 `test_crsp_rebuild_measurements.py:114-121` 两段注释已经把判例写好了，引用即可"
  prohibitions:
    - "不得从 `quantlab/dataset/crsp/rebuild.py:CRSP_SIDECAR_SUFFIXES` 元组里删除 `.crsp_adjustment.json`（R-05；元组本身一行不动，只改它上方的说明注释）"
    - "不得把 `.crsp_adjustment.json` 这个字面量放进零残留清单 —— 那正是「成员规则」要挡的误报"
    - "不得删除 `tests/test_crsp_ticker_sidecar.py:346-359` 的 `test_a_corrupt_sidecar_names_the_exception_type_and_the_way_out` —— 它的**主体是 ticker 旁车**，只有 docstring 里引用已删机制那句要改（RESEARCH §19.4.3 已把 §6.2 的 B 档误判纠正）"
    - "不得删除 `tests/test_ingest_wrds_crsp.py:383-430` 的 `_plant_stale_equity_store()` —— 它是 GAP-D 用例的 fixture，真正的断言是「被种下的文件原封不动」；只去掉种锚点旁车那段并改 docstring"
    - "不得改动 `quantlab/base/backtest.py`（D-11：`_compare_fingerprints` 保持 warning，本 phase 不动它）"
    - "不得新增任何锚点钉旁车、不得加 append 前的反解比对 —— **D-10 裁定用 1-A 纯重算**，RESEARCH §19.1.6 推荐的 1-C 被明确否决"
    - "不得改动 `quantlab/base/chunking.py`（把 `start_date` 加进 `ChunkLedger.assert_consistent` 属于另起一个 phase）"
    - "不得改动 `quantlab/dataset/crsp/__init__.py:571-594` 哨兵派生、`:607-612` 三条谓词、`:665-741` `_assert_anchor_usable`、`:1456-1466` `select(columns)`"
    - "不得为旧 store 写任何兼容读路径（`project_no_backward_compat`）"
    - "禁止用 `sed` 范围删除做本 plan 的任何一处删除（`feedback_no_sed_range_delete_planning_docs`：范围删除漏匹配会一路删到 EOF）。改用带 assert 的精确替换，并在每次删除后核对文件行数"
---

<!-- planner-discipline-allow: ADJUSTMENT_SIDECAR_SUFFIX, ADJUSTMENT_RULE, adjustment_sidecar_path, _adjustment_record, _assert_anchor_unchanged, _write_adjustment_record, total_return_backward_from_last_close, ANCHOR GATE, the two WRITES -->
<!-- 理由：这些字面量是本 plan 要删除的符号名，acceptance 对它们做的是 `quantlab/**/*.py` 内的
     计数归零检查。零残留 gate 的扫描范围逐字限定为 `quantlab/`（`.planning/`、`example/`、
     `tests/` 故意出范围，见 `tests/test_no_identity_residue.py:36-41`），所以它们出现在
     PLAN.md 的 action 里不会进入被检查的文件，不存在自我失效。 -->

<objective>
删掉整套「锚点会移动」的窗口机械，并用一条跨运行的 append 用例（I-2）取代它原本守着的那件事。

锚改成 `.first()` 之后，那套机械的存在理由消失了：旧闸门比对的是**转换窗口四元组**
（`start_date` / `end_date` / `product_end` / `rule`），所以它对**每一次合法 append** 都说不；
`.first()` 锚在固定 `start_date`、延长 `end_date` 时永不移动，append 实测逐位相等。

但删掉闸门也删掉了唯一的检测点。**D-10 裁定用 1-A（纯重算）**，因此本 plan 的交付不是新机制，
而是 (a) 一条钉住正常 append 的 I-2 回归测试、(b) 把两个失效场景写成**具名的已知限制**。

Purpose: 让 store 在只向前 append 时真正不可变，并让「历史值不被改写」这件事的边界被写下来
而不是被默认。
Output: 13 条删除 + 一条 I-2 用例 + 一个仓库级零残留 gate + 一批成对的测试删除/改写。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-CONTEXT.md
@.planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-RESEARCH.md
@.planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-PATTERNS.md
@.planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-VALIDATION.md
@.planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-01-SUMMARY.md
</context>

## 读 CONTEXT.md 与 RESEARCH.md 的必读前提

- **CONTEXT.md 的 D-01 / D-02 / D-03 / D-06 已作废。** 权威是 `## ⚠ 2026-09-22 决策修订`
  一节与 **D-10 / D-11 / D-12**。**D-04 / D-05 / D-07 / D-08 / D-09 仍然有效。**
- **RESEARCH.md 的 §2.1 / §2.2 / §2.3 / §2.4 / §5 / §6.2 / §6.3 / §6.4 / §11 / §14 结论已失效。**
  权威是 **§19**，本 plan 的三张清单以 **§19.5** 为准（删除 13 / 保留 8 / 改动 4），
  **不是** §1 的原始清单。
- **§19.1.6 推荐的 1-C（锚点钉旁车 + 累乘重启）被 D-10 明确否决**，不要实现它。

## Probe notes（显式记录，不是静默丢弃）

- **Spec-less probe fallback：可见跳过。** 本 phase 无 `*-SPEC.md`，ROADMAP 的 `Requirements`
  字段是 `TBD`，`phase_req_ids` 为空 ⇒ 无可探测的需求文本，因此本 plan 不含探针派生的
  `must_haves` 谓词、不含 Edge Coverage / Prohibitions 探针条目。验收契约是 I-1..I-5。
- **API Coverage 探针 `detected: false`** ⇒ 不产出 `COVERAGE.md`。
- **Assumption-Delta 探针 `detected: false`**；**Schema Push Gate** 不适用（无 ORM schema）。
- **Package Legitimacy Gate 不适用** —— 本 plan 无任何包安装任务。

## Artifacts this phase produces（全 phase 清单）

| 符号 | 位置 | 类型 | 产出 plan |
|---|---|---|---|
| `tests/test_crsp_first_anchor.py` | 仓库根 `tests/` | 新文件 | 01 |
| `test_the_anchor_is_first_usable_row_and_its_adjclose_equals_its_close` | 同上 | 用例（I-1） | 01 |
| `test_switching_the_anchor_is_a_constant_rescale_per_permno` | 同上 | 用例（I-3） | 01 |
| `test_the_day_over_day_ratios_unchanged_by_the_anchor_switch` | 同上 | 用例（I-4） | 01 |
| `test_the_anchor_is_the_FIRST_usable_row` | `tests/test_crsp_dataset.py` | 用例（改名） | 01 |
| **`test_a_full_and_incremental_build_agree_bit_for_bit`** | `tests/test_crsp_first_anchor.py` | 用例（**I-2**） | **02** |
| **`tests/test_no_anchor_machinery_residue.py`** | 仓库根 `tests/` | **新文件** | **02** |
| **`PACKAGE_ROOT` / `DELETED_IN_03_12_02` / `_sources` / `_hits`** | 同上 | 模块常量 / 文件私有 helper | **02** |
| **`test_the_deleted_window_anchor_machinery_leaves_no_residue_in_quantlab`** | 同上 | 用例 | **02** |
| `test_the_real_store_reproduces_its_adjusted_columns` | `tests/test_crsp_rebuild_measurements.py` | 用例（I-1 + I-5 真实数据门） | 04 |

**本 phase 不新增任何生产侧符号。** 本 plan 的生产侧净变化是**删除 6 个方法/常量 + 4 段
docstring + 2 对调用点 + 1 行脚本输出**，加上文字改写。

**本 plan 删除的生产侧符号**（逐字进零残留清单）：
`ADJUSTMENT_SIDECAR_SUFFIX`、`ADJUSTMENT_RULE`、`adjustment_sidecar_path`、`_adjustment_record`、
`_assert_anchor_unchanged`、`_write_adjustment_record`、`total_return_backward_from_last_close`。

<tasks>

<task type="auto">
  <name>Task 1: 删除整套窗口锚点机械（6 个方法/常量 + 4 段 docstring + 2 对调用点 + 1 行脚本输出），并把 D-10 的两条已知限制写进 `_derivation()`</name>
  <files>quantlab/dataset/crsp/__init__.py, quantlab/dataset/crsp/rebuild.py, scripts/ingest_wrds_crsp.py</files>
  <read_first>
    - `quantlab/dataset/crsp/__init__.py:1-58` —— 模块 docstring。`:10-17` 的 GLOBAL 段（D1，**改写不删**）、`:19-31` 的 anchor-MOVES 整段（D2，**删**）、`:44-58` 的 arithmetic 段（plan 01 已把 LAST 改成 FIRST，本 task 确认不重复改）
    - `quantlab/dataset/crsp/__init__.py:378-392` —— 两个类常量与各自的 `#:` 注释块（D4 / D5）
    - `quantlab/dataset/crsp/__init__.py:492-540` —— config setter 里 `_derivation_cache` 的清空（`:494-497`，K8 保留）与 `_derivation()` 开头：`:514-529` docstring（`:519-528` 是 D7 要删的段）、`:530-532` 缓存早返回（K8 保留）、`:534` 的 gate 调用（D6）
    - `quantlab/dataset/crsp/__init__.py:1466-1572` —— `:1468` 的分节注释、`adjustment_sidecar_path`（D11）、`_adjustment_record`（D12）、`_assert_anchor_unchanged`（D13）、`_write_adjustment_record`（D14）四个方法全文
    - `quantlab/dataset/crsp/__init__.py:1572-1636` —— `_raw_axes_in_range`：`:1587-1592` docstring 里「the two WRITES」那段、`:1596` 的 `record = ...`、`:1627-1631` 的「Written LAST」注释、`:1633` 的写调用（D15）
    - `quantlab/dataset/crsp/__init__.py:1742-1767` —— `_raw_data_to_xr`：`:1749-1752` 的「omission 被 LATCHED」段（D17）、`:1760` 与 `:1765` 的一对（D16）
    - `quantlab/dataset/crsp/rebuild.py:25-60` —— `CRSP_SIDECAR_SUFFIXES` 及其上方注释，特别是 `:49-53` 的 `.crsp_symbology_report.json` 段（**M-D 的句式模板**，逐字照它改写）
    - `scripts/ingest_wrds_crsp.py:512-532` —— 打印旁车路径那一段（D19）
    - `quantlab/base/chunking.py:307-372` —— `ChunkLedger.assert_consistent` 检查的四件事（**它不看 `start_date`**，这是已知限制注释要引用的事实）
    - `03.12-RESEARCH.md` §19.1.1（分块语义与缓存因果的精度修正）、§19.1.2（三处防护逐条）、§19.1.4（三个失效场景的实测数字）、§19.5.1 / §19.5.2 / §19.5.4
    - `03.12-PATTERNS.md` §3（R-05 判例本体，逐字）、§5.1（`_derivation()` docstring 的改写要点）、§5.7（三个要删方法的当前形状）
  </read_first>
  <action>
⚠️ **禁止 `sed` 范围删除**（`feedback_no_sed_range_delete_planning_docs`：范围删除一旦漏匹配
就会一路删到 EOF）。每一处删除都用带唯一锚点的精确替换完成，**删完后核对文件行数**
（`wc -l`），并把删前/删后行数记进 SUMMARY。

💡 **执行提示：逐处提交，别攒成一个大 commit。** 本 task 要删 6 个方法/常量 + 4 段 docstring
+ 2 对调用点 + 1 行脚本输出，跨三个文件；一次性做完再提交会让上下文在收尾阶段吃紧，而
「哪一处删漏了」恰恰要靠逐处的精确替换与行数核对来抓。建议按下面 A/B/C/D 四组各自成一个
（或数个）提交，每组删完就 `wc -l` 核对并提交，再进下一组。这只是执行节奏建议 ——
**不改 plan 划分、不改 wave 结构、不改 `files_modified`**，本 task 的范围一个字没变。
（顺带一提：本 plan 的 `estimate.tokens: 110000` 略超 100k 预算，checker 标了
`scope_sanity` WARNING。按 ADR-2629 Decision 5 超预算永远是 WARNING、不阻断执行；且
`confidence: low`（本项目尚无校准样本），这个数字本身不精确。**不拆 plan** 的理由是三个
task 有真实内聚性：Task 1 删机械、Task 2 是取代被删测试的 I-2、Task 3 是钉住删干净的零残留
gate —— 拆开会让「删除」与「证明删干净」跨 plan，反而更难验证。）

**A. `quantlab/dataset/crsp/__init__.py` 的删除（D4/D5/D6/D7/D11-D17）**

1. **D4 / D5**：删除类常量 `ADJUSTMENT_SIDECAR_SUFFIX`（`:385`）与 `ADJUSTMENT_RULE`（`:390`），
   **连同它们各自上方的 `#:` 注释块一起删**（`:380-390` 整片）。
2. **D6 / D7**：删除 `_derivation()` 里 `:534` 的那一行 gate 调用；删除 `:519-528` 那段
   解释 gate 为什么落在 `_derivation()` 的 docstring 段落。
   ⚠️ **D7 的事实要搬家，不要一起丢掉**：「`_derivation()` 是 `from_raw_data_chunked`
   （经 `_raw_axes_in_range`）与 `from_raw_data`（经 `_raw_data_to_xr`）**唯一共同**经过的方法」
   这条结构事实仍然为真且有用，把它并进 `_derivation()` 剩下的 docstring 里一句话带过。
3. **K8 的因果改写**：`:516-517` 现在写「Computed once per instance and cached. Every window
   densifier slices this, so every chunk of one conversion shares one anchor per PERMNO」——
   它把「共享一个锚」的功劳记在了**缓存**头上。改写成：正确性来自 `_derivation()` 按 config
   **全窗口**取数（`_scan_raw()` 不传参，见 `quantlab/dataset/stock.py:433/436`）而
   `_raw_data_to_xr_window`（`:1703-1741`）**在派生之后才切日期**；缓存省的是 N 次重算，
   不是正确性。缓存早返回（`:530-532`）与 config setter 的清空（`:494-497`）**保留不动**。
4. **D11 / D12 / D13 / D14**：删除 `adjustment_sidecar_path()`、`_adjustment_record()`、
   `_assert_anchor_unchanged()`、`_write_adjustment_record()` 四个方法全文，
   以及 `:1468` 那行分节注释（`# -- the adjustment anchor, and its sidecar ---`）。
   删完后 `_finalise` 的结尾应直接接到 `# -- axes and windows ---` 那一节。
5. **D15**：`_raw_axes_in_range` 里删掉 `:1596` 的 `record = self._adjustment_record()` 与
   `:1633` 的 `self._write_adjustment_record(record)`。同时改写两处文字：
   - `:1587-1592` 的 docstring 段现在说「the gate itself now lives in `_derivation()` …
     this method is still the right place for **the two WRITES**」——现在只剩**一个** write
     （`_write_identity_reports`），这段必须改写成单数，并保留「`from_raw_data_chunked`
     调用它 ONCE、在 `ChunkLedger.assert_consistent` 与任何 append 之前」这条仍然为真的时序理由；
   - `:1627-1631` 的「Written LAST … a run that fails in the derivation (an unusable adjustment
     anchor, a changed anchor) must not leave an anchor record」——「a changed anchor」这半句
     指向已删机制，删掉；「an unusable adjustment anchor」仍然为真（`_assert_anchor_usable`
     还在），保留，并把「anchor record」换成「identity reports」。
6. **D16 / D17**：`_raw_data_to_xr` 里删掉 `:1760` 与 `:1765` 的一对，以及 `:1749-1752`
   那段讲「omission 被 LATCHED」的 docstring。该方法 override 的**其余**理由
   （非分块入口也要留下 identity reports）仍然成立，保留并改写成单数写入。
7. **D1 / D2**：模块 docstring `:10-17` 的 GLOBAL 段 —— 它的前半句仍然正确（派生一次、
   跨 `[config.start_date, config.end_date]`），只有最后一句「The cached derivation is the
   reason `granularity="year"` 和 `"month"` produce the same store」把因果记错了，按第 3 步
   同样的口径改写。`:19-31` 的整段（锚随窗口延长而移动、所以 store 要记自己的锚、旁车比对、
   不一致就拒绝）**整段删除** —— 锚不再随 `end_date` 移动。

**B. D-10 的两条已知限制，写进 `_derivation()` 的注释**

8. 在 `_derivation()` 的 docstring 末尾加一段 **KNOWN LIMITATIONS**，用两个具名条目写明
   （措辞要求：陈述它**会发生**并给出实测后果，**不要**写成「不会发生」或「实践中不成立」）：
   - **`start_date` 后移**：重叠区 112,359 / 112,934 行改变，max rel **0.75**。
   - **raw tier 向前回填**（`config.start_date` 一个字没改，但 raw 里出现了比原锚更早的行）：
     实测 **756 / 756 行静默改写**，max rel **0.76**。这是 `.first()` 锚引入的**新**风险方向——
     `.last()` 锚怕 `end_date` 变，`.first()` 锚怕 `start_date` 变。
   同时写明**代码不检查其中任何一条**：`ChunkLedger.assert_consistent`
   （`quantlab/base/chunking.py:307-372`）检查符号轴指纹、store/ledger 的空与非空、
   store 末尾是否等于最后一个窗口的 end —— **它不看 `start_date`**。
   最后写明这是 **D-10 的明示取舍**：逐位相等是「实测是 0」而非「结构上不可能不是 0」，
   它依赖「start 不动 + raw 不向前长 + vintage 不改写锚行」三个外部事实；若其中任何一条
   被打破，需要另起 phase 补锚点钉机制，届时会再 churn 一次期望值。
   **不要**在这里实现任何检测、不要加旁车、不要加反解比对。

**C. `quantlab/dataset/crsp/rebuild.py` 的 M-D（注释改写，元组一行不动）**

9. `:30-32` 现在写 `.crsp_adjustment.json` 是「the adjustment anchor the adjusted series was
   built from. THE reason this phase rebuilds at all…」。改写成：**不再生成**；留在这张清单上
   是为了清掉旧 store 旁的遗留。句式**直接套** `:49-53` 的 `.crsp_symbology_report.json` 段
   （「stays on this list even though … A suffix dropped from here would leave a file describing
   a deleted mechanism sitting beside a store it never described」）。
   `:44` 那句「**All five must be DELETED before a rebuild**」的计数仍是五，不变。
   **`CRSP_SIDECAR_SUFFIXES` 元组本身一行不动**，`.crsp_adjustment.json` 留在里面（R-05）。

**D. `scripts/ingest_wrds_crsp.py` 的 D19**

10. 删掉 `:522-525` 那个打印 `Adjustment anchor sidecar: ...` 的 `print(...)` 调用整块。
    紧随其后的 `Security filter sidecar:` 那块保留。如果 `CrspStockDataset` 的 import 因此
    变成未使用，顺带清理；如果它还有别的使用点，保留 import。

**E. 越界检查**

11. `PHASE_BASE="$(git log --diff-filter=A --format=%H -n1 -- .planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-01-PLAN.md)^"; git diff --name-only "$PHASE_BASE" -- quantlab/ scripts/` 必须只含本 task 的三个文件（范围钉在 phase 基线，否则提交之后 `git diff` 变空会假绿）。特别确认 `quantlab/base/backtest.py`
    **没有**出现（D-11：`_compare_fingerprints` 保持 warning，本 phase 不动它），
    `quantlab/base/chunking.py`、`quantlab/base/config.py`、`quantlab/backtest/` 也没有出现。
12. 清 stale bytecode 后跑编译门与 CRSP 全家桶，确认没有悬挂引用。
  </action>
  <verify>
    <automated>find . -name __pycache__ -type d -prune -exec rm -rf {} + ; uv run python -m compileall -q quantlab && uv run pytest -q tests/test_crsp_first_anchor.py tests/test_crsp_rebuild.py -p no:cacheprovider</automated>
    <fails_when>non-zero exit, or the summary line reports "no tests ran", or any output line matches "failed" or "error"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -rc 'ADJUSTMENT_SIDECAR_SUFFIX' quantlab/ --include='*.py' | grep -v ':0$'` 无输出（全部为 0）
    - `grep -rn 'ADJUSTMENT_RULE\|adjustment_sidecar_path\|_adjustment_record\|_assert_anchor_unchanged\|_write_adjustment_record\|total_return_backward_from_last_close' quantlab/ scripts/ --include='*.py'` 无输出
    - `grep -c '\.crsp_adjustment\.json' quantlab/dataset/crsp/rebuild.py` ≥ `2`（元组里一处 + 注释里至少一处）
    - `uv run python -c "from quantlab.dataset.crsp.rebuild import CRSP_SIDECAR_SUFFIXES as S; assert '.crsp_adjustment.json' in S, S; assert len(S)==5, S"` 退出码 `0`
    - `uv run python -c "from quantlab.dataset.crsp import CrspStockDataset as C; assert not hasattr(C,'adjustment_sidecar_path'); assert not hasattr(C,'_assert_anchor_unchanged'); assert not hasattr(C,'ADJUSTMENT_RULE')"` 退出码 `0`
    - `grep -c 'start_date' quantlab/dataset/crsp/__init__.py` 的匹配里存在 `_derivation` 的 KNOWN LIMITATIONS 段：`grep -c 'KNOWN LIMITATION' quantlab/dataset/crsp/__init__.py` ≥ `1`
    - `grep -c '0.75\|0.76\|756' quantlab/dataset/crsp/__init__.py` ≥ `2`（两条已知限制的实测后果被写下来）
    - `grep -c 'assert_consistent' quantlab/dataset/crsp/__init__.py` ≥ `1`（已知限制里点名了不检查它的那个函数）
    - `grep -c 'Adjustment anchor sidecar' scripts/ingest_wrds_crsp.py` 返回 `0`
    - **先把改动清单落进变量**（退出码必须在这一步捕获）：`PHASE_BASE="$(git log --diff-filter=A --format=%H -n1 -- .planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-01-PLAN.md)^"; CHANGED="$(git diff --name-only "$PHASE_BASE" -- quantlab/ scripts/)"`
    - 随后 `printf '%s\n' "$CHANGED" | sort` 恰好输出三行：`quantlab/dataset/crsp/__init__.py`、`quantlab/dataset/crsp/rebuild.py`、`scripts/ingest_wrds_crsp.py`
    - `uv run python -m compileall -q quantlab` 退出码 `0`
    - SUMMARY 记录了 `quantlab/dataset/crsp/__init__.py` 删前/删后的 `wc -l` 两个数字
  </acceptance_criteria>
  <done>七个专属名字在 `quantlab/` 与 `scripts/` 里归零；`.crsp_adjustment.json` 仍在清理清单；`_derivation()` 带着两条具名的已知限制；编译门与 CRSP 回归绿。</done>
  <reversibility rating="costly">删除 6 个方法/常量与 4 段 docstring。代码本身 `git revert` 可回，但一旦 plan 04 用新锚重建了真实 store，回滚就要连带再重建一次 store 与因子库。因此本 task 之后、plan 04 之前是最后一个低成本回滚点。</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: I-2 —— 全量 vs 增量逐位相同，并成对删除/改写它所取代的那批测试</name>
  <files>tests/test_crsp_first_anchor.py, tests/test_crsp_dataset.py, tests/test_ingest_wrds_crsp.py, tests/test_crsp_ticker_sidecar.py, tests/test_crsp_rebuild_measurements.py</files>
  <read_first>
    - `tests/test_crsp_first_anchor.py`（plan 01 建的文件：helper、`ANCHOR_*` 常量、`_anchor_series_rows()`）
    - `tests/test_crsp_dataset.py:1049-1087` —— `test_year_and_month_granularity_produce_identical_stores` **逐字**。I-2 照抄这个形状，只换两处：两个 store 的差别从 `granularity` 换成「一次性全量」vs「先建 A 再 `update()`」；`_pull` 一次、`_dataset_config` 两个不同 `store=` 路径
    - `tests/test_dataset_update.py:166-201` —— 增量那一半的既有写法：`from_raw_data_chunked(granularity="year")` → 改变输入 → `StockDataset(config).update(granularity="year")`
    - `quantlab/base/data.py:380-393` —— `BaseDataset.update()` 就是 `from_raw_data_chunked(..., on_new_listing=self._AUTOMATIC)`，没有第二条 append 路径
    - `quantlab/dataset/crsp/__init__.py:492-500` —— config setter 在赋值时清空 `_derivation_cache`（换 `end_date` 必须构造/赋一个新 config 才会重新派生）
    - `tests/test_crsp_dataset.py:1038-1046` —— `_sidecar_path()` helper（随其唯一读者一起删）
    - `tests/test_crsp_dataset.py:1281-1404` —— 四条锚点旁车用例：`test_the_sidecar_records_the_anchor_beside_the_store`（删）、`test_a_second_convert_with_the_same_anchor_resumes`（改写）、`test_extending_the_window_is_refused_naming_both_anchors`（删）、`test_a_store_without_the_sidecar_is_refused`（删）
    - `tests/test_crsp_dataset.py:1506-1602` —— `test_from_raw_data_leaves_a_complete_sidecar_set`（改）与 `test_from_raw_data_runs_the_anchor_gate_too`（删，但它保护的 WR-02 事实要搬走）
    - `tests/test_ingest_wrds_crsp.py:130-145` 与 `:383-430` —— 旁车清单与 `_plant_stale_equity_store()` helper
    - `tests/test_crsp_ticker_sidecar.py:346-359` —— 只改 docstring 的那条
    - `tests/test_crsp_rebuild_measurements.py:114-128` —— `EXPECTED_WRITTEN_SIDECARS` 与它上方已经写好的「两张清单答不同问题」注释
    - `03.12-RESEARCH.md` §19.4.3（逐条处置表）、§19.4.5（I-2 的用例骨架与 `assert_identical` 的理由）、§19.5.4
    - `03.12-PATTERNS.md` §1.4 / §1.5 / §2.3
  </read_first>
  <behavior>
    - **I-2**：用 `_anchor_series_rows()` 拉一次 raw（跨 2019/2020 两年）。store A：一次性
      `convert([ANCHOR_START, ANCHOR_END], granularity="year")`。store B：先
      `convert([ANCHOR_START, "2019-12-31"], granularity="year")`，再构造一个同 `zarr_file_path`、
      `end_date=ANCHOR_END` 的 `CrspDatasetConfig` 并 `CrspStockDataset(cfg).update(granularity="year")`。
      `xr.testing.assert_identical(panel_A, panel_B)` 通过
    - I-2 的非空性自检：`panel_A.sizes["timestamp"] > 200`（否则一个空面板也能 `assert_identical`）
    - I-2 的增量性自检：store B 的 `.chunks.json` 记录了 **2 个以上**窗口（证明第二次真的是
      append 而不是整库重写）
    - 「延长 `end_date` 后的第二次转换」**不再抛 `ValueError`** —— 这正是被删掉的
      `test_extending_the_window_is_refused_naming_both_anchors` 的反面
    - `tests/test_crsp_rebuild_measurements.py` 的 `EXPECTED_WRITTEN_SIDECARS` 为三元组，
      不含 `.crsp_adjustment.json`
  </behavior>
  <action>
**A. 新增 I-2 用例**

1. 在 `tests/test_crsp_first_anchor.py` 新增
   `test_a_full_and_incremental_build_agree_bit_for_bit(mock_crsp_session, tmp_path)`，
   照抄 `tests/test_crsp_dataset.py:1049-1087` 的形状。
   **必须用 `xr.testing.assert_identical`**（逐位 + 变量名 + 坐标），**不是** `assert_allclose`。
   docstring 写明它与那条姐妹用例的区别：姐妹比的是**同一次运行**的两种分块，钉不住跨运行；
   I-2 钉的正是**跨运行**的 append，同时钉住「拼接处无虚假收益」与「历史值不被改写」。
   docstring 还要点名这是 **D-10** 的验收锚点，并指向 `_derivation()` 的 KNOWN LIMITATIONS
   段说明它**不覆盖**哪两个场景。
2. 增量那一半：A 窗口切在 `2019-12-31`（`_anchor_series_rows()` 的序列从 `ANCHOR_START =
   "2019-11-01"` 起，所以这个切点落在两个 year 窗口的边界上）。第二段用**新构造**的
   `CrspDatasetConfig`（同 `zarr_file_path`，`end_date=ANCHOR_END`）+
   `CrspStockDataset(cfg).update(granularity="year")`；不要试图原地改旧 config 的字段。
3. 加两条自检：`panel_A.sizes["timestamp"] > 200`；读 `{zarr}.chunks.json` 确认 store B
   的窗口数 ≥ 2。

**B. 成对删除/改写它所取代的那批测试**（⚠️ 逐条精确替换，禁止 `sed` 范围删除）

4. `tests/test_crsp_dataset.py`：
   - **删** `_sidecar_path()`（`:1038-1046`，随其唯一读者）；
   - **删** `test_the_sidecar_records_the_anchor_beside_the_store`（`:1281-1307`）；
   - **删** `test_extending_the_window_is_refused_naming_both_anchors`（`:1332-1377`）——
     它测的行为在 `.first()` 下是合法 append，已由 I-2 取代；
   - **删** `test_a_store_without_the_sidecar_is_refused`（`:1380-1404`）；
   - **改写** `test_a_second_convert_with_the_same_anchor_resumes`（`:1310-1330`）：
     保留它的**意图**（第二次转换必须能续，而不是被拒），把名字与断言从「同一个锚 ⇒ 放行」
     改成「延长窗口的第二次转换不再因锚点被拒」。改完的名字里不得再出现 anchor 旁车的措辞；
   - **改** `test_from_raw_data_leaves_a_complete_sidecar_set`（`:1506-1566`）：旁车集合里
     去掉锚点旁车（它由 `_sidecar_path()` 取得，那个 helper 已删），剩 `filter_report` 与
     `ticker_sidecar` 两个；docstring 里讲 LATCHED 的那段随机制删除；结尾那个
     `except ValueError` 分支里「断言消息不含锚点旁车名」的那行也随之删除，`"chunk ledger"`
     那条**保留**（它是 ledger 的拒绝，不是锚的）；
   - **删** `test_from_raw_data_runs_the_anchor_gate_too`（`:1568-1602`）。
     ⚠️ **它保护的 WR-02 事实必须搬家**：「两个转换入口（`from_raw_data_chunked` 经
     `_raw_axes_in_range`，`from_raw_data` 经 `_raw_data_to_xr`）都要留下 identity reports」
     —— 把这条并进上一条 `test_from_raw_data_leaves_a_complete_sidecar_set` 的 docstring，
     它本来就是这条事实的用例。
5. `tests/test_ingest_wrds_crsp.py`：
   - `:136-140` 的旁车清单里去掉 `".crsp_adjustment.json"`，只剩
     `".crsp_filter_report.json"`；上方那段解释「两个旁车，不是三个」的注释要相应改成
     「一个旁车」并补一句说明锚点旁车在 03.12 停止生成；
   - `_plant_stale_equity_store()`（`:383-430`）**改，不删**：去掉 `:400-424` 种锚点旁车那段
     （连同它上方解释「旁车 NAME 来自生产 classmethod」的注释），返回值从四元组收缩成
     `(store, store_entries)`，并同步改 `:387-390` 的 docstring（它现在讲
     「`_assert_anchor_unchanged` is right to refuse」，那个机制没了；真正的断言是
     「被种下的文件原封不动」，这条保留）。所有调用点跟着改。
6. `tests/test_crsp_ticker_sidecar.py:346-359`：**只改 docstring 第一句**（它把已删机制当作
   消息形状的出处）。改成指向本仓库的 shaped-error 惯例本身：`type(exc).__name__` +
   这个 sidecar 记录什么 + 重建的出路。**测试主体是 ticker 旁车，一行不动，不要删这条用例。**
7. `tests/test_crsp_rebuild_measurements.py:122-128`：从 `EXPECTED_WRITTEN_SIDECARS` 里
   **删掉 `".crsp_adjustment.json"` 一行**。`:114-121` 那段注释**不必重写** ——
   它已经把 R-05 的判例写好了，`.crsp_adjustment.json` 只是加入 `.crsp_symbology_report.json`
   的行列，成为「清理清单里有、必写清单里没有」的第二个例子。可选：在注释里加一句点名这第二个例子。
   ⚠️ **不要**在本 task 碰这个文件的其他部分（I-1/I-5 真实数据门是 plan 04 的任务）。
8. `tests/test_crsp_rebuild.py:360-370` 的清理清单断言**一行不改** ——
   `CRSP_SIDECAR_SUFFIXES` 仍然是那五个（R-05）。

**C. 核对**

9. 每次删除后核对文件行数，并确认没有把相邻用例误删：
   `uv run pytest -q tests/test_crsp_dataset.py --collect-only -q | tail -1` 的用例数，
   删前/删后两个数字都记进 SUMMARY，差值应正好等于净删除的用例数（4 条）。
  </action>
  <verify>
    <automated>find . -name __pycache__ -type d -prune -exec rm -rf {} + ; uv run pytest -q tests/test_crsp_first_anchor.py tests/test_crsp_dataset.py tests/test_ingest_wrds_crsp.py tests/test_crsp_ticker_sidecar.py tests/test_crsp_rebuild.py -p no:cacheprovider</automated>
    <fails_when>non-zero exit, or the summary line reports "no tests ran", or any output line matches "failed" or "error"</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -q tests/test_crsp_first_anchor.py -k full_and_incremental -p no:cacheprovider` 输出的 summary 行含 `1 passed`
    - `grep -c 'assert_identical' tests/test_crsp_first_anchor.py` ≥ `1`
    - `grep -c 'assert_allclose' tests/test_crsp_first_anchor.py` 返回 `0`（I-2 必须逐位比较）
    - `grep -c '\.update(' tests/test_crsp_first_anchor.py` ≥ `1`（增量那一半走 `BaseDataset.update()`）
    - `grep -c '_sidecar_path' tests/test_crsp_dataset.py` 返回 `0`
    - `grep -c 'crsp_adjustment' tests/test_crsp_dataset.py tests/test_ingest_wrds_crsp.py` 两个文件都返回 `0`
    - `uv run python -c "import re; s=open('tests/test_crsp_rebuild_measurements.py').read(); m=re.search(r'EXPECTED_WRITTEN_SIDECARS.*?\)', s, re.S).group(0); assert '.crsp_adjustment.json' not in m, m; assert m.count('\".crsp') + m.count('\".chunks') == 3, m"` 退出码 `0`
    - `grep -c 'def test_a_corrupt_sidecar_names_the_exception_type_and_the_way_out' tests/test_crsp_ticker_sidecar.py` 返回 `1`（这条**没有**被删）
    - `grep -c 'def _plant_stale_equity_store' tests/test_ingest_wrds_crsp.py` 返回 `1`（这个 helper **没有**被删）
    - `grep -c 'def test_extending_the_window_is_refused_naming_both_anchors\|def test_the_sidecar_records_the_anchor_beside_the_store\|def test_a_store_without_the_sidecar_is_refused\|def test_from_raw_data_runs_the_anchor_gate_too' tests/test_crsp_dataset.py` 返回 `0`
    - `uv run pytest -q tests/test_crsp_rebuild.py -p no:cacheprovider` 全绿（清理清单断言未被误改）
    - SUMMARY 记录了 `tests/test_crsp_dataset.py` 删前/删后的 `--collect-only` 用例数两个数字
  </acceptance_criteria>
  <done>I-2 绿；四条为窗口锚点旁车而存在的用例被删，两条被改写而不是删，`.crsp_adjustment.json` 从必写清单移出、在清理清单里留存。</done>
  <reversibility rating="reversible">测试增删改，`git revert` 可回。</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 仓库级「锚点机械零残留」gate —— 照 `tests/test_no_identity_residue.py` 的三条设计约束新建</name>
  <files>tests/test_no_anchor_machinery_residue.py</files>
  <read_first>
    - `tests/test_no_identity_residue.py`（**整文件，144 行，逐字范本**）：模块 docstring `:1-48` 的三条设计约束与它们各自的理由、`PACKAGE_ROOT` 的解析方式（`Path(__file__).resolve().parent.parent / "quantlab"`，从本文件解析所以任何工作目录下都成立）、`DELETED_IN_03_11_07` 清单的分组注释体例、`_sources()` 的非空守卫（`len(sources) > 20`）、`_hits()` 的词边界正则、断言消息把命中逐条列出来的写法
    - `quantlab/dataset/crsp/rebuild.py:25-60` —— 确认 `.crsp_adjustment.json` 这个**字面量**必须继续存在于 `CRSP_SIDECAR_SUFFIXES` 里（R-05），因此**不能**上零残留清单
    - `03.12-PATTERNS.md` §4（范本的核心三段逐字 + 三条必须继承的设计约束 + 本 phase 的清单候选）
    - `03.12-RESEARCH.md` §19.5.1（七个名字的出处坐标）、§9 的 R-05 与 R-09
  </read_first>
  <behavior>
    - 在当前树上跑：`quantlab/**/*.py` 里这七个名字的命中数为 0，用例绿
    - 把任意一个名字写回 `quantlab/` 下任意一个 `.py` 文件的**注释**里（不是代码），用例变红 —— 注释计入命中
    - 扫描非空：walk 到的 `.py` 文件数 > 20，否则用例自己断言失败（一个扫不到文件的 gate 比没有 gate 更坏）
    - 断言失败时的消息把每一条命中按 `path:line: text` 逐条列出来，不截断、不采样
    - `tests/` 与 `example/` 与 `.planning/` 里出现这些名字**不**让用例变红（扫描范围只有 `quantlab/`）
  </behavior>
  <action>
新建 `tests/test_no_anchor_machinery_residue.py`。

**为什么是新文件而不是并进 `tests/test_no_identity_residue.py`**：后者的模块 docstring
逐字讲的是 03.11 的 ticker-identity 机械，文件名就是那张清单的家；把一张无关的清单塞进去会让
文件名变成谎话。03.11 的判例是「一张清单，一个家」——本 phase 照这条判例开第二个家。
把这个理由写进新文件的模块 docstring。

1. 模块 docstring 写清楚三件事：
   - **它守什么**：03.12 把复权锚点改成每个 PERMNO 在 store 内第一个可用行之后，整套为
     「锚点随转换窗口移动」而存在的机械（窗口四元组旁车、跨运行闸门、它的记录器与写入器）
     被删除；这个文件是让它保持被删除的东西。
   - **命中计注释与 docstring，这是故意的**：照抄范本 `:23-35` 的论证 ——
     `quantlab/dataset/` 的 docstring 密度异常高且互相交叉引用，一条指向已删机制的注释不是杂物，
     是**关于系统如何工作的假陈述**。所以不做 `grep -v '^#'`、不做丢弃 docstring 的 AST 扫描。
     这一条对本 phase 尤其要紧：被删的四段 docstring 分布在
     `quantlab/dataset/crsp/__init__.py` 的 `:10-31` / `:519-528` / `:1749-1752` 与若干注释里。
   - **扫描只有 `quantlab/`**：`.planning/`、`example/`、`tests/` 故意出范围 ——
     计划文档要能写「`_assert_anchor_unchanged` 因为…被删了」，散文文档要能说某个机制在
     03.12 被移除。点名死者正是解释一次删除的方式；规则只是**出货包**不许出现它们。
   - **成员规则**：只有**专属于**被删机械的名字才上清单。
     ⚠️ 显式记下一条排除判例：**`.crsp_adjustment.json` 这个字面量不上清单**，
     因为它必须继续活在 `quantlab/dataset/crsp/rebuild.py:CRSP_SIDECAR_SUFFIXES` 的清理清单里
     （R-05）；把它列上就是「会叫狼的 gate」，而会叫狼的 gate 会被人学会跳过。
2. `PACKAGE_ROOT = Path(__file__).resolve().parent.parent / "quantlab"`，逐字照范本
   （从本文件解析，所以 gate 在任何工作目录下都成立）。
3. `DELETED_IN_03_12_02: tuple[str, ...]`，按出处分组加注释，七个成员：
   `"ADJUSTMENT_SIDECAR_SUFFIX"`、`"ADJUSTMENT_RULE"`、`"adjustment_sidecar_path"`、
   `"_adjustment_record"`、`"_assert_anchor_unchanged"`、`"_write_adjustment_record"`、
   `"total_return_backward_from_last_close"`。
4. `_sources()` 与 `_hits()` 照抄范本（`rglob("*.py")` + `sorted()` + `len(sources) > 20`
   的非空守卫 + 词边界正则 `(?<![A-Za-z0-9_])(?:...)(?![A-Za-z0-9_])` + `path:line: text` 的
   命中格式）。**不要**把它们从 `tests/test_no_identity_residue.py` import 过来 ——
   本仓库测试文件各自持有 helper。
5. 用例名 `test_the_deleted_window_anchor_machinery_leaves_no_residue_in_quantlab`，
   断言 `_hits(DELETED_IN_03_12_02) == []`，失败消息照范本把命中条数与逐条内容拼进去，
   并明说「注释里的引用也算，而且是故意列在这里的」。
6. **变异验证**（写进 SUMMARY，不留在树上）：临时把 `_assert_anchor_unchanged` 这个词
   写进 `quantlab/dataset/crsp/rebuild.py` 的一行注释里，跑一次确认用例**变红**且消息里
   出现那一行的 `path:line:`，然后撤销。这证明 gate 不是空转的。
7. ⚠️ 跑之前先清 stale bytecode（R-09）：`.pyc` 会让已删函数继续可导入，gate 虽然是静态扫源码
   不受影响，但同一次 verify 里的其他用例会假绿。
  </action>
  <verify>
    <automated>find . -name __pycache__ -type d -prune -exec rm -rf {} + ; uv run pytest -q tests/test_no_anchor_machinery_residue.py tests/test_no_identity_residue.py -p no:cacheprovider</automated>
    <fails_when>non-zero exit, or the summary line reports "no tests ran", or the summary line does not report at least "2 passed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `test -f tests/test_no_anchor_machinery_residue.py` 退出码 `0`
    - `uv run python -c "import re; s=open('tests/test_no_anchor_machinery_residue.py').read(); import ast; t=[n for n in ast.walk(ast.parse(s)) if isinstance(n,ast.Assign)]; names={getattr(n.targets[0],'id',None) for n in t}; assert 'DELETED_IN_03_12_02' in names, names"` 退出码 `0`
    - `uv run python -c "import sys; sys.path.insert(0,'tests'); import test_no_anchor_machinery_residue as m; assert len(m.DELETED_IN_03_12_02)==7, m.DELETED_IN_03_12_02; assert '.crsp_adjustment.json' not in m.DELETED_IN_03_12_02; assert len(m._sources())>20"` 退出码 `0`
    - `grep -c "grep -v" tests/test_no_anchor_machinery_residue.py` 返回 `0`（不得剥注释）
    - `grep -c 'PACKAGE_ROOT' tests/test_no_anchor_machinery_residue.py` ≥ `2`（常量定义 + `_sources()` 使用）
    - `grep -c 'from tests.test_no_identity_residue import\|from test_no_identity_residue import' tests/test_no_anchor_machinery_residue.py` 返回 `0`
    - `uv run pytest -q tests/test_no_anchor_machinery_residue.py -p no:cacheprovider` 输出的 summary 行含 `1 passed`
    - SUMMARY 记录了第 6 步变异验证的两次运行结果（注入后红、撤销后绿），含红那次消息里的 `path:line:`
  </acceptance_criteria>
  <done>一个会在注释里也抓人的仓库级 gate 存在并绿，其扫描范围限定 `quantlab/`，清单恰好七个专属名字，且已做过变异验证。</done>
  <reversibility rating="reversible">纯新增测试文件。</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw tier → `_derivation()` | vendor 落盘事实进入派生算术。删除闸门之后，这条边界上**不再有**任何跨运行的一致性检查 |
| `_derivation()` → zarr store（append 路径） | 一次 append 把新窗口写进一个已存在的 store。旧闸门守的就是这里 |
| 已删机制的**名字** → `quantlab/` 源码与注释 | 一条指向已删机制的注释是关于系统如何工作的假陈述，会把下一个读者送去找一个不存在的东西 |

**不存在的边界，如实记录**：本 plan 不引入网络调用、不解析外部输入、不读写凭证、
不新增反序列化面。删除的是本地文件读写（一个 JSON 旁车）。

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.12-05 | Tampering | append 路径（`_derivation()` 删掉闸门之后） | medium | mitigate | 实测：`start_date` 后移改写 112,359 行（max rel 0.75）；**raw tier 向前回填**在 `start_date` 一字未改的情况下静默改写 756/756 行（max rel 0.76）。`ChunkLedger.assert_consistent`（`quantlab/base/chunking.py:307-372`）两者都不看。**D-10 明确否决了检测机制（1-C）**，所以缓解是「把边界写下来 + 把被支持的形态钉住」：两条具名 KNOWN LIMITATION 进 `_derivation()` 注释与 `example/wrds_crsp.md`（plan 03），I-2 用例钉住唯一被支持的 append 形态（固定 start、延长 end）逐位相等。残余风险具名且归属操作流程，不归属代码 |
| T-03.12-06 | Tampering | `quantlab/dataset/crsp/rebuild.py:CRSP_SIDECAR_SUFFIXES` | high | mitigate | 若顺手把 `.crsp_adjustment.json` 从**清理**清单里删掉，重建后会留下一个描述已删机制的 JSON 躺在一个它从未描述过的新 store 旁边 —— 一条「自信地错」的审计线索，比没有线索更坏。缓解：R-05 判例写进 prohibitions；acceptance 用 `CRSP_SIDECAR_SUFFIXES` 的导入断言（含 `len == 5`）钉住；`tests/test_crsp_rebuild.py:364-370` 的既有断言是第二道门 |
| T-03.12-07 | Repudiation | 被删机制在注释/docstring 里的残留 | medium | mitigate | 删了代码留下注释，等于留下一份关于系统如何工作的假陈述。缓解：Task 3 的零残留 gate **计注释与 docstring**，且经过变异验证（注入一条注释必须让它变红） |
| T-03.12-08 | Tampering | stale `.pyc`（R-09） | medium | mitigate | 03.11-14 的实战教训：残留字节码让已删函数继续可导入，删除类 verify 会假绿。缓解：每条删除任务的 verify 命令以 `find . -name __pycache__ -type d -prune -exec rm -rf {} +` 开头；零残留 gate 本身扫源码文本而非导入，不受 `.pyc` 影响 |
| T-03.12-09 | Denial of Service | `sed` 范围删除（`feedback_no_sed_range_delete_planning_docs`） | medium | mitigate | 范围删除一旦漏匹配会一路删到 EOF，把一个删除变成一次文件截断。缓解：prohibitions 明令禁止；每处删除后核对 `wc -l` 与 `--collect-only` 用例数，两组数字进 SUMMARY |
| T-03.12-SC | Tampering | npm/pip/cargo installs | low | accept | **本 plan 无任何包安装任务。** RESEARCH `## Package Legitimacy Audit` 逐字：本 phase 不安装任何外部包；审计表为空，`[SLOP]` / `[SUS]` 均为 none。因此不注入包合法性 `checkpoint:human-verify` |
</threat_model>

<verification>
1. `find . -name __pycache__ -type d -prune -exec rm -rf {} +`（R-09）
2. `uv run python -m compileall -q quantlab` 退出码 0
3. `uv run pytest -q tests/test_no_anchor_machinery_residue.py tests/test_no_identity_residue.py -p no:cacheprovider` 全绿
4. `uv run pytest -q tests/test_crsp_dataset.py tests/test_crsp_identity.py tests/test_crsp_tracer.py tests/test_crsp_rebuild.py tests/test_crsp_ticker_sidecar.py tests/test_crsp_reference_tables.py tests/test_ingest_wrds_crsp.py tests/test_crsp_first_anchor.py -p no:cacheprovider` 全绿（CRSP 全家桶）
5. `uv run pytest -q tests/test_backtest_run.py tests/test_backtest_run_cv.py tests/test_backtest_persistence.py tests/test_backtest_rebuild.py tests/test_factor_kunquant.py tests/test_universe_filtered_factor.py -p no:cacheprovider` 全绿（D-04 / D-11 的护栏）
6. `uv run pytest -q --ignore=tests/test_factor_hierarchy.py --ignore=tests/test_crsp_rebuild_measurements.py -p no:cacheprovider` 全量回归绿
7. 先落变量（退出码在这一步捕获）：`PHASE_BASE="$(git log --diff-filter=A --format=%H -n1 -- .planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-01-PLAN.md)^"; CHANGED="$(git diff --name-only "$PHASE_BASE" -- quantlab/ scripts/ tests/)"`；
   另外 `git diff --name-only "$PHASE_BASE" -- quantlab/base/ quantlab/backtest/` 为空
8. `printf '%s\n' "$CHANGED" | sort` 的输出是 plan 01 与 plan 02 两者 `files_modified` 的并集（不含 `example/`，那是 plan 03 的）
</verification>

<success_criteria>
- [ ] 七个专属名字在 `quantlab/` 与 `scripts/` 里归零，且由一个计注释的 gate 守着
- [ ] `.crsp_adjustment.json` 仍在 `CRSP_SIDECAR_SUFFIXES`（5 元组），不在 `EXPECTED_WRITTEN_SIDECARS`（3 元组）
- [ ] I-2 绿，且用的是 `assert_identical` 而不是 `assert_allclose`
- [ ] `_derivation()` 带着两条具名的 KNOWN LIMITATION，含实测数字与「代码不检查」这句话
- [ ] `test_a_corrupt_sidecar_names_the_exception_type_and_the_way_out` 与 `_plant_stale_equity_store` 都还在（只改了 docstring / 去掉种旁车那段）
- [ ] `quantlab/base/backtest.py` 零改动（D-11）；`quantlab/base/config.py` 零改动
- [ ] 零残留 gate 做过变异验证，SUMMARY 里有红/绿两次运行的证据
- [ ] 全量回归绿
</success_criteria>

<output>
Create `.planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-02-SUMMARY.md` when done.
SUMMARY 必须包含：`quantlab/dataset/crsp/__init__.py` 删前/删后 `wc -l`、
`tests/test_crsp_dataset.py` 删前/删后 `--collect-only` 用例数、零残留 gate 变异验证的
红/绿两次输出、以及 `<verification>` 七条命令各自的 summary 行。
</output>
