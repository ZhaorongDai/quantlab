---
phase: "03.12"
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - quantlab/dataset/crsp/__init__.py
  - tests/test_crsp_first_anchor.py
  - tests/test_crsp_dataset.py
  - tests/test_crsp_tracer.py
  - tests/test_crsp_identity.py
autonomous: true
requirements: [DATA-03]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "**I-1**：重建/转换后的 store 里，每个 PERMNO 第一个同时满足 D-05 三条谓词的行上 `adjClose == close`（`pytest.approx(..., rel=1e-12)`，**禁用精确 `==`**：真实 store 上精确相等只有 514/520），且同一行 `adjVolume == volume`（RESEARCH §19.4.5）"
    - "**I-3**：把同一个 store 的原始列按旧 `.last()` 锚重算一遍，`adjClose^first_t / adjClose^last_t` 在 t 上恒定（`rel=1e-9`）—— 换锚是纯粹的每标的常数重标度，任何接缝都会让它变量（RESEARCH §19.4.5）"
    - "**I-4**：`adjClose_t / adjClose_{t-1}` 在两个锚下相同（`rel=1e-9`；真实 store 实测 max rel 4.900e-16）—— 只换刻度，不换经济含义"
    - "**D-05** 完整保留：`_assert_anchor_usable` 与 CR-01 / CR-02 的三条**同行**谓词（`close.is_not_null() & (close > 0.0) & dlycumfacshr.is_not_null()`）一个字不改；`close` 的哨兵派生（`quantlab/dataset/crsp/__init__.py:571-594`）一个字不改"
    - "**D-07** 三种复权因子仍然分开：`adjClose` 走 `cum_prod(1 + dlyret)`、`splitFactor` 走 `dlycumfacpr`、`adjVolume` 走 `dlycumfacshr`，本 plan 不把任何两者合并"
    - "**D-04** 兑现：`quantlab/base/config.py`、`quantlab/base/`、`quantlab/backtest/`、`quantlab/factor/`、`quantlab/label/` 全部零改动；store 仍是 27 个 `data_vars`，五个 `adj*` 仍是落盘列"
    - "`tests/test_crsp_dataset.py:1090` 的用例不再叫 `test_the_anchor_is_the_last_non_null_close` —— 它的三条断言在 `.first()` 下仍绿，但名字与 docstring 会变成谎话（「测试通过但不再测它声称测的东西」）"
  artifacts:
    - tests/test_crsp_first_anchor.py
    - quantlab/dataset/crsp/__init__.py
  key_links:
    - "`quantlab/dataset/crsp/__init__.py:570` 的 `frame.sort([\"permno\", \"timestamp\"])` → `:615-617` 的 `.first()` 归约 —— polars 的组内聚合按帧内行序取值，这一行是 `.first()` 语义的**唯一**依据，删掉它 `.first()` 取到的就是任意一行"
    - "`:607-612` 的三条同行谓词 → `:615-617` 的一次 `group_by().agg()` → `close_A` / `G_A` / `cumfacshr_A` 三个量来自**同一个索引** —— 这是 CR-02 的防线，归约必须保持是一次"
    - "`:1456-1466` `_finalise` 的 `select(columns)` → 27 个 `data_vars` —— 五个 `adj*` 仍在 `TIINGO_VARIABLES` 里，这一段一个字不动"
  prohibitions:
    - "不得删除或移动 `:570` 的 `frame.sort([\"permno\", \"timestamp\"])`"
    - "不得改动 `:607-612` 的三条谓词、不得把 `close` 的哨兵派生换成 `dlyprc > 0`（D-05 / R-02）"
    - "不得改动 `:1456-1466` 的 `select(columns)`、不得从 `TIINGO_VARIABLES` 里移除任何 `adj*`（store 保持 27 个 data_vars）"
    - "不得新增 `CrspDatasetConfig.anchor` 字段或任何锚点参数（`quantlab/base/config.py` 一个字节不改；2026-09-22 修订裁定，M3/M4 作废）"
    - "不得实现 `.last()` / `\"listing\"` / 锚点参数化 —— **D-01 / D-02 / D-03 / D-06 已于 2026-09-22 作废**，本 phase 不实现其中任何一条"
    - "不得把 Alpha101 截面归一化（D-08）或 Alpaca 复权（D-09）纳入本 plan —— 两者均已在 backlog"
    - "不得在本 plan 删除 `_assert_anchor_unchanged` 及其旁车机械（那是 plan 02 的范围）；本 plan 结束时该机械仍在，因此**不要**在本 plan 写「延长 end_date 后 append」的用例"
    - "不得把 I-1 的锚行等式写成精确 `==`（真实 store 上只有 514/520 精确相等）"
---

<!-- planner-discipline-allow: test_the_anchor_is_the_last_non_null_close, last row -->
<!-- 理由：这两个字面量是本 plan 要**改掉**的既有名字/措辞，acceptance 对它们做的是 `quantlab/` 与
     `tests/` 内的计数归零检查；它们出现在 PLAN.md 的 action 里不会进入被检查的文件。 -->

<objective>
把 CRSP 的复权锚点从「转换窗口内**最后**一个可用行」（前复权）改成「每个 PERMNO 在 store 内
**第一个**可用行」（后复权），仍在保存 zarr 时计算，并用三条可断言的不变量（I-1 / I-3 / I-4）
把这次换锚钉死。

核心改动只有三行（`quantlab/dataset/crsp/__init__.py:615/616/617` 的 `.last()` → `.first()`）。
本 plan 的份量全在**证据**上：换锚之后必须能证明 (a) 锚确实落在第一个可用行、(b) 换锚只是每标的
一个常数重标度、(c) 收益序列一个位都没动。

Purpose: 锚在只向前 append 的前提下永不移动 ⇒ store 真正不可变 ⇒ 后续 plan 才能把整套
「锚点会移动」的窗口机械删掉。
Output: 改动后的选锚三行 + 新测试文件 `tests/test_crsp_first_anchor.py`（I-1 / I-3 / I-4）+
6 条硬编码期望值的重新固化。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-CONTEXT.md
@.planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-RESEARCH.md
@.planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-PATTERNS.md
@.planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-VALIDATION.md
</context>

## 读 CONTEXT.md 与 RESEARCH.md 的必读前提

- **CONTEXT.md 的 D-01 / D-02 / D-03 / D-06 已作废。** 权威是文件开头的
  `## ⚠ 2026-09-22 决策修订` 一节及其后的 **D-10 / D-11 / D-12**。
  **D-04 / D-05 / D-07 / D-08 / D-09 仍然有效。**
- **RESEARCH.md 的 §2.1 / §2.2 / §2.3 / §2.4 / §5 / §6.2 / §6.3 / §6.4 / §11 / §14 结论已失效。**
  权威是 **§19**。仍然有效：§1 的坐标、§3 下游读者清单、§4 Runtime State、§7 verify 命令、
  §7.1 本机数据现状、§9 的 R-01 / R-02 / R-05 / R-09 / R-11。
- `03.12-PATTERNS.md` 与 `03.12-VALIDATION.md` **都已按修订版编写，可直接信任**。

## Probe notes（显式记录，不是静默丢弃）

- **Spec-less probe fallback：可见跳过。** 本 phase 无 `*-SPEC.md`，ROADMAP 的 `Requirements`
  字段是 `TBD`，`phase_req_ids` 为空 ⇒ 没有可探测的需求文本。因此本 plan **不**含探针派生的
  `must_haves` 谓词，也不含 Edge Coverage / Prohibitions 探针条目；`must_haves.truths` 的核心
  是 VALIDATION.md 的五条不变量 I-1..I-5（本 plan 承担 I-1 / I-3 / I-4）。
- **API Coverage 探针：`detected: false`** ⇒ 不产出 `COVERAGE.md`。
- **Assumption-Delta 探针：`detected: false`** ⇒ 无身份模型检查点。
- **Schema Push Gate：静默跳过** —— 本仓库是 Python 量化项目，无任何 ORM schema 模式。
- **Package Legitimacy Gate：不适用** —— 本 phase 不安装任何外部包（RESEARCH `## Package
  Legitimacy Audit` 逐字：本 phase 不安装任何外部包），无 `uv add` / `pip install` 任务。

## Artifacts this phase produces（全 phase 清单）

| 符号 | 位置 | 类型 | 产出 plan |
|---|---|---|---|
| `tests/test_crsp_first_anchor.py` | 仓库根 `tests/` | **新文件** | 01 |
| `SYNTHETIC_PERMNO` / `SYNTHETIC_SYMBOL` / `SYNTHETIC_AXIS` | `tests/test_crsp_first_anchor.py` | 模块常量 | 01 |
| `ANCHOR_START` / `ANCHOR_END` / `ANCHOR_SPLIT_DAY` / `ANCHOR_DIVIDEND_DAY` / `ANCHOR_DIVIDEND` | 同上 | 模块常量（复刻） | 01 |
| `_synthetic_secinfo` / `_reference_rows` / `_pull` / `_dataset_config` / `_convert` / `_panel` / `_at` / `_build_store` / `_build` / `_anchor_series_rows` | 同上 | 文件私有 helper | 01 |
| `test_the_anchor_is_first_usable_row_and_its_adjclose_equals_its_close` | 同上 | 用例（**I-1**） | 01 |
| `test_switching_the_anchor_is_a_constant_rescale_per_permno` | 同上 | 用例（**I-3**） | 01 |
| `test_the_day_over_day_ratios_unchanged_by_the_anchor_switch` | 同上 | 用例（**I-4**） | 01 |
| `test_the_anchor_is_the_FIRST_usable_row` | `tests/test_crsp_dataset.py`（改名自既有用例） | 用例 | 01 |
| `test_a_full_and_incremental_build_agree_bit_for_bit` | `tests/test_crsp_first_anchor.py` | 用例（**I-2**） | 02 |
| `tests/test_no_anchor_machinery_residue.py` | 仓库根 `tests/` | **新文件** | 02 |
| `PACKAGE_ROOT` / `DELETED_IN_03_12_02` / `_sources` / `_hits` | `tests/test_no_anchor_machinery_residue.py` | 模块常量 / helper | 02 |
| `test_the_deleted_window_anchor_machinery_leaves_no_residue_in_quantlab` | 同上 | 用例 | 02 |
| `test_the_real_store_reproduces_its_adjusted_columns` | `tests/test_crsp_rebuild_measurements.py` | 用例（**I-1 + I-5** 真实数据门） | 04 |

**本 phase 不新增任何生产侧符号**：无新类、无新函数、无新 CLI flag、无新 dataclass 字段、
无新模块。`quantlab/base/config.py` 一个字节不改。生产侧的净变化是 **3 行算术 + 一批删除 + 文字**。

**本 phase 删除的生产侧符号**（plan 02 执行，逐字进零残留 gate 清单）：
`ADJUSTMENT_SIDECAR_SUFFIX`、`ADJUSTMENT_RULE`、`adjustment_sidecar_path`、`_adjustment_record`、
`_assert_anchor_unchanged`、`_write_adjustment_record`、`total_return_backward_from_last_close`。

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 端到端切片 —— 选锚三行改 `.first()`，一个最小合成 store 端到端产出后复权 `adjClose` 并断言 I-1</name>
  <files>quantlab/dataset/crsp/__init__.py, tests/test_crsp_first_anchor.py</files>
  <read_first>
    - `quantlab/dataset/crsp/__init__.py:565-640` —— 改动现场全文。`:570` 的 `frame.sort(["permno","timestamp"])`（M-A 的唯一语义依据，**必须留着**）、`:571-594` 的哨兵 `close` 派生与 `_G`（D-05 / K3 / K5，一个字不改）、`:596-606` 的选锚注释、`:607-612` 的三条谓词（K2，一个字不改）、`:615-617` 的三个 `.last()`（**就是 M-A**）
    - `quantlab/dataset/crsp/__init__.py:665-741` —— `_assert_anchor_usable` 全体（D-05 保留）。特别是 `:706` 的错误消息措辞
    - `quantlab/dataset/crsp/__init__.py:44-58` —— 模块 docstring 里「the anchor `A` is the PERMNO's ... row carrying a USABLE LEVEL」那一段（D3）
    - `quantlab/dataset/crsp/__init__.py:1456-1466` —— `_finalise` 的 `select(columns)`（**确认它不需要改**：五个 `adj*` 仍在 `TIINGO_VARIABLES` 里，store 保持 27 个 data_vars）
    - `tests/test_crsp_dataset.py:1-38` —— 模块 docstring 与 import 约定（**两条硬规则**：所有 quantlab import 在函数体内；合成行带 `# SYNTHETIC` provenance 注释）
    - `tests/test_crsp_dataset.py:39-216` —— 常量与 helper 五件套 `_synthetic_secinfo` / `_reference_rows` / `_pull` / `_dataset_config` / `_convert` / `_panel` / `_build_store` / `_build` / `_at`，以及 `mock_crsp_session` fixture 的用法
    - `tests/test_crsp_dataset.py:1090-1131` —— 既有的锚点用例与它的三行合成 fixture（把无价格行从**末尾**搬到**开头**就是 I-1 的 fixture）
    - `tests/conftest.py:1246` 附近 —— `mock_crsp_session` fixture 的定义
    - `03.12-PATTERNS.md` 的 `### 1. tests/test_crsp_first_anchor.py`（§1.1 / §1.2 / §1.3 / §1.6 逐字骨架）与 `### 5. _derivation() 本体`（§5.2 / §5.4 / §5.5）
    - `03.12-RESEARCH.md` §19.4.5（I-1 的实测口径与容差理由）、§19.5.3 的 M-A 行
  </read_first>
  <behavior>
    - 一个 PERMNO，三个合成交易日：`2020-03-02` **无价格**（`dlyprc=None`、`dlyprcflg=None`、`dlyret=None`、`dlyretmissflg="MP"`），`2020-03-03` 价 100.0 / 收益 0.010000，`2020-03-04` 价 102.0 / 收益 0.020000。转换窗口 `2020-03-01..2020-03-31`
    - 锚跳过第一行的无价格行，落在 `2020-03-03`：`adjClose(2020-03-03) == pytest.approx(100.0, rel=1e-12)`，即锚行的 `adjClose` 就是它自己的 `close`（**I-1**）
    - 同一锚行上 `adjVolume(2020-03-03) == pytest.approx(volume(2020-03-03), rel=1e-12)`（`dsf_row` 默认 `dlyvol=1_000_000`，且锚前后 `dlycumfacshr` 为单位因子）
    - `adjClose(2020-03-04) == pytest.approx(102.0, rel=1e-12)`：锚在首行之后，后一天按 `G_t/G_A = 1.02` 放大，正好等于它自己的 close
    - `adjClose(2020-03-02)` 是 **NaN**（无价格那天不发布价格，掩码由 `close` 决定，与锚无关）
    - 用 `_anchor_series_rows()` 造的 11 个月多标的序列转换后不抛 `ValueError`（D-05 的 `_assert_anchor_usable` 在 `.first()` 下不会变成常态化拒绝 —— R-01 的风险方向已反转，但仍要有一条 verify）
  </behavior>
  <action>
**第一步：先写红的测试（本文件是 TDD-red 文件，所有 quantlab import 必须在函数体内）。**

新建 `tests/test_crsp_first_anchor.py`：

1. 模块 docstring 复刻 `tests/test_crsp_dataset.py:1-27` 的两条硬规则，逐字保留这两段的措辞意图：
   (a)「Every quantlab import is INSIDE a test or helper body」——理由是 TDD gate #3770：
   模块级 import 会把 RED 变成 collection error，零个用例被发现，什么都没证明；
   (b) provenance 规则——`tests/crsp_fixtures.py` 里逐字实盘的行 vs 本文件用 `dsf_row` 造的行，
   后者**每一条都必须带 `# SYNTHETIC` 注释**。
   docstring 还要说明这个文件的主题：`.first()` 锚的四条不变量 I-1..I-4（本 task 只落 I-1）。
2. 模块常量：`SYNTHETIC_PERMNO = "10107"`、`SYNTHETIC_SYMBOL = "MSFT"`、
   `SYNTHETIC_AXIS = int(SYNTHETIC_PERMNO)`。
3. **复刻**（不要跨文件 import）`tests/test_crsp_dataset.py:66-216` 的 helper：
   `_synthetic_secinfo` / `_reference_rows` / `_pull` / `_dataset_config` / `_convert` /
   `_panel` / `_build_store` / `_build` / `_at`。理由是既有 CRSP 测试文件之间**互不 import
   helper**，各自持有一份（`tests/test_crsp_tracer.py` 甚至把 `at()` 定义成测试体内的闭包）。
   在文件顶部用一句注释记下这条约定与它的出处行号。
4. 写 `test_the_anchor_is_first_usable_row_and_its_adjclose_equals_its_close(mock_crsp_session, tmp_path)`，
   覆盖 `<behavior>` 的前四条。fixture 就是 `tests/test_crsp_dataset.py:1097-1110` 那三行，
   **行序倒过来**：无价格行搬到 `2020-03-02`（开头），`100.0` 与 `102.0` 顺延到 `03-03` / `03-04`。
   那块合成行上方的 provenance 注释也要跟着改成「SYNTHETIC: the window's FIRST row has no price」。
   **I-1 必须用 `pytest.approx(..., rel=1e-12)`，不得写成精确 `==`** —— 真实 store 上
   `close_A * G_A / G_A == close_A` 只有 514/520 精确成立（RESEARCH §19.4.5）。
5. 跑 `uv run pytest -q tests/test_crsp_first_anchor.py -p no:cacheprovider`，**确认它是红的**
   （当前锚是 `.last()`，锚落在 `2020-03-04`，`adjClose(2020-03-03)` 会是 `102.0/1.02`），
   把真实的 pytest 输出摘进 SUMMARY 作为红证据。**不得伪造 TAP**（`project_pytest_tdd_red_evidence`：
   GSD 的 tdd-red-evidence 只认 node TAP，本项目是 pytest，须投影真实运行结果）。

**第二步：M-A —— 三行算术。**

6. `quantlab/dataset/crsp/__init__.py:615-617`，`group_by("permno").agg(...)` 里的三个
   `pl.col(...).last()` 改成 `pl.col(...).first()`，三行都改（`_close_anchor` / `_G_anchor` /
   `_cumfacshr_anchor`）。**不要动 `:607-612` 的三条谓词**，**不要动 `:570` 的
   `frame.sort(["permno", "timestamp"])`** —— polars 的组内聚合按帧内行序取值，那一行排序是
   `.first()` 语义的唯一依据，没有它 `.first()` 取到的是任意一行。
7. `:596-606` 的注释：把「the PERMNO's **last** row carrying a USABLE LEVEL」改成
   「**first** row」，并把同段里「not simply its last row, and not merely its last non-null one」
   同步改成 first 的说法。CR-01 / CR-02 的整段解释（哨兵是数字 `0.0`、`dlycumfacshr` 为什么
   在同一个谓词里）**一个字不改**。
8. `:706` 的错误消息：`The anchor is a PERMNO's last row inside that window` 里的 `last row`
   改成 `first row`。方法其余部分（`:665-741`）保留不动（D-05）。
9. `:51-53` 模块 docstring 里「the anchor `A` is the PERMNO's LAST row carrying a USABLE LEVEL」
   的 `LAST` 改成 `FIRST`；同段落里说明 CR-01 / CR-02 的句子不改。
   **`:10-31` 的两段（GLOBAL 段与 anchor-MOVES 段）本 task 不动** —— 它们与旁车机械成对，
   属于 plan 02。

**第三步：确认没有越界。**

10. 确认 `quantlab/base/config.py`、`quantlab/base/data.py`、`quantlab/backtest/`、
    `quantlab/factor/`、`quantlab/label/` 在本 task 的 diff 里一行都没有（D-04）。
11. 确认 `:1456-1466` 的 `select(columns)` 未被触碰，转换出来的面板仍是 27 个 `data_vars`。
  </action>
  <verify>
    <automated>find . -name __pycache__ -type d -prune -exec rm -rf {} + ; uv run pytest -q tests/test_crsp_first_anchor.py tests/test_crsp_rebuild.py -p no:cacheprovider</automated>
    <fails_when>non-zero exit, or the summary line reports "no tests ran", or any output line matches "failed" or "error"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '\.last()' quantlab/dataset/crsp/__init__.py` 返回 `0`
    - `grep -n '_close_anchor\|_G_anchor\|_cumfacshr_anchor' quantlab/dataset/crsp/__init__.py` 中 `group_by("permno").agg(...)` 内的三行均为 `.first()`
    - `grep -c 'frame = frame.sort(\["permno", "timestamp"\])' quantlab/dataset/crsp/__init__.py` 返回 `1`
    - `grep -c "PERMNO's first row inside that window\|first row inside that window" quantlab/dataset/crsp/__init__.py` ≥ `1`
    - **D-04 零改动门**（范围必须钉在 phase 基线：一个无范围的工作树比较对已提交的改动会输出空，从而假绿）：`PHASE_BASE="$(git rev-list -n1 --grep='docs(03.12): create phase plan' HEAD)^"; git diff --name-only "$PHASE_BASE" -- quantlab/base/ quantlab/backtest/ quantlab/factor/ quantlab/label/` 输出为空
    - `uv run python -c "import ast,sys; src=open('quantlab/dataset/crsp/__init__.py').read(); assert src.count('.first()')>=3"` 退出码 `0`
    - `uv run pytest -q tests/test_crsp_first_anchor.py -k anchor_is_first -p no:cacheprovider` 输出的 summary 行含 `1 passed`
    - SUMMARY 里逐字抄录了第 5 步那次 RED 运行的真实 pytest 输出（含失败断言的实际值与期望值）
  </acceptance_criteria>
  <done>`.first()` 锚端到端跑通：合成 raw → acquisition → `_derivation()` → `_finalise` → zarr → 面板，锚行的 `adjClose` 与 `adjVolume` 等于它自己的 `close` / `volume`（`rel=1e-12`），且 `_assert_anchor_usable` 未被触发。</done>
  <reversibility rating="reversible">三行算术 + 三处文字。`git revert` 即可回到 `.last()`；盘上 store 由 plan 04 统一重建，本 task 不碰任何真实数据。</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: I-3 与 I-4 —— 证明换锚只是每标的一个常数重标度，收益序列一位未动</name>
  <files>tests/test_crsp_first_anchor.py</files>
  <read_first>
    - `tests/test_crsp_first_anchor.py`（Task 1 刚建的文件：helper、常量、docstring 约定）
    - `tests/test_crsp_dataset.py:970-1036` —— `ANCHOR_START` / `ANCHOR_END` / `ANCHOR_SPLIT_DAY` / `ANCHOR_DIVIDEND_DAY` / `ANCHOR_DIVIDEND` 五个常量与 `_anchor_series_rows()` 生成器全文（跨 2019/2020 两年、含 `2020-08-31` 的 4:1 拆股与 `2020-08-07` 的分红）
    - `tests/test_crsp_tracer.py:193-225` —— 比值断言的既定形状：一律带显式 `rel=1e-9`
    - `quantlab/dataset/crsp/__init__.py:571-640` —— 复刻 `.last()` 锚重算所需的完整算术：`close` 的哨兵派生、`_G = cum_prod(1 + dlyret.fill_null(0.0))`、`adjClose = close_A * _G / _G_anchor`、`adjVolume = volume * dlycumfacshr / cumfacshr_A`
    - `03.12-RESEARCH.md` §19.2.1 / §19.2.2（每标的常数比值的实测）、§19.4.5 的 I-3 / I-4 行与实测容差
    - `03.12-PATTERNS.md` §1.5（`_anchor_series_rows()` 可复用）与 §1.6（容差约定）
  </read_first>
  <behavior>
    - **I-3**：用 `_anchor_series_rows()` 建一个 store；只读它自己的原始列（`close` / `ret` / `cumfacshr`）用 numpy 复刻旧 `.last()` 锚重算一条 `adjClose^last` 序列；`adjClose^first_t / adjClose^last_t` 在所有非 NaN 的 t 上**恒定**（该列的 max/min 比值之差在 `rel=1e-9` 内）
    - **I-3 自检**：重算出来的 `.last()` 锚序列，其最后一个可用行上 `adjClose^last == close`（`rel=1e-12`）—— 证明这条对照序列确实是按旧锚算的，而不是抄了 store 自己的值
    - **I-4**：`adjClose^first_t / adjClose^first_{t-1}` 与 `adjClose^last_t / adjClose^last_{t-1}` 在每一对相邻有价日上相同（`rel=1e-9`；真实 store 上实测 max rel `4.900e-16`）
    - 两条用例的比较格数 > 200（序列跨 11 个月的工作日），断言一条非空性检查，防止在一个空掩码上「全部通过」
  </behavior>
  <action>
1. 在 `tests/test_crsp_first_anchor.py` 里**复刻** `tests/test_crsp_dataset.py:972-1036` 的五个
   `ANCHOR_*` 常量与 `_anchor_series_rows()` 生成器（连同它的 `SYNTHETIC` docstring）。
   同样不跨文件 import；在复刻块上方写一行注释注明出处 `tests/test_crsp_dataset.py:972-1036`
   与复刻而非 import 的理由。
2. 新增一个文件私有 helper `_adjclose_anchored_last(panel, symbol)`：从**面板自己的原始列**
   （`close` / `ret` / `cumfacshr`）用 numpy 复刻 `quantlab/dataset/crsp/__init__.py:571-640`
   的算术，但归约取**最后**一个满足三条谓词的行。三条谓词必须**同行**取出
   （一个 `idx` 同时索引 `close` / `_G` / `cumfacshr` 三个数组），并且 `close` 直接用面板里
   已经哨兵守卫过的那一列 —— 面板里的 `close` 已经是 `_derivation()` 守过的（`:571-594`），
   所以读侧直接用它是安全的；**但不得用 `dlyprc > 0` 这种绕过哨兵的写法**（D-05 / R-02）。
   `_G` 用 `np.cumprod(1.0 + np.nan_to_num(ret, nan=0.0))`，**不要**用 dask 或手写分块归约
   （分块归约改变结合顺序会破坏浮点可比性，RESEARCH `## Don't Hand-Roll`）。
3. 写 `test_switching_the_anchor_is_a_constant_rescale_per_permno(mock_crsp_session, tmp_path)`：
   建 store → 取面板 → 对每个 symbol 算 `ratio = adjClose_store / _adjclose_anchored_last(...)`
   → 断言 `np.nanmax(ratio) == pytest.approx(np.nanmin(ratio), rel=1e-9)`，并断言
   `np.isfinite(ratio).sum() > 200`。用例 docstring 写明它抓什么：任何一处接缝（一列两锚）
   都会让这个比值在 t 上变量，这正是 I-3 的作用。
4. 同一个用例里加 I-3 自检：`_adjclose_anchored_last` 的输出在它自己最后一个可用行上等于
   该行的 `close`（`rel=1e-12`）。
5. 写 `test_the_day_over_day_ratios_unchanged_by_the_anchor_switch(mock_crsp_session, tmp_path)`：
   两条序列各自算 `x[1:] / x[:-1]`，在两边都有限的位置上逐格比较，断言
   `max relative difference <= 1e-9`，并断言比较格数 > 200。
6. 两条用例的所有数值断言**一律显式写 `rel=`**（`tests/test_crsp_tracer.py:204-215` 的既定体例）。
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_crsp_first_anchor.py -p no:cacheprovider</automated>
    <fails_when>non-zero exit, or the summary line reports "no tests ran", or the summary line does not report at least "3 passed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -q tests/test_crsp_first_anchor.py -k constant_rescale -p no:cacheprovider` 输出的 summary 行含 `1 passed`
    - `uv run pytest -q tests/test_crsp_first_anchor.py -k ratios_unchanged -p no:cacheprovider` 输出的 summary 行含 `1 passed`
    - `grep -c 'rel=1e-9' tests/test_crsp_first_anchor.py` ≥ `2`
    - `grep -c 'rel=1e-12' tests/test_crsp_first_anchor.py` ≥ `2`
    - `grep -c 'dlyprc > 0' tests/test_crsp_first_anchor.py` 返回 `0`
    - `grep -c 'from tests.test_crsp_dataset import' tests/test_crsp_first_anchor.py` 返回 `0`（不跨文件 import helper）
    - `grep -cE '^(import|from) quantlab' tests/test_crsp_first_anchor.py` 返回 `0`（所有 quantlab import 在函数体内）
    - `grep -c 'SYNTHETIC' tests/test_crsp_first_anchor.py` ≥ `2`
  </acceptance_criteria>
  <done>I-3 与 I-4 各有一条绿色用例，且都在 > 200 格上比较；对照序列的自检证明它确实按旧 `.last()` 锚算出。</done>
  <reversibility rating="reversible">纯新增测试。</reversibility>
</task>

<task type="auto">
  <name>Task 3: 重新固化 6 条硬编码期望值，并把「锚是最后一行」那条用例改写成它现在真正测的东西</name>
  <files>tests/test_crsp_tracer.py, tests/test_crsp_identity.py, tests/test_crsp_dataset.py</files>
  <read_first>
    - `tests/test_crsp_tracer.py:193-225` —— `at()` 断言块全体。要改的是 `:200`（`adjClose` 水平）、`:218` 与 `:219`（两个 `adjVolume` 水平）；同块里 `:204-215` 的四条**比值**断言一个字不改
    - `tests/test_crsp_identity.py:1055-1070` —— QQQ 基准 store 的断言块。要改的是 `:1066`（`adjClose`）与 `:1067-1069`（`adjVolume`）
    - `tests/test_crsp_dataset.py:455-470` —— Lehman 退市块。要改的是 `:461`；同块 `:463-468` 的两条比值断言一个字不改
    - `tests/test_crsp_dataset.py:1090-1131` —— 要改写的用例全体（名字、docstring、fixture 行序）
    - `03.12-VALIDATION.md` 的 `## 已知会红且必须更新期望值的 6 条` 表（六个新值逐字给出，它们是从 fixture 精确算出的，不是估计）
    - `03.12-RESEARCH.md` §19.4.1（同一张表的出处与算法）、§19.4.2（**不用改**的断言清单，用来确认没有改过头）
  </read_first>
  <action>
按 VALIDATION.md 的表逐条替换，**只换数字/容差，断言形状不动**：

1. `tests/test_crsp_tracer.py:200`：`at("adjClose", "2020-08-31")` 的期望 `129.04` →
   `459.6241256733458`。⚠️ 紧挨着的 `:199` 断言的是 `at("close", "2020-08-31") == approx(129.04)`
   —— **那一行是原始 close，不改**。同时把该块上方那句「The split day is the anchor: its
   adjusted close IS its close」改写：拆股日不再是锚了，锚是该 PERMNO 的首个可用行。
2. `tests/test_crsp_tracer.py:218`：`at("adjVolume", "2020-08-28")` 的期望
   `4 * 1_000_000` → `1_000_000`。
3. `tests/test_crsp_tracer.py:219`：`at("adjVolume", "2020-08-31")` 的期望
   `1_000_000` → `250_000`。
   ⚠️ `:217` 的注释「Volume rides `dlycumfacshr`, which is 4 before the split and 1 after」
   仍然正确（因子本身没变），但两个**水平**现在是相对首行的 `cumfacshr_A = 4.0` 归一化的，
   注释要补一句说明这一点。
4. `tests/test_crsp_identity.py:1066`：`_at(panel, "adjClose", "2025-12-31", qqq)` 的期望
   `614.31` → `100.34793698111775`。⚠️ `:1065` 的 `close` 断言是 `614.31`，**不改**。
5. `tests/test_crsp_identity.py:1067-1069`：`_at(panel, "adjVolume", "1999-03-10", qqq)` 的期望
   `2616100 * 2.0` → `2616100 * 1.0`（写成 `2616100 * 1.0` 而不是 `2616100`，保留「它是一个
   归一化后的因子乘出来的量」这个形状）。
6. `tests/test_crsp_dataset.py:461`：保持期望值 `0.052`，**加上 `rel=1e-4`**，写成
   `pytest.approx(0.052, rel=1e-4)`。并把 `:460` 那句「The delisting row is the last row with
   a price, so it IS the anchor」改写成：这一行**不再是**锚（锚是该 PERMNO 的首个可用行），
   所以它的 `adjClose` 只是**近似**等于它自己的 `close`，实测相对差 `5.355e-06`——
   正好超出 `pytest.approx` 默认的 `rel=1e-6`，所以容差必须写出来。
   **这条比直接写新值 `0.051999721543107405` 更有信息**，因为它把「这一行不再是锚」说了出来。
   ⚠️ `:463-468` 的两条比值断言不改（它们是比值，两锚下最大相对差 `4.900e-16`）。

7. 改写 `tests/test_crsp_dataset.py:1090-1131`：
   - 用例改名为 `test_the_anchor_is_the_FIRST_usable_row`；
   - docstring 重写：锚是该 PERMNO 第一个同时带着严格为正的 `close` 与非空 `dlycumfacshr`
     的行，**不是**简单的第一行；一个首行是 Missing-Price 日的 PERMNO 必须把锚往后推一行，
     否则整段历史锚在 null 上全成 NaN。无价格那天自己仍然是 NaN（那天没有价格，
     把锚的水平发布在那里等于凭空造一个价）；
   - fixture 行序倒过来：无价格行从 `2020-03-04`（末尾）搬到 `2020-03-01`（开头），
     `100.0`（ret 0.010000）与 `102.0`（ret 0.020000）顺延到 `03-02` / `03-03`，
     并把 `rows` 上方的 provenance 注释改成「SYNTHETIC: the window's FIRST row has no price」；
   - 三条断言相应调整：`adjClose(2020-03-02) == approx(100.0)`（锚行 = 它自己的 close）、
     `isnan(adjClose(2020-03-01))`、`adjClose(2020-03-03) == approx(102.0)`。
   ⚠️ 改这条用例的**理由必须写进 docstring**：原用例的三条断言在 `.first()` 下仍然全绿，
   但它的名字与 docstring 会变成谎话 —— 「测试通过但不再测它声称测的东西」。

8. **不要改** RESEARCH §19.4.2 列出的那批断言（`tests/test_crsp_dataset.py:416-419`、
   `:463-468`、`:1049-1087`、`tests/test_crsp_tracer.py:204-215`、
   `tests/test_crsp_identity.py:869-874` / `:790-793` 等）。它们是比值或有限性断言，锚无关。
   改动结束后用 `git diff --stat "$(git rev-list -n1 --grep='docs(03.12): create phase plan' HEAD)^" -- tests/` 核对本 task 只动了这三个测试文件（外加 Task 1/2 的新文件），且行数在个位数到二十行量级。
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_crsp_dataset.py tests/test_crsp_tracer.py tests/test_crsp_identity.py tests/test_crsp_first_anchor.py -p no:cacheprovider</automated>
    <fails_when>non-zero exit, or the summary line reports "no tests ran", or any output line matches "failed" or "error"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '459.6241256733458' tests/test_crsp_tracer.py` 返回 `1`
    - `grep -c '250_000\|250000' tests/test_crsp_tracer.py` ≥ `1`
    - `grep -c '100.34793698111775' tests/test_crsp_identity.py` 返回 `1`
    - `grep -c '2616100 \* 2.0' tests/test_crsp_identity.py` 返回 `0`
    - `grep -c 'rel=1e-4' tests/test_crsp_dataset.py` ≥ `1`
    - `grep -c 'test_the_anchor_is_the_last_non_null_close' tests/test_crsp_dataset.py` 返回 `0`
    - `grep -c 'def test_the_anchor_is_the_FIRST_usable_row' tests/test_crsp_dataset.py` 返回 `1`
    - 本 plan 结束时**先把改动清单落进变量**（退出码必须在这一步就被捕获，不能等到管道末段）：`PHASE_BASE="$(git rev-list -n1 --grep='docs(03.12): create phase plan' HEAD)^"; CHANGED="$(git diff --name-only "$PHASE_BASE" -- .)"`
    - 随后 `printf '%s\n' "$CHANGED" | sort` 的输出恰好是本 plan `files_modified` 的五个路径
    - `uv run pytest -q tests/test_crsp_dataset.py tests/test_crsp_tracer.py -p no:cacheprovider` 的 summary 行不含 `failed`
  </acceptance_criteria>
  <done>6 条水平断言按 fixture 精确算出的新值重新固化，比值断言一条未动；锚点用例的名字、docstring 与 fixture 都与它现在真正测的行为一致。</done>
  <reversibility rating="reversible">纯测试期望值更新。</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw tier（`data/downloads/us_equity/1d/wrds_crsp/wrds/**.pqt`）→ `_derivation()` | vendor 落盘事实进入派生算术。本 plan 唯一真正的边界 |
| `_derivation()` → zarr store（`data/data/us_equity/1d/*.zarr`） | 派生结果变成**持久**的历史价格；一次静默的错误会长期留在盘上 |
| pytest fixture（`tests/crsp_fixtures.py` 的 `FakeCrspSession`）→ 测试内的转换 | 进程内假会话，不出网；`rebuild.py:11-14` 保证重建路径离线 |

**不存在的边界，如实记录**：本 plan 不引入网络调用、不解析任何外部输入、不读写凭证、
不新增反序列化面、不新增用户输入路径。WRDS 凭证只在 acquisition 层，本 plan 一行不碰。

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.12-01 | Tampering | `quantlab/dataset/crsp/__init__.py:570` 的 `frame.sort(["permno","timestamp"])` | high | mitigate | 这一行是 `.first()` 的**唯一**语义依据；删掉它，polars 的组内聚合会取到任意一行，锚静默变错而所有测试仍可能绿。缓解：`must_haves.key_links` 与 prohibitions 双写，acceptance 用 `grep -c` 钉住它存在，并由 I-1 用例（锚必须落在第二行）在行为上覆盖 |
| T-03.12-02 | Tampering | `:607-612` 三条同行谓词 + `:615-617` 的一次 `group_by().agg()` | high | mitigate | 拆成「先按 close 选行、再回读 cumfacshr」就是 CR-02 本体（整段退市历史 `adjVolume` 变 NaN 而 `volume` 满格）。缓解：D-05 明令保留，本 plan 只改三个归约方向词；既有的 `test_a_permno_whose_priced_rows_lack_cumfacshr_is_refused_by_name`（`tests/test_crsp_dataset.py:1183`）与 `test_a_permno_with_only_sentinel_prices_is_refused_by_name`（`:1133`）是回归门 |
| T-03.12-03 | Tampering | I-1 断言的容差写法 | medium | mitigate | 若把 I-1 写成精确 `==`，真实 store 上 520 个标的里 6 个会红（`close_A * G_A / G_A` 的末位），团队会倾向于「放宽到 `approx()` 默认」从而同时掩盖一个真实的 5e-06 级偏移。缓解：容差被固定为 `rel=1e-12` 并写进 prohibitions 与 acceptance |
| T-03.12-04 | Information Disclosure | 全部 | low | accept | 本 plan 无网络边界、无凭证读写、无日志输出新增。测试用的是进程内 `FakeCrspSession`，不出网 |
| T-03.12-SC | Tampering | npm/pip/cargo installs | low | accept | **本 plan 无任何包安装任务**。RESEARCH 的 `## Package Legitimacy Audit` 逐字记录「本 phase 不安装任何外部包」，审计表为空，`[SLOP]` / `[SUS]` 均为 none。因此不注入 `checkpoint:human-verify` 包合法性门 |
</threat_model>

<verification>
1. `find . -name __pycache__ -type d -prune -exec rm -rf {} +`（R-09：stale `.pyc` 会让已删/已改的定义继续生效）
2. `uv run pytest -q tests/test_crsp_first_anchor.py tests/test_crsp_dataset.py tests/test_crsp_tracer.py tests/test_crsp_identity.py -p no:cacheprovider` 全绿
3. `uv run pytest -q tests/test_backtest_run.py tests/test_backtest_run_cv.py tests/test_factor_kunquant.py tests/test_universe_filtered_factor.py -p no:cacheprovider` 全绿（D-04 的护栏：因子 / 回测一行不改也不回归）
4. `uv run python -m compileall -q quantlab` 退出码 0
5. `PHASE_BASE="$(git rev-list -n1 --grep='docs(03.12): create phase plan' HEAD)^"; git diff --name-only "$PHASE_BASE" -- quantlab/` 只输出 `quantlab/dataset/crsp/__init__.py` 一行

⚠️ **本 plan 结束时 `_assert_anchor_unchanged` 仍然在位**，所以「延长 `end_date` 再 append」
在本 wave 仍会被拒绝，这是预期状态；I-2 属于 plan 02。
</verification>

<success_criteria>
- [ ] `quantlab/dataset/crsp/__init__.py` 中 `.last()` 的出现次数为 0，`group_by("permno").agg()` 三行全部是 `.first()`
- [ ] `:570` 的 sort 仍在；`:607-612` 三条谓词、`:571-594` 哨兵派生、`:1456-1466` `select(columns)` 逐字未变
- [ ] `tests/test_crsp_first_anchor.py` 存在，三条用例（I-1 / I-3 / I-4）全绿
- [ ] 6 条期望值按 VALIDATION.md 的表重新固化，比值断言一条未动
- [ ] `test_the_anchor_is_the_FIRST_usable_row` 存在且 fixture 的无价格行在**开头**
- [ ] `quantlab/base/config.py` / `quantlab/backtest/` / `quantlab/factor/` 零改动
- [ ] RED 证据是真实 pytest 输出，不是伪造的 TAP
</success_criteria>

<output>
Create `.planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-01-SUMMARY.md` when done.
SUMMARY 必须包含：Task 1 第 5 步的真实 RED pytest 输出逐字摘录、改动后三条 `.first()` 行的
最终文本、以及四条 verify 命令的实际 summary 行。
</output>
