---
phase: "03.12"
plan: 04
type: execute
wave: 3
depends_on: ["03.12-02", "03.12-03"]
files_modified:
  - tests/test_crsp_rebuild_measurements.py
autonomous: false
user_setup: []

requirements: [DATA-03]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "**I-1（真实数据门）**：重建后的真实 store（`data/data/us_equity/1d/wrds_crsp_sp500_1d.zarr`，252×520）里，每个 PERMNO 第一个可用行上 `adjClose == close`（`rtol=1e-12`）且 `adjVolume == volume`。实测口径：520/520 成立，精确 `==` 只有 514/520 —— **必须 `rtol`，不能 `==`**"
    - "**I-5**：`CrspStoreRebuilder._measure()` 的四个不变量在 `.first()` 下**原值不变** —— `adj_close_le_zero == 0`、`close_eq_zero == 0`、`adj_close_nan == structural_gaps`、`adj_volume_nan == structural_gaps`，本机实测 `0 / 0 / 764 / 764`。CR-01 / CR-02 的回归门原样存活"
    - "重建产物结构不变：`{'timestamp': 252, 'symbol': 520}`、**27 个 `data_vars`**、`symbol` on-disk dtype `int64`、`.crsp_symbology_report.json` 不存在、`.crsp_tickers.json` 覆盖 520 个 PERMNO"
    - "重建**没有**写出 `.crsp_adjustment.json`（`EXPECTED_WRITTEN_SIDECARS` 现在是三元组），但重建**清掉**了旧 store 旁边那一份（`CRSP_SIDECAR_SUFFIXES` 仍是五元组）—— 两张清单答不同问题"
    - "备份代数只增不覆盖：本次重建取一个此前不存在的备份目录名，既有的 `data/_backup_pre_03.11*` 五代一个字节未动"
    - "**D-12**：store 重建之后，因子库与标签库的重算状态被**逐项记录**（产物路径 + mtime，或「本机不存在任何因子库/标签库 zarr」的 `find` 实证），而不是默认它不存在就等于不需要"
    - "**D-11**：`quantlab/base/backtest.py:1216 _compare_fingerprints` 一行未改，仍然只发 warning；本机既有 backtest run 数以 `find` 实证记录（研究实测为 0），远端数量列为 UAT 步骤而**不作任何断言**"
  artifacts:
    - tests/test_crsp_rebuild_measurements.py
  key_links:
    - "`CrspStoreRebuilder.rebuild()`（`quantlab/dataset/crsp/rebuild.py:64-173` → `quantlab/base/rebuild.py:232`）的四步 assert_inputs_present → backup → clear → _convert，其中 `clear` 用的就是 `CRSP_SIDECAR_SUFFIXES`"
    - "`tests/test_crsp_rebuild_measurements.py` 的 module-scoped `rebuilt` fixture（`:336-388`）→ 全文件共用**一次**重建。新用例必须挂上去，绝不自己再 `rebuild()` 一次"
    - "`quantlab/base/backtest.py:1143-1160` 的 `factor_store[i]` 指纹 → 因子库**自己**的变量 → D-12：只重建 dataset store 会让 digest 保持不变而底层已变"
  prohibitions:
    - "不得在新用例里自己调用 `rebuilder.rebuild()` —— 必须复用 module-scoped 的 `rebuilt` fixture（重建是破坏性的，跑两次不会多出任何证据）"
    - "不得给 `tests/test_crsp_rebuild_measurements.py` 引入 `pytest.mark.skipif` 或任何条件跳过 —— 该文件的模块 docstring 逐字要求这个 marker 的 `grep -c` 为 0：被跳过的数据门在每一个操作者看到的 summary 行里都读作「通过」"
    - "不得把真实数据根写成绝对路径 —— 走 `QUANTLAB_DATA_ROOT=\"$(dirname \"$(git rev-parse --path-format=absolute --git-common-dir)\")\"`（`project_gsd_worktree_verify_path`：计划里写死主仓库绝对路径会让隔离执行测到未改动代码而报假绿）"
    - "不得把 I-1 的锚行等式写成精确 `==`（真实 store 上只有 514/520 精确成立）"
    - "不得改动 `quantlab/base/backtest.py`（D-11）；不得改动 `quantlab/dataset/crsp/rebuild.py:_measure()`（M6 已作废，19.4.4 实测它照常工作）"
    - "不得覆盖任何既有的 `data/_backup_*` 目录"
    - "不得对远端训练服务器（`zhrdai@frp-one.com:~/projects/quantlab2`）的 backtest run 数量做任何断言 —— 研究会话的 ssh 未连通（`Host key verification failed`），本 phase 无观测"
---

<objective>
在真实 CRSP 数据上重建 store，并把 I-1 与 I-5 变成一个会**失败而不是跳过**的真实数据门。

这是本 phase 唯一的 **one-way** 动作：`BaseStoreRebuilder.rebuild()` 会 backup → **clear** →
convert，盘上的 `data/data/us_equity/1d/wrds_crsp_sp500_1d.zarr` 会被删掉重写，
既有的 backtest run 指纹全部失效。因此它前面有一个 `checkpoint:decision`。

重建之后 **D-12** 的义务立刻生效：只重建 dataset store 而不重算因子库/标签库，
`factor_store[i]` 的 digest 会**保持不变而底层数据已变** —— 一个看起来没问题的旧因子库。

Purpose: 让换锚这件事在真实数据上被证明，而不是只在合成 fixture 上。
Output: 一条真实数据门用例 + 一次真实重建的完整测量记录 + D-12 / D-11 的证据。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-CONTEXT.md
@.planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-RESEARCH.md
@.planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-PATTERNS.md
@.planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-VALIDATION.md
@.planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-02-SUMMARY.md
@.planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-03-SUMMARY.md
</context>

## 读 CONTEXT.md 与 RESEARCH.md 的必读前提

- **CONTEXT.md 的 D-01 / D-02 / D-03 / D-06 已作废。** 权威是 `## ⚠ 2026-09-22 决策修订`
  一节与 **D-10 / D-11 / D-12**。**D-04 / D-05 / D-07 / D-08 / D-09 仍然有效。**
- **RESEARCH.md 的 §2.4 / §5 / §6.2 / §6.3 / §6.4 / §11 已失效。** 本 plan 的依据是
  **§19.3**（指纹失效范围）、**§19.4.4**（`_measure()` 不必改）、**§19.4.5**（I-1 / I-5）、
  **§19.6**（重建执行路径与本机实测）。
- ✅ **不需要「重建前固化 golden 快照」** —— §6.4 那条任务顺序约束是为已作废的 D-03 服务的。
  I-1 / I-5 都是 store 的**自洽**性质，重建后随时可验；而 `data/_backup_pre_03.11*` 下的
  备份 store **就是现成的对照物**（I-3 / I-4 若要在真实数据上复核，直接与备份比）。

## Probe notes（显式记录，不是静默丢弃）

- **Spec-less probe fallback：可见跳过。** 本 phase 无 `*-SPEC.md`，ROADMAP 的 `Requirements`
  是 `TBD`，`phase_req_ids` 为空 ⇒ 无可探测需求文本，本 plan 不含探针派生的 `must_haves` 谓词、
  不含 Edge Coverage / Prohibitions 探针条目。验收契约是 I-1..I-5。
- **API Coverage 探针 `detected: false`** ⇒ 不产出 `COVERAGE.md`。
- **Assumption-Delta 探针 `detected: false`**；**Schema Push Gate** 不适用（无 ORM schema）。
- **Package Legitimacy Gate 不适用** —— 本 plan 无任何包安装任务。

## Artifacts this phase produces（全 phase 清单）

| 符号 | 位置 | 类型 | 产出 plan |
|---|---|---|---|
| `tests/test_crsp_first_anchor.py` | 仓库根 `tests/` | 新文件 | 01 |
| `test_the_anchor_is_first_usable_row_and_its_adjclose_equals_its_close` | 同上 | 用例（I-1） | 01 |
| `test_switching_the_anchor_is_a_constant_rescale_per_permno` | 同上 | 用例（I-3） | 01 |
| `test_the_day_over_day_ratios_unchanged_by_the_anchor_switch` | 同上 | 用例（I-4） | 01 |
| `test_the_anchor_is_the_FIRST_usable_row` | `tests/test_crsp_dataset.py` | 用例（改名） | 01 |
| `test_a_full_and_incremental_build_agree_bit_for_bit` | `tests/test_crsp_first_anchor.py` | 用例（I-2） | 02 |
| `tests/test_no_anchor_machinery_residue.py` | 仓库根 `tests/` | 新文件 | 02 |
| `PACKAGE_ROOT` / `DELETED_IN_03_12_02` / `_sources` / `_hits` | 同上 | 模块常量 / helper | 02 |
| `test_the_deleted_window_anchor_machinery_leaves_no_residue_in_quantlab` | 同上 | 用例 | 02 |
| **`test_the_real_store_reproduces_its_adjusted_columns`** | **`tests/test_crsp_rebuild_measurements.py`** | **用例（I-1 + I-5 真实数据门）** | **04** |

**本 phase 不新增任何生产侧符号。** 本 plan 的代码改动只有一条新用例；其余产出是
**盘上的数据**（重建后的 store 与旁车、一代新的备份目录）与**证据**（测量输出、`find` 清点）。

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: 决策门 —— 用新锚重建真实 CRSP store（one-way）</name>
  <read_first>
    - `.planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-02-SUMMARY.md`（删除是否干净、零残留 gate 是否绿）
    - `.planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-01-SUMMARY.md`（I-1 / I-3 / I-4 是否绿）
    - `quantlab/base/rebuild.py:149-274` —— `assert_inputs_present` / `backup` / `clear` / `rebuild` 四步，特别是 `clear` 删什么
    - `tests/test_crsp_rebuild_measurements.py:239-279` —— `_fresh_backup_dir()`：备份代数如何取名、为什么每一代都是只读的
    - `03.12-RESEARCH.md` §19.6.2（本机上一次真实重建的完整实测：6 passed / 6.03 s、自动新开备份目录 `data/_backup_pre_03.11_18_rerun3`、产物 9.9 MB / 252×520 / 27 vars）
    - `03.12-RESEARCH.md` §19.3.1 / §19.3.2（哪些指纹键会变、本机既有 run 数为 0、远端无观测）
  </read_first>
  <decision>
**要不要现在用 `.first()` 锚重建真实 CRSP store？**

这是本 phase 唯一不可逆的动作。

**它会做什么（实测，不是估计）：**
- `CrspStoreRebuilder.rebuild()` 先 **backup**（自动开一个此前不存在的代，例如上次是
  `data/_backup_pre_03.11_18_rerun3`），再 **clear** 掉
  `data/data/us_equity/1d/wrds_crsp_sp500_1d.zarr` 与它的**五个**旁车后缀，再重新 convert。
- 产物规模不变：**9.9 MB / `{'timestamp': 252, 'symbol': 520}` / 27 `data_vars`**，
  耗时量级 **~6 s**（整个真实数据门 6 个用例含一次完整重建实测 6.03 s）。
- `_measure()` 的四个不变量**不变**：`0 / 0 / 764 / 764`。

**它会打破什么：**
- **所有带值的指纹键全部改变**：`price_dataset`、KunQuant `factor[i]`、Polars `factor[i]`、
  `factor_store[i]` / `train_factor_store[i]` / `train_label_store[i]`、`train_factor[i]` /
  `train_label[i]`。只有轴那一行（`n_timestamps` / `n_symbols` / `start` / `end`）不变。
- ⇒ **既有 backtest run 全部不可重放**。按 `project_no_backward_compat`，直接失效、不写兼容层。
- 数值方向：`adjClose` 中位数变化 **1.74%**，p99 **5.0×**，max **49×**（每 PERMNO 一个常数）。

**为什么不可逆：** `clear` 之后旧 store 只存在于备份目录里；备份代数只增不覆盖，
所以数据本身不会丢，但**重建后的 store 与所有下游产物（因子库、标签库、run 指纹）
要一起回滚才算真回滚**。本 task 之前是本 phase 最后一个低成本回滚点。

**替代方案（如果现在不做）：** 代码改动可以先合并，重建推迟到操作者选定的时间窗口。
代价是仓库里的代码与盘上的 store 不一致 —— store 里的 `adj*` 是 `.last()` 锚算的，
而代码会按 `.first()` 算，`test_crsp_rebuild_measurements.py` 会在下一次运行时重建它。
  </decision>
  <context>
本机既有 backtest run 实测 **0 个**（`find . -name "fingerprint.json"` 无输出）；远端
`zhrdai@frp-one.com:~/projects/quantlab2` **无观测**（研究会话 ssh 返回
`Host key verification failed`，连接未建立）。所以「谁会被这次重建打断」这个问题，
本机答案是「没有人」，远端答案是「不知道」。
重建本身离线（`quantlab/dataset/crsp/rebuild.py:11-14`），不需要 WRDS 凭证与网络。
  </context>
  <options>
    <option id="option-a">
      <name>A：现在重建（推荐）</name>
      <pros>本机 0 个 run 被打断；备份代数只增不覆盖，旧 store 完整留底；重建后 I-1 / I-5 立刻可验，且 `data/_backup_pre_03.11*` 就是 I-3 / I-4 在真实数据上的现成对照物；代码与盘上数据不一致的窗口最短</pros>
      <cons>不可逆：盘上 store 被 clear 重写，所有带值的指纹键改变，既有 backtest run 全部不可重放</cons>
    </option>
    <option id="option-b">
      <name>B：只合并代码，重建推迟</name>
      <pros>保留一个低成本回滚点；可以把重建挪到操作者选定的时间窗口</pros>
      <cons>仓库里的代码按 `.first()` 算，盘上的 store 里的 `adj*` 还是 `.last()` 锚算的 —— 两者不一致，且下一次有人跑 `tests/test_crsp_rebuild_measurements.py` 就会在无人预期的时刻触发重建</cons>
    </option>
    <option id="option-c">
      <name>C：先在远端重建</name>
      <pros>先处理真实产物规模更大的那一侧</pros>
      <cons>研究会话的 ssh 未连通，连通性本身要先解决；本机与远端不一致的窗口反而更长</cons>
    </option>
  </options>
  <resume-signal>回复 `A`、`B` 或 `C`（或写出等价的明确选择，例如「现在重建」/「先合并代码」/「先远端」）。不要用「好」「继续」这类不指向具体选项的回复 —— 这是一个不可逆动作，需要一个能被逐字记进 SUMMARY 的选择。</resume-signal>
  <recommendation>
**A。** 三条理由：(1) 本机既有 backtest run 数实测为 **0**，重建在本机不打断任何人；
(2) 备份代数只增不覆盖，旧 store 完整留底；(3) 重建后 I-1 / I-5 立刻可验，且
`data/_backup_pre_03.11*` 就是 I-3 / I-4 在真实数据上的现成对照物 —— 越晚重建，
代码与盘上数据不一致的窗口越长。
  </recommendation>
  <action>
把上面的 `<decision>` / `<options>` / `<recommendation>` 原样呈现给操作者，**等待明确选择**。

⚠️ `workflow.auto_advance` 为 `false`，且本 checkpoint 的动作不可逆 —— **不得自动推进**。

记录操作者的选择与理由到 SUMMARY。若选 B 或 C，Task 2 只做「加用例 + 跑合成回归」那一半，
**不执行**真实数据门；Task 3 相应缩减为「记录 store 与代码不一致」+「把重建列为 UAT 首项」。
  </action>
  <verify>
    <human-check>操作者在 A / B / C 中做出了明确选择，选择与理由已写进 SUMMARY。</human-check>
  </verify>
  <acceptance_criteria>
    - SUMMARY 里有一行逐字记录操作者选择的选项字母与一句理由
    - 若选 A：`data/` 下在重建前已列出既有备份代数清单（`ls -d data/_backup_*`），并确认本次将使用的新代目录名**此前不存在**
    - 若选 B 或 C：SUMMARY 里有一句「盘上 store 仍是 `.last()` 锚，与代码不一致」，且 UAT 首项是重建
  </acceptance_criteria>
  <done>操作者已就这次不可逆的重建做出选择，选择被记录。</done>
  <reversibility rating="one-way">`rebuild()` 会 clear 掉真实 store 并让所有既有 backtest run 的指纹失效。备份代数保留旧数据，但下游产物（因子库、标签库、run）要一起回滚才算真回滚。</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 真实数据门 —— `test_the_real_store_reproduces_its_adjusted_columns`（I-1 + I-5），挂既有 `rebuilt` fixture</name>
  <files>tests/test_crsp_rebuild_measurements.py</files>
  <precondition>真实 CRSP raw tier 与 reference tier 在主仓库的 `data/downloads/us_equity/1d/wrds_crsp/`（raw 132 个 `.pqt` / 138,893 行 / 539 PERMNO；`_reference/manifest.json` 的 `product_end = 2025-12-31`）。`data/` 是 gitignored，隔离 worktree 里**不存在**，所以这条 verify 必须在主仓库工作树里跑，数据根由 `git rev-parse --path-format=absolute --git-common-dir` 推导。</precondition>
  <read_first>
    - `tests/test_crsp_rebuild_measurements.py:1-80` —— 模块 docstring（含「`grep -c` 条件跳过 marker 必须为 0」这条逐字规则）与**模块级** import 区（本文件不是 TDD-red 文件，新增用例沿用它的模块级 import 风格，**不要**套用 `tests/test_crsp_dataset.py` 的函数体内 import 约定）
    - `tests/test_crsp_rebuild_measurements.py:100-130` —— `SYMBOL_COUNT_BAND` 与 `EXPECTED_WRITTEN_SIDECARS`（plan 02 已把它改成三元组）
    - `tests/test_crsp_rebuild_measurements.py:179-224` —— `_data_root()`：为什么是 `pytest.fail` 而不是 `pytest.skip`（「被跳过的数据门在任何 summary 行里都读作通过，所以它比没有门更坏」），以及它给出的那条逐字运行命令
    - `tests/test_crsp_rebuild_measurements.py:336-388` —— module-scoped `rebuilt` fixture：它返回 `(measurement, started_at, config)`，**全文件只重建一次**；备份代数只增不覆盖的整段规则
    - `tests/test_crsp_rebuild_measurements.py:429-492` —— `test_sp500_2024_rebuild_matches_post_fix_measurements`：`print(...)` 是本文件的既定证据形式（SUMMARY 逐行抄录）
    - `tests/test_crsp_rebuild_measurements.py:567-590` —— `test_rebuild_refreshed_every_sidecar`：它迭代 `EXPECTED_WRITTEN_SIDECARS`，确认 plan 02 的三元组改动已经生效
    - `quantlab/dataset/crsp/rebuild.py:120-165` —— `_measure()` 返回的七个键名（`anomaly_flag_true` / `adj_close_le_zero` / `close_eq_zero` / `adj_close_nan` / `adj_volume_nan` / `structural_gaps` / `symbol_count`）
    - `quantlab/dataset/crsp/__init__.py:596-620` —— 选锚三行（新用例要在面板上复刻「第一个满足三条谓词的行」这个定义）
    - `03.12-RESEARCH.md` §19.4.5（I-1 / I-5 的实测口径与容差）、§19.6.2（上一次真实重建的全部实测数字）、§19.6.3（重建后立即可验证的清单）
    - `03.12-PATTERNS.md` §2.1 / §2.2（真实数据根的取法、新用例必须复用 `rebuilt` fixture）
  </read_first>
  <behavior>
    - **I-5**：`measurement.metrics` 满足 `adj_close_le_zero == 0`、`close_eq_zero == 0`、
      `adj_close_nan == structural_gaps`、`adj_volume_nan == structural_gaps`
      （本机实测这四个数是 `0 / 0 / 764 / 764`）
    - **I-1**：`xr.open_zarr(measurement.store_path)` 打开面板后，对**每一个** symbol 找出第一个
      同时满足「`close` 非空且严格为正」与「`cumfacshr` 非空」的时间索引，断言该行的
      `adjClose == pytest.approx(close, rel=1e-12)` 且 `adjVolume == pytest.approx(volume, rel=1e-12)`
    - **I-1 的覆盖率断言**：满足上述条件的 symbol 数 == `measurement.metrics["symbol_count"]`
      （520），即**没有一个标的**因为找不到可用行而被悄悄跳过
    - 用例通过 `print(...)` 输出：`store_path`、`dims`、`metrics` 七个键、I-1 的检查标的数与
      「精确 `==` 成立的标的数」（后者预期 < 520，正是不能用 `==` 的直接证据）
    - 整个文件的用例数比 plan 02 之后多 1，且没有任何一条被 skip
  </behavior>
  <action>
1. 在 `tests/test_crsp_rebuild_measurements.py` 新增
   `test_the_real_store_reproduces_its_adjusted_columns(rebuilt)`。
   **必须挂既有的 module-scoped `rebuilt` fixture**，签名就是 `(rebuilt)` ——
   绝不自己再构造 `CrspStoreRebuilder` 或调用 `rebuild()`。重建是破坏性的，跑两次不会多出
   任何证据，只会多毁一次盘上的 store 并多开一代备份。
2. docstring 写清楚这条用例是什么：`.first()` 锚的两条自洽性质在**真实** 252×520 面板上的门。
   I-1 是「锚行的 `adjClose` 就是它自己的 `close`」——`G_A / G_A = 1`；I-5 是 CR-01 / CR-02
   的四个回归数字**原值不变**，证明换锚没有把那两道防线弄松。
   并写明**为什么是 `rtol` 不是 `==`**：`close_A * G_A / G_A` 在 IEEE 下不保证逐位等于
   `close_A`，真实 store 上精确相等只有 514/520 —— 把 6 个末位差当成失败会让人去放宽到
   `approx()` 的默认 `rel=1e-6`，从而同时盖掉一个真实的 5e-06 级偏移。
3. I-5 部分：从 `measurement.metrics` 直接取四个键断言，**不要**重新打开面板算一遍
   （`_measure()` 是权威口径，重算一份会让两个定义有机会漂移）。
4. I-1 部分：`xr.open_zarr(measurement.store_path)` 打开面板（用 `try/finally` + `close()`，
   照 `quantlab/dataset/crsp/rebuild.py:_measure()` 的写法）。
   对每个 symbol 用 numpy 求第一个满足
   `np.isfinite(close) & (close > 0.0) & np.isfinite(cumfacshr)` 的索引 ——
   **三个条件必须来自同一个索引**（这就是 CR-02 的形状，D-05）。断言该索引上
   `adjClose` 与 `close`、`adjVolume` 与 `volume` 在 `rel=1e-12` 下相等。
   ⚠️ 面板里 NULL 落盘成 NaN，所以谓词用 `np.isfinite(...)` 而不是 `is_not_null()`；
   `close > 0.0` 那一条**不能省**（CRSP 的无价格哨兵是数字 `0.0`，CR-01）。
5. 覆盖率断言：被检查的 symbol 数必须等于 `measurement.metrics["symbol_count"]`。
   若有 symbol 一行都不满足，用例必须报出它的 PERMNO 而不是静默少查一个。
6. `print(...)` 出证据：`store_path`、`dims`、`metrics`、检查标的数、精确 `==` 成立的标的数。
   这是本文件的既定证据形式（`:429-492` 的既有用例就这么做），SUMMARY 逐行抄录。
7. **不要**给这个文件加 `pytest.mark.skipif` 或任何条件跳过（模块 docstring 逐字要求
   该 marker 的 `grep -c` 为 0）。**不要**改 `_data_root()`、`_fresh_backup_dir()`、
   `rebuilt` fixture 或 `EXPECTED_WRITTEN_SIDECARS`（后者已由 plan 02 改完）。
8. 跑真实数据门。⚠️ **命令逐字沿用 03.11 实证过的形态**（数据根由 `--git-common-dir` 推导，
   不写死绝对路径 —— `project_gsd_worktree_verify_path`）。
   ⚠️ 若 Task 1 的决策是 B 或 C，**跳过这一步**，只跑合成回归，并在 SUMMARY 记录跳过原因。
9. 把测量输出逐行抄进 SUMMARY，并与 RESEARCH §19.6.2 的上一次实测逐项对照：
   `dims` 应为 `{'timestamp': 252, 'symbol': 520}`、`data_vars` 27 个、
   `adj_close_le_zero=0` / `close_eq_zero=0` / `adj_close_nan=764` / `adj_volume_nan=764` /
   `structural_gaps=764` / `symbol_count=520`。任何一项不一致都要在 SUMMARY 里具名说明，
   **不要**悄悄接受一个不同的数字。
  </action>
  <verify>
    <automated>find . -name __pycache__ -type d -prune -exec rm -rf {} + ; QUANTLAB_DATA_ROOT="$(dirname "$(git rev-parse --path-format=absolute --git-common-dir)")" uv run pytest -q -s tests/test_crsp_rebuild_measurements.py -p no:cacheprovider</automated>
    <fails_when>non-zero exit, or the summary line reports fewer than "7 passed", or any output line matches "failed" or "error" or "skipped"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def test_the_real_store_reproduces_its_adjusted_columns' tests/test_crsp_rebuild_measurements.py` 返回 `1`
    - `grep -c 'skipif\|pytest.skip' tests/test_crsp_rebuild_measurements.py` 返回 `0`
    - `grep -c 'CrspStoreRebuilder(' tests/test_crsp_rebuild_measurements.py` 返回 `1`（只有 `rebuilt` fixture 里那一处）
    - `grep -c 'rel=1e-12' tests/test_crsp_rebuild_measurements.py` ≥ `2`
    - `grep -c 'isfinite' tests/test_crsp_rebuild_measurements.py` ≥ `1` 且 `grep -c '> 0.0' tests/test_crsp_rebuild_measurements.py` ≥ `1`（三条同行谓词完整）
    - 真实数据门的 stdout 含 `adj_close_le_zero': 0` 与 `close_eq_zero': 0`
    - 真实数据门的 stdout 含 `'timestamp': 252` 与 `'symbol': 520`
    - 真实数据门的 stdout 里 I-1 检查的标的数为 `520`，且「精确 `==` 成立的标的数」被打印出来
    - `ls -d data/_backup_*` 的目录数比 Task 1 记录的清单**多 1**，且既有各代的 `du -sk` 未变
    - `test ! -e data/data/us_equity/1d/wrds_crsp_sp500_1d.zarr.crsp_adjustment.json` 退出码 `0`（重建清掉了它且不再写回）
  </acceptance_criteria>
  <done>真实 252×520 面板上 I-1（520/520，`rtol=1e-12`）与 I-5（`0 / 0 / 764 / 764`）各有一条会失败而不会跳过的门，测量输出已逐行留证。</done>
  <reversibility rating="one-way">本 task 的 verify 命令**本身就执行那次重建**（`rebuilt` fixture 是 module-scoped 且破坏性）。新用例本身可回滚，盘上的 store 不可回滚到「未重建」，只能从备份代恢复。</reversibility>
</task>

<task type="auto">
  <name>Task 3: D-12 因子库 / 标签库的重算状态，与 D-11 指纹 warning 的证据</name>
  <files>tests/test_crsp_rebuild_measurements.py</files>
  <precondition>Task 2 的真实数据门已跑完（或 Task 1 选了 B / C 并已记录跳过原因）。本 task 只读盘与跑回归，不写任何数据产物。</precondition>
  <read_first>
    - `quantlab/base/backtest.py:1135-1237` —— 五组指纹记录点与 `_compare_fingerprints`。
      特别是 `:1143-1160`（`factor_store[i]` / `train_factor_store[i]` / `train_label_store[i]`
      覆盖的是**因子库自己的变量**）与 `:1216-1237`（只发 warning 的理由逐字：数据集会被追加、
      复权会回溯重算，变了的数据仍然可以回测；以及 `:1225-1234` 的 `partial=True` 分支
      是在**失败路径**上比较，raise 会掩盖原始异常）
    - `quantlab/base/backtest.py:1763-1765` —— `_run_dir_name()` = `{class}_{YYYYmmdd_HHMMSS_ffffff}`，run 目录的形状（清点时按这个找）
    - `quantlab/utils/module.py` 的 `load_backtester_from_config` —— 重放一个既有 run 的入口
    - `03.12-CONTEXT.md` 的 **D-11** 与 **D-12** 两节（逐字裁定）
    - `03.12-RESEARCH.md` §19.3.1（四个记录点逐条翻转的表）、§19.3.2（本机 run 数 `find` 实测为 0；远端 ssh 未连通、**无观测**）、§19.3.3（warning vs raise 的完整取舍）
    - `03.12-VALIDATION.md` 的 `## Manual-Only Verifications` 三行
  </read_first>
  <action>
本 task **不改任何生产代码**。它的产出是**证据**，全部进 SUMMARY。

**A. D-12 —— 因子库 / 标签库的重算状态（不是可选项）**

1. 清点本机所有因子库 / 标签库产物。至少跑这两条并把完整输出抄进 SUMMARY：
   - `find data -maxdepth 4 -name '*.zarr' -prune | sort`
   - `ls -la data/data/us_equity/1d/`
   判定规则：凡是由 `adj*` 算出来的面板（KunQuant 因子库、Polars 因子库、标签库）
   **都必须在 store 重建之后重算**。
2. **若存在任何因子库 / 标签库 zarr**：逐个重算，并把每个产物的路径与 `mtime`
   （`stat -f '%m %N'`）记进 SUMMARY，确认 mtime **晚于** Task 2 记录的重建开始时刻。
3. **若一个都不存在**（研究会话实测：本机 `data/data/us_equity/1d/` 下只有
   `stock_alpaca.zarr` / `us_all.zarr` / `wrds_crsp_sp500_1d.zarr` /
   `wrds_crsp_sp500_membership.zarr`，没有因子库）：
   把 `find` 的**完整输出**抄进 SUMMARY 作为实证，并显式写一句
   「本机不存在任何因子库 / 标签库产物，因此本机无需重算」。
   ⚠️ **不得**把「找不到」写成「不需要」而不给出清点命令与输出 ——
   D-12 要抓的正是「digest 保持不变而底层已变」这种看不见的情形，
   而看不见的东西只能用清点来排除。
4. 无论 2 还是 3，SUMMARY 都必须重述一遍 D-12 的理由：
   `factor_store[i]` 指纹覆盖的是因子库**自己**的变量，所以只重建 dataset store
   而不重算因子库，那个 digest 会保持不变而底层数据已变 —— 这个键本来就是为抓这种情形设的。
   并把「远端 `zhrdai@frp-one.com:~/projects/quantlab2` 的 store 与因子库需另行重建」
   列为 UAT 的一项。

**B. D-11 —— 指纹 warning 的证据（不改行为）**

5. 确认 `quantlab/base/backtest.py` 在本 phase 的全部 diff 里**一行未改**
   （`PHASE_BASE="$(git rev-list -n1 --grep='docs(03.12): create phase plan' HEAD)^"; git diff --name-only "$PHASE_BASE" -- quantlab/base/backtest.py` 应为空。范围必须钉在 phase 基线：裸 `git diff HEAD` 对一个已提交的改动会输出空，从而假绿）。
6. 清点本机既有 backtest run：`find . -name "fingerprint.json" -not -path "./.git/*"`
   与 `find . -name "metrics.json" -not -path "./.git/*"`。完整输出进 SUMMARY。
   - **若 ≥ 1 个**：用其中一个 run 的 `config.json` 走
     `quantlab/utils/module.py:load_backtester_from_config` 重跑一次，
     把 warning 的**条数与键名**逐字抄进 SUMMARY（这是 D-11 的替代强化：
     「既有 run 全部失效」因此有了证据，而没有改任何行为）。
   - **若 0 个**（研究会话 `find` 实测为 0）：SUMMARY 记录这个 0 与产生它的两条命令，
     并把「登录远端清点受影响的 run 数」列为 UAT 的一项。
7. ⚠️ **不得对远端 run 数做任何断言** —— 研究会话的 ssh 返回
   `Host key verification failed`，连接未建立，本 phase **无观测**。既不说「有」，也不说「没有」。
8. ⚠️ **不得顺手把 `_compare_fingerprints` 改成 raise。** D-11 已裁定保持 warning：
   改 raise 会推翻 `backtest.py:1222-1223` 的既有论证（那是条长期真理），
   并让 `:1225-1234` 的失败路径比较掩盖原始异常。若操作者仍想要 raise，那是独立的 quick task。

**C. 合并后的全树复跑（`project_gsd_worktree_verify_path`）**

9. 在**主仓库工作树**（不是隔离 worktree）里复跑两条：
   - 全量回归：`uv run pytest -q --ignore=tests/test_factor_hierarchy.py --ignore=tests/test_crsp_rebuild_measurements.py -p no:cacheprovider`
   - 真实数据门：Task 2 的那条命令
   两条的 summary 行都抄进 SUMMARY。
   理由：隔离执行时 verify 若解析到未改动的代码会报假绿，而 `data/` 在 worktree 里根本不存在。
10. `D-08 的观察项（不处理，只记录）`：后复权让长期赢家的近期 `adjClose` 高于名义价，
    `alpha084` 的 `Pow(ts_rank(...), delta(close,5))` 指数溢出风险随之加重（实测下溢格数
    ×1.5–1.7）。UAT 中对一次 Alpha101 计算统计 `alpha084` 的非有限格数并与重建前对比，
    记入证据。**本 phase 不修它**（D-08 出范围，已在 backlog）。
  </action>
  <!-- 探针会把下面 verify 里的 `$(git rev-parse --show-toplevel)` 标成
       verify_command_path_resolvability=unresolvable。这是**预期的**，不是疏漏：项目规则
       `project_gsd_worktree_verify_path` 明令 verify 块禁止写死主仓库绝对路径 —— 隔离执行时
       绝对路径会指向未改动的主树，测出假绿。动态解析仓库根是**正确**写法；探针只是无法静态
       求值它。命令保持原样，不要替换成字面路径。 -->
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && PHASE_BASE="$(git rev-list -n1 --grep='docs(03.12): create phase plan' HEAD)^" && test -z "$(git diff --name-only "$PHASE_BASE" -- quantlab/base/backtest.py)" && echo BACKTEST_UNTOUCHED && find data -maxdepth 4 -name '*.zarr' -prune | sort && find . -name "fingerprint.json" -not -path "./.git/*" | wc -l && uv run pytest -q --ignore=tests/test_factor_hierarchy.py --ignore=tests/test_crsp_rebuild_measurements.py -p no:cacheprovider</automated>
    <fails_when>non-zero exit, or the line `BACKTEST_UNTOUCHED` is absent from stdout, or the pytest summary line reports "no tests ran", or any output line matches "failed" or "error"</fails_when>
  </verify>
  <acceptance_criteria>
    - `PHASE_BASE="$(git rev-list -n1 --grep='docs(03.12): create phase plan' HEAD)^"; git diff --name-only "$PHASE_BASE" -- quantlab/base/backtest.py` 输出为空（D-11：一行未改。范围钉在 phase 基线，裸 `git diff HEAD` 对已提交改动会空输出而假绿）
    - `grep -c 'warnings.warn' quantlab/base/backtest.py` 与本 phase 开始前相同，且 `grep -c 'raise' quantlab/base/backtest.py` 在 `_compare_fingerprints` 方法体内为 `0`
    - SUMMARY 含 `find data -maxdepth 4 -name '*.zarr' -prune | sort` 的完整输出
    - SUMMARY 含 `find . -name "fingerprint.json" -not -path "./.git/*"` 的完整输出与计数
    - SUMMARY 含一句关于 D-12 的明确结论：要么逐个列出重算后的因子库/标签库产物路径与 mtime（晚于重建开始时刻），要么写明「本机不存在任何因子库 / 标签库产物」并附上清点输出
    - SUMMARY 的 UAT 清单至少含三项：远端 store 重建、远端因子库/标签库重算、远端 backtest run 数清点
    - SUMMARY **不含**任何对远端 run 数量的断言
    - 全量回归的 summary 行在 SUMMARY 里，且不含 `failed`
    - 主仓库工作树里的真实数据门 summary 行在 SUMMARY 里
  </acceptance_criteria>
  <done>D-12 的重算状态被逐项清点而不是被默认；D-11 的 warning 行为一行未改且本机 run 数有实证；远端事项进 UAT 且无任何无观测的断言。</done>
  <reversibility rating="reversible">本 task 不改代码、不写数据产物，只读盘与跑回归。</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `CrspStoreRebuilder.clear()` → 盘上的真实 store 与五个旁车 | 本 phase 唯一的破坏性写。备份代数是它唯一的安全网 |
| 重建后的 store → 下游因子库 / 标签库 / backtest run 指纹 | 值变了但列名与形状没变，所以**结构性检查全部通过**而数值全变 —— 这是本 plan 最需要看住的一条 |
| 本机观测 → SUMMARY 里的事实陈述 | 远端未连通。把「无观测」写成「0 个」会造出一个后面没人会再去核对的假事实 |

**不存在的边界，如实记录**：本 plan 不引入网络调用（`quantlab/dataset/crsp/rebuild.py:11-14`
与 `quantlab/registry.py:565-571` 逐字保证重建路径**离线**，不需要 WRDS 凭证）、
不解析外部输入、不安装任何包。

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.12-14 | Tampering | `factor_store[i]` / `train_factor_store[i]` / `train_label_store[i]` 指纹（`quantlab/base/backtest.py:1143-1160`） | high | mitigate | 只重建 dataset store 而不重算因子库，digest **保持不变而底层已变** —— 这个键本来就是为抓这种情形设的，却抓不到。缓解：D-12 写成 Task 3 的强制清点（`find` 输出必须进 SUMMARY），并在 `example/wrds_crsp.md` 的重建 checklist 第 3 步以「不是可选项」的措辞落地（plan 03） |
| T-03.12-15 | Denial of Service | `BaseStoreRebuilder.backup()` 的 `dirs_exist_ok=True` | high | mitigate | 两次重建指向同一个备份目录会**就地覆盖**旧代，而每一代都是当前代码已无法再产出的那个面板的唯一记录。缓解：`_fresh_backup_dir()` 在 `rebuild()` 之前就选名并断言其不存在；Task 1 要求先列出既有代数清单，Task 2 的 acceptance 要求代数**多 1** 且既有各代 `du -sk` 未变 |
| T-03.12-16 | Repudiation | 对远端 backtest run 数量的陈述 | medium | mitigate | 研究会话 ssh 未连通（`Host key verification failed`），本 phase 对远端**无观测**。把它写成「0 个」会造出一个假事实并让远端的失效 run 无人清点。缓解：prohibitions 明令禁止任何远端断言；acceptance 要求 SUMMARY **不含**远端 run 数量陈述，并把清点列为 UAT 项 |
| T-03.12-17 | Tampering | 真实数据门被 skip 而不是 fail | high | mitigate | 一个被跳过的数据门在每一个操作者会看的 summary 行里都读作「通过」，所以它比没有门更坏（`tests/test_crsp_rebuild_measurements.py:179-204` 的逐字论证）。缓解：prohibitions 禁止引入 `skipif` / `pytest.skip`；acceptance 用 `grep -c` 归零门；verify 的 `fails_when` 把 `skipped` 也列为失败信号 |
| T-03.12-18 | Tampering | I-1 在真实数据上的容差 | medium | mitigate | 精确 `==` 在真实 store 上只有 514/520，把 6 个末位差当失败会诱导团队放宽到 `approx()` 默认 `rel=1e-6`，从而同时盖掉一个真实的 5e-06 级偏移。缓解：容差固定 `rel=1e-12` 并写进 prohibitions 与 acceptance；用例同时打印「精确 `==` 成立的标的数」作为这条取舍的现场证据 |
| T-03.12-19 | Spoofing | 隔离 worktree 里的假绿（`project_gsd_worktree_verify_path`） | medium | mitigate | verify 若写死主仓库绝对路径，隔离执行会测到未改动的代码而报假绿；而 `data/` 是 gitignored，worktree 里根本不存在。缓解：数据根一律由 `git rev-parse --path-format=absolute --git-common-dir` 推导；Task 3 第 9 步要求合并后在**主树**独立复跑全量回归与真实数据门 |
| T-03.12-20 | Information Disclosure | 重建路径的凭证需求 | low | accept | `quantlab/dataset/crsp/rebuild.py:11-14` 与 `quantlab/registry.py:565-571` 逐字保证重建**离线**：不连 WRDS、不读凭证。`QUANTLAB_DATA_ROOT` 是一个本地路径，不是密钥 |
| T-03.12-SC | Tampering | npm/pip/cargo installs | low | accept | **本 plan 无任何包安装任务。** RESEARCH `## Package Legitimacy Audit` 逐字：本 phase 不安装任何外部包；审计表为空，`[SLOP]` / `[SUS]` 均为 none |
</threat_model>

<verification>
1. `find . -name __pycache__ -type d -prune -exec rm -rf {} +`（R-09）
2. 真实数据门（**主仓库工作树**）：`QUANTLAB_DATA_ROOT="$(dirname "$(git rev-parse --path-format=absolute --git-common-dir)")" uv run pytest -q -s tests/test_crsp_rebuild_measurements.py -p no:cacheprovider` —— 7 passed、0 skipped
3. 全量回归（**主仓库工作树**）：`uv run pytest -q --ignore=tests/test_factor_hierarchy.py --ignore=tests/test_crsp_rebuild_measurements.py -p no:cacheprovider` 全绿
4. 编译门：`uv run python -m compileall -q quantlab` 退出码 0
5. 零残留 gate：`uv run pytest -q tests/test_no_anchor_machinery_residue.py tests/test_no_identity_residue.py -p no:cacheprovider` 全绿
6. `PHASE_BASE="$(git rev-list -n1 --grep='docs(03.12): create phase plan' HEAD)^"; git diff --name-only "$PHASE_BASE" -- quantlab/base/backtest.py quantlab/base/chunking.py quantlab/base/config.py` 为空（D-11 + D-04）
7. `ls -d data/_backup_*` 比重建前多一代；既有各代 `du -sk` 未变
8. `test ! -e data/data/us_equity/1d/wrds_crsp_sp500_1d.zarr.crsp_adjustment.json` 退出码 0

**留给 UAT（本 phase 不断言）**：
- 登录 `zhrdai@frp-one.com:~/projects/quantlab2` 清点受影响的 backtest run 数
- 远端 store 重建 + 远端因子库 / 标签库重算
- 全市场 / 长跨度下的数值量级复核，以及 `alpha084` 非有限格数的前后对比（D-08，只观察不修）
- 用一个既有 run 的 `config.json` 重放，抄下指纹 warning 的条数与键名（D-11 的替代强化）
</verification>

<success_criteria>
- [ ] 操作者就 one-way 重建做出了明确选择，选择与理由进 SUMMARY
- [ ] `test_the_real_store_reproduces_its_adjusted_columns` 存在并在真实 252×520 面板上绿
- [ ] I-1 覆盖 520/520 标的（`rtol=1e-12`），且「精确 `==` 成立的标的数」被打印
- [ ] I-5 四个数为 `0 / 0 / 764 / 764`，与 `.last()` 锚下完全一致
- [ ] 重建产物为 `{'timestamp': 252, 'symbol': 520}` / 27 `data_vars`，旁车不含 `.crsp_adjustment.json`
- [ ] 备份代数多一代，既有各代字节未变
- [ ] 该测试文件里 `skipif` / `pytest.skip` 计数为 0，真实数据门 0 skipped
- [ ] D-12 的清点输出进 SUMMARY，结论明确（重算了哪些 / 本机一个都没有）
- [ ] `quantlab/base/backtest.py` 一行未改（D-11），本机 run 数有 `find` 实证
- [ ] SUMMARY 不含任何对远端 run 数量的断言，远端事项全部进 UAT 清单
- [ ] 全量回归与真实数据门都在**主仓库工作树**里复跑过，summary 行进 SUMMARY
</success_criteria>

<output>
Create `.planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-04-SUMMARY.md` when done.
SUMMARY 必须包含：Task 1 的决策记录、真实数据门的完整 `print` 输出逐行抄录、
重建前后的备份代数清单、D-12 的清点输出与结论、D-11 的 `find` 输出与计数、
`<verification>` 八条的实际结果，以及一份 UAT 清单（至少四项，全部关于远端或 D-08 观察）。
</output>

