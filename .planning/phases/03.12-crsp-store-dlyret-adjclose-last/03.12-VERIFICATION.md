---
phase: 03.12-crsp-store-dlyret-adjclose-last
verified: 2026-09-23T06:40:00Z
status: gaps_found
score: 11/13 must-haves verified
covered_files:
  - ".planning/REQUIREMENTS.md"
  - ".planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-01-PLAN.md"
  - ".planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-01-SUMMARY.md"
  - ".planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-02-PLAN.md"
  - ".planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-02-SUMMARY.md"
  - ".planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-03-PLAN.md"
  - ".planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-03-SUMMARY.md"
  - ".planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-04-PLAN.md"
  - ".planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-04-SUMMARY.md"
  - ".planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-CONTEXT.md"
  - ".planning/phases/03.12-crsp-store-dlyret-adjclose-last/03.12-REVIEW.md"
  - ".planning/phases/03.12-crsp-store-dlyret-adjclose-last/deferred-items.md"
  - "example/wrds_crsp.md"
  - "quantlab/dataset/crsp/__init__.py"
  - "quantlab/dataset/crsp/rebuild.py"
  - "scripts/ingest_wrds_crsp.py"
  - "tests/test_crsp_dataset.py"
  - "tests/test_crsp_first_anchor.py"
  - "tests/test_crsp_identity.py"
  - "tests/test_crsp_rebuild_measurements.py"
  - "tests/test_crsp_ticker_sidecar.py"
  - "tests/test_crsp_tracer.py"
  - "tests/test_ingest_wrds_crsp.py"
  - "tests/test_no_anchor_machinery_residue.py"
covered_digest: "v1:sha256:c78614a13a58c755b48a3cebc84d7bd95849b41de3f2aba16379e5aadf3bba93"
behavior_unverified: 0
overrides_applied: 0
gaps:
  - truth: "真实 CRSP store 按新锚重建一次，因子库与标签库一并重算（ROADMAP goal 末句）"
    status: failed
    reason: >-
      操作者在 plan 04 Task 1 的 blocking-human 决策门主动选 B（只合并代码、重建推迟），
      这是一次**明示的推迟**而不是漏做。但目标句本身未兑现，且后果是可测的：
      本次验证在盘上 store 上实算，520/520 个 PERMNO 的 `adjClose` 仍等于它**最后**一个
      可用行的 `close`，0/520 等于**第一个**可用行的 `close` —— 盘上数据 100% 仍是
      `.last()` 前复权，与 `.first()` 的代码**不一致**。任何人现在读这个 store 算因子，
      拿到的是旧锚数值。已登记为 WINDOWS #31（`unrun-verify`）。
    artifacts:
      - path: "data/data/us_equity/1d/wrds_crsp_sp500_1d.zarr"
        issue: "252×520、27 个 data_vars，但 adj* 由 .last() 锚算出；旁边还留着已删机制的 .crsp_adjustment.json 旁车"
      - path: "tests/test_crsp_rebuild_measurements.py"
        issue: "I-1 真实数据门 test_the_real_store_reproduces_its_adjusted_columns 已写入并可 collect（7 tests collected），但从未执行；I-5 的四个不变量同样未在新锚下取值"
    missing:
      - "在主仓库工作树执行 CrspStoreRebuilder 重建 wrds_crsp_sp500_1d.zarr（备份代数只增不覆盖）"
      - "重建后跑 tests/test_crsp_rebuild_measurements.py，取得 I-1（520/520，rel=1e-12）与 I-5（0 / 0 / 764 / 764）的真实数字"
      - "按 example/wrds_crsp.md:818 的四步 checklist 第 3 步重算因子库与标签库（本机现为 0 个，远端未观测）"
      - "关闭 WINDOWS #31"
  - truth: "I-5：`CrspStoreRebuilder._measure()` 的四个不变量在 `.first()` 下原值不变（0 / 0 / 764 / 764）"
    status: failed
    reason: >-
      与上一条同根同源，单列是因为它是 ROADMAP「五条验收不变量」里唯一一条**完全没有取过值**的。
      I-1/I-2/I-3/I-4 都有合成 fixture 上的绿证据，I-5 只能在真实 store 重建后测得。
      断言代码在盘上（`tests/test_crsp_rebuild_measurements.py:704-729`），只差一次执行。
    artifacts:
      - path: "tests/test_crsp_rebuild_measurements.py"
        issue: "test_the_real_store_reproduces_its_adjusted_columns / test_sp500_2024_rebuild_matches_post_fix_measurements 未执行（该文件被排除在日常回归外，因为它会重建真实 store）"
    missing:
      - "重建后执行该文件，把 metrics 四元组抄进证据"
advisory: []
human_verification:
  - test: "裁决 REVIEW CR-01：`.first()` 锚让 `adj*` 的水平失去截面可比性，而 Alpha101/Alpha158 把 adjClose/adjVolume 直接喂给 KunQuant 的截面 Rank"
    expected: "操作者在三条路里选一条（补一条截面不变量并如实记录取舍 / 给因子层换输入量 / 明确接受并登记为 backlog），并把裁决登记成 WINDOWS 条目或 backlog 项"
    why_human: "这是一个语义/口径取舍，不是代码缺陷。D-08 已把它划出本 phase 范围，但 CONTEXT 同时记下「风险方向变了，UAT 应当观察」。本次验证已独立复算确认技术前提成立（见下表），但选哪条路必须由操作者定。"
  - test: "重建真实 store 之后，用一个既有 backtest run 的 config.json 重放一次（load_backtester_from_config），抄下指纹 warning 的条数与键名"
    expected: "D-11 的替代强化证据：既有 run 全部失效这件事有证据，而 _compare_fingerprints 一行未改"
    why_human: "本机既有 run = 0（find fingerprint.json 实测），远端 zhrdai@frp-one.com 本 phase 无观测（ssh Host key verification failed）"
---

# Phase 03.12: CRSP 复权锚点改为 store 首个可用行 验证报告

**Phase Goal:** CRSP 的复权锚点从「转换窗口内最后一个可用行」（前复权）改成「每个 PERMNO 在
store 内**第一个**可用行」（后复权），**仍在保存 zarr 时计算**（五个 `adj*` 仍是落盘列，store
保持 27 个 `data_vars`），从而让锚在只向前 append 时永不移动 ⇒ store 真正不可变 ⇒ 整套为
「锚点会移动」而存在的窗口机械被**删除**而不是保留在开关后面。真实 CRSP store 按新锚重建一次，
因子库与标签库一并重算。

**Verified:** 2026-09-23T06:40:00Z
**Status:** gaps_found
**Re-verification:** No — initial verification
**Diff base:** `9e9d2ab`（phase 起点）→ `99aa05f`（phase 末提交）。`5027a54`（`CrspMarketRoster`）
是 phase 之后的无关后续工作，已排除在范围外。

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | 锚 = 每个 PERMNO 在 store 内第一个满足 D-05 三条**同行**谓词的行，仍在**建库时**算 | ✓ VERIFIED | `quantlab/dataset/crsp/__init__.py:622-634`：`derived.filter(close.is_not_null() & close>0.0 & dlycumfacshr.is_not_null()).group_by("permno").agg(close.first(), _G.first(), dlycumfacshr.first())` —— **一次** `agg`，三个锚量同索引。`:584` 的 `frame.sort(["permno","timestamp"])`（`.first()` 语义的唯一依据）原位保留。派生仍在 `_derivation()` 内、`_finalise()` 落盘之前 |
| 2 | store 保持 **27 个 `data_vars`**，五个 `adj*` 仍是落盘列 | ✓ VERIFIED | 结构证据：`git diff 9e9d2ab..99aa05f -- quantlab/dataset/crsp/__init__.py \| grep -E "TIINGO_VARIABLES\|EXTRA_VARIABLES\|columns ="` = **空**，列集合一字未动（12 `TiingoColumns.EOD` + 14 `CRSP_EXTRA_VARIABLES` + `anomaly_flag`）。盘上实测复核：`xr.open_zarr(...)` → `data_vars` = **27**、`dims {'timestamp':252,'symbol':520}` |
| 3 | **I-1**：锚行 `adjClose == close` 且同行 `adjVolume == volume`（`rel=1e-12`） | ✓ VERIFIED | `tests/test_crsp_first_anchor.py::test_the_anchor_is_first_usable_row_and_its_adjclose_equals_its_close` —— 实跑 **PASS**。fixture 带一个前置无价格日 + 4:1 拆股 + 分红，因此确实能区分两个锚。真实 store 上的同名门未跑，见 Gap 1 |
| 4 | **I-2**：全量建 `[A,B]` 与先建 `A` 再 `update()` 到 `B`，`assert_identical` 相等 | ✓ VERIFIED | `tests/test_crsp_first_anchor.py::test_a_full_and_incremental_build_agree_bit_for_bit` —— 实跑 **PASS**。本次验证**独立复核**了「第二次 run 确实是 append 而非重写」：对增量 store 做 run 前/run 后文件级 mtime+size 快照，**22 个原始数据 chunk 字节未变**，被改写的 30 个全是 `zarr.json` 元数据（append 后 shape 变大），文件数 52 → 158。append 路径为真 |
| 5 | **I-3**：换锚是每标的一个常数重标度（`rel=1e-9`） | ✓ VERIFIED | `test_switching_the_anchor_is_a_constant_rescale_per_permno` 实跑 **PASS**。对照序列有自检（`control[last_anchor] == close[last_anchor]`, `rel=1e-12`），所以对照不是从 store 抄来的 |
| 6 | **I-4**：`adjClose_t/adjClose_{t-1}` 在两锚下相同 | ✓ VERIFIED | `test_the_day_over_day_ratios_unchanged_by_the_anchor_switch` 实跑 **PASS** |
| 7 | **I-5**：`_measure()` 四不变量在 `.first()` 下原值不变（0/0/764/764） | ✗ FAILED | 断言代码存在（`tests/test_crsp_rebuild_measurements.py`），**从未执行**。只能在真实 store 重建后取值。见 Gap 2 |
| 8 | 整套窗口锚点机械被**删除**，而不是留在开关后面 | ✓ VERIFIED | 七个名字在 `quantlab/` 与 `scripts/` 里 `grep` 命中数均为 **0**（`ADJUSTMENT_SIDECAR_SUFFIX` / `ADJUSTMENT_RULE` / `adjustment_sidecar_path` / `_adjustment_record` / `_assert_anchor_unchanged` / `_write_adjustment_record` / `total_return_backward_from_last_close`）。diff 显示两个类常量 + 四个方法整体删除，`scripts/ingest_wrds_crsp.py` 的打印调用点一并去掉。新增仓库级 gate `tests/test_no_anchor_machinery_residue.py` 实跑 **PASS**。**没有任何开关/配置字段**被引入来保留旧路径 |
| 9 | 「延长 `end_date` 再 append」从**被拒绝**变成**合法** | ✓ VERIFIED | 被删的用例里就有 `test_extending_the_window_is_refused_naming_both_anchors`、`test_a_store_without_the_sidecar_is_refused`、`test_from_raw_data_runs_the_anchor_gate_too`，取而代之的正是 I-2。成对删除，没有留下描述已删机制的绿测试 |
| 10 | **D-04**：`quantlab/base/`（含 `config.py`）、`quantlab/backtest/`、`quantlab/factor/`、`quantlab/label/` 零改动 | ✓ VERIFIED | `git diff --name-only 9e9d2ab..HEAD -- quantlab/base/ quantlab/backtest/ quantlab/factor/ quantlab/label/` = **空**。到 `HEAD`（含 phase 后的 `5027a54`）也是空 |
| 11 | **D-05 / D-07** 完整保留 | ✓ VERIFIED | 哨兵派生（`:560-574`：`dlyprcflg ∈ _NO_PRICE_FLAGS \| dlyprc == 0.0 → None`，再 `abs()`）一字未改，没有退化成 `dlyprc > 0`；`_assert_anchor_usable`（`:680-755`）两条拒绝原位；`adjClose` 走 `cum_prod(1+dlyret)`、`adjVolume` 走 `dlycumfacshr`、`splitFactor` 走 `dlycumfacpr`，三者未合并 |
| 12 | **D-10** 两个失效场景以**具名已知限制**活在 `_derivation()` 注释与文档里 | ✓ VERIFIED | `quantlab/dataset/crsp/__init__.py:513-545` 的 `KNOWN LIMITATIONS (D-10)` 段落带实测数字（`start_date` 后移 112,359 行 / max rel 0.75；raw tier 向前回填 756/756 行 / max rel 0.76），并写明 `ChunkLedger.assert_consistent` 不看 `start_date`。`example/wrds_crsp.md:497-516` 同样是表格 + 「**没有任何代码检查它们**」，措辞没有弱化成「不会发生」 |
| 13 | **真实 CRSP store 按新锚重建一次，因子库与标签库一并重算** | ✗ FAILED | 盘上实算：`adjClose == close` 在**最后**一个可用行成立 **520/520**，在**第一个**可用行成立 **0/520**。盘上 store 100% 仍是 `.last()` 前复权，与代码不一致。操作者在 plan 04 Task 1 选 B（推迟），已登记 WINDOWS #31。见 Gap 1 |

**Score:** 11/13 truths verified（0 present-behavior-unverified）

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `quantlab/dataset/crsp/__init__.py` | `.first()` 选锚 + 删机械 + D-10 注释 | ✓ VERIFIED | +256/-497 净删；六个成员整体消失；`_assert_anchor_usable` / 哨兵派生 / `select(columns)` 三处禁改点原样 |
| `quantlab/dataset/crsp/rebuild.py` | `CRSP_SIDECAR_SUFFIXES` 元组**不动**，只改说明注释 | ✓ VERIFIED | diff 只有注释块（+7/-3），`.crsp_adjustment.json` 仍在清理清单里（R-05 判例） |
| `tests/test_crsp_first_anchor.py` | I-1 / I-2 / I-3 / I-4 | ✓ VERIFIED | 690 行新文件，5 个用例全绿 |
| `tests/test_no_anchor_machinery_residue.py` | 仓库级零残留 gate | ⚠️ 见 WR-01 | 134 行，实跑绿；但只匹配**标识符**不匹配**散文**，且明确不扫 `scripts/` |
| `tests/test_crsp_rebuild_measurements.py` | I-1 + I-5 真实数据门 | ⚠️ 已写未跑 | collect-only **7 tests collected**；新用例 `test_the_real_store_reproduces_its_adjusted_columns` 从未执行 |
| `example/wrds_crsp.md` | 复权节改写 + 两条已知限制 + 重建 checklist | ✓ VERIFIED | `:442` 整节改写成 `.first()` 后复权；`:497` 已知限制表；`:818` 四步重建 checklist，第 3 步是 D-12「必须一并重算因子库与标签库」，第 4 步是 D-11；`:803` 追记把旁车个数 4 → 3 说清楚 |
| `scripts/ingest_wrds_crsp.py` | 去掉锚点旁车打印 | ✓ VERIFIED | -4 行，只删那一处 |

---

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `crsp/__init__.py:584` `frame.sort(["permno","timestamp"])` | `:622-634` `.first()` 归约 | polars 组内保序 | ✓ WIRED | sort 在派生之前、归约之前，未被移动或删除 |
| `:607-612` 三条同行谓词 | `:628-633` 一次 `group_by().agg()` | 单索引 | ✓ WIRED | `close_A` / `G_A` / `cumfacshr_A` 出自同一次 `agg`，CR-02 防线完整 |
| `_derivation()` | `_finalise()` → `select(columns)` | 27 个 data_vars | ✓ WIRED | 列集合未改，盘上实测 27 |
| `rebuild.py:CRSP_SIDECAR_SUFFIXES`（清理） | `test_crsp_rebuild_measurements.py:EXPECTED_WRITTEN_SIDECARS`（必写） | 两张清单答不同问题 | ✓ WIRED | `.crsp_adjustment.json` 留在清理（5 元组）、离开必写（3 元组），两处注释都写了判例 |
| 文档重建 checklist | `base/backtest.py:1143-1160` `factor_store[i]` 指纹口径 | D-12 | ✓ WIRED | 文档 `:831-835` 逐条复述了「digest 保持不变而底层已变」的机理 |
| `_derivation()` 选锚 | `_apply_security_filter()` | 顺序 | ⚠️ 见 WR-04 | 选锚在过滤**之前**（`:636` vs `:665`），而真实数据门从**面板**重建锚行 —— 两者可以不是同一行。门未跑，暂无观测 |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| `crsp/__init__.py` | `adjClose` | `close_A × _G / _G_anchor`，`_G = cum_prod(1+dlyret).over("permno")` | ✓ 真实 polars 表达式，无静态回退 | ✓ FLOWING |
| `crsp/__init__.py` | `adjVolume` | `volume × dlycumfacshr / _cumfacshr_anchor` | ✓ | ✓ FLOWING |
| 盘上 `wrds_crsp_sp500_1d.zarr` | `adjClose` | **由旧 `.last()` 代码写出** | ✗ 与当前代码不一致 | ⚠️ STALE（Gap 1） |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| 锚点/残留用例全绿 | `uv run pytest -q tests/test_crsp_first_anchor.py tests/test_no_anchor_machinery_residue.py` | `6 passed in 3.75s` | ✓ PASS |
| CRSP 全家桶回归 | `uv run pytest -q tests/test_crsp_dataset.py tests/test_crsp_identity.py tests/test_crsp_tracer.py tests/test_crsp_ticker_sidecar.py tests/test_ingest_wrds_crsp.py tests/test_crsp_rebuild.py` | `169 passed in 29.49s` | ✓ PASS |
| 真实数据门可收集 | `uv run pytest --collect-only -q tests/test_crsp_rebuild_measurements.py` | `7 tests collected` | ✓ PASS |
| I-2 是否真的 append（自建探针） | run 前/后文件快照 diff | `22 个数据 chunk 字节未变；30 个 zarr.json 被改写；52 → 158 文件` | ✓ PASS |
| 盘上 store 锚点朝向（自建探针） | 逐 PERMNO 比 `adjClose` 与首/末可用行的 `close` | `first 0/520 · last 520/520` | ✗ FAIL（Gap 1，符合已知推迟） |
| D-11 证据 | `git diff --stat 9e9d2ab..HEAD -- quantlab/base/backtest.py` | 空 | ✓ PASS |
| D-12 证据 | `find data -maxdepth 4 -name '*.zarr' -prune` | 12 个，全部是 dataset store / 备份代数，**0 个因子库或标签库** | ✓ PASS |
| 既有 backtest run 数 | `find . -name fingerprint.json -not -path './.git/*' \| wc -l` | `0` | ✓ PASS |

> 注：全量 `uv run pytest` 未重跑。编排器已给出四次实测的同一基线
> （`55 failed, 1719 passed, 1 skipped`，55 条全部与 CRSP 无关且每 wave 增量为 0），
> 本次不重复取样；`tests/test_cross_sectional_zscore.py` 的已知间歇性死锁
> （D-03.11-12-B）与 `tests/test_crsp_rebuild_measurements.py` 的破坏性也让全量跑代价
> 大于收益。取而代之的是上面**针对性**的 175 个 CRSP 用例。

---

### Probe Execution

本仓库没有 `scripts/*/tests/probe-*.sh` 形态的探针，PLAN / SUMMARY 也未声明任何探针。
**Step 7c: SKIPPED（no probes declared or discovered）。**

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| DATA-03 | 01 / 02 / 03 / 04（四个 plan 全部声明） | 数据层抽象按市场/频率可扩展，新增市场或频率不需改上层因子/模型/回测 | ✓ SATISFIED（advanced，**未 COMPLETE**，符合预期） | ROADMAP 明写「本 phase 不 COMPLETE 任何 requirement」。本 phase 对该需求的推进由 D-04 的**零改动实证**直接兑现：换掉 vendor 内部的复权锚点，`quantlab/factor/`、`quantlab/label/`、`quantlab/backtest/`、`quantlab/base/` 一行未动 —— 这正是 DATA-03 要的「换一层不牵动上层」。`REQUIREMENTS.md:23` 仍为 `[ ]`，`:139` 仍为 `Pending`，与「目标尚有一条未闭合」一致 |

**Orphaned requirements:** 无。`grep -E "Phase 03.12" .planning/REQUIREMENTS.md` 无额外映射。

⚠️ **可追溯性小缺口（WARNING，非 blocker）**：`REQUIREMENTS.md:139` 的 DATA-03 溯源行为
03.10 与 03.11 各追加过一段说明，**03.12 没有追加**。`.planning/REQUIREMENTS.md` 也不在
本 phase 的 `git diff` 里。状态字（`Pending`）本身是对的，缺的只是那一句溯源记述。

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| — | — | `TBD` / `FIXME` / `XXX` 债务标记 | — | 八个改动文件全扫，**0 命中**。无债务门 blocker |
| `quantlab/dataset/crsp/__init__.py` | 1580 | 散文残留：「…**anchor sidecar** has carried exactly this guard since it was introduced」—— 现在时完成体指向一个已删除的旁车 | ⚠️ Warning | 与 REVIEW WR-01 同一处。plan 02 自己的立论是「指向已删机制的注释是一句关于系统如何工作的假陈述」，零残留 gate 按标识符匹配抓不到它。**不影响行为** |
| `tests/test_no_anchor_machinery_residue.py` | 66（`PACKAGE_ROOT`） | gate 只扫 `quantlab/`，不扫 `scripts/` | ⚠️ Warning | 本次验证独立复核：`scripts/` 里七个标识符命中数**也是 0**，所以当下无实害；但 gate 对 `scripts/` 的复发**没有防护** |
| `tests/test_crsp_first_anchor.py` | 682-688 | I-2 的「增量性」自检 `len(ledger["windows"]) >= 2` **无法区分 append 与整体重写** | ⚠️ Warning | 本次验证实证：一次性全量建同一区间的 store，其台账**也是 2 个 window**（`2019-11-01..2019-12-31` / `2020-01-01..2020-09-30`）。所以该断言恒真，不承载任何鉴别力。**不影响 I-2 当下的结论**——独立文件快照探针已证明 `update()` 走的确实是 append（22 个数据 chunk 字节未变）——但若将来 `update()` 的 `_reconcile_new_listings` 走上 `rebuild` 分支，这条自检不会变红。与 REVIEW WR-03 同一处，**裁定：WR-03 成立** |
| `tests/test_crsp_dataset.py` | 1309 | `test_the_anchor_is_the_FIRST_usable_row` 在 `.last()` 下同样为绿（REVIEW WR-02） | ℹ️ Info | plan 01 的 truth 只要求「名字不再是谎话」，已兑现；真正的鉴别力在新文件的 I-1。可接受 |
| `data/data/us_equity/1d/` | — | `wrds_crsp_sp500_1d.zarr.crsp_adjustment.json` 仍躺在盘上 | ℹ️ Info | 重建会清掉（`CRSP_SIDECAR_SUFFIXES` 保留了该 suffix，正是为此）。重建推迟 ⇒ 残留是预期的 |

---

## CR-01 裁定（REVIEW 唯一一条 Critical，本次验证独立复算）

REVIEW 的技术前提**成立**，我用本机盘上的真实 store 独立复算了它的两张表（按 `.first()`
锚重算每标的的刻度，取该标的最后一个可用日）：

| 量 | min | p50 | p95 | max | 恰好 == 1.0 |
|---|---|---|---|---|---|
| `adjClose / close` | 1.000 | **1.017** | 1.062 | **50.0** | 0/520（`rtol=1e-12`） |
| `adjVolume / volume` | **0.020** | 1.000 | 1.000 | 1.000 | 503/520（**96.7%**） |

与 REVIEW 的数字一致（p50 1.017、p95 1.062、max 50、adjVolume min 0.02、96.7%）；唯一差异是
「恰好 ==1.0」的占比，REVIEW 记 17.5%，我在 `rtol=1e-12` 下得 0% —— 容差口径差异，不改结论。

消费方也复核过：`quantlab/factor/alpha101.py:82-86` 与 `alpha158.py:104-108` 都是
`Input("adjClose")` / `Input("adjVolume")` 直接进 `AllData`，而 Alpha101 的多条公式对它们套
截面 `Rank`。所以「`Rank(adjVolume)` 排的是成交量 ÷ 该标的自己的历史拆股因子」这句为真。

**裁定：WARNING + 必须由人裁决，不是本 phase 的 blocker。**

- 它不是交付缺陷：`.first()` 锚是操作者 2026-09-22 明示裁定的目标本身，代码忠实实现了它。
- 它也不是被漏掉的：`03.12-CONTEXT.md` D-08 把 Alpha101 截面水平敏感性划出范围，并在修订段
  补记「D-08 的风险方向变了（不重开围栏，仅记录），UAT 应当观察」。
- 但它**当下没有任何登记项**（WINDOWS 只有 #30 / #31，都不是这条），所以会随 phase 关闭
  而消失。这是本报告把它列进 `human_verification` 的理由。
- 回测侧确认不受影响（`engine_vectorbt.py` 用 `size_type="targetpercent"`、无
  `size_granularity`、费用皆比例，每标的一个常数缩放不改 P&L）——「回测全绿」不能拿来
  支持因子侧安全。

---

## Deferred Items

无。本次核对了 ROADMAP 里 03.12 之后的所有 phase（03.11 / 03.1-03.9 均在其**前**，
Phase 4/5/6/7 是模型、组合优化、端到端回测与测试质量），**没有任何一个 phase 的目标或
验收条件覆盖「重建 CRSP store」**。所以 Gap 1 不是被后续 phase 接手的延后项，而是一条
真实未闭合的目标句（由操作者主动推迟，登记在 WINDOWS #31）。

---

## Human Verification Required

### 1. 裁决 CR-01：`.first()` 锚的截面可比性

**Test:** 在上表两组实测刻度（`adjClose/close` p50=1.017 / max=50；`adjVolume/volume`
min=0.02）的基础上，决定因子层怎么办。REVIEW 给了三条路：(a) 补一条**截面**不变量并如实
记录取舍；(b) 给因子层换输入量；(c) 明确接受并登记 backlog。
**Expected:** 选定一条，并登记成 WINDOWS 条目或 backlog 项，不让它随 phase 关闭而消失。
**Why human:** 语义/口径取舍，不是代码缺陷。D-04 护栏明令 `quantlab/factor/` 零改动，
所以修复路径本身需要越过一条本 phase 的硬约束。

### 2. 重建真实 store，并跑真实数据门（同时闭合 Gap 1 + Gap 2）

**Test:** 按 `example/wrds_crsp.md:818` 的四步 checklist 重建
`data/data/us_equity/1d/wrds_crsp_sp500_1d.zarr`，然后
`QUANTLAB_DATA_ROOT=... uv run pytest tests/test_crsp_rebuild_measurements.py`。
**Expected:** I-1 = 520/520（`rel=1e-12`，其中精确 `==` 约 514）；I-5 = `0 / 0 / 764 / 764`；
旁车三份（`.chunks.json` / `.crsp_filter_report.json` / `.crsp_tickers.json`），
`.crsp_adjustment.json` 被清掉且不再生成；既有 `data/_backup_pre_03.11*` 五代一字节未动。
**Why human:** 破坏性操作，且 plan 04 Task 1 的 blocking-human 门已明确由操作者掌控。

### 3.（重建后）观察 WR-04：门用面板重建锚行，生产在过滤前选锚

**Test:** 若上一步的 I-1 门变红，先核对是不是 `_apply_security_filter`（`:665`，在
`_assert_anchor_usable` 的 `:636` **之后**）丢掉了生产实际用的那一行。
**Expected:** 要么 520/520 全绿（说明过滤从不丢首个可用行），要么红灯指向具体 PERMNO。
**Why human:** 只有真实 store 上才有观测；合成 fixture 碰不到这个分叉。

### 4.（重建后）D-11 的替代强化

**Test:** 用一个既有 run 的 `config.json` 经
`quantlab/utils/module.py:load_backtester_from_config` 重放一次，抄下指纹 warning 的
**条数与键名**。
**Expected:** warning（不是 raise），键名覆盖 `price_dataset` 与 KunQuant `factor[i]`。
**Why human:** 本机既有 run = 0（已实证），远端 `zhrdai@frp-one.com:~/projects/quantlab2`
本 phase 无观测（ssh `Host key verification failed`）。

---

## Gaps Summary

**代码侧的目标已经达成，数据侧还差一次执行。**

目标的四条前半句 —— 换锚到 `.first()`、仍在建库时算、27 个 `data_vars` 不变、整套窗口机械
**删除**而非藏在开关后 —— 全部在代码里得到独立证实，而且是用比 SUMMARY 更硬的证据：
七个标识符在 `quantlab/` 和 `scripts/` 里 `grep` 归零、D-04 的四个目录 `git diff` 为空、
I-2 的 append 语义用文件级字节快照证明（22 个数据 chunk 未变）、盘上 store 的 `data_vars`
实测 27。175 个 CRSP 用例全绿。

缺的是目标的**末句**：真实 store 至今仍是 `.last()` 前复权（实测 520/520），因子库/标签库
的重算因此也无从谈起（本机现有 0 个因子库，这一侧已清点实证）。这是操作者在 plan 04 的
blocking-human 门主动选 B 的结果 —— 一次**记录在案的推迟**，不是漏做。但后果是实在的：
**代码与盘上数据现在不一致**，任何人现在跑一次因子计算，算出来的是旧锚数值上的因子。
连带的是 ROADMAP 五条验收不变量里的 I-5 完全没有取过值，I-1 的真实数据门也只是被写下来、
被 collect 到，从未被执行。

两条 gap 同根：**一次重建**同时闭合它们。

另有一条 REVIEW 的 Critical（CR-01：`.first()` 让 `adj*` 的水平失去截面可比性，而
Alpha101/Alpha158 直接把它们喂给截面 `Rank`）经本次独立复算确认技术前提成立。它不阻塞本
phase 的目标（D-08 已划出范围，且 CONTEXT 明写风险方向变了、仅记录），但它目前**没有任何
登记项**，需要操作者裁决并落成 WINDOWS 或 backlog 条目。

三条低severity 观察一并记录，都不影响行为：`__init__.py:1580` 的散文残留（零残留 gate
按标识符匹配抓不到）、gate 不扫 `scripts/`（当下命中为 0，但无复发防护）、I-2 的增量性
自检恒真（本次实证全量建同区间的台账**也是** 2 个 window，故该断言不承载鉴别力 ——
REVIEW WR-03 成立）。

---

_Verified: 2026-09-23T06:40:00Z_
_Verifier: Claude (gsd-verifier)_
