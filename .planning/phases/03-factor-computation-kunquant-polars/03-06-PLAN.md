---
phase: 03-factor-computation-kunquant-polars
plan: 06
type: execute
wave: 1
depends_on: []
gap_closure: true
status: superseded
superseded_date: "2026-09-06"
superseded_by: user-decision
superseded_note: >-
  Gap 1 stands, but is being closed by a smaller manual fix chosen by the user on 2026-09-06 (resolve config.factor_names in FactorPolars.read() from data_backend.get_lazyframe().collect_schema().names() minus _INDEX_COLUMNS). This plan describes a broader change that is no longer the route being taken. See 03-VERIFICATION.md "Gap Dispositions" for what remains open under gap 1 (the both-backends read() regression test, and the false read() precondition still asserted in four prose locations).
files_modified:
  - base/factor_polars.py
  - tests/test_factor_polars.py
  - tests/test_factor_hierarchy.py
  - README.md
autonomous: true
requirements: [FACTOR-02, FACTOR-03, FACTOR-04]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A `DLConfig` carrying one KunQuant factor and one Polars factor with `factor_data_strategy=\"read\"` drives BOTH through `read()` -> `get_features()` -> `_get_factor_names()` with no backend-dependent failure (D-03 interchangeability, the user's 无缝替换 requirement)."
    - "A fresh `FactorPolars` subclass instance whose config carries no `factor_names` resolves them from the persisted store's non-index data variables during `read()`, symmetrically with `cal()` (D-05)."
    - "`base/factor_polars.py`'s documented factor-name precondition and `README.md`'s interchangeability claim are true statements about the code, each pointed at the test that locks them."
  artifacts:
    - base/factor_polars.py
    - tests/test_factor_polars.py
    - tests/test_factor_hierarchy.py
    - README.md
  key_links:
    - "`base/model.py:_get_features_batch` (`factor_data_strategy=\"read\"`) -> `FactorPolars.read()` -> `config.factor_names` -> `base/model.py:get_factor_names`"
    - "`XrBackend.get_xarray_dataset().data_vars` (persisted factor store) -> `config.factor_names` (the D-05 dynamic-name source on the read path)"
---

<objective>
Close VERIFICATION.md gap 1 / 03-REVIEW.md CR-01: `FactorPolars` is not interchangeable with `FactorKunQuant` on the `read()` path, so a `DLConfig` with `factor_data_strategy="read"` works for every KunQuant factor and raises `RuntimeError: {class}: factor names are not resolved yet.` for every Polars factor at `base/model.py:187`.

Purpose: `factor_data_strategy` is a first-class, config-selectable `Literal["read", "cal"]`. Selecting `"read"` currently makes the model layer able to tell which backend produced a factor store — exactly the coupling D-03 exists to remove, and exactly what `README.md:34-40` ships a claim against. The missing TEST matters as much as the missing line: the phase's live D-03 proof pins `"cal"` and hand-calls `cal()`, so the `read()` half of the model's call surface is driven for NEITHER backend and the asymmetry is invisible to a 66/66 green suite.

Output: a `FactorPolars.read()` override resolving names from the persisted store, a per-backend unit lock, a live two-backend `read`-strategy integration lock, and docs that match the code.

**Requirements coverage, stated honestly:** FACTOR-03 and FACTOR-04 are the real work here — the Polars backend conforming to the same `Factor` contract, and `xr.Dataset` remaining the only thing that crosses the boundary on the read path too. **FACTOR-02 (KunQuant streaming) carries NO gap** (VERIFICATION.md truth 2: ✓ VERIFIED). It is listed only as a no-regression obligation: nothing in this plan may touch the streaming surface, and `tests/test_factor_stream.py` must stay green under this plan's full-suite gate. It is not re-implemented work.

**Planner note (MVP mode):** the ROADMAP Phase 3 `**Goal:**` line is outcome-shaped but is NOT in `As a / I want to / so that` user-story form. Per the MVP-mode rules no story was invented here; the outcome-shaped goal above is used as-is. Run `/gsd mvp-phase 3` if a formal story is wanted.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-factor-computation-kunquant-polars/03-VERIFICATION.md
@.planning/phases/03-factor-computation-kunquant-polars/03-CONTEXT.md
@base/factor_polars.py
@base/factor.py
@tests/test_factor_polars.py
@tests/test_factor_hierarchy.py
</context>

<scope_fence>
The user limited this round to the two Criticals. The following are KNOWN and deliberately OUT OF SCOPE — do not fix them, and do not opportunistically touch them while editing neighbouring lines:

- **WR-01** — `Factor.get_factor_names()` (`base/factor.py:161-162`) returns `self.config.factor_names` directly, bypassing `_get_factor_names()`, so `FactorPolars`'s `RuntimeError` guard is unreachable from the public surface (`num_factors` on an unresolved Polars factor raises `TypeError`, not the documented `RuntimeError`). **Do not change `base/factor.py`.** After this plan the guard is reached far less often anyway, because `read()` now resolves names.
- **WR-05** — `base/factor.py:141` `_get_lazyframe()` is dead code with zero call sites. Leave it.
- **WR-09** — both Zarr fixtures in `tests/conftest.py` build OHLC as fixed multiples of one base series. **Do not modify `tests/conftest.py`.**

If closing this gap turns out to require one of the above, stop and say so in the SUMMARY rather than widening scope silently.
</scope_fence>

<tasks>

<task type="tracer">
  <name>Task 1: Resolve Polars factor names on the read() path — one end-to-end save -> fresh instance -> read -> names slice</name>
  <files>base/factor_polars.py, tests/test_factor_polars.py</files>
  <read_first>
    - `base/factor_polars.py` in full — `_maybe_resolve_factor_names()` (the deliberate no-op, L55-61), `_get_factor_names()`'s guard (L63-71) and `cal()`'s name assignment (L107-114), which is the symmetry this task mirrors onto `read()`.
    - `base/factor.py:126-129` — `Factor.read()`, the base implementation to delegate to: it calls `data_backend.read(config.file_path)` then `_auto_filter()` and returns `Self`. Neither touches `config.factor_names`.
    - `base/factor.py:81-94` — `_maybe_resolve_factor_names()`'s `if ... is None` idiom, which the new resolution must follow so an explicitly pinned `factor_names` is never overwritten.
    - `base/factor.py:146-147` — `_get_xarray_dataset()`, the accessor that returns the store the backend just read.
    - `tests/test_factor_polars.py` in full — the `_momentum_config()` helper (L39-53) that the new test reuses, and `test_factor_names_resolve_dynamically_from_the_lazyframe_schema` (L95-117), the existing cal()-only proof this test becomes the read() sibling of.
    - `dataset/backend.py:15-33` — `XrBackend.read()`'s `hasattr(self, "data")` early return (a FRESH instance has no `data`, so it genuinely reads) and `write()`.
  </read_first>
  <reversibility rating="reversible">A single overriding method on one class; deleting it restores today's behaviour exactly, and nothing persisted on disk changes format.</reversibility>
  <action>
    Add a `read()` override to `FactorPolars` in `base/factor_polars.py`, placed immediately before `cal()` so the two name-resolving entry points read as a pair.

    Behaviour, per 03-REVIEW.md CR-01's prescribed fix and D-05:
    - Delegate to the base implementation first (`super().read()`) so the store load, the `_auto_filter()` date/symbol filtering and the return contract stay in exactly one place. Do not reimplement any of it.
    - Then, ONLY when `self.config.factor_names` is still `None`, assign it the tuple of non-index data variables of the just-read store, sourced from `self._get_xarray_dataset().data_vars` and filtered against the module-level `_INDEX_COLUMNS` constant already used by `cal()`. Guarding on `None` matches the base class's own `_maybe_resolve_factor_names()` idiom and means a caller who pinned an explicit `factor_names` keeps their pin.
    - Assign a `tuple`, matching `cal()`'s assignment type exactly.
    - Return `self`, typed `Self`, so the method chains identically to `cal()` and to the base `read()`.
    - Write a short docstring stating that the factor store's non-index data variables ARE the factor names (D-05), that this is the read-path counterpart of the resolution `cal()` performs on the computed frame's schema, and that without it `base/model.py`'s `factor_data_strategy="read"` branch raises for Polars factors only.

    Do not add any streaming member and do not touch `base/factor.py` (see the scope fence).

    Then add the per-backend regression lock to `tests/test_factor_polars.py`, named `test_factor_names_resolve_from_the_persisted_store_on_read`, placed directly after `test_factor_names_resolve_dynamically_from_the_lazyframe_schema`:
    - Build a `PolarsFactorConfig` via the existing `_momentum_config(spot_kline_zarr(), tmp_path, n=5)` helper; call `Momentum(config).cal().save(mode="w")` to populate the store. Pass `mode="w"` explicitly rather than relying on `Factor.save()`'s `"a"` default, so the test does not depend on zarr append semantics for a store that does not exist yet.
    - Construct a SECOND, fresh `Momentum` from a SECOND `_momentum_config(...)` call over the same `spot_kline_zarr()` `DatasetConfig` and the same `tmp_path` (the helper points both at `tmp_path/"factors"/"momentum.zarr"`), so the instance under test shares nothing with the one that computed the store.
    - Assert the fresh instance's `config.factor_names` is `None` BEFORE `read()` — that assertion is what makes the test a genuine round-trip proof rather than a restatement of the constructor.
    - Call `read()`, then assert `get_factor_names() == ("momentum_5",)`, `num_factors == 1`, and `sorted(get_features().data_vars) == ["momentum_5"]`.
    - Docstring: state that before this fix the `_get_factor_names()` call raised `RuntimeError` on this exact sequence while the KunQuant sibling returned its names, cite VERIFICATION.md gap 1 / 03-REVIEW.md CR-01, and note that `base/model.py:158-165`'s `"read"` branch is the production caller.
  </action>
  <verify>
    <automated>uv run pytest tests/test_factor_polars.py -v</automated>
    <fails_when>Non-zero exit. Specifically, without the `read()` override the new test errors with `RuntimeError: Momentum: factor names are not resolved yet.` raised from `base/factor_polars.py:_get_factor_names`; if the `None` pre-assertion is dropped the test still passes for the wrong reason, so the run output must show `test_factor_names_resolve_from_the_persisted_store_on_read PASSED` alongside the five pre-existing tests in the file (6 passed).</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_factor_polars.py -v` exits 0 and reports 6 passed, including `test_factor_names_resolve_from_the_persisted_store_on_read`.
    - `grep -n "def read" base/factor_polars.py` prints exactly one line.
    - `grep -c "super().read()" base/factor_polars.py` returns 1 — the override delegates rather than reimplementing the base load.
    - `git diff --name-only` for this task lists only `base/factor_polars.py` and `tests/test_factor_polars.py`; `base/factor.py` and `tests/conftest.py` are untouched (scope fence).
  </acceptance_criteria>
  <done>A fresh `Momentum` instance driven through `save()` -> new instance -> `read()` reports `("momentum_5",)` from `get_factor_names()` and `1` from `num_factors`, where it previously raised.</done>
</task>

<task type="auto">
  <name>Task 2: Lock D-03 interchangeability on the read strategy — both backends through base/model.py's read branch</name>
  <files>tests/test_factor_hierarchy.py</files>
  <read_first>
    - `tests/test_factor_hierarchy.py:417-522` — `test_kunquant_and_polars_factors_are_interchangeable_in_one_dlconfig`, the existing cal()-path proof. The new test is its read()-path sibling and must mirror its structure, its four numbered `base/model.py` interaction comments and its no-runtime-type-branching discipline.
    - `tests/test_factor_hierarchy.py:1-58` — the module docstring and the `BASE_MODEL_CALL_SURFACE` / `FORBIDDEN_SUBSTRINGS` constants, so the new test is described in the docstring alongside the others.
    - `base/model.py:158-172` — `_get_features_batch()`, whose `case "read": ds = factor.read().get_features()` branch the new test replicates verbatim.
    - `base/model.py:184-189` — `get_factor_names()`, the `_get_factor_names()` call that raised for Polars factors and is the assertion this test really turns on.
    - `base/factor_polars.py` — the `read()` override added by Task 1.
  </read_first>
  <action>
    Add `test_kunquant_and_polars_factors_are_interchangeable_under_the_read_strategy` to `tests/test_factor_hierarchy.py`, immediately after the existing cal()-path interchangeability test, reusing the module's existing imports and the `_KUNQUANT_FACTOR_NAME` / `_MOMENTUM_HORIZON` constants.

    Structure it in two phases so the read path is driven against a store no live object in the assertion phase computed:

    Phase A — populate. Build an `Alpha158SpotKline` over a `FactorConfig` (`window=10`, `SpotKlineDataset(dataset_config)`, `mode="batch"`, `data_columns=["open","close","volume"]`, `factor_names=[_KUNQUANT_FACTOR_NAME]`, `njobs=4`, `file_path` = `tmp_path/"factors"/"kunquant.zarr"`) and a `Momentum` over a `PolarsFactorConfig` (`window=10`, its own `SpotKlineDataset(dataset_config)`, `kwargs={"n": _MOMENTUM_HORIZON}`, `file_path` = `tmp_path/"factors"/"polars.zarr"`). Pin `factor_names` on the KunQuant side so its constructor does not enumerate all 169 Alpha158 names and the compiled graph stays ~1.5 s, per this file's established cost-control convention. Call `.cal().save(mode="w")` on each.

    Phase B — read back. Construct FRESH instances of both classes from FRESH config objects pointing at the same two `file_path`s (the Polars one again leaving `factor_names` unset, which is the whole point), put them in one `DLConfig` with `factor_data_strategy="read"` and `label_data_strategy="read"`, `labels=[]`, `start_date="2024-01-01"`, `end_date="2024-02-29"` (matching the 60-day fixture window used by the sibling test).

    Then replicate `base/model.py`'s own call order, with the same numbered comments the sibling test uses:
    1. `_reset_factors_config`: set each factor's `config.start_date`/`config.end_date` from the `DLConfig` and call `_reset_dataset_config()`.
    2. `_get_features_batch`, `"read"` branch: `all_ds = [factor.read().get_features() for factor in dl.factors]`, then `xr.combine_by_coords(all_ds)`. Assert `set(data.dims) == {"timestamp", "symbol"}`, `_KUNQUANT_FACTOR_NAME in data.data_vars` and `f"momentum_{_MOMENTUM_HORIZON}" in data.data_vars`.
    3. `get_factor_names`: `list(chain.from_iterable(factor._get_factor_names() for factor in dl.factors))` — **this is the line that raised before Task 1** — and assert both names are present.
    4. `get_config`: assert each factor's `get_config()` carries `"dataset"` and `"factor_names"`, and that neither `"factor_names"` value is `None` (a `None` there is what `BaseModel._save_model()` would serialize into `config.json`, making a Polars checkpoint unreconstructable — CR-01's second consequence).

    Keep this file's hard discipline: the test body and its docstring must contain NO runtime type check and NO per-backend branch of any kind. Prove types by using the object (reading `.dims`/`.data_vars`, using `in` on the config mapping), exactly as the sibling test does, and do not spell the names of the two runtime-type builtins anywhere in the function.

    Docstring must state that this is the read half of D-03's live proof, that the cal half is the test above it, and that the loop is uniform across both backends by construction. Extend the module docstring's plan-provenance paragraph with a sentence noting that 03-06 added the read-strategy sibling to close VERIFICATION.md gap 1.
  </action>
  <verify>
    <automated>uv run pytest tests/test_factor_hierarchy.py -v</automated>
    <fails_when>Non-zero exit. Reverting Task 1's `read()` override makes this test error at the `_get_factor_names()` comprehension with `RuntimeError: Momentum: factor names are not resolved yet.`; the run output must show `test_kunquant_and_polars_factors_are_interchangeable_under_the_read_strategy PASSED` and the pre-existing `..._in_one_dlconfig` test still PASSED.</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_factor_hierarchy.py -v` exits 0 and both interchangeability tests appear as PASSED in the output.
    - `uv run pytest tests/test_factor_hierarchy.py -k "read_strategy" -v` exits 0 and collects exactly 1 test.
    - `grep -c 'factor_data_strategy="read"' tests/test_factor_hierarchy.py` returns 1 — the model's read branch is genuinely selected by config, not merely called by hand.
    - `grep -c "factor.read().get_features()" tests/test_factor_hierarchy.py` returns 1.
  </acceptance_criteria>
  <done>One `DLConfig` with `factor_data_strategy="read"` carries an `Alpha158SpotKline` and a `Momentum` through `base/model.py`'s full read-path call sequence, merges both into one `xr.Dataset`, and collects both factor names, with no branch on backend anywhere in the test.</done>
</task>

<task type="auto">
  <name>Task 3: Make the shipped documentation true and point it at the new locks</name>
  <files>base/factor_polars.py, tests/test_factor_polars.py, README.md</files>
  <read_first>
    - `base/factor_polars.py:43-49` — the class docstring's "Documented precondition on factor names" paragraph, which now describes a code path that exists rather than one that does not; and `_get_factor_names()`'s error message at L63-71.
    - `tests/test_factor_polars.py:98-106` — `test_factor_names_resolve_dynamically_from_the_lazyframe_schema`'s docstring, which restates the precondition in prose while asserting only the `cal()` half.
    - `README.md:24-50` — the Factor Backends section, whose L34-40 interchangeability claim VERIFICATION.md flagged as contradicted by the code.
  </read_first>
  <action>
    Bring the three pieces of prose in line with the code Tasks 1 and 2 landed. No behaviour changes in this task.

    `base/factor_polars.py` class docstring: rewrite the "Documented precondition on factor names" paragraph so it describes the symmetry rather than a gap. It should now say that factor names come from the computed frame's schema in `cal()` and from the persisted store's non-index data variables in `read()` (D-05), that both entry points into the model layer therefore resolve them, and that `_get_factor_names()`'s `RuntimeError` remains only for the narrow case of a factor that has done neither. Delete the "narrow, intentional gap, not an oversight" framing — the gap is closed, and leaving that sentence would re-document a defect as a design choice. Adjust `_get_factor_names()`'s error message text only if Task 1's implementation made any clause of it inaccurate; if it is already accurate, leave it byte-identical.

    `tests/test_factor_polars.py`: update `test_factor_names_resolve_dynamically_from_the_lazyframe_schema`'s docstring so it describes what that test actually covers (the `cal()` half and the pre-resolution `RuntimeError`) and names `test_factor_names_resolve_from_the_persisted_store_on_read` as the test covering the `read()` half. The false-precondition prose VERIFICATION.md flagged must not survive as a claim this test does not make.

    `README.md:34-40`: keep the interchangeability claim — it is now true — and make it verifiable. State that the model layer selects between the two paths with `DLConfig.factor_data_strategy` and that BOTH the `"cal"` and the `"read"` strategy are locked by live two-backend tests in `tests/test_factor_hierarchy.py`. Do not restructure the section or the backend table.

    Finish by running the full suite to confirm the phase's other 66 tests — including `tests/test_factor_stream.py`, this plan's FACTOR-02 no-regression obligation — are still green.
  </action>
  <verify>
    <automated>uv run pytest tests/ -q</automated>
    <fails_when>Non-zero exit, or the summary line reports fewer than 68 passed (66 pre-existing + the 2 tests added by Tasks 1 and 2), or any test in `tests/test_factor_stream.py` is reported as failed/errored.</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/ -q` exits 0 and its summary line reports at least 68 passed with 0 failed.
    - `grep -c "factor_data_strategy" README.md` returns at least 1 — the README names the config field that selects the two paths.
    - `grep -c "test_factor_names_resolve_from_the_persisted_store_on_read" tests/test_factor_polars.py` returns at least 2 (the test definition plus the cross-reference from the sibling docstring).
    - `uv run pytest tests/test_factor_stream.py -q` exits 0 (FACTOR-02 no-regression).
  </acceptance_criteria>
  <done>`base/factor_polars.py`'s docstring, `tests/test_factor_polars.py`'s docstring and `README.md`'s interchangeability paragraph each describe behaviour the suite proves, and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator filesystem -> `XrBackend.read()` | A Zarr factor store path taken from a config object the operator authors is opened by `xr.open_dataset`. Single-user, backend-only research tool (PROJECT.md scope): no network, no multi-tenant surface, no untrusted uploader. |
| (none added) | This plan adds no network call, no deserialization of an externally-supplied format, no subprocess, and no credential read. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06-01 | Tampering | `base/factor_polars.py:read()` — factor names are now derived from a store's `data_vars` rather than declared in code | low | accept | The store is written by this same pipeline under an operator-controlled path. A tampered store already corrupts factor VALUES, which no name check would catch; deriving names from it adds no new authority. ASVS L1 requires no integrity control for local single-user research artifacts. |
| T-03-06-02 | Information Disclosure | `config.factor_names` serialized into model `config.json` by `BaseModel._save_model()` | low | accept | Factor names are the operator's own research metadata, not secrets. This plan makes the field non-null for Polars factors, which is the fix, not a new exposure. |
| T-03-06-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager install task in this plan; the dependency set is unchanged. 03-RESEARCH.md L81 records that Phase 3 requires no new third-party packages, so the Package Legitimacy Gate does not trigger. |

No high-severity threat is present, so the configured "block on high" gate does not fire. 03-REVIEW.md found no security defect on this phase's surface (`TIINGO_API_KEY` is read from the environment with an explicit raise; no injection, eval or unsafe-deserialization surface exists) — this register is deliberately thin rather than padded.
</threat_model>

<verification>
1. `uv run pytest tests/test_factor_polars.py -v` — the per-backend read round trip.
2. `uv run pytest tests/test_factor_hierarchy.py -v` — both live interchangeability proofs, cal and read.
3. `uv run pytest tests/ -q` — full suite, at least 68 passed, 0 failed.
4. `git diff --name-only HEAD` lists only the four files in `files_modified`; `base/factor.py` and `tests/conftest.py` are untouched (scope fence: WR-01, WR-05, WR-09 stay open).
</verification>

<success_criteria>
- A fresh `FactorPolars` subclass resolves `config.factor_names` during `read()` from the persisted store's non-index data variables.
- A `DLConfig` with `factor_data_strategy="read"` completes `base/model.py`'s full read-path call sequence for a KunQuant factor and a Polars factor in the same list, with no backend-dependent failure and no runtime type branch in the test that proves it.
- Reverting the `read()` override makes both new tests fail with `RuntimeError: Momentum: factor names are not resolved yet.` — the tests fail in the direction of the defect they close.
- `base/factor_polars.py`, `tests/test_factor_polars.py` and `README.md` make no claim the suite does not prove.
- `tests/test_factor_stream.py` stays green (FACTOR-02 no-regression obligation).
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | What it establishes |
|----------|------|---------------------|
| `base/factor_polars.py` — `FactorPolars.read()` | source | D-05 name resolution on the read path, symmetric with `cal()`; closes VERIFICATION.md gap 1 / CR-01 |
| `tests/test_factor_polars.py::test_factor_names_resolve_from_the_persisted_store_on_read` | test | Per-backend lock: save -> fresh instance -> read -> names |
| `tests/test_factor_hierarchy.py::test_kunquant_and_polars_factors_are_interchangeable_under_the_read_strategy` | test | Live D-03 lock on `factor_data_strategy="read"` across both backends |
| `base/factor_polars.py` docstring, `tests/test_factor_polars.py` docstring, `README.md:34-40` | docs | Shipped prose that matches the code and names its lock |

<output>
Create `.planning/phases/03-factor-computation-kunquant-polars/03-06-SUMMARY.md` when done.

Record in it: whether the `read()` resolution was left guarded on `factor_names is None` (and why), the exact pre-fix failure signal reproduced for each new test, and confirmation that `base/factor.py` and `tests/conftest.py` were not touched.
</output>
