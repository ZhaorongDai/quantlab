---
phase: 03-factor-computation-kunquant-polars
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - base/config.py
  - base/factor.py
  - tests/test_factor_hierarchy.py
autonomous: true
requirements: [FACTOR-01, FACTOR-04]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A shared abstract `Factor` base class exists in `base/factor.py` and `FactorKunQuant` is a subclass of it (D-03)"
    - "The four concrete subclasses this plan can actually exercise behave exactly as before: `Alpha101SpotKline`/`Alpha158SpotKline` still return identical `.cal()` results (03-01's end-to-end tests re-run unmodified), and `SpotReturn`/`SpotBinaryReturn` still construct with `config.factor_names` resolved eagerly by the hoisted `config` setter and `_maybe_resolve_factor_names()` hook"
    - "`Alpha101Stock` is deliberately NOT exercised by this plan: it crashes at graph-construction time on the pre-existing missing-`amount` defect that 03-03 fixes, so its behaviour preservation is asserted in 03-03, not here. This plan asserts only that it still subclasses `FactorKunQuant` and still resolves the full `base/model.py` call surface"
    - "`Factor` carries every one of the seven members `base/model.py` invokes on a factor object, so a non-KunQuant factor backend can be dropped into `DLConfig.factors` with zero edits to `base/model.py` (D-03 interchangeability)"
    - "Every KunQuant-streaming-specific member (`init_stream`, `cal_stream`, `_make`, `_make_stream`, `_get_factor_func`, `_to_xarray_dataset`, `_stream_context`, `_buffer_name_to_id`) stays on `FactorKunQuant` and is absent from `Factor` (D-07)"
    - "`FactorConfig.mode` is read only through `FactorKunQuant` overrides — `Factor`'s own `_auto_filter`/`symbols`/`num_symbols` never touch `.mode`, so a `PolarsFactorConfig` without a `mode` field cannot raise `AttributeError` at runtime"
    - "`DLConfig.factors`/`.labels` and `MLConfig.factors`/`.labels` are typed against the shared `Factor` type, not `FactorKunQuant`"
    - "`Factor.__init__` assigns `self.config` before `self.data_backend`, and no method reachable from the `config` setter reads `self.data_backend` — both asserted by source introspection, so reversing the order or introducing such a read fails at test time rather than raising `AttributeError` on every future factor construction (Hazard 1)"
    - "No public method of `Factor` or `FactorKunQuant` accepts or returns a bare pandas/polars DataFrame — `xr.Dataset` is the only exchange type at the public boundary (FACTOR-04)"
    - "`Alpha158SpotKline.cal()`/`Alpha101SpotKline.cal()` still return an `xr.Dataset` after the refactor — 03-01's end-to-end tests pass unchanged"
  artifacts:
    - path: "base/factor.py"
      provides: "Factor(ABC) shared base + FactorKunQuant(Factor) refactored subclass"
      contains: "class FactorKunQuant(Factor)"
    - path: "base/config.py"
      provides: "BaseFactorConfig / FactorConfig(BaseFactorConfig) / PolarsFactorConfig(BaseFactorConfig) split and the DLConfig/MLConfig type widening"
      contains: "class PolarsFactorConfig(BaseFactorConfig)"
    - path: "tests/test_factor_hierarchy.py"
      provides: "Hierarchy-shape, mode-isolation, `__init__`-ordering, boundary-contract and call-surface assertions locking D-03/D-07/FACTOR-04/Hazard 1, plus the `SpotReturn`/`SpotBinaryReturn` construction regression that is `label/spot.py`'s only test coverage"
      contains: "issubclass"
  key_links:
    - from: "base/factor.py:FactorKunQuant"
      to: "base/factor.py:Factor"
      via: "class inheritance"
      pattern: "class FactorKunQuant\\(Factor\\)"
    - from: "base/config.py:DLConfig.factors"
      to: "base/factor.py:Factor"
      via: "TYPE_CHECKING forward reference"
      pattern: "list\\[\"Factor\"\\]"
    - from: "base/config.py:FactorConfig"
      to: "base/config.py:BaseFactorConfig"
      via: "dataclass inheritance, kw_only=True"
      pattern: "class FactorConfig\\(BaseFactorConfig\\)"
---

<objective>
Deliver D-03, the phase's core architectural requirement and the user's own words: "我需要这两个因子类可以无缝替换" (I need these two factor classes to be seamlessly interchangeable).

Extract a shared abstract `Factor` base out of `base/factor.py:FactorKunQuant`, make `FactorKunQuant` a subclass of it, split `FactorConfig` into a shared `BaseFactorConfig` plus KunQuant and Polars siblings, and widen `DLConfig`/`MLConfig`'s factor/label type hints to the shared type. This is a **refactor, not a rewrite** — behaviour must not change for any existing subclass, and `base/model.py` must require zero edits (that zero-edit fact is the actual proof of interchangeability, not the type hint).

Purpose: D-03; unblocks 03-04's `FactorPolars(Factor)` sibling. Also delivers FACTOR-04's boundary contract as an enforced, automated assertion rather than a claim.
Output: `Factor(ABC)` in `base/factor.py`, the three-way config dataclass split in `base/config.py`, and a hierarchy test file that fails loudly if a later change re-couples the shared base to KunQuant.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-factor-computation-kunquant-polars/03-CONTEXT.md
@.planning/phases/03-factor-computation-kunquant-polars/03-PATTERNS.md
@base/factor.py
@base/config.py
</context>

<interfaces>
**03-PATTERNS.md §2 holds the byte-accurate current source of every method below, with on-disk line numbers. Read it rather than reconstructing current state from memory.** The authoritative classification table is 03-RESEARCH.md "Pattern 1: Proposed Class Hierarchy". The three hazards below are the ones a naive hoist gets wrong.

**Hazard 1 — `__init__` ordering (03-PATTERNS.md §2, watch-item 1).** `FactorKunQuant.__init__` (lines 22-27) runs `self.config = config` FIRST, which fires the `config` property setter, which calls `self._get_factor_names()` and `self._reset_dataset_config()` — all BEFORE `self.data_backend = XrBackend()` is assigned. `Factor.__init__` must preserve this exact order (`self.config = config`, then `self.data_backend = XrBackend()`). No hoisted setter path currently touches `self.data_backend`; do not introduce one.

**Hazard 2 — `mode` is read by THREE members, not one (03-PATTERNS.md §2b, watch-item 2).** `_auto_filter()` (lines 32-42), `num_symbols` (lines 83-90) and `symbols` (lines 99-107) all branch on `self.config.mode`. `mode` moves to the KunQuant-only `FactorConfig`; `PolarsFactorConfig` has no such field. A naive hoist of any of these three onto `Factor` produces `AttributeError: 'PolarsFactorConfig' object has no attribute 'mode'` at RUNTIME, not at type-check time. All three must be split: the batch-shaped body on `Factor`, the mode-branching wrapper on `FactorKunQuant`. `super().<name>` works for property getters as well as ordinary methods (verified on this interpreter), so each override can be a two-line delegate.

**Hazard 3 — `_get_factor_names()` resolution timing.** The `config` setter's three lines `if self._config.factor_names is None: self._config.factor_names = self._get_factor_names()` become a call to a new overridable hook `Factor._maybe_resolve_factor_names()` whose default body is exactly those three lines. `FactorKunQuant` inherits the eager default unchanged (zero behaviour change for every existing subclass); 03-04's `FactorPolars` will override it to a no-op because resolving Polars factor names requires a disk read (D-05). Introduce the hook here even though nothing overrides it yet — it is the seam 03-04 depends on.

**Config split (03-RESEARCH.md "Proposed config dataclass split", 03-PATTERNS.md §3).** `BaseFactorConfig` takes `window`, `dataset`, `file_path`, `factor_names`, `start_date`, `end_date`, `symbols`, `kwargs`, `name` and the single `to_dict()` definition. `FactorConfig(BaseFactorConfig)` adds `mode`, `data_columns`, `njobs`. `PolarsFactorConfig(BaseFactorConfig)` adds nothing (D-04..D-07's scope needs no extra field). All three carry `@dataclass(kw_only=True)`.

**Why `kw_only=True` is a zero-behaviour-change edit:** 03-PATTERNS.md §3's construction-site audit found every `FactorConfig(...)` construction in the repository is 100% keyword-argument — `config/__init__.py:127`, `:154`, `:184`, `test.py:27`, and `utils/module.py:18`'s `FactorConfig(**config)` dict-splat. Zero positional constructions exist. `kw_only=True` is required because otherwise `FactorConfig`'s mandatory `mode`/`data_columns` would have to follow `BaseFactorConfig`'s defaulted fields, which plain dataclasses forbid.

**`asdict()` behaviour to preserve (03-PATTERNS.md §3, watch-item 7):** `to_dict()` calls `dataclasses.asdict()`, which recurses into `dataset` (a non-dataclass, passed through by reference); `Factor.get_config()` then overwrites `cfg["dataset"]` with `self.config.dataset.get_config()`. Define `to_dict()` ONCE on `BaseFactorConfig` — do not duplicate it onto the subclasses. Inheriting the fields changes the KEY ORDER of the returned dict (base fields first) but not the key set; verified that no consumer depends on order (`utils/module.py`'s `FactorConfig(**config)` splat and `base/model.py:_save_model`'s JSON dump are both order-independent).

**The `Factor` call surface `base/model.py` requires (03-PATTERNS.md §7 — verified by reading `base/model.py:115-201`).** These seven and only these seven are invoked on a factor/label object, and every one must resolve on `Factor`: `.config` (read + write of `start_date`/`end_date`), `._reset_dataset_config()`, `.cal()` (abstract), `.read()`, `.get_features()`, `.get_labels()`, `._get_factor_names()` (abstract — note `base/model.py` calls the PRIVATE method directly, not the public `get_factor_names()` wrapper), `.get_config()`. `base/model.py` never imports `base/factor.py` and dispatches purely through `match self.config.factor_data_strategy` — no `isinstance` branching. **`base/model.py` must not be edited by this plan.**

**Purity constraint.** `tests/test_extensibility_contract.py:CORE_LAYER_FILES` already includes `base/factor.py` and forbids the non-comment substrings `SpotKlineDataset`, `StockDataset`, `crypto_spot`, `us_equity` in it. The refactored file must stay clean. (03-04 adds `base/factor_polars.py` to that tuple; do not add it here — the file does not exist yet and the test would raise `FileNotFoundError`.)

**Explicitly out of scope (03-RESEARCH.md Open Question 1).** `utils/module.py:18` hardcodes `FactorConfig(**config)` when rebuilding a factor from a saved checkpoint, which will not work for a `PolarsFactorConfig`-backed factor. That path is only reachable from Phase 4's model-checkpoint reload flow and no Phase-3 success criterion touches it. Do NOT add a `Factor.config_cls` ClassVar "to unblock Phase 4" — an unused attribute violates QUAL-02's "no unnecessary code", and Phase 4 should pick its own reload mechanism. Record the forward-flag in the SUMMARY instead.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Split FactorConfig into BaseFactorConfig / FactorConfig / PolarsFactorConfig and widen DLConfig/MLConfig</name>
  <files>base/config.py</files>
  <read_first>base/config.py, config/__init__.py, utils/module.py, .planning/phases/03-factor-computation-kunquant-polars/03-PATTERNS.md</read_first>
  <action>
  Edit `base/config.py` only. Replace the single `@dataclass class FactorConfig` (lines 66-83) with three dataclasses, all decorated `@dataclass(kw_only=True)`:

  `BaseFactorConfig` — fields in this order: `window: int`, `dataset: "Dataset"`, `file_path: str | None = None`, `factor_names: list | None = None`, `start_date: str | None = None`, `end_date: str | None = None`, `symbols: list | None = None`, `kwargs: dict | None = None`, `name: str | None = None`. It carries the one and only `to_dict()` returning `asdict(self)`. Give it a short English docstring stating it holds every field shared by both factor backends.

  `FactorConfig(BaseFactorConfig)` — adds `mode: Literal["stream", "batch"]`, `data_columns: list`, and `njobs: int = 128`. Docstring: these three are KunQuant-specific (`mode` drives the batch/stream branch, `data_columns` names the KunQuant graph inputs, `njobs` sizes `kr.createMultiThreadExecutor`). Keep the class name `FactorConfig` — every existing call site and `utils/module.py` reference it by that name.

  `PolarsFactorConfig(BaseFactorConfig)` — adds no fields (D-04..D-07 need none). Docstring: the Polars backend is batch-only per D-07 so it has no `mode`; factor names are resolved dynamically from the lazyframe schema per D-05 so `factor_names` is expected to be `None` until `cal()` runs.

  Then change the `TYPE_CHECKING` import on line 8 from `from .factor import FactorKunQuant` to `from .factor import Factor`, and change all four annotations — `DLConfig.factors`, `DLConfig.labels`, `MLConfig.factors`, `MLConfig.labels` — from `list["FactorKunQuant"]` to `list["Factor"]` (D-03's explicit blast-radius fix). Keep the import inside the existing `if TYPE_CHECKING:` guard; promoting it to a runtime import would create the one import cycle 03-PATTERNS.md §6 identified.

  Touch nothing else in this file: `DatasetConfig`, `AcquisitionConfig` and `UniverseConfig` are unchanged, and `DLConfig`/`MLConfig`'s other fields keep their current order and defaults.
  </action>
  <acceptance_criteria>
    - `uv run python -c "import dataclasses as d; from base.config import BaseFactorConfig, FactorConfig, PolarsFactorConfig; assert issubclass(FactorConfig, BaseFactorConfig) and issubclass(PolarsFactorConfig, BaseFactorConfig); n=[f.name for f in d.fields(FactorConfig)]; assert {'mode','data_columns','njobs'} <= set(n); p=[f.name for f in d.fields(PolarsFactorConfig)]; assert 'mode' not in p and 'window' in p; print('ok')"` prints `ok`.
    - `uv run python -c "import dataclasses as d; from base.config import DLConfig, MLConfig; ts=[f.type for c in (DLConfig, MLConfig) for f in d.fields(c) if f.name in ('factors','labels')]; assert len(ts)==4; assert all('FactorKunQuant' not in t for t in ts); assert all('Factor' in t for t in ts); print('ok')"` prints `ok`.
    - `uv run python -c "from config import alpha101_config, alpha158_config, spot_label_config; print('ok')"` prints `ok` — the existing keyword-only factories still construct under `kw_only=True`.
    - `uv run pytest tests/ -q` shows zero failures.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/ -q</automated>
  </verify>
  <done>The config layer distinguishes shared factor configuration from KunQuant-only configuration, and the model layer's factor/label type hints name the shared factor type.</done>
</task>

<task type="auto">
  <name>Task 2: Extract Factor(ABC) from FactorKunQuant, behaviour-preserving</name>
  <files>base/factor.py, tests/test_factor_hierarchy.py</files>
  <read_first>base/factor.py, base/data.py, base/model.py, factor/alpha101.py, factor/alpha158.py, label/spot.py, .planning/phases/03-factor-computation-kunquant-polars/03-PATTERNS.md, .planning/phases/03-factor-computation-kunquant-polars/03-RESEARCH.md</read_first>
  <action>
  Restructure `base/factor.py` into two classes in one file: a new `class Factor(ABC)` followed by `class FactorKunQuant(Factor)`. Move, do not rewrite — every hoisted method body must be byte-identical to what 03-PATTERNS.md §2 shows on disk today, including all Chinese docstrings and inline comments (03-PATTERNS.md §8: "for hoisted code, preserve the original Chinese verbatim").

  **Moves onto `Factor` unchanged** (03-PATTERNS.md §2a): `__repr__`, `_reset_dataset_config`, the `import_path`/`class_name`/`num_factors` properties, `read`, `save`, `_get_lazyframe`, `_get_xarray_dataset`, `_get_features`/`get_features`, `_get_labels`/`get_labels`, `get_factor_names`, `get_config`.

  **`Factor.__init__(self, config: "BaseFactorConfig")`** assigns `self.config = config` and then `self.data_backend = XrBackend()`, in that order (Hazard 1). `FactorKunQuant.__init__(self, config: FactorConfig)` calls `super().__init__(config)` and then sets its three KunQuant-only attributes `self._stream_context = None`, `self._lib = None`, `self._buffer_name_to_id = dict()`.

  **`config` property + setter hoist to `Factor`** with the docstring and Chinese comments preserved verbatim, except that the three lines resolving `factor_names` are replaced by a call to a new method `self._maybe_resolve_factor_names()`. Define `_maybe_resolve_factor_names(self) -> None` on `Factor` with exactly those three replaced lines as its body (Hazard 3). Do not override it on `FactorKunQuant` — inheriting the eager default is what makes the refactor behaviour-preserving.

  **Three-way `mode` split (Hazard 2).** On `Factor`, define `_auto_filter()` with the current body's INNER block only — the `filter_by_date` call plus the `if self.config.symbols is not None: filter_by_symbol` block — with no `mode` check; define `num_symbols` returning `self.config.dataset.num_symbols` and `symbols` returning `self.config.dataset.symbols` (the current `"batch"` branches). On `FactorKunQuant`, override all three to restore today's exact behaviour: `_auto_filter` calls `super()._auto_filter()` only when `self.config.mode == "batch"`; `num_symbols` and `symbols` return `super().num_symbols`/`super().symbols` when mode is `"batch"`, return `len(self.config.dataset.config.symbols)`/`list(self.config.dataset.config.symbols)` when mode is `"stream"`, and otherwise raise the existing `ValueError(f"mode {self.config.mode} is not supported")` with its message unchanged.

  **Abstract members on `Factor`:** `_get_factor_names(self) -> tuple[str, ...]` and `cal(self) -> Self`, both `@abstractmethod`. `cal()` being abstract on the shared base is the mechanism that makes `base/model.py`'s `factor.cal()` polymorphic across backends.

  **Stays on `FactorKunQuant`, unmoved** (D-07, 03-PATTERNS.md §2c): `init_stream`, `cal_stream`, `_make`, `_make_stream`, `_to_xarray_dataset`, the concrete `cal`, and the abstract `_get_factor_func`. Do not hoist any of them and do not import `KunQuant.*` inside `Factor`'s own methods.

  **Imports.** `Factor` needs `pandas`, `polars`, `xarray`, `XrBackend`, `Date`, `Timer`, `BaseFactorConfig`, and `ABC`/`abstractmethod`/`Literal`/`Self`. `FactorKunQuant` additionally needs the existing `KunQuant` imports and `numpy` and `FactorConfig`. Keep all imports at module top as they are today. Do not introduce any reference to a concrete `Dataset` subclass name or a market literal — `base/factor.py` is under the `tests/test_extensibility_contract.py` purity check.

  **Cover the label classes, which nothing tests today.** 03-01's end-to-end tests exercise `Alpha101SpotKline`/`Alpha158SpotKline` only; no test in the repository touches `label/spot.py`. `SpotReturn` and `SpotBinaryReturn` are direct `FactorKunQuant` subclasses, so their construction runs the hoisted `config` setter and the brand-new `_maybe_resolve_factor_names()` hook — exactly the code paths this task rewrites. Add one test, `test_label_classes_construct_through_the_refactored_hierarchy`, to `tests/test_factor_hierarchy.py` (03-01's scaffold): build a `SpotReturn` against the `spot_kline_zarr` fixture with a `FactorConfig` carrying `factor_names=None` and `mode="batch"`, and assert that after construction `config.factor_names` is a non-empty tuple/list — i.e. the setter resolved names eagerly, as it did before the refactor. In the same test assert, for both `SpotReturn` and `SpotBinaryReturn`, that each is an `issubclass` of `FactorKunQuant` and therefore of `Factor`, and that every name in the `base/model.py` call-surface tuple resolves via `dir()`. Construction only — do not call `.cal()`; label computation is out of this plan's scope and would add a KunQuant compile to the wave-2 runtime. The docstring must state that this is the only coverage `label/spot.py` has and that it exists because the refactor rewrites its construction path.
  </action>
  <acceptance_criteria>
    - `uv run python -c "import inspect; from base.factor import Factor, FactorKunQuant; assert inspect.isabstract(Factor); assert issubclass(FactorKunQuant, Factor); print('ok')"` prints `ok`.
    - `uv run python -c "from base.factor import Factor, FactorKunQuant as K; own=set(K.__dict__); base=set(Factor.__dict__); assert {'init_stream','cal_stream','_make','_make_stream','_to_xarray_dataset','_get_factor_func'} <= own; assert not ({'init_stream','cal_stream','_make','_make_stream','_to_xarray_dataset','_get_factor_func'} & base); print('ok')"` prints `ok`.
    - `uv run python -c "import inspect; from base.factor import Factor; src=''.join(inspect.getsource(m) for n,m in vars(Factor).items() if inspect.isfunction(m)); assert '.mode' not in src; print('ok')"` prints `ok` — no `Factor` method reads `config.mode` (Hazard 2).
    - `uv run pytest tests/test_factor_kunquant.py -x -q` shows 2 passed — 03-01's end-to-end `Alpha158SpotKline.cal()`/`Alpha101SpotKline.cal()` proofs still hold, unmodified, through the new hierarchy. This is the behaviour-preservation gate.
    - `uv run python -c "from label.spot import SpotReturn, SpotBinaryReturn; from base.factor import Factor, FactorKunQuant; SURFACE=('config','_reset_dataset_config','cal','read','get_features','get_labels','_get_factor_names','get_config'); assert all(issubclass(c, FactorKunQuant) and issubclass(c, Factor) for c in (SpotReturn, SpotBinaryReturn)); assert all(n in dir(c) for c in (SpotReturn, SpotBinaryReturn) for n in SURFACE); print('ok')"` prints `ok` — the two label classes, which no test covered before this plan, still subclass the refactored hierarchy and still expose the full call surface.
    - `uv run pytest tests/test_factor_hierarchy.py -k label -q` shows 1 passed — a real `SpotReturn` instantiated against the `spot_kline_zarr` fixture still has `config.factor_names` resolved eagerly by the hoisted setter, proving `_maybe_resolve_factor_names()`'s default preserved the old behaviour for label classes too.
    - `uv run pytest tests/test_extensibility_contract.py -q` shows all passed — `base/factor.py` stayed pure.
    - `git diff base/model.py` is empty — `base/model.py` was not edited.
    - `uv run pytest tests/ -q` shows zero failures.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_factor_kunquant.py tests/test_factor_hierarchy.py tests/test_extensibility_contract.py -x -q</automated>
  </verify>
  <done>`Factor` is the shared abstract base, `FactorKunQuant` is one implementation of it, the two crypto-spot factor classes still compute identical values, the two previously-untested label classes still construct with eagerly-resolved factor names, and `base/model.py` needed zero edits.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the hierarchy shape, mode isolation and xarray-only boundary with automated assertions</name>
  <files>tests/test_factor_hierarchy.py</files>
  <read_first>tests/test_factor_hierarchy.py, tests/test_extensibility_contract.py, base/factor.py, base/config.py, base/model.py</read_first>
  <action>
  Extend `tests/test_factor_hierarchy.py` (created as a scaffold in 03-01 — keep its existing `base/model.py` purity test) with the assertions below. Follow the file conventions already established in `tests/test_extensibility_contract.py`: module-level constants for the things being checked, one behaviour per test, and a docstring on each test naming the requirement or decision it proves.

  `test_factor_kunquant_subclasses_shared_factor_base` — `Factor` is abstract (`inspect.isabstract`), `FactorKunQuant` is a subclass of it, and `cal` and `_get_factor_names` are in `Factor.__abstractmethods__`. Proves D-03's hierarchy exists as specified.

  `test_streaming_members_stay_on_the_kunquant_subclass` — every name in a module-level tuple of the KunQuant-only members (`init_stream`, `cal_stream`, `_make`, `_make_stream`, `_to_xarray_dataset`, `_get_factor_func`) is present in `FactorKunQuant.__dict__` and absent from `Factor.__dict__`. Proves D-07 — the shared base imposes no streaming obligation on a batch-only backend.

  `test_shared_factor_base_never_reads_config_mode` — collect the source of every function defined directly in `vars(Factor)` via `inspect.getsource`, and assert the attribute access `.mode` appears in none of it. This is the runtime-`AttributeError` guard for the `PolarsFactorConfig`-has-no-mode hazard; a grep over the whole file would be meaningless because `FactorKunQuant`'s legitimate overrides do read it.

  `test_factor_base_carries_the_full_base_model_call_surface` — for each name in a module-level tuple `("config", "_reset_dataset_config", "cal", "read", "get_features", "get_labels", "_get_factor_names", "get_config")`, assert `hasattr(Factor, name)`. Docstring must record that this tuple is the exhaustive set `base/model.py:115-201` invokes on a factor or label object (03-PATTERNS.md §7), so any backend satisfying `Factor` is droppable into `DLConfig.factors` unchanged.

  `test_model_configs_are_typed_against_the_shared_factor_base` — for `DLConfig` and `MLConfig`, take `dataclasses.fields(cls)` and for the `factors` and `labels` fields assert the raw annotation string contains `Factor` and does not contain `FactorKunQuant`. (Verified: `dataclasses.fields()` exposes these as the literal source strings.)

  `test_factor_init_assigns_config_before_the_storage_backend` — the runtime guard for Hazard 1, written in the same source-introspection style as the `mode` guard above, because nothing else fails if an executor reverses the two lines. Two assertions in one test. First: take `inspect.getsource(Factor.__init__)` and assert the index of the `self.config` assignment is strictly lower than the index of the `self.data_backend` assignment, so the `config` setter is still the first thing that fires. Second: concatenate `inspect.getsource` of every function reachable from the `config` setter — `Factor.config.fset` (reach it via `vars(Factor)["config"].fset`), `Factor._maybe_resolve_factor_names`, `Factor._reset_dataset_config`, and the `FactorKunQuant` override of any of those three that exists in `vars(FactorKunQuant)` — and assert the storage-backend attribute name appears nowhere in that concatenation. `Factor.__init__` and `FactorKunQuant.__init__` are deliberately excluded from the second assertion: they are the assignment site, not a setter-reachable path. The docstring must state that reversing the two `__init__` lines, or making any setter-reachable method read the storage backend, produces an `AttributeError` on every factor construction, and that today no such path exists — this test is what keeps it that way.

  `test_public_factor_api_exchanges_only_xarray_datasets` — for both `Factor` and `FactorKunQuant`, iterate the public methods named in a module-level tuple (`cal`, `read`, `save`, `get_features`, `get_labels`, `get_factor_names`, `get_config`) and assert `"DataFrame"` does not appear in `str(inspect.signature(method))`. Additionally assert `get_features` and `get_labels` annotate their return as `xr.Dataset`. The docstring must state that private helpers are deliberately excluded because `Factor._get_lazyframe()` legitimately returns a `pl.LazyFrame` as an internal type — FACTOR-04 constrains the module boundary, not internals.
  </action>
  <acceptance_criteria>
    - `uv run pytest tests/test_factor_hierarchy.py -q` shows 9 passed (the 03-01 scaffold test, Task 2's label-class regression, plus the seven added here).
    - Each added test has a docstring naming the requirement, decision ID or hazard it proves (FACTOR-04, D-03, D-07, Hazard 1, Hazard 2).
    - `test_shared_factor_base_never_reads_config_mode` fails if `Factor._auto_filter` is edited to branch on `self.config.mode` — confirm by temporarily adding that branch, observing the failure, and reverting.
    - `test_factor_init_assigns_config_before_the_storage_backend` fails if the two assignments in `Factor.__init__` are swapped — confirm by temporarily swapping them, observing the failure, and reverting. Without this mutation check the Hazard 1 ordering is prescribed but unasserted, exactly as the Hazard 2 `mode` split would be without its own guard.
    - `uv run pytest tests/ -q` shows zero failures.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_factor_hierarchy.py -q</automated>
  </verify>
  <done>D-03's hierarchy, D-07's streaming isolation, the mode-isolation hazard, the `__init__`-ordering hazard and FACTOR-04's xarray-only public boundary are all enforced by automated tests that fail loudly if a future change re-couples the shared base to KunQuant or reorders its construction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shared `Factor` base <-> backend-specific subclasses | The refactor's contract surface: anything hoisted incorrectly becomes a runtime failure in a sibling backend that does not exist yet |
| Config dataclass <-> persisted checkpoint JSON | `Factor.get_config()` output is serialized by `base/model.py:_save_model` and re-splatted by `utils/module.py` — the key SET must survive the dataclass split |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-02-01 | Tampering (contract integrity) | `base/factor.py:Factor._auto_filter`/`symbols`/`num_symbols` | high | mitigate | Hoisting a `config.mode` read onto the shared base would raise `AttributeError` only at runtime, only for the not-yet-written Polars backend. Task 2 splits all three members; Task 3's `test_shared_factor_base_never_reads_config_mode` asserts on `inspect.getsource` of `Factor`'s own functions so the regression is caught at test time instead |
| T-03-02-02 | Tampering (behaviour drift) | every existing `FactorKunQuant` subclass | high | mitigate | The refactor could silently change factor values. 03-01's end-to-end `Alpha158SpotKline.cal()`/`Alpha101SpotKline.cal()` tests are re-run unmodified as Task 2's gating verify; `git diff base/model.py` being empty is asserted separately |
| T-03-02-04 | Tampering (construction-order integrity) | `base/factor.py:Factor.__init__` | medium | mitigate | The `config` setter fires before `self.data_backend` exists; an executor reordering the two lines, or a future edit making a setter-reachable method read the storage backend, would raise `AttributeError` on every factor construction. Task 3's `test_factor_init_assigns_config_before_the_storage_backend` asserts both the source ordering and the absence of any setter-reachable reference, with a mutation check in the acceptance criteria |
| T-03-02-03 | Denial of Service (import cycle) | `base/config.py` -> `base/factor.py` | medium | mitigate | Promoting the `Factor` import out of `if TYPE_CHECKING:` would create the single import cycle 03-PATTERNS.md §6 identified. Task 1's action requires the guard be preserved; `uv run python -c "from config import alpha101_config..."` in the acceptance criteria fails immediately if a cycle is introduced |
| T-03-02-SC | Tampering | npm/pip/cargo installs | n/a | accept | Zero new packages installed; 03-RESEARCH.md records the Package Legitimacy Audit as "Not applicable" for this phase. No `[ASSUMED]`/`[SUS]` package exists, so no legitimacy checkpoint applies |

**Register is deliberately minimal, not omitted:** this plan is a pure in-process class-hierarchy refactor over already-local data — no credentials, no network, no new external integration (03-RESEARCH.md Security Domain).
</threat_model>

<verification>
Run `uv run pytest tests/ -q` — the whole suite green proves the refactor is behaviour-preserving across Phase 1, Phase 2 and 03-01's factor tests.

Then confirm the two facts that carry D-03: `git diff base/model.py` is empty, and `uv run pytest tests/test_factor_hierarchy.py -q` shows 9 passed.
</verification>

<success_criteria>
- `Factor(ABC)` exists in `base/factor.py` and `FactorKunQuant` subclasses it, with every existing concrete factor and label class behaving identically (03-01's `.cal()` tests pass unchanged).
- No method defined on `Factor` reads `config.mode`; all three mode-branching members are overridden on `FactorKunQuant`.
- `Factor.__init__` assigns `self.config` before the storage backend, and no setter-reachable method reads the storage backend — both asserted, not merely prescribed (Hazard 1).
- `SpotReturn`/`SpotBinaryReturn`, previously untested, are covered by a construction regression proving eager factor-name resolution survived the refactor.
- Every KunQuant-streaming member stays on `FactorKunQuant` (D-07).
- `DLConfig`/`MLConfig` factor and label fields are typed against `Factor`.
- `base/model.py` is unedited.
- The xarray-only public boundary (FACTOR-04) is asserted, not merely claimed.
</success_criteria>

<output>
Create `.planning/phases/03-factor-computation-kunquant-polars/03-02-SUMMARY.md` when done. Record in it the forward-flag for Phase 4: `utils/module.py:18`'s hardcoded `FactorConfig(**config)` cannot rebuild a `PolarsFactorConfig`-backed factor from a checkpoint, and Phase 4 must choose a reload mechanism (03-RESEARCH.md Open Question 1).
</output>
