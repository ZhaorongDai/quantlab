---
status: testing
phase: 03.1-index-historical-constituents
source: [03.1-VERIFICATION.md]
started: 2026-09-07T22:15:00Z
updated: 2026-09-07T22:15:00Z
---

## Current Test

number: 1
name: P-1 — 被覆盖窗口截断的答案不得看起来完整
expected: |
  裁定 `estimate_dense_panel()` 是否属于本条 prohibition 所管辖的"答案"。

  verifier 判断(非权威):对两个成员查询 HELD;对 `estimate_dense_panel` NOT HELD ——
  对 `get_symbols_in_range` 已拒绝的窗口 `('sp500_constituent','1900-01-01','2020-01-01')`,
  它仍返回 `symbols=766, trading_days=30240`,且该结果会 gate 一次真实 fetch。

  需人工:一个 sizing estimate 算不算 membership answer,是范围界定问题,不是测试结果。
awaiting: user response

## Tests

### 1. P-1 — 被覆盖窗口截断的答案不得看起来完整
expected: 裁定 `estimate_dense_panel()` 是否属于本条 prohibition 管辖的"答案"。verifier 非权威判断:两个成员查询 HELD;`estimate_dense_panel` NOT HELD(对已被拒绝的窗口返回 `symbols=766, trading_days=30240`,并 gate 真实 fetch)。
result: [pending]

### 2. P-2 — 不得记载比变更日志实际支持更宽的覆盖窗口
expected: 权衡两处文档缺陷是否触犯本条。verifier 非权威判断:被点名的失败模式 HELD(两份文档现在都写明两个查询都会 raise);但 `acquisition/universe.py:1556-1558` 陈述了一个可证伪的不变量(905 行 `sp500_constituent` 中 74 行早于覆盖起点,最早 1957-03-04),且 `example/constituent.md:386,388` 在一个标注为真实运行的代码块里引用了过期报错文本。需人工:一条虚假的 load-bearing docstring 不变量,算不算"暗示了更宽的覆盖窗口"。
result: [pending]

### 3. P-3 — 不得引入绕过注册表的按类别特例
expected: 确认注册表仍是边界的唯一来源。verifier 非权威判断:HELD —— 两个查询体内均无类别字面量(仅注释中出现)。附带保留:注册行为本身是唯一未被校验的输入(WR-05)。需人工:judgment tier,记录的结论是判断而非测试结果。
result: [pending]

## Summary

total: 3
passed: 0
issues: 0
pending: 3
skipped: 0
blocked: 0

## Gaps
