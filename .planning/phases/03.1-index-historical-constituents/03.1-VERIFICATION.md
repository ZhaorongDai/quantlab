---
phase: 03.1-index-historical-constituents
verified: 2026-09-07T19:05:00Z
status: passed
score: 11/11 must-haves verified
covered_files:
  - ".planning/REQUIREMENTS.md"
  - ".planning/phases/03.1-index-historical-constituents/03.1-01-PLAN.md"
  - ".planning/phases/03.1-index-historical-constituents/03.1-01-SUMMARY.md"
  - ".planning/phases/03.1-index-historical-constituents/03.1-02-PLAN.md"
  - ".planning/phases/03.1-index-historical-constituents/03.1-02-SUMMARY.md"
  - ".planning/phases/03.1-index-historical-constituents/03.1-03-PLAN.md"
  - ".planning/phases/03.1-index-historical-constituents/03.1-03-SUMMARY.md"
  - ".planning/phases/03.1-index-historical-constituents/03.1-04-PLAN.md"
  - ".planning/phases/03.1-index-historical-constituents/03.1-04-SUMMARY.md"
  - ".planning/phases/03.1-index-historical-constituents/03.1-05-PLAN.md"
  - ".planning/phases/03.1-index-historical-constituents/03.1-05-SUMMARY.md"
  - ".planning/phases/03.1-index-historical-constituents/03.1-REVIEW-FIX.md"
  - ".planning/phases/03.1-index-historical-constituents/03.1-REVIEW.md"
  - ".planning/phases/03.1-index-historical-constituents/03.1-SECURITY.md"
  - ".planning/phases/03.1-index-historical-constituents/03.1-UAT.md"
  - ".planning/phases/03.1-index-historical-constituents/03.1-VALIDATION.md"
  - "README.md"
  - "acquisition/universe.py"
  - "base/config.py"
  - "base/constituent.py"
  - "base/data.py"
  - "config/__init__.py"
  - "dataset/cleaning.py"
  - "dataset/constituent.py"
  - "enums/data.py"
  - "example/constituent.md"
  - "ingest_tiingo.py"
  - "ingest_us_equity.py"
  - "tests/test_constituent_panel.py"
  - "tests/test_dataset_hierarchy.py"
  - "tests/test_ingest_tiingo_universe_wiring.py"
  - "tests/test_ndx_constituent.py"
  - "tests/test_universe.py"
  - "tests/test_volume_guard.py"
  - "utils/cli.py"
covered_digest: "v1:sha256:5144ed338319f39d44561763da5902c285043fb8c0f4f6c01bdce41731861fbe"
behavior_unverified: 0
overrides_applied: 0
unverified_prohibitions: 0
re_verification:
  previous_status: human_needed
  previous_score: 11/11
  gaps_closed:
    - "Prohibition P-1 (DATA-05, judgment tier) — was `unverified`/`flagged`; adjudicated HELD by human-in-the-loop UAT (`03.1-UAT.md`). The ruling's decisive ordering claim was re-verified independently in the source this run."
    - "Prohibition P-2 (DATA-05, judgment tier) — was `unverified`/`flagged`; adjudicated HELD. WR-03/WR-04 confirmed real but recorded as debt on a different axis (change-log SHAPE, not coverage WIDTH)."
    - "Prohibition P-3 (DATA-05, judgment tier) — was `unverified`/`flagged`; adjudicated HELD and reclassified from judgment to executable fact. Re-verified this run by grep over the whole module."
    - "`coincidental_reliance_items` (WR-05) — the undeclared precondition SC-3 rested on. Closed by `525c332`: two parametrised registry-contract tests. Mutation-checked this run — the test rejects a missing, an unpadded (`2010-1-1`) and an ISO-basic (`20070201`) constant, and admits only the canonical form."
  gaps_remaining: []
  regressions: []
advisory:
  - finding: >-
      WR-02 — `estimate_dense_panel` remains a THIRD copy of the membership-window
      predicate and does not route through `_assert_within_coverage`. **The P-1 ruling
      explicitly did NOT close this**; it held that the prohibition does not govern a
      sizing dict, while recording that the safety conclusion rests on CALLER ORDERING
      and that the method is public with no precondition in its docstring.
      **New evidence this run, which NARROWS the ordering argument:** the ordering that
      makes the estimator unreachable was verified only for `ingest_us_equity.py`
      (line 352 `resolve_symbols(..., mode="in_range")` precedes line 403
      `_print_estimate(catalog, args, symbols)`). It does NOT extend to the other two
      front doors. `ingest_tiingo.py` and `ingest_alpaca.py` resolve membership with
      `mode="as_of"` on `args.as_of_date`, but size the fetch from `_sizing_window(args)`
      — i.e. `args.start_date`, which no membership query ever sees. Driven through the
      real shared helpers this run: `resolve_symbols(universe='sp500',
      as_of_date='2020-01-01')` → 522 symbols, no raise; then `volume_pricing` returns
      the live catalog with window `1900-01-01..2020-01-01`; then
      `estimate_dense_panel('sp500_constituent','1900-01-01','2020-01-01')` →
      `symbols=766, trading_days=30240`, while `get_symbols_in_range` on the IDENTICAL
      window raises. `assert_dense_panel_fits` on a pre-coverage window returns `None`
      (admits).
    category: architectural
    reason: >-
      Deliberately deferred; does not falsify SC-3, which governs a membership QUERY —
      the estimator returns counts (`symbols`/`trading_days`/`dense_cells`/`density`)
      and never returns tickers, which is the human ruling's primary and
      ordering-independent basis. Reported, not blocking. Resolves by routing
      `estimate_dense_panel` through `_assert_within_coverage`, or by documenting the
      precondition on the public method.
    evidence_status: "reproduced by verifier probe through the real CLI helper chain; no failing test in repo"
  - finding: >-
      WR-03 — `acquisition/universe.py:1556-1558` states a provably false load-bearing
      invariant: "every membership interval already starts at or after its own coverage
      start". Re-measured against `data/data/reference/universe.parquet` this run:
      **74 of 905** `sp500_constituent` rows start before 1976-07-01, earliest
      `1957-03-04`. The raise-not-clamp conclusion survives only because all 74 are
      open-ended (74/74 `end_date IS NULL`) — a data-dependent accident, not the stated
      reason. A future backfill of a CLOSED pre-1976 interval would silently invalidate
      the argument.
    category: other
    reason: >-
      Documentation accuracy in a module that treats stated invariants as contracts.
      Adjudicated NOT a P-2 violation (it misstates the change log's SHAPE, not its
      WIDTH), but the UAT ruling explicitly requires it be booked separately at a
      severity above ordinary doc debt.
    evidence_status: "reproduced by verifier probe against the live table; no failing test in repo"
  - finding: >-
      WR-01 — `get_symbols_in_range` answers an INVERTED window with a silent `[]`.
      Re-measured this run: `get_symbols_in_range('sp500_constituent','2020-01-01',
      '1900-01-01')` → `[]`. The coverage guard passes (left edge is inside coverage),
      then the overlap predicate matches nothing, and `[]` is documented in this module
      as a legitimate answer.
    category: other
    reason: >-
      New scope relative to the closed gap (argument ordering, not the coverage guard).
      Resolves by a `start_date > end_date` check beside the other two validations.
    evidence_status: "reproduced by verifier probe; no failing test in repo"
  - finding: >-
      WR-04 — `example/constituent.md:386,388` still quotes the pre-delta error text in
      a block labelled 实跑输出, omitting the `{field}={date!r} precedes it.` fragment
      this phase added. Confirmed still present this run.
    category: other
    reason: "Documentation accuracy in the phase's own reference example."
    evidence_status: "none provided (documentation finding, no red test)"
  - finding: >-
      WR-06 — the four original range-coverage tests use bare `pytest.raises(ValueError)`
      with no `match=`, while `get_symbols_in_range` raises `ValueError` for four
      distinct reasons.
    category: other
    reason: >-
      Test-strength defect, not a behaviour defect: the underlying behaviour was
      established independently by execution against the live table in both this run and
      the previous one.
    evidence_status: "none provided (test-quality finding, no red test)"
  - finding: >-
      The phase carries `**Mode:** mvp` but the ROADMAP goal is not a valid User Story
      (`user-story.validate` → `valid: false`, all three slots reported missing).
    category: other
    reason: >-
      Format discrepancy predating this run. Verification proceeded against the four
      explicit ROADMAP Success Criteria, which are the contract; noted so it is not
      mistaken for a passing MVP-format check.
    evidence_status: "none provided (format finding)"
---

# Phase 03.1: Index Historical Constituents Data Layer — Verification Report

**Phase Goal:** Users can obtain daily point-in-time membership panels for the S&P 500 and the Nasdaq-100 as `xarray.Dataset`, so any downstream layer can mask its universe per day without survivorship bias.
**Verified:** 2026-09-07T19:05:00Z
**Status:** passed — 11/11 must-haves verified; all three judgment-tier prohibitions human-adjudicated HELD; no human verification items remain
**Re-verification:** Yes — narrow refresh over `d02aa09..HEAD`. Previous verdict: `human_needed`, 11/11.

## What this refresh actually re-verified

The prior run (`d02aa09`) already established all 11 must-haves and stated the phase goal is achieved. It returned `human_needed` for exactly one reason: three judgment-tier prohibitions in `03.1-05-PLAN.md` carried `status: unverified, flagged: true`, and the fail-closed prohibition policy forbids absorbing a flagged prohibition into a green verdict. This refresh therefore verified two things and re-checked for regression, rather than re-deriving the phase from scratch.

**Scope of change, measured not assumed.** Five commits landed after `d02aa09`. `git diff --name-only d02aa09..HEAD -- . ':(exclude).planning'` returns exactly **one file: `tests/test_universe.py`**, and its diff is `74 insertions(+), 0 deletions(-)` — purely additive, test-only. Four of the five commits touch only `.planning/`. No production file changed; `base/`, `dataset/`, `config/` and `enums/` remain byte-identical to `158a5b0`.

### Change 1 — the three prohibitions are adjudicated

`03.1-UAT.md` is `status: complete`, 3 passed, 0 issues, 0 pending. Each ruling carries its reasoning. Two of the three DISAGREE with the prior run's non-authoritative verifier judgments, and the disagreements are evidenced. Per the prohibition policy, an interactive human resolution at the end-of-phase checkpoint is exactly what a judgment-tier prohibition requires; that has now happened, so none remains flagged.

**The load-bearing factual claims in those rulings were re-verified in the source rather than taken on faith:**

| Ruling | Claim to check | Verified this run |
|---|---|---|
| P-1 | `resolve_symbols(..., mode="in_range")` runs BEFORE `_print_estimate`, so the estimator is unreachable with a pre-coverage window in the real backfill path | **CONFIRMED** in `ingest_us_equity.py`: line 352 `symbols = resolve_symbols(args, catalog, mode="in_range")`; line 403 `_print_estimate(catalog, args, symbols)`. `_print_estimate` takes `symbols` as a parameter (`:157`), so it is necessarily downstream. Both consume the same `args.category/start_date/end_date` (`:158-159`). |
| P-1 | `estimate_dense_panel` returns a sizing dict, never tickers | **CONFIRMED** by execution: returns `{'symbols': 766, 'trading_days': 30240, 'bars_per_day', 'timestamps', 'dense_cells', 'observed_cells', 'density', 'dense_bytes', 'observed_bytes'}` — `symbols` is an `int` count. No roster is reachable through it. |
| P-2 | WR-03's misstatement is about change-log SHAPE, not coverage WIDTH | **CONFIRMED**: `universe.py:1556-1558` claims "every membership interval already starts at or after its own coverage start"; measured 74/905 pre-coverage rows, min `1957-03-04`, 74/74 open-ended. The claim is about interval placement, and nothing in it widens the advertised coverage window. |
| P-3 | The only category literals are a comment plus the two `CATEGORY` registration declarations | **CONFIRMED**: `grep` over `acquisition/universe.py` finds the membership tokens only at `:1063` (`CATEGORY = "sp500_constituent"`) and `:1131` (`CATEGORY = "nasdaq100_constituent"`) — registration itself — plus a comment at `:482`. The guard is called at `:1576` (inside `get_symbols_in_range`, body `1523-1618`) and `:2348` (inside `get_symbols_as_of`, body from `2330`); neither body contains a category literal or an `if` on category. `_coverage_start` (`:1467`) is a one-line dict comprehension over `MEMBERSHIP_FETCHERS`. |

**One correction to the record, which does not overturn the P-1 verdict.** The P-1 ruling gave two bases: (a) a sizing dict is not a membership answer, and (b) caller ordering makes the estimator unreachable. Basis (a) is confirmed and is sufficient on its own. Basis (b) is **narrower than recorded** — it holds for `ingest_us_equity.py` but not for `ingest_tiingo.py` / `ingest_alpaca.py`, which resolve membership by `mode="as_of"` on `args.as_of_date` while sizing from `args.start_date`, a value no membership query sees. This is reported as strengthened WR-02 evidence (see Advisory), which is precisely where the human ruling itself parked the reservation — it does not change the verdict, because the estimator still never returns a roster.

### Change 2 — `525c332` closes the prior run's `coincidental_reliance_items`

The prior verdict recorded SC-3 as `✓ VERIFIED (coincidental-reliance)`: the guard was correct only because the two registered `PIT_COVERAGE_START` constants happened to be canonical zero-padded ISO, with nothing in the code establishing that. `525c332` adds two parametrised registry-contract tests. This refresh **mutation-checked** them rather than accepting that they exist:

| Injected `PIT_COVERAGE_START` | Test outcome | Correct? |
|---|---|---|
| absent (bare annotation, as on the ABC at `:543`) | rejected, message names the class | ✓ |
| `"2010-1-1"` (unpadded) | rejected | ✓ |
| `"20070201"` (ISO basic — accepted by `date.fromisoformat` since 3.11) | rejected | ✓ |
| `"2007-02-01"` (canonical) | admitted | ✓ |

The lexicographic hazard the test docstring asserts was also confirmed real in both directions: `"2010-06-30" < "2010-1-1"` is `True`, and `"2007-01-15" < "20070201"` is `True`. The second test pins that `_coverage_start()` — the lookup the guard actually consults — resolves every registered CATEGORY to its own canonical constant and returns `None` for the roster categories (the D-02 contract). The precondition SC-3 rested on is now declared and enforced over the registry, so a third index inherits the check by being registered. Reliance flag cleared.

Residual: lookup-time validation still does not exist — the constant is validated by the test suite, not by `_coverage_start` itself. Since registration means editing the in-repo `MEMBERSHIP_FETCHERS` tuple, the test covers every real registration path, so this is noted rather than carried as a reliance item.

## Goal Achievement

### Observable Truths — ROADMAP Success Criteria (the contract)

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | SC-1 — Daily S&P 500 and Nasdaq-100 membership panels as `xr.Dataset`, dims `timestamp`/`symbol`, boolean `is_member`, persisted to and reloaded from Zarr | ✓ VERIFIED (regression re-executed) | Panel layer byte-identical: `git diff --name-only 158a5b0..HEAD -- base/ dataset/ config/ enums/` → **0 files**. Re-executed live this run: `SP500ConstituentDataset(...).from_raw_data().get_xarray_dataset()` → `dims={'timestamp': 18331, 'symbol': 876}`, `dtype=bool`, `data_vars=['is_member']`; Nasdaq-100 → `dims={'timestamp': 7159, 'symbol': 275}`, same dtype and variable. Zarr round-trip locked by `tests/test_constituent_panel.py`. |
| 2 | SC-2 — Survivorship-bias-free within coverage; all-time-union symbol axis; left edge clamped to 1976-07-01 / 2007-02-01; removal effective date still a membership day; **matching `get_symbols_as_of()` exactly** | ✓ VERIFIED (re-executed) | Exact-match half re-run against the live table, not inherited: 122 sampled days for `sp500_constituent` and 48 for `nasdaq100_constituent`, **0 mismatches** in both. Symbol axes unchanged from the previously-verified state (876 / 275 = all-time unions). Left-edge clamp consistent with the measured spans: 18331 days from 1976-07-01 and 7159 days from 2007-02-01 both land on today's date. |
| 3 | SC-3 — **A membership query before an index's coverage start raises rather than returning a silently incomplete roster** | ✓ VERIFIED | **The reliance caveat the prior run attached to this truth is now gone** (see Change 2). Re-probed against the live table: pre-coverage raises for both queries × both categories; CR-01's non-dashed ISO forms (`'20070115'`, `'20070101'`, cross-year `'19700101'`) all raise quoting the NORMALISED date; the inclusive boundary does not over-fire (`as_of(ndx,'2007-02-01')` → 104, `in_range(sp500,'1976-07-01',…)` → 766); boundary-free categories still answer `[]` without raising (D-02). |
| 4 | SC-4 — One shared `BaseDataset`; no OHLCV-only member reachable from the constituent side; adding a further index requires zero edits under `base/` | ✓ VERIFIED (regression check) | `git diff --name-only 158a5b0..HEAD -- base/` is **empty**. `tests/test_dataset_hierarchy.py` locks pass inside the green suite. The constructive third-index proof from the earlier verification stands unchanged because its subject was not touched. |

### Observable Truths — 03.1-05 plan `must_haves` (merged, SC scope not reduced)

| # | Truth | Status | Evidence |
|---|---|---|---|
| 5 | `get_symbols_in_range` raises for BOTH registered membership categories where it previously returned `[]` / a 1976-censored roster | ✓ VERIFIED (re-executed) | Live table: `('sp500_constituent','1900-01-01','2020-01-01')` → `ValueError: Cannot answer sp500_constituent membership before 1976-07-01 -- start_date='1900-01-01' precedes it`; `('nasdaq100_constituent','1900-01-01','2006-01-01')` → the 2007-02-01 analogue. |
| 6 | Boundary is INCLUSIVE (strict `<`), byte-identical in shape to `get_symbols_as_of`'s | ✓ VERIFIED (re-executed) | `as_of(ndx,'2007-02-01')` → 104; `as_of(ndx,'2007-01-31')` → raises. `in_range(sp500,'1976-07-01',…)` → 766; `as_of(sp500,'1976-06-30')` → raises. One helper at `:1490` serves both. |
| 7 | Boundary-free categories untouched; `[]` stays a legitimate answer wherever coverage exists (D-02) | ✓ VERIFIED (re-executed) | `in_range('us_all','1900-01-01','1901-01-01')` → `[]`; `as_of('nasdaq_all','1900-01-01')` → `[]`. Now also locked by the new registry test asserting `_coverage_start(roster_cls.CATEGORY) is None` for every `ROSTER_FETCHERS` entry. |
| 8 | **(backstop)** For any window at or after the coverage start, the SET returned by `get_symbols_in_range` is unchanged; the method still promises no positional ordering | ✓ VERIFIED — carried forward, subject unchanged | Established last run by held-out differential evidence (worktree at `158a5b0`, 84 windows, 0 set diffs, 6 intended status changes). `acquisition/universe.py` is byte-identical since, so the differential result stands. Regression re-checked: full suite green. |
| 9 | ONE shared registry-driven helper called by BOTH queries; neither method body contains a category literal | ✓ VERIFIED (re-verified this run) | See the P-3 row above — grep over the whole module, both method bodies clean, `_coverage_start` a comprehension over `MEMBERSHIP_FETCHERS`. **Strengthened since last run:** the registry contract is now a test, not an observation. |
| 10 | The guard reaches the only real caller: `utils/cli.py:resolve_symbols(..., mode='in_range')` with a pre-coverage `--start-date` raises | ✓ VERIFIED | Established last run against the live catalog with positive controls; `utils/cli.py` unchanged since. Locked by `tests/test_ingest_tiingo_universe_wiring.py:206`, which builds a real `UniverseCatalog`. Green in this run's suite. |
| 11 | Fix confined to `acquisition/universe.py` + tests + the two docs; `base/` untouched, so DATA-06 is not regressed | ✓ VERIFIED | `base/` = 0 files changed since `158a5b0`. The only post-`d02aa09` code change is additive test code. |

**Score:** 11/11 truths verified (0 present-but-behavior-unverified) — **0 flagged prohibitions remaining**.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `acquisition/universe.py` | One registry-driven guard shared by both membership queries; contains `_assert_within_coverage` | ✓ VERIFIED | `_normalize_iso_date:1428`, `_coverage_start:1467`, `_assert_within_coverage:1490`, called at `:1576` and `:2348`. Unchanged since `d02aa09`; re-exercised by execution this run. |
| `tests/test_universe.py` | Coverage locks; **new:** registry contract for `PIT_COVERAGE_START` | ✓ VERIFIED (strengthened) | Prior locks intact. New: `test_every_membership_fetcher_declares_a_canonical_iso_coverage_start` (parametrised over `MEMBERSHIP_FETCHERS`, 2 cases) and `test_coverage_start_lookup_agrees_with_the_registered_constants`. Selected run: **3 passed**. Mutation-checked — genuinely discriminating, not a tautology. |
| `tests/test_ingest_tiingo_universe_wiring.py` | CLI-path proof | ✓ VERIFIED | `:206`, real catalog, positive control. Unchanged, green. |
| `README.md` | Coverage prose naming both membership queries | ✓ VERIFIED | Lines 103-124. Unchanged. |
| `example/constituent.md` | Internals walkthrough | ✓ VERIFIED (with known defect) | Lines 89-118 correct. Defect persists: `:386,388` quote pre-delta error text in a 实跑输出 block (WR-04, advisory). |
| `utils/cli.py` | Normalisation rebound on the explicit-symbol override | ✓ VERIFIED | `:457-458`. Unchanged. |
| `03.1-UAT.md` | Human adjudication of the three flagged prohibitions | ✓ VERIFIED | `status: complete`, 3/3 pass, 0 issues, 0 pending; each item carries a reasoned `ruling`. |
| `03.1-VALIDATION.md` | Refreshed validation strategy | ✓ VERIFIED | `status: validated`, `nyquist_compliant: true`. |
| `03.1-SECURITY.md` | Threat verification | ✓ VERIFIED | `status: verified`, `threats_open: 0`, `asvs_level: 1`. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `get_symbols_in_range` | `PIT_COVERAGE_START` | `_assert_within_coverage()` → `_coverage_start()` registry lookup | ✓ WIRED | `:1576`, verified by execution. |
| `get_symbols_as_of` | `_assert_within_coverage` | the same helper | ✓ WIRED | `:2348`. One guard, not two that can drift. |
| `utils/cli.py:resolve_symbols` | `get_symbols_in_range` | `mode='in_range'` dispatch | ✓ WIRED | Locked by the wiring test; unchanged. |
| Both membership queries | `_normalize_iso_date` | rebinding assignment | ✓ WIRED | All 9 call sites rebind; re-confirmed by the non-dashed-form probes raising on the normalised value. |
| `MEMBERSHIP_FETCHERS` registration | `_coverage_start` | **new registry test** | ✓ WIRED (new this delta) | `test_coverage_start_lookup_agrees_with_the_registered_constants` pins the lookup, not just the constants. |
| `estimate_dense_panel` | `_assert_within_coverage` | — | ✗ NOT_WIRED | WR-02. Advisory by decision; does not govern a membership query. Reachability broadened this run — see Advisory. |
| `base/constituent.py:IndexConstituentDataset` | `base/data.py:BaseDataset` | inheritance | ✓ WIRED (unchanged) | `base/` untouched. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| `_assert_within_coverage` | `coverage_start` | `MEMBERSHIP_FETCHERS[*].PIT_COVERAGE_START` class constants | Yes — no literal, no default; now shape-checked by a registry test | ✓ FLOWING |
| `get_symbols_in_range` / `get_symbols_as_of` | roster | `PlBackend` lazyframe over the live `universe.parquet` (905 sp500 rows) | Yes — measured 766 / 522 / 104 non-empty rosters | ✓ FLOWING |
| `base/constituent.py:_densify` | `is_member` | interval rows → boolean grid | Yes — `(18331, 876)` and `(7159, 275)` grids built live this run | ✓ FLOWING |

No hardcoded roster, no static fallback, no `return []` stub on either query path.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Full suite regression (run once) | `uv run python -m pytest tests/ -q` | **505 passed**, 0 failed, 25.9s (was 502; +3 from `525c332`) | ✓ PASS |
| The three new registry tests | `pytest tests/test_universe.py -k "canonical_iso_coverage_start or coverage_start_lookup_agrees"` | 3 passed | ✓ PASS |
| New registry test is DISCRIMINATING (mutation check) | verifier probe injecting absent / `2010-1-1` / `20070201` / canonical constants | 3 rejected, 1 admitted — all correct | ✓ PASS |
| Panel/hierarchy/universe/wiring non-regression | `pytest tests/test_constituent_panel.py tests/test_ndx_constituent.py tests/test_dataset_hierarchy.py tests/test_universe.py tests/test_ingest_tiingo_universe_wiring.py -q` | 124 passed | ✓ PASS |
| SC-1 panels build live | verifier probe, both indices | `(18331,876)` / `(7159,275)`, `is_member` bool | ✓ PASS |
| SC-2 panel ≡ `get_symbols_as_of` (re-executed) | verifier probe, 170 sampled days across both indices | 0 mismatches | ✓ PASS |
| SC-3 pre-coverage refusal, both queries × both categories | verifier probe vs live table | all raise, quoting the normalised date | ✓ PASS |
| SC-3 CR-01 bypass class (ISO basic, cross-year) | verifier probe vs live table | all raise | ✓ PASS |
| SC-3 inclusive boundary must not over-fire | `as_of(ndx,'2007-02-01')`, `in_range(sp500,'1976-07-01',…)` | 104 / 766, no raise | ✓ PASS |
| D-02 boundary-free categories | `in_range('us_all',…)`, `as_of('nasdaq_all',…)` | `[]`, no raise | ✓ PASS |
| P-1 caller ordering claim | source inspection, `ingest_us_equity.py:352` vs `:403` | ordering confirmed; `_print_estimate` takes `symbols` as a param | ✓ PASS |
| P-3 category-literal claim | `grep` over `acquisition/universe.py` | literals only at `:482` (comment), `:1063`, `:1131` (registration) | ✓ PASS |
| WR-01 inverted window | `in_range('sp500_constituent','2020-01-01','1900-01-01')` | `[]`, silent | ✗ FAIL (advisory, deferred) |
| WR-02 sizing bypass, direct | `estimate_dense_panel('sp500_constituent','1900-01-01','2020-01-01')` | `symbols=766, trading_days=30240` | ✗ FAIL (advisory, deferred) |
| WR-02 sizing bypass, **through the real CLI helper chain** | `resolve_symbols(mode='as_of')` → `volume_pricing` → `estimate_dense_panel` | resolves 522 symbols with no raise, then sizes over `1900-01-01..2020-01-01` unguarded | ✗ FAIL (advisory, new evidence) |
| WR-03 docstring invariant vs live table | polars count of `sp500_constituent` rows with `start_date < '1976-07-01'` | **74 of 905**, min `1957-03-04`, all 74 open-ended | ✗ FAIL (advisory, deferred) |
| WR-04 stale error text | `sed -n '380,392p' example/constituent.md` | pre-delta text still present, no `field=` fragment | ✗ FAIL (advisory, deferred) |

All verifier probe files were deleted after execution. `git status --porcelain` shows only the pre-existing ` M test.py` and `?? .planning/milestone.lock`.

### Probe Execution

No `scripts/*/tests/probe-*.sh` exists in this repository and no plan declares one. **SKIPPED (no probes declared or discoverable).** Behavioural evidence was gathered through the live-table and pytest paths above instead.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| DATA-05 | 03.1-02, 03.1-03, 03.1-04, 03.1-05 | 日频 PIT 成分面板（`is_member`），可回溯区间内无幸存者偏差；超出可回溯起点的查询必须显式报错而非静默返回不完整名单 | ✓ SATISFIED | The 显式报错 clause holds for both membership queries across three ISO input forms and through the CLI (truths 3, 5-7, 9, 10). The panel + survivorship halves re-executed this run (truths 1-2). All three DATA-05 prohibitions human-adjudicated HELD. |
| DATA-06 | 03.1-01, 03.1-04, 03.1-05 | 成分股类与行情类共享 `BaseDataset`；不继承 OHLCV 专用成员；新增指数不需改动上层 | ✓ SATISFIED | Non-regression measured: `git diff --name-only 158a5b0..HEAD -- base/` is empty; hierarchy locks green (truths 4, 11). |

**Orphaned requirements:** none. `.planning/REQUIREMENTS.md:108-109` maps exactly DATA-05 and DATA-06 to Phase 03.1. Every ID declared in a plan's `requirements` frontmatter is accounted for: `03.1-01` → DATA-06; `03.1-02`, `03.1-03` → DATA-05; `03.1-04`, `03.1-05` → DATA-05 + DATA-06. Both are marked `[x]` / `Complete` in REQUIREMENTS.md, consistent with this verdict.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| — | — | — | — | None. `grep -nE "TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER"` over the only file changed since `d02aa09` (`tests/test_universe.py`) returned zero matches. No debt-marker gate trigger. |

### Advisory (New Scope, Unevidenced)

Re-verification evidence gate applied. None of the findings below is a carried-forward gap, and none is a debt marker. `acquisition/universe.py` and `example/constituent.md` have not been git-modified since the prior `verified:` timestamp, so these predate the gap-closure round unflagged and none has a red test in the repo — they are reported rather than blocking, and none reverts a completed must-have.

| # | Finding | Category | Why Advisory |
|---|---|---|---|
| 1 | **WR-02** — `estimate_dense_panel` is a third, unguarded copy of the predicate. **New this run:** the "caller ordering makes it unreachable" argument holds only for `ingest_us_equity.py`; `ingest_tiingo.py` / `ingest_alpaca.py` size from `--start-date` while guarding only `--as-of-date`, and the bypass was driven through the real shared helpers | architectural | The estimator returns counts, never a roster — the human ruling's primary and ordering-independent basis — so SC-3 is not falsified. Deliberately deferred; explicitly NOT closed by the P-1 ruling |
| 2 | **WR-03** — `universe.py:1556-1558` states a provably false load-bearing invariant (74/905 rows, min 1957-03-04); the raise-not-clamp conclusion survives only because all 74 are open-ended | other | Adjudicated NOT a P-2 violation (shape, not width), but the UAT ruling requires it be booked above ordinary doc debt. A future closed pre-1976 interval would silently invalidate it |
| 3 | **WR-01** — inverted window returns a silent `[]` | other | New scope (argument ordering, not the coverage guard); reproduced by probe, no red test |
| 4 | **WR-04** — `example/constituent.md:386,388` quotes pre-delta error text in a 实跑输出 block | other | Documentation accuracy in the phase's own reference example |
| 5 | **WR-06** — four original coverage tests assert only "some `ValueError`" | other | Test-strength defect; the behaviour was established independently by execution |
| 6 | Phase is `Mode: mvp` but the ROADMAP goal is not a valid User Story (`user-story.validate` → `valid: false`) | other | Format discrepancy predating this run. Verified against the four ROADMAP Success Criteria, which are the contract |

**Closed since the prior report:** WR-05 (`PIT_COVERAGE_START` never validated at registration) — closed by `525c332` and mutation-checked this run. It is no longer an advisory and no longer a `coincidental_reliance_items` entry.

### Human Verification Required

**None.** The three judgment-tier prohibitions that held the previous verdict at `human_needed` were resolved by human-in-the-loop UAT (`03.1-UAT.md`, `status: complete`, 3 passed, 0 issues). No plan contains a deferred `<verify><human-check>` block (`grep -c "human-check"` → 0 across all five PLANs). No truth is present-but-behavior-unverified.

### Gaps Summary

**No gaps. Status upgraded from `human_needed` to `passed`.**

The previous verdict was already 11/11 on must-haves and already stated the phase goal is achieved; it withheld `passed` for exactly one reason, which no longer applies. The three judgment-tier prohibitions in `03.1-05-PLAN.md` are now adjudicated by a completed UAT with a reasoned ruling per item, and the fail-closed policy's requirement — explicit human resolution per item at the end-of-phase checkpoint — is satisfied. `unverified_prohibitions` drops 3 → 0.

The rulings were not accepted on narrative. Each carries a factual claim, and each was re-checked in the source: P-1's ordering (`ingest_us_equity.py:352` before `:403`, with `_print_estimate` taking `symbols` as a parameter) is real; P-1's semantic basis (the estimator returns a sizing dict whose `symbols` is an `int` count, never tickers) is real; P-2's distinction (WR-03 misstates the change log's SHAPE, not its coverage WIDTH) holds on inspection of the actual sentence; P-3's reclassification from judgment to executable fact is correct — the only category literals in the module are a comment and the two `CATEGORY` registration declarations, and neither query body contains a branch on category. One correction is recorded: P-1's *secondary* basis, caller ordering, is narrower than the ruling states — it does not extend to `ingest_tiingo.py` or `ingest_alpaca.py`, where the sizing window comes from `--start-date` while only `--as-of-date` is guarded, a bypass this run drove end-to-end through the real shared helpers. That strengthens WR-02, which the ruling itself deliberately left open, and it does not disturb the verdict, which rests on the sufficient primary basis.

The second change is a genuine strengthening rather than a formality. The prior report marked SC-3 `✓ VERIFIED (coincidental-reliance)`: the guard was right only because two hand-written constants happened to be canonical, and `PIT_COVERAGE_START` on the ABC is a bare annotation with no value. `525c332` turns that undeclared precondition into a registry-level contract, and the test was mutation-checked rather than merely observed to pass — it rejects an absent constant, an unpadded `2010-1-1`, and an ISO-basic `20070201`, and admits only the canonical form. The lexicographic hazard its docstring cites was confirmed real in both directions. The reliance flag is cleared.

Nothing regressed. The only code change in the five new commits is 74 lines of additive test code in one file; `base/`, `dataset/`, `config/` and `enums/` are byte-identical to `158a5b0`. The full suite is green at 505 (was 502, +3 exactly accounting for the new parametrised tests), and the panel, exact-match, coverage-guard and CLI behaviours were all re-executed against the live reference table rather than inherited.

What remains is not gap-shaped and is reported rather than absorbed. Six advisories stay open, and two deserve emphasis for the next pass. **WR-02 is the structurally significant one, and it grew this run**: it is a third copy of the membership-window predicate that bypasses the shared guard — the same "shared guard covering N-1 entry points" shape that produced this phase's original gap — and the reachability argument that made it tolerable turns out to cover one of three front doors, not all three. **WR-03 is the one that is factually wrong rather than merely incomplete**: the docstring's justification for raising over clamping is false against the live table, and the conclusion survives on a data-dependent accident the docstring does not name.

The phase goal — daily point-in-time membership panels for both indices, as `xarray.Dataset`, Zarr-persisted, survivorship-bias-free within coverage, on a shared `BaseDataset`, with pre-coverage membership queries refused rather than answered — **is achieved**.

---

_Verified: 2026-09-07T19:05:00Z_
_Verifier: Claude (gsd-verifier)_
