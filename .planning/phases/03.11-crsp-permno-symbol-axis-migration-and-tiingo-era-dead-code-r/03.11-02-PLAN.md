---
phase: 03.11
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - quantlab/utils/symbol_axis.py
  - quantlab/dataset/backend.py
  - tests/test_symbol_axis_contract.py
  - tests/conftest.py
  - tests/test_widening_fixture_realism.py
  - tests/test_symbol_axis_widening.py
autonomous: true
requirements: [DATA-03]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "在 int64 symbol 轴的 store 上做超集宽化后，非 NaN 的格数与宽化前完全相等——不再静默清空整个 store（D-09，RESEARCH §R2-A 实跑：non-NaN cells after widen: 0 of 12）"
    - "superset 守卫在**归一化后**的值上比较，所以请求一个真子集时它确实抛错，而不再是装饰（D-09）"
    - "symbol 轴的顺序是数值序这条契约只表达一次、在一个模块里，而不是在 8 个调用点各写一遍 sorted()（D-19）"
    - "tests/conftest.py 的 symbol 坐标编码模型认识第三种编码 int64，并且按 dtype KIND 判定而不是按宽度字面量（D-09）"
    - "test_widening_fixture_realism.py 的元组字面量 pin 与新的三元组同步，它红是设计如此而不是回归（Pitfall 6）"
    - "XrBackend.filter_by_symbol 一个字都没改——这一族的约定是 backend 不猜类型（D-16 / operator RULING 3）"
    - statement: "另外 5 个消费 SYMBOL_COORD_ENCODINGS 的测试文件在加第三个 arm 之后仍然全绿"
      verification: backstop
  artifacts:
    - quantlab/utils/symbol_axis.py
    - tests/test_symbol_axis_contract.py
  key_links:
    - "XrBackend.widen_symbol_axis → quantlab/utils/symbol_axis.py:normalize_to_axis_dtype()（backend.py:451 的无条件 str() 被它取代）"
    - "tests/conftest.py:stored_symbol_encoding → dtype.kind == 'i' 的第三个 arm"
    - "quantlab/utils/symbol_axis.py:sort_symbol_axis ← crsp_membership.py:326-333 的数值序契约 docstring（论证搬家，不是复制）"
  prohibitions:
    - "不修改 quantlab/dataset/backend.py:1477-1479 的 filter_by_symbol —— 它的兄弟 filter_by_date:1473-1475 同样是一行裸 .sel，这一族不做类型加工"
    - "不修改 quantlab/dataset/cleaning.py（D-06）"
    - "不把 SYMBOL_COORD_ENCODINGS 的三个 arm 按 dtype 宽度字面量判定（conftest 的 docstring 逐字禁止）"
    - "不写任何 config 迁移或兼容层（D-04）"
    - "不删除 quantlab/enums/data.py:TRADEABLE_TICKER_PATTERN（D-06）"
---

<!-- planner-discipline-allow: requested = [str(symbol) for symbol in symbols], SYMBOL_COORD_ENCODINGS[0], SYMBOL_COORD_ENCODINGS[1], np.int64 -->
<objective>
在换轴**之前**先把 int64 轴上的真 bug 修掉，并把「symbol 轴的顺序是数值序」这条契约落成一处。

**为什么这一条必须排在 W1 之前或同一个 wave。** `XrBackend.widen_symbol_axis` 在 `backend.py:451` 把请求标签
无条件 `str()`，而 `:456-459` 的 superset 守卫把两侧都 `str()`，于是 `dropped` 恒为空、守卫不报警；接着
`:795` / `:869` 的 `reindex` 在 int64 index 上找不到任何一个字符串标签 → 整个面板变全 NaN；最后 `:543-545`
的 `os.replace` 把空库变成权威库并 `rmtree` 原库。**无异常，无日志。** RESEARCH §R2-A 已实跑复现
（`superset guard 'dropped': [] -> guard passes: True` / `non-NaN cells after widen: 0 of 12`）。
一旦 plan 03 把轴换成 int64，任何一次 `widen_and_append` 都会不可逆地抹掉盘上数据。

Purpose: 把 CIA 三元里 Availability + Integrity 的一处真实损失堵上，并给后续所有 wave 一个可以 import 的
数值序来源。
Output: 新模块 `quantlab/utils/symbol_axis.py`、`backend.py` 的归一化修复、conftest 的第三个编码 arm。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-CONTEXT.md
@.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-RESEARCH.md
@.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-PATTERNS.md
</context>

## Artifacts this phase produces

| 符号 | 位置 | 类型 |
|---|---|---|
| `sort_symbol_axis` | `quantlab/utils/symbol_axis.py` | 函数 |
| `normalize_to_axis_dtype` | `quantlab/utils/symbol_axis.py` | 函数 |
| `tests/test_symbol_axis_contract.py` | 新测试文件 | — |
| `SYMBOL_COORD_ENCODINGS` 的第三个成员 `"int64"` | `tests/conftest.py:1460` | 常量扩展 |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: quantlab/utils/symbol_axis.py —— 数值序契约与轴 dtype 归一化，各表达一次（D-19）</name>
  <files>quantlab/utils/symbol_axis.py, tests/test_symbol_axis_contract.py</files>
  <read_first>
    - `quantlab/dataset/crsp_membership.py:310-347`（全仓唯一把数值序写成契约的地方，docstring 本身就是论证；本任务把这段论证**搬**过来，不是复制一份）
    - `quantlab/dataset/backend.py:445-468`（`requested` 的构造与失效的 superset 守卫，逐字）
    - `quantlab/dataset/backend.py:470-505`（S3 守卫体例：收集 offenders → 拼 described → `raise ValueError` 带「refusing to …」开头 + 实测证据 + 补救）
    - `quantlab/dataset/backend.py:1000-1044`（本文件错误消息一律带实测证据的先例，以及 coord dtype 往返的实测注释）
    - `.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-RESEARCH.md` 的 `### Pattern 1` 与 `### Pattern 3`
  </read_first>
  <behavior>
    - `sort_symbol_axis([7000, 14593, 93436, 10107])` 返回 `[7000, 10107, 14593, 93436]`
    - `sort_symbol_axis(["AAPL", "MSFT", "A"])` 返回 `["A", "AAPL", "MSFT"]`（非整数值退回字典序，不抛错）
    - `sort_symbol_axis(["10107", "7000"])` 返回 `["7000", "10107"]` —— 数字串按数值序排，但**元素类型不变**（仍是 str）
    - `sort_symbol_axis([])` 返回 `[]`
    - 混合类型（`[1, "AAPL"]`）退回字典序的 `str` 比较，不抛错
    - `normalize_to_axis_dtype(["10107"], pd.Index([10107], dtype="int64"))` 返回 `[10107]`，元素是 numpy int64 或 python int
    - `normalize_to_axis_dtype([10107], pd.Index(["10107"], dtype=object))` 返回 `["10107"]`
    - `normalize_to_axis_dtype(["AAPL"], pd.Index([10107], dtype="int64"))` 抛 `ValueError`，消息含被拒的值与目标 dtype
  </behavior>
  <action>
新建 `quantlab/utils/symbol_axis.py`。模块 docstring 陈述本模块存在的唯一理由：symbol 轴的**顺序**与
**dtype** 是两条契约，在此之前它们散在 8 个以上的调用点上各自 `sorted()` / `str()`，于是能各自演化。

`def sort_symbol_axis(values) -> list`：
- 先把 `values` 物化成 list。空则返回 `[]`。
- 判定「是否全体都能解释成整数」：元素是 `int`/`np.integer`，或是 `str` 且 `.isdigit()`。全体成立时用
  `key=int` 排序；否则用 `key=str` 排序。**返回的元素类型与输入一致**，函数只决定顺序，不做转换。
- docstring 把 `crsp_membership.py:326-333` 的论证搬过来（逐字保留那句 **The order is part of the contract,
  and it is NUMERIC.** 及其理由：`sorted()` 在文本上会把 `"14593"` 排在 `"7000"` 前；`utils/cli.py:resolve_symbols`
  用 `--limit` 切这个 list，顺序不稳会让每次运行截到不同批次，第二次运行永远碰不上第一次写的 watermark），
  并注明历史 PERMNO 恰好都是 5 位（≈10000–93436）所以今天两种排序相等，`7000` 这种 4 位 PERMNO 一出现就分叉。

`def normalize_to_axis_dtype(labels, stored_index) -> list`：
- 用 `pd.Index(list(labels)).astype(stored_index.dtype)` 归一化，`.tolist()` 返回。
- 转换失败（`TypeError`/`ValueError`）时**重新抛出**成一条 S3 体例的 `ValueError`：以
  `f"normalize_to_axis_dtype: refusing to coerce ..."` 开头，点名无法转换的值（最多列前 10 个）与
  `stored_index.dtype`，说明为什么不能硬转（无声的错值会让下游 `reindex` 全部 miss 而不报错），
  并给出补救（在调用层把标签类型对齐到轴，而不是在这里猜）。

新建 `tests/test_symbol_axis_contract.py` 覆盖上面 `<behavior>` 的 8 条，外加一条回归断言：
`sort_symbol_axis` 对 `[7000, 10107, 14593, 93436]` 的输出与 `sorted(str(...))` 的输出**不相等**——
这是 RESEARCH §R2-H 那一处实测分叉的固化。
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_symbol_axis_contract.py -p no:cacheprovider</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line, or the summary reports fewer than 9 passed</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "def sort_symbol_axis" quantlab/utils/symbol_axis.py` 返回 1
    - `grep -c "def normalize_to_axis_dtype" quantlab/utils/symbol_axis.py` 返回 1
    - `grep -c "NUMERIC" quantlab/utils/symbol_axis.py` ≥ 1（数值序论证已搬家）
    - `uv run python -c "from quantlab.utils.symbol_axis import sort_symbol_axis as s; assert s([7000,14593,93436,10107])==[7000,10107,14593,93436]"` 退出码 0
    - `uv run pytest -q tests/test_symbol_axis_contract.py -p no:cacheprovider` ≥ 9 passed
  </acceptance_criteria>
  <done>两条契约各有一个可 import 的实现与一组锁定它的测试。</done>
  <reversibility rating="reversible">纯新增模块，无既有调用点改动。</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/conftest.py 的第三个编码 arm "int64"，以及被它牵动的元组字面量 pin</name>
  <files>tests/conftest.py, tests/test_widening_fixture_realism.py</files>
  <read_first>
    - `tests/conftest.py:1455-1545`（`SYMBOL_COORD_ENCODINGS` / `symbol_coord` / `stored_symbol_dtype` / `stored_symbol_encoding` / `assert_stored_symbol_encoding` 五件套，逐字；尤其 `symbol_coord` 里那张 in-memory-spelling → on-disk-result 的实测映射表，与 `stored_symbol_encoding` 的「By dtype KIND … never by a width literal」）
    - `tests/test_widening_fixture_realism.py:230-245`（`assert SYMBOL_COORD_ENCODINGS == ("fixed_width", "variable_length")` 的字面量 pin）
    - `.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-PATTERNS.md` 的 `### 4.2 Chokepoint #2` 四条必须照做的约定
  </read_first>
  <behavior>
    - `SYMBOL_COORD_ENCODINGS` 成为三元组，第三项是 `"int64"`
    - `symbol_coord(["10107","14593"], "int64")` 返回 dtype 为 `np.int64` 的 ndarray
    - 未知 encoding 的 `ValueError` 消息**遍历**元组列出全部合法值，不再硬编码 `[0]` / `[1]` 两项
    - `stored_symbol_encoding(int64_store)` 返回 `"int64"`，判定依据是 `dtype.kind == "i"`，不是 `int64` 字面量
    - `stored_symbol_encoding` 对既有两种字符串编码的返回值完全不变（control arm 不动）
    - `test_widening_fixture_realism.py` 的字面量 pin 更新为三元组后重新变绿
  </behavior>
  <action>
改 `tests/conftest.py`：

1. `:1460` 的 `SYMBOL_COORD_ENCODINGS` 扩成 `("fixed_width", "variable_length", "int64")`。它上方那段
   `#:` 注释里「这两个名字是 load-bearing」的措辞要改成三个，并说明第三个 arm 为什么存在（PERMNO 轴的
   store 是 int64 坐标，而本 helper 在此之前对它直接 `AssertionError`）。
2. `symbol_coord` 加第三个分支：`np.asarray(list(symbols), dtype=np.int64)`。按本函数既有体例，**必须在
   docstring 的实测映射表里加一行**说明这个拼写在 zarr 上落成什么（在本任务里实测一次并把结果写进去），
   不要凭记忆填。注意输入可能是数字串也可能是 int，用 `np.asarray([int(s) for s in symbols], dtype=np.int64)`
   更稳。
3. `symbol_coord` 末尾那条 `ValueError`（`:1490-1495`）当前硬编码 `SYMBOL_COORD_ENCODINGS[0]!r` 与 `[1]!r`
   —— 加了第三个 arm 它就会**漏报一个合法值**。改成遍历整个元组拼一句（例如 `", ".join(repr(name) for name in
   SYMBOL_COORD_ENCODINGS)`）。这是本 chokepoint 的第二处元组耦合点。
4. `stored_symbol_encoding`（`:1517` 起）在两条字符串分支之后加 `if getattr(dtype, "kind", None) == "i":
   return "int64"`。**按 KIND 判定**，不要 pin `np.int64` 这个具体 dtype——它的 docstring 逐字说明了为什么
   （宽度/具体 dtype 是标签的属性，pin 住它只在一组标签上复现）。同时把 `AssertionError` 文案里的
   「The two modelled encodings」改成不写死数量的措辞。
5. `stored_symbol_dtype` 一个字不改——它读 zarr 原始 dtype 而不是 `xr.open_zarr` 的解码值，这是它存在的
   全部理由（docstring 逐字：「the DECODED value is what hid the defect」）。

改 `tests/test_widening_fixture_realism.py`：把 `:238` 的字面量 pin 更新成三元组。**这个 pin 红了是对的**——
它存在的目的就是让这个元组不能被悄悄改，所以它必须出现在显式改动清单里而不是被当成回归（Pitfall 6）。
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_symbol_coord_encoding.py tests/test_widening_fixture_realism.py tests/test_variable_axis_widening.py tests/test_factor_update.py tests/test_nbbo_dataset.py -p no:cacheprovider</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line, or any line containing "AssertionError: store" in the output</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "from tests.conftest import SYMBOL_COORD_ENCODINGS as E; assert E==('fixed_width','variable_length','int64'), E"` 退出码 0（若 import 路径不便，改用 `uv run pytest` 里的等价断言）
    - `grep -c 'SYMBOL_COORD_ENCODINGS\[0\]' tests/conftest.py` 返回 0
    - `grep -c 'SYMBOL_COORD_ENCODINGS\[1\]' tests/conftest.py` 返回 0
    - `grep -c 'kind", None) == "i"' tests/conftest.py` ≥ 1
    - `grep -c 'np.int64' tests/conftest.py` ≥ 1 且 `stored_symbol_encoding` 函数体内不出现 `np.int64`（按 KIND 判定）
    - 上面的 `<automated>` 五文件命令全绿
  </acceptance_criteria>
  <done>编码模型认识 int64，两处元组耦合点同步，其余 5 个消费者仍然全绿。</done>
  <reversibility rating="reversible">只是扩一个测试常量与两处文案，git revert 即可。</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: widen_symbol_axis 的 int64 修复 —— 先红后绿，且让 superset 守卫真的在守</name>
  <files>tests/test_symbol_axis_widening.py, quantlab/dataset/backend.py</files>
  <read_first>
    - `quantlab/dataset/backend.py:440-560`（`widen_symbol_axis` 全体：`:451` 的 `requested`、`:456-468` 的 superset 守卫、`:470-505` 的 offenders 守卫、`:543-545` 的 `os.replace` + `rmtree`）
    - `quantlab/dataset/backend.py:780-880`（`:795` 与 `:869` 两处 `reindex({dim: requested})`）
    - `quantlab/utils/symbol_axis.py`（Task 1 的 `normalize_to_axis_dtype`）
    - `tests/conftest.py:1455-1560`（Task 2 的三 arm 模型与 `assert_stored_symbol_encoding`）
    - `tests/test_symbol_axis_widening.py` 现有两个 arm 的断言形状
    - `.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-RESEARCH.md` 的 `### A.` 小节（实跑输出即这条修复的期望值与错误消息要引的证据）
  </read_first>
  <behavior>
    - RED 先行：给 `tests/test_symbol_axis_widening.py` 的参数化加上 `int64` arm，在修 backend 之前跑，断言失败，并把这次真实的 pytest 输出记进 SUMMARY 作为红证据（operator P9：pytest 项目要投影真实运行结果）
    - int64 store + 请求一个**超集**（原 3 列 + 1 列新标签）→ 宽化后非 NaN 的格数与宽化前完全相等，新列全 NaN
    - int64 store + 请求一个**真子集**（丢掉一个已存标签）→ `ValueError`，消息以 `XrBackend.widen_symbol_axis: refusing to widen` 开头并列出被丢的标签
    - 宽化后 `stored_symbol_encoding(path) == "int64"`（坐标编码没被顺手改掉）
    - 既有两个字符串 arm 的断言全部不变、全部仍绿
    - 请求里含一个既不在轴上、也无法转成轴 dtype 的值（如 `"AAPL"` 对 int64 轴）→ 抛 `ValueError` 而不是静默 miss
  </behavior>
  <action>
**先写红测试。** 在 `tests/test_symbol_axis_widening.py` 里把 `int64` 接入既有参数化（标签用 PERMNO 形态的
`[10107, 14593, 93436]`，经 `conftest.symbol_coord(..., "int64")` 造坐标），加两个断言：
(1) 超集宽化后 `int(ds[var].notnull().sum())` 与宽化前相等；(2) 子集请求抛 `ValueError`。跑一次，把失败输出
留作红证据——RESEARCH §R2-A 的实跑给出的期望形态是 `non-NaN cells after widen: 0 of 12`，即第一条断言会红。

**再修 backend。** 在 `quantlab/dataset/backend.py` 的 `widen_symbol_axis` 内：

- `:451` 的 `requested = [str(symbol) for symbol in symbols]` 要等到 `stored` 打开之后才能归一化，因为它需要
  `stored[dim]` 的 dtype。把 `requested` 的计算**移到** `stored = xr.open_zarr(path)` 之后、`try:` 之内，
  改成 `stored_index = stored[dim].to_index()` + `requested = normalize_to_axis_dtype(symbols, stored_index)`。
- `:456-459` 的 superset 守卫改成在**归一化后**的值上比较：`dropped = stored_index.difference(pd.Index(requested)).tolist()`。
  在此之前两侧都 `str()`，所以 `str(10107) == "10107"` 让 `dropped` 恒为空——守卫只是装饰。
- `:795` 与 `:869` 的两处 `reindex({dim: requested})` 不用改代码，它们现在拿到的是归一化后的 `requested`。
- 既有 `ValueError` 文案保留其结构，**补一句实测证据**，沿用本文件 `:1000-1006` 的体例（那里逐字写着
  "measured 2026-09-07, np.full(shape, np.nan) yields 0 for int64 and True for bool"）：这里要写的是
  2026-09-20 实测的「对 int64 轴无条件 `str()` 后 reindex，12 格里 0 格非 NaN，而 superset 守卫 dropped 为空」。
- 追加一条新的 `ValueError`（或让 `normalize_to_axis_dtype` 的错误冒泡，二选一并在 docstring 说明选了哪条）
  覆盖「请求里有无法转成轴 dtype 的标签」。
- `widen_symbol_axis` 的 docstring 加一段：这不是兼容性调整，而是一处**静默数据销毁**——`os.replace` 之后
  空库成为权威库、原库被 `rmtree`，无异常无日志。

**不要**动 `filter_by_symbol`（`:1477-1479`）。它的兄弟 `filter_by_date`（`:1473-1475`）同样是一行裸 `.sel`，
这一族的约定是 backend 不猜类型；对应的类型纪律由 plan 08 装在 config setter 上（RULING 3）。
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_symbol_axis_widening.py tests/test_variable_axis_widening.py tests/test_symbol_coord_encoding.py tests/test_widening_fixture_realism.py tests/test_factor_update.py tests/test_nbbo_dataset.py tests/test_backend_indexes.py -p no:cacheprovider</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line, or the summary line reports any failed or errored test</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "normalize_to_axis_dtype" quantlab/dataset/backend.py` ≥ 1
    - `grep -c 'requested = \[str(symbol) for symbol in symbols\]' quantlab/dataset/backend.py` 返回 0
    - `grep -c "2026-09-20" quantlab/dataset/backend.py` ≥ 1（新增的实测证据）
    - `grep -c 'self.data = self.data.sel({col: list(symbols)})' quantlab/dataset/backend.py` 返回 1（`filter_by_symbol` 的那一行裸 `.sel` 逐字保留，未被加上类型强转）
    - SUMMARY 里有本任务 int64 arm 的**红证据**（修 backend 之前那次运行的真实 pytest 输出片段）
    - 上面的 `<automated>` 七文件命令全绿
  </acceptance_criteria>
  <done>int64 store 在超集宽化后数据完整；子集请求被真正拒绝；两个字符串 arm 不受影响；filter_by_symbol 未被触碰。</done>
  <reversibility rating="reversible">改的是一处判断逻辑与错误文案，git revert 即可；修复本身消除的是一个不可逆的数据销毁路径。</reversibility>
</task>

</tasks>

<threat_model>
> ASVS level 1；`security_block_on: high`。

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 内存中的请求标签 → 盘上 zarr 轴 | `widen_symbol_axis` 在此处决定哪些历史被保留、哪些被 reindex 成 NaN |
| 宽化临时目录 → 权威 store | `os.replace` + `rmtree` 的换位点，错一步就是不可逆的数据丢失 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.11-06 | Denial of Service | `XrBackend.widen_symbol_axis`（`backend.py:451` → `:795`/`:869` → `:543-545`） | critical | mitigate | 请求标签按 `stored[dim].dtype` 归一化；superset 守卫在归一化后的值上比较；`tests/test_symbol_axis_widening.py` 的 int64 arm 先红后绿，断言非 NaN 格数不变 |
| T-03.11-07 | Tampering | 宽化后 symbol 坐标编码被顺手改写而无人察觉 | medium | mitigate | 保留并在 int64 arm 上使用 `assert_stored_symbol_encoding`，它是唯一能看见「不抛错、值/dtype/NaN 计数全过、编码被改」的观察点 |
| T-03.11-08 | Tampering | 无法转成轴 dtype 的标签被静默丢弃 | high | mitigate | `normalize_to_axis_dtype` 转换失败时抛 `ValueError` 并点名被拒的值，而不是让 reindex 静默 miss |
| T-03.11-04 | Tampering | symbol 作为文件路径段 | low | accept | `TRADEABLE_TICKER_PATTERN` 与 `_assert_vendor_root` 不变；PERMNO 数字串天然满足前者（`wrds_crsp.py:317-319`） |
| T-03.11-SC | Tampering | npm/pip/cargo 安装 | low | accept | 本 phase 零安装（RESEARCH `## Package Legitimacy Audit`） |
</threat_model>

<verification>
- `uv run pytest -q tests/test_symbol_axis_contract.py -p no:cacheprovider` 全绿
- widening 五件套 + `test_nbbo_dataset.py` + `test_backend_indexes.py` 全绿
- `uv run pytest -q --ignore=tests/test_factor_hierarchy.py -p no:cacheprovider` 相对 03.10 基线零新增失败
</verification>

<success_criteria>
- int64 store 在超集宽化后非 NaN 格数不变（RESEARCH §R2-A 实跑的 `0 of 12` 变成 `12 of 12` 量级）
- 请求真子集时 superset 守卫抛错
- 数值序契约只在 `quantlab/utils/symbol_axis.py` 表达一次
- `filter_by_symbol` 未被修改
</success_criteria>

<output>
Create `.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-02-SUMMARY.md` when done
</output>
