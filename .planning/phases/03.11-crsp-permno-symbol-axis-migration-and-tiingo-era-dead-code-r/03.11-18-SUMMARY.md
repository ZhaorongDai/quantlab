---
phase: 03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r
plan: 18
subsystem: data
tags: [crsp, permno, zarr, rebuild, sidecar, audit-report, pytest, backup]

requires:
  - phase: 03.11-01
    provides: "CrspStoreRebuilder / BaseStoreRebuilder —— assert → backup → clear → convert → measure 的重建入口，以及「盘上产物必须由当前 HEAD 生成」的 W0 规矩"
  - phase: 03.11-10
    provides: "上一次真实数据重建：data_root 经 --git-common-dir 解析的手法、备份分代的做法、tests/test_crsp_rebuild_measurements.py 的四个用例"
  - phase: 03.11-13
    provides: "代码侧删除 `_permno_breakdown` 的冗余 `symbol` 字段（G-03.11-2 裁定的一半）"
  - phase: 03.11-14
    provides: "全树字节码缓存清场——本 plan 的 worktree 是干净检出，未用 pycache_prefix"
provides:
  - "盘上 `.crsp_filter_report.json` 由当前 HEAD 重新生成：`roster_overrides.permnos` 每条记录的键集恰好 `{types, rows, first, last}`，冗余 `symbol` 字段从盘上消失（G-03.11-5 关闭）"
  - "键集断言进入常设的真实数据门，此后每次重建都复检，不再靠一次手工核对"
  - "`_fresh_backup_dir()` + `assert not backup_dir.exists()`：备份代目录「只增不覆盖」的**前置**不变量，排在 `rebuild()` 之前，由一条 `tmp_path` 单测常设锁住"
  - "`_assert_breakdown_shape()`：两支共用一个键集判据，对应生产侧共用一个 `_permno_breakdown` 渲染"
  - "`data/_backup_pre_03.11_18` —— 带冗余字段那一代的唯一记录（当前代码已无法再产出）"
  - "D-03.11-18-A：主树与 worktree 之间回归门 ±1 的归因，关掉 03.11-09/-10 两次留下的未解释差值"
affects: [03.11-VERIFICATION, 03.11 phase acceptance]

actuals:
  tokens: 4319
  tasks: 3
  commits: 3
plan_head_before: 1af47c8f9b51949c59d6c81802586f5882c2fe6e

tech-stack:
  added: []
  patterns:
    - "破坏性操作的保护必须是**前置**的：`shutil.copytree(..., dirs_exist_ok=True)` 之后的任何检查（mtime、日志行）只能报告损失，阻止不了损失"
    - "撞上已占用的目标时「另选一个名字」优于「停下来报错」：报错会让门变成一次性的，而最省事的绕过恰好是手工执行那次覆盖"
    - "一条依赖破坏性实跑才能观察的不变量，要另配一条 `tmp_path` 单测直接考它 —— 否则它只被证明过一次，此后靠信任维持"
    - "空载的断言要打印记录数并在注释里点名它真正被覆盖的地方，让「0 条」读作一次测量而不是一次通过"

key-files:
  created: []
  modified:
    - tests/test_crsp_rebuild_measurements.py
    - .planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/deferred-items.md

key-decisions:
  - "备份代目录的保护做成 `_fresh_backup_dir()` 递进换名而不是撞上即 `pytest.fail`：两者都满足「复制之前判定」，但报错会让这个门在第一次跑完之后永远红，而最省事的解法恰好是 `rm -rf` 掉那个目录——手工执行这条保护要阻止的那次覆盖"
  - "`_assert_breakdown_shape()` 刻意**不**检查非空：两支的非空期望互不相容（`roster_overrides` 必须非空、`dropped_permnos` 实测为空），把它折进 helper 会逼其中一支说谎；非空由各自的调用点自己声明"
  - "`dropped_permnos` 那一支不断非空、也不静默通过：`print` 出记录数（实测 0），注释里点名它真正被覆盖的地方是 `tests/test_crsp_identity.py:1085-1103` 的合成 QQQ 用例，并写明两支同形是**由构造**（共用 `_permno_breakdown`）而非由本次测量得出"
  - "G-03.11-5 第二条 `missing:`（接受盘上滞后并写进 deferred-items）按操作者裁定**不适用**，不两支都打勾"
  - "回归门那个 ±1 追到根因后记进 deferred-items（D-03.11-18-A）而不是就地修：修法涉及 `tests/test_entry_point_contracts.py` 那条契约自己的射程，超出本 plan 的 files_modified"

patterns-established:
  - "备份代目录只增不覆盖：每次重建拿走一个新名，已存在的一律只读；名字由函数挑、由断言守，不靠人记得换"
  - "「文件是新的」与「文件内容与代码一致」是两个问题：mtime 新鲜度断言答不了后者，G-03.11-5 正是后者失守"

requirements-completed: [DATA-03]

coverage:
  - id: D1
    description: "盘上 `.crsp_filter_report.json` 由当前 HEAD 重新生成，`roster_overrides.permnos` 每条记录的键集恰好 `{types, rows, first, last}`，冗余 `symbol` 字段消失"
    requirement: DATA-03
    verification:
      - kind: integration
        ref: "tests/test_crsp_rebuild_measurements.py#test_the_filter_report_carries_no_redundant_symbol_field（QUANTLAB_DATA_ROOT=/Users/daizhaorong/projects/quantlab，6 passed，跑了两次）"
        status: pass
      - kind: other
        ref: "python3 -c \"...json.load(...); assert all(set(v)=={'types','rows','first','last'} ...)\" → 退出码 0；实测 {\"75154\": {\"first\": \"2024-01-02\", \"last\": \"2024-12-31\", \"rows\": 252, \"types\": [\"UG/EQTY/COM/CORP/N\"]}}"
        status: pass
    human_judgment: false
  - id: D2
    description: "PERMNO 75154 那条记录的端点语义未被删字段改动：rows 252、first 2024-01-02、last 2024-12-31、types 非空"
    requirement: DATA-03
    verification:
      - kind: integration
        ref: "同上测试内的四条逐字段断言（ROSTER_RESCUED_ROWS / _FIRST / _LAST + types 非空）"
        status: pass
    human_judgment: false
  - id: D3
    description: "交给 `rebuild()` 的备份代目录在交付之前就已被判定为不存在（前置，不是事后观察）"
    requirement: DATA-03
    verification:
      - kind: other
        ref: "源码顺序检查：`assert not backup_dir.exists()` 在第 346 行，`rebuilder.rebuild(` 在第 362 行 → 退出码 0"
        status: pass
      - kind: unit
        ref: "tests/test_crsp_rebuild_measurements.py#test_a_fresh_backup_generation_never_lands_on_an_existing_one（只用 tmp_path，不设 QUANTLAB_DATA_ROOT → 1 passed, 5 deselected）"
        status: pass
    human_judgment: false
  - id: D4
    description: "本次运行之前就存在的三个代目录全部原封未动；两次重建各取一个新代目录"
    requirement: DATA-03
    verification:
      - kind: other
        ref: "运行前后 `stat -f '%m'` 逐字相同：_backup_pre_03.11=1789959727、_backup_pre_03.11_10=1789975991、_backup_deleted_custom_store_03.11=1789988009；新增 _18=1790035667、_18_rerun2=1790035799"
        status: pass
    human_judgment: false
  - id: D5
    description: "raw tier 在任何删除动作之前被确认在位（132 个 .pqt + 6 张参考表 + manifest）"
    requirement: DATA-03
    verification:
      - kind: other
        ref: "find .../wrds -name '*.pqt' -type f | wc -l → 132；ls .../_reference → 6 parquet + manifest.json（均在第一次 rebuild 之前执行）"
        status: pass
    human_judgment: false
  - id: D6
    description: "`dropped_permnos` 那一支的 0 条被打成一次可见的测量，并指向它真正被覆盖的地方，而不是被一个空循环签收"
    requirement: DATA-03
    verification:
      - kind: integration
        ref: "`-s` 输出含 `dropped_permnos: 0 record(s)`；grep 复核：非空断言计数 0、helper 定义计数 1"
        status: pass
    human_judgment: true
    rationale: "grep 与 print 能证明「记录数被打印了」「没有非空断言」，证明不了那段注释对下一个读者是否真的把「0 条」读成测量而不是通过。措辞是否成立要人读一遍。"
  - id: D7
    description: "回归门无新增失败（55 failed / 1700 passed / 1 skipped，失败集合逐文件与 D-03.11-12-A 一致）；四个 gap-closure suite 120 passed"
    verification:
      - kind: integration
        ref: "uv run pytest -q --ignore=tests/test_factor_hierarchy.py --ignore=tests/test_crsp_rebuild_measurements.py → 55 failed, 1700 passed, 1 skipped in 263.09s"
        status: pass
      - kind: integration
        ref: "uv run pytest -q tests/test_crsp_ticker_sidecar.py tests/test_model_predict_panel.py tests/test_crsp_identity.py tests/test_extensibility_contract.py -p no:cacheprovider → 120 passed"
        status: pass
    human_judgment: false
  - id: D8
    description: "四个 gap 的 11 条 `missing:` 逐条对账，out-of-scope 的项目显式标为未处理"
    verification: []
    human_judgment: true
    rationale: "对账表的每一行都是一次判断——「这条 missing 真的被那条断言兑现了吗」。本 plan 只能核对自己那两条；另外九条的兑现证据来自 14/15/17 三份 SUMMARY，需要相位验收的人独立确认。"

duration: ~30 min
completed: 2026-09-22
status: complete
---

# Phase 03.11 Plan 18: 盘上审计产物重生成 + 备份代目录只增不覆盖 Summary

**盘上 `.crsp_filter_report.json` 由当前 HEAD 重新生成，那个与自己 JSON key 逐字节相同的 `symbol` 字段从盘上消失（G-03.11-5 关闭）；同时把「备份代目录只增不覆盖」从一条纪律变成一道排在 `rebuild()` 之前的前置判定，由源码顺序检查与一条 `tmp_path` 单测各锁一半——这条保护当天就兑现了，第二次重建落在 `_18_rerun2`，第一代展品原封未动。**

## Performance

- **Duration:** ~30 min（起始时刻为重建，未在首次读文件前捕获 `PLAN_START_TIME`，此处按首次实跑往前推算）
- **Started:** ~2026-09-21T23:52:00Z
- **Completed:** 2026-09-22T00:22:00Z
- **Tasks:** 3
- **Files modified:** 2（新建 0）

## 数据根解析（防假绿的那条区分）

| 项 | 值 |
|---|---|
| 解析出的 data_root | **`/Users/daizhaorong/projects/quantlab`**（主仓库，非 worktree） |
| 被测代码来自 | **`/Users/daizhaorong/projects/quantlab/.claude/worktrees/agent-a9392cb1aabb23c93`**（worktree） |

两件事并列可见，是这个门防假绿的核心。证据：测量记录自报 `data_root: /Users/daizhaorong/projects/quantlab`（并被测试自身的 `endswith("/quantlab")` 断言），而同一次运行的警告行指向 `.../.claude/worktrees/agent-a9392cb1aabb23c93/quantlab/dataset/crsp.py:1168` —— 数据来自主树、代码来自工作树。

解析手法沿用 03.11-10：先单跑 `git rev-parse --path-format=absolute --git-common-dir` 得到 `/Users/daizhaorong/projects/quantlab/.git`，其父目录显式传给 pytest。**未**把 `$(git rev-parse ...)` 嵌进环境变量赋值——那个命令形状会被 worktree 隔离守卫拒绝（03.11-01 与 03.11-10 都踩过，本次照记录规避，未复现）。

## RED 证据：重建之前盘上的原文

在任何删除动作之前读出的 `data/data/us_equity/1d/wrds_crsp_sp500_1d.zarr.crsp_filter_report.json`：

```json
"roster_overrides": {
  "permnos": {
    "75154": {
      "first": "2024-01-02",
      "last": "2024-12-31",
      "rows": 252,
      "symbol": "75154",
      "types": ["UG/EQTY/COM/CORP/N"]
    }
  },
  "rows_rescued": 252,
  "sources": [
    "config.permnos: 520 PERMNO(s) named explicitly, exempt on every date",
    "config.roster_universe='crsp_sp500': 1956 member PERMNO(s) over 2084 membership spell(s), exempt on the dates inside a spell"
  ]
},
"dropped_permnos": {}
```

`"symbol": "75154"` 与它自己的 JSON key 逐字节相同 —— 正是 UAT test 2 裁定删除、plan 13 已从代码里删掉、却仍留在盘上的那个字段。`dropped_permnos` 实测为 `{}`，与规划时一致，这决定了那一支只能如实记为「无从观察」。

**这一代已被保存**在 `data/_backup_pre_03.11_18/wrds_crsp_sp500_1d.zarr.crsp_filter_report.json`，复核读出的正是上面这条带 `symbol` 的记录。写它的代码路径已经改了，这是它唯一的记录。

## raw tier 前置确认（在任何删除之前）

| 项 | 判据 | 实测 |
|---|---|---|
| `data/downloads/us_equity/1d/wrds_crsp/wrds` 下 `.pqt` | 132 | **132** ✅ |
| 兄弟目录 `_reference` | 6 张参考表 + `manifest.json` | `ccmxpf_lnkhist` / `dsp500list_v2` / `idxcst_his` / `stkdelists` / `stkdistributions` / `stksecurityinfohist` + `manifest.json` ✅ |

一处值得记一笔：`.pqt` 是 hive 分区的，直接 `ls | grep -c '\.pqt$'` 在顶层返回 **0**（顶层只有 84 个 `month=YYYY-MM` 目录）。必须递归 `find ... -name '*.pqt' -type f` 才数得到 132。第一次数出 0 时没有把它当成「raw tier 缺失」去改判据，而是去看目录长什么样——判据没错，数法错了。

## 重建实测（**两次**，数字逐字相同）

`-s` 打出的七行证据（取自 **Task 1 的第一次运行**，按计划「取证只认这一次」）：

```
backup generation: /Users/daizhaorong/projects/quantlab/data/_backup_pre_03.11_18
data_root:    /Users/daizhaorong/projects/quantlab
store_path:   /Users/daizhaorong/projects/quantlab/data/data/us_equity/1d/wrds_crsp_sp500_1d.zarr
dims:         {'timestamp': 252, 'symbol': 520}
metrics:      {'anomaly_flag_true': 13, 'adj_close_le_zero': 0, 'close_eq_zero': 0,
               'adj_close_nan': 764, 'adj_volume_nan': 764, 'structural_gaps': 764,
               'symbol_count': 520}
removed:      (store 本体, .chunks.json, .crsp_adjustment.json,
               .crsp_filter_report.json, .crsp_tickers.json)
backup_path:  /Users/daizhaorong/projects/quantlab/data/_backup_pre_03.11_18
data_vars:    27
symbol_count: 520 (W0 ticker axis: 523, band: 500-540)
symbol dtype: int64 (kind 'i')
ticker intervals: 520 PERMNO(s)
vintage_product_end: 2025-12-31
roster_overrides.permnos: 1 record(s)
```

（Task 2 的 verify 让同一个门再跑一遍，`backup generation` / `backup_path` 变成
`_backup_pre_03.11_18_rerun2`，**其余每一个数字逐字相同**，并多打出 `dropped_permnos: 0 record(s)`。
两次都绿即观察到了键集这条性质的幂等；逐字节确定性仍按 backstop 记，本 plan 未逐字节比对两代产物。）

### 既有断言全部仍成立（一条未放宽）

| 断言 | 判据 | 实测 |
|---|---|---|
| `adj_close_le_zero` | == 0 | **0** ✅ |
| `close_eq_zero` | == 0 | **0** ✅ |
| `anomaly_flag_true` | **等值** 13 | **13** ✅（未改成不等式） |
| `adj_close_nan` | == `structural_gaps` | 764 == 764 ✅ |
| `adj_volume_nan` | == `structural_gaps` | 764 == 764 ✅ |
| `symbol` 轴原始 Zarr dtype | kind `'i'` | **int64** ✅ |
| `.crsp_symbology_report.json` | 不存在 | 不存在 ✅ |
| `.crsp_tickers.json` 的 PERMNO 集合 | == 面板轴 | 520 == 520，且**按集合**相等 ✅ |
| `symbol_count` | 落在 [500, 540] | **520** ✅ |

`validate_schema` 自报 `764/131040 (0.6%)` —— 同一条规则同一个数，与 03.11-10 一致。

### 重建后的盘上原文

```json
"roster_overrides": {"permnos": {"75154": {"first": "2024-01-02", "last": "2024-12-31",
                                           "rows": 252, "types": ["UG/EQTY/COM/CORP/N"]}}}
"dropped_permnos": {}
```

键集 `['first', 'last', 'rows', 'types']` —— 恰好四项，`symbol` 消失。端点语义未被删字段改动。

## 备份代目录账本（「只增不覆盖」在纸面上可查）

| 目录 | 大小 | 运行前 mtime | 运行后 mtime | 判定 |
|---|---|---|---|---|
| `data/_backup_pre_03.11` | 10M | `1789959727` (Sep 20 23:02:07) | `1789959727` | **未被触碰** ✅ |
| `data/_backup_pre_03.11_10` | 10M | `1789975991` (Sep 21 03:33:11) | `1789975991` | **未被触碰** ✅ |
| `data/_backup_deleted_custom_store_03.11` | 784K | `1789988009` (Sep 21 06:53:29) | `1789988009` | **未被触碰** ✅ |
| `data/_backup_pre_03.11_18` | 10M | 不存在 | `1790035667` (Sep 21 20:07:47) | 新建：**带冗余字段那一代的唯一记录** |
| `data/_backup_pre_03.11_18_rerun2` | 10M | 不存在 | `1790035799` (Sep 21 20:09:59) | 新建：第二次重建（已是修好的那一代） |

**关键的一行是第四行的「运行后 mtime」：`_18` 在第二次重建之后 mtime 仍是 `1790035667`。** 第二次重建没有碰它，而是自己去拿了 `_rerun2`。这就是第二步之二那套机制存在的全部理由——如果 fixture 还像 03.11-10 那样写死一个目录名，第二次运行会拿刚重建完的 store 把展品盖掉，盘上只剩两份一模一样的新代。判据用的是「运行前存在的都没被动过」这个一般性质，运行前先 `stat` 记了基线，不是只盯着两个已知旧代名。

**未为腾地方删除任何代目录**，未对 `data/` 下任何已存在目录执行写操作。

## Task Commits

1. **Task 1（tracer）：盘上键集断言 + 备份代目录前置不变量** — `e1544db` (test)
2. **Task 2：两支共用键集判据，空载那一支打成可见测量** — `3c290b0` (test)
3. **Task 3：回归门 ±1 的归因（D-03.11-18-A）** — `c7f00ab` (docs)

**Plan metadata:** 见本文件的 docs commit。

`commits: 3` 是写 SUMMARY 时用 `git rev-list --count 1af47c8..HEAD` **测**出来的，不是数出来的；`plan_head_before: 1af47c8f9b51949c59d6c81802586f5882c2fe6e` 一并记录，供 `/gsd-verify-work` 用同一把尺复核。不含随后那次 docs 提交（与本 phase 其他 plan 同一口径）。

## Tracer feedback gate（Task 1）

Task 1 是 `type="tracer"`。按 `checkpoints.md` 的精度链逐行判：

| # | 条件 | 本次 |
|---|---|---|
| 1 | 任务带 `gate="blocking-human"` | **否**（无 `gate` 属性） |
| 2 | auto 模式（`AUTO_CHAIN` / `AUTO_CFG`） | **否**：`.planning/config.json` 的 `workflow.auto_advance` 与 `workflow._auto_chain_active` 均为 `false` |
| 3 | 交互 + `end-of-phase` + `<verify>` 只含 `<automated>` | **命中**：`workflow.human_verify_mode` 缺省即 `end-of-phase`，本任务四条 `<verify>` 全是 `<automated>`、无 `<human-check>` |

→ **重跑 verify，通过则继续扩展、不生成 checkpoint。** 提交后原样重跑：源码顺序检查退出码 0；`-k fresh_backup` → `1 passed, 5 deselected`；盘上 JSON 键集检查退出码 0。第三条（破坏性全门）在提交前刚以同一份代码跑出 `6 passed`，提交未改动任何被测内容，故未再触发一次额外的破坏性重建——它随即作为 Task 2 的 verify 完整重跑了一遍，仍 `6 passed`。⚡ Tracer verified end-to-end — expanding。

## Task 3：四个 gap 的 11 条 `missing:` 逐条对账

| gap | `03.11-VERIFICATION.md` 的 `missing:` | 由谁兑现 |
|---|---|---|
| G-03.11-3 | WR-01：把 `RecursionError` 加进 `payload` 属性的 except 元组 | **03.11-15 Task 1 GREEN** `4da320d` —— 收在 `payload` 而不是 `label`，两个入口才都被覆盖 |
| G-03.11-3 | WR-01：补一条深嵌套 JSON 的回归（解析期失败与结构损坏分开） | **03.11-15 Task 1 RED** `2169328` —— `nested-past-the-parser` 参数化，RED 时以 `RecursionError` 形式红 |
| G-03.11-3 | WR-02：**需要操作者裁定**——收窄元组，或改 docstring，不要两者都留 | **03.11-15 Task 2 GREEN** `b10806f` —— 走收窄一支，提为模块常量 `_UNUSABLE` |
| G-03.11-3 | 若选收窄：补一条「本模块内部 bug 会上抛」的回归 | **03.11-15 Task 2 RED** `f989ff2` —— 两条子类注入，RED 时 `DID NOT RAISE KeyError` / `AttributeError` |
| G-03.11-4 | 清除全仓字节码缓存 | **03.11-14 Task 1** —— 17 个 `__pycache__` → 0，无提交（按设计 gitignored） |
| G-03.11-4 | 清除后重跑回归门，重新记录基线与当前两组数字 | **03.11-14 Task 1/3** —— 四 suite `98 passed`、回归门 `55 failed, 1679 passed, 1 skipped` |
| G-03.11-4 | 把「等长注入 + 恢复 → `.pyc` 不失效」写进注意事项 | **03.11-14 Task 2** `709eceb`（`tests/test_stale_bytecode_lesson.py`，3 条子进程用例）+ **Task 3** `597ac7d`（`tests/conftest.py` 散文，与 macOS OpenMP 守卫并列） |
| **G-03.11-5** | 用 `CrspStoreRebuilder` 重生成 `.crsp_filter_report.json`，并复核 `roster_overrides.permnos` 的键集为 `{types, rows, first, last}` | **本 plan Task 1** `e1544db` —— 实跑重建两次，盘上键集实测 `['first','last','rows','types']`；断言进入常设门（`test_the_filter_report_carries_no_redundant_symbol_field`） |
| **G-03.11-5** | 或：明确裁定接受盘上滞后并写进 `deferred-items.md` | **按操作者裁定不适用** —— 这是二选一的另一支，已走第一支（重生成）。**不打勾**，也不写进 deferred-items |
| G-03.11-6 | WR-04：让 `product_end` 走同一条形状检查（或抽 `_object_payload()`） | **03.11-17 Task 2** `123264c` (RED) + `febca46` (GREEN) —— 走抽 helper 不删，三条 `product_end` 回归 |
| G-03.11-6 | WR-03：加一次性降级日志 `_degrade()`，并在既有参数化里断言 `warning_messages` | **03.11-17 Task 1** `9b899d6` (RED) + `b063ba4` (GREEN) —— 既有参数化补 `warning_messages`，另加 at-most-once / absent / non-integer / per-permno 四组 |

### 显式标为**未处理**的项（不从表里消失）

| 项 | 状态 | 为什么本轮不碰 |
|---|---|---|
| **UAT test 4** —— `_compare_fingerprints` warning 风暴 backstop | **仍 `blocked_by: other`，未处理** | 需要人跑一次 load 模式的真实回测才能确认那屏 warning 没有掩盖真实问题（D-12 已接受指纹失效本身）。本 plan 又重建了一次 store，所以既有 run 的指纹仍然失效——这条账没有变小 |
| **D-03.11-12-A** —— 55 个 `ingest_*` 导入失败 | **未处理**（本相位基线内） | 相位基准提交就已存在（`git ls-tree` 在基准提交上 grep ingest 为空），修它是重指十个测试文件的独立工作 |
| **D-03.11-12-B** —— KunQuant `~MultiThreadExecutor()` 偶发死锁 | **未处理** | 间歇性；本轮回归门 263.09s 正常结束，**未复现** |
| **D-03.11-12-C** —— `FactorConfig.njobs` 默认 128 | **未处理** | `quantlab/base/` 的生产代码，在本 plan 的 `files_modified` 之外 |
| **`.planning/WINDOWS.md` 条目 28** —— custom store 未重建 | **未处理** | 03.11-10 开的账：`wrds_crsp_custom_1d.zarr` 仍是迁移前的 ticker 轴面板。它的窗口/名册本相位从未指定，凭猜构造配置做破坏性重建是 Rule 4 的形状 |
| **D-03.11-18-A**（本 plan 新开） | **已归因，未修** | 见下节 |

## 回归门：55 failed, 1700 passed, 1 skipped（263.09s）

失败集合逐文件与 `deferred-items.md` D-03.11-12-A 的表**逐行相同**：

| 文件 | 本次 | D-03.11-12-A |
|---|---|---|
| `tests/test_ingest_shells.py` | 14 | 14 |
| `tests/test_ingest_tiingo_universe_wiring.py` | 11 | 11 |
| `tests/test_data_dir_cli.py` | 10 | 10 |
| `tests/test_ingest_conversion_gate.py` | 6 | 6 |
| `tests/test_volume_guard.py` | 5 | 5 |
| `tests/test_factor_kunquant.py` | 3 | 3 |
| `tests/test_chunked_ingest.py` | 2 | 2 |
| `tests/test_spot_dataset.py` | 2 | 2 |
| `tests/test_entry_point_contracts.py` | 1 | 1 |
| `tests/test_universe.py` | 1 | 1 |
| **合计** | **55** | **55** |

**本 plan 新增失败 = 0。** 结构上也不可能有：本 plan 唯一改动的测试文件正是回归门 `--ignore` 掉的那一个（不被收集），另一个改动是规划文档。

### passed 差值逐 plan 拆解

1700 > 03.11-14 Task 3 记录的 1679 ✅。以 D-03.11-12-A 记录的相位基线 **1675** 为起点，逐 plan 拆开后**逐字对上**：

| 来源 | 增量 | 依据（本 worktree 实测的收集数） | 累计 passed |
|---|---|---|---|
| D-03.11-12-A 记录的相位基线 | — | — | 1675 |
| 03.11-14 | **+3** | `tests/test_stale_bytecode_lesson.py` 收集 3 条 | 1678 |
| 03.11-15 | **+8** | `test_crsp_ticker_sidecar.py` 26 → 34（六条解析期 + 两条子类注入） | 1686 |
| 03.11-16 | **+0** | 只加强既有断言，未新增用例 | 1686 |
| 03.11-17 | **+14** | `test_crsp_ticker_sidecar.py` 34 → 48（5+1 / 4+3 / 1） | **1700** |
| **03.11-18（本 plan）** | **+0** | 两条新用例都在被 `--ignore` 的文件里 | **1700** |

`test_crsp_ticker_sidecar.py` 本 worktree 实测收集 **48** 条（26 + 8 + 14 = 48 ✅），`test_stale_bytecode_lesson.py` 实测 **3** 条。1675 + 3 + 8 + 0 + 14 + 0 = **1700** = 实测值。

本 plan 自己的真实数据门（被回归门排除，单独跑）从 **4** 个用例扩到 **6** 个。

### 四个 gap-closure suite

默认缓存目录下（03.11-14 之后不需要 `pycache_prefix`）：
`uv run pytest -q tests/test_crsp_ticker_sidecar.py tests/test_model_predict_panel.py tests/test_crsp_identity.py tests/test_extensibility_contract.py -p no:cacheprovider` → **120 passed** in 13.83s，与 03.11-17 记录的 120 一致，≥ 98 ✅。

### 排除性质未被削弱（本 plan 改的正是那个被排除的文件）

| 排除的理由 | 判据 | 实测 |
|---|---|---|
| 1. `_data_root()` 缺变量时 `pytest.fail` 而**不是** skip | `grep -c 'pytest.fail'` ≥ 3 | **6**（3 个调用点 + 3 处散文），`pytest.fail(` 调用点 **3** ✅ |
| | `grep -cE '^\s*pytest\.skip\('` == 0 | **0** ✅ |
| | `grep -c 'pytest.mark.skipif'` == 0 | **0** ✅ |
| | **行为复核**：不设 `QUANTLAB_DATA_ROOT` 单跑该文件 | `1 passed, 5 errors` —— errors 不是 skips，且重建**未发生**（`_data_root()` 在任何删除之前就停下） ✅ |
| 2. `rebuilt` fixture 仍是 module scope 的破坏性重建 | `grep -c 'scope="module"'` == 1 | **1** ✅ |
| 回归门的 `--ignore` 仍点名它 | `grep -c 'test_crsp_rebuild_measurements' .planning/config.json` == 1 | **1** ✅ |

那次「不设变量单跑」还顺带证明了第二步之三那条单测的独立性：`1 passed` 的正是 `fresh_backup` 那一条——它没有把 `rebuilt` fixture 拉起来。

## Files Created/Modified

- `tests/test_crsp_rebuild_measurements.py`（+264/−10，4 个用例 → **6 个**）
  - 模块 docstring 补上本次新增的两组内容，两条排除理由一字未动
  - 新常量：`FILTER_REPORT_SIDECAR`、`BREAKDOWN_RECORD_KEYS`、`ROSTER_RESCUED_PERMNO/_ROWS/_FIRST/_LAST`、`BACKUP_GENERATION_BASE`
  - 新模块级 helper：`_fresh_backup_dir()`（挑一个保证不存在的代目录，撞上则递进 `_rerun2`/`_rerun3`/…）、`_assert_breakdown_shape()`（两支共用的键集判据，刻意不含非空检查）
  - `rebuilt` fixture：`backup_dir = _fresh_backup_dir(root)` → `assert not backup_dir.exists()` → `print` → 才是 `rebuilder.rebuild(backup_dir=backup_dir)`；docstring 把「这一次为什么换目录」升格成「代目录只增不覆盖」这条常规，并列出三代各自是什么
  - 新用例 `test_a_fresh_backup_generation_never_lands_on_an_existing_one`（只用 `tmp_path`）
  - 新用例 `test_the_filter_report_carries_no_redundant_symbol_field`（读盘；非空 → 键集 → 逐字段值；`dropped_permnos` 那一支打印 0 并注明空载）
- `.planning/phases/03.11-.../deferred-items.md`（+44，116 → **160** 行，纯追加，三条既有条目未动）—— 新增 D-03.11-18-A

**未使用任何 `sed` 范围删除**，全部经 Edit 工具的精确字符串替换；deferred-items 的行数 116 → 160 与 `+44` 对得上。

## Decisions Made

见 frontmatter `key-decisions` 五条。额外一条执行期的：

**回归门那个 −1 先追根因再下结论，没有当成「已知波动」放过去。** orchestrator 给的基线是 `55 failed, 1701 passed`，本 worktree 实测 `1700`。相同的未归因 ±1 被 03.11-09 与 03.11-10 各记过一次（03.11-10 的 Issues §2：「1667 vs 1668 … 同一处未归因的 ±1」）。根因是 `tests/test_entry_point_contracts.py:44` 的 `ENTRY_POINTS = sorted(REPO_ROOT.glob("*.py"))` 读的是**工作树**而不是 git：主仓库根上躺着一个未被 git 跟踪的 `jerry_query_data.py`（`git ls-files --error-unmatch` 实测「did not match any file(s) known to git」），worktree 里没有它，于是主树多收集一条通过的参数化用例。collected 1757 vs 1756，逐项对得上。**两个数字都不是错的**，差的是测量所在的那棵树。记进 D-03.11-18-A，未就地修——修法涉及那条契约测试自己声称的射程（它现在断言的是「根目录下**每一个** `.py` 都能安全 import」，包括别人随手丢进去的临时脚本），是一个关于契约的问题，不是关于本相位的问题。

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] raw tier 的 `.pqt` 用顶层 `ls` 数出来是 0**

- **Found during:** Task 1（前置确认，在任何删除之前）
- **Issue:** 按 `<precondition>` 数 `data/downloads/us_equity/1d/wrds_crsp/wrds` 下的 132 个 `.pqt` 时，`ls -1 ... | grep -c '\.pqt$'` 返回 **0**。若照字面判定，这就是「raw tier 缺失」，按计划应当 `pytest.fail` 并中止上报。
- **Fix:** 先去看目录长什么样，而不是先改判据。顶层是 84 个 hive 分区目录（`month=2019-01` … ），`.pqt` 在分区内。改用 `find ... -name '*.pqt' -type f | wc -l` → **132**，与计划一致。判据没错，数法错了。
- **Files modified:** 无（纯验证动作）
- **Verification:** 132 与 `_reference` 的 6 张表 + `manifest.json` 都在位；随后 `rebuild()` 自己的 `assert_inputs_present()` 也通过（它若不通过会在任何删除之前抛 `FileNotFoundError`）
- **Committed in:** 无（无源码改动）

**2. [Rule 2 - Missing Critical] 回归门 passed 数比给定基线少 1，不能当波动放过**

- **Found during:** Task 3
- **Issue:** 实测 1700 vs 交接说明的 1701。计划的 `<fails_when>` 写着「passed 数低于 03.11-14 Task 3 记录的数字」即算失败（1679，本次 1700 高于它），但与**当前**基线差 1 仍是一个必须解释的事实——两个前序 plan 已经把它当未解释项留下过两次。
- **Fix:** 追到根因（`REPO_ROOT.glob("*.py")` 读工作树 + 主树上一个未跟踪的 `jerry_query_data.py`），实测 collected 1757 vs 1756 印证，记入 `deferred-items.md` 的 D-03.11-18-A。未修那条契约测试（超出 `files_modified`）。
- **Files modified:** `.planning/phases/03.11-.../deferred-items.md`
- **Verification:** `git ls-files --error-unmatch jerry_query_data.py` 报「did not match any file(s) known to git」；两棵树的 `ls *.py` 逐行 diff 只差这一个文件；`--collect-only` 实测 1756
- **Committed in:** `c7f00ab`

---

**Total deviations:** 2（1 blocking 自解，1 missing-critical 归因并立项）
**Impact on plan:** 都在计划意图之内，零范围外源码改动。偏差 1 反而印证了前置确认的价值——它在任何删除之前发生。偏差 2 关掉了一条跨三个 plan 的未解释账。

## Issues Encountered

1. **嵌套 `$(git rev-parse ...)` 的命令形状仍被 worktree 隔离守卫拒绝**（与 03.11-01、03.11-10 同一处置）。按记录规避：先单跑 `git rev-parse --path-format=absolute --git-common-dir`，把父目录 `/Users/daizhaorong/projects/quantlab` 显式写进同一条 pytest 命令。两种形式解析出的值逐字相同，且都由同一条 git 命令产生。守卫另外拒绝了两条含运行时变量的 `sed` / `git` 复合命令，均拆成单条重跑。

2. **`dropped_permnos` 这一支这次没有被观察到，如实记录。** 它在本 store 上实测为 `{}`，本轮**未**对它做任何有效断言——`_assert_breakdown_shape` 在它上面跑了零条记录。本 SUMMARY**不**声称这一支已验证。它真正被覆盖的地方是 `tests/test_crsp_identity.py:1085-1103` 的合成 QQQ 用例（那里 `dropped_permnos` 确有记录，同样断 `set(record) == {types, rows, first, last}`）。两支形状一致的理由是**结构性**的（`crsp.py:1099` 与 `:1116` 共用同一个 `_permno_breakdown` 渲染），而这恰恰也是不能拿它当已验证的理由：共用渲染说明「大概率也对」，不说明「已经测过」。

3. **本 plan 又重建了一次 store，所以既有 backtest run 的指纹仍然失效**（D-12 已接受）。UAT test 4 那条 backstop 因此没有变小，仍需要人跑一次 load 模式的真实回测。已在对账表里显式列为未处理。

## Known Stubs

None。本 plan 没有留下 stub、没有 `skip`、没有未跑的 `<verify>`。

一条如实记账（不是 stub，是明确的「无从观察」）：`dropped_permnos` 分支在这个 store 上空载，见 Issues §2。代码里以 `print` 记录数 + 一段点名合成用例的注释留痕，不靠 SUMMARY 单方面说明。

`.planning/WINDOWS.md` 本 plan 未新增条目；既有条目 28（custom store 未重建）仍 `open`，见对账表。

## Threat Flags

None。本 plan 不引入任何新的网络端点、认证路径或信任边界；重建走 `registry.convert()` 的离线路径（`quantlab/dataset/crsp_rebuild.py` 模块 docstring 与 `quantlab/acquisition/registry.py:565-571` 逐字保证不碰凭证、不碰网络），`pyproject.toml` 与 `uv.lock` 未改动（零安装）。

计划 `<threat_model>` 里五条 `mitigate` 的落实：

| Threat ID | 落实 |
|---|---|
| T-03.11-64（重建删除真实 store） | `rebuild()` 的 assert → backup → clear 顺序未变；raw tier 在任何删除之前被确认（132 + 6 + manifest）；备份写进新代目录，`backup_path` 实测指向 `_18`；旧代 mtime 逐个未变 |
| T-03.11-65（覆盖已存在的代目录） | **前置缓解已实装并当天兑现**：`_fresh_backup_dir()` + `assert not backup_dir.exists()`（源码第 346 行 vs `rebuild(` 第 362 行）；`tmp_path` 单测锁「已存在的不会被选中」；第二次重建实测落在 `_rerun2` 而 `_18` mtime 未变。既有的 mtime / `backup_path:` 两条事后判据保留 |
| T-03.11-65b（经由人手：删掉占用的目录再跑） | 机制上撞上不报错、直接另选名字，诱惑被拿掉；本轮**未对 `data/` 下任何已存在目录执行写或删** |
| T-03.11-66（skip 被读作 pass） | `pytest.skip(` / `skipif` 计数均为 0；行为复核实测 `1 passed, 5 errors`（errors 不是 skips） |
| T-03.11-68（空载断言伪装成验证） | `roster_overrides` 那一支先断 `len >= 1`（实测 1）再逐条查；`dropped_permnos` 那一支打印 0 条并在注释与本 SUMMARY 两处点名「无从观察」 |

T-03.11-67（失败消息打印绝对路径）按计划 `accept`，本 plan 未改这些消息。

## User Setup Required

None - no external service configuration required。重建是纯离线重转，不需要 WRDS 账号、Duo 推送或网络。

## Next Phase Readiness

- **G-03.11-5 关闭**：盘上审计产物与 HEAD 是同一件东西，且这条一致性进了每次重建都会跑的门。
- **给相位验收的三件事：**
  1. **UAT test 4 仍待人跑**（load 模式真实回测，确认 `_compare_fingerprints` 的 warning 风暴没有掩盖真实问题）。本 plan 又重建了一次 store，这条账没有变小。
  2. **`.planning/WINDOWS.md` 条目 28**（custom store 仍停在迁移前的 ticker 轴）需要 operator 决定补重建还是接受。
  3. **D-03.11-18-A** 需要一次关于契约射程的裁定：`test_entry_point_contracts.py` 要不要把 glob 收窄到 git 跟踪的文件。在此之前，主树与 worktree 的 passed 数差 1 是**预期**的，不要再当成回归追。
- **DATA-03 本 plan 已声明完成**；共享 ID 门会在最后一个声明它的 plan 产出 SUMMARY 时统一判定。
- **环境：** 回归门单跑 263.09s 正常结束，未复现 D-03.11-12-B 的 KunQuant 死锁；全程**未使用 `git stash`**、未使用 `-X pycache_prefix`、未对主仓库执行任何 git 写操作。
- **未修改 `STATE.md` / `ROADMAP.md`**（并行执行，由 orchestrator 合并后统一更新）。

---
*Phase: 03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r*
*Completed: 2026-09-22*

## Self-Check: PASSED

- **`key-files.modified` 两项 + 本 SUMMARY 自身**在盘上（`FOUND` 3/3）。
- **三个任务提交全部在 `git log 1af47c8..HEAD` 里**：`e1544db`、`3c290b0`、`c7f00ab`。`commits: 3` 是 `git rev-list --count 1af47c8..HEAD` **测**出来的；`plan_head_before` 已写进 frontmatter。工作树干净，`git diff --diff-filter=D` 为空（**零删除**）。
- **`STATE.md` / `ROADMAP.md` 未被修改** —— 不在本 plan 任何一次 `git add` 里，`git status` 干净。
- **Task 1 的 9 条 `<acceptance_criteria>` 逐条复跑：** RED 原文已抄进 SUMMARY 且含 `symbol` ✅；`grep -c 'backup_dir='` = **1** ✅；源码顺序检查退出码 **0**（第 346 行 < 第 362 行）✅；`-k fresh_backup` 不设变量 → **1 passed, 5 deselected**（非 `no tests ran`）✅；`backup generation:` 与 `backup_path:` 同指 `_backup_pre_03.11_18`，且它在运行前不存在 ✅；运行前存在的三个代目录 mtime 逐个未变（`1789959727` / `1789975991` / `1789988009`）✅；盘上键集 python3 检查无断言失败 ✅；75154 的 `rows`/`first`/`last`/`types` 逐字段对上 ✅；用例数 4 → **6**（`--collect-only` 实测 6）✅；`pytest.skip(` = **0**、`skipif` = **0** ✅。
- **Task 2 的 6 条逐条复跑：** `grep -c dropped_permnos` = **5** ≥ 2 ✅；`grep -c roster_overrides` = **4** ≥ 2 ✅；非空断言 `grep -c 'assert dropped_permnos\|assert len(dropped'` = **0** ✅；helper 定义计数 = **1** ✅；`-s` 输出含 `dropped_permnos: 0 record(s)` ✅；门 **6 passed** ✅。
- **Task 3 的 6 条逐条复跑：** `grep -c 'pytest.fail'` = **6** ≥ 3 ✅；`grep -c 'scope="module"'` = **1** ✅；`grep -c test_crsp_rebuild_measurements .planning/config.json` = **1** ✅；回归门失败集合逐文件与 D-03.11-12-A 一致（55 = 14+11+10+6+5+3+2+2+1+1）且 passed **1700** > 1679，差值已按 1675 基线逐 plan 拆解并逐字对上 ✅；四 suite **120 passed** ≥ 98 ✅；对账表含 11 条 `missing:`，UAT test 4 与 D-03.11-12-A/B/C 显式标为未处理 ✅。
- **计划级 `<verification>` 七条：** RED 原文已抄 ✅；键集恰好四项 + 75154 逐字段 ✅；新代目录由**前置**判定保证（源码顺序 + `tmp_path` 单测各锁一半），三个旧代 mtime 未变 ✅；既有四条清洁度断言 + int64 轴 + 两条 sidecar 断言全绿 ✅；`dropped_permnos` 的 0 条被打成测量且未声称已验证 ✅；回归门零新增失败、四 suite 全绿 ✅；对账表齐全 ✅。
- **`prohibitions` 逐条核对：** 无 skip、无 `skipif`（计数 0）✅；备份未指向 `_backup_pre_03.11` 或 `_backup_pre_03.11_10`（`backup_path` 实测 `_18` / `_18_rerun2`）✅；未让 `rebuild()` 在任何已存在的代目录上运行（两次均由 `_fresh_backup_dir` 挑出空名并 `assert not ... exists()`）✅；保护是前置的（源码第 346 行 < 第 362 行），事后的 mtime / `backup_path:` 两条判据保留但不充当这条保护 ✅；未为腾地方删除任何代目录（五个目录全部在位）✅；`anomaly_flag_true == 13` 等值断言未放宽 ✅；未声称 `dropped_permnos` 已验证（Issues §2 与代码注释两处写明）✅；`files_modified` 里没有 `.crsp_filter_report.json`、没有 `data/**` ✅。
- **项目规矩：** 全程未用 `git stash` ✅；未用 `-X pycache_prefix` 让任何东西变绿 ✅；未用 `sed` 范围删除（deferred-items 116 → 160 与 `+44` 对得上）✅；未走 `quantlab/config` 工厂（`_config()` 直接构造 `CrspDatasetConfig`）✅；未对主仓库执行任何 git 写操作 ✅。
</content>
</invoke>
