---
phase: 03.11
plan: 08
type: execute
wave: 5
depends_on: ["03.11-07"]
files_modified:
  - quantlab/base/config.py
  - quantlab/dataset/crsp.py
  - quantlab/base/factor.py
  - scripts/ingest_wrds_crsp.py
  - tests/test_ingest_wrds_crsp.py
  - tests/test_config_roundtrip.py
  - tests/test_jsonable.py
  - tests/test_crsp_identity.py
  - tests/test_no_identity_residue.py
autonomous: true
requirements: [DATA-03]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "symbol_overrides 与 nan_adj_at_permno_seam 两个配置字段被删除，没有写任何迁移或兼容层（D-04）"
    - "collision_universe 字段本身没有被删，只是改名为 roster_universe，因为它的第二个职责（显式名册覆盖 security_filter）还活着（D-17 / D-11 坑 2 / operator P8）"
    - "config.symbols 这个基类字段一个字未改：它有 12+ 个非 CRSP 读者，PERMNO 是 CRSP 专有标识，Alpaca 与 WRDS TAQ 根本没有 PERMNO（D-16 / operator RULING 3）"
    - "拒绝装在**两个** config setter 上，不是一个：CrspStockDataset 的 setter 拒绝非 None 的 CrspDatasetConfig.symbols，BaseFactorConfig.symbols 这个独立字段在 CRSP 背景的因子上同样被拒绝（D-16 的修正条款）"
    - "两条拒绝都在 config 赋值期发生并点名 permnos 作为替代，而不是在运行中途抛一个 KeyError（RESEARCH §R2-G）"
    - "quantlab/dataset/backend.py:1477-1479 的 filter_by_symbol 一个字未改 —— 类型纪律装在 config 层，不装在 backend（D-16）"
    - "crsp.py 里 config.symbols 的两个读者（钉轴过滤与窗口过滤）被删除（§R5e 选项 A）"
    - "symbol 这个维度名没有被重命名，quantlab/ 下的 215 处与 tests/ 下的 436 处 symbol 字面量原样保留（RULING 3）"
  artifacts:
    - quantlab/base/config.py
    - quantlab/dataset/crsp.py
    - quantlab/base/factor.py
    - scripts/ingest_wrds_crsp.py
  key_links:
    - "CrspStockDataset.config setter → 对 CrspDatasetConfig.symbols 的拒绝（base/config.py:17 继承而来）"
    - "BaseFactorConfig.symbols（base/config.py:289）→ base/factor.py:41-43 的 _auto_filter → XrBackend.filter_by_symbol:1477-1479 的裸 .sel（第二个拒绝点要挡在这条链路之前）"
    - "collision_universe → roster_universe：crsp.py:420-427 的 setter 校验、crsp.py:801-813 的报告文案、scripts/ingest_wrds_crsp.py:501 的 CLI 绑定"
  prohibitions:
    - "不得删除或重命名基类字段 BaseDatasetConfig.symbols（base/config.py:17）—— 12+ 个非 CRSP 读者，Binance 永远不会有 PERMNO（RULING 3）"
    - "不得删除 BaseFactorConfig.symbols（base/config.py:289）本身 —— 只加一条针对 CRSP 背景的拒绝"
    - "不得删除 collision_universe 字段本身（只改名）"
    - "不得修改 quantlab/dataset/backend.py:1477-1479 的 filter_by_symbol 或它的兄弟 filter_by_date:1473-1475（RULING 3：这一族的约定是 backend 不猜类型）"
    - "不得重命名 symbol 这个维度名（RULING 3）"
    - "不写任何 config 迁移或兼容层；旧 config.json 直接 TypeError 是 D-04 已接受的后果"
    - "不得删除 permnos 的 WR-01 空元组拒绝（crsp.py:382-396）"
    - "不修改 quantlab/dataset/cleaning.py（D-06）"
---

<!-- planner-discipline-allow: symbol_overrides, nan_adj_at_permno_seam, collision_universe -->

<objective>
收敛配置层：删掉两个只为 ticker 轴存在的字段，把一名两责的字段改成准确的名字，并把
「`config.symbols` 在 CRSP 面板上意味着什么」这个**未预决**的问题按 operator RULING 3 定下来。

**`symbols` 不能删——它是基类字段。** 全仓 reader 枚举显示它有 12+ 个非 CRSP 读者：`base/data.py:239`、
`base/factor.py:42/223/232`、`dataset/nbbo.py` ×4、`base/acquisition.py:1379/1765`、
`acquisition/inspector.py:125/146`、`acquisition/wrds_crsp.py:642`、以及三个 `scripts/*.py`。
PERMNO 是 CRSP 专有标识——Alpaca 和 WRDS TAQ 按 ticker 键，根本没有 PERMNO——所以基类字段不改名，
**`symbol` 这个维度名也不改**，只有它的内容在 CRSP 面板上变成 int64 PERMNO。

**修正条款（RULING 3 里新增、CONTEXT 与 RESEARCH 都没有的一条）：拒绝要装在两个 setter 上。**
有两个同名但不同类的 `symbols` 字段，在两个不同的流水线阶段过滤：
1. `CrspDatasetConfig`（继承自 `BaseDatasetConfig.symbols`，`base/config.py:17`），读者在
   `crsp.py:1367-1371` 与 `crsp.py:1445-1449`；
2. **`BaseFactorConfig.symbols`（`base/config.py:289`）——一个完全独立的字段**，被 `base/factor.py:41-43`
   的 `_auto_filter` 经 `XrBackend.filter_by_symbol`（`backend.py:1477-1479`，一行裸 `.sel`）消费。
   一个跑在 CRSP 面板上、`symbols=("AAPL",)` 的因子会拿字符串去 `.sel` 一个 int64 轴，运行中途抛 `KeyError`。

Purpose: 把 §R2-G 那个「跑到一半才 KeyError、而且报错信息误导」的形态变成两条赋值期的清晰拒绝。
Output: 删两个字段、改一个名、装两道拒绝、删 CRSP 侧的两个 `symbols` 读者。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-CONTEXT.md
@.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-RESEARCH.md
@.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-PATTERNS.md
@.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-07-SUMMARY.md
</context>

## Artifacts this phase produces

| 符号 | 位置 | 类型 |
|---|---|---|
| `roster_universe`（由 `collision_universe` 改名） | `quantlab/base/config.py` | 配置字段 |
| `CrspStockDataset.config` setter 里对 `config.symbols` 的拒绝 | `quantlab/dataset/crsp.py` | 守卫 |
| `BaseFactorConfig` 消费侧对 CRSP 背景 `symbols` 的拒绝 | `quantlab/base/factor.py` | 守卫 |

<tasks>

<task type="auto">
  <name>Task 1: 删两个字段 + collision_universe 改名为 roster_universe（D-17）</name>
  <files>quantlab/base/config.py, quantlab/dataset/crsp.py, scripts/ingest_wrds_crsp.py, tests/test_ingest_wrds_crsp.py</files>
  <read_first>
    - `quantlab/base/config.py:108-200`（`permnos` 字段 + `symbol_overrides:133-137` + `nan_adj_at_permno_seam:156-161` + `collision_universe:164-189` 的两责 docstring，逐字）
    - `quantlab/base/config.py:236-246`（`qqq_benchmark` 里的 `permnos=(QQQ_PERMNO,)` / `security_filter="none"` / `symbol_overrides={QQQ_PERMNO: "QQQ"}`）
    - `quantlab/dataset/crsp.py:370-432`（config setter：`permnos` 的 digit 校验与 WR-01 空元组拒绝 `:382-396`，以及 `collision_universe` 的 INDEXES 校验 `:420-427`）
    - `quantlab/dataset/crsp.py:754-781`（`_member_intervals`，plan 07 已重写 docstring）与 `:798-815`（`_roster_sources` 的报告文案）
    - `quantlab/dataset/crsp.py:860-920`（`_roster_exemption` / `_member_days`）
    - `scripts/ingest_wrds_crsp.py`（CLI 结构；`:501` 一带的 `collision_universe=args.universe`，以及 `:296-304` 的 PERMNO digit 校验）
    - `.planning/phases/03.11-...-r/03.11-RESEARCH.md` 的 `### (b)` 与 `### quantlab/base/config.py（438 行）`
  </read_first>
  <action>
**删（D-04：直接删，不写迁移、不留兼容层；旧 `config.json` 会在 `cls(config_cls(**config))` 处 `TypeError`，
这是已接受的后果）：**
- `base/config.py:133-137`：`symbol_overrides: dict[str, str] | None = None` 及其 `#:` 注释。
- `base/config.py:156-161`：`nan_adj_at_permno_seam: bool = True` 及其 `#:` 注释。
- `base/config.py:242`：`qqq_benchmark` 里的 `symbol_overrides={QQQ_PERMNO: "QQQ"}` 这一项。
  **保留同一个构造里的 `permnos=(QQQ_PERMNO,)`（`:241`）与 `security_filter="none"`（`:243`）**
  ——PERMNO 86755 全程不变，QQQ 的 QQQQ 时期本来就不需要钉。

**改名（D-17：`collision_universe` → `roster_universe`）。** 字段本身不能删——它的第二个职责（显式名册覆盖
`security_filter`，GAP-C）还活着，而且 operator P8「名册优先于安全过滤器」正是它的理由。选 `roster_universe`
是为了和已有的报告键 `roster_overrides`（`crsp.py:1068`）对齐。
- `base/config.py:164-189`：字段改名，**整段 docstring 重写**。它现在逐字论证「两个用途，第二个更大」
  以及「名字比职责窄，但没改，因为盘上已有的 `config.json` 按键回读，改名会破坏往返」——**D-04 已经消灭了
  那个成本**（store 要重建），而且第一个用途已经不存在了。新 docstring 只描述一个职责：它是一份显式名册，
  按日期覆盖 `security_filter`，覆盖结果记在 `roster_overrides` 里、绝不静默应用。沿用本文件的 `#:` 块 +
  大写强调关键句的体例。
- `base/config.py:113` 逐字写着「the inherited `symbols` is the TICKER-side one, applied after symbology」——
  符号学已经没了，这句必须改写（见 Task 2 的裁定）。
- 连带改名的调用点：`crsp.py:420-427` 的 setter 校验（含错误文案里的字段名）、`crsp.py:801-813` 的
  `_roster_sources` 分支与它拼出的报告文案、`crsp.py:871-874`、`scripts/ingest_wrds_crsp.py:501` 的
  `collision_universe=args.universe`。**CLI 的 `--universe` 参数名不改**——它对用户的语义一直是「名册」。
  改之前先 `grep -rn "collision_universe" quantlab/ scripts/ tests/ example/` 拿到完整清单，改完再跑一次确认归零
  （`example/` 的同步属于 plan 10）。

`tests/test_ingest_wrds_crsp.py`：`--permnos` 与改名相关的断言同步；**PERMNO digit 校验与 WR-01 的空元组
拒绝测试全部保留**。
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_ingest_wrds_crsp.py tests/test_config_roundtrip.py tests/test_jsonable.py -p no:cacheprovider</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line, or the summary line reports any failed or errored test</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -rc "symbol_overrides" quantlab/ scripts/` 每个文件均为 0
    - `grep -rc "nan_adj_at_permno_seam" quantlab/ scripts/` 每个文件均为 0
    - `grep -rc "collision_universe" quantlab/ scripts/` 每个文件均为 0
    - `grep -c "roster_universe" quantlab/base/config.py` ≥ 1 且 `grep -c "roster_universe" quantlab/dataset/crsp.py` ≥ 3
    - `grep -c "permnos=(QQQ_PERMNO,)" quantlab/base/config.py` 返回 1（QQQ 的 PERMNO 名册保留）
    - `uv run python -c "from quantlab.base.config import CrspDatasetConfig as C; import dataclasses as d; f={x.name for x in d.fields(C)}; assert 'symbol_overrides' not in f and 'nan_adj_at_permno_seam' not in f and 'roster_universe' in f and 'collision_universe' not in f and 'symbols' in f and 'permnos' in f, sorted(f)"` 退出码 0
    - `grep -c "EMPTY tuple" quantlab/dataset/crsp.py` ≥ 1（WR-01 的空元组拒绝仍在）
    - `uv run pytest -q tests/test_ingest_wrds_crsp.py tests/test_config_roundtrip.py tests/test_jsonable.py -p no:cacheprovider` 全绿
  </acceptance_criteria>
  <done>两个字段删净、一个字段改名到位、QQQ 的 PERMNO 名册与 WR-01 拒绝完好。</done>
  <reversibility rating="costly">
删字段让盘上已有的 `config.json` 在 `load_dataset_from_config` 处直接 `TypeError`，历史 backtest run
不可重放。D-04 已**预先**授予这个决定（「项目未进生产；删配置字段、不写迁移、重建 store；历史 backtest run
不可重放是接受的」），所以这不是一个待决的单向决策，不插入 checkpoint。
  </reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 拒绝之一 —— CrspStockDataset 的 config setter 拒绝非 None 的 symbols（RULING 3 选项 A）</name>
  <files>quantlab/dataset/crsp.py, quantlab/base/config.py, tests/test_crsp_identity.py</files>
  <read_first>
    - `quantlab/base/config.py:14-20`（`BaseDatasetConfig.symbols: tuple | None = None` —— **不改**）
    - `quantlab/base/config.py:108-131`（`permnos` 字段 docstring，`:113` 那句「the inherited `symbols` is the TICKER-side one」要改写）
    - `quantlab/dataset/crsp.py:370-432`（config setter 的既有校验体例）
    - `quantlab/dataset/crsp.py:1360-1380`（`config.symbols` 的钉轴过滤读者 —— 删）
    - `quantlab/dataset/crsp.py:1438-1452`（`elif self.config.symbols:` 的窗口过滤读者 —— 删）
    - `quantlab/dataset/backend.py:470-505`（S3 守卫体例）
    - `.planning/phases/03.11-...-r/03.11-RESEARCH.md` 的 `### (e)` 全节（12+ 个非 CRSP 读者的枚举与三选一的代价表）
  </read_first>
  <behavior>
    - `CrspStockDataset(CrspDatasetConfig(..., symbols=("AAPL",)))` 在 **config 赋值期**抛 `ValueError`，消息里出现字段名 `permnos` 作为替代
    - `symbols=None`（默认）时行为与改动前完全一致
    - `symbols=()` 与 `symbols=("AAPL",)` 都被拒（非 `None` 即拒，不区分空与非空 —— 空元组的两义性由 `permnos` 的 WR-01 单独处理）
    - 非 CRSP 的 Dataset 子类（Tiingo / Alpaca / NBBO）传 `symbols` 的行为一个字未变
    - `crsp.py` 里不再有任何读 `config.symbols` 的代码
  </behavior>
  <action>
按 RESEARCH §R5e 的**选项 A**（推荐项，代价最小）落地，并把它记成本 phase 的一条新决策：

1. 在 `CrspStockDataset` 的 config setter 里（`crsp.py:370-432` 那一段既有校验的体例内）加一道拒绝：
   `config.symbols` 非 `None` 时抛 `ValueError`。文案按 S3 体例——以
   `f"{self.class_name}: config.symbols ..."` 开头，说明**为什么**：这个面板的 `symbol` 轴是 int64 PERMNO
   （D-01），而 `symbols` 是基类的 ticker 侧字段，两个名字装一个事实会分叉；给出具体的下一步（改用
   `config.permnos`，它是 raw 侧的 PERMNO 名册，同时也是覆盖 `security_filter` 的显式名册）。
   把被拒的值 `!r` 出来，这样传错的人能一眼看到自己写了什么。
2. 删掉 `crsp.py:1367-1371` 的钉轴过滤与 `crsp.py:1445-1449` 的 `elif self.config.symbols:` 窗口过滤
   ——CRSP 里 `config.symbols` 只有这两个读者，删完这个字段在 CRSP 路径上就只剩「被拒绝」这一个语义。
   （plan 03 已把它们的类型改成 int 让其自洽；本任务是整段删除。）
3. 改写 `base/config.py:113` 那句「the inherited `symbols` is the TICKER-side one, applied after symbology」。
   符号学已经没了，而且现在 `symbols` 在 CRSP 上是被拒绝的。新文案要说清楚：`permnos` 是 raw 侧筛选 +
   显式名册；基类继承来的 `symbols` 在 `CrspStockDataset` 上**被拒绝**，因为 PERMNO 是 CRSP 专有标识。
4. **`base/config.py:17` 的基类字段一个字不改。** 它有 12+ 个非 CRSP 读者，Binance 永远不会有 PERMNO。
5. **`backend.py:1477-1479` 的 `filter_by_symbol` 一个字不改。** 它的兄弟 `filter_by_date:1473-1475` 同样是
   一行裸 `.sel`，这一族的约定是「backend 不猜类型」；类型纪律装在 config 层。

`tests/test_crsp_identity.py` 补上 `<behavior>` 的 5 条，其中非 CRSP 那条是 control arm。
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_crsp_identity.py tests/test_crsp_dataset.py -p no:cacheprovider</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line, or the summary line reports any failed or errored test</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "config.symbols" quantlab/dataset/crsp.py` ≤ 3（只剩 setter 那一条拒绝的判断与文案；读取用法由下一条单独断言为 0）
    - `grep -c "self.config.symbols" quantlab/dataset/crsp.py` 返回 0（读者已删净）
    - `grep -c 'symbols: tuple | None = None' quantlab/base/config.py` 返回 1（基类字段逐字保留）
    - `grep -c 'self.data = self.data.sel({col: list(symbols)})' quantlab/dataset/backend.py` 返回 1（`filter_by_symbol` 的裸 `.sel` 逐字保留）
    - `grep -c "TICKER-side one" quantlab/base/config.py` 返回 0（旧文案已改写）
    - `uv run pytest -q tests/test_crsp_identity.py tests/test_crsp_dataset.py -p no:cacheprovider` 全绿且新增 ≥ 5 个测试
  </acceptance_criteria>
  <done>CRSP 上用 ticker 选股的入口在赋值期被清晰拒绝并指向 permnos；基类字段与 backend 都未被触碰。</done>
  <reversibility rating="reversible">新增一道守卫 + 删两个读者；基类契约未变。</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 拒绝之二 —— 因子层的 BaseFactorConfig.symbols（RULING 3 的修正条款）</name>
  <files>quantlab/base/factor.py, tests/test_crsp_identity.py, tests/test_no_identity_residue.py</files>
  <read_first>
    - `quantlab/base/config.py:285-292`（`BaseFactorConfig` 的字段清单，`symbols: tuple[str, ...] | None = None` 在 `:289` —— **和 `BaseDatasetConfig.symbols` 是两个不同的字段**）
    - `quantlab/base/factor.py:35-50`（`_auto_filter`：`if self.config.symbols is not None: ... filter_by_symbol("symbol", self.config.symbols)`）
    - `quantlab/base/factor.py:215-240`（stream 模式的 `num_symbols` / `symbols` 读者）
    - `quantlab/dataset/backend.py:1473-1480`（`filter_by_date` 与 `filter_by_symbol` 两个兄弟，各一行裸 `.sel` —— **不改**）
    - `quantlab/dataset/crsp.py`（`CrspStockDataset` 的类名与 Task 2 的拒绝文案，本任务的文案要与它同形）
    - `quantlab/dataset/backend.py:470-505`（S3 守卫体例）
  </read_first>
  <behavior>
    - 一个 `dataset` 是 `CrspStockDataset` 实例的因子，config 里 `symbols` 非 `None` 时在**因子的 config 赋值期**抛 `ValueError`，消息同时点名 `permnos` 与「这个面板的 symbol 轴是 int64 PERMNO」
    - 同一个因子 `symbols=None` 时行为与改动前完全一致
    - 一个 `dataset` 是 Tiingo / Alpaca 的因子，`symbols=("AAPL",)` 的行为一个字未变（control arm）
    - 拒绝发生在 `_auto_filter` 被调用**之前**，所以永远不会走到 `XrBackend.filter_by_symbol` 的裸 `.sel`
    - `_auto_filter` 自身的代码一个字未改
  </behavior>
  <action>
**这一条是 operator RULING 3 里新增的修正条款**——CONTEXT.md 与 RESEARCH.md 都没有它，是在本次规划中追出来的：
`BaseFactorConfig.symbols`（`base/config.py:289`）是一个**和 `BaseDatasetConfig.symbols` 完全不同的字段**，
在流水线的另一个阶段过滤。一个 CRSP 背景的因子设了 `symbols=("AAPL",)`，会经 `base/factor.py:41-43` 的
`_auto_filter` 走到 `XrBackend.filter_by_symbol`（`backend.py:1477-1479`，一行裸 `.sel`），拿字符串去 `.sel`
一个 int64 轴，**运行中途抛 KeyError**。

实现：在 `FactorKunQuant` 的 config 属性 setter（或最靠近它的那个 config 赋值入口，先读 `base/factor.py`
确认既有体例）里加一道拒绝：当 `config.symbols` 非 `None` **且** 该因子绑定的 dataset 是
`CrspStockDataset` 的实例时，抛 `ValueError`。文案与 Task 2 的那条**同形**（同样的开头结构、同样点名
`permnos`、同样说明面板轴是 int64 PERMNO），这样两条拒绝读起来像同一条纪律的两个安装点，而不是两个
临时补丁。

**先找钩子，找不到才用 isinstance——这是有序的，不是二选一。** CLAUDE.md 的 Architectural Constraints
逐字记着本仓库的分层是**单向**的：`base` → `dataset`/`factor`/`label` → `dl_model`/`ml_model` → `backtest`，
「Circular imports: None observed」。在 `quantlab/base/factor.py` 里写一个 `isinstance(CrspStockDataset, ...)`
是**反着这条分层**加一条 base → dataset 的依赖，即使延迟 import 躲开了 import 环，它也让基类开始知道某个
具体子类的名字。

**实测的前情，免得读到代码时困惑：** `base/factor.py:20` 已经有一条 `from quantlab.dataset.backend import
XrBackend`——也就是说这个文件对 `dataset/` 的单向性**本来就已经是近似的**。所以这里要守的不是一条纯净的
不变量，而是**不把一处已知偏离再加深一层**：依赖一个存储后端和依赖一个具体的 vendor Dataset 子类，
耦合强度不是一个量级。所以顺序是：

1. **首选**：先读 `base/factor.py` 与 `base/data.py`，找有没有既有的「让子类声明自己不接受某字段」的钩子
   （形如一个类属性 / classmethod，子类覆盖它来声明拒绝集合）。有就用它，`CrspStockDataset` 侧声明
   `symbols` 被拒，基类只读这个声明——方向仍是 dataset → base，分层不破。
2. **没有现成钩子**：就在基类上开一个最小的（例如一个类属性 `REJECTED_CONFIG_FIELDS: frozenset[str] = frozenset()`，
   由 `CrspStockDataset` 覆盖成 `frozenset({"symbols"})`），基类只做「读声明 → 拒绝」，仍然不点名任何子类。
3. **兜底**：确实两条都不可行，才用方法体内的**延迟 import** + `isinstance` 硬编码类型判断。

**SUMMARY 里必须写明最终选了 1 / 2 / 3 里的哪一条，以及为什么前面的选项不可行。** 若落到第 3 条，
额外在 SUMMARY 里记一句：这是一处已知的、对 CLAUDE.md 单向分层的偏离，连同它的理由。

**不要改 `_auto_filter` 本身**，也**不要改 `backend.py:1477-1479`**：这道纪律的位置是 config 层。

最后把 plan 07 建的 `tests/test_no_identity_residue.py` 里那个「plan 08 将删的两个配置字段」分组从
`xfail`/skip 翻成**正式断言**（Task 1 已把它们删净）。
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_crsp_identity.py tests/test_no_identity_residue.py tests/test_factor_kunquant.py tests/test_factor_stream.py -p no:cacheprovider</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line, or the summary line reports any failed, errored or xfailed test</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "permnos" quantlab/base/factor.py` ≥ 1（拒绝文案点名了替代字段）
    - `quantlab/base/factor.py` 没有新增**模块级**的 `quantlab.dataset.crsp` import：`! grep -qE '^(from|import) +quantlab\.dataset\.crsp' quantlab/base/factor.py` 退出码 0（写成 `! ... grep -q` 而不是 `grep -c ... == 0`，零命中时 `grep -c` 自己退出码是 1）。**注意断言的是 `.crsp` 这一个子模块，不是整个 `quantlab.dataset`**：`base/factor.py:20` 已经有 `from quantlab.dataset.backend import XrBackend`（实测，本 phase 之前就在），拿整个 `quantlab.dataset` 去断言会结构性变红
    - SUMMARY 里写明拒绝的安装方式选了「钩子 / 新开最小钩子 / 延迟 import + isinstance」中的哪一条，以及为什么前面的选项不可行
    - `grep -c 'self.data = self.data.sel({col: list(symbols)})' quantlab/dataset/backend.py` 返回 1（`filter_by_symbol` 的裸 `.sel` 逐字保留）
    - `grep -c 'symbols: tuple\[str, ...\] | None = None' quantlab/base/config.py` ≥ 1（`BaseFactorConfig.symbols` 字段本身未被删）
    - `tests/test_crsp_identity.py` 里存在一条断言「CRSP 背景因子 + 非 None symbols → ValueError」的测试，和一条 Tiingo 背景的 control arm 测试
    - `grep -c "xfail" tests/test_no_identity_residue.py` 返回 0（分组已翻成正式断言）
    - `uv run pytest -q tests/test_no_identity_residue.py -p no:cacheprovider` 全绿
    - `uv run pytest -q tests/test_factor_kunquant.py tests/test_factor_stream.py -p no:cacheprovider` 全绿（因子层未被殃及）
  </acceptance_criteria>
  <done>两个 symbols 字段各有一个赋值期拒绝；残留 gate 的两个分组合并成一条正式断言；backend 与 _auto_filter 均未被触碰。</done>
  <reversibility rating="reversible">新增一道守卫。</reversibility>
</task>

</tasks>

<threat_model>
> ASVS level 1；`security_block_on: high`。

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户提供的 config → 数据集 / 因子的过滤语义 | 两个同名字段在两个阶段过滤，类型错误在此跨界 |
| 盘上已有的 config.json → 对象重建 | 删字段让旧 config 在 `cls(config_cls(**config))` 处 `TypeError` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.11-25 | Denial of Service | CRSP 背景因子设了字符串 `symbols` → 运行中途 `KeyError`，报错指向「索引里找不到」而真因是 dtype | high | mitigate | 两个 config setter 各装一条赋值期拒绝，文案同形并点名 `permnos`；`backend.py` 的裸 `.sel` 保持不变 |
| T-03.11-26 | Tampering | 误删基类 `BaseDatasetConfig.symbols` → 12+ 个非 CRSP 读者（含 Binance，永远不会有 PERMNO）全部断裂 | high | mitigate | prohibitions 点名；验收要求 `symbols` 仍在字段集合里且 `:17` 不在 diff 中 |
| T-03.11-27 | Tampering | 误删 `collision_universe` 字段本身 → GAP-C 的名册覆盖消失，指数成分被类型过滤挡掉 | high | mitigate | 只改名不删；验收要求 `roster_universe` 在 `crsp.py` 出现 ≥ 3 次 |
| T-03.11-28 | Repudiation | 旧 `config.json` 的 `TypeError` 被读成 bug 而不是已接受的后果 | low | accept | D-04 已明确接受并禁止写迁移；SUMMARY 里记一句即可 |
| T-03.11-SC | Tampering | npm/pip/cargo 安装 | low | accept | 本 phase 零安装（RESEARCH `## Package Legitimacy Audit`） |
</threat_model>

<verification>
- `uv run pytest -q tests/test_crsp_identity.py tests/test_crsp_dataset.py tests/test_ingest_wrds_crsp.py tests/test_config_roundtrip.py tests/test_jsonable.py tests/test_no_identity_residue.py tests/test_factor_kunquant.py tests/test_factor_stream.py -p no:cacheprovider` 全绿
- `grep -rn "symbol_overrides\|nan_adj_at_permno_seam\|collision_universe" quantlab/ scripts/` 无命中
- `uv run pytest -q --ignore=tests/test_factor_hierarchy.py --ignore=tests/test_crsp_rebuild_measurements.py -p no:cacheprovider` 相对 03.10 基线零新增失败
</verification>

<success_criteria>
- 两个字段删净、`roster_universe` 改名到位、字段本身未删
- 两个 `symbols` 字段各有一条赋值期拒绝，文案同形并点名 `permnos`
- 基类 `symbols` 字段、`symbol` 维度名、`filter_by_symbol` 三者的 diff 为空
- 残留 gate 的两个分组合并成一条正式断言并全绿
</success_criteria>

<output>
Create `.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-08-SUMMARY.md` when done
</output>
