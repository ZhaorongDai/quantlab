---
status: complete
phase: 03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r
source: [03.11-VERIFICATION.md, 03.11-REVIEW.md]
started: 2026-09-21T07:40:00Z
updated: 2026-09-21T21:55:00Z
---

## Current Test

[testing complete]

## Tests

### 1. base/backtest.py 的 vendor import 要不要现在改（WR-03）
expected: |
  事实（我与 reviewer、verifier 三方独立确认）：
  - `quantlab/base/backtest.py:15` = `from quantlab.dataset.crsp_tickers import CrspTickerLookup`
  - 同一 phase 内，`base/factor.py:80-115` 面对完全相同的问题选择了另一条路：
    由 dataset 声明 `REJECTED_FACTOR_CONFIG_FIELDS`，base 层只读，零 vendor 类名。
    一个 phase 里出现了两种模式。
  - `tests/test_extensibility_contract.py` 的 `CORE_LAYER_FILES` 只覆盖
    `base/factor.py`、`base/model.py`、`base/backend.py` —— `base/backtest.py` 不在门内，
    所以这条违规不会被任何测试抓住。
  - verifier 补充的关键细节：该文件在 phase 之前（`ba4f5b3`）就已经 import 了
    `XrBackend`（同样来自 `quantlab.dataset`）。所以这不是全新的违规类别，
    但它把一个「通用 backend 依赖」升级成了「具名 vendor 依赖」。
  - plan 09 的 SUMMARY 没有误导：line 18 把纯度断言明确限定在 `base/model.py`（实测 0 命中），
    line 203 公开记录了 `base/backtest.py == 4`。

  裁定选项：
  (a) 现在改 —— 照 `base/factor.py` 的注入模式重构，并把 `base/backtest.py`
      加进 `CORE_LAYER_FILES` 让门覆盖它
  (b) 记成 backlog —— 接受现状，但至少把 `base/backtest.py` 加进门以免继续恶化
  (c) 接受并明确豁免 —— 认定 backtester 是唯一知道 store 位置的层，这条依赖是合理的，
      在 CLAUDE.md 或门里写明豁免理由
result: pass
resolved_by: 03.11-13 → 03.11-16
resolved_at: 2026-09-21
verified_in_uat_round_3: |
  编排器独立复核（不采信 SUMMARY / verifier）：
  `tests/test_extensibility_contract.py:31-36` 的 `CORE_LAYER_FILES` 现为四项，
  含 `"quantlab/base/backtest.py"`；`FORBIDDEN_SUBSTRINGS:63-68` 仍是原四项
  （SpotKlineDataset / StockDataset / crypto_spot / us_equity），无 `CrspTickerLookup`
  —— 被否决的选项 (a) 未被偷偷执行。相关 suite 实跑 152 passed。
issue_history:
  reported: "b"
  severity: minor
decision: |
  (b) 记成 backlog —— 接受 `base/backtest.py` 对 `CrspTickerLookup` 的具名依赖，
  不在本 phase 重构；但把 `quantlab/base/backtest.py` 加进
  `tests/test_extensibility_contract.py:CORE_LAYER_FILES` 以免继续恶化。
  实测澄清（verify-work 现场核对）：`FORBIDDEN_SUBSTRINGS` 当前为
  ("SpotKlineDataset","StockDataset","crypto_spot","us_equity")，
  `base/backtest.py` 对这四项命中 0 —— 加进门今天即为绿，挡的是未来的
  market-literal 泄漏；`CrspTickerLookup` 本身门抓不到（加进去会立刻变红，那是选项 a）。
  plan 09 SUMMARY line 203 的「base/backtest.py == 4」指 `CrspTickerLookup` 出现 4 次，
  不是门的命中数。

### 2. dropped_permnos 审计报告丢了 ticker（WR-04）
expected: |
  `quantlab/dataset/crsp.py:1239` 仍然是 `pl.col("symbol").last()`，而 symbol 现在就是 PERMNO，
  所以审计报告输出 `{"75154": {"symbol": "75154", ...}}` —— 键和值逐字节相同，ticker 没了。
  verifier 在**已落盘的** `.crsp_filter_report.json` 里确认了这一点，不只是在源码里。

  来龙去脉：`tests/test_crsp_identity.py:585-587` 把这件事交棒给 plan 09 的 sidecar，
  但 plan 09 的五个展示点不含 filter report —— 交棒掉在地上了，而那条测试现在
  把退化后的输出**锁死**了。

  不算 must_have 失败（plan 07 只要求保住 `_permno_breakdown`，它做到了），
  但 D-17 的审计链因此少了人可读的那一半。

  裁定：现在补（用 `CrspTickerLookup` 还原 ticker，并解掉那条锁死退化输出的断言），
  还是记成 backlog。
result: pass
resolved_by: 03.11-13 → 03.11-16 → 03.11-18
resolved_at: 2026-09-21
verified_in_uat_round_3: |
  编排器独立复核：`quantlab/dataset/crsp.py:1226-1227` 的 `_permno_breakdown`
  docstring 现为 `{PERMNO: {types, rows, first, last}}`，:1234 明写
  「**There is no `symbol` field, deliberately** (G-03.11-2)」，并记录了
  逐字重复 key 的旧形状与操作者裁定。两个调用点共用同一渲染。
issue_history:
  reported: "b"
  severity: minor
decision: |
  (b) 删掉 `_permno_breakdown` 输出里的 `symbol` 字段 —— 承认 PERMNO(key) + `types`
  已经回答了审计问题「丢得对不对」，人可读的 ticker 不是审计正确性的必需。
  明确排除 (c) 保持现状：`{"75154": {"symbol": "75154"}}` 是逐字重复 key 的冗余字段，
  且已被测试锁死，比「没有这个字段」更糟。

  verify-work 现场核实的两点修正（UAT 原文的修法行不通，fix plan 必须据此改写）：
  1. `crsp.py:656` 的 `_build_ticker_intervals(derived)` 在 `_apply_security_filter`
     之后调用，docstring 明写「Only the panel's own PERMNOs」——
     `.crsp_tickers.json` 按设计不含被丢弃的 PERMNO，`CrspTickerLookup` 查它们必然为空。
     （若将来改选 (a)，正确取数源是 `self._symbology`，不是 sidecar。）
  2. `_permno_breakdown` 有两个调用点：`crsp.py:1116` 的 `dropped_permnos`
     和 `crsp.py:1099` 的 `roster_overrides["permnos"]`。删字段会同时影响后者 ——
     fix plan 必须覆盖两处，不能只改 dropped 那一支。

  需要解掉/改写的测试锁：
  - `tests/test_crsp_identity.py:585-587` 的注释（把交棒给 plan 09 的说法删掉）
  - `tests/test_crsp_identity.py:588` `assert qqq["symbol"] == QQQ_PERMNO_TEXT`
  - `tests/test_crsp_identity.py:1097` 同一断言的第二处
  - `example/wrds_crsp.md:329` 附近的 `dropped_permnos` 描述需同步

### 3. CrspTickerLookup.label() 的「永不抛错」在损坏 sidecar 上是假的（WR-01）
expected: |
  docstring 与 `03.11-09-SUMMARY.md:145` 声称 label() 面对「缺失/损坏」sidecar 都不抛错。
  缺失那一半是真的（verifier 实测通过）；**损坏那一半是假的**，三个复现：
  - `{"intervals": [1,2,3]}`                        → AttributeError
  - `{"intervals": {"13407":[{"ticker":"FB"}]}}`    → KeyError: 'start'
  - `[]`                                            → AttributeError
  守卫只 catch 了 `(FileNotFoundError, ValueError)`，`as_of` 里对
  `span["start"]/["end"]/["ticker"]` 的索引没有保护。

  最糟的调用点：`DLModel._spell`（model.py:1312）是在**构造 ValueError 消息的过程中**被调用的，
  一旦抛错就会把真正的诊断信息顶掉，换成一个无关的 KeyError。

  裁定：现在修（扩大守卫范围 + 加损坏 sidecar 的测试），还是记成 backlog。
result: pass
resolved_by: 03.11-12 → 03.11-15
resolved_at: 2026-09-21
verified_in_uat_round_3: |
  编排器独立复核：`quantlab/dataset/crsp_tickers.py:119` 的
  `_UNUSABLE = (FileNotFoundError, ValueError)` 是模块常量，
  :450 与 :483 两处 `except _UNUSABLE as exc` 共用它；
  :206 的 payload 守卫为 `except (OSError, ValueError, RecursionError)`
  —— WR-01 的深嵌套 JSON 逃逸口已堵上。
  守卫方向也已与 docstring 一致（WR-02）：本模块内部 bug 上抛而非静默降级。
  `tests/test_crsp_ticker_sidecar.py` 与 `tests/test_model_predict_panel.py`
  （含 `test_a_corrupt_ticker_sidecar_leaves_the_warning_working`）实跑全绿。
issue_history:
  reported: "修"
  severity: major
decision: |
  现在修。三项裁定里唯一一个「能让本来正常运行的流程崩掉」的。

  verify-work 现场核实的补充（UAT 原文只点了 1268，fix plan 必须覆盖两处）：
  `quantlab/base/model.py:1281` 的 `shown = self._spell(extra[:20], as_of)`
  是**裸调用**，不在任何 try 内，且位于**快乐路径**上 ——
  面板多出未训练标的本来只是丢弃 + 一条 logger.warning，预测照常完成；
  sidecar 一旦损坏，这次本来会成功的 predict_panel 直接崩。
  一个只喂日志文本的审计文件，格式坏了能让模型预测挂掉。
  `model.py:1268` 那一处（ValueError 消息构造中途抛错，真正诊断被顶掉）本来就要抛错，
  危害次之。

  修法范围：
  1. `quantlab/dataset/crsp_tickers.py:166-167` —— `as_of()` 对
     `span["start"]` / `span["end"]` / `span["ticker"]` 的索引访问没有保护；
     `intervals` 本身也可能不是 dict（`[]` / `[1,2,3]` 两个复现）。
  2. `crsp_tickers.py:187` 的守卫 except 元组 `(FileNotFoundError, ValueError)`
     补全 `KeyError` / `AttributeError` / `TypeError`。
  3. 加损坏 sidecar 的测试，至少覆盖三个已复现用例：
     `{"intervals": [1,2,3]}` / `{"intervals": {"13407":[{"ticker":"FB"}]}}` / `[]`
  4. 断言 `label()` 在这三种输入下回落到 PERMNO 自身拼写而非抛错 ——
     把 docstring 与 `03.11-09-SUMMARY.md:145` 的「永不抛错」声明变成真的。

### 4. _compare_fingerprints 的 warning 风暴 backstop（phase 自报盲点）
expected: |
  plan 10 与 plan 11 都把它声明为 `verification: backstop`，**没有任何测试驱动它**。
  `03.11-11-SUMMARY.md` 第 119/238/329 行如实记录了这一项是 auto-approved、无人逐字读过。
  这是 verifier 判定 77/79 时扣掉的那 2 条。

  背景：D-12 已接受「既有 backtest run 的指纹因本次重建全部失效」，
  `backtest.py:1145-1188` 只 warn 不阻断。需要确认这个 warning 不会掩盖真实问题。

  请人工确认：跑一次既有 backtest run，看 warning 是否可读、是否会淹没别的信息。
result: skipped
reason: |
  前提主语为空集 —— 不是「没条件跑」，是「没有可跑的对象」。

  第三轮 verify-work 登录操作者的服务器（`zhrdai@frp-one.com:~/projects/quantlab2`）
  实测，这是本项唯一可能的执行环境：

  1. **既有 backtest run 共 14 个**，全部带 `fingerprint.json`（2026-09-16 ~ 09-19，
     早于本 phase）。
  2. **它们全部消费 `us_all.zarr`** —— `price_dataset.zarr_file_path` 逐个核对为
     `/home/zhrdai/projects/quantlab2/data/data/us_equity/1d/us_all.zarr`，
     `vendor: tiingo`、`StockDataset`、`n_symbols: 14430`（ticker 轴）。
     **phase 03.11 没有碰过这个 store。**
  3. **服务器上没有 CRSP store** —— `find data -maxdepth 4 -name "*crsp*"` 零命中。
     本机也没有任何 backtest run（`fingerprint.json` 零命中）。

  也就是说：存在的 run 与 03.11 迁移过的 store（`wrds_crsp_sp500_1d.zarr`）
  两个集合**不相交**。重放那 14 个 run 读到的是未被本 phase 改动的数据，
  指纹匹配、warning 为 0 条；而被迁移的 CRSP store 上**从来没有过任何 run**。
  D-12 接受的「既有 run 因本次重建全部失效」，在 CRSP store 上主语为空。

  附带纠正一处措辞：**「warning 风暴」的量级是错的。**
  `_compare_fingerprints` 遍历的是**指纹键**而非标的
  （`backtest.py:1190` `for key in sorted(set(expected) | set(actual))`），每键至多一条。
  实读服务器上那份真实 `fingerprint.json`：**恰好 3 个键**
  （`factor[0]:UniverseFilteredFactor` / `factor[1]:...` / `price_dataset`）——
  即便全不匹配也只有 3 行，14430 个标的不会变成 14430 条告警。

  机制本身另有常设覆盖：`tests/test_backtest_rebuild.py:513`
  `test_changed_store_rebuild_warns_and_completes`（改一个 `adjClose` →
  告警含 `'price_dataset'` → 回测照常完成）。

  据此本项不再作为 open 项保留。verifier 134/136 扣掉的 2 条是本项的两次声明
  （plan 10 与 plan 11 各一次），随本判定一并收口。

### 5. example/wrds_crsp.md 的 16 处重写读起来是否正确（phase 自报盲点）
expected: |
  机械检查全过（`example/` 里 symbology_report 零命中、`crsp_tickers.json` ×8、反转段落存在、
  777→918 行与 numstat 一致），但**散文准确性是 auto-approved、无人逐字读过**。
  16 处里有 5 处是整段重写。

  plan 10 自己也把这条标成 `human_judgment: true`。重点看两处：
  - `:82-83` 退市行：哪一半机制还在（判决继承活着，symbol carry 死了）
  - `:189-197` ticker 只写 sidecar 成为主线叙述
result: pass
resolved_by: 03.11-15（那一处假声明随 G-03.11-3 一并收口）
resolved_at: 2026-09-21
verified_in_uat_round_3: |
  编排器独立复核 `example/wrds_crsp.md:247-249`：原先的
  「`label(permnos, day)` …（**永不抛**，缺旁车就原样回落成数字）」已改写为
  「（**永不抛**，旁车缺失**或损坏**——含解析不了与解析得了但形状不对——都原样回落成数字）」，
  并紧接着点名三种「解析不了」，其中第三种逐字写着
  「**嵌套**深到解析器自己爆栈（`RecursionError` 是 `RuntimeError` 子类，
  03.11-15 之前从这里逃出去过，G-03.11-3 / WR-01）」。
  16 处重写里唯一那处错误已消除，声明现在为真。
issue_history:
  reported: "yes"（认可 verify-work 现场逐字复核后给出的判定）
  severity: minor
decision: |
  16 处重写里 15 处准确，1 处复制了一个已知的假声明。

  现场逐字复核（UAT 原文的行号已过期，文件 777→918 行后位置漂移；
  实际位置是 `:90-102` 与 `:224-250`）：

  ✅ `example/wrds_crsp.md:90-102` 退市行 —— 准确。两半分得清楚且与代码一致：
     死掉的一半「没有名字就进不了面板」在 PERMNO 轴上不存在（键是 80599）；
     symbol carry 已在 03.11-07 删除，只剩「让旁车给死掉证券的最后一天写上名字」
     这一残留作用，与 `_build_ticker_intervals` docstring 的
     「A carried delisting-day interval is NOT one of these」一致；
     活着的一半 `dlydelflg='Y'` 判决继承必须留着（退市行 sharetype/securitytype 全 NULL）。
     并点明了「两条规则曾经挨着写，这正是第二条容易被连坐删掉的原因」。

  ✅ `:224-250` 其余部分准确：区间表而非 last-ticker 的理由（D-03 否决 1-D coord 的同一缺陷）、
     `as_of` 严格抛 vs `label` 回落的分工、「判据是盘上有没有旁车而不是厂商」。

  ❌ `example/wrds_crsp.md:248` 有错：
     「`label(permnos, day)` 是展示层的批量入口（**永不抛**，缺旁车就原样回落成数字）」
     —— 这正是 WR-01 / G-03.11-3 那条假声明的第三个副本
     （另两个：`crsp_tickers.py` docstring、`03.11-09-SUMMARY.md:145`）。
     缺旁车那一半为真，损坏旁车会抛 KeyError / AttributeError。

  修复归并进 G-03.11-3（该 gap 的 artifacts 已追加这一处文档）。

### 6. WINDOWS.md 条目 22/23/24 仍是 open，但交棒工作已完成
expected: |
  verifier 实测：这三条的交棒工作确实做完了
  （`tests/test_ingest_wrds_crsp.py` + `tests/test_no_identity_residue.py` → 32 passed；
  `DELETED_IN_03_11_08` 已提升进 `DELETED_IN_03_11_07`），但台账还写着 open。

  方向是安全的（宁可留着不关，也好过悄悄关掉），但台账与代码不一致。
  条目 21/25/26/27/28 状态正确（28 带着你的删除裁定与完整证据链）。

  裁定：把 22/23/24 标为 fixed（附证据），还是保持 open 等下一轮。
result: pass
resolved_by: 本轮 UAT 内直接执行（台账 bookkeeping）
resolved_at: 2026-09-21
verified_in_uat_round_3: |
  编排器复核 `.planning/WINDOWS.md`：条目 22 / 23 / 24 状态均为 `fixed`，
  三行各带完整证据列（32 passed、`DELETED_IN_03_11_08` 已消失、名字已提升进
  `tests/test_no_identity_residue.py:58` 的 `DELETED_IN_03_11_07`）。
  本轮复跑 `tests/test_ingest_wrds_crsp.py` 与 `tests/test_no_identity_residue.py`
  仍全绿（并入 152 passed 批次）。
issue_history:
  reported: "fixed"
  severity: minor
decision: |
  标为 fixed，已在本轮 verify-work 内直接执行（台账 bookkeeping，非代码改动）。

  现场独立复核（不只采信 verifier 的说法）：
  - `uv run pytest tests/test_ingest_wrds_crsp.py tests/test_no_identity_residue.py -q` → **32 passed**
  - strict-xfail 分组 `DELETED_IN_03_11_08` 已不存在；
    `symbol_overrides` / `nan_adj_at_permno_seam` 已提升进
    `tests/test_no_identity_residue.py:58` 的 `DELETED_IN_03_11_07`，
    该处注释明写「PROMOTED here out of the handover group 03.11-07 parked them in」
  - 三条的交棒 owner 均为 plan 03.11-08，该 plan 已执行完毕

  执行方式：`gsd_run windows fixed 22|23|24`（处理器无 --note 旗标，证据列随后手工补入）。
  diff 核对：只动 22/23/24 三行 + 计数（open 21→18，fixed 7→10），未误伤其他条目。
  条目 21/25/26/27/28 状态本就正确，未触碰。
  status: resolved_in_uat

### 7. plans 14-18 的 6 条 backstop 声明如实记为「未证」而非「通过」
expected: |
  `03.11-VERIFICATION.md` 的 human_verification 第 3 条。
  plans 14-18 共 16 条 `verification: backstop` 声明，验证器对其中 10 条有直接观察或构造证据，
  另 6 条**事后不可观察**，按 honest-verifier 规则如实记为「未证」而不是「通过」：
  - plan 14 ×2：清缓存动作的串行性与幂等性（动作已完成，事后不可观察）
  - plan 15：concurrency（`_payload` 惰性填充在竞态下最坏重复读盘）
  - plan 16：concurrency
  - plan 17：concurrency 运行时（`_degraded` 读改写非原子）
  - plan 18：idempotency（逐字节确定性，SUMMARY 自述本轮未逐字节比对两代产物）

  请确认这 6 条不需要额外证据即可收口（它们不进 134/136 的分母，
  扣掉的 2 条是 test 4 那一项）。
result: pass
reported: "pass"
note: |
  操作者确认 6 条按 honest-verifier 规则保留为「未证」，不追加证据。

  verify-work 现场提示（不改变判定）：前 5 条事后确实无痕（两条是一次性动作，
  三条 concurrency 需构造竞态）；但 **plan 18 的 idempotency 性质不同——它可验证，
  只是没验**：两代产物都仍在盘上（`data/_backup_pre_03.11_18` 与
  `_backup_pre_03.11_18_rerun2`），逐字节比对是做得到的。
  操作者选择不做，该条继续记为「未证」而非「通过」——方向安全。

### 8. verification 被判 stale —— 处置裁定
expected: |
  `gsd_run query verification.status` 返回 `stale`，phase 因此无法 transition。
  本轮 verify-work 已把根因查到底（不是猜测）：

  - staleness **不是** mtime 判据（`findStaleVerificationSummary` 实测 `stale: false`），
    而是 #4155 的内容指纹：VERIFICATION.md 的
    `covered_digest: v1:sha256:d337627c…` 对 57 个 covered_files 重算得
    `v1:sha256:535ffd9e…` —— 不匹配。
  - 逐文件比对 `809ad54`（验证器的取数 HEAD）与当前 HEAD，**只有 7 个文件变了**，
    全是 `03.11-1[2-8]-SUMMARY.md`；`git diff` 统计为 7 files / 7 insertions / 7 deletions，
    唯一的改动是 `requirements-completed: [DATA-03]` → `[]`。
  - 也就是说：验证器算完指纹之后，同一次提交（15e987d）里执行了 W-1 的台账收口，
    指纹因此当场失效。covered_files 里**零个实现文件**发生变化，
    134/136 的每一条证据都仍然成立。

  裁定选项：
  (a) 只刷新指纹 —— 把 VERIFICATION.md 的 `covered_digest` 更新为
      `v1:sha256:535ffd9e…` 并记一行原因，不重跑验证
  (b) 重跑一次完整验证 —— 最保守，但已知会重复 134/136 的全部工作
  (c) 先不动，等 test 4 跑完之后连同结果一起刷新
result: pass
reported: "a"
decision: |
  (a) 只刷新指纹，不重跑验证。依据是本轮查实的三点：
  staleness 来自内容指纹而非 mtime；变动仅 7 个 SUMMARY 的一行 frontmatter；
  covered_files 里零个实现文件变化，134/136 的证据基础未被触动。

  执行顺序（有坑，已避开）：`03.11-UAT.md` 本身也在 `covered_files` 里，
  所以指纹必须**最后**算 —— 等本文件全部写完再重算 57 个文件的摘要，
  写进 `03.11-VERIFICATION.md`（它不在自己的覆盖清单内），然后一次提交。
  否则刚写完就又 stale。

## Summary

total: 8
passed: 7
issues: 0
pending: 0
skipped: 1
blocked: 0

其中 test 6 已在本轮 verify-work 内直接收口（台账 bookkeeping），不生成 gap-closure plan。
待 gap-closure plan 的是 G-03.11-1 / G-03.11-2 / G-03.11-3（test 1/2/3，test 5 并入 G-03.11-3）。

**Gap closure 已完成（2026-09-21）。** G-03.11-1 / G-03.11-2 由 plan 03.11-13 收口，
G-03.11-3 由 plan 03.11-12 收口，三条 gap 状态已置 resolved，编排器逐条独立复核：
`CORE_LAYER_FILES` 含 `quantlab/base/backtest.py` 且 `FORBIDDEN_SUBSTRINGS` 仍为原四项
（被否决的选项 a 未被执行）；`_permno_breakdown` 返回 `{types, rows, first, last}` 且两个
调用点共用同一渲染；`label()` 的 `unusable` 元组为显式五项，非 `except Exception`。

**UAT 仍为 partial** —— test 4（`_compare_fingerprints` warning 风暴 backstop）依旧 blocked，
不在本轮 gap-closure 范围内。

---

**第三轮 verify-work 对账（2026-09-21，本次会话）**

Gap 对账（按 `gap_ids` frontmatter ↔ 对应 SUMMARY 是否落盘）：
**3 条 resolved，0 条仍 open。**
- G-03.11-1 / G-03.11-2 —— 03.11-13（首轮）+ 03.11-16（IN-02/03/04 收口）+ 03.11-18（盘上产物）
- G-03.11-3 —— 03.11-12（首轮，不完整）+ 03.11-15（WR-01/WR-02 收口），由
  `partially_resolved` 升为 `resolved`

UAT 之外由 code review 产生的三条 gap（不在本文件 Gaps 节，台账在 VERIFICATION.md）：
G-03.11-4（投毒字节码，阻断级）→ plan 14；G-03.11-5（盘上产物）→ plan 18；
G-03.11-6（WR-03/WR-04）→ plan 17 —— 验证器均已实测关闭。

VERIFICATION.md 的 3 条 human_verification：
- W-1（plans 12-18 的 `requirements-completed: [DATA-03]` 会释放 shared-ID 门）
  —— **已在提交 15e987d 内收口**，七份 SUMMARY 全部置空，本轮实测确认；
  `.planning/REQUIREMENTS.md` 的 DATA-03 仍为 `- [ ]`，未被误签收。不再需要人工裁定。
- UAT test 4 —— 仍 blocked，见下。
- backstop 6 条未证确认 —— 新增为 test 7。

新增 test 8：verification `stale` 的根因已查清（内容指纹失效，而非实现变更），需要处置裁定。

## Gaps

<!-- verifier 未发现实现缺口 —— 12/12 phase-goal truths 与 77/79 plan must-haves 全部通过。
     以下三条源自需要人工裁定的事项，裁定结果本身构成可执行的收口工作。 -->

- gap_id: G-03.11-1
  truth: "`base/` 层不出现具体 vendor 类名；核心层纯度门覆盖 `base/backtest.py`"
  status: resolved
  resolved_by: 03.11-13 → 03.11-16（第二轮：IN-03/IN-04 advisory 收口）
  resolved_at: 2026-09-21
  resolution_evidence: |
    `tests/test_extensibility_contract.py:52-63` 明写这个门只看得见下方四个 legacy market 字面量，
    并把「门是绿的」与「文件已被证明干净」分开；`FORBIDDEN_SUBSTRINGS` 仍是原四项
    （被否决的选项 a 未被执行）。模块 docstring 点名的
    `test_core_layer_purity_no_market_specific_logic` 实际存在（grep 命中 1）。
  reason: "操作者裁定 (b)：接受 `base/backtest.py:15` 对 `CrspTickerLookup` 的具名依赖不重构，但把 `quantlab/base/backtest.py` 加进 `tests/test_extensibility_contract.py:CORE_LAYER_FILES`，防止继续恶化。"
  severity: minor
  test: 1
  artifacts:
    - path: "tests/test_extensibility_contract.py"
      issue: "`CORE_LAYER_FILES` (行 31-35) 只含 base/factor.py、base/model.py、base/backend.py；base/backtest.py 不在门内"
  missing:
    - "把 `quantlab/base/backtest.py` 加进 `CORE_LAYER_FILES`"
    - "不要把 `CrspTickerLookup` 加进 `FORBIDDEN_SUBSTRINGS`（会立刻变红，那是被否决的选项 a）"
  verified_in_verify_work: |
    `FORBIDDEN_SUBSTRINGS` 当前为 ("SpotKlineDataset","StockDataset","crypto_spot","us_equity")，
    `base/backtest.py` 对这四项命中 0 —— 加进门今天即为绿，挡的是未来的 market-literal 泄漏。
    plan 09 SUMMARY line 203 的「base/backtest.py == 4」指 `CrspTickerLookup` 出现 4 次，不是门的命中数。

- gap_id: G-03.11-2
  truth: "过滤审计报告 `dropped_permnos` 的每个字段都携带信息，不出现逐字重复 key 的冗余字段"
  status: resolved
  resolved_by: 03.11-13 → 03.11-16（第二轮：IN-02 断言同强）→ 03.11-18（盘上产物重生成）
  resolved_at: 2026-09-21
  resolution_evidence: |
    盘上 `.crsp_filter_report.json` 已由当前 HEAD 重生成，验证器直接读盘：
    `roster_overrides.permnos` 1 条（PERMNO 75154），键集恰为 `['first','last','rows','types']`，
    `symbol` 字段消失；键集断言已进常设真实数据门。
    `tests/test_crsp_identity.py:1109` 补齐 `set(dropped) == {"types","rows","first","last"}`，
    与 :591 / :1495 两个兄弟完全同强。
  reason: "操作者裁定 (b)：删掉 `_permno_breakdown` 输出里的 `symbol` 字段。PERMNO(key) + `types` 已回答审计问题「丢得对不对」，人可读 ticker 不是审计正确性的必需；但保持现状最差 —— `{\"75154\": {\"symbol\": \"75154\"}}` 是冗余字段且已被测试锁死。"
  severity: minor
  test: 2
  artifacts:
    - path: "quantlab/dataset/crsp.py"
      issue: "`_permno_breakdown` (行 1226-1258) 的 `pl.col(\"symbol\").last()` 在 PERMNO 轴下退化为重复 key"
    - path: "tests/test_crsp_identity.py"
      issue: "行 588 与行 1097 两处断言把退化输出锁死；行 585-587 注释把修复交棒给 plan 09 的 sidecar（该交棒已掉在地上）"
    - path: "example/wrds_crsp.md"
      issue: "行 329 附近的 dropped_permnos 描述需同步"
  missing:
    - "删除 `_permno_breakdown` 输出里的 `symbol` 字段"
    - "覆盖两个调用点：crsp.py:1116 的 `dropped_permnos` 与 crsp.py:1099 的 `roster_overrides[\"permnos\"]`"
    - "解掉 tests/test_crsp_identity.py:588 与 :1097 的断言，改写 :585-587 的交棒注释"
    - "同步 example/wrds_crsp.md:329 附近描述"
  verified_in_verify_work: |
    UAT 原文提议的修法行不通：`crsp.py:656` 的 `_build_ticker_intervals(derived)` 在
    `_apply_security_filter` 之后调用，docstring 明写「Only the panel's own PERMNOs」——
    `.crsp_tickers.json` 按设计不含被丢弃的 PERMNO，`CrspTickerLookup` 查它们必然为空。
    （若将来改选恢复 ticker，正确取数源是 `self._symbology`，不是 sidecar。）

- gap_id: G-03.11-3
  truth: "`CrspTickerLookup.label()` 面对缺失或损坏的 sidecar 都不抛错，回落到 PERMNO 自身拼写"
  status: resolved
  resolved_by: 03.11-12 → 03.11-15（第二轮收口）
  resolved_at: 2026-09-21
  resolution_evidence: |
    03.11-VERIFICATION.md（2026-09-22T00:41:18Z，134/136）逐条实测：
    WR-01 —— `RecursionError` 已进 `payload` 的 except 元组（crsp_tickers.py:206）；
      深嵌套 JSON 下 `label()` 返回 `['13407']` 不抛、`as_of()` 抛 shaped `ValueError`。
    WR-02 —— `_UNUSABLE = (FileNotFoundError, ValueError)` 提为模块常量（:119），两处 except 共用；
      本模块内部 bug 实测**上抛**而非静默降级 —— 守卫方向已与 docstring 一致。
    plan 12 的 must-have 12-6 由上一轮 ✗ 转 ✓。
  reopened_reason: |
    编排器在 code review 后独立复现，「永不抛」契约仍有一个洞，且守卫方向与 docstring 相反：
    (WR-01) 深嵌套 JSON 让 json.loads 抛 RecursionError（RuntimeError 子类），payload 的
      `except (OSError, ValueError)` 与 label 的五元组都不含它 —— label() 直接抛出。
      实测：`{"a":`×100000 的 sidecar → label() RAISED RecursionError。
    (WR-02) 反过来，五元组里的 KeyError/AttributeError/TypeError 在同一提交加了结构守卫后
      已不可能来自数据损坏，唯一来源是本模块的 bug —— 而 docstring 声称它们会传给调用方。
      实测：注入 `span["tikcer"]` 错字后，面对完全合法的 sidecar，label() 静默返回
      ["13407"] 而非 ["FB"]。声称的安全网正是掩盖故障的东西。
  reason: "操作者裁定：现在修。docstring 与 03.11-09-SUMMARY.md:145 声称「永不抛错」，缺失那一半为真、损坏那一半为假。三个复现：`{\"intervals\": [1,2,3]}` → AttributeError；`{\"intervals\": {\"13407\":[{\"ticker\":\"FB\"}]}}` → KeyError: 'start'；`[]` → AttributeError。"
  severity: major
  test: 3
  artifacts:
    - path: "quantlab/dataset/crsp_tickers.py"
      issue: "`as_of()` 行 166-167 对 span[\"start\"]/[\"end\"]/[\"ticker\"] 的索引无保护；行 187 守卫只 catch (FileNotFoundError, ValueError)；`intervals` 本身也可能不是 dict"
    - path: "quantlab/base/model.py"
      issue: "行 1281 `shown = self._spell(extra[:20], as_of)` 是裸调用且位于快乐路径 —— sidecar 损坏会让本应成功的 predict_panel 崩溃；行 1268 在构造 ValueError 消息途中调用，抛错会顶掉真正的诊断信息"
    - path: "example/wrds_crsp.md"
      issue: "行 248「label(...) 永不抛」是同一条假声明的第三个副本（UAT test 5 逐字复核发现）；另两处为 crsp_tickers.py docstring 与 03.11-09-SUMMARY.md:145"
  missing:
    - "扩大 `as_of()` 的守卫范围，覆盖非 dict 的 intervals 与缺字段的 span"
    - "补全 label() 守卫的 except 元组：KeyError / AttributeError / TypeError"
    - "加损坏 sidecar 的测试，至少覆盖三个已复现用例"
    - "断言 label() 在三种损坏输入下回落到 PERMNO 自身拼写而非抛错"
    - "同步三处「永不抛」声明：crsp_tickers.py docstring、example/wrds_crsp.md:248（03.11-09-SUMMARY.md 是历史记录，不改）"
  verified_in_verify_work: |
    UAT 原文只点了 model.py:1268。现场复核发现 model.py:1281 危害更大：它是裸调用、不在任何 try 内、
    且位于快乐路径上 —— 面板多出未训练标的本来只是丢弃 + 一条 logger.warning，预测照常完成；
    sidecar 一旦损坏，这次本来会成功的 predict_panel 直接崩。
    一个只喂日志文本的审计文件，格式坏了能让模型预测挂掉。

## Deferred Follow-Ups

（暂无）
