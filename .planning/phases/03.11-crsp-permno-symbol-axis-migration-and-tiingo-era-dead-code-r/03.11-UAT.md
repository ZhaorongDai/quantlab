---
status: testing
phase: 03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r
source: [03.11-VERIFICATION.md, 03.11-REVIEW.md]
started: 2026-09-21T07:40:00Z
updated: 2026-09-21T07:40:00Z
---

## Current Test

number: 5
name: example/wrds_crsp.md 的 16 处重写读起来是否正确（phase 自报盲点）
expected: |
  机械检查全过，但散文准确性是 auto-approved、无人逐字读过。16 处里有 5 处是整段重写。
  重点看两处：`:82-83` 退市行（哪一半机制还在）、`:189-197` ticker 只写 sidecar。
awaiting: user response

## Tests

### 1. base/backtest.py 的 vendor import 要不要现在改（WR-03）
expected: |
  事实（我与 reviewer、verifier 三方独立确认）：
  - `quantlab/base/backtest.py:15` = `from quantlab.dataset.crsp_tickers import CrspTickerLookup`
  - 同一 phase 内，`base/factor.py:80-115` 面对完全相同的问题选择了另一条路：
    由 dataset 声明 `REJECTED_FACTOR_CONFIG_FIELDS`，base 层只读，零 vendor 类名。
    一个 phase 里出现了两种模式。
  - `tests/test_extensibility_contract.py` 的 `CORE_LAYER_FILES` 只覆盖
    `base/factor.py`、`base/model.py`、`base/backend.py` —— `base/backtest.py` 不在门内，
    所以这条违规不会被任何测试抓住。
  - verifier 补充的关键细节：该文件在 phase 之前（`ba4f5b3`）就已经 import 了
    `XrBackend`（同样来自 `quantlab.dataset`）。所以这不是全新的违规类别，
    但它把一个「通用 backend 依赖」升级成了「具名 vendor 依赖」。
  - plan 09 的 SUMMARY 没有误导：line 18 把纯度断言明确限定在 `base/model.py`（实测 0 命中），
    line 203 公开记录了 `base/backtest.py == 4`。

  裁定选项：
  (a) 现在改 —— 照 `base/factor.py` 的注入模式重构，并把 `base/backtest.py`
      加进 `CORE_LAYER_FILES` 让门覆盖它
  (b) 记成 backlog —— 接受现状，但至少把 `base/backtest.py` 加进门以免继续恶化
  (c) 接受并明确豁免 —— 认定 backtester 是唯一知道 store 位置的层，这条依赖是合理的，
      在 CLAUDE.md 或门里写明豁免理由
result: issue
reported: "b"
severity: minor
decision: |
  (b) 记成 backlog —— 接受 `base/backtest.py` 对 `CrspTickerLookup` 的具名依赖，
  不在本 phase 重构；但把 `quantlab/base/backtest.py` 加进
  `tests/test_extensibility_contract.py:CORE_LAYER_FILES` 以免继续恶化。
  实测澄清（verify-work 现场核对）：`FORBIDDEN_SUBSTRINGS` 当前为
  ("SpotKlineDataset","StockDataset","crypto_spot","us_equity")，
  `base/backtest.py` 对这四项命中 0 —— 加进门今天即为绿，挡的是未来的
  market-literal 泄漏；`CrspTickerLookup` 本身门抓不到（加进去会立刻变红，那是选项 a）。
  plan 09 SUMMARY line 203 的「base/backtest.py == 4」指 `CrspTickerLookup` 出现 4 次，
  不是门的命中数。

### 2. dropped_permnos 审计报告丢了 ticker（WR-04）
expected: |
  `quantlab/dataset/crsp.py:1239` 仍然是 `pl.col("symbol").last()`，而 symbol 现在就是 PERMNO，
  所以审计报告输出 `{"75154": {"symbol": "75154", ...}}` —— 键和值逐字节相同，ticker 没了。
  verifier 在**已落盘的** `.crsp_filter_report.json` 里确认了这一点，不只是在源码里。

  来龙去脉：`tests/test_crsp_identity.py:585-587` 把这件事交棒给 plan 09 的 sidecar，
  但 plan 09 的五个展示点不含 filter report —— 交棒掉在地上了，而那条测试现在
  把退化后的输出**锁死**了。

  不算 must_have 失败（plan 07 只要求保住 `_permno_breakdown`，它做到了），
  但 D-17 的审计链因此少了人可读的那一半。

  裁定：现在补（用 `CrspTickerLookup` 还原 ticker，并解掉那条锁死退化输出的断言），
  还是记成 backlog。
result: issue
reported: "b"
severity: minor
decision: |
  (b) 删掉 `_permno_breakdown` 输出里的 `symbol` 字段 —— 承认 PERMNO(key) + `types`
  已经回答了审计问题「丢得对不对」，人可读的 ticker 不是审计正确性的必需。
  明确排除 (c) 保持现状：`{"75154": {"symbol": "75154"}}` 是逐字重复 key 的冗余字段，
  且已被测试锁死，比「没有这个字段」更糟。

  verify-work 现场核实的两点修正（UAT 原文的修法行不通，fix plan 必须据此改写）：
  1. `crsp.py:656` 的 `_build_ticker_intervals(derived)` 在 `_apply_security_filter`
     之后调用，docstring 明写「Only the panel's own PERMNOs」——
     `.crsp_tickers.json` 按设计不含被丢弃的 PERMNO，`CrspTickerLookup` 查它们必然为空。
     （若将来改选 (a)，正确取数源是 `self._symbology`，不是 sidecar。）
  2. `_permno_breakdown` 有两个调用点：`crsp.py:1116` 的 `dropped_permnos`
     和 `crsp.py:1099` 的 `roster_overrides["permnos"]`。删字段会同时影响后者 ——
     fix plan 必须覆盖两处，不能只改 dropped 那一支。

  需要解掉/改写的测试锁：
  - `tests/test_crsp_identity.py:585-587` 的注释（把交棒给 plan 09 的说法删掉）
  - `tests/test_crsp_identity.py:588` `assert qqq["symbol"] == QQQ_PERMNO_TEXT`
  - `tests/test_crsp_identity.py:1097` 同一断言的第二处
  - `example/wrds_crsp.md:329` 附近的 `dropped_permnos` 描述需同步

### 3. CrspTickerLookup.label() 的「永不抛错」在损坏 sidecar 上是假的（WR-01）
expected: |
  docstring 与 `03.11-09-SUMMARY.md:145` 声称 label() 面对「缺失/损坏」sidecar 都不抛错。
  缺失那一半是真的（verifier 实测通过）；**损坏那一半是假的**，三个复现：
  - `{"intervals": [1,2,3]}`                        → AttributeError
  - `{"intervals": {"13407":[{"ticker":"FB"}]}}`    → KeyError: 'start'
  - `[]`                                            → AttributeError
  守卫只 catch 了 `(FileNotFoundError, ValueError)`，`as_of` 里对
  `span["start"]/["end"]/["ticker"]` 的索引没有保护。

  最糟的调用点：`DLModel._spell`（model.py:1312）是在**构造 ValueError 消息的过程中**被调用的，
  一旦抛错就会把真正的诊断信息顶掉，换成一个无关的 KeyError。

  裁定：现在修（扩大守卫范围 + 加损坏 sidecar 的测试），还是记成 backlog。
result: issue
reported: "修"
severity: major
decision: |
  现在修。三项裁定里唯一一个「能让本来正常运行的流程崩掉」的。

  verify-work 现场核实的补充（UAT 原文只点了 1268，fix plan 必须覆盖两处）：
  `quantlab/base/model.py:1281` 的 `shown = self._spell(extra[:20], as_of)`
  是**裸调用**，不在任何 try 内，且位于**快乐路径**上 ——
  面板多出未训练标的本来只是丢弃 + 一条 logger.warning，预测照常完成；
  sidecar 一旦损坏，这次本来会成功的 predict_panel 直接崩。
  一个只喂日志文本的审计文件，格式坏了能让模型预测挂掉。
  `model.py:1268` 那一处（ValueError 消息构造中途抛错，真正诊断被顶掉）本来就要抛错，
  危害次之。

  修法范围：
  1. `quantlab/dataset/crsp_tickers.py:166-167` —— `as_of()` 对
     `span["start"]` / `span["end"]` / `span["ticker"]` 的索引访问没有保护；
     `intervals` 本身也可能不是 dict（`[]` / `[1,2,3]` 两个复现）。
  2. `crsp_tickers.py:187` 的守卫 except 元组 `(FileNotFoundError, ValueError)`
     补全 `KeyError` / `AttributeError` / `TypeError`。
  3. 加损坏 sidecar 的测试，至少覆盖三个已复现用例：
     `{"intervals": [1,2,3]}` / `{"intervals": {"13407":[{"ticker":"FB"}]}}` / `[]`
  4. 断言 `label()` 在这三种输入下回落到 PERMNO 自身拼写而非抛错 ——
     把 docstring 与 `03.11-09-SUMMARY.md:145` 的「永不抛错」声明变成真的。

### 4. _compare_fingerprints 的 warning 风暴 backstop（phase 自报盲点）
expected: |
  plan 10 与 plan 11 都把它声明为 `verification: backstop`，**没有任何测试驱动它**。
  `03.11-11-SUMMARY.md` 第 119/238/329 行如实记录了这一项是 auto-approved、无人逐字读过。
  这是 verifier 判定 77/79 时扣掉的那 2 条。

  背景：D-12 已接受「既有 backtest run 的指纹因本次重建全部失效」，
  `backtest.py:1145-1188` 只 warn 不阻断。需要确认这个 warning 不会掩盖真实问题。

  请人工确认：跑一次既有 backtest run，看 warning 是否可读、是否会淹没别的信息。
result: blocked
blocked_by: other
reason: "blocked"
note: |
  需要操作者实际跑一次既有 backtest run 才能判断 warning 的可读性，
  本轮 UAT 无法完成。该项仍是 verifier 77/79 扣掉的两条之一，保持 open。

### 5. example/wrds_crsp.md 的 16 处重写读起来是否正确（phase 自报盲点）
expected: |
  机械检查全过（`example/` 里 symbology_report 零命中、`crsp_tickers.json` ×8、反转段落存在、
  777→918 行与 numstat 一致），但**散文准确性是 auto-approved、无人逐字读过**。
  16 处里有 5 处是整段重写。

  plan 10 自己也把这条标成 `human_judgment: true`。重点看两处：
  - `:82-83` 退市行：哪一半机制还在（判决继承活着，symbol carry 死了）
  - `:189-197` ticker 只写 sidecar 成为主线叙述
result: [pending]

### 6. WINDOWS.md 条目 22/23/24 仍是 open，但交棒工作已完成
expected: |
  verifier 实测：这三条的交棒工作确实做完了
  （`tests/test_ingest_wrds_crsp.py` + `tests/test_no_identity_residue.py` → 32 passed；
  `DELETED_IN_03_11_08` 已提升进 `DELETED_IN_03_11_07`），但台账还写着 open。

  方向是安全的（宁可留着不关，也好过悄悄关掉），但台账与代码不一致。
  条目 21/25/26/27/28 状态正确（28 带着你的删除裁定与完整证据链）。

  裁定：把 22/23/24 标为 fixed（附证据），还是保持 open 等下一轮。
result: [pending]

## Summary

total: 6
passed: 0
issues: 0
pending: 6
skipped: 0
blocked: 0

## Gaps

（verifier 未发现 gap —— 12/12 phase-goal truths 与 77/79 plan must-haves 全部通过。
以上 6 项是需要**人工裁定**的事项，不是实现缺口。）
