---
phase: 03.11
plan: 10
type: execute
wave: 7
depends_on: ["03.11-09"]
files_modified:
  - tests/test_crsp_rebuild_measurements.py
  - example/wrds_crsp.md
autonomous: true
requirements: [DATA-03]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "盘上 CRSP store 在全部删除与改动之后被最终重建一次，落盘 symbol dtype 是 int64，且 W0 的五项清洁度断言全部仍然成立（D-15 的 post-fix 列）"
    - "{zarr}.crsp_symbology_report.json 在最终重建之后**不存在** —— 它的六个字段全死，sidecar 本身也被删"
    - "{zarr}.crsp_tickers.json 在最终重建之后存在，且 intervals 只覆盖面板实际包含的 PERMNO"
    - "example/wrds_crsp.md 的 16 处与本 phase 相关的位置全部同步，用精确替换而不是 sed 范围删除，改完核对行数（operator P6）"
    - "example/wrds_crsp.md 里记下了 Phase 03.10 身份轴决策的反转（D-08 的三处记录之一，另两处在 plan 11）"
    - "最终重建仍在主仓库的 data/ 上执行，解析出的绝对根路径被打印进测量记录（operator P7）"
    - statement: "既有 backtest run 的指纹失效会让 _compare_fingerprints 刷一屏 warning（D-12 已接受），由 plan 11 的 checkpoint 确认它没有掩盖真实问题"
      verification: backstop
  artifacts:
    - example/wrds_crsp.md
    - tests/test_crsp_rebuild_measurements.py
  key_links:
    - "CrspStoreRebuilder（plan 01）→ 最终重建，写出 int64 轴的 store 与 .crsp_tickers.json"
    - "TICKER_SIDECAR_SUFFIX（plan 09）→ 最终重建后必须出现的 sidecar"
    - "roster_universe（plan 08 的改名）→ 重建 config 里的字段名"
  prohibitions:
    - "不得用 sed 范围删除改 example/wrds_crsp.md 或任何 planning 文档（operator P6：漏匹配会删到 EOF）"
    - "不得把 data/** 写进 files_modified"
    - "不得把 anomaly_flag_true == 13 放宽成不等式 —— 差异意味着有新事实，要停下来报告"
    - "不修改 quantlab/dataset/cleaning.py（D-06）"
    - "不写任何 config 迁移或兼容层（D-04）"
    - "不得走 quantlab/config 的工厂函数构造 config（operator P2）"
---

<!-- planner-discipline-allow: crsp_symbology_report, nan_adj_at_permno_seam, symbol_overrides, collision_universe, resolve_collisions -->
<objective>
收尾之一：把盘上 store 最终重建成 PERMNO 轴，把 `example/wrds_crsp.md` 同步到代码现在的样子。
（路线图 / STATE / 需求台账的反转记录与两个 operator 决策点在 plan 11。）

**为什么要再重建一次。** plan 01 的 W0 重建拿到的是 **ticker 轴**的 post-fix 基线——那是后续所有 wave 推理
的依据。走到这里，轴、死代码、配置字段、sidecar 全都变了，盘上 store 必须与代码重新对齐一次：新的面板是
int64 PERMNO 轴，`.crsp_symbology_report.json` 不再产生（它的六个字段全死），`.crsp_tickers.json` 第一次出现。

**验收用的是不变量而不是数值。** `adjClose <= 0 == 0`、`close == 0 == 0`、`adjClose NaN == adjVolume NaN ==
结构性空格数` 这四条对「列如何分组」是稳健的，所以换轴之后仍然正确；`symbol_count` 则可能合法地变化
（同窗口内的改名合并两列、ticker 回收拆成两列），因此只做区间断言并把实测值记进 SUMMARY。

Purpose: 让盘上数据与文档都与代码一致。
Output: 最终重建的 int64 store + 同步好的 `example/wrds_crsp.md`。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-CONTEXT.md
@.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-RESEARCH.md
@.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-01-SUMMARY.md
@.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-09-SUMMARY.md
</context>

## Artifacts this phase produces

| 符号 | 位置 | 类型 |
|---|---|---|
| `example/wrds_crsp.md` 的 PERMNO 轴重写（16 处） | `example/wrds_crsp.md` | 文档 |
| `tests/test_crsp_rebuild_measurements.py` 的 PERMNO 轴断言组 | `tests/test_crsp_rebuild_measurements.py` | 测试扩展 |

<tasks>

<task type="auto">
  <name>Task 1: 最终重建 —— PERMNO 轴的 store，以及消失的 / 出现的两个 sidecar</name>
  <files>tests/test_crsp_rebuild_measurements.py</files>
  <precondition>解析出的 `QUANTLAB_DATA_ROOT` 下同时存在 `data/downloads/us_equity/1d/wrds_crsp/wrds` 与 `.../\_reference`；缺任一则本任务停止并报告解析出的绝对路径，不得跳过。</precondition>
  <read_first>
    - `.planning/phases/03.11-...-r/03.11-01-SUMMARY.md`（W0 的 post-fix 重测表与解析出的绝对 `data_root`）
    - `quantlab/dataset/crsp_rebuild.py` 与 `quantlab/base/rebuild.py`（plan 01 的重建入口；`CRSP_SIDECAR_SUFFIXES` 的四项清场清单）
    - `tests/test_crsp_rebuild_measurements.py`（plan 01 写的真实数据门）
    - `quantlab/dataset/crsp.py` 的 `TICKER_SIDECAR_SUFFIX`（plan 09 新增）
    - 本仓库 `03.11-01-PLAN.md` 的 `## data/ 在 worktree 下如何解析` 小节
  </read_first>
  <action>
更新 `tests/test_crsp_rebuild_measurements.py`，让它在 PERMNO 轴下继续是一条**真实数据门**：

1. config 构造里把 `collision_universe="crsp_sp500"` 改成 `roster_universe="crsp_sp500"`（plan 08 的改名）。
   窗口仍是 2024-01-01 → 2024-12-31（D-15：只重建 2024，扩窗口会让 pre/post 基线失去可比性）。
2. 保留并继续断言 W0 的**不变量形式**清洁度条件：`adj_close_le_zero == 0`、`close_eq_zero == 0`、
   `adj_close_nan == structural_gaps`、`adj_volume_nan == structural_gaps`。这四条对「列如何分组」
   是稳健的，所以换轴之后仍然是正确的验收。
3. `anomaly_flag_true` 仍断言 `== 13`。**若不等于 13 就停下来报告实测值与差异**，不要把断言放宽成不等式
   ——13 的构成（12 个拆股日误报 + `GL 2024-04-11` 的真实 −53%）在 RESEARCH §R1 已逐条列出，
   差异意味着有新事实需要解释。
4. 新增三条换轴后才有意义的断言：
   - 落盘 `zarr.open_group(store)["symbol"].dtype` 的 `kind` 是 `"i"`（读 zarr 原始 dtype，不读解码值）；
   - `{store}.crsp_symbology_report.json` **不存在**（它的六个字段全死，sidecar 本身随之消失）；
   - `{store}.crsp_tickers.json` **存在**，且它的 `intervals` 的 key 数 == 面板的 `symbol_count`
     （只写面板实际包含的 PERMNO）。
5. `symbol_count` **不与 W0 的 523 做等值断言**——PERMNO 轴下同窗口内的改名会合并两列、同窗口内的 ticker
   回收会拆成两列，列数可能合法地变化。改为：把实测的 `symbol_count` 与 W0 基线的 523 一起 `print()` 并
   写进 SUMMARY，同时断言它落在 `[500, 540]` 这个区间内（sp500 一年名册的合理范围），显著偏离就是信号。
6. sidecar mtime 那条测试的清单改成：`.chunks.json`、`.crsp_adjustment.json`、`.crsp_filter_report.json`、
   `.crsp_tickers.json` 四个都晚于本次运行开始时刻。

执行仍按 plan 01 规定的解析方式：被测代码来自当前工作树，被读写的数据来自主仓库。
  </action>
  <verify>
    <automated>QUANTLAB_DATA_ROOT="$(dirname "$(git rev-parse --path-format=absolute --git-common-dir)")" uv run pytest -q -s tests/test_crsp_rebuild_measurements.py -p no:cacheprovider</automated>
    <fails_when>non-zero exit, or "no tests ran" / "skipped" appearing in the summary line, or the printed data_root does not end in "/quantlab"</fails_when>
  </verify>
  <acceptance_criteria>
    - 上面的 `<automated>` 命令退出码 0
    - 输出中 `adj_close_le_zero` 为 0、`close_eq_zero` 为 0、`anomaly_flag_true` 为 13
    - 输出中 `adj_close_nan`、`adj_volume_nan`、`structural_gaps` 三者相等
    - `ls <root>/data/data/us_equity/1d/ | grep -c crsp_symbology_report` 返回 0
    - `ls <root>/data/data/us_equity/1d/ | grep -c crsp_tickers` ≥ 1
    - SUMMARY 里同时记下 W0 的 523 与本次的 `symbol_count`，并对差异给出一句解释
  </acceptance_criteria>
  <done>盘上 store 是 int64 PERMNO 轴的 post-fix 面板；旧的符号学审计文件消失、新的 ticker sidecar 出现。</done>
  <reversibility rating="costly">再次删除并重建盘上 store。plan 01 的 `data/_backup_pre_03.11/` 备份仍在，且重转只要秒级。</reversibility>
</task>

<task type="auto">
  <name>Task 2: example/wrds_crsp.md 同步 —— 精确替换、改完核对行数</name>
  <files>example/wrds_crsp.md</files>
  <read_first>
    - `example/wrds_crsp.md` 全文（777 行）；先 `grep -n` 定位 RESEARCH §R10 表列出的位置
    - `.planning/phases/03.11-...-r/03.11-RESEARCH.md` 的 `### example/wrds_crsp.md（777 行）` 逐行表（16 处）
    - `.planning/phases/03.11-...-r/03.11-01-SUMMARY.md` 与 `03.11-10` Task 1 的重测数字（`:603` 那一行要换成这些数）
    - `quantlab/dataset/crsp.py`、`quantlab/dataset/crsp_symbology.py`、`quantlab/dataset/crsp_tickers.py`（改动之后的真实状态——文档要描述代码现在的样子，不是计划里的样子）
  </read_first>
  <action>
按 RESEARCH §R10 的逐行表同步 16 处。**每一处都用精确替换**（先 `grep -n` 定位、替换、再 `grep -n` 复核），
**绝不用 `sed` 范围删除**——已记录的事故是范围删除漏匹配后一路删到 EOF（operator P6）。改完用
`git diff --stat -- example/wrds_crsp.md` 核对增删行数，并把这个数字写进 SUMMARY。

逐处动作：

| 位置 | 动作 |
|---|---|
| `:7` | 导读里「符号学 `crsp_symbology.py:CrspSymbology`」改写成 sidecar 定位：它现在只提供 PERMNO → ticker 的区间表 |
| `:82-83` | 退市行 ticker NULL → 幸存者偏差的叙述：**保留但重写**。PERMNO 轴下「贴不上标签」这个问题不存在了，但 `crsp.py:952-977` 的判决继承仍在（退市行的 type 列仍会变 NULL），要说清楚剩下的是哪一半 |
| `:90-99` | ticker 回收 / seam / `nan_adj_at_permno_seam`：**整段改写** —— seam 机制被删，因为 PERMNO 轴下不存在「一列换东家」 |
| `:103` | 三条撞车规则：删 |
| `:109-125` | 类别后缀（BRK / BF）、六条规则、三个例外、以及「89/876 缺口在这个厂商身上不存在」：改写成「类别后缀只在 sidecar 的 ticker 拼写里还有意义，面板轴上没有」 |
| `:181` | `dsf_v2` 行说明：小改 |
| `:189-197` | 「原始层按 PERMNO，ticker 在转换时才产生」：**这一节现在成了主线**，改写成「ticker 根本不产生，只写 sidecar」 |
| `:208` | `stksecurityinfohist` 的用途行：改成 ticker sidecar 的数据源 |
| `:224-228` | 「PERMNO seam（D-18）与它的开关」：**整节删** |
| `:235` | `--universe` / `collision_universe`：改名同步为 `roster_universe`（CLI 参数 `--universe` 不变） |
| `:364` `:457` | 两个 sidecar 审计文件：`crsp_symbology_report.json` → `crsp_tickers.json`，并说明新文件是区间表 |
| `:405` | `symbol_overrides` 钉 QQQ：删（PERMNO 86755 全程不变） |
| `:453` | 流程图「符号学贴标签 → 撞车解决 → PERMNO seam → 证券过滤」：改成「PERMNO 轴 → 证券过滤」 |
| `:470` | 「**之前**就已经是 PERMNO」：**保留**（这是坑 f 的正向陈述，`wrds_crsp.py:317-319` 未被触碰） |
| `:543` | `--permnos ['AAPL'] are not PERMNOs` 的错误示例：按 plan 08 的两条拒绝同步，并补一句 `config.symbols` 在 CRSP 上被拒绝、该用 `permnos` |
| `:603` | 「S&P 符号学报告｜加类别后缀 466 处，撞车 0、seam 0、退市 carry 0、不合规 symbol 0、未贴标签 0」：**删整行**（这六个字段全死），换成 Task 1 的重测数字与新的 `admitted_without_ticker` 字段 |

另外在文档里新增一小段记下 D-08 的反转（见 Task 3 的三处之一）：03.10 曾把 ticker 定为面板身份轴，
本 phase 反转了它，理由与证据。
  </action>
  <verify>
    <automated>! grep -q "crsp_symbology_report\|nan_adj_at_permno_seam\|symbol_overrides\|collision_universe\|resolve_collisions" example/wrds_crsp.md</automated>
    <fails_when>non-zero exit — i.e. any one of the five dead-mechanism names still appears in the file. Written as `! grep -q` rather than `grep -c ... == 0` because grep's own exit code on zero matches is 1, and an executor judges a verify command by exit code.</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "crsp_tickers.json" example/wrds_crsp.md` ≥ 2
    - `grep -c "roster_universe" example/wrds_crsp.md` ≥ 1
    - `grep -c "REVERSED" example/wrds_crsp.md` ≥ 1（D-08 的反转在文档里有记录）
    - `grep -n "之前.*已经是 PERMNO\|already.*PERMNO" example/wrds_crsp.md` 仍有命中（`:470` 的正向陈述保留）
    - `git diff --stat -- example/wrds_crsp.md` 的增删行数与 SUMMARY 里记录的核对数字一致
    - SUMMARY 里逐条列出 16 处的处理结果（改 / 删 / 保留）
  </acceptance_criteria>
  <done>文档描述的是代码现在的样子；16 处全部经精确替换处理并核对了行数。</done>
  <reversibility rating="reversible">文档改动，git revert 即可。</reversibility>
</task>

</tasks>

<threat_model>
> ASVS level 1；`security_block_on: high`。

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 代码状态 → 文档陈述 | 文档若落后于代码，下一个读者会按一个不存在的机制去推理 |
| raw tier → 最终落盘 store | 最后一次重建决定盘上权威数据 |
| worktree 代码 → 主仓库 data/ | 解析错误会读到错误的树 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.11-35 | Denial of Service | 用 `sed` 范围删除改文档，漏匹配后一路删到 EOF | high | mitigate | 明令精确替换；验收要求 `git diff --stat` 的增删行数与 SUMMARY 的核对数字一致（operator P6，已有真实事故记录） |
| T-03.11-36 | Repudiation | 文档仍描述已删机制（撞车规则、seam、符号学报告） | medium | mitigate | Task 2 的 `<automated>` 是一条对已删机制名的零命中 grep |
| T-03.11-01 | Denial of Service | 最终重建删除真实 store | high | mitigate | 仍走 `CrspStoreRebuilder` 的 assert → backup → clear 顺序；plan 01 的 `data/_backup_pre_03.11/` 备份仍在 |
| T-03.11-03 | Tampering | `data_root` 解析错误导致读写错误的工作树（假绿） | high | mitigate | 仍由 `git rev-parse --git-common-dir` 显式给出；测量记录打印绝对路径并被 `<fails_when>` 检查 |
| T-03.11-02 | Repudiation | 符号学 sidecar 留在盘上，审计文件描述一个已不存在的机制 | medium | mitigate | 它在 `CRSP_SIDECAR_SUFFIXES` 的清场清单里；Task 1 断言重建后它**不存在** |
| T-03.11-SC | Tampering | npm/pip/cargo 安装 | low | accept | 本 phase 零安装（RESEARCH `## Package Legitimacy Audit`） |
</threat_model>

<verification>
- `QUANTLAB_DATA_ROOT="$(dirname "$(git rev-parse --path-format=absolute --git-common-dir)")" uv run pytest -q -s tests/test_crsp_rebuild_measurements.py -p no:cacheprovider` 全绿
- `! grep -q "crsp_symbology_report\|nan_adj_at_permno_seam\|symbol_overrides\|collision_universe\|resolve_collisions" example/wrds_crsp.md` 退出码 0
- `uv run pytest -q --ignore=tests/test_factor_hierarchy.py --ignore=tests/test_crsp_rebuild_measurements.py -p no:cacheprovider` 相对 03.10 基线（1694 collected）零新增失败
</verification>

<success_criteria>
- 盘上 store 是 int64 PERMNO 轴，符号学 sidecar 消失、ticker sidecar 出现
- `example/wrds_crsp.md` 的 16 处同步完成且已核对行数
- 文档里记下了 D-08 的反转（三处之一）
- 四条不变量形式的清洁度断言全绿
</success_criteria>

<output>
Create `.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-10-SUMMARY.md` when done
</output>
