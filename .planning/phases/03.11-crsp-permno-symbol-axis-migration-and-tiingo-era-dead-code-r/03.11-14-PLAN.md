---
phase: 03.11
plan: 14
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_stale_bytecode_lesson.py
autonomous: true
gap_closure: true
gap_ids: [G-03.11-4]
requirements: [DATA-03]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "G-03.11-4：本仓库工作树里不存在任何被 CPython 判为「新鲜」的投毒字节码 —— 清缓存后 `uv run pytest -q tests/test_crsp_ticker_sidecar.py` 在**默认缓存目录**下全绿，不需要 `-X pycache_prefix` 才绿"
    - "G-03.11-4：四个 gap-closure suite 在默认缓存目录下一次跑出 98 passed，与 03.11-VERIFICATION.md 在干净缓存里量到的数字逐字相同"
    - "G-03.11-4：回归门（`workflow.test_command`）在清缓存之后重跑一次，FAILED 行集与 deferred-items.md D-03.11-12-A 记录的 55 条 `ingest_*` 导入失败逐字相同 —— 没有新增失败、也没有本相位代码造成的失败"
    - "「等长注入 + 恢复 mtime ⇒ .pyc 不失效」这条教训以**可执行**形式落在 `tests/test_stale_bytecode_lesson.py`：合成模块被证明会读到旧字节码，而 `touch` 源文件是被证明有效的补救"
    - "`tests/conftest.py` 里有一段与 macOS OpenMP 守卫并列的散文教训，指向那个可执行证明，并写明复现类实验的两条安全做法（子类/猴补丁，或恢复后 touch）"
    - "`quantlab/` 与 `tests/` 里不存在 03.11-VERIFICATION.md:134 记录的那两个等长标识符错字的任何残留"
    - statement: "concurrency — 清缓存与回归门在同一棵工作树里串行执行；Wave 0 期间没有并发的 pytest 进程会一边重写 __pycache__ 一边被测量"
      verification: backstop
    - statement: "idempotency — 再跑一次清缓存命令是无害的空操作（第二次 find 命中 0 个目录），重跑回归门得到同一组 FAILED 行"
      verification: backstop
  artifacts:
    - tests/conftest.py
    - tests/test_stale_bytecode_lesson.py
  key_links:
    - "`tests/conftest.py` 的散文教训 → `tests/test_stale_bytecode_lesson.py` 的可执行证明（散文点名文件，文件证明散文）"
    - "清缓存动作 → Wave 1/2/3 每个 plan 的 `<precondition>`：本 plan 之后取得的测试结果才是可采信证据"
  prohibitions:
    - statement: "不得把一次测试结果当作证据呈报，而不先说明它是在哪种字节码缓存状态下取得的 —— 一个绿色摘要若不反映工作树里的源码，比没有摘要更危险"
      status: unverified
      verification: flagged
    - statement: "不得用 `-X pycache_prefix=<tmp>` 绕过问题来「让它变绿」—— 那是取证手段，不是修复；修复是让默认缓存目录下也绿"
      status: unverified
      verification: flagged
    - statement: "不得删除 `.venv` 下的字节码或任何 `.venv` 内容 —— 清场范围只限本仓库自己的源码树"
      status: unverified
      verification: flagged
  gap_ids: [G-03.11-4]
---

<!-- planner-discipline-allow: __pycache__, pycache_prefix, OMP_NUM_THREADS -->

<objective>
**这是整轮 gap closure 的前置条件，不是一件并列的任务。**

`quantlab/dataset/__pycache__/crsp_tickers.cpython-313.pyc` 里的字节码，编译自一份被注入了等长
标识符错字的源码。因为注入和原文**长度相同**，恢复源码后文件 size 没变、mtime 也被保留，
CPython 的「mtime + size」失效判据因此判定这份 `.pyc` 仍然新鲜、不重编译。
03.11-VERIFICATION.md:136-143 实测了 pyc header：`records source mtime 1790024442 size 14427`
与源码当前的 mtime/size 逐字相同。

后果是可测的：同一份干净源码，
`uv run pytest -q tests/test_crsp_ticker_sidecar.py` 是 **5 failed, 21 passed**；
换成干净缓存目录跑同一批四个 suite 是 **98 passed**。

**在这一步完成之前，这棵工作树里跑出来的任何绿或红都不是证据。**

Purpose: 让后续三个 wave 的每一次 `<verify>` 都测到真正的 HEAD，而不是一份幽灵字节码。
Output: 清空的字节码缓存 + 重新记录的两组基线数字 + 一份**可执行**的教训（合成模块上复现
同一个陷阱，并证明 `touch` 是有效补救），外加 `tests/conftest.py` 里与 macOS OpenMP 守卫并列
的一段散文说明。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-VERIFICATION.md
@.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/deferred-items.md
@tests/conftest.py
@tests/test_macos_openmp_guard.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 清空字节码缓存，把「测试结果反映 HEAD」这条路从头到尾走通一次</name>
  <files>（无源码改动 —— 这是环境动作 + 证据采集；`__pycache__` 按设计 gitignored，不列入 files_modified）</files>
  <read_first>
    - `.planning/phases/03.11-.../03.11-VERIFICATION.md` 的 `gaps:` 块，`gap_id: G-03.11-4` 一节（lines ~126-150）—— 实测数字、pyc header 证据、以及三条 `missing:` 就是本任务的验收清单
    - `.planning/phases/03.11-.../deferred-items.md` 的 D-03.11-12-A —— 55 条前置失败的**逐文件分布表**，Task 3 的对照基准
    - `.planning/config.json` 的 `workflow.test_command` —— 回归门的确切拼写（带两个 `--ignore`）
    - `CLAUDE.md` 的 Platform Requirements 一节 —— macOS 上 `OMP_NUM_THREADS=1` 由 `tests/conftest.py` 负责，本任务不要另设
  </read_first>
  <precondition>当前 shell 的 cwd 是主仓库根 `/Users/daizhaorong/projects/quantlab`（不是 worktree）—— 若在隔离 worktree 中执行，被清的必须是**执行代码所在的那棵树**的缓存，否则清了也测不到。</precondition>
  <reversibility rating="reversible">字节码缓存是纯派生产物，下一次 import 会重新生成；误删的唯一代价是一次冷启动编译。</reversibility>
  <action>
从仓库根执行 VERIFICATION 点名的清场命令：`find . -path ./.venv -prune -o -name '__pycache__' -type d -print0 | xargs -0 rm -rf`。
注意 `-path ./.venv -prune` 这一段必须保留 —— 清场范围只限本仓库源码树，虚拟环境里的缓存不属于本仓库、删了只会白白拖慢下一次启动。
清场**前**先把命中数记下来（同一条 find 去掉 `-print0 | xargs` 改成 `-print | wc -l`），清场**后**再记一次，
这两个数字是「清场确实发生了」的证据，写进 SUMMARY。

然后在**默认缓存目录**下（不加任何 `pycache_prefix` 参数）跑四个 gap-closure suite，
命令与 03.11-VERIFICATION.md 行为抽检表第一行、以及 03.11-REVIEW.md Summary 最后一条用的是同一条：
`uv run pytest -q tests/test_crsp_ticker_sidecar.py tests/test_model_predict_panel.py tests/test_crsp_identity.py tests/test_extensibility_contract.py`。
**关键点在于不加 `pycache_prefix`**：VERIFICATION 里那次 98 passed 是靠换缓存目录取得的取证结果；
本任务要证明的是默认路径下也是 98 passed，也就是幽灵字节码真的没了。

最后单独复跑一次 VERIFICATION 里报 5 failed 的那条命令
（`uv run pytest -q tests/test_crsp_ticker_sidecar.py`），确认它现在是 26 passed，
把「5 failed → 0 failed」这一对数字写进 SUMMARY —— 它是本 gap 唯一的直接证据。
  </action>
  <acceptance_criteria>
    - `find . -path ./.venv -prune -o -name '__pycache__' -type d -print | wc -l` 在清场后返回 0（清场前的数字已记录在 SUMMARY 里）
    - `uv run pytest -q tests/test_crsp_ticker_sidecar.py tests/test_model_predict_panel.py tests/test_crsp_identity.py tests/test_extensibility_contract.py` 的摘要行是 `98 passed`，且命令里**不含** `pycache_prefix`
    - `uv run pytest -q tests/test_crsp_ticker_sidecar.py` 的摘要行是 `26 passed`（VERIFICATION 记录的同一条命令此前是 `5 failed, 21 passed`）
    - `git grep -n -e 'tikcer' -e 'paylaod' -- quantlab tests` 无任何匹配（退出码非 0）
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/test_crsp_ticker_sidecar.py tests/test_model_predict_panel.py tests/test_crsp_identity.py tests/test_extensibility_contract.py</automated>
    <fails_when>退出码非 0，或摘要行不是 `98 passed`（出现任何 `failed` / `error` 计数，或 passed 数不等于 98）</fails_when>
    <automated>find . -path ./.venv -prune -o -name '__pycache__' -type d -print | wc -l</automated>
    <fails_when>输出不是 0（说明清场没有覆盖全树，或被 prune 规则误挡）</fails_when>
  </verify>
  <done>默认缓存目录下四个 suite 98 passed；此前 5 failed 的那条命令现在 26 passed；全树 `__pycache__` 目录数为 0。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 把这条教训写成可执行证明 —— `tests/test_stale_bytecode_lesson.py`</name>
  <files>tests/test_stale_bytecode_lesson.py</files>
  <read_first>
    - `tests/test_macos_openmp_guard.py` —— 本仓库「把一条环境陷阱锁成测试」的既有范式：模块 docstring 逐条写明锁住了什么、什么会让它变红，并用 `subprocess` 起新解释器取证。新文件照这个骨架写
    - `.planning/phases/03.11-.../03.11-VERIFICATION.md:130-150` —— G-03.11-4 的 pyc header 实测（`records source mtime … size …` 与源码逐字相同）就是本测试要在合成模块上重现的那个条件
    - `tests/conftest.py:20-44` —— macOS OpenMP 守卫那段注释的写法与篇幅，Task 3 的散文要与它并列
  </read_first>
  <behavior>
    - Test 1（陷阱本身）：在 `tmp_path` 里写一个合成模块，函数返回一个**中性**的定长常量（例如 `"AAAA"`）。先用一个子解释器 import 它、写出 `__pycache__` 里的 `.pyc`，并确认返回 `"AAAA"`。记下源文件的 `st_mtime_ns` 与 `st_size`。把源码改成返回**等长**的另一个常量（`"BBBB"`），再用 `os.utime(path, ns=(atime_ns, 原 mtime_ns))` 把 mtime 恢复。断言 `st_size` 未变。然后起一个**全新**子解释器 import 同一个模块 —— 断言它打印的是 `"AAAA"`，也就是旧字节码。这一条**就是**教训：源码已经是 B，运行时仍然是 A。
    - Test 2（补救有效）：接着 Test 1 的状态，`os.utime(path, None)` 把 mtime 推到现在（就是 `touch`），再起一个全新子解释器 —— 断言它现在打印 `"BBBB"`。
    - Test 3（另一条补救，对照）：不碰源文件，改用给子解释器传干净的 `pycache_prefix` 目录 —— 断言它打印 `"BBBB"`。这一条记录的是 03.11-VERIFICATION 本轮取证时用的办法，与 `touch` 并列成为两条可选做法。
    - 三条都必须在**独立子进程**里取证：同一个解释器内 `importlib.reload` 不走 `.pyc` 失效判据，会把这个陷阱整个测没。
  </behavior>
  <action>
新建 `tests/test_stale_bytecode_lesson.py`。模块 docstring 按 `tests/test_macos_openmp_guard.py` 的写法组织：
先讲清楚失效判据是 **mtime + size** 两项、两项都没变就判新鲜；再讲为什么本相位踩到它
（复现实验把一个标识符换成了**等长**的另一个拼写，恢复源码后两项都没变）；最后写明两条安全做法
——复现实验要么改用子类 / `monkeypatch` 注入（本相位 03.11-REVIEW.md 复现 WR-02 时用的就是这个），
要么在恢复源码之后 `touch` 源文件或删掉缓存。

**不要在这个文件里写出那两个真实的错字拼写。** 用中性的合成常量（`"AAAA"` / `"BBBB"`）作为等长替换的载体：
Task 1 的验收标准之一是仓库里对那两个拼写的 `git grep` 零命中，而一条散文提及与一次真实残留在 grep 面前是一样的。
`tests/test_crsp_rebuild_measurements.py:141-143` 对 skip 标记用的就是这个手法，照抄它的理由句式。

子进程用 `subprocess.run([sys.executable, "-c", ...], cwd=tmp_path, ...)` 起，
靠 `cwd` 让合成模块可被 import，不要改动 `sys.path` 全局状态。
第三条用例给子进程传 `-X pycache_prefix=<另一个 tmp 子目录>`。
每个子进程都设 `timeout`，并在断言失败时把 `stdout`/`stderr` 一并带进断言消息 —— 一个只说
「预期 BBBB 得到 AAAA」的失败，读者无从判断是陷阱重现了还是子进程根本没起来。
  </action>
  <acceptance_criteria>
    - `tests/test_stale_bytecode_lesson.py` 存在，且 `uv run pytest -q tests/test_stale_bytecode_lesson.py` 至少 3 passed
    - `grep -c 'subprocess' tests/test_stale_bytecode_lesson.py` 返回 ≥ 1（取证在独立解释器里发生）
    - `grep -c 'os.utime' tests/test_stale_bytecode_lesson.py` 返回 ≥ 2（一次恢复旧 mtime 制造陷阱，一次 touch 解除陷阱）
    - `git grep -n -e 'tikcer' -e 'paylaod' -- tests/test_stale_bytecode_lesson.py` 无任何匹配
    - 测试在被删掉 `os.utime` 恢复那一行之后会变红（源文件 mtime 前移，`.pyc` 正常失效，Test 1 将读到 `"BBBB"`）—— 这一点由执行者手工验证一次并写进 SUMMARY，不留在代码里
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/test_stale_bytecode_lesson.py -p no:cacheprovider</automated>
    <fails_when>退出码非 0，或摘要行报告的 passed 数少于 3</fails_when>
  </verify>
  <done>三条用例全绿：等长编辑 + 恢复 mtime 后子解释器读到旧常量；`touch` 之后读到新常量；换 `pycache_prefix` 之后也读到新常量。</done>
</task>

<task type="auto">
  <name>Task 3: `tests/conftest.py` 的散文教训 + 清缓存后的回归门基线</name>
  <files>tests/conftest.py</files>
  <read_first>
    - `tests/conftest.py:20-44`（macOS OpenMP 守卫那段注释）—— 新的一段要与它并列、同一种口吻：先说症状，再说根因，再说「实测过什么、拒绝了什么」，最后点名锁住它的那个测试文件
    - `tests/test_stale_bytecode_lesson.py`（Task 2 刚写的）—— 散文要点名这个文件作为可执行证明
    - `.planning/phases/03.11-.../deferred-items.md` D-03.11-12-A 的逐文件失败分布表 —— 回归门比对的基准
    - `.planning/config.json` 的 `workflow.test_command`
  </read_first>
  <action>
**散文半边。** 在 `tests/conftest.py` 的 macOS OpenMP 守卫注释**之后**、其余 import 之前，加一段新的注释块。
它不是可执行代码，只是一段与 OpenMP 那段并列的「环境陷阱」说明：`.pyc` 的失效判据是 mtime + size 两项，
所以一次**等长**的源码编辑若同时保留了 mtime，就能让运行时行为与工作树源码分家，而 `git status` 和
按内容 grep 都看不出来。写明两条安全做法（子类 / `monkeypatch` 注入；或恢复后 `touch` 源文件、或删缓存），
并点名 `tests/test_stale_bytecode_lesson.py` 是这三件事的可执行证明。
篇幅按 OpenMP 那段的量级，不超过它。**不要**在这里写出那两个真实错字的拼写（理由同 Task 2）。
**不要**在这里加任何可执行守卫 —— 没有便宜且无误报的运行时检测，加一个半吊子的只会制造新的假信号。

**基线半边。** 在清缓存之后跑一次完整回归门，用 `.planning/config.json` 里 `workflow.test_command` 的
确切拼写：`uv run pytest -q --ignore=tests/test_factor_hierarchy.py --ignore=tests/test_crsp_rebuild_measurements.py`。
把摘要行数字和 FAILED 行**全集**落进 SUMMARY，并与 deferred-items.md D-03.11-12-A 的逐文件分布表逐行对照。
判据是：FAILED 集合里每一条都是 `ModuleNotFoundError: No module named 'ingest_tiingo' / 'ingest_binance_spot'`
或它的 `FileNotFoundError` 孪生，逐文件计数与该表一致。**新增的 passed 数是允许的**（本轮新加了测试），
新增的 FAILED 不允许。若出现 D-03.11-12-B 那个 KunQuant teardown 间歇性挂起，按 deferred-items 的记录处理并重跑一次，
不要把它当成新失败，也不要顺手去修它（它在本轮 out-of-scope）。
  </action>
  <acceptance_criteria>
    - `grep -c 'test_stale_bytecode_lesson' tests/conftest.py` 返回 ≥ 1（散文点名了它的可执行证明）
    - `sed -n '1,80p' tests/conftest.py | grep -v '^#' | grep -c 'OMP_NUM_THREADS'` 仍返回 ≥ 1，且 `os.environ.setdefault("OMP_NUM_THREADS", "1")` 这一行仍在 `import base64` 之前 —— 新注释块没有把 OpenMP 守卫挤到任何 import 之后
    - `uv run pytest -q tests/test_macos_openmp_guard.py` 全绿（它按 AST 断言守卫是 conftest 的第一段可执行代码；新注释若插错位置会让它变红）
    - 回归门的 FAILED 行集与 deferred-items.md D-03.11-12-A 的逐文件分布表逐行一致，SUMMARY 里有这张对照表
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/test_macos_openmp_guard.py tests/test_stale_bytecode_lesson.py -p no:cacheprovider</automated>
    <fails_when>退出码非 0，或 `tests/test_macos_openmp_guard.py` 中任何一条用例报 failed（说明新注释块把 OpenMP 守卫挤出了「第一段可执行代码」的位置）</fails_when>
    <automated>uv run pytest -q --ignore=tests/test_factor_hierarchy.py --ignore=tests/test_crsp_rebuild_measurements.py</automated>
    <fails_when>FAILED 行集里出现任何一条不是 `ingest_tiingo` / `ingest_binance_spot` 导入失败的条目，或逐文件失败计数与 deferred-items.md D-03.11-12-A 的表不一致</fails_when>
  </verify>
  <done>conftest 有一段与 OpenMP 守卫并列、点名可执行证明的教训；OpenMP 守卫锁测试仍绿；清缓存后的回归门 FAILED 集与已立项的 55 条前置失败逐字相同。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 工作树源码 → 磁盘上的 `.pyc` | 运行时真正执行的是派生产物，不是被 review、被 grep、被 commit 的那份文本 |
| 一次测试运行 → 呈报给人的「绿/红」结论 | 摘要行是人做决策的唯一输入；它若不反映 HEAD，整条证据链断在这里 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.11-50 | Tampering | `quantlab/**/__pycache__/*.pyc` | high | mitigate | Task 1 全树清场（`.venv` 除外），并在默认缓存目录下复测到 98 passed；清场前后的目录计数作为证据 |
| T-03.11-51 | Repudiation | 测试摘要行作为验收证据 | high | mitigate | Task 1 要求明确记录「不加 `pycache_prefix`」这一条件；Task 3 把回归门 FAILED 全集与已立项的前置失败逐行对照，任何新增条目都不得被吸收进摘要 |
| T-03.11-52 | Denial of Service | 清场命令的 `-path ./.venv -prune` | low | mitigate | prune 段落写死在 action 与验收标准里；误删 `.venv` 只会触发一次重装，但会让本 wave 白跑 |

（本 plan 无 npm/pip/cargo 安装动作，故无 `T-03.11-SC` 供应链条目；依赖集一字未动。）
</threat_model>

<verification>
1. 全树 `__pycache__` 目录计数为 0（清场前的计数已记录在 SUMMARY）。
2. 四个 gap-closure suite 在默认缓存目录下 98 passed。
3. `uv run pytest -q tests/test_crsp_ticker_sidecar.py` 从 `5 failed, 21 passed` 变成 `26 passed`。
4. `tests/test_stale_bytecode_lesson.py` 三条用例全绿。
5. `tests/test_macos_openmp_guard.py` 仍全绿。
6. 回归门 FAILED 集 == deferred-items.md D-03.11-12-A 的 55 条。
</verification>

<success_criteria>
G-03.11-4 关闭：这棵工作树里此后取得的任何测试结果都反映 HEAD；那条会让未来的复现实验再次踩坑的规律，
以一条会变红的测试 + 一段与 OpenMP 守卫并列的注释，同时留在了代码里。
</success_criteria>

## Artifacts this phase produces

| 符号 / 文件 | 位置 | 性质 |
|---|---|---|
| `tests/test_stale_bytecode_lesson.py` | 新文件 | 三条用例的可执行教训 |
| （测试函数名由执行者按 behavior 三条各取一个，全部以 `test_` 开头并在 SUMMARY 中逐个列出） | 同上 | 新测试函数 |
| `tests/conftest.py` 的字节码陷阱注释块 | 既有文件，新增注释 | 散文教训，点名上面那个文件 |

**非源码的操作目标（按设计 gitignored，不进 `files_modified`）：**

| 路径 | 动作 |
|---|---|
| `**/__pycache__/**`（`.venv` 除外） | 删除 |
