---
phase: 03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r
plan: 06
subsystem: factor
tags: [universe-filter, kunquant, xarray, permno, dead-code-removal, tdd]

# Dependency graph
requires:
  - phase: 03.11-03
    provides: "CRSP 价格面板的 symbol 维已是 int64 PERMNO —— 这正是九条 ticker 正则失去存在理由的原因"
  - phase: 03.11-01
    provides: "quantlab/utils/symbol_axis.py 与 widen_symbol_axis 的 int64 修复"
provides:
  - "UniverseFilteredFactor 只剩 (b) min_price 与 (c) min_dollar_volume 两条阈值；条件 (a)（九条 NON_COMMON_TICKER_PATTERNS、其开关、classmethod 与四个消费点）在 quantlab/ 下零残留"
  - "_mask_panel 永不删列：任意窗口、任意标的集合下输出 symbol 轴与输入逐元素相等，LS-3 的「符号轴与日期窗口无关」变成平凡成立"
  - "example/universe.md 的「为什么是删而不是关」小节：机制论证（正则全部要求字母，对 PERMNO 数字串恒 True，无声 no-op）+ 承接者（CRSP security_filter / equity_common，按日期、带审计报告）"
  - "when_issued_or_called（-WD/-WI/-CL）这条从未被单独测量的悬案随条件 (a) 整体删除一并结清，不再需要全库 distinct 统计"
affects: [03.11-07, 03.11-08, 因子层, 回测层]

actuals:
  tokens: 10264
  tasks: 2
  commits: 3
plan_head_before: 16d76c74fef44d59e6223341db0a2da66f999a16

tech-stack:
  added: []
  patterns:
    - "删除而不是关闭：一个在当前数据形状上恒为 no-op 的过滤，留下默认关闭的开关比删掉更难发现"
    - "删除类改动的 RED 证据：把删除前的实现用 `git show <base>:<file>` 临时放回，对新测试取真实 RED，再 `git checkout --` 还原"

key-files:
  created: []
  modified:
    - quantlab/factor/universe_filter.py
    - tests/test_universe_filtered_factor.py
    - tests/universe_fixtures.py
    - example/universe.md
    - example/README.md

key-decisions:
  - "条件 (a) 删除（不是把 exclude_non_common 默认改成 False）：留下的开关仍会被 to_dict() 序列化、仍在文档里、但什么都不做"
  - "删除的书面论证写成机制论证而不是比较论证：九条正则全部要求字母，对 PERMNO 数字串一条都不匹配 —— 它不是被取代，它是失效"
  - "tests/test_ticker_pattern_reconciliation.py 一行未改：逐个核过 5 个测试，对账对象是 roster builder ↔ TRADEABLE_TICKER_PATTERN / _WELL_FORMED_TICKER，没有一个以 universe_filter 的九条正则为一侧"
  - "fixture 里的 WARRANT('JUNKW') 造景删除后用第 13 个普通股补位，维持 16 个标的的 SIMD 块宽对齐（aarch64 上 16 可以、13 不行）"

patterns-established:
  - "标的轴不变式：因子包装类只产生 NaN 格子，绝不窄化 symbol 轴 —— 下游 DLModel._align_prediction_symbols 因此少一个风险来源"

requirements-completed: [DATA-03]

coverage:
  - id: D1
    description: "条件 (a) 的十一个位置全部删除，(b)(c) 与 :86-123 的 Div(v, mask) 图改写机制完好"
    requirement: "DATA-03"
    verification:
      - kind: other
        ref: "grep -rn 'NON_COMMON_TICKER_PATTERNS|is_common_ticker|exclude_non_common' quantlab/ -> 0 命中"
        status: pass
      - kind: other
        ref: "grep -c min_price / min_dollar_volume quantlab/factor/universe_filter.py -> 8 / 8 (>= 3)"
        status: pass
      - kind: other
        ref: "grep -c 'Div(source, mask)' quantlab/factor/universe_filter.py -> 1"
        status: pass
      - kind: unit
        ref: "tests/test_universe_filtered_factor.py (31 collected, all pass)"
        status: pass
    human_judgment: false
  - id: D2
    description: "_mask_panel 永不删列：任意窗口下输出 symbol 轴与输入逐元素相等（D-05 点名要求显式记下的红利）"
    requirement: "DATA-03"
    verification:
      - kind: unit
        ref: "tests/test_universe_filtered_factor.py#test_mask_panel_never_drops_a_column[ticker_axis]"
        status: pass
      - kind: unit
        ref: "tests/test_universe_filtered_factor.py#test_mask_panel_never_drops_a_column[permno_axis]"
        status: pass
      - kind: unit
        ref: "tests/test_universe_filtered_factor.py#test_the_symbol_axis_is_the_same_in_two_disjoint_date_windows"
        status: pass
    human_judgment: false
  - id: D3
    description: "两处 pattern 本体（_PREFERRED_SHARE_PATTERN / _BABY_BOND_PATTERN）与 enums/data.py:TRADEABLE_TICKER_PATTERN 的定义行逐字未被触碰（D-06）"
    verification:
      - kind: other
        ref: "grep -c 定义行 x3 -> 1 / 1 / 1；git diff 16d76c7..HEAD 不含 quantlab/acquisition/universe.py 与 quantlab/enums/data.py"
        status: pass
      - kind: unit
        ref: "tests/test_ticker_pattern_reconciliation.py (5 tests, unmodified, all pass)"
        status: pass
    human_judgment: false
  - id: D4
    description: "机制论证与悬案处置写进 example/universe.md 的「为什么是删而不是关」小节"
    verification:
      - kind: other
        ref: "grep -c 'no-op' example/universe.md -> 2; grep -c when_issued_or_called -> 2"
        status: pass
    human_judgment: true
    rationale: "grep 只能证明这些字符串在文件里，证明不了那段论证读起来是否站得住、是否足以取代原来那 43 行测量注释。需要人读一遍。"
  - id: D5
    description: "旧配置的兼容性姿态：带 exclude_non_common 的旧 run config 被 from_config 点名拒绝，而不是悄悄重建成另一个股票池"
    verification:
      - kind: unit
        ref: "tests/test_universe_filtered_factor.py#test_get_config_carries_exactly_the_three_threshold_parameters"
        status: pass
      - kind: unit
        ref: "tests/test_universe_filtered_factor.py#test_from_config_refuses_a_missing_or_unknown_parameter"
        status: pass
    human_judgment: false

# Metrics
duration: 47 min
completed: 2026-09-21
status: complete
---

# Phase 03.11 Plan 06: 删除 universe_filter 的条件 (a) Summary

**`UniverseFilteredFactor` 从三条规则收缩成两条纯阈值：九条 ticker 正则及其开关、classmethod 与四个消费点全部删除，`_mask_panel` 因此从「会删列」变成「永不删列」，并把「它不是被取代、它是失效」的机制论证写进 `example/universe.md`。**

## Performance

- **Duration:** 47 min
- **Started:** 2026-09-21T03:48:00Z (估算，首个提交前的准备与阅读)
- **Completed:** 2026-09-21T04:35:05Z
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- **条件 (a) 在 `quantlab/` 下零残留。** RESEARCH 表里的十一个位置逐个处理：两行 import、43 行 Tiingo 测量注释、九条正则元组、`__init__` 形参、实例字段、`__repr__` 字段、`is_common_ticker` classmethod、batch/stream 两处分支、`_mask_panel` 的删列分支、`to_dict()` 的键与 `_PARAMETER_KEYS` 集合字面量。顺带删掉因此变成未使用的 `import re`。
- **(b)(c) 与图改写机制完好。** `min_price` / `min_dollar_volume` / `window` 三个参数原样；`:404-406`、`:541-543` 两处阈值式一字未动（它们本来就只读 `close`/`volume` 数组）；`_mask_cross_sectional_inputs` 的 `Div(v, universe_mask)` 图改写 —— 全仓唯一的「截面算子只看池内标的」机制 —— 原样保留。
- **拿到 D-05 点名要求显式记下的红利并加了锁。** `_mask_panel` 现在是纯阈值，出池只表现为 NaN 格子。新测试 `test_mask_panel_never_drops_a_column` 在三种窗口（完整 / 短于成交额窗口 / 尾部两根）× 两种轴（ticker 轴、int64 PERMNO 轴）上断言输出 symbol 轴与输入逐元素相等，并带一条「掩码确实施加了」的反证断言。
- **机制论证写进文档，两个 Still-open 一并结清。** `example/universe.md` 的「票代码规则」整节改写成「为什么是删而不是关」：(i) 九条正则全部要求字母 → 对 `"10107"` 一条都不匹配 → 静态判定恒 True → 整条过滤在 CRSP 面板上无声变成 no-op（Still-open #3 的书面论证）；(ii) 承接者是 CRSP 的 `security_filter`（`equity_common`），按日期判定、带审计报告，类型词表里根本不存在 7 类正则目标，唯一真实的 "unit" 是 `sharetype=UG` 且已被排除，还额外排掉正则排不掉的 ADR/ETF/CEF。顺带记下 `when_issued_or_called`（`-WD`/`-WI`/`-CL`）这条唯一从未被单独测量的规则随整体删除一并消解（Still-open #5），不再需要一次全库 distinct 统计。
- **`tests/test_ticker_pattern_reconciliation.py` 一行未改。** 逐个核过 5 个测试，对账的两端是「roster builder 的输出」↔「`enums/data.py:TRADEABLE_TICKER_PATTERN` / `acquisition/universe.py:_WELL_FORMED_TICKER`」，**没有一个**以 `universe_filter` 的九条正则为一侧。它们守的是路径段安全与 Tiingo 采集预检（真实事故：全 roster 预检曾在 `NXG-R-W` 上 raise 杀掉多小时作业），与本 plan 无关，全部保留。

## Task Commits

1. **Task 1: 删除条件 (a) 的全部十一个位置** — `e8dd2f4` (refactor)
2. **Task 2 (RED): 红利断言** — `e6f475a` (test)
3. **Task 2 (docs): 机制论证** — `01d5496` (docs)

## Files Created/Modified

- `quantlab/factor/universe_filter.py` — 删掉条件 (a) 的十一个位置 + `import re`；模块 docstring 的 LS-1 去掉 (a)（字母编号刻意保留为 (b)(c)，引用它们的文档不必跟着改）并补一段删除理由，LS-3 改写成「永不删列」；类 docstring 与 `_mask_panel` docstring 各补一段红利说明。净 −169 行中的大头是那 43 行测量注释与九条正则。
- `tests/test_universe_filtered_factor.py` — 删掉 6 个针对条件 (a) 的测试（含 33 + 21 个参数化 ticker 断言）；新增 3 个测试（`test_mask_panel_never_drops_a_column` 两参数、`test_the_static_ticker_rule_is_not_a_constructor_knob_any_more`、`test_get_config_carries_exactly_the_three_threshold_parameters`）；`test_ticker_rule_symbols_absent_in_every_window_threshold_failures_kept` 改名为 `test_the_symbol_axis_is_the_same_in_two_disjoint_date_windows` 并改断言为「整个 fixture roster 都在轴上」。
- `tests/universe_fixtures.py` — 删掉专为九条正则造的 `WARRANT = "JUNKW"` 与 `WARRANT_ADJ_CLOSE`；`PLAIN_COMMONS` 加第 13 个普通股补位维持 `SYMBOLS16` 的 16 个标的（KunQuant 的 SIMD 块宽对齐，aarch64 上 16 可以、13 不行）；`pandas_universe_mask` 去掉 `exclude_non_common` 形参与那段 ticker 循环，成为两条件参考实现。
- `example/universe.md` — 「票代码规则」→「为什么是删而不是关」；LS-1 从三条改两条；参数表去掉第四个参数并说明旧配置会被点名拒绝；「标的轴规则」改写成「永不删列」；事故叙述补一句证券类型那一半的归属；与 `UniverseMask` 的对比表更新。239 → 250 行，diffstat +50/−39（净 +11，一致）。
- `example/README.md` — 索引表里 `universe.md` 那一行不再承诺过滤权证/测试代码。75 → 75 行，+1/−1（一致）。

## Decisions Made

- **删而不是关。** 把 `exclude_non_common` 默认改成 `False` 会留下一个仍被 `to_dict()` 序列化进每一份 run config、仍在文档里描述、仍在每次 `compute_universe_mask` 里执行一遍、但什么都不做的旋钮 —— 比删掉更难发现。
- **论证写成机制论证而不是比较论证。** 「CRSP 更强」是一个比较；「九条正则全部要求字母，对数字串恒不匹配」是一个机制事实，读者可以自己验证，而且不依赖对 CRSP 的信任。两者都写了，机制那条放在前面。
- **LS-1 的字母编号保留为 (b)(c)。** 不重新编号成 (a)(b)，这样 RESEARCH / CONTEXT / 其它文档里「条件 (b)」「条件 (c)」的引用继续有效。
- **`example/README.md` 的那一行也改了。** 原文写「结果里混进了权证/仙股/测试代码时」查这篇文档 —— 删掉 (a) 之后本类不再过滤权证/测试代码，留着这句会把读者导向错误的模块。

## Deviations from Plan

### 1. [Rule 3 - Blocking] Task 1 提前改了 `tests/test_universe_filtered_factor.py`

- **Found during:** Task 1
- **Issue:** Task 1 的 `<files>` 只列了 `universe_filter.py` 与 `universe_fixtures.py`，但它的 `<verify>` 要求 `tests/test_universe_filtered_factor.py` 全绿。该文件里有 6 个测试直接调用被删掉的 `is_common_ticker` / 传 `exclude_non_common` / 断言 `WARRANT` 不在轴上 —— Task 1 删完代码后它们必然 `AttributeError` / `TypeError`，Task 1 的 verify 不可能通过。
- **Fix:** 把「删掉针对条件 (a) 的测试」这一半（Task 2 `<action>` 第 1 步的前半句）提前到 Task 1 一起做；Task 2 只做新增与文档。两个任务的 verify 因此都能诚实地跑绿。
- **Files modified:** `tests/test_universe_filtered_factor.py`
- **Verification:** `uv run pytest -q tests/test_universe_filtered_factor.py tests/test_universe.py -p no:cacheprovider` 在 Task 1 提交前跑过（除一个 pre-existing 失败外全绿，见下）
- **Committed in:** `e8dd2f4`

### 2. [Rule 3 - Blocking] fixture 删掉 WARRANT 后用普通股补位

- **Found during:** Task 1
- **Issue:** 计划要求删掉「专为九条正则造的 ticker 造景」，`WARRANT = "JUNKW"` 正是。但它是 `SYMBOLS16` 的 16 个标的之一，直接删会变成 15 个，撞上 KunQuant 的 SIMD 块宽约束（本机 aarch64 上实测 16 可以、13 不行），所有 `cal()` 测试会以 `Bad shape at open` 失败。
- **Fix:** `PLAIN_COMMONS` 加第 13 个普通股（`AMD`）补位，`SYMBOLS16` 仍是 16 个。
- **Files modified:** `tests/universe_fixtures.py`
- **Verification:** 全部 KunQuant 路径测试（batch / stream / read / backtest / DL 头）跑绿
- **Committed in:** `e8dd2f4`

### 3. [计划预期的分支] `tests/test_ticker_pattern_reconciliation.py` 未修改

- **Found during:** Task 2
- **Issue:** 计划写「若它对账的是 universe_filter 的九条正则 vs TRADEABLE_TICKER_PATTERN，删 (a) 后一半失去对象」，要求先 `grep '^def test_'` 逐个判断。
- **判断结果:** 5 个测试**没有一个**以 `universe_filter` 的九条正则为一侧 —— 该文件只 import `quantlab.base.acquisition` 与 `quantlab.enums.data`，对账的是 roster builder 的输出 ↔ 取数守卫 / change-log 守卫。**全部保留，文件一行未改。**
- **Files modified:** 无
- **Verification:** `uv run pytest -q tests/test_ticker_pattern_reconciliation.py` → 5 passed（Task 2 的 verify 命令里一并跑过）

---

**Total deviations:** 2 auto-fixed（均为 Rule 3 阻塞项）+ 1 计划内分支判断
**Impact on plan:** 都不是范围扩张。偏差 1 只是把计划里本就写明的「删掉条件 (a) 的测试」这一步挪早一个任务；偏差 2 是删除造景的必要配套；分支判断的结果是「少改一个文件」。计划的 prohibitions 全部遵守：`acquisition/universe.py` 与 `enums/data.py` **零改动**（`git diff 16d76c7..HEAD` 里根本没有这两个文件），`min_price`/`min_dollar_volume`/`window` 与 `:86-123` 图改写机制完好，没有写任何 config 迁移或兼容层。

## TDD Gate Compliance

Task 2 标了 `tdd="true"`，但它锁的行为是 **Task 1 删除动作的红利**，而 Task 1 在计划顺序上先于它 —— 天真地执行就会撞上 TDD 的 fail-fast 规则 1（unexpected GREEN in RED phase）。

处理方式：**把删除前的实现临时放回，对新测试取真实 RED**。

```
git show 16d76c7:quantlab/factor/universe_filter.py > quantlab/factor/universe_filter.py
uv run pytest -q -p no:cacheprovider tests/test_universe_filtered_factor.py -k "<三个新测试>"
  -> 4 tests, 1 pass, 3 fail
git checkout -- quantlab/factor/universe_filter.py      # 还原（未用 git stash，用户明令禁止）
  -> 同一条命令 4 passed
```

`gsd-tools check tdd-red-evidence` 对该次运行的 TAP 投影判定 **`RED_EVIDENCE_OK` / `target_test_failed`**，target 为 `test_mask_panel_never_drops_a_column[ticker_axis]`，失败原因是对计划行为的断言（symbol 轴被窄化 / DID NOT RAISE TypeError / 序列化键集多出一项），不是 import 错误或 fixture 崩溃。

**那一个 PASS 本身就是机制证据**：`test_mask_panel_never_drops_a_column[permno_axis]` 在**删除前**的代码上也通过 —— 九条正则没有一条能匹配数字串，所以它在 PERMNO 轴上本来就什么都没删。这条过滤不是被取代，它早就停止工作了。

| Gate | Commit | 说明 |
|---|---|---|
| RED | `e6f475a` `test(03.11-06)` | ✓ 真实 RED，证据经 `check tdd-red-evidence` 判定 OK |
| GREEN | `e8dd2f4` `refactor(03.11-06)` | ⚠ **在 RED 之前**，且是 `refactor` 不是 `feat` |
| REFACTOR | — | 无 |

**违规说明（按 tdd.md「Fail-Fast Rules」第 3 条记录）：** 没有 `feat(03.11-06)` 提交，且使 GREEN 成立的改动（`e8dd2f4`）在 RED 提交之前。这是**计划的任务顺序**决定的 —— Task 1 是删除、Task 2 是给删除的红利加锁 —— 而不是执行时的纪律松动。RED 证据是真实测量的，只是测量的对象是「删除前的实现」而不是「尚未写的实现」。删除类改动大概率都是这个形状。

## Issues Encountered

**全库回归的口径差异（1 个测试，非本 plan 引入）。** 编排器在 base `16d76c7` 上独立测得的基线是 `60 failed, 1671 passed, 1 skipped`（总计 1,732）。本 plan 完成后同一条命令测得 `60 failed, 1617 passed, 1 skipped`（总计 1,678，`--collect-only` 复核一致）。

- **失败集合逐条不变**：仍是 60 个，其中 55 个是长期存在的（`scripts/` 不在 pythonpath、`test_factor_kunquant.py` 的 `KeyError 'amount'`），另 5 个是 tracer 交给下游 plan 的（`test_crsp_constituent.py::test_membership_symbols_agree_with_the_crsp_price_panel` → plan 05，`test_ingest_wrds_crsp.py` 4 个 → plan 08）。**零新增失败。**
- **通过数 −54**：其中 53 个精确对应本 plan 的测试手术 —— `tests/test_universe_filtered_factor.py` 从 84 个收集项（29 个 def，含 33 + 21 个参数化 ticker 断言）降到 31 个（27 个 def）。
- **剩下 1 个对不上**，且没有表现为任何新增失败或新增 skip。基线是编排器在主工作树上独立测的，本 plan 的改动只触及 5 个文件、其中只有两个 `tests/` 文件，逐个 def 的 diff（见下）也核过没有误删。这 1 个差异记录在此不掩饰，但不构成本 plan 的回归。

逐个 def 的 diff 复核（base vs HEAD）确认删除的正是那 6 个条件 (a) 测试、新增的正是那 3 个，另有 1 个改名：

```
- test_common_tickers_survive_the_rule            - test_is_common_ticker_is_case_insensitive
- test_non_common_tickers_are_excluded            - test_exclude_non_common_false_drops_no_symbol
- test_mask_excludes_a_warrant_ticker_unless_the_rule_is_disabled
- test_ticker_rule_symbols_absent_in_every_window_threshold_failures_kept  (改名)
+ test_mask_panel_never_drops_a_column            + test_the_static_ticker_rule_is_not_a_constructor_knob_any_more
+ test_get_config_carries_exactly_the_three_threshold_parameters
+ test_the_symbol_axis_is_the_same_in_two_disjoint_date_windows            (改名后)
```

**`tests/test_universe.py::test_every_universe_category_is_reachable_from_the_cli` 在 Task 1 的 verify 里失败**，经查是 `ModuleNotFoundError: No module named 'ingest_alpaca'` —— 即基线里那 55 个「`scripts/` 不在 pythonpath」的长期失败之一，与本 plan 无关，未处理（scope boundary）。

## Estimate vs Actuals

| | estimate | actual |
|---|---|---|
| tokens | 60,000 | **10,264** |
| tasks | 2 | 2 |
| commits | — | 3 |

`actual.tokens` 按 SUMMARY 模板的口径算：`git diff 16d76c7..HEAD` 的 41,054 字符 ÷ 4。若改按「改动过的 5 个文件的全文」口径则是 93,003 ÷ 4 = 23,251。两个数都列出来，是因为计划的 60,000 估值用的是哪个口径无从确认 —— 不往估值方向凑数，免得污染后续校准。删除类计划实际成本低于估值是可预期的（删掉的行不需要被想出来），但 6× 的差距值得后续 plan 在估值时留意。

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- **因子层不再有任何 ticker 形状假设。** `UniverseFilteredFactor` 现在只读 `close`/`volume` 数组，完全不看 symbol 值，int64 PERMNO 轴对它零影响。
- **下游可以依赖「因子包装类永不窄化 symbol 轴」这条不变式** —— 有 `test_mask_panel_never_drops_a_column` 把守。
- **无阻塞。** 两处 pattern 本体与 `TRADEABLE_TICKER_PATTERN` 未被触碰，Tiingo 采集与路径段安全检查不受影响。
- 一个留给人读的点（coverage D4）：`example/universe.md` 那段机制论证是否足以取代原来那 43 行测量注释，grep 证明不了，建议在 phase 验收时读一遍。

## Self-Check: PASSED

- `03.11-06-SUMMARY.md` 存在于计划目录 ✓
- 四个提交均在 `16d76c7..HEAD` 里：`e8dd2f4` / `e6f475a` / `01d5496` / `d515a87` ✓
- `git status --short` 干净（无未跟踪文件、无未提交改动）✓
- 无文件删除：`git diff --diff-filter=D --name-only 16d76c7..HEAD` 为空 ✓
- 无 stub / 无 skipped test / 所有 `<verify>` 命令均真实跑过 ✓

---
*Phase: 03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r*
*Completed: 2026-09-21*
