---
phase: 03.11
plan: 16
type: execute
wave: 1
depends_on: ["03.11-14"]
files_modified:
  - tests/test_crsp_identity.py
  - tests/test_extensibility_contract.py
autonomous: true
gap_closure: true
# 本 plan 收的是 03.11-VERIFICATION.md `advisory:` 块里的三条 Info，不是四个 primary gap
# 中的任何一条。它加固的是 G-03.11-1（门的覆盖面）与 G-03.11-2（冗余字段）这两条**已关闭**
# 的 gap 各自的守卫 —— IN-02 正是「守着 G-03.11-2 的那条断言比它的兄弟弱一半」，
# IN-03/IN-04 是 G-03.11-1 那次门加宽留下的两处台账不实。
gap_ids: [G-03.11-1, G-03.11-2]
covers_advisory: [IN-02, IN-03, IN-04]
requirements: [DATA-03]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "IN-02：`tests/test_crsp_identity.py` 的 `test_the_equity_store_over_the_same_raw_tier_drops_qqq` 里，被丢弃 PERMNO 的记录断言与它的两个兄弟（`:587-588`、`:1478-1479`）同强 —— `\"symbol\" not in record` 之外另有 `set(record) == {\"types\", \"rows\", \"first\", \"last\"}`"
    - "IN-02：加强之后，一条退化成 `{}` 的记录**会让这条用例变红**（此前 `{}` 能满足它）—— 执行者以一次手工变异验证并把输出写进 SUMMARY"
    - "IN-04：`tests/test_extensibility_contract.py:6` 的模块 docstring 点名的是真实存在的函数名 `test_core_layer_purity_no_market_specific_logic`（定义在 `:59`），按文档名 grep 能命中"
    - "IN-03：`tests/test_extensibility_contract.py:31-56` 的注释明写这个门的覆盖面有多窄 —— 它只看得见下方那四个 legacy market 字面量，而 `quantlab/base/backtest.py` 自身的两处具体耦合（`:15` 的具名 vendor import，按裁定 (b) 故意排除；`:153` 散文里的具体市场类名）在结构上不在它的射程内"
    - "IN-03：`FORBIDDEN_SUBSTRINGS` 仍是原来那四项，一字未增 —— 被否决的选项 (a) 没有被偷偷执行"
    - "empty：`{}` 这个退化记录现在是三处断言各自的反例，而不只是其中两处的"
    - "ordering：记录的四个键用**集合**比较，不依赖 JSON 里的键序；三处断言的写法一致"
    - statement: "encoding — 过滤审计报告按 `json.dumps` 写、按 `json.load` 读，三处断言都在 Python 对象上比较，不触及字节编码；本 plan 不引入任何新的编码面"
      verification: backstop
    - statement: "idempotency — 三处断言都是纯读，重复运行不改变被断言对象；用例之间不共享 store"
      verification: backstop
    - statement: "adjacency — `first` / `last` 是窗口两端的日期，三处兄弟断言中有两处已逐字段断值到端点；本 plan 只补齐键集这一半，不改动端点语义"
      verification: backstop
    - statement: "concurrency — 两个测试文件各自用 tmp_path 构建 store，pytest 并行与否都不共享状态"
      verification: backstop
  artifacts:
    - tests/test_crsp_identity.py
    - tests/test_extensibility_contract.py
  key_links:
    - "`_permno_breakdown`（`quantlab/dataset/crsp.py:1263-1271`）→ `dropped_permnos`（`:1116`）→ `tests/test_crsp_identity.py` 的第三处断言 —— 三处断言共守同一个字典字面量，强度必须一致"
    - "`CORE_LAYER_FILES` → `test_core_layer_purity_no_market_specific_logic`（`:59`）→ 未来 market literal 泄漏进 `quantlab/base/backtest.py` 时变红；模块 docstring 必须能把读者带到这个函数"
  prohibitions:
    - statement: "不得把 `CrspTickerLookup` 或任何形式的具名 vendor 类加进 `FORBIDDEN_SUBSTRINGS` —— 那是 03.11-UAT test 1 上被明确否决的选项 (a)，会让门立刻变红"
      status: unverified
      verification: flagged
    - statement: "不得重构 `quantlab/base/backtest.py:15` 的具名 vendor import —— 裁定是 (b)：接受现状、把文件纳入门以免继续恶化；本 plan 的 files_modified 里没有它"
      status: unverified
      verification: flagged
    - statement: "不得让一段注释把一个窄门说成一次纯度证明 —— 读者据此判断「这个文件已被覆盖」，而实际覆盖面只有四个字面量"
      status: unverified
      verification: flagged
    - statement: "不得用放宽断言的方式让用例变绿 —— 本 plan 的方向是加强；一条 `{}` 也能通过的断言等于没有断言"
      status: unverified
      verification: flagged
  gap_ids: [G-03.11-1, G-03.11-2]
  covers_advisory: [IN-02, IN-03, IN-04]
---

<!-- planner-discipline-allow: FORBIDDEN_SUBSTRINGS, CORE_LAYER_FILES, CrspTickerLookup, dropped_permnos -->

<objective>
把 03.11-REVIEW.md 的三条 Info 收掉。它们都不是缺陷判定，而是**台账与事实对不上**：
一条断言比它的两个兄弟弱一半、一段 docstring 点名了一个不存在的函数、一段注释容易被读成
「这个文件已被纯度覆盖」而实际覆盖面只有四个字面量。三条都在同一个位置上会造成同一种后果：
下一个读者据此做出一个错误的判断，而没有任何东西会变红。

**为什么和 03.11-15 并行。** 两者零文件交集：15 动 `crsp_tickers.py` / `base/model.py` /
`test_model_predict_panel.py` / `test_crsp_ticker_sidecar.py` / `example/wrds_crsp.md`，
本 plan 只动 `tests/test_crsp_identity.py` 与 `tests/test_extensibility_contract.py`。

Purpose: 让三处台账与它们描述的事实一致，且让加强后的断言真的会因退化而变红。
Output: 一条补齐的键集断言 + 一处函数名订正 + 一句覆盖面说明。**零生产代码改动。**
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-REVIEW.md
@.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-VERIFICATION.md
@.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-13-PLAN.md
@tests/test_extensibility_contract.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1: IN-02 —— 让第三处断言与它的两个兄弟同强，并证明它会因退化而变红</name>
  <files>tests/test_crsp_identity.py</files>
  <read_first>
    - `tests/test_crsp_identity.py:583-595`（第一个兄弟：`"symbol" not in` + `set(...) == {...}` + 四个字段逐个断值）
    - `tests/test_crsp_identity.py:1474-1486`（第二个兄弟：同款）
    - `tests/test_crsp_identity.py:1096-1102`（要改的第三处，在 `test_the_equity_store_over_the_same_raw_tier_drops_qqq` 里，只有否定的一半）
    - `.planning/phases/03.11-.../03.11-REVIEW.md` 的 IN-02 一节（lines 347-368）—— 逐字指出 `"symbol" not in record` 被 `{}`、被字段改名、被任何带 `__contains__` 的非 dict 对象满足，并给了三行的修法
    - `quantlab/dataset/crsp.py:1226-1271` —— `_permno_breakdown` 的 docstring 与那个只剩四个键的字典字面量；断言要守的就是它
    - `.planning/phases/03.11-.../03.11-13-PLAN.md` 的 `patterns-established` 一节 —— 「只删不补」这条自我警告，IN-02 是它的小号版本
  </read_first>
  <precondition>03.11-14 已清空全树 `__pycache__`；本任务的 pytest 结果若取自清场之前，都不是 HEAD 的结果。</precondition>
  <action>
在 `test_the_equity_store_over_the_same_raw_tier_drops_qqq` 里，把那条只有否定一半的断言补成与两个兄弟同款。
REVIEW IN-02 的 Fix 段给了确切的三行形状：先把记录取成一个局部名（两个兄弟都这么做，读起来也短），
再断 `"symbol" not in`，再断键集恰好是那四个。

**两个兄弟另有「四个字段逐个断值」的第三层**。这一处要不要也加，由这条用例自己能拿到什么决定：
它断言的是**被丢弃**的 QQQ，窗口常量是 `QQQ_DAYS`，`dropped_by_type` 那一行（`:1103`）已经拿
`len(QQQ_DAYS)` 断过行数。把 `rows` / `first` / `last` 也接到同一组常量上是自然的，
但若发现某个字段在这条用例里没有稳定可引的常量，就**只补键集**并在断言旁写一句为什么止于此 ——
不要为了凑齐三层而把一个魔数写进断言。键集那一条是 IN-02 明确要求的最低线。

注释按两个兄弟的口吻写（`:585-589` 那段解释「为什么没有 `symbol` 字段」的话已经把理由说清楚了），
不要整段复制第三遍；这一处只需要一句指向 G-03.11-2 的裁定 + 一句说明为什么键集断言是必需的
（`"symbol" not in` 单独一条会被 `{}` 满足）。

**变异验证（不留在代码里，写进 SUMMARY）。** 临时把 `quantlab/dataset/crsp.py:1263-1271` 那个字典字面量
改成返回空 dict，跑这条用例确认它**变红**，再把改动丢掉。这是「加强确实加强了」的唯一直接证据。
改动用 `git checkout --` 或 `git stash pop` 之外的方式撤回 —— 按本项目既有的约定，
A/B 对照不要走 `git stash`（一次中断会把工作树留在 stash 里）；用 `git show HEAD:<file>` 取原文
写进临时副本、或直接手工改回并用 `git diff` 确认已归零。
  </action>
  <acceptance_criteria>
    - `grep -c 'set(dropped) ==\|set(record) ==\|set(qqq) ==\|set(rescued) ==' tests/test_crsp_identity.py` 返回 3（三处断言各一条键集比较；局部名由执行者统一）
    - `grep -c '"symbol" not in' tests/test_crsp_identity.py` 仍返回 3（三条否定断言一条不少）
    - `uv run pytest -q tests/test_crsp_identity.py` 全绿，用例数与改动前相同（本任务只加强既有用例，不新增用例）
    - 把 `quantlab/dataset/crsp.py` 的 `_permno_breakdown` 临时改成返回空 dict 之后，`test_the_equity_store_over_the_same_raw_tier_drops_qqq` **变红**；`git diff --stat quantlab/dataset/crsp.py` 在任务结束时为空
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/test_crsp_identity.py -p no:cacheprovider</automated>
    <fails_when>退出码非 0，或摘要行出现任何 failed / error 计数</fails_when>
    <automated>git diff --stat -- quantlab/dataset/crsp.py</automated>
    <fails_when>输出非空（说明变异验证的临时改动没有被撤回，生产代码被本 plan 污染了）</fails_when>
  </verify>
  <done>三处断言同强；空记录会让第三处变红并已实测；`quantlab/dataset/crsp.py` 一字未改。</done>
</task>

<task type="auto">
  <name>Task 2: IN-04 函数名订正 + IN-03 覆盖面说明</name>
  <files>tests/test_extensibility_contract.py</files>
  <read_first>
    - `tests/test_extensibility_contract.py:1-60` 全段 —— 模块 docstring（`:6` 点名的函数名已过期，`:7-9` 是 plan 13 刚改过的「四个核心层」）、`CORE_LAYER_FILES`（`:31-36`，四项）、裁定 (b) 的注释块（`:38-50`）、`FORBIDDEN_SUBSTRINGS`（`:51-56`，原四项）、函数定义（`:59`）
    - `.planning/phases/03.11-.../03.11-REVIEW.md` 的 IN-03（lines 371-398）与 IN-04（lines 402-412）—— IN-03 的 Fix 段逐字给了要补的那句话，并明确写着「No code change is warranted」；IN-04 是一行改名
    - `quantlab/base/backtest.py:15` 与 `:153` —— IN-03 要点名的那两处具体耦合。`:15` 是具名 vendor import（按裁定 (b) 故意排除），`:153` 是 docstring 里的具体市场类名（门会扫 docstring 行，但小写的模块路径字面量不出现在 CamelCase 类名里，所以它也过得去）
    - `.planning/phases/03.11-.../03.11-UAT.md` test 1 的裁定原文 —— 选项 (a)（把查表类加进禁词表）被否决，(b) 被采纳
  </read_first>
  <action>
**IN-04。** `tests/test_extensibility_contract.py:6` 的模块 docstring 项目符号点名了一个不存在的函数。
真实定义在 `:59`，名字是 `test_core_layer_purity_no_market_specific_logic`。把 `:6` 改成这个真名。
只改这一处标识符，`:7-9`「四个核心层」那段是 plan 13 刚改对的，不要动。
同时确认 `:10` 点名的 `test_fake_dataset_lifecycle` 确实存在（`grep -c 'def test_fake_dataset_lifecycle'` 应为 1）——
一条已知过期的文档在同一段里往往不止一处，顺手核一遍比下一轮再立一个 IN 便宜。

**IN-03。** 在 `:38-50` 那个记录裁定 (b) 的注释块末尾**追加一句**（REVIEW 的 Fix 段给了原文），
说明这个门看得见什么、看不见什么：它只扫下方那四个 legacy market 字面量；
`quantlab/base/backtest.py` 自身的两处具体耦合 —— `:15` 的具名 vendor import（按裁定 (b) 故意排除）
与 `:153` 散文里的具体市场类名 —— 在结构上不在它的射程内。
把「门今天是绿的」与「这个文件已被纯度覆盖」这两件事明确分开：前者成立，后者不成立。
**不改任何代码，不改 `FORBIDDEN_SUBSTRINGS`，不改 `CORE_LAYER_FILES`。**

散文里点 `:15` / `:153` 时，用「具名 vendor import」「散文里的具体市场类名」这类描述 +
行号来指路，不要把那两个具体符号名写成禁词表读得到的字面量 —— 这个文件自己就是按子串扫描工作的，
在它的注释里种下一个新字面量是可以想见的自伤。
  </action>
  <acceptance_criteria>
    - `grep -c 'test_core_layer_purity_no_market_specific_logic' tests/test_extensibility_contract.py` 返回 2（docstring 一处 + 函数定义一处）
    - `grep -n 'test_no_market_specific_logic_in_core_layers' tests/test_extensibility_contract.py` 无匹配（退出码非 0）
    - `grep -c 'def test_fake_dataset_lifecycle' tests/test_extensibility_contract.py` 返回 1
    - `grep -c 'us_equity' tests/test_extensibility_contract.py` 返回 1（**全文口径**：今天全文恰好一处，就在禁词元组里 —— 改动后仍是 1 即证明新追加的注释没有种下第二个。不要用 `sed -n '30,60p'` 这类固定行段口径：本任务正是要往那个注释块里追加几行，被它推下去的禁词表会掉出行段，判据于是返回 0 —— 一次与它真正要防的事毫无关系的假红）
    - `python3 -c "import ast; src=open('tests/test_extensibility_contract.py').read(); t=ast.parse(src); print(sorted(n.id for n in ast.walk(t) if isinstance(n, ast.Name)))"` 里 `FORBIDDEN_SUBSTRINGS` 与 `CORE_LAYER_FILES` 仍在；且这两个元组的元素个数分别为 4 与 4
    - `uv run pytest -q tests/test_extensibility_contract.py` 全绿
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/test_extensibility_contract.py -p no:cacheprovider</automated>
    <fails_when>退出码非 0；或 `test_core_layer_purity_no_market_specific_logic` 报 failed（说明注释里种进了一个被扫到的市场字面量）</fails_when>
    <automated>uv run pytest -q tests/test_crsp_identity.py tests/test_extensibility_contract.py -p no:cacheprovider</automated>
    <fails_when>退出码非 0，或两个文件的 passed 总数低于改动前的数字（本 plan 只加强、不新增、不删除用例）</fails_when>
  </verify>
  <done>docstring 点名的是真实函数名；裁定 (b) 的注释块末尾有一句把「门今天是绿的」与「已被纯度覆盖」分开的说明；两个元组一字未改。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 测试断言 → 「这一层是干净的」这个结论 | 断言是这个结论的唯一来源；一条被 `{}` 满足的断言让退化悄悄通过 |
| 注释 / docstring → 下一个读者的判断 | 一段说「已覆盖」的注释会让人跳过一次本该做的检查 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.11-58 | Repudiation | `tests/test_crsp_identity.py` 第三处记录断言 | medium | mitigate | Task 1 补齐键集断言，并以一次变异验证证明空记录会让它变红 |
| T-03.11-59 | Information Disclosure | `tests/test_extensibility_contract.py:38-56` 的注释 | low | mitigate | Task 2 追加一句把门的实际射程写清楚，避免「绿 = 已证纯度」的误读 |
| T-03.11-60 | Tampering | `FORBIDDEN_SUBSTRINGS` | low | mitigate | 两条 prohibitions + 一条元素个数为 4 的验收标准；被否决的选项 (a) 不得被执行 |

（本 plan 零生产代码改动、零依赖变动、无 npm/pip/cargo 安装动作，故无供应链条目。）
</threat_model>

<verification>
1. `tests/test_crsp_identity.py` 的三处记录断言同强（各有 `"symbol" not in` + 键集比较）。
2. 把 `_permno_breakdown` 临时改成返回空 dict 会让第三处变红；任务结束时 `quantlab/dataset/crsp.py` 的 diff 为空。
3. `tests/test_extensibility_contract.py:6` 点名的函数名能 grep 到定义。
4. 裁定 (b) 的注释块末尾有一句覆盖面说明；`FORBIDDEN_SUBSTRINGS` 与 `CORE_LAYER_FILES` 各仍是四项。
5. 两个文件在清缓存后的默认缓存目录下全绿。
</verification>

<success_criteria>
IN-02 / IN-03 / IN-04 三条全部收掉：一条断言不再能被退化产物满足，一段 docstring 不再指向空气，
一段注释不再把一个窄门说成一次纯度证明。三处都没有触碰任何生产代码，也没有执行被否决的选项 (a)。
</success_criteria>

## Artifacts this phase produces

| 符号 / 改动 | 位置 | 性质 |
|---|---|---|
| 第三处记录的键集断言 | `tests/test_crsp_identity.py`，`test_the_equity_store_over_the_same_raw_tier_drops_qqq` 内 | 既有用例内新增断言行（**不新增测试函数**） |
| `:6` 的函数名订正 | `tests/test_extensibility_contract.py` | 模块 docstring 一行 |
| 覆盖面说明 | `tests/test_extensibility_contract.py:38-50` 注释块末尾 | 追加一句 |

**本 plan 不新增任何符号（无新函数、无新常量、无新文件），也不改动任何生产代码。**
