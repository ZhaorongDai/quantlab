---
phase: 03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r
plan: 16
subsystem: testing
tags: [pytest, crsp, permno, extensibility-contract, assertion-strength, docs-accuracy]

# Dependency graph
requires:
  - phase: 03.11-14
    provides: 清空全树 __pycache__ 的清场，以及默认缓存目录下 98 passed 的可信基线
provides:
  - "`tests/test_crsp_identity.py` 三处 `dropped_permnos` / `roster_overrides` 记录断言强度一致（各有否定断言 + 键集比较 + 逐字段断值）"
  - "一次实测的变异证据：`_permno_breakdown` 退化成空记录时，第三处用例在新增的键集行上变红"
  - "`tests/test_extensibility_contract.py` 的模块 docstring 点名的是真实存在的函数名"
  - "一段写明 core-layer 纯度门实际射程的注释，把「门是绿的」与「文件已被证明干净」分开"
affects: [03.11-VERIFICATION, 未来任何加宽 CORE_LAYER_FILES 或改动 _permno_breakdown 的 phase]

actuals:
  tokens: 22200
  tasks: 2
  commits: 2
plan_head_before: db7b7ae428943de4d8afb857a7231d99bce62848

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "同一个数据结构被多处断言时，三处的强度必须一致 —— 弱的那一处决定了整组断言的真实强度"
    - "加强断言之后用一次抛弃式变异验证证明它确实会变红，证据写进 SUMMARY 而不留在代码里"
    - "在按子串扫描工作的文件里写注释时，用角色 + 行号指路，不把符号名写成扫描器读得到的字面量"

key-files:
  created: []
  modified:
    - tests/test_crsp_identity.py
    - tests/test_extensibility_contract.py

key-decisions:
  - "第三处断言补齐到与两个兄弟完全同款的三层（否定 + 键集 + 逐字段断值），而不是只补 IN-02 要求的键集最低线 —— `QQQ_DAYS` 是稳定可引的常量，`rows` / `first` / `last` 都能接到它，不需要写任何魔数"
  - "变异验证走 `cp` 备份 + `git checkout --` 复原，不碰 `git stash`（项目既有约定：一次中断会把工作树留在 stash 里）"
  - "新增注释按角色 + 行号描述那两处具体耦合，不写具名 vendor 类或具体市场类名的字面量 —— 这个文件自己就是按子串扫描工作的"
  - "IN-03 按 REVIEW 的判定「No code change is warranted」执行：只加散文，`FORBIDDEN_SUBSTRINGS` 与 `CORE_LAYER_FILES` 一字未改"

patterns-established:
  - "断言强度对齐：一个字典字面量被 N 处断言守着时，最弱的那一处就是它实际的保护水平"
  - "变异验证作为「加强确实加强了」的直接证据，并记录到底是哪一行变红"

requirements-completed: [DATA-03]

coverage:
  - id: D1
    description: "IN-02：`test_the_equity_store_over_the_same_raw_tier_drops_qqq` 的记录断言补齐到与 `:587-588` / `:1478-1479` 两个兄弟同强"
    requirement: "DATA-03"
    verification:
      - kind: unit
        ref: "tests/test_crsp_identity.py#test_the_equity_store_over_the_same_raw_tier_drops_qqq"
        status: pass
      - kind: other
        ref: "grep -c 'set(dropped) ==\\|set(record) ==\\|set(qqq) ==\\|set(rescued) ==' tests/test_crsp_identity.py -> 3"
        status: pass
      - kind: other
        ref: "grep -c '\\\"symbol\\\" not in' tests/test_crsp_identity.py -> 3"
        status: pass
    human_judgment: false
  - id: D2
    description: "IN-02 变异验证：`_permno_breakdown` 退化成空记录时，这条用例确实变红（此前 `{}` 能满足它）"
    requirement: "DATA-03"
    verification:
      - kind: other
        ref: "手工变异 quantlab/dataset/crsp.py:1263-1271 -> 1 failed；失败行为新增的 `set(dropped) == {...}`；复原后 `git diff --stat` 为空"
        status: pass
    human_judgment: false
  - id: D3
    description: "IN-04：`tests/test_extensibility_contract.py` 模块 docstring 点名的函数名可按文档名 grep 到定义"
    requirement: "DATA-03"
    verification:
      - kind: other
        ref: "grep -c 'test_core_layer_purity_no_market_specific_logic' -> 2；grep 'test_no_market_specific_logic_in_core_layers' 无匹配"
        status: pass
      - kind: unit
        ref: "tests/test_extensibility_contract.py#test_core_layer_purity_no_market_specific_logic"
        status: pass
    human_judgment: false
  - id: D4
    description: "IN-03：裁定 (b) 注释块末尾写明这道门的实际射程，读者不会把绿门误读成纯度证明"
    requirement: "DATA-03"
    verification:
      - kind: other
        ref: "AST 检查 FORBIDDEN_SUBSTRINGS / CORE_LAYER_FILES 各仍为 4 项；grep -c 'us_equity' -> 1（未种下第二个字面量）"
        status: pass
    human_judgment: true
    rationale: "「这段散文是否真的能阻止下一个读者误判」只有人能判断 —— 自动化能证明的只是元组没被改、没种下新字面量，证明不了措辞达到了目的"

# Metrics
duration: 9 min
completed: 2026-09-21
status: complete
---

# Phase 03.11 Plan 16: 收掉 IN-02 / IN-03 / IN-04 三条台账不实 Summary

**一条被 `{}` 满足的断言补成了会因退化变红的断言（并以一次变异实测为证），一段点名空气的 docstring 改指真实函数，一段容易被读成「纯度已证明」的注释写清了它实际只看得见四个字面量。零生产代码改动。**

## Performance

- **Duration:** 约 9 min
- **Started:** 2026-09-21T22:51:00Z（近似；基线测试起跑时刻）
- **Completed:** 2026-09-21T22:59:44Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- **IN-02 收掉，并且是被证明过的。** `test_the_equity_store_over_the_same_raw_tier_drops_qqq` 此前只有 `"symbol" not in` 这否定的一半；现在补上了 `set(dropped) == {"types", "rows", "first", "last"}`，并按两个兄弟的第三层把 `types` / `rows` / `first` / `last` 逐个接到 `QQQ_DAYS` 上。三处断言的写法、局部名习惯与强度自此一致。
- **变异验证给出了直接证据。** 把 `_permno_breakdown` 临时改成产出空记录后，这条用例变红，**失败的正是新增的键集行**，而它上面的否定断言依旧放行 —— 这恰好逐字复现了 IN-02 预言的失效方式。生产代码随后复原，`git diff --stat -- quantlab/dataset/crsp.py` 为空。
- **IN-04 收掉。** 模块 docstring 点名的 `test_no_market_specific_logic_in_core_layers` 从来不存在；改成真实定义 `test_core_layer_purity_no_market_specific_logic`。顺手核了同段的 `test_fake_dataset_lifecycle`，确认它是真实存在的（1 处定义）。
- **IN-03 收掉，且没有越界。** 在记录裁定 (b) 的注释块末尾追加一段，说明这道门只看得见下方四个 legacy market 字面量，而 `quantlab/base/backtest.py:15` 的具名 vendor import（按裁定 (b) 故意排除）与 `:153` 散文里的具体市场类名（CamelCase，与元组里那个小写点分模块路径永不相交）在结构上都不在它的射程内；并把「门是绿的」与「文件已被证明干净」明确分成两句话。`FORBIDDEN_SUBSTRINGS` 与 `CORE_LAYER_FILES` 各仍是四项，一字未改。

## Task Commits

1. **Task 1（tracer）: IN-02 断言加强 + 变异验证** - `7b93388` (test)
2. **Task 2: IN-04 函数名订正 + IN-03 覆盖面说明** - `6929a96` (docs)

**Plan metadata:** 见本文件的 `docs(03.11-16)` 提交。

## Files Created/Modified

- `tests/test_crsp_identity.py` — 在既有用例内新增 6 行断言（键集 + 四个字段）与一段说明为什么键集断言是必需的注释；**未新增测试函数**。
- `tests/test_extensibility_contract.py` — docstring 一处函数名订正；裁定 (b) 注释块末尾追加 12 行射程说明。

## Decisions Made

- **第三层「逐字段断值」也补了，不止补键集最低线。** 判据是 plan 给的那条：只有在「某个字段没有稳定可引的常量」时才止于键集。这条用例里 `QQQ_DAYS = ("1999-03-10", "2010-06-01", "2025-12-31")` 就是那组常量，`rows` 接 `len(QQQ_DAYS)`、`first` / `last` 接两端，一个魔数都不需要写。
- **注释措辞规避了自伤。** 初稿的注释里写了 `"symbol" not in dropped` 这个字面量，直接把 `grep -c '"symbol" not in'` 从 3 推到 4 —— 一条与它要防的事毫无关系的假红。改写成「the absence check below」后归位。同理，Task 2 的新注释全程用「具名 vendor import」「具体市场类名」+ 行号指路，`grep -c 'us_equity'` 改动后仍是 1。
- **A/B 对照不走 `git stash`。** 按项目既有约定，用 `cp` 到 scratchpad 备份 + `git checkout -- <file>` 复原，并用 `diff -q` 与备份比对二次确认。
- **变异验证后清空了 `__pycache__`。** 变异跑会写入字节码缓存；复原生产代码后先清场再复跑，避免 03.11-14 那条陈旧字节码陷阱。

## Deviations from Plan

None - plan executed exactly as written.

（Task 1 action 里给的「若某字段没有稳定常量就只补键集」是 plan 自带的分支条件，不是偏离；实际走的是「有稳定常量」那一支。）

## Issues Encountered

- **初稿注释把 `grep -c '"symbol" not in'` 从 3 顶到了 4。** 验收判据是全文口径的计数，而我在新注释里引用了那条断言的原文。改写措辞后归位。这件事本身正是 Task 2 那条「不要在按子串扫描的文件里种字面量」告诫的同构版本 —— 判据用全文计数，就意味着散文也在判据的射程内。

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- 03.11-REVIEW.md `advisory:` 块里的三条 Info 全部收掉，且都不是靠放宽判据收掉的。
- 两个文件在清场后的默认缓存目录下合计 **42 passed**，与改动前的基线逐数字相同（只加强、不新增、不删除用例）。
- `quantlab/dataset/crsp.py` 与其余生产代码一字未改；被否决的选项 (a)（把查表类加进禁词表）没有被执行。
- 遗留的诚实说明：IN-03 那段散文本身无法被自动化证伪 —— 它有没有达到「下一个读者不会误判」的目的，只有人能判断，已在 coverage 里标成 `human_judgment: true`。

## Self-Check: PASSED

- `tests/test_crsp_identity.py` — FOUND
- `tests/test_extensibility_contract.py` — FOUND
- `7b93388` — FOUND in `git log`
- `6929a96` — FOUND in `git log`
- 计划级 verification 五条逐条复跑：三处断言同强（3/3）✓；变异变红且 `git diff --stat` 为空 ✓；docstring 函数名可 grep 到定义（2 处）✓；两个元组各仍 4 项 ✓；两文件清场后 42 passed ✓。

---
*Phase: 03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r*
*Completed: 2026-09-21*
