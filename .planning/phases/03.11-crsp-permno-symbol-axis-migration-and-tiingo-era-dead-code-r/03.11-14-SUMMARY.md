---
phase: 03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r
plan: 14
subsystem: testing
tags: [pytest, cpython, bytecode, pycache, regression-gate, conftest]

requires:
  - phase: 03.11 (plans 01-13)
    provides: 四个 gap-closure suite 与 03.11-VERIFICATION.md 记录的两组实测数字
provides:
  - 全树 `__pycache__` 清场（17 个目录 → 0），G-03.11-4 的投毒字节码被移除
  - 默认缓存目录下重新取得的两组基线：四 suite `98 passed`、`tests/test_crsp_ticker_sidecar.py` `26 passed`（此前 `5 failed, 21 passed`）
  - `tests/test_stale_bytecode_lesson.py`：三条子进程用例，把「等长编辑 + 恢复 mtime ⇒ .pyc 不失效」以及两条补救锁成可执行证明
  - `tests/conftest.py`：与 macOS OpenMP 守卫并列的散文教训，点名上面那个可执行证明
  - 清缓存后的回归门基线：`55 failed, 1679 passed, 1 skipped`，逐文件失败分布与 deferred-items.md D-03.11-12-A 逐行一致
affects: [03.11 plans 15-18, 任何后续 wave 的 verify 证据链]

actuals:
  tokens: 2909
  tasks: 3
  commits: 2
plan_head_before: fe54bf15a6f4d529f20d96e7678cd8ace13fddea

tech-stack:
  added: []
  patterns:
    - "环境陷阱 = 一段与既有守卫并列的散文 + 一个会变红的可执行证明（沿用 260914-lno 的 OpenMP 守卫范式）"
    - "取证性子进程探针：`subprocess.run([sys.executable, ...], cwd=tmp_path)`，断言消息必带 stdout/stderr"

key-files:
  created:
    - tests/test_stale_bytecode_lesson.py
  modified:
    - tests/conftest.py

key-decisions:
  - "清场后的『__pycache__ 目录数为 0』只在清场那一刻成立：任何 pytest 运行都会按设计重建缓存。因此额外引入一个与运行顺序无关的判据 —— 把每个缓存 .pyc 与其源码的新鲜编译逐一比对 —— 作为『树上没有幽灵字节码』的终态证据"
  - "合成模块的源码在第一次编译前先把 mtime 回拨一小时：pyc header 只记录整秒 mtime，同一秒内写入-投毒-touch 会让 touch 这条补救静默失效（实测过一次红）"
  - "`tests/conftest.py` 只加散文、不加任何运行时守卫 —— 没有便宜且无误报的检测手段，半吊子守卫只会制造新的假信号"
  - "不在新文件与 conftest 里写出那两个真实错字拼写：验收判据是 `git grep` 零命中，一次散文提及与一次真实残留在 grep 面前无法区分"

patterns-established:
  - "字节码新鲜度取证：读 pyc 前 16 字节 header（magic/flags/mtime/size）与源码 stat 对照"
  - "marshal 字节相等只能当快路径：重复对象的反向引用编码会让结构相同的 code object 产出不同字节，必须有结构化比较兜底"

requirements-completed: [DATA-03]

coverage:
  - id: D1
    description: "全树字节码缓存清场，默认缓存目录下四个 gap-closure suite 回到 98 passed"
    requirement: "DATA-03"
    verification:
      - kind: other
        ref: "find . -path ./.venv -prune -o -name '__pycache__' -type d -print | wc -l  → 17 → 0"
        status: pass
      - kind: integration
        ref: "uv run pytest -q tests/test_crsp_ticker_sidecar.py tests/test_model_predict_panel.py tests/test_crsp_identity.py tests/test_extensibility_contract.py → 98 passed（不含 pycache_prefix，跑了两次）"
        status: pass
      - kind: integration
        ref: "uv run pytest -q tests/test_crsp_ticker_sidecar.py → 26 passed（此前 5 failed, 21 passed）"
        status: pass
    human_judgment: false
  - id: D2
    description: "「等长注入 + 恢复 mtime ⇒ .pyc 不失效」以及两条补救，落成可执行证明"
    requirement: "DATA-03"
    verification:
      - kind: unit
        ref: "tests/test_stale_bytecode_lesson.py#test_equal_length_edit_with_restored_mtime_keeps_the_old_bytecode_live"
        status: pass
      - kind: unit
        ref: "tests/test_stale_bytecode_lesson.py#test_touching_the_source_invalidates_the_stale_bytecode"
        status: pass
      - kind: unit
        ref: "tests/test_stale_bytecode_lesson.py#test_a_clean_pycache_prefix_also_bypasses_the_stale_bytecode"
        status: pass
      - kind: other
        ref: "mutation: 删掉 fixture 的 os.utime 恢复行 → 3 errors；连同 fixture 自检一并删掉 → Test 1 读到 'BBBB' 而变红"
        status: pass
    human_judgment: false
  - id: D3
    description: "conftest 里与 OpenMP 守卫并列的散文教训，且没有把守卫挤出第一段可执行代码的位置"
    requirement: "DATA-03"
    verification:
      - kind: unit
        ref: "tests/test_macos_openmp_guard.py（7 passed，含 test_guard_is_the_first_executable_code_in_conftest）"
        status: pass
      - kind: other
        ref: "ast.dump(HEAD:tests/conftest.py) == ast.dump(工作树 conftest.py) → True（改动纯注释）"
        status: pass
    human_judgment: false
  - id: D4
    description: "清缓存后的完整回归门基线，FAILED 逐文件分布与 D-03.11-12-A 一致"
    requirement: "DATA-03"
    verification:
      - kind: integration
        ref: "uv run pytest -q --ignore=tests/test_factor_hierarchy.py --ignore=tests/test_crsp_rebuild_measurements.py → 55 failed, 1679 passed, 1 skipped in 302s"
        status: pass
    human_judgment: false

duration: 15 min
completed: 2026-09-21
status: complete
---

# Phase 03.11 Plan 14: 清除幽灵字节码并把这条教训锁成测试 Summary

**全树 `__pycache__` 清场（17→0）让 `tests/test_crsp_ticker_sidecar.py` 从 `5 failed, 21 passed` 回到 `26 passed`、四个 gap-closure suite 在默认缓存目录下回到 `98 passed`；「等长编辑 + 恢复 mtime ⇒ .pyc 不失效」以三条子进程用例和一段与 macOS OpenMP 守卫并列的 conftest 注释同时留在了代码里。**

## Performance

- **Duration:** 15 min
- **Started:** 2026-09-21T22:33:02Z
- **Completed:** 2026-09-21T22:48:00Z
- **Tasks:** 3
- **Files modified:** 2（1 新建 + 1 修改）

## 执行位置（本 plan 刻意不隔离）

`git rev-parse --show-toplevel` → **`/Users/daizhaorong/projects/quantlab`**，`.git` 是目录而非文件，
即主工作树而非 worktree。这是本 plan 的前提：`__pycache__` 按设计 gitignored，隔离 worktree 里
根本不存在那份投毒字节码，在那里跑出来的 98 passed 是「trivially green」，清的也不是执行代码所在的那棵树。
Task 1 的 `<precondition>` 就是这一条，已满足。

## Accomplishments

### 1. 清场前的直接取证（G-03.11-4 的机理，实测而非引述）

清场**之前**读 `quantlab/dataset/__pycache__/crsp_tickers.cpython-313.pyc` 的 16 字节 header：

```
pyc records source mtime: 1790024442 size: 14427
actual source  mtime: 1790024442 size: 14427   -> MATCH -> CPython 判为「新鲜」
  投毒 token b'tikcer' 出现在字节码中: True
  投毒 token b'paylaod' 出现在字节码中: False
源码含 'tikcer' / 'paylaod': False / False
```

与 03.11-VERIFICATION.md:136-143 记录的数字逐字相同。`tikcer` 只存在于**派生产物**里，源码是干净的 ——
这正是 `git status` 干净、按内容 grep 零命中却仍然红的原因。（`paylaod` 那次注入没有留在这份 .pyc 里，
说明它在写出这份缓存之前就已经被重新编译覆盖过；本 plan 只记录观察，不做推断。）

### 2. 清场与两组基线（均**不含** `pycache_prefix`）

| 度量 | 值 |
|---|---|
| 清场前 `__pycache__` 目录数 | **17** |
| 清场后 `__pycache__` 目录数 | **0** |
| `.venv` 内 `__pycache__` 目录数（prune 生效的证据） | 897（一个都没动） |
| 四个 gap-closure suite（默认缓存目录） | **98 passed** in 18.22s |
| 四个 suite 二次复跑（清场后重来一遍） | **98 passed** in 14.46s |
| `tests/test_crsp_ticker_sidecar.py` 单跑 | **26 passed**（VERIFICATION 记录此前为 `5 failed, 21 passed`） |
| `git grep -n -e 'tikcer' -e 'paylaod' -- quantlab tests` | 无匹配（exit 1） |

清场命令逐字使用 VERIFICATION 点名的那一条，`-path ./.venv -prune` 段落原样保留：

```
find . -path ./.venv -prune -o -name '__pycache__' -type d -print0 | xargs -0 rm -rf
```

### 3. 终态证据：一个与运行顺序无关的「无幽灵」判据

「清场后目录数为 0」只在清场那一刻成立 —— 之后每一次 pytest 都会按设计重建缓存，
所以把它当终态断言会自相矛盾（plan 的 verify 第一条要跑 pytest，第二条要求计数为 0）。
本 plan 因此另取了一个不依赖运行顺序的判据：**把树上每个缓存 .pyc 与其源码的新鲜编译逐一比对**
（用 pyc 内 `co_filename` 重新 compile，flags 与 CPython `source_to_code` 一致）。
回归门跑完后的终态：

```
checked 181 cached .pyc files against a fresh compile of their source
  96 pytest-assertion-rewritten (expected to differ)
RESULT: clean -- no cached .pyc disagrees with its source
```

这个工具写对之前误报过两次，两次都查到根因后才被采信（见「Issues Encountered」）。它是取证脚本，
留在 scratchpad，未进仓库。

### 4. `tests/test_stale_bytecode_lesson.py` —— 三条可执行用例

| 测试函数 | 锁住什么 |
|---|---|
| `test_equal_length_edit_with_restored_mtime_keeps_the_old_bytecode_live` | **陷阱本身**：源码已是 `BBBB`，全新子解释器仍然读到 `AAAA` |
| `test_touching_the_source_invalidates_the_stale_bytecode` | **补救一**：`os.utime(path, None)`（即 `touch`）之后读到 `BBBB` |
| `test_a_clean_pycache_prefix_also_bypasses_the_stale_bytecode` | **补救二**：`-X pycache_prefix=<干净目录>` 之后读到 `BBBB`，且默认缓存仍然留在盘上（它是取证手段，不是修复） |

- 三条都在**独立子进程**里取证（`subprocess.run([sys.executable, ...], cwd=tmp_path)`，各带 60s timeout）；
  同解释器内 `importlib.reload` / 再 import 都不走 `.pyc` 失效判据，会把这个陷阱整个测没 —— 这一点写进了模块 docstring。
- 子进程环境剔除 `PYTHONDONTWRITEBYTECODE` / `PYTHONSAFEPATH` / `PYTHONPYCACHEPREFIX`，
  否则机制本身会被外部环境静默关掉。
- 断言消息一律带上 stdout/stderr：一条只说「预期 BBBB 得到 AAAA」的失败，读者无从判断是陷阱重现了还是子进程根本没起来。
- 用中性常量 `"AAAA"` / `"BBBB"` 承载等长性质，**不写出那两个真实错字**（理由写在 docstring 里，句式照抄
  `tests/test_crsp_rebuild_measurements.py:141-143` 对 skip 标记的处理）。

**Mutation 验证（acceptance criterion 要求手工做一次）：**

| Mutation | 结果 |
|---|---|
| 删掉 fixture 里的 `os.utime(module, ns=(before.st_atime_ns, before.st_mtime_ns))` | **3 errors** —— fixture 自己的 `restored.st_mtime_ns == before.st_mtime_ns` 先炸 |
| 同时删掉那一行**和** fixture 的两条自检 | **1 failed, 2 passed**，且失败消息逐字是 `expected the STALE value 'AAAA' (the trap firing) but got 'BBBB'` —— 正是 plan 预言的那一条 |

两次 mutation 都从 scratchpad 的副本原样恢复（**没有用 `git stash`**），恢复后 3 passed。

### 5. `tests/conftest.py` 的散文教训

在 macOS OpenMP 守卫注释**之后**、`import base64` **之前**插入 16 行注释块（OpenMP 那段是 16 行，未超过它）：
症状（运行结果与你正在读的源码不一致，而 `git status` 干净、按内容 grep 零命中）→ 根因（失效判据只有
整秒 mtime + size 两项）→ 实测数字（`5 failed / 21 passed` vs `98 passed`）→ 两条安全做法 → 点名
`tests/test_stale_bytecode_lesson.py` 是三件事的可执行证明。**没有加任何运行时守卫**。

改动是纯注释，机械可证：`ast.dump(parse(HEAD:tests/conftest.py)) == ast.dump(parse(工作树))` 为 `True`。
`os.environ.setdefault("OMP_NUM_THREADS", "1")` 仍在第 41 行，`import base64` 在第 59 行，
`tests/test_macos_openmp_guard.py` 全绿（含按 AST 断言守卫是第一段可执行代码的那条）。

### 6. 清缓存后的回归门基线

```
uv run pytest -q --ignore=tests/test_factor_hierarchy.py --ignore=tests/test_crsp_rebuild_measurements.py
-> 55 failed, 1679 passed, 1 skipped, 1306 warnings in 302.03s (0:05:02)
```

（运行前又清了一次缓存，计数确认为 0 后才起跑。未出现 D-03.11-12-B 的 KunQuant teardown 挂起。）

**逐文件对照 deferred-items.md D-03.11-12-A：**

| Test file | D-03.11-12-A 记录 | 本次实测 | 一致 |
|---|---|---|---|
| `tests/test_ingest_shells.py` | 14 | 14 | ✓ |
| `tests/test_ingest_tiingo_universe_wiring.py` | 11 | 11 | ✓ |
| `tests/test_data_dir_cli.py` | 10 | 10 | ✓ |
| `tests/test_ingest_conversion_gate.py` | 6 | 6 | ✓ |
| `tests/test_volume_guard.py` | 5 | 5 | ✓ |
| `tests/test_factor_kunquant.py` | 3 | 3 | ✓ |
| `tests/test_chunked_ingest.py` | 2 | 2 | ✓ |
| `tests/test_spot_dataset.py` | 2 | 2 | ✓ |
| `tests/test_entry_point_contracts.py` | 1 | 1 | ✓ |
| `tests/test_universe.py` | 1 | 1 | ✓ |
| **合计** | **55** | **55** | ✓ |

**passed 从 1675 增至 1679（+4），全部可归因，无一来自本相位代码：** 本 plan 新增 3 条
（`test_stale_bytecode_lesson.py`），plan 03.11-13 的 `1f22652 test(03.11-13): assert _permno_breakdown
records carry no symbol field` 新增 1 条。

## Task Commits

1. **Task 1: 清空字节码缓存并重取基线** — 无提交（纯环境动作 + 证据采集；`__pycache__` 按设计 gitignored，
   plan 的 `<files>` 也写明「无源码改动」）
2. **Task 2: `tests/test_stale_bytecode_lesson.py`** — `709eceb` (test)
3. **Task 3: `tests/conftest.py` 散文教训 + 回归门基线** — `597ac7d` (docs)

**Plan metadata:** 见下方 docs 提交。

## Files Created/Modified

- `tests/test_stale_bytecode_lesson.py`（新建，224 行）— 三条子进程用例，把 mtime+size 失效判据及两条补救锁成回归
- `tests/conftest.py`（+16 行注释）— 与 macOS OpenMP 守卫并列的字节码陷阱说明，点名上面那个文件

## Decisions Made

1. **终态判据改为「每个 .pyc 对上它的源码」，而非「`__pycache__` 目录数为 0」。** 后者与 plan 自己的
   verify 顺序矛盾（第一条要跑 pytest，跑完必然重建缓存）。目录计数仍作为「清场确实发生了」的证据记录
   （17→0，两次），终态则由 181 个缓存文件逐一比对给出。
2. **合成模块在第一次编译前把 mtime 回拨一小时。** pyc header 只记整秒 mtime，写入-投毒-touch 若发生在
   同一秒内，`touch` 这条补救会静默失效 —— 这是实测出来的（Test 2 第一版真的红了），不是预防性猜测。
   这一行为本身也写进了注释，因为它是判据粒度的直接后果。
3. **conftest 只加散文，不加运行时守卫。** 没有便宜且无误报的检测手段；半吊子守卫只会制造新的假信号，
   而假信号正是这个 gap 的本体。
4. **不写出那两个真实错字。** 验收判据是 `git grep` 零命中，散文提及与真实残留在 grep 面前不可区分。

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] 新测试的 Test 2 因 mtime 整秒粒度而必然红**
- **Found during:** Task 2（第一次运行新文件）
- **Issue:** fixture 把源码 mtime 恢复成「刚写入时」的值，而 `touch` 把它推到「现在」；两者在同一整秒内，
  pyc header 只记整秒，于是 `touch` 之后缓存仍被判新鲜，Test 2 读到 `AAAA` 而失败
  （`expected 'BBBB' after touching the source, got 'AAAA'`）。
- **Fix:** 第一次编译前先 `os.utime(module, ns=(now-3600s, now-3600s))` 把源码「做旧」一小时，
  让后续每一次比较都无歧义；并把这条理由写进注释。
- **Files modified:** `tests/test_stale_bytecode_lesson.py`
- **Verification:** 3 passed；随后两次 mutation 均按预期变红。
- **Committed in:** `709eceb`

---

**Total deviations:** 1 auto-fixed（1 bug）
**Impact on plan:** 修的是新写测试自身的定时脆弱性，不涉及任何既有代码；无 scope creep。

## Issues Encountered

**1. 取证脚本两次误报，都查到根因后才采信（未写进仓库）**
- 第一版用 `marshal.dumps(compile(src, str(path)))` 与缓存比对，报告 65 个文件「全是幽灵」——
  根因是 `co_filename` 被编进 code object，我传的绝对路径与缓存记录的不一致。
- 第二版修好后仍报 6 个 test 文件不一致 —— 根因是 **pytest 的断言改写器**：它重写测试模块的
  `assert` 语句并把结果写进 `__pycache__`，与普通 compile 理应不同。加入 `@pytest_ar` 识别后分类为「预期差异」。
- 第三版在回归门之后报 `quantlab/utils/cli.py` 一个幽灵 —— 逐属性拆开比对后确认是
  **marshal 的反向引用编码**：结构完全相同的 code object 可以序列化成不同字节。改为「字节相等当快路径，
  不等则走结构化比较」后，终态干净。
- 这三次都没有被当成结论呈报。记下来是因为它和本 gap 同类：一个未经核对的测量工具，与一份未经核对的
  字节码缓存，产生的是同一种伤害。

**2. deferred-items.md D-03.11-12-A 的**散文**比它的表更绝对（未修改，仅记录）**
- 该条写「Every one of the 55 is a `ModuleNotFoundError: No module named 'ingest_tiingo'` /
  `'ingest_binance_spot'`, or the `FileNotFoundError` twin」。逐条拆开本次 38 个失败段落后，
  55 条里确实绝大多数如此，但另有几类：`refresh_us_equity_universe` / `ingest_us_equity` / `ingest_alpaca`
  的同类 ModuleNotFoundError、三条 `tests/test_factor_kunquant.py` 的 `KeyError: 'amount'`
  （`StockDataset._to_kunquant` 的 amount 合成），以及若干把同一缺失表达成 assert 的条目。
- **逐文件计数与该表逐行一致（表见上）**，且本 plan 的改动（一个无人 import 的新测试文件 + 一段
  AST 可证的纯注释）在物理上都够不到这些代码，因此判定全部为前置失败、无新增。
- **未改动 `deferred-items.md`** —— 它不在本 plan 的 `files_modified` 内。建议由编排器决定是否单立一条
  修正该散文的措辞，使其与自己的表一致。

**3. must_haves 的 idempotency backstop 需要按字面澄清**
- 原文说「再跑一次清缓存命令是无害的空操作（第二次 find 命中 0 个目录）」。只在两次清场之间**没有任何
  import 活动**时成立；本 plan 第二次清场前跑过两轮 pytest，因此命中 11 个目录（随后归 0）。
- 清场本身确实幂等且无害；「第二次命中 0」这句需要加上「中间没有跑过任何 Python」的前提。

## User Setup Required

None - 无外部服务配置。

## Next Phase Readiness

- **G-03.11-4 关闭。** 本工作树此后取得的测试结果反映 HEAD：181 个缓存 .pyc 无一与其源码不一致。
- **Wave 1/2/3 的 `<precondition>` 已满足**：plans 15-18 可以把自己的 `<verify>` 结果当作证据。
  两组可引用的基线：四 suite `98 passed`（默认缓存目录）、回归门 `55 failed, 1679 passed, 1 skipped`。
- **提醒后续 plan**：若在回归门里看到与上表不同的 FAILED 分布，先确认是不是自己的改动造成的，
  而不是先怀疑缓存 —— 缓存这一条已经有可执行证明和散文两道记录。

---
*Phase: 03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r*
*Completed: 2026-09-21*
