---
phase: 03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r
plan: 08
subsystem: database
tags: [crsp, permno, config, guard-rails, layering, pytest, tdd]

requires:
  - phase: 03.11-03
    provides: "PERMNO 轴落地（D-01），并把 5 条 ticker 轴断言留给本 plan 收尾"
  - phase: 03.11-05
    provides: "成分股面板也在 PERMNO 轴上，membership 断言可按 PERMNO 写"
  - phase: 03.11-07
    provides: "身份解析层清空；零残留 gate，以及把本 plan 的删除清单托管在 strict xfail 分组里"
provides:
  - "CrspDatasetConfig 少两个字段：symbol_overrides 与 nan_adj_at_permno_seam 直接删，零迁移零兼容层（D-04）"
  - "collision_universe -> roster_universe（D-17）：字段本身保留，名字对齐它唯一还活着的职责与报告键 roster_overrides"
  - "两个同名不同类的 symbols 字段各有一条赋值期拒绝，文案同形并点名 permnos"
  - "BaseDataset.REJECTED_FACTOR_CONFIG_FIELDS —— 供应商声明、基类只读的拒绝钩子，方向仍是 dataset -> base"
  - "零残留 gate 合并成一份清单：strict xfail 交棒分组已兑现并删除"
  - "全仓回归 55 failed，与 orchestrator 独立测得的 55 条长期既有失败逐文件逐条一致，零新增"
affects: [03.11-09, 03.11-10, 03.11-11]

actuals:
  tokens: 15000
  tasks: 3
  commits: 5
  plan_head_before: 02e3cfb8fb098efc2d8abc7dff763dcc0ef7fbb8

tech-stack:
  added: []
  patterns:
    - "同一条纪律要装在两个地方时，两条错误文案写成同形：读起来像一条纪律的两个安装点，而不是两个临时补丁"
    - "跨层拒绝用「供应商声明 + 基类只读」的类属性钩子表达，而不是基类里 isinstance 一个具体子类——分层方向不反转，且有既有 purity gate 机器校验"
    - "删除字段后，连解释这次删除的 docstring 也不能在 quantlab/ 里叫出死者的名字：零残留 gate 计入注释，解释的位置是 example/ 与 SUMMARY"
    - "删掉一个字段前先问它现有的测试测的是「字段」还是「缺陷类」：WR-07 测的是缺陷类，所以迁移到幸存字段而不是随字段一起删"

key-files:
  created: []
  modified:
    - quantlab/base/config.py
    - quantlab/base/data.py
    - quantlab/base/factor.py
    - quantlab/dataset/crsp.py
    - scripts/ingest_wrds_crsp.py
    - tests/test_crsp_identity.py
    - tests/test_ingest_wrds_crsp.py
    - tests/test_no_identity_residue.py
    - tests/test_crsp_rebuild.py
    - tests/test_crsp_rebuild_measurements.py

key-decisions:
  - "因子层的拒绝选了计划有序三选一中的第 2 条（在基类上新开一个最小钩子）：第 1 条不存在——base/data.py 与 base/factor.py 里没有任何「子类声明自己不接受某字段」的既有机制；第 3 条（延迟 import + isinstance）会把 base -> dataset 的已知偏离从一个存储后端加深到一个具体 vendor Dataset 子类"
  - "钩子做成 {字段: 为什么} 的映射而不是裸字段集合：拒绝的理由属于供应商，基类只负责拼装与抛出，这样 base/factor.py 一个 vendor 类名都不用出现"
  - "symbols 的拒绝条件是 is not None 而不是真值：() 与 ('AAPL',) 同样被拒。这刻意不同于 permnos 的 WR-01——那里空元组的两义性本身是缺陷，这里整个字段在本 vendor 上就是错的，装什么都一样"
  - "roster_universe 与 qqq_benchmark 的新 docstring 都不叫出被删字段的旧名字：零残留 gate 计入注释，而解释一次删除的位置是 example/（plan 10）与本 SUMMARY"
  - "WR-07 的轴钉测试迁移到 config.permnos 而不是删除：它测的是「symbol 轴过滤了但 timestamp 轴取自未过滤帧」这个缺陷类，缺陷类比字段活得久"

patterns-established:
  - "跨层守卫的方向检查交给既有 purity gate：tests/test_extensibility_contract.py 扫 base/factor.py 的 vendor 类名，且把 docstring 行算作实引用——写 docstring 时也得守分层"
  - "TDD 红证据在 pytest 项目里按 operator P9 投影成 node TAP 后由 gsd-tools 判定，投影的是真实运行的逐条结果"

requirements-completed: [DATA-03]

coverage:
  - id: D1
    description: "symbol_overrides 与 nan_adj_at_permno_seam 两个字段删净，没有迁移也没有兼容层（D-04）；QQQ 的 permnos=(QQQ_PERMNO,) 与 security_filter='none' 完好"
    requirement: DATA-03
    verification:
      - kind: other
        ref: "grep -rn 'symbol_overrides|nan_adj_at_permno_seam' quantlab/ scripts/ -> 零命中；dataclasses.fields(CrspDatasetConfig) 断言两者不在、symbols/permnos 仍在"
        status: pass
      - kind: unit
        ref: "tests/test_no_identity_residue.py#test_the_deleted_identity_machinery_leaves_no_residue_in_quantlab"
        status: pass
      - kind: integration
        ref: "tests/test_crsp_identity.py#test_the_qqq_benchmark_store_is_one_symbol_across_the_qqqq_years"
        status: pass
    human_judgment: false
  - id: D2
    description: "collision_universe -> roster_universe 改名到位，字段本身与 GAP-C 名册覆盖职责完好（D-17 / operator P8）"
    requirement: DATA-03
    verification:
      - kind: other
        ref: "grep -rn 'collision_universe' quantlab/ scripts/ tests/ -> 零命中；grep -c roster_universe quantlab/dataset/crsp.py == 11"
        status: pass
      - kind: integration
        ref: "tests/test_crsp_identity.py 的五条 GAP-C roster-override 测试全绿（test_a_member_is_not_dropped_by_the_filter_during_its_spell 等）"
        status: pass
    human_judgment: false
  - id: D3
    description: "CrspStockDataset 的 config setter 在赋值期拒绝非 None 的 symbols，文案点名 permnos 并回显被拒的值；crsp.py 里 config.symbols 的两个读者删净"
    requirement: DATA-03
    verification:
      - kind: unit
        ref: "tests/test_crsp_identity.py#test_a_ticker_roster_in_config_symbols_is_refused_at_assignment"
        status: pass
      - kind: unit
        ref: "tests/test_crsp_identity.py#test_an_empty_config_symbols_tuple_is_refused_too"
        status: pass
      - kind: unit
        ref: "tests/test_crsp_identity.py#test_crsp_has_no_reader_of_config_symbols"
        status: pass
      - kind: unit
        ref: "tests/test_crsp_identity.py#test_config_symbols_none_is_the_only_accepted_spelling"
        status: pass
    human_judgment: false
  - id: D4
    description: "因子层的 BaseFactorConfig.symbols 在 CRSP 背景下于因子 config 赋值期被拒，且发生在 _auto_filter 可达之前（T-03.11-25）"
    requirement: DATA-03
    verification:
      - kind: unit
        ref: "tests/test_crsp_identity.py#test_a_crsp_backed_factor_refuses_a_ticker_roster_at_assignment"
        status: pass
      - kind: unit
        ref: "tests/test_crsp_identity.py#test_the_factor_refusal_fires_before_auto_filter_is_reachable"
        status: pass
      - kind: unit
        ref: "tests/test_crsp_identity.py#test_a_crsp_backed_factor_with_no_roster_is_unchanged"
        status: pass
    human_judgment: false
  - id: D5
    description: "基类 BaseDatasetConfig.symbols、symbol 维度名、XrBackend.filter_by_symbol 三者 diff 为空；非 CRSP vendor 的行为一个字未变（T-03.11-26 / RULING 3）"
    requirement: DATA-03
    verification:
      - kind: unit
        ref: "tests/test_crsp_identity.py#test_a_non_crsp_dataset_still_accepts_config_symbols（Tiingo control arm）"
        status: pass
      - kind: unit
        ref: "tests/test_crsp_identity.py#test_a_non_crsp_backed_factor_still_accepts_a_ticker_roster（因子层 control arm）"
        status: pass
      - kind: other
        ref: "git diff 02e3cfb..HEAD 里 quantlab/dataset/backend.py 零改动；'symbols: tuple | None = None' 与 filter_by_symbol 的裸 .sel 各 grep -c == 1"
        status: pass
    human_judgment: false
  - id: D6
    description: "因子层的拒绝没有反转分层：base/factor.py 没有 quantlab.dataset.crsp 的 import，也没有出现任何具体 Dataset 子类名"
    requirement: DATA-03
    verification:
      - kind: unit
        ref: "tests/test_crsp_identity.py#test_the_factor_base_does_not_import_the_crsp_module"
        status: pass
      - kind: unit
        ref: "tests/test_extensibility_contract.py#test_core_layer_purity_no_market_specific_logic（既有 gate，实测抓到过一次违规，见 Deviation 5）"
        status: pass
    human_judgment: false
  - id: D7
    description: "零残留 gate 的两个分组合并成一条正式断言，strict xfail 交棒兑现并删除；gate 对新晋名字实测会红"
    requirement: DATA-03
    verification:
      - kind: unit
        ref: "tests/test_no_identity_residue.py#test_the_deleted_identity_machinery_leaves_no_residue_in_quantlab；grep -c xfail == 0"
        status: pass
      - kind: other
        ref: "注入 `# TEMPORARY RESIDUE PROBE: symbol_overrides / nan_adj_at_permno_seam` 到 crsp.py 末尾 -> gate 在自己的断言上变红并逐字打印 quantlab/dataset/crsp.py:1665；撤销后复绿（未使用 git stash）"
        status: pass
    human_judgment: false
  - id: D8
    description: "全仓回归相对基线零新增失败：55 failed，失败集合逐文件与基线的 55 条长期既有失败一致"
    requirement: DATA-03
    verification:
      - kind: other
        ref: "uv run pytest -q --ignore=test_factor_hierarchy --ignore=test_crsp_rebuild_measurements -p no:cacheprovider -> 55 failed / 1641 passed / 1 skipped（基线 60/1627/1/1，交棒的 5 条全部转绿）"
        status: pass
    human_judgment: true
    rationale: "failed 数与失败集合逐条对得上（55 = 基线 60 − 交棒的 5），这是决定性的量。但 collected 总数比基线算术预期少 1（1697 vs 1698），未能归因，与 03.11-07 记录的同向 ±1 摆动一致。口径上这 1 个差额要人来判断能否接受为「零新增失败」的证据。"
  - id: D9
    description: "两条拒绝的文案同形且对下一个读者有用；五处重写的 docstring 保住了本仓库「结论 + 为什么 + 被否决的替代」的密度体例"
    requirement: DATA-03
    verification: []
    human_judgment: true
    rationale: "grep 能证明旧文案消失、新文案点名了 permnos，不能证明两段文案读起来真的像同一条纪律，也不能证明重写后的 permnos / roster_universe / FILTERABLE_COLUMNS / _raw_axes_in_range 四处对下一个读者有用。这是人的判断。"

duration: 33min
completed: 2026-09-21
status: complete
---

# Phase 03.11 Plan 08: 收敛配置层 Summary

**删掉两个只为 ticker 轴存在的配置字段、把一名两责的 `collision_universe` 改名成 `roster_universe`，并把「`config.symbols` 在 CRSP 面板上意味着什么」定成两条赋值期拒绝——装在两个同名不同类的字段上，其中因子层那条用「供应商声明、基类只读」的钩子实现，一个 vendor 类名都没进 `base/`。**

## Performance

- **Duration:** 33 min
- **Started:** 2026-09-21T06:09:06Z
- **Completed:** 2026-09-21T06:41:41Z
- **Tasks:** 3
- **Files modified:** 10

## Accomplishments

- **两个字段整删，一个字段改名，零迁移零兼容层。** `symbol_overrides` 与 `nan_adj_at_permno_seam` 守的两个失败形态在 PERMNO 轴上**不可表达**——一个标的在整段历史里就是一列，没有哪一列会中途换公司——所以删的是机器本身，不是给它加守卫。`collision_universe` 的两个职责里死掉的是撞车 tie-break（03.11-07 已删其机器），活着的是显式名册覆盖 `security_filter`（GAP-C / operator P8），于是名字改成 `roster_universe`，和它喂的报告键 `roster_overrides` 对齐。旧 `config.json` 会在 `cls(config_cls(**config))` 处直接 `TypeError`——D-04 **预先**接受过这个后果。

- **一条纪律，两个安装点，文案同形。** `BaseDatasetConfig.symbols` 与 `BaseFactorConfig.symbols` 是**两个不同的字段**，在流水线的两个阶段过滤。前者的读者在 `crsp.py`（已删净两处），后者经 `_auto_filter` 走到 `XrBackend.filter_by_symbol` 的一行裸 `.sel`——拿字符串去 `.sel` 一个 int64 轴，**跑到一半抛 `KeyError`，而且报错指着索引说话，真因却是调用者选错了字段**。现在两条拒绝都在 config 赋值期发生，都点名 `permnos`，都把被拒的值 `!r` 出来。

- **因子层那条没有反转分层。** 计划给了有序三选一，落在第 **2** 条（详见 Decisions 1）：在 `BaseDataset` 上开一个最小钩子 `REJECTED_FACTOR_CONFIG_FIELDS: dict[str, str]`，`CrspStockDataset` 覆盖它并**自己写拒绝的理由**，`base/factor.py` 只负责读声明、拼文案、抛错。方向仍是 dataset → base，`base/factor.py` 里一个具体 vendor 类名都没有——**而且这一条是机器校验的**：仓库里既有的 `tests/test_extensibility_contract.py` 会扫这三个核心模块的 vendor 类名，并且**把 docstring 行算作实引用**。它在全仓回归里抓到过我写的一处违规（Deviation 5）。

- **交棒兑现了，而且 gate 对新名字实测会红。** 03.11-07 把本 plan 的删除清单放进 `xfail(strict=True)` 分组，赌的就是「删了字段不来改清单 → 意外通过 → strict 判失败」。两个名字已提升进 `DELETED_IN_03_11_07`、分组删除、`grep -c xfail` 归零。随后往 `crsp.py` 注入一行同时提到两个名字的**纯注释**，gate 在自己的断言上变红并逐字打印 `quantlab/dataset/crsp.py:1665: # TEMPORARY RESIDUE PROBE: ...`，`git checkout` 撤销后复绿（**未使用 `git stash`**）。

- **交棒的 5 条失败全部转绿，全仓零新增失败。** `tests/test_ingest_wrds_crsp.py` 的 5 条里，3 条是 `['AAPL']`/`['QQQ']` 的轴断言，另外 2 条其实还卡在一个**计划没提到的**地方：它们断言脚本会打印 `.crsp_symbology_report.json` 这条 sidecar 路径，而 03.11-07 的 Deviation 1 已经把那三行 print 删了（见 Deviation 2）。全仓 **55 failed**，失败集合逐文件与 orchestrator 独立测得的 55 条长期既有失败一致。

## Task Commits

1. **Task 1: 删两个字段 + collision_universe 改名** — `dbe8ca6` (refactor)
2. **Task 2 RED: config.symbols 拒绝的失败测试** — `492910c` (test)
3. **Task 2 GREEN: CrspStockDataset 的赋值期拒绝 + 删两个读者** — `9cc8a7a` (feat)
4. **Task 3 RED: 因子层拒绝的失败测试** — `b7d7d10` (test)
5. **Task 3 GREEN: 声明钩子 + 基类只读的拒绝 + 残留清单合并** — `8c7c150` (feat)

**Plan metadata:** 见本文件的 docs commit。

`commits: 5` 是写 SUMMARY 时用 `git rev-list --count 02e3cfb..HEAD` **测**出来的（与 03.11-02 / 03.11-03 / 03.11-07 同一口径），不含随后那次 docs 提交。

## TDD Gate Compliance

| Task | RED | GREEN | REFACTOR | Status |
|---|---|---|---|---|
| 1（非 TDD） | — | `dbe8ca6` | — | n/a |
| 2（`tdd="true"`） | `492910c` | `9cc8a7a` | — | Pass |
| 3（`tdd="true"`） | `b7d7d10` | `8c7c150` | — | Pass |

两次 RED 都不是「非零退出即红」：目标测试各自在**自己的断言**上失败，并且每次都有 control arm 同时**按预期变绿**（`symbols=None` 不受影响、非 CRSP vendor 不受影响），证明红的是要改的那一条而不是环境。

本项目是 pytest，按 operator P9 把**真实运行的逐条结果**投影成 node TAP 后交给 `gsd_run check tdd-red-evidence` 判定（投影的是实跑输出，不是编造的）：

- Task 2：`5 tests / 2 pass / 3 fail`，target = `test_a_ticker_roster_in_config_symbols_is_refused_at_assignment`（`DID NOT RAISE ValueError`）→ **`RED_EVIDENCE_OK`**
- Task 3：`5 tests / 3 pass / 2 fail`，target = `test_a_crsp_backed_factor_refuses_a_ticker_roster_at_assignment`（`DID NOT RAISE ValueError`）→ **`RED_EVIDENCE_OK`**

两次都不是 collection error、不是 fixture 崩溃、不是零发现（#3770）。REFACTOR 阶段没有产生改动，所以没有第三个提交——守卫本身已经是最小实现。

## Files Created/Modified

- `quantlab/base/config.py` — 删 `symbol_overrides`（字段 + `#:` 注释）与 `nan_adj_at_permno_seam`；`collision_universe` → `roster_universe` 并**整段 docstring 重写**（旧文案逐字论证「名字比职责窄但不改，因为盘上 config.json 按键回读」——D-04 已消灭那个成本，而且第一个职责已不存在）；`qqq_benchmark` 去掉 `symbol_overrides=` 那一项，docstring 从「三个设置」改成「两个设置 + 第三个为什么被删」；`permnos` 的 docstring 里那句「the inherited `symbols` is the TICKER-side one, applied after symbology」改写成一整段「`symbols` 在本 vendor 上**被拒绝**，以及为什么基类字段不能动」。
- `quantlab/base/data.py` — 新增 `BaseDataset.REJECTED_FACTOR_CONFIG_FIELDS: dict[str, str] = {}`（默认空）。`#:` 注释写明它是**声明**、读者是 `base/factor.py`、为什么方向必须是 dataset → base，以及为什么值是「理由句」而不是裸字段集合。
- `quantlab/base/factor.py` — 新增 `Factor._reject_declared_config_fields()`，在 config setter 里**第一件事**调用（早于 `_maybe_resolve_factor_names` 的探针读，所以拒绝路径一寸磁盘都不碰）。`_auto_filter` 一个字未改。docstring 里不出现任何 vendor 类名（见 Deviation 5）。
- `quantlab/dataset/crsp.py` — config setter 新增 `config.symbols is not None` 的拒绝；删 `_raw_axes_in_range` 的钉轴过滤与 `_raw_data_to_xr_window` 的 `elif self.config.symbols:` 窗口过滤；`collision_universe` 的 6 处调用点改名；新增 `REJECTED_FACTOR_CONFIG_FIELDS` 覆盖；重写四处已成假话的文案（`FILTERABLE_COLUMNS` 的「ticker 过滤器是 config.symbols」、`resolve_security_filter` 的错误文案、`_raw_axes_in_range` 的 docstring、`permnos` digit 校验的错误文案）。
- `scripts/ingest_wrds_crsp.py` — `collision_universe=args.universe` → `roster_universe=args.universe`。**CLI 的 `--universe` 参数名不改**，它对用户的语义一直是「名册」。
- `tests/test_crsp_identity.py`（1833 → 2103）— 新增 10 个测试（5 条数据集层 + 5 条因子层，各含 control arm）；WR-07 轴钉测试迁到 `permnos`（见 Deviation 3）；QQQ benchmark 测试的 docstring 与断言改成「一列跨越 QQQQ 年代」这个**结果**而不是那个已删字段；round-trip 测试去掉两个被删字段。
- `tests/test_ingest_wrds_crsp.py` — 交棒的 5 条：3 处轴断言改 PERMNO，1 处 sidecar 清单去掉 symbology 那条，1 处 membership 断言改 PERMNO。
- `tests/test_no_identity_residue.py`（158 → 148）— 两份清单合并成一份，strict xfail 测试删除，模块 docstring 交代「它曾经是两份清单、为什么、以及交棒兑现了」。
- `tests/test_crsp_rebuild.py` / `tests/test_crsp_rebuild_measurements.py` — 各 1 处改名（见 Deviation 1）。

## Decisions Made

1. **因子层拒绝的安装方式：选了计划有序三选一里的第 2 条。** 计划要求 SUMMARY 写明选了哪条、以及前面的为什么不可行——
   - **第 1 条（用既有钩子）不可行：不存在。** 读过 `base/data.py` 与 `base/factor.py` 后确认，仓库里有类属性声明的**体例**（`NEW_LISTING_STRATEGIES`、`config_cls`、`SUPERSEDED_SUFFIX`、`ADJUSTMENT_SIDECAR_SUFFIX`），但没有任何「子类声明自己不接受某个 config 字段」的机制。`grep -rn "REJECT|rejected|_unsupported|not_supported|SUPPORTED"` 在这三个文件里只命中一条无关的 `save(mode="a")` 错误文案。
   - **第 2 条（新开最小钩子）可行，已落地。** `BaseDataset.REJECTED_FACTOR_CONFIG_FIELDS`，默认空 dict，`CrspStockDataset` 覆盖成 `{"symbols": <理由句>}`。基类只做「读声明 → 拼文案 → 抛」。
   - **第 3 条（延迟 import + isinstance）没有用到，而且确实更差。** `base/factor.py:20` 本来就有 `from quantlab.dataset.backend import XrBackend`，所以这个文件对 `dataset/` 的单向性本来就是**近似的**；但「依赖一个存储后端」和「依赖一个具体 vendor Dataset 子类」不是一个量级的耦合，第 3 条是把一处已知偏离**再加深一层**。并且这一条不是口头纪律：`tests/test_extensibility_contract.py` 会扫 `base/factor.py` 里的 `StockDataset` 等子串，`isinstance(CrspStockDataset, ...)` 会直接把它打红。
   - 钩子的值做成 `{字段: 为什么}` 的**映射**而不是裸 `frozenset`：拒绝的理由属于供应商（「这个面板的轴是 int64 PERMNO，改用 `config.permnos`」），让供应商自己写，`base/factor.py` 就连 `permnos` 这个 CRSP 概念都只需要在 docstring 里举例提一次，代码里一次都不出现。

2. **`symbols` 的拒绝条件是 `is not None`，不是真值——`()` 与 `('AAPL',)` 同样被拒。** 这刻意**不同于** `permnos` 的 WR-01：那里空元组的两义性（「没有证券」/「所有证券」）本身就是缺陷，所以按名拒绝并把两个含义都说出来。这里是整个**字段**在本 vendor 上就是错的，装什么都一样，所以条件写成 `is not None`，理由写进了测试 `test_an_empty_config_symbols_tuple_is_refused_too` 的 docstring——免得后来者把两条拒绝读成同一个模式而去「统一」它们。

3. **两处新 docstring 都不叫出被删字段的旧名字。** `roster_universe` 的重命名说明与 `qqq_benchmark` 里对第三个设置的追述，最初都写了 `collision_universe` / `symbol_overrides` 的字面量——然后意识到：零残留 gate **计入注释**，而本 plan 正要把这两个名字提升进它的清单。按 03.11-07 定下的规矩「叫出死者名字的位置是 `.planning/`、`example/` 和 `tests/`，不是 shipping package」，两处都改写成描述性的说法（「它曾经还有第二个用途：…」「第三个设置曾经站在它们旁边：…」），并指向 `example/wrds_crsp.md`——那份文档的同步属于 plan 10。

4. **WR-07 的轴钉测试迁移而不是删除。** 见 Deviation 3。

5. **`crsp.py` 的 `permnos` digit 校验错误文案也改了。** 旧文案写着「The TICKER filter is `config.symbols`」——本 plan 之后这是一句**假话**（那个字段在本类上会抛）。改成「permnos selects the RAW tier, which keys on the PERMNO -- there is no ticker-side roster on this vendor to put a ticker in instead」。WR-01 的空元组拒绝与 digit 校验本身**一个字未动**。

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] 三个 `files_modified` 之外的测试文件持有 `collision_universe`**

- **Found during:** Task 1（改名前的全仓 grep）
- **Issue:** 计划 Task 1 的 `files_modified` 只列了 `tests/test_ingest_wrds_crsp.py`。实际 `tests/test_crsp_identity.py`（8 处）、`tests/test_crsp_rebuild.py`（1 处）、`tests/test_crsp_rebuild_measurements.py`（1 处）都按关键字传这个字段，改名不同步会让它们在构造 config 时 `TypeError`。
- **Fix:** 三个文件按词边界整体改名（`perl -pi -e 's/\bcollision_universe\b/roster_universe/g'`，**不是** `sed` 范围删除）。改前改后各 grep 一次计数核对：`collision_universe` 归零、`roster_universe` 分别为 9 / 1 / 1。
- **Files modified:** `tests/test_crsp_identity.py`, `tests/test_crsp_rebuild.py`, `tests/test_crsp_rebuild_measurements.py`
- **Verification:** `grep -rn "collision_universe" tests/` 归零；三文件门与全仓回归全绿
- **Committed in:** `dbe8ca6`

**2. [Rule 1 - Bug] 交棒的 5 条失败里有 2 条不是轴断言，是 03.11-07 删掉的那三行 print**

- **Found during:** Task 1（跑交棒的 5 条失败）
- **Issue:** prior-wave context 说这 5 条「断言 `['AAPL']` / `['QQQ']`，而轴现在是 PERMNO」。实际 `test_end_to_end_permnos_pull_and_convert_uses_one_connection` 与 `test_qqq_alone_...` 先卡在另一处：它们断言脚本打印的 sidecar 清单里有 `.crsp_symbology_report.json`，而 03.11-07 的 Deviation 1 已经把那三行 `print` 删了（符号学报告不再生成）。只改轴断言，这两条还是红的。
- **Fix:** 把 `.crsp_symbology_report.json` 从期望的 sidecar 清单里去掉，并在原地写明为什么（「列在这里就是断言一行没有代码能发出的输出」）。另两条 sidecar（调整锚、security filter）保留——它们指向的文件还在写。
- **Files modified:** `tests/test_ingest_wrds_crsp.py`
- **Verification:** `tests/test_ingest_wrds_crsp.py` 26 passed
- **Committed in:** `dbe8ca6`

**3. [Rule 1 - Bug] WR-07 的轴钉测试建立在「`config.symbols` 在 CRSP 上可用」之上**

- **Found during:** Task 2（写 RED 之前通读 `test_crsp_identity.py`）
- **Issue:** `test_a_symbol_restricted_conversion_pins_only_that_symbols_days` 用 `symbols=(AXIS_KEPT_PERMNO,)` 构造两次 config。Task 2 一装拒绝，这条测试就变成「构造即抛」。计划没有提到它。
- **Fix:** 迁移到 `config.permnos`，不删。判断依据是：它测的不是**字段**，是**缺陷类**——「symbol 轴按 roster 过滤了，timestamp 轴却取自未过滤的帧」，而 `_derivation()` 正是按 `config.permnos` 过滤的，所以缺陷类原样存在。docstring 里加了一段写明迁移和理由，免得后来者以为这条测试一直就是这么写的。
- **Files modified:** `tests/test_crsp_identity.py`
- **Verification:** 该测试在 GREEN 后仍绿（含它的 chunk-ledger 断言）
- **Committed in:** `492910c`（RED 提交，与新测试同批）

**4. [Rule 3 - Blocking] Task 3 的钩子必须落在 `base/data.py` 与 `crsp.py`，两者都不在 Task 3 的 `files_modified` 里**

- **Found during:** Task 3
- **Issue:** Task 3 的 `files_modified` 只有 `quantlab/base/factor.py` 与两个测试文件。但选项 2（计划自己列的路径）要求「在基类上开一个最小钩子，由 `CrspStockDataset` 覆盖」——基类在 `base/data.py`，覆盖在 `dataset/crsp.py`。
- **Fix:** 照做，两处各加一个类属性；`base/factor.py` 只加读取逻辑。这是计划自身指令的必然结果，不是范围蔓延——改动分别是 22 行与 23 行，且都是新增。
- **Files modified:** `quantlab/base/data.py`, `quantlab/dataset/crsp.py`
- **Verification:** 因子层 5 条测试全绿；`test_the_factor_base_does_not_import_the_crsp_module` 绿
- **Committed in:** `8c7c150`

**5. [Rule 1 - Bug] 我自己写的 `base/factor.py` docstring 违反了既有的核心层纯净度 gate**

- **Found during:** 全仓回归（Task 3 GREEN 之后）
- **Issue:** 第一次全仓跑出 **56 failed**，比目标多 1 条，新增的那条是 `tests/test_extensibility_contract.py::test_core_layer_purity_no_market_specific_logic`。这个既有 gate 扫 `base/factor.py` / `base/model.py` / `base/backend.py` 里的具体 `Dataset` 子类名，**只豁免以 `#` 开头的行**——docstring 行按实引用处理。我的 `_reject_declared_config_fields` docstring 里写了「The message SHAPE matches the dataset-side refusal in `CrspStockDataset`'s own config setter」，命中了 `StockDataset` 子串。
- **Fix:** 改写成不点名的说法（「a declaring vendor ... installs on its OWN config setter」），并在同一段 docstring 里写明**这条规则本身**由 `tests/test_extensibility_contract.py` 机器校验、且连 docstring 行都算——这正是本方法整个设计所围绕的那条规则，值得写在它旁边。
- **Files modified:** `quantlab/base/factor.py`
- **Verification:** `tests/test_extensibility_contract.py` 绿；全仓复跑 **55 failed**，与目标逐字一致
- **Committed in:** `8c7c150`（`--amend` 进 Task 3 的 GREEN 提交；该提交尚未推送，历史未被他人看到）

---

**Total deviations:** 5（3 blocking、2 bug）
**Impact on plan:** 五条都在计划自己的意图之内。1、2、4 是「不动就坏」的强制项；3 保住了一条本来会被字段带走的缺陷类覆盖；5 是我自己引入的违规，被仓库既有的 gate 当场抓住——这恰好证明了 Task 3 想守的那条分层规则是机器可验的，而不是口头纪律。删除面严格等于计划点名的清单：`BaseDatasetConfig.symbols`、`symbol` 维度名、`filter_by_symbol`、`_auto_filter`、`cleaning.py` 全部零改动。

## Issues Encountered

1. **`grep -c "permnos=(QQQ_PERMNO,)" quantlab/base/config.py` 的验收条件写的是 1，实测是 2——而基线也是 2。** 两处分别是 `qqq_benchmark` docstring 里的项目符号（`:204`）和真正的构造调用（`:235`）。用 `git show 02e3cfb:quantlab/base/config.py | grep -c` 核过基线同样是 2，所以这是验收条件写作时的一次误读，不是我改坏了什么。验收条件的**意图**——QQQ 的 PERMNO 名册保留——完好：构造调用与 docstring 都在。

2. **全仓 collected 总数比基线的算术预期少 1，未能归因，如实记录。** 基线（orchestrator 在 `02e3cfb` 上独立测得）`60 failed / 1627 passed / 1 skipped / 1 xfailed` = 1689 collected。本 plan 净增 9 个用例（新增 10、删除 1 个 strict-xfail 用例），预期 1698；实测 `55 failed / 1641 passed / 1 skipped / 0 xfailed` = **1697**，`--collect-only` 复核也是 1697。已核对：`test_crsp_identity.py` 29 → 39（+10，逐个 `def test_` 计数），`test_ingest_wrds_crsp.py` 26 → 26（未增未减），`test_no_identity_residue.py` −1。03.11-07 的 SUMMARY 记录过**同向的 ±1 摆动**且同样未能归因。**决定性的那个量——失败集合——是逐文件核对过的，完全一致**：`test_ingest_shells.py` 14、`test_ingest_tiingo_universe_wiring.py` 11、`test_data_dir_cli.py` 10、`test_ingest_conversion_gate.py` 6、`test_volume_guard.py` 5、`test_factor_kunquant.py` 3、`test_spot_dataset.py` 2、`test_chunked_ingest.py` 2、`test_universe.py` 1、`test_entry_point_contracts.py` 1 = 55，全部是长期既有失败（52 条 `ModuleNotFoundError`，`scripts/` 未进 pythonpath；3 条 `KeyError: 'amount'`），**本 plan 触碰的 10 个文件没有一个出现在失败集合里**。这条差异写在这里，不被抹平。

3. **Task 3 的验收条件之一要求 `tests/test_factor_kunquant.py` 全绿，但它带着 3 条长期既有失败。** 那 3 条全是 `StockDataset._to_kunquant` 里的 `KeyError: 'amount'`，属于 orchestrator 点名的 55 条既有失败之一，本 plan 一行相关代码都没碰。按 scope boundary 不修。`tests/test_factor_stream.py` 全绿，`test_no_identity_residue.py` 全绿，因子层未被殃及这个**意图**是满足的。

4. **本 plan 全程未读写主仓库 `/Users/daizhaorong/projects/quantlab/data`。** 残留探针的 A/B 用 `perl -ni` 删行还原并 `grep -c` 核验归零，**未使用 `git stash`**（`git stash list` 前后都只有那条 2026-09-12 的 `gsd-pre-phase-03.6`，不是本 agent 的）。全仓 pytest 跑之前 `ps` 确认过机器上没有其他 pytest 进程，跑完也没有留下后台进程。

## Known Stubs

None。本 plan 没有留下 stub，没有 `skip`，没有未跑的 `<verify>`，没有新增 `xfail`——恰恰相反，它**消灭**了仓库里唯一一个 `xfail`。

因此没有向 `.planning/WINDOWS.md` 追加任何条目：没有开着的缺陷可记。

## Threat Flags

None。计划 `<threat_model>` 的三条 `mitigate` 全部落地且各有断言：

| Threat | 落地位置 | 锁 |
|---|---|---|
| T-03.11-25（CRSP 背景因子设字符串 `symbols` → 跑到一半 KeyError） | 两个 config setter 各一条赋值期拒绝，文案同形并点名 `permnos`；`backend.py` 的裸 `.sel` 零改动 | 数据集层 4 条 + 因子层 3 条测试，含 `test_the_factor_refusal_fires_before_auto_filter_is_reachable` |
| T-03.11-26（误删基类 `BaseDatasetConfig.symbols`） | 基类字段与 `symbol` 维度名零改动 | 两条 control arm 测试 + `grep -c 'symbols: tuple \| None = None' == 1` + 字段集合断言 |
| T-03.11-27（误删 `collision_universe` 字段本身） | 只改名不删 | `grep -c roster_universe quantlab/dataset/crsp.py == 11`（要求 ≥ 3）+ 五条 GAP-C 测试全绿 |

T-03.11-28（旧 `config.json` 的 `TypeError`）是 `accept`：本 plan 确实让盘上已有的 `config.json` 在 `load_dataset_from_config` 处直接 `TypeError`，历史 backtest run 不可重放。D-04 **预先**接受过，且明确禁止写迁移层——本 plan 一行迁移代码都没写。

T-03.11-SC（包安装）：**本 plan 零安装**，`pyproject.toml` / `uv.lock` 均未改动。

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- **plan 09（ticker sidecar）不受影响，而且更干净了。** `CrspSymbology.symbol_intervals()` 是它唯一的数据源，本 plan 没碰 `crsp_symbology.py` 一个字。新增的好处是：`symbol_overrides` 已经不存在，所以 sidecar 不必再考虑「某个 PERMNO 的 ticker 被人钉过」这种情况——区间表就是 CRSP 自己说的话。QQQ 的 `QQQQ` 时期从此**只**以 sidecar 区间的形式存在，`qqq_benchmark` 的 docstring 里写明了这一点。
- **plan 10（文档同步）多了三项。** `example/wrds_crsp.md` 现在有 4 处指向已删/已改名的配置：`:99` 与 `:226` 讲 `nan_adj_at_permno_seam=False` 的 opt-out（机制已不存在）、`:235` 讲 `--universe` 对应 `CrspDatasetConfig.collision_universe`（应改成 `roster_universe`）、`:405` 讲 `symbol_overrides` 钉 QQQ（机制已不存在）。零残留 gate 刻意不扫 `example/`，正是为了让文档能写「这个机制在 03.11 被移除」——本 plan 按删除纪律没有越界去改它。**另外建议 plan 10 顺手在文档里写一句旧 `config.json` 的对照**：`quantlab/base/config.py` 的 `roster_universe` docstring 指向了 `example/wrds_crsp.md` 作为「手上还拿着旧 config 的人」的去处。
- **plan 11 之后，`config.symbols` 在 CRSP 路径上只剩一个语义：被拒绝。** 如果将来有人真的需要在因子层按 PERMNO 子集过滤 CRSP 面板，正确的做法不是放开这条拒绝，而是给 `BaseFactorConfig` 加一个类型明确的新字段——现在的拒绝文案已经把「用 `config.permnos` 限制转换」指出来了，那是当前唯一的路。
- **`REJECTED_FACTOR_CONFIG_FIELDS` 是可复用的。** 任何未来 vendor 只要在自己的 Dataset 子类上覆盖它，就能在因子 config 赋值期拒掉一个字段并给出自己的理由，`base/` 一行都不用改——这正是 DATA-03「新市场 = 新 Dataset 子类，核心层零改动」的契约。
- **环境状态：** 本 agent 的全仓 pytest 跑了三次（含 `--collect-only`），每次都是串行、前台、跑完即退；结束时 `ps` 无残留 pytest 进程。未触碰任何其他 agent 的进程。

---
*Phase: 03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r*
*Completed: 2026-09-21*

## Self-Check: PASSED

- `key-files.modified` 10/10 全部在盘上；SUMMARY 本身也在。
- 五个任务提交全部在 `git log 02e3cfb..HEAD` 里：`dbe8ca6` / `492910c` / `9cc8a7a` / `b7d7d10` / `8c7c150`；`commits: 5` 是用 `rev-list --count` **测**出来的，`plan_head_before` 同源。
- 三个任务的 `<acceptance_criteria>` 逐条复跑：Task 1 八条过、一条（`permnos=(QQQ_PERMNO,)` 计数）与基线同为 2 而非条件写的 1，见 Issues 1；Task 2 六条全过（`config.symbols` 行数 3 ≤ 3、`self.config.symbols` 为 0、基类字段逐字保留、`filter_by_symbol` 裸 `.sel` 逐字保留、`TICKER-side one` 归零、新增 5 个测试）；Task 3 八条全过（`permnos` 在 `factor.py` ≥ 1、无 `.crsp` 模块级 import、`xfail` 归零、CRSP + control arm 两条测试都在、安装方式已写明）。
- 计划级 `<verification>` 三条复跑：六文件门 **115 passed**；`grep -rn` 三个名字在 `quantlab/ scripts/` **零命中**；全仓门 **55 failed / 1641 passed / 1 skipped**，相对基线零新增失败（口径差 1 个 collected，见 Issues 2）。
- prohibitions 逐条核对：`BaseDatasetConfig.symbols`（`base/config.py:17`）不在 diff 里、`BaseFactorConfig.symbols` 字段仍在、`roster_universe` 字段未删（只改名）、`quantlab/dataset/backend.py` 整个文件零改动、`symbol` 维度名未改、零迁移/兼容层、`permnos` 的 WR-01 空元组拒绝仍在（`EMPTY tuple` ×1）、`cleaning.py` 未出现在 `git diff --name-only` 里。
- 构建门：`uv run python -m compileall -q quantlab scripts` 退出码 0。
