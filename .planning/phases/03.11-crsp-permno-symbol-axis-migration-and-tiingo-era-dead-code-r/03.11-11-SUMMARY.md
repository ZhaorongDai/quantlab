---
phase: 03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r
plan: 11
subsystem: infra
tags: [planning-records, decision-reversal, requirements-ledger, windows-ledger, backlog-todo, pytest-regression]

requires:
  - phase: 03.11-03
    provides: "TRACER —— 这个 phase 打通的那条 PERMNO 轴纵切片，反转记录所依据的事实基础"
  - phase: 03.11-10
    provides: "最终重建（252 × 520 int64 PERMNO 轴）、example/wrds_crsp.md 的反转记录（三处之一），以及它 escalate 上来的两件待裁定事项"
provides:
  - "D-08 反转记录的**另两处**：ROADMAP.md 里 03.10 Identifier 决策**下方**的 REVERSED 标注，与 STATE.md 的一条决策记录（第三处由 plan 10 写在 example/wrds_crsp.md）"
  - "REQUIREMENTS.md 的 DATA-03 追加第二条 advanced-by 记录（状态标记与计数一律未动）"
  - "D-21 裁定 backlog + 一条能重建现场的 todo；D-20 裁定保持默认"
  - "WINDOWS 条目 28 由 open 置 fixed，并写明 operator 的处置与实测结果"
  - "phase 收尾的全套回归门：相对基线零新增失败"
affects: [03.11-VERIFICATION, phase-2-DATA-03]

actuals:
  tokens: 2940
  tasks: 3
  commits: 2
plan_head_before: 1c8203ea32e182bd66f9f274df0ab72b72a9ca15

tech-stack:
  added: []
  patterns:
    - "反转历史决策的正确形状：历史文本一字不改，标注加在其**下方**，硬不变量是 `git diff --numstat` 的**删除列为 0**（比行数差更可靠）"
    - "planning 文档的定位锚用**内容 grep**而非行号——ROADMAP 被 plan-phase/`/gsd-phase` 反复编辑，RESEARCH §R10 记的 `:276-277` 已漂到别的小节"
    - "一条被裁定为 backlog 的风险，todo 要写到能**重建现场**：完整机制链 + 原始裁定 + 未解问题，而不是一句「以后加个断言」"

key-files:
  created:
    - .planning/todos/pending/2026-09-21-cross-vendor-permno-ticker-merge-is-silently-all-nan.md
  modified:
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/REQUIREMENTS.md
    - .planning/WINDOWS.md

key-decisions:
  - "ROADMAP 的 REVERSED 标注写成 03.10 bullet 下方的一个缩进段落（空行 + 8 行正文），而不是新起一条顶层 bullet：顶层 bullet 会读成「03.10 自己又做了一个决定」，而这是 03.11 对它的推翻"
  - "REQUIREMENTS.md 只改表格行的**单元格内文本**，文件行数 181 → 181 不变；`DATA-08` 计数 6→6、`[x]` 行数 7→7、`- [ ] **DATA-03**` 状态标记原样"
  - "D-21 → backlog（operator 裁定）。todo 的 `resolves_phase` 显式为 null，不是 03.11"
  - "D-20 → 保持默认（operator 裁定）。不给 CRSP 写专属 required_columns，`numtrd` 的 warn 保留；本 plan 零代码改动"
  - "WINDOWS 条目 28 置 **fixed** 而非 accepted：operator 选择删除 custom store，它是被真正处理掉的，不是被容忍的"
  - "plan 10 的「零命中 grep vs 新旧对照」张力：维持 plan 10 的选择（文档只描述代码现状，逐字对照指向 03.11-08-SUMMARY.md），不返工文档"
  - "`requirements-completed` 置空而非 [DATA-03] —— 见 Deviation 1，这是本 plan 自己的威胁模型 T-03.11-38 所要求的"

patterns-established:
  - "`gate=\"blocking-human\"` 的 decision 即使在被授权自动执行的 phase 里也必须停下来交还给人；执行者记录 operator 的答复，而不是替他选一个"
  - "被自动批准的 human-verify 必须在 SUMMARY 里**标注为 auto-approved**，好让 phase 验收时还看得见哪几项没人逐字读过"

requirements-completed: []

coverage:
  - id: D1
    description: "03.10 的 Identifier locked decision 下方新增 REVERSED 标注，且 03.10 历史文本一字未改"
    requirement: DATA-03
    verification:
      - kind: other
        ref: "git diff --numstat -- .planning/ROADMAP.md → `9  0`（删除列为 0，纯插入）"
        status: pass
      - kind: other
        ref: "grep -c 'REVERSED by Phase 03.11' .planning/ROADMAP.md → 0 改前 / 1 改后"
        status: pass
      - kind: other
        ref: "grep -c '^- Identifier: the panel' .planning/ROADMAP.md → 1 改前 / 1 改后"
        status: pass
    human_judgment: false
  - id: D2
    description: "STATE.md 按既有体例记录 D-08 反转（证据 + 新状态 + 三处记录的去向）"
    requirement: DATA-03
    verification:
      - kind: other
        ref: "grep -c 'D-08' .planning/STATE.md → 1 改前 / 2 改后；git diff --numstat → `1  0`；wc -l 383 → 384"
        status: pass
    human_judgment: false
  - id: D3
    description: "REQUIREMENTS.md 的 DATA-03 只追加 advanced-by 记录，状态标记与所有计数未被扰动"
    requirement: DATA-03
    verification:
      - kind: other
        ref: "grep -c 'DATA-08' → 6/6；grep -c '^- \\[x\\]' → 7/7；wc -l → 181/181；grep -c '^- \\[ \\] \\*\\*DATA-03\\*\\*' → 1"
        status: pass
      - kind: other
        ref: "grep -c 'Phase 03.11' .planning/REQUIREMENTS.md → 0 改前 / 1 改后"
        status: pass
    human_judgment: false
  - id: D4
    description: "全套回归门相对基线零新增失败"
    verification:
      - kind: integration
        ref: "uv run pytest -q --ignore=tests/test_factor_hierarchy.py --ignore=tests/test_crsp_rebuild_measurements.py -p no:cacheprovider → 55 failed / 1667 passed / 1 skipped in 269.43s；失败集合按文件逐条与基线相同"
        status: pass
    human_judgment: false
  - id: D5
    description: "D-21 裁定 backlog，并落一条能重建现场的 todo（机制链 + D-02 原始裁定 + 未解的逃生口问题）"
    verification:
      - kind: other
        ref: ".planning/todos/pending/2026-09-21-cross-vendor-permno-ticker-merge-is-silently-all-nan.md（97 行，resolves_phase: null）"
        status: pass
    human_judgment: false
  - id: D6
    description: "WINDOWS 条目 28 由 open 置 fixed，resolution 写明 operator 处置与实测结果；表格与 JSON 两处同步"
    verification:
      - kind: other
        ref: "gsd-tools windows status → open_count 22→21, fixed_count 6→7, total 28；表格行与 header 同为 12 个 awk 字段"
        status: pass
    human_judgment: false
  - id: D7
    description: "Task 3 的四项收尾人工确认"
    verification:
      - kind: manual_procedural
        ref: "第 1 项（历史未篡改）与第 4 项（523→520 解释）由编排器独立核实；第 2、3 项按 operator 授权 auto-approved"
        status: pass
    human_judgment: true
    rationale: "第 2 项（example/wrds_crsp.md 16 处改写读起来是否正确，其中 5 处整段重写）与第 3 项（指纹失效 warning 是否掩盖真实问题）按 operator 的类别授权自动通过，**没有人逐字读过**。如实标注，好让 phase 验收时还能看见这个缺口。"

duration: 2h 57m（其中约 2h 42m 是 blocking-human checkpoint 等待，实际执行约 20 min）
completed: 2026-09-21
status: complete
---

# Phase 03.11 Plan 11: 反转记录、需求台账与两个 operator 裁定 Summary

**把 Phase 03.10 的身份轴 locked decision 反转记录进 ROADMAP（标注在历史**下方**，删除列为 0）与 STATE，给 DATA-03 追加第二条 advanced-by（状态与计数分毫未动），拿回 D-21/D-20 两个 operator 裁定并落成 backlog todo，关闭 WINDOWS 条目 28，全套回归相对基线零新增失败。**

## Performance

- **Duration:** 2h 57m 墙钟；**实际执行约 20 min**，其余 2h 42m 是两个 `gate="blocking-human"` checkpoint 的等待
- **Started:** 2026-09-21T08:00:00Z（约）
- **Completed:** 2026-09-21T10:57:00Z
- **Tasks:** 3（1 个 auto + 2 个 checkpoint）
- **Files modified:** 4 改 + 1 新建

## Accomplishments

- **D-08 反转的另两处记录落地**，03.10 历史文本一字未改——硬证据是 `git diff --numstat` 的**删除列为 0**。
- **DATA-03 台账只追加不扰动**：文件行数、`DATA-08` 计数、`[x]` 行数、状态标记四项改动前后完全相同。
- **两个 blocking-human 决策交还给真人并拿回答复**，没有替 operator 猜任何一个。
- **D-21 的风险没有被「记一笔」了事**：todo 里写下了完整的静默失败链、D-02 的原始裁定、以及「加断言必须先设计逃生口」这个未解问题，并纠正了计划里已经漂掉的三处行号。
- **WINDOWS 条目 28 真正关闭**（operator 选择删除 custom store），顺带关掉了 plan 03.11-10 唯一那条未满足的验收条件。

## 反转记录的三处（本 plan 补齐后两处）

| # | 位置 | 由谁写 | 形式 |
|---|---|---|---|
| 1 | `example/wrds_crsp.md` | plan 10 | 新增「身份轴决策的反转（Phase 03.10 → 03.11，D-08）」小节 |
| 2 | `.planning/ROADMAP.md` | **本 plan** | 03.10 Identifier bullet **下方**的缩进标注（空行 + 8 行） |
| 3 | `.planning/STATE.md` | **本 plan** | Decisions 小节末尾一条 `- [Phase 03.11]: D-08 REVERSES …` |

第 2 处的定位用 `grep -n '^- Identifier: the panel'`（全文唯一命中，**实测在 :328**）。计划特意警告过：`03.11-RESEARCH.md` §R10 把这条决策记成 `.planning/ROADMAP.md:276-277` 并标了 `[VERIFIED]`，**那个行号已经漂了**——`:276-277` 现在落在 03.11 自己的 wave 列表上，照它插入会把标注插进完全错误的小节。按锚定位后一切正确。

## Task 1 验收逐条实测

| 断言 | 改动前 → 改动后 | 判定 |
|---|---|---|
| `grep -c "REVERSED by Phase 03.11" .planning/ROADMAP.md` | 0 → **1** | ✅ |
| `grep -c '^- Identifier: the panel' .planning/ROADMAP.md` | 1 → **1** | ✅ 03.10 历史 bullet 仍在 |
| `git diff --numstat -- .planning/ROADMAP.md` | — → **`9  0`** | ✅ **删除列 0**，纯插入 |
| `wc -l .planning/ROADMAP.md` | 1356 → **1365** | ✅ 差 9，与 numstat 一致 |
| `grep -c "D-08" .planning/STATE.md` | 1 → **2** | ✅ ≥1 |
| `wc -l .planning/STATE.md` | 383 → **384**（numstat `1  0`） | ✅ |
| `grep -c "Phase 03.11" .planning/REQUIREMENTS.md` | 0 → **1** | ✅ ≥1 |
| `grep -c "DATA-08" .planning/REQUIREMENTS.md` | 6 → **6** | ✅ 未扰动 |
| `grep -c "^- \[x\]" .planning/REQUIREMENTS.md` | 7 → **7** | ✅ 未扰动 |
| `wc -l .planning/REQUIREMENTS.md` | 181 → **181** | ✅ 单元格内追加 |
| DATA-03 状态标记 | `- [ ]` → `- [ ]` | ✅ |
| `git diff --stat`（三文件） | — → **+11 / −1** | ✅ ROADMAP +9/−0、STATE +1/−0、REQUIREMENTS +1/−1（同行单元格改写） |

**验收条件里那个陷阱按计划避开了**：没有用 `grep -c "stays the \*\*ticker\*\* valid at each date"` 断言 1——那条模式在 03.11 自己那段反转叙述里也出现，实测会返回 2。

**做法合规**：全程 Edit 工具精确字符串替换，**零 `sed`**（operator P6：范围删除漏匹配会一路删到 EOF，本项目已有真实事故），**零 `git stash`**。

## 全套回归门

```
uv run pytest -q --ignore=tests/test_factor_hierarchy.py --ignore=tests/test_crsp_rebuild_measurements.py -p no:cacheprovider
55 failed, 1667 passed, 1 skipped, 1306 warnings in 269.43s (0:04:29)
```

失败按文件逐条分解：`test_ingest_shells` 14、`test_ingest_tiingo_universe_wiring` 11、`test_data_dir_cli` 10、`test_ingest_conversion_gate` 6、`test_volume_guard` 5、`test_factor_kunquant` 3、`test_chunked_ingest` 2、`test_spot_dataset` 2、`test_universe` 1、`test_entry_point_contracts` 1 = **55**。

与 pre-phase 基线的分解（52 × `ModuleNotFoundError`，因 `scripts/` 不在 `pythonpath` 上；3 × `KeyError: 'amount'`，Tiingo/StockDataset 路径）**逐字相同**。**本 plan 新增失败 = 0**，收集总数 1723 ≥ 1600 的门槛。这 55 条全部是长期存在的既有失败，不属于本 phase 造成、也不在其 scope 内。

`passed` 1667 比编排器测的基线 1668 少 1，是 03.11-09 与 03.11-10 都报过的**同一处未归因 ±1**。决定性的量是失败集合，逐文件一致；且本 plan 只改 planning 文档，结构上不可能影响收集或结果。

## Task 2 — operator 裁定（`checkpoint:decision`，`gate="blocking-human"`）

这个 gate 在**任何模式下都不自动批准**（`checkpoints.md` golden rule 6）。计划作者在同一份文件里对别的 checkpoint 用 `blocking`、对这两个特意选了 `blocking-human`，这个区分是故意的。执行到此**停下来把问题原样交还**，operator 本人作答如下。

### D-21 → `backlog`

不在本 phase 实现跨 vendor dtype/交集断言。落一条 todo：
`.planning/todos/pending/2026-09-21-cross-vendor-permno-ticker-merge-is-silently-all-nan.md`（97 行，`resolves_phase: null`，**不是** 03.11）。

todo 里写全了三件事，让未来的人不必重新发现：

1. **完整的静默失败链**（行号已**重新实测**，见下）：
   `quantlab/base/model.py:218/:239/:248` 的 `xr.combine_by_coords` 外连出一条交集为空的并集轴 → `quantlab/base/backtest.py:782-784` 的 `predictions.reindex(timestamp=..., symbol=prices.symbol.values)` 铺出全 NaN 预测 → 空组合回测，全程唯一的外部迹象是 `quantlab/backtest/selection.py:162-166` 那条 `only {k} eligible symbol(s)` warning，而它与「早期 bar 上universe 本来就薄」这种合法情况**无法区分**。
2. **D-02 的原始裁定**，连同那句决定了处理方式的原文：「Optional task — propose it, let the operator decide; **do not assume it is wanted**.」
3. **未解的那一半**：朴素的 dtype/交集断言会**误拒合法的部分重叠**（故意只取交集一部分的板块切片、流动性筛选、刻意收窄的名册），所以实现者欠的是一个**逃生口设计**，不只是两行 assert。todo 里把「按 dtype 拒（窄，正好抓这个 bug）还是按交集空拒（宽，会误伤）」摆成了首先要评估的问题。

> **行号勘误（本 plan 实测）**：计划 `<context>` 引的 `model.py:207/:228/:237` 与 `backtest.py:756-757` **都已漂移**。实测值为 `model.py:218/:239/:248` 与 `backtest.py:782-784`（`:756-757` 现在落在 `_backtest_window` 的签名附近，与 reindex 无关）。todo 里同时记下了新旧两组并要求后来者按内容 grep 定位。

### D-20 → 保持默认

不给 CRSP 写专属 `required_columns`，`numtrd` 的 warn 保留。**本 plan 零改动**，按计划把这个裁定记录下来即可。理由仍然成立：消掉这条 warning 要么得动 `quantlab/dataset/cleaning.py`（D-06 明令禁止），要么得写一份本 phase 没有证据去塑造的 CRSP 专属列契约。`numtrd` 是**真实的厂商缺值**（NYSE 不报交易笔数），不是数据缺陷。

### WINDOWS 条目 28 → 删除，状态置 `fixed`

operator 选择**删除** `wrds_crsp_custom_1d.zarr`。**编排器已在主仓库工作树执行完毕**，本执行者未触碰主树的 `data/`：

- 备份：`data/_backup_deleted_custom_store_03.11/`（784K，zarr 本体 + 四个 sidecar，含 58,846 B 的 `.crsp_symbology_report.json`）
- 已删除：store 本体及 `.chunks.json` / `.crsp_adjustment.json` / `.crsp_filter_report.json` / `.crsp_symbology_report.json`
- 删除后实测：`1d/` 目录内 `crsp_symbology_report` 命中数 = **0**

置 `fixed` 而非 `accepted`——它是被**真正处理掉**的，不是被容忍的。`gsd-tools windows status` 实测 `open_count` 22 → **21**、`fixed_count` 6 → **7**、`total_count` 28。表格行与 JSON 两处同步更新，表格列数与 header 同为 12（awk `-F'|'` 实测）。

**这条顺带关闭了 plan 03.11-10 唯一一条未满足的验收条件。** plan 10 记录 `ls <root>/data/data/us_equity/1d/ | grep -c crsp_symbology_report` 应返回 0、实测返回 **1**（命中的正是 custom 的旁车），并如实标了 ⚠️。现在返回 **0**，那条验收条件事后成立。

### 另：plan 10 escalate 的文档张力 → 维持原状，不返工

plan 10 的 `<automated>` gate 是对五个已删机制名在 `example/wrds_crsp.md` 里的**零命中 grep**，而交接说明要求同一份文件里补一段旧名→新名对照——两者不能同时成立。plan 10 选了更严的一边（gate 满足，逐字对照指向 `03.11-08-SUMMARY.md`）。operator 裁定：**维持 plan 10 的选择，不返工文档**。

记录其自洽性以备后来者查：`tests/test_no_identity_residue.py` 的模块 docstring 刻意把 `example/` 排除在扫描外，正是为了**允许**散文点名已删机制——但那是**允许**，不是**要求**。两条规矩不矛盾，只是必须显式选一边；本 phase 选的是「文档只描述代码现在的样子」。

## Task 3 — 收尾人工确认（`checkpoint:human-verify`，`gate="blocking-human"`）

operator 已对这一类授权自动通过（只有 blocking-human 的 **decision** 需要他本人拍板）。四项的处置**如实分列**：

| # | 确认项 | 处置 | 证据 |
|---|---|---|---|
| 1 | 03.10 历史文本一字未改 | **编排器独立核实通过** | `git diff --numstat` 删除列为 0；`grep -c '^- Identifier: the panel'` 仍为 1。标注加在 bullet **下方**而非替换它 |
| 2 | `example/wrds_crsp.md` 16 处同步读起来是否正确 | ⚠️ **auto-approved，无人逐字读过** | 16 处里有 5 处整段重写；plan 10 自己也把它标成 `human_judgment: true` |
| 3 | 指纹失效的 warning 是否掩盖真实问题 | ⚠️ **auto-approved，无人逐字读过** | D-12 已接受既有 run 指纹全失效（`backtest.py:1145-1188` 只 warn 不阻断） |
| 4 | W0 的 523 与重建后 520 的差异解释是否可接受 | **编排器独立核实通过**（直接读盘上 zarr） | dims 252×520、`data_vars` 27、`permno` 不再是变量、raw zarr `symbol` dtype `int64`、ticker sidecar 520 个 PERMNO 与轴集合相等；三次改名在 sidecar 区间里逐条可见：12449 `FLT`→`CPAY` 2024-03-25、17700 `CDAY`→`DAY` 2024-02-01、67598 `HCP`→`PEAK`→`DOC` 2024-03-04。523 − 3 = 520 |

**第 2、3 项标注为 auto-approved 是刻意的**——好让 phase 验收时还能看见这两项没有人真正读过。

## Task Commits

1. **Task 1：三处反转记录 + 需求台账追加 + 全套回归门** — `f32f6a5` (docs)
2. **Task 2：D-21/D-20 裁定落地 + WINDOWS 28 关闭** — `0ef7a28` (docs)
3. **Task 3：收尾人工确认** — 无文件改动，记录在本 SUMMARY

**Plan metadata:** 见本文件的 docs commit。

`commits: 2` 是写 SUMMARY 时用 `git rev-list --count 1c8203ea32e182bd66f9f274df0ab72b72a9ca15..HEAD` **测**出来的（账本落在 `<git-dir>/gsd-plan-head-before-03.11-11`），不含随后那次 docs 提交——与本 phase 其他 plan 同一口径。

## Files Created/Modified

- `.planning/ROADMAP.md`（+9/−0）— 03.10 Identifier 决策**下方**的 `**REVERSED by Phase 03.11**` 缩进标注：一句话理由（回收的 ticker 拼接两家公司，而 `resolve_collisions` 只看得见同日争用，2000-2024 窗口里 96.6% 的回收是它看不见的顺序继承）+ 新状态 + 指向 03.11-CONTEXT.md 的 D-08。
- `.planning/STATE.md`（+1/−0）— Decisions 小节一条记录：旧决策原文、新状态（`symbol` = int64 PERMNO，28→27 data_vars，ticker 进 `.crsp_tickers.json` as-of sidecar，读侧 `CrspTickerLookup`）、三组实测证据、以及「反转记录本身是交付物」的出处。
- `.planning/REQUIREMENTS.md`（+1/−1，同行）— DATA-03 追加第二条 advanced-by，并显式写明「Status unchanged: still Pending, still owned by Phase 2」。
- `.planning/WINDOWS.md`（+7/−7）— 条目 28 置 fixed + resolution；表格与 JSON 两处同步；计数行随之更新。
- `.planning/todos/pending/2026-09-21-cross-vendor-permno-ticker-merge-is-silently-all-nan.md`（新建，97 行）— D-21 的 backlog todo。

## Decisions Made

见 frontmatter `key-decisions` 七条。额外两条执行期的：

1. **ROADMAP 标注选「缩进段落」而非「顶层 bullet」。** 那一节的标题是 03.10 的 **User decisions already made**；在其中新起一条 `- **REVERSED …**` 顶层 bullet 会被读成「03.10 自己又做了一个决定」，恰好是最容易误导后来者的形状。缩进在 Identifier bullet 之下，视觉与语义都从属于它、且明确晚于它。代价是列表从 tight 变 loose（纯渲染差异，无内容影响）。
2. **`gate="blocking-human"` 的两个 checkpoint 没有被自动批准，即使这个 phase 被授权自动执行。** golden rule 6 与 executor 的 checkpoint 协议一致规定 blocking-human 在任何模式下都要停；计划作者对这两个特意选了更严的 gate。所以执行到 Task 2 停下、交还、等到真人答复后才继续。D-21 的两个选项都有真实代价（A 会误拒合法部分重叠），替 operator 猜一个正是这个 gate 存在的理由所要防的事。

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical / 威胁模型 T-03.11-38] `requirements-completed` 置空，而非照抄 `[DATA-03]`**

- **Found during:** 写 SUMMARY frontmatter 时
- **Issue:** 计划 frontmatter 写着 `requirements: [DATA-03]`，SUMMARY 模板要求逐字照抄进 `requirements-completed`。但本 plan 自己的三处约束都说 DATA-03 **没有**完成：ROADMAP 的 phase 段落写「**No requirement is COMPLETED here**」，`must_haves.truths` 写「状态标记不改、任何计数不动」，`prohibitions` 写「不得改动 REQUIREMENTS.md 里 DATA-03 的状态标记或任何计数」。更关键的是 **shared-ID 门的时序**：plan 10 也声明了 `[DATA-03]`，该门一直把它压着不放，**直到最后一个声明它的 plan（就是本 plan）产出 SUMMARY**。本 plan 若照抄，门即释放、`requirements.mark-complete` 会把 DATA-03 翻成 Complete——正是本 plan 威胁模型里 **T-03.11-38（severity high、disposition mitigate）**逐字描述的危害：「误改 REQUIREMENTS.md 的状态标记或计数，让一个 Pending 的 requirement 看起来已完成」。
- **Fix:** `requirements-completed: []`，并在此处与交接报告里显式说明。**本执行者也没有运行 `requirements.mark-complete`。**
- **Files modified:** 仅本 SUMMARY 的 frontmatter
- **Verification:** `grep -c '^- \[x\]' .planning/REQUIREMENTS.md` 改动前后同为 7；`- [ ] **DATA-03**` 状态标记原样；`DATA-03 | Phase 2 | Pending — …` 表格行仍以 Pending 开头
- **Committed in:** 本 SUMMARY 的 docs commit
- **⚠ 留给编排器/验收的显式提醒:** 若下游流程仍要对本 phase 跑 `requirements.mark-complete`，**请勿把 DATA-03 传进去**。它由 Phase 2 拥有并交付；03.10 与 03.11 都只是 advance 它。

**2. [Rule 1 - Bug] 计划 `<context>` 引用的三处源码行号已全部漂移**

- **Found during:** Task 2（写 D-21 的 todo，核对机制链时）
- **Issue:** 计划 D-21 的 `<context>` 写 `base/model.py:207`/`:228`/`:237` 与 `base/backtest.py:756-757`。实测在 `1c8203e` 上：`combine_by_coords` 在 **:218/:239/:248**，`predictions.reindex(...)` 在 **:782-784**（`:756-757` 现在落在 `_backtest_window` 的签名附近，与 reindex 无关）。一条写着错行号的 backlog todo，正是「几年后没人找得到现场」的典型死法——而这条 todo 的**全部价值**就是让后来者能重建现场。
- **Fix:** todo 里用实测行号，并同时记下计划引的旧值、加一句「按内容 grep 定位，不要按行号」的指引（与本 plan 在 ROADMAP 上刚踩过的 RESEARCH §R10 行号漂移同一教训）。
- **Files modified:** 新建的 todo 文件
- **Verification:** `grep -n 'combine_by_coords' quantlab/base/model.py` → 218/239/248（另有 :203 docstring、:248 的 feature+label 合并）；`grep -n 'reindex' quantlab/base/backtest.py` → 唯一命中 782
- **Committed in:** `0ef7a28`

**3. [Rule 2 - Missing Critical] `windows fixed` 不接受理由参数，resolution 会丢失**

- **Found during:** Task 2
- **Issue:** operator 明确要求 resolution 写明处置经过。但 `gsd-tools windows fixed` 只收 1 个位置参数（`waive` 收 2 个：id + reason），实测 `--reason` 报 `Unknown flag`，所以它只翻 `status`/`resolved_at`，把 `reason` 留空——条目 25/26/27 的 `reason` 也都是空的。只跑这个动词，operator 要求的那段记录就会**不存在**。
- **Fix:** 先跑 `windows fixed 28` 让工具翻状态与时间戳（保持两处口径一致），再对 `reason` 字段做**两处**精确替换：markdown 表格行与文件末尾的 JSON 块。表格文案里刻意**不含 `|` 字符**（原本要写的 `ls … | grep -c …` 管道会把表格列切断），改写成「1d/ 目录内 crsp_symbology_report 命中数为 0」。
- **Files modified:** `.planning/WINDOWS.md`
- **Verification:** `gsd-tools windows status` 仍 `ok: true`（JSON 未被写坏），`open_count` 21 / `fixed_count` 7 / `total_count` 28；表格行 awk `-F'|'` 字段数 12，与 header 行及兄弟行 25 完全一致
- **Committed in:** `0ef7a28`

---

**Total deviations:** 3（2 missing-critical auto-added，1 bug auto-fixed）
**Impact on plan:** 全部在计划自己的意图之内，零 scope 蔓延。1 是本 plan 威胁模型 T-03.11-38 的直接落实（也是唯一一条需要编排器知情的）；2 让那条 backlog todo 真的可用；3 是把 operator 明确要求的 resolution 真的写进去。**零代码改动**——本 plan 未触碰 `quantlab/` 下任何文件。

## Issues Encountered

1. **两个 `gate="blocking-human"` checkpoint 使本 plan 分两段执行。** Task 1 完成并提交（`f32f6a5`）后，在 Task 2 停下并交还 `checkpoint_return_format`；operator 作答后继续 Task 2/3。这是设计内的行为，不是故障。墙钟 2h 57m 里约 2h 42m 是等待。
2. **`requirements-completed` 与 shared-ID 门的时序冲突**（见 Deviation 1）。这是本 plan 发现的唯一一处「按模板照做会造成实际损害」的地方，已按威胁模型处置并显式上报。
3. **plan 10 那条曾经 ⚠️ 的验收条件，在本 plan 期间由 operator 的删除动作事后关闭。** 值得记一笔的是它**不是**被本 plan 的代码或文档改动关掉的，而是被一次数据侧的处置关掉的——`data/` 是 gitignored 的，所以 git 历史里看不到这件事发生，只有 WINDOWS 条目 28 的 resolution 记着它。

## Known Stubs

None。本 plan 没有留下 stub、没有 `skip`、没有未跑的 `<verify>`。

未新增 WINDOWS 条目；**关闭**了一条（28）。D-21 的风险按 operator 裁定转成了 `.planning/todos/pending/` 里的一条 todo，不是 WINDOWS 条目——它是一个被明确裁定推迟的设计任务，不是本 phase 留下的破窗。

## Threat Flags

None。本 plan 不引入任何网络端点、认证路径、文件访问模式或信任边界的改动；**零代码改动、零安装**（`pyproject.toml` / `uv.lock` 未触碰，T-03.11-SC accept）。

计划 `<threat_model>` 里三条 `mitigate` 的落实：

| Threat ID | 落实 |
|---|---|
| T-03.11-34（编辑 03.10 历史文本来「消除矛盾」，反转不可追溯） | `git diff --numstat` 删除列为 **0**（纯插入）——这是比行数差更硬的不变量；`grep -c '^- Identifier: the panel'` 改动前后同为 1；标注在 bullet **下方**并指向 D-08；Task 3 第 1 项由编排器目视 `git diff` 核实通过 |
| T-03.11-38（误改 REQUIREMENTS 状态/计数，让 Pending 看起来已完成） | `[x]` 行数 7→7、`DATA-08` 计数 6→6、`wc -l` 181→181、状态标记仍 `- [ ]`；**并额外把 `requirements-completed` 置空**，见 Deviation 1——这条威胁真正的触发路径是 SUMMARY frontmatter 而非文件本身 |
| T-03.11-35（`sed` 范围删除漏匹配一路删到 EOF） | 全程 Edit 精确替换，**零 `sed`**；四个文件的 `git diff --stat` 增删行数与本 SUMMARY 记录的核对数字逐条一致 |
| T-03.11-39（跨 vendor 合并静默全 NaN） | `transfer` —— 按 D-21 裁定转为 backlog todo，机制链与未解问题完整留档 |
| T-03.11-37（指纹失效 warning 刷屏掩盖真实问题） | `accept` —— Task 3 第 3 项，按 operator 授权 auto-approved，**已如实标注无人逐字读过** |

## User Setup Required

None - no external service configuration required。本 plan 纯 planning 文档改动，不需要 WRDS 账号、凭证或网络。

## Next Phase Readiness

- **D-08 反转的三处记录已全部就位**，03.10 历史完好，后来者读到那条 locked decision 时能立刻看到它被推翻以及为什么。
- **DATA-03 仍是 Pending，仍归 Phase 2**；03.10 与 03.11 两条 advanced-by 记录并列在同一行里。**下游若跑 `requirements.mark-complete`，请勿传入 DATA-03**（Deviation 1）。
- **WINDOWS 本 phase 相关条目全部关闭**（25/26/27 由 plan 10、28 由本 plan）；`open_count` 21 条全部属于更早的 phase。
- **一条新的 backlog todo** 等待未来某个 phase 认领：跨 vendor 合并的静默全 NaN。它不阻塞任何现有流程——今天没有任何已交付代码会把 CRSP 面板和 ticker 轴面板合到一起；风险发生在有人第一次写出那个 config 的那天。
- **phase 未被标记完成，未写 VERIFICATION.md** —— 这是编排器的活，本执行者按 `<shared_planning_files_exception>` 的边界未越界。
- **环境：** 回归跑 269.43s 正常结束，未复现 03.11-07 报的 KunQuant + loky 死锁；运行前 `ps` 确认无其他 pytest 进程，运行后无孤儿。全程**未使用 `git stash`**、**未使用 `sed` 范围删除**、**未 push** 这条 `worktree-agent-*` 临时分支。

---
*Phase: 03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r*
*Completed: 2026-09-21*

## Self-Check: PASSED

- **`key-files` 全部在盘上：** 4 个 modified + 1 个 created 均 `FOUND`（5/5），本 SUMMARY 自身也在。
- **两个任务提交都在 `git log 1c8203e..HEAD` 里：** `f32f6a5`、`0ef7a28`；`commits: 2` 是 `git rev-list --count` **测**出来的，不是数出来的；`plan_head_before` 取自账本文件而非记忆。
- **Task 1 的 7 条 `<acceptance_criteria>` 逐条复跑：** REVERSED == 1 ✅；Identifier 锚 == 1 ✅；numstat 删除列 == 0 且新增 9 = 标注实占行数 ✅；STATE 的 D-08 ≥ 1（实测 2）✅；REQUIREMENTS 的 Phase 03.11 ≥ 1（实测 1）✅；DATA-08 计数与 `[x]` 行数均与改动前相同（6/6、7/7）✅；`git diff --stat` 与本 SUMMARY 记录的核对数字一致 ✅；回归零新增失败 ✅。
- **计划级 `<verification>` 三条：** 回归相对基线零新增失败 ✅；`REVERSED by Phase 03.11` 为 1 且 03.10 原句仍在 ✅；两个 checkpoint 的答复都已写进本 SUMMARY ✅。
- **`<success_criteria>` 五条：** 反转记录在三处且历史一字未改 ✅；DATA-03 只追加不改状态不动计数 ✅；D-20/D-21 两个裁定已记入 ✅；四项人工确认已处置并**如实标注**哪两项是 auto-approved ✅；全套零新增失败 ✅。
- **`prohibitions` 逐条核对：** 未编辑 03.10 历史决策文本（删除列 0）✅；未改 DATA-03 状态标记或任何计数 ✅；零 `sed` 范围删除，提交前已核对行数 ✅；未在 operator 批准前实现 D-21 断言（裁定为 backlog，零代码改动）✅；未修改 `quantlab/dataset/cleaning.py`（本 plan 零代码改动）✅；零 config 迁移/兼容层 ✅。
- **`git status --porcelain` 在返回前为空**（本 SUMMARY 提交后复核）。
