---
phase: 03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r
verified: 2026-09-21T11:30:11Z
status: human_needed
score: 77/79 must-haves verified
covered_files:
  - ".planning/REQUIREMENTS.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-01-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-01-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-02-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-02-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-03-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-03-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-04-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-04-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-05-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-05-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-06-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-06-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-07-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-07-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-08-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-08-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-09-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-09-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-10-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-10-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-11-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-11-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-REVIEW.md"
  - "quantlab/acquisition/inspector.py"
  - "quantlab/backtest/engine_vectorbt.py"
  - "quantlab/base/backtest.py"
  - "quantlab/base/config.py"
  - "quantlab/base/constituent.py"
  - "quantlab/base/data.py"
  - "quantlab/base/factor.py"
  - "quantlab/base/model.py"
  - "quantlab/base/rebuild.py"
  - "quantlab/dataset/backend.py"
  - "quantlab/dataset/constituent.py"
  - "quantlab/dataset/crsp.py"
  - "quantlab/dataset/crsp_membership.py"
  - "quantlab/dataset/crsp_rebuild.py"
  - "quantlab/dataset/crsp_symbology.py"
  - "quantlab/dataset/crsp_tickers.py"
  - "quantlab/dataset/masking.py"
  - "quantlab/dataset/stock.py"
  - "quantlab/factor/universe_filter.py"
  - "quantlab/utils/cli.py"
  - "quantlab/utils/symbol_axis.py"
  - "scripts/ingest_wrds_crsp.py"
covered_digest: "v1:sha256:93299dbf439f87e278d055757aa5400a7947bc71f8af7ca4812193b9712086f4"
behavior_unverified: 2
overrides_applied: 0
behavior_unverified_items:
  - truth: "既有 backtest run 的指纹失效会让 _compare_fingerprints 刷一屏 warning（D-12 已接受），确认它没有掩盖真实问题（plan 10 backstop）"
    test: "在 PERMNO 轴重建后的 store 上重跑一个既有 backtest run（load 模式），读完 _compare_fingerprints 打出的全部 warning"
    expected: "每一条 warning 都能归因到 D-12 已接受的指纹失效（digest / n_symbols 变了），没有一条是别的真实问题被裹在里面"
    why_human: "没有任何测试驱动 backtest.py:1145-1188 的 warning 路径；plan 11 的 SUMMARY 自己标注这一项是按 operator 类别授权 auto-approved、**无人逐字读过**（03.11-11-SUMMARY.md:119, :238, :329）"
  - truth: "既有 backtest run 的指纹失效会让 _compare_fingerprints 刷一屏 warning（D-12 已接受），需确认它没有掩盖真实问题（plan 11 backstop，同一条，重复声明）"
    test: "同上"
    expected: "同上"
    why_human: "同上 —— 同一条 backstop 在 plan 10 与 plan 11 各声明一次，两次都未被人读过"
human_verification:
  - test: "在 PERMNO 轴重建后的 store 上重跑一个既有 backtest run（load 模式），读完 _compare_fingerprints 打出的全部 warning"
    expected: "每一条 warning 都能归因到 D-12 已接受的指纹失效，没有别的真实问题被裹在里面"
    why_human: "无测试覆盖；plan 11 SUMMARY 自认 auto-approved 且无人逐字读过（T-03.11-37）"
  - test: "逐字读 example/wrds_crsp.md 本 phase 改动的 16 处（其中 5 处是整段重写），核对它们描述的是当前代码而不是迁移前的行为"
    expected: "16 处全部与 quantlab/dataset/crsp.py / crsp_tickers.py / crsp_symbology.py 的现状一致"
    why_human: "机械检查已过（example/ 下 symbology_report 零残留、crsp_tickers.json 出现 8 次、反转记录存在），但散文的**正确性**只有人能判；plan 11 SUMMARY:119 明确标注这一项 auto-approved、无人逐字读过"
  - test: "决策：quantlab/base/backtest.py:15 `from quantlab.dataset.crsp_tickers import CrspTickerLookup` —— 接受这条 base/ → 厂商模块的硬依赖，还是改成与 base/factor.py:80-115 同一形态（由 dataset 侧声明、base 侧只读一个映射/可调用）"
    expected: "一个明确的裁定：接受（并把 base/backtest.py 纳入 CORE_LAYER_FILES 的豁免记录），或开一条 gap-closure/backlog 把耦合改成注入式"
    why_human: "CLAUDE.md 的单向分层约束 vs. 本 phase 自己在 base/factor.py 示范的干净写法 —— 同一个 phase 用了两种做法，取舍是设计判断不是缺陷判定。code review WR-03，本验证已独立复现"
  - test: "决策：CrspTickerLookup.label() 的 docstring 写着「Never raises」，但对**可解析但结构错误**的 sidecar 会抛 KeyError/AttributeError（已复现三例）。现在修，还是记 backlog"
    expected: "要么把 guard 扩到整个函数体并补一条 `{\"intervals\": {\"13407\": [{\"ticker\": \"FB\"}]}}` 的回落测试，要么立一条 todo 并把 docstring 的「Never raises」改成它实际成立的范围"
    why_human: "不是任何 must_have 的失败（T-03.11-30 的计划原文只覆盖「sidecar 缺失」，缺失路径实测正确）；但 03.11-09-SUMMARY.md:145 把它写成「缺失/**损坏**时五个点全部优雅回落」，这一半不成立。修不修是优先级判断。code review WR-01，本验证已独立复现"
  - test: "决策：{zarr}.crsp_filter_report.json 的 dropped_permnos / roster_overrides 里，每个 PERMNO 的 `symbol` 字段现在与它自己的 key 逐字相同（实测 roster_overrides.permnos['75154'].symbol == '75154'），审计报告丢掉了人可读的名字。补 ticker，还是接受"
    expected: "要么让 _permno_breakdown 经 CrspTickerLookup 补一个 ticker 字段，要么明确接受并把 tests/test_crsp_identity.py:585-587 那句「留给 plan 09」的注释改成「已裁定不做」"
    why_human: "tests/test_crsp_identity.py:585-587 把这件事交接给了 plan 09，plan 09 的六个展示点不含 filter report，交接落空且现在被测试锁成既定行为。不是 must_have 失败（plan 07 只要求保留 _permno_breakdown，它确实保留了）。code review WR-04，本验证已在盘上产物复现"
  - test: "清理：WINDOWS.md 的 22 / 23 / 24 三条仍是 `open`，但它们点名的接管工作已完成（tests/test_ingest_wrds_crsp.py + tests/test_no_identity_residue.py 本次实跑 32 passed，DELETED_IN_03_11_08 已并入 DELETED_IN_03_11_07）"
    expected: "把 22 / 23 / 24 标成 fixed 并写明证据，或说明为什么留开"
    why_human: "台账状态与代码事实不符（方向是安全的——留开而不是被静默关闭），但需要一次人为确认再改台账"
advisory:
  - finding: "BaseStoreRebuilder.clear() 先把路径记进 removed 再 shutil.rmtree(..., ignore_errors=True)，删除失败被吞掉，随后 RebuildMeasurement 把没删掉的路径当成已删除记进「证据」"
    category: other
    reason: "W0 前置机制里的假绿通道：删除失败 → _write_identity_reports 的 store-exists 守卫跳过 → 旧 sidecar 留在新面板旁边，而测量对象报告一切正常。本次未发生（盘上 store 实测已是 int64 轴、symbology sidecar 已消失、四个数全部复现），所以只记不拦。code review WR-05"
    evidence_status: "机制已在 quantlab/base/rebuild.py:203-230 复现读证；失败路径本轮未触发"
  - finding: "normalize_to_axis_dtype 对 float / bool 标签在 int64 轴上静默强转，与它自己的拒绝契约相抵（code review WR-02）"
    category: other
    reason: "未被本 phase 任何 must_have 覆盖；实际调用点传的都是 int/str"
    evidence_status: "none provided（本次验证未复现）"
  - finding: "tests/test_factor_hierarchy.py 自 2026-09-15 的 `change label names`（2e2ea12）起就 ImportError 无法 collect（SpotReturn 已从 quantlab.label.fret 消失），早于本 phase"
    category: other
    reason: "回归门的 --ignore 因此是正当的，但它掩盖的是一份彻底失效的测试文件，不是一份慢测试。属于既有技术债，应单独立项"
    evidence_status: "已实跑复现：`uv run pytest -q tests/test_factor_hierarchy.py` → 1 error during collection；`git merge-base --is-ancestor 2e2ea12 ba4f5b3` 为真"
---

# Phase 03.11: CRSP PERMNO symbol axis migration and Tiingo-era dead code removal — Verification Report

**Phase Goal (ROADMAP.md:141):** CRSP 价格面板的 `symbol` 维变成 **int64 PERMNO** 而不是每个日期上有效的 ticker；每一处「只为了让被回收/改名的 ticker 还能当身份轴用」的适配都被**删除**而不是藏进兼容分支；`ticker` 完全离开面板，进旁车做 as-of 展示查询。

**Verified:** 2026-09-21T11:30:11Z
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Part A — 相位目标可观察真值（从 ROADMAP Goal 段落派生）

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| G1 | CRSP 价格面板的 `symbol` 维是 int64 PERMNO，不再是日期有效 ticker | ✓ VERIFIED | 核心两行已换：`quantlab/dataset/crsp.py:568` `frame.with_columns(pl.col("symbol").cast(pl.Int64))`（原 `drop("symbol")` + `label_rows` 已消失）。盘上实测 `data/data/us_equity/1d/wrds_crsp_sp500_1d.zarr`：dims 252×520，raw zarr `symbol` dtype **int64**，值 `[10104 10107 10138 10145 10516 …]` |
| G2 | `permno` 不再是数据变量（它就是轴本身） | ✓ VERIFIED | `CRSP_EXTRA_VARIABLES`（crsp.py:140-155）现为 14 项，`permno` 已移除；盘上 store `data_vars` = 27，`'permno' in ds.data_vars` → **False** |
| G3 | 被回收的 ticker 不能再把两家公司拼成一列 | ✓ VERIFIED | 行为测试 `tests/test_crsp_tracer.py:465 test_a_recycled_ticker_yields_two_columns` — **实跑 passed** |
| G4 | 改名不再断开身份：PERMNO 13407 在 FB→META 前后是同一列 | ✓ VERIFIED | 行为测试 `tests/test_crsp_tracer.py:544 test_permno_13407_is_one_column_across_the_fb_meta_rename` — **实跑 passed** |
| G5 | 身份适配机制被**删除**而非藏进兼容分支 | ✓ VERIFIED | 仓库级零残留 gate `tests/test_no_identity_residue.py:126`（覆盖 `_resolve_identity` / `resolve_collisions` / `_class_collision_pass` / `_collision_message` / `_member_spans` / `label_rows` / `_per_permno` / `symbology_report_path` / `SYMBOLOGY_REPORT_SUFFIX` / `_symbology_report` / `symbol_overrides` / `nan_adj_at_permno_seam`）**实跑 passed**。我独立 grep `quantlab/ scripts/ example/`：唯一命中是 `example/wrds_crsp.md:271` 的历史解释文字（反转记录段落里说明 `label_rows` 为什么被删），不是活代码残留 |
| G6 | `CrspSymbology` 收缩成纯区间提供者（D-18） | ✓ VERIFIED | `quantlab/dataset/crsp_symbology.py` 现 152 行，公开面只剩 `SUFFIX_DELIMITER`、`__init__`、`symbol_intervals()` |
| G7 | `ticker` 完全离开面板，进旁车做 as-of 查询 | ✓ VERIFIED | 盘上 `{zarr}.crsp_tickers.json` 存在，`intervals` 是**区间表**（13407 → FB 至 2022-06-08、META 自 2022-06-09；519/520 个 PERMNO 有多条区间），不是「最后一个 ticker」。**我实跑** `CrspTickerLookup.beside_store(...)`：`as_of(13407, 2022-06-08)` → `FB`；`as_of(13407, 2022-06-09)` → `META`；`label([13407,10107,99999999], 2023-01-03)` → `['META','MSFT','99999999']`（无名回落成数字）。面板内 `ticker` 既不是 2-D 变量也不是 1-D coord |
| G8 | sidecar 只写面板实际包含的 PERMNO | ✓ VERIFIED | sidecar `intervals` 键数 = **520** = 面板 symbol 轴长，集合相等（非全库 40,518） |
| G9 | int64 硬破坏点被修好，`widen_symbol_axis` 不再静默清空 store | ✓ VERIFIED | `quantlab/dataset/backend.py:13` 引入 `normalize_to_axis_dtype` / `sort_symbol_axis`；`:502` 在**归一化后**的值上做 superset 守卫。行为测试 `tests/test_symbol_axis_widening.py:441 test_widening_an_int64_axis_preserves_every_stored_cell` 与 `:501 test_the_superset_guard_actually_guards_an_int64_axis` — **实跑 passed**（该文件 22 个测试全绿） |
| G10 | `universe_filter.py` 条件 (a) 被删除，(b)(c) 与图改写机制保留 | ✓ VERIFIED | `NON_COMMON_TICKER_PATTERNS` / `exclude_non_common` / `is_common_ticker` 在 `quantlab/` 下零命中；仅剩 `tests/test_universe_filtered_factor.py:781` 的 `TypeError` 拒绝锁与 `example/universe.md` 的删除论证。`min_price`（:178/:210/:344/:467）、`min_dollar_volume`、`Div(v, mask)` 图改写（:130/:290）完好。D-06 保护项 `_PREFERRED_SHARE_PATTERN` / `_BABY_BOND_PATTERN` / `TRADEABLE_TICKER_PATTERN` 一字未改 |
| G11 | 03.10 的 locked decision 反转被记录在三处，03.10 历史文本未改 | ✓ VERIFIED | (1) `.planning/ROADMAP.md` —— 03.10 Identifier bullet 原文保留，其**下方**新增「**REVERSED by Phase 03.11**」段落；(2) `.planning/STATE.md:306`；(3) `example/wrds_crsp.md`「身份轴决策的反转（Phase 03.10 → 03.11，D-08）」整节 |
| G12 | **DATA-03 仍为 Pending**（ROADMAP 逐字：「No requirement is COMPLETED here」） | ✓ VERIFIED | `.planning/REQUIREMENTS.md:23` 仍是 `- [ ] **DATA-03**`；`:139` 的追踪行只**追加**了一条 advanced-by 记录，逐字以「Status unchanged: still Pending, still owned by Phase 2」结尾。状态标记与计数均未动 |

**Part A 得分：12/12**

### Part B — 11 个 plan 的 must_haves 汇总

| Plan | Truths | Status | 关键证据 |
|------|--------|--------|----------|
| 01（W0 重建） | 7 | 7 ✓ | `quantlab/base/rebuild.py`（ABC，`BaseStoreRebuilder` + 4 个 abstractmethod）+ `quantlab/dataset/crsp_rebuild.py`（`CrspStoreRebuilder`）两层结构，**不在 scripts/ 下**；两个文件均不 import `quantlab.config` 工厂。`rebuild()` 顺序 assert→backup→clear→convert（rebuild.py:232）。`_measure()` 从 `cleaning.REQUIRED_COLUMNS` 导入结构性空格定义而非重列。**我在盘上产物独立复现七项测量**：`structural_gaps=764`、`adj_close_nan=764`、`adj_volume_nan=764`、`adj_close_le_zero=0`、`close_eq_zero=0`、`anomaly_flag_true=13`、`symbol_count=520` —— 五条清洁度断言全部成立。backstop（轴长 8 的倍数仍被因子测试覆盖）：`520 % 8 == 0`，且 `tests/test_factor_stream.py` + `tests/test_cross_sectional_zscore.py` **实跑 12 passed** |
| 02（int64 硬破坏点） | 7 | 7 ✓ | `quantlab/utils/symbol_axis.py`（182 行，`sort_symbol_axis` / `normalize_to_axis_dtype`，数值序契约只在一处表达）；`tests/conftest.py:1479` `SYMBOL_COORD_ENCODINGS = ("fixed_width","variable_length","int64")`，第三 arm 按 dtype KIND 判定；`XrBackend.filter_by_symbol`（backend.py:1543-1545）与其兄弟 `filter_by_date` 在 `git diff ba4f5b3 HEAD` 中**零改动**。backstop（另 5 个消费者仍全绿）：`test_symbol_coord_encoding / test_variable_axis_widening / test_widening_fixture_realism / test_factor_update / test_nbbo_dataset` **实跑 72 passed** |
| 03（TRACER） | 9 | 9 ✓ | 见 G1/G3/G4。`admitted_without_ticker` 落在 `crsp.py:1198 _render_admitted_without_ticker` + `:1210 _warn_admitted_without_ticker`，盘上 filter report 确有该键（本窗口为 `{"permnos":[],"rows":0}`，S&P 500 成分股全部有 ticker，符合预期）；无类型谓词、无日期门。退市判决继承与 `_DELISTING_FLAG`（crsp.py:238 / :977）保留，`git diff` 显示该段只改了 docstring。vectorbt int64 列索引：`tests/test_backtest_engine.py:1151 test_int64_column_index_round_trips_through_vectorbt` **单测实跑 passed**。backstop（SIMD 宽度）：同 plan 01 |
| 04（checkpoint 三端 + _stored_symbol_axis） | 5 | 5 ✓ | `base/model.py:27` 引入 `sort_symbol_axis`，写侧 `:352`、读侧归一 `:569`、对齐侧 `:1277/:1287` 三处统一；`:1341/:1364` 明确拒绝把 ticker-era 记录强转成 int。`base/data.py:825 _stored_symbol_axis` docstring 逐字记录「labels come back in the store's OWN spelling」，不再 `str()` 强转。`tests/test_model_predict_panel.py` 6 个新测试（含 `test_four_digit_permno_aligns_in_numeric_order`）**实跑 passed** |
| 05（成分/掩码轴对齐） | 7 | 7 ✓ | `dataset/constituent.py:172` / `:219` 已改用 `CrspMembership.permno_intervals()`；`crsp_membership.py` 的 `symbol_intervals()` / `_symbol_frame()` 已删（只在 :21/:68 的解释性 docstring 里被提及）。`UniverseMask.report()` 的 `missing_symbols` 仍是完整列表（masking.py:231，docstring :210-219 逐字承诺不截断、不采样，新增的 `missing_labels` 与之等长同序）。`tests/test_universe_mask.py` / `test_crsp_constituent.py` **实跑 passed** |
| 06（universe_filter 条件 a） | 6 | 6 ✓ | 见 G10。`_mask_panel` 永不删列：universe_filter.py:47-53 与 :155-158 记录了「条件 (a) 删除后唯一的删列分支随之消失，LS-3 变成平凡成立」这条红利；删除论证写的是**机制论证**（九条正则都要求字母，对 PERMNO 数字串恒不匹配 → 整条过滤无声变 no-op），不是比较论证 |
| 07（死代码删除） | 8 | 8 ✓ | 见 G5/G6。`_assert_unique_panel_keys` 保留；`_apply_security_filter` / `_roster_sources` / `_build_filter_report` / `_permno_breakdown` / 调整锚 / `adjustment_sidecar_path` 全部保留（盘上 `.crsp_adjustment.json` 与 `.crsp_filter_report.json` 仍在）。仓库级 grep gate `tests/test_no_identity_residue.py` 计入注释，**实跑 passed** |
| 08（config 字段） | 8 | 8 ✓ | `symbol_overrides` / `nan_adj_at_permno_seam` 零残留（gate 覆盖）。`collision_universe` → `roster_universe`：字段本身存活于 `base/config.py:191`，消费点 crsp.py:484-490 / :765-773 / :795-804、`scripts/ingest_wrds_crsp.py:501`。**两个**拒绝点：`crsp.py:428-440`（`CrspStockDataset` config setter，非 None 即拒，点名 `config.permnos`）与 `base/factor.py:113-120`（经 dataset 侧声明的 `REJECTED_FACTOR_CONFIG_FIELDS`，挡在 `_auto_filter` → `filter_by_symbol` 之前，**且 base/factor.py 不 import 任何厂商类**）。`filter_by_symbol` 零改动 |
| 09（ticker sidecar） | 9 | 9 ✓ | 见 G7/G8。`CrspTickerLookup` 是类（crsp_tickers.py:57），惰性读 + 哨兵缓存，缺文件的 `FileNotFoundError` 带类名前缀/路径/补救动作（**我实跑复现**）。五个人可见还原点全部落地：`engine_vectorbt.py:303-309`（强平记录同时写 `symbol` 与 `axis_symbol`）、`base/model.py:1312 _spell`（经注入的 `symbol_labeller`，`CrspTickerLookup` 在 base/model.py **零出现**，我已 grep 确认）、`masking.py:232 missing_labels`、`inspector.py:538-548 browse_zarr`、`utils/cli.py:133-140 --symbols` 帮助文案。SUMMARY 诚实地把「六个点」更正为五个（`liquidations.json` 随引擎记录一起生效，无独立还原代码） |
| 10（最终重建 + 文档） | 7 | 5 ✓ / 1 ✓ / 1 ⚠️ | 盘上 store 已是 int64 轴；`{zarr}.crsp_symbology_report.json` **不存在**（`ls data/data/us_equity/1d/` 只剩 `.chunks.json` / `.crsp_adjustment.json` / `.crsp_filter_report.json` / `.crsp_tickers.json`）；`.crsp_tickers.json` 存在且只覆盖面板 PERMNO；store mtime 2026-09-21 03:34，早于 plan 10 合入的 03:58，时序自洽。文档机械核对通过：`example/` 下 `symbology_report` **零命中**，`crsp_tickers.json` 出现 8 次，`example/backtest.md` 的 `axis_symbol` 3 次，`example/constituent.md` 的 `missing_labels` 2 次。⚠️ 第 7 条 backstop（指纹 warning）见下 |
| 11（记录与回归门） | 6 | 5 ✓ / 1 ⚠️ | 见 G11/G12。D-21 未采纳 → backlog todo 已落地 `.planning/todos/pending/2026-09-21-cross-vendor-permno-ticker-merge-is-silently-all-nan.md`；D-20 保持默认。回归门：编排器独立测得 **55 failed / 1668 passed**，失败节点集与 phase 前 `ba4f5b3` 基线**逐字节相同** → 本 phase 净新增失败 **0**。⚠️ 第 6 条 backstop（指纹 warning）见下 |

**Part B 得分：77/79**（2 条未达 = plan 10 与 plan 11 各自声明的同一条指纹 backstop）

---

### Deferred Items

无。本 phase 的 must_haves 没有落到后续 phase 的项。

---

### Advisory（新范围、无确定性证据 / 既有技术债）

| # | Finding | Category | Why Advisory |
|---|---------|----------|--------------|
| 1 | `BaseStoreRebuilder.clear()` 吞掉删除失败，仍把未删路径记进「证据」对象（rebuild.py:203-230） | other | 机制已读证复现，但本轮未触发（盘上产物七项测量全部正确）。属于假绿通道，不是已发生的缺陷 |
| 2 | `normalize_to_axis_dtype` 对 float/bool 静默强转（symbol_axis.py:143-168，code review WR-02） | other | 未被任何 must_have 覆盖；本次未复现 |
| 3 | `tests/test_factor_hierarchy.py` 自 `2e2ea12`（2026-09-15，**早于本 phase**）起 ImportError 无法 collect | other | 回归门的 `--ignore` 正当，但它挡掉的是一份彻底失效的文件。既有技术债，应单独立项 |
| 4 | 回归门里 55 条既有失败（52 × `ModuleNotFoundError`：`scripts/` 不在 pythonpath；3 × `KeyError: 'amount'`） | other | 编排器确认与 phase 前基线逐字节相同，本 phase 净新增 0。既有技术债 |

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `quantlab/base/rebuild.py` | 重建 ABC | ✓ VERIFIED | 274 行，`BaseStoreRebuilder(ABC)` + 4 abstractmethod，被 `crsp_rebuild.py` 继承 |
| `quantlab/dataset/crsp_rebuild.py` | CRSP 具体重建器 | ✓ VERIFIED | 173 行，`CrspStoreRebuilder(BaseStoreRebuilder)`，实现四个 hook |
| `quantlab/utils/symbol_axis.py` | 数值序 + dtype 归一化契约 | ✓ VERIFIED | 182 行，被 `backend.py:13`、`model.py:27`、`crsp.py` 消费 |
| `quantlab/dataset/crsp.py` | PERMNO 轴 + 身份解析清空 | ✓ VERIFIED | 1780 行（迁移前含整套身份解析），核心 cast 在 :568 |
| `quantlab/dataset/crsp_symbology.py` | 收缩成区间提供者 | ✓ VERIFIED | 152 行，只剩 `symbol_intervals()` |
| `quantlab/dataset/crsp_tickers.py` | ticker sidecar 读侧 | ✓ VERIFIED | 205 行，`CrspTickerLookup` 类，`as_of` / `label` / `beside_store` / `payload` / `product_end` |
| `quantlab/factor/universe_filter.py` | 只删条件 (a) | ✓ VERIFIED | 597 行，(b)(c) + 图改写完好 |
| `quantlab/base/config.py` | `roster_universe` 改名 | ✓ VERIFIED | :191 `roster_universe: str \| None = None` |
| `tests/test_no_identity_residue.py` | 仓库级零残留 gate | ✓ VERIFIED | 12 个名字的 grep gate，计入注释，实跑 passed |
| `tests/test_crsp_ticker_sidecar.py` | sidecar 写侧 + 读侧 | ✓ VERIFIED | 19 个测试，实跑 passed |
| `tests/test_crsp_rebuild_measurements.py` | W0 真数据门 | ✓ VERIFIED（非破坏性复现） | 该文件会**删除并重建**真实 store，故不由验证器执行；我改为直接在盘上产物复现它的全部断言，七项数字全部吻合 |
| `example/wrds_crsp.md` | 16 处同步 + 反转记录 | ⚠️ 机械检查通过，散文准确性待人读 | 见 human_verification #2 |
| `.planning/REQUIREMENTS.md` | 只追加 advanced-by | ✓ VERIFIED | :23 仍 `- [ ]`，:139 追加且逐字保留 Pending |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `crsp.py:568` | 落盘 zarr `symbol` 轴 | `pl.col("symbol").cast(pl.Int64)` | ✓ WIRED | 盘上 raw zarr dtype = int64 |
| `XrBackend.widen_symbol_axis` | `symbol_axis.normalize_to_axis_dtype` | backend.py:502 | ✓ WIRED | 无条件 `str()` 已被取代 |
| `CrspSymbology.symbol_intervals()` | `{zarr}.crsp_tickers.json` | `crsp.py:_ticker_intervals` → `_write_identity_reports` | ✓ WIRED | 盘上 sidecar 由该链路产出，520 个 PERMNO |
| `CrspTickerLookup.label()` | 五个人可见点 | 注入式（backtester → model）+ 直接持有（mask / inspector） | ✓ WIRED | engine_vectorbt.py:303、model.py:1312、masking.py:232、inspector.py:538、cli.py:133 |
| `dataset/constituent.py:_build_intervals` | `CrspMembership.permno_intervals()` | :172 / :219 | ✓ WIRED | `symbol_intervals()` 已删除 |
| `BaseFactorConfig.symbols` | 拒绝（挡在 `filter_by_symbol` 之前） | `base/factor.py:113` 读 `REJECTED_FACTOR_CONFIG_FIELDS` | ✓ WIRED | 厂商侧声明，base 侧零厂商名 |
| `CrspStockDataset.config` setter | `config.symbols` 拒绝 | crsp.py:428-440 | ✓ WIRED | 赋值期抛错并点名 `config.permnos` |
| `base/backtest.py` | `quantlab.dataset.crsp_tickers.CrspTickerLookup` | :15 硬 import | ⚠️ WIRED BUT COUPLED | 功能连通，但违反 CLAUDE.md 单向分层（见 WR-03 / human_verification #3） |
| `roster_universe` | CLI 绑定 | `scripts/ingest_wrds_crsp.py:501` | ✓ WIRED | |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `wrds_crsp_sp500_1d.zarr` | `symbol` 轴 | `crsp.py:568` cast 自 raw tier 的 PERMNO 列 | ✓（实测 `[10104 10107 …]`，int64） | ✓ FLOWING |
| `.crsp_tickers.json` | `intervals` | `CrspSymbology.symbol_intervals()` ← `stksecurityinfohist` 参考表 | ✓（520 键，519 键有多条区间，13407 有 FB/META 两段） | ✓ FLOWING |
| `.crsp_filter_report.json` | `admitted_without_ticker` | `_render_admitted_without_ticker` ← 无区间覆盖的 PERMNO | ✓（本窗口真值为 0，非硬编码） | ✓ FLOWING |
| `.crsp_filter_report.json` | `roster_overrides[*].symbol` | `_permno_breakdown` 的 `pl.col("symbol").last()` | ⚠️ 值与 key 逐字相同（`"75154" → symbol: "75154"`），人可读名称丢失 | ⚠️ DEGRADED（非断流；见 WR-04 / human_verification #5） |
| `UniverseMask.report()` | `missing_labels` | `CrspTickerLookup.label()`；无 sidecar 时回落原拼写 | ✓ | ✓ FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| 盘上 store 是 int64 PERMNO 轴 | `xr.open_zarr(...)` 读 dims / dtype / data_vars | 252×520，27 vars，symbol dtype int64，`permno` 不在 data_vars | ✓ PASS |
| ticker sidecar 是区间表且 as-of 正确 | `CrspTickerLookup.beside_store(...).as_of(13407, …)` | 2022-06-08 → `FB`；2022-06-09 → `META` | ✓ PASS |
| `label()` 对未知 PERMNO 回落成数字 | `.label([13407,10107,99999999], 2023-01-03)` | `['META','MSFT','99999999']` | ✓ PASS |
| 缺 sidecar 时抛带补救动作的 FileNotFoundError | `CrspTickerLookup('/tmp/nope…').as_of(...)` | `FileNotFoundError: CrspTickerLookup: no ticker sidecar at '…'. It is written by the CRSP conversion beside the store…` | ✓ PASS |
| W0 七项清洁度测量（非破坏性复现） | 直接在盘上 store 上按 `cleaning.REQUIRED_COLUMNS` 计算 | `structural_gaps=764 == adj_close_nan == adj_volume_nan`；`adj_close_le_zero=0`；`close_eq_zero=0`；`anomaly_flag_true=13`（plan 10 禁止放宽的那个数）；`symbol_count=520` | ✓ PASS |
| 回收 ticker → 两列 | `pytest -k test_a_recycled_ticker_yields_two_columns` | 1 passed | ✓ PASS |
| FB→META 不断列 | `pytest -k test_permno_13407_is_one_column…` | 1 passed | ✓ PASS |
| vectorbt 接受 int64 列索引 | `pytest -k test_int64_column_index_round_trips_through_vectorbt` | 1 passed | ✓ PASS |
| int64 轴宽化不清空 store + superset 守卫真的守 | `pytest tests/test_symbol_axis_widening.py` | 22 passed | ✓ PASS |
| 相位相关全量测试 | `pytest -q` × 14 个文件（tracer / identity / ticker_sidecar / residue / symbol_axis ×2 / crsp_dataset / symbology / universe_filtered / universe_mask / crsp_constituent / model_predict_panel / extensibility / crsp_rebuild） | **256 passed** | ✓ PASS |
| 接管台账 22/23/24 点名的文件 | `pytest tests/test_ingest_wrds_crsp.py tests/test_no_identity_residue.py` | 32 passed | ✓ PASS |
| SIMD 宽度 backstop | `520 % 8 == 0`；`pytest tests/test_factor_stream.py tests/test_cross_sectional_zscore.py` | 0；12 passed | ✓ PASS |
| SYMBOL_COORD_ENCODINGS 其余 5 个消费者 | `pytest` × 5 文件 | 72 passed | ✓ PASS |
| 全仓回归门 | 编排器独立测量（phase 前后各一次） | 55 failed / 1668 passed，失败集与基线逐字节相同 → 净新增 **0** | ✓ PASS |
| WR-01 复现（对抗性） | 三个可解析但结构错误的 sidecar 喂给 `label()` | `{"intervals":[1,2,3]}` → `AttributeError`；`{"intervals":{"13407":[{"ticker":"FB"}]}}` → `KeyError: 'start'`；`[]` → `AttributeError` | ✗ FAIL（见 human_verification #4） |
| WR-04 复现（对抗性） | 读盘上 `.crsp_filter_report.json` | `roster_overrides.permnos["75154"].symbol == "75154"` —— key 与 value 逐字相同 | ✗ FAIL（见 human_verification #5） |
| 指纹失效 warning 是否掩盖真实问题 | 无可执行检查（需跑一个既有 backtest run 并人读输出） | — | ? SKIP → 人工 |

### Probe Execution

| Probe | Command | Result | Status |
|-------|---------|--------|--------|
| — | — | 本 phase 未声明 `scripts/*/tests/probe-*.sh` 形态的探针；`find scripts -path '*/tests/probe-*.sh'` 无命中 | N/A |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| DATA-03 | 01–11 全部 plan 的 `requirements:` | 数据层抽象按市场/频率扩展，上层因子/模型/回测不需改动 | ✓ **正确保持 Pending（advanced，未完成）** | `.planning/REQUIREMENTS.md:23` 仍 `- [ ]`。:139 追踪行追加了 03.11 的 advance 记录并逐字以「Status unchanged: still Pending, still owned by Phase 2」结尾。plan 03.11-11 的 `requirements-completed: []`（而非从自身 frontmatter 复制 `[DATA-03]`）是**刻意的**：它是最后一个声明 DATA-03 的 plan，复制会释放共享 ID 门并把 DATA-03 翻成 Complete —— 这正是 T-03.11-38（severity high）点名的危害。ROADMAP 03.11 段落逐字写着「No requirement is COMPLETED here」。**与设计一致，不作为缺口。** |

**孤儿需求检查：** `grep -E "Phase 03.11" .planning/REQUIREMENTS.md` 只映射 DATA-03，已被每个 plan 声明。**无孤儿需求。**

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| （本 phase 改动的 53 个文件） | — | `TBD` / `FIXME` / `XXX` | — | **零命中** |
| （同上） | — | `TODO` / `HACK` / `PLACEHOLDER` | — | **零命中** |
| `quantlab/base/backtest.py` | 15 | 核心层 `base/` 硬 import 厂商具体类 `CrspTickerLookup` | ⚠️ Warning | 违反 CLAUDE.md 单向分层。注：该文件 phase 前已 import `XrBackend`（通用存储后端），本 phase 把耦合推进到**点名厂商**的模块。同一 phase 在 `base/factor.py:80-88` 用注入/声明式明确避开了同一问题并写了理由——两种做法并存是不一致，不是纯粹的既有债。`tests/test_extensibility_contract.py` 的 `CORE_LAYER_FILES` 只含 `base/factor.py`/`base/model.py`/`base/backend.py`，`base/backtest.py` 在门外，所以门不会红。**plan 09 的 SUMMARY 如实披露了这一点**（:203 逐字写 `base/backtest.py == 4`），并把纯净性断言明确限定在 `base/model.py`（我已核实该文件 `CrspTickerLookup` 零出现）—— 不是隐瞒 |
| `quantlab/dataset/crsp_tickers.py` | 185-205 / 161-168 | docstring 承诺「Never raises」，实测对可解析但结构错误的 sidecar 抛 `KeyError` / `AttributeError` | ⚠️ Warning | 三个展示调用点依赖该不变量，其中 `DLModel._spell`（model.py:1312）在**构造 ValueError 消息的过程中**被调用——抛错会顶掉真正的诊断信息。计划原文的 T-03.11-30 只覆盖「sidecar 缺失」（该路径实测正确），但 `03.11-09-SUMMARY.md:145` 把它写成「缺失/**损坏**时五个点全部优雅回落」，对「结构损坏」这一半不成立 |
| `quantlab/dataset/crsp.py` | 1239 | `pl.col("symbol").last()` 在 PERMNO 轴上使审计报告的 `symbol` 退化成 key 的副本 | ⚠️ Warning | 盘上已复现。`tests/test_crsp_identity.py:585-587` 把它交接给 plan 09 的 sidecar，plan 09 的展示点清单不含 filter report，交接落空，且该测试现在把退化输出锁成既定行为 |
| `quantlab/base/rebuild.py` | 203-230 | `clear()` 先记后删 + `ignore_errors=True`，删除失败被吞并报告成已删 | 📋 Advisory | 假绿通道；本轮未触发 |

---

### Human Verification Required

#### 1. 指纹失效 warning 是否掩盖真实问题（phase 自己的 backstop，两次声明均未被人读过）

**Test:** 在 PERMNO 轴重建后的 store 上重跑一个既有 backtest run（load 模式），读完 `_compare_fingerprints`（`quantlab/base/backtest.py:1145-1188`）打出的全部 warning。
**Expected:** 每一条 warning 都能归因到 D-12 已接受的指纹失效（`digest` / `n_symbols` 变了），没有别的真实问题被裹在刷屏里。
**Why human:** 没有任何测试驱动这条 warning 路径；`03.11-11-SUMMARY.md:119 / :238 / :329` 自己标注该项是按 operator 类别授权 **auto-approved、无人逐字读过**（T-03.11-37）。plan 10 与 plan 11 各自把它声明为 `verification: backstop`，两次都无证据。

#### 2. `example/wrds_crsp.md` 16 处改写的散文准确性

**Test:** 逐字读本 phase 改动的 16 处（其中 5 处是整段重写），核对它们描述的是当前代码而不是迁移前的行为。
**Expected:** 16 处全部与 `crsp.py` / `crsp_tickers.py` / `crsp_symbology.py` 的现状一致。
**Why human:** 机械检查已通过（`example/` 下 `symbology_report` 零残留、`crsp_tickers.json` 出现 8 次、反转记录整节存在），但散文的**正确性**只有人能判；`03.11-11-SUMMARY.md:119` 明确标注该项 auto-approved、无人逐字读过。

#### 3. 决策：`base/backtest.py` 的厂商硬依赖（code review WR-03，本验证已独立复现）

**Test:** 裁定 `quantlab/base/backtest.py:15` 的 `from quantlab.dataset.crsp_tickers import CrspTickerLookup` —— 接受，还是改成与 `base/factor.py:80-115` 同一形态（dataset 侧声明、base 侧只读一个映射/可调用）。
**Expected:** 明确裁定：接受（并把 `base/backtest.py` 写进 `CORE_LAYER_FILES` 的豁免记录，让门与规则口径一致），或开一条 gap-closure / backlog 把耦合改成注入式。
**Why human:** CLAUDE.md 的单向分层约束 vs. 本 phase 自己在 `base/factor.py` 示范的干净写法——同一个 phase 用了两种做法。不是任何 must_have 的失败（plan 09 的纯净性真值只声明 `base/model.py`，该声明属实），取舍是设计判断。

#### 4. 决策：`CrspTickerLookup.label()` 的「Never raises」契约（code review WR-01，本验证已独立复现）

**Test:** 已复现三例——`{"intervals":[1,2,3]}` → `AttributeError`、`{"intervals":{"13407":[{"ticker":"FB"}]}}` → `KeyError: 'start'`、顶层 `[]` → `AttributeError`。现在修，还是记 backlog。
**Expected:** 要么把 guard 扩到整个函数体、`as_of` 容忍缺键，并补一条 `{"intervals": {"13407": [{"ticker": "FB"}]}}` → `label()` 返回 `["13407"]` 的测试；要么立 todo 并把 docstring 的「Never raises」改成它实际成立的范围。
**Why human:** 不是 must_have 失败（T-03.11-30 的计划原文只覆盖「sidecar 缺失」，缺失路径实测正确），但 SUMMARY 的「缺失/损坏」表述有一半不成立，且失败点在一个会顶掉真实诊断的位置。修不修是优先级判断。

#### 5. 决策：审计报告丢掉的人可读名字（code review WR-04，本验证已在盘上产物复现）

**Test:** 盘上 `.crsp_filter_report.json` 的 `roster_overrides.permnos["75154"].symbol == "75154"`。补 ticker，还是接受。
**Expected:** 要么让 `_permno_breakdown` 经 `CrspTickerLookup` 补一个 ticker 字段，要么明确接受并把 `tests/test_crsp_identity.py:585-587` 那句「留给 plan 09」的注释改成「已裁定不做」。
**Why human:** 交接链断了（测试交给 plan 09，plan 09 的展示点清单不含它），且当前测试把退化输出锁成既定行为。不是 must_have 失败——plan 07 只要求**保留** `_permno_breakdown`，它确实保留了。

#### 6. 清理：WINDOWS.md 的 22 / 23 / 24 仍标 `open`

**Test:** 这三条点名的接管工作已完成（`tests/test_ingest_wrds_crsp.py` + `tests/test_no_identity_residue.py` 本次实跑 **32 passed**，`DELETED_IN_03_11_08` 已并入 `DELETED_IN_03_11_07`）。
**Expected:** 标成 `fixed` 并写明证据，或说明为什么留开。
**Why human:** 台账状态与代码事实不符。方向是安全的（留开而不是被静默关闭），但改台账需要一次人为确认。

---

### Gaps Summary

**没有缺口。相位目标在代码里成立。**

我对 SUMMARY 的叙事做了对抗性证伪，核心的三条主张全部独立复现而不是采信：

1. **轴确实换了。** 不是「文件存在」，而是盘上 zarr 的 `symbol` 数组 raw dtype 是 int64、值是 PERMNO、`permno` 已从 27 个 data_vars 里消失；核心两行 `crsp.py:568` 是一次 `cast(pl.Int64)`，`drop("symbol")` + `label_rows` 的组合已不在代码里。
2. **旧机制确实被删了，不是关掉。** 12 个名字的仓库级 grep gate 实跑绿；我自己再 grep 一遍 `quantlab/ scripts/ example/`，唯一命中是反转记录段落里解释「`label_rows` 为什么被删」的历史文字。`CrspSymbology` 从整套身份解析收缩到 152 行、只剩 `symbol_intervals()`。
3. **ticker 确实进了旁车而且真的能 as-of。** 我在真实 sidecar 上跑了 `as_of(13407, 2022-06-08) → FB` / `2022-06-09 → META`，520 个 PERMNO 与面板集合相等，519 个有多条区间——是区间表，不是被 D-03 否决的「最后一个 ticker」。

**DATA-03 正确保持 Pending**，追踪行只追加了 advance 记录，状态标记与计数未动——这是设计如此（plan 11 的 `requirements-completed: []` 刻意不复制自身 frontmatter 的 `[DATA-03]`，以免释放共享 ID 门），不是遗漏。

**状态是 `human_needed` 而不是 `passed`，有两类原因：**

- **相位自己留下的两条无证据 backstop。** 指纹失效 warning 是否掩盖真实问题，plan 10 与 plan 11 各声明一次，两次都没有测试、也没有人读过——`03.11-11-SUMMARY.md` 自己如实标注了这个缺口。`example/wrds_crsp.md` 的 16 处改写同理：机械检查全过，散文准确性无人核对。这两条恰恰是 SUMMARY **诚实标注**出来的，值得表扬，但它们仍然是未闭合的验证项。
- **code review 的两条警告，我独立复现了，且都不是 must_have 失败。** WR-03（`base/backtest.py` 硬 import 厂商类）与 WR-04（审计报告的 `symbol` 退化成 key 的副本）。前者违反的是 CLAUDE.md 的架构约束而不是本 phase 声明的任何真值，且 plan 09 的 SUMMARY 如实披露了命中数、把纯净性断言明确限定在 `base/model.py`；后者的交接链在 plan 09 断掉，现在被测试锁成既定行为。两条都需要一次人为裁定（接受 / 修 / 记 backlog），而不是一轮 gap-closure。另外我额外复现了 WR-01（`label()` 的「Never raises」在结构损坏的 sidecar 上不成立），一并交给同一次裁定。

**回归门没有退步：** 编排器在 `ba4f5b3` 与每次 wave 合入后各测一次，最终失败节点集与 phase 前基线逐字节相同，净新增失败 **0**。那 55 条是既有债（52 × `scripts/` 不在 pythonpath、3 × `KeyError: 'amount'`），另有一条更早的债——`tests/test_factor_hierarchy.py` 自 2026-09-15 的 `2e2ea12` 起就已 ImportError 无法 collect，这使得回归门的 `--ignore` 正当，但它挡掉的是一份彻底失效的文件，应单独立项。

---

_Verified: 2026-09-21T11:30:11Z_
_Verifier: Claude (gsd-verifier)_
