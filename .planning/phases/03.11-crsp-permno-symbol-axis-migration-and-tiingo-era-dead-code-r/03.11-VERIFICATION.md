---
phase: 03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r
verified: 2026-09-22T00:41:18Z
status: passed
score: 134/136 must-haves verified
covered_files:
  - ".planning/REQUIREMENTS.md"
  - ".planning/ROADMAP.md"
  - ".planning/STATE.md"
  - ".planning/WINDOWS.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-01-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-01-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-02-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-02-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-03-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-03-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-04-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-04-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-05-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-05-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-06-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-06-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-07-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-07-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-08-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-08-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-09-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-09-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-10-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-10-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-11-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-11-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-12-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-12-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-13-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-13-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-14-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-14-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-15-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-15-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-16-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-16-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-17-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-17-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-18-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-18-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-REVIEW.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-UAT.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/deferred-items.md"
  - "example/wrds_crsp.md"
  - "quantlab/backtest/engine_vectorbt.py"
  - "quantlab/base/backtest.py"
  - "quantlab/base/model.py"
  - "quantlab/dataset/crsp.py"
  - "quantlab/dataset/crsp_tickers.py"
  - "quantlab/dataset/masking.py"
  - "tests/conftest.py"
  - "tests/test_crsp_identity.py"
  - "tests/test_crsp_rebuild_measurements.py"
  - "tests/test_crsp_ticker_sidecar.py"
  - "tests/test_extensibility_contract.py"
  - "tests/test_model_predict_panel.py"
  - "tests/test_stale_bytecode_lesson.py"
covered_digest: "v1:sha256:e32834c6c25f1908b465ad4b9c5e18877d7149310988b5c50fd4d6f492fbf3a5"
behavior_unverified: 2
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 88/91
  previous_verified: 2026-09-21T21:24:31Z
  gaps_closed:
    - "G-03.11-3 —— WR-01：`RecursionError` 已加进 `payload` 属性的 except 元组（crsp_tickers.py:206），本验证实测深嵌套 JSON 下 `label()` 返回 `['13407']` 不抛、`as_of()` 抛 shaped `ValueError`；WR-02：`_UNUSABLE = (FileNotFoundError, ValueError)` 是模块常量（:119），两处 `except` 共用，本模块内部 bug 实测**上抛**而非静默降级"
    - "G-03.11-4 —— 投毒字节码已清除。本验证独立核对 `.pyc` header（mtime/size 与当前源码一致且内容含本轮新增符号），四 suite 在**默认缓存目录**下实测 120 passed，与编排器主树基线逐字相同；`tests/test_stale_bytecode_lesson.py` 3 passed"
    - "G-03.11-5 —— 盘上 `.crsp_filter_report.json` 已由当前 HEAD 重生成。本验证直接读盘：`roster_overrides.permnos` 1 条（PERMNO 75154），键集恰为 `['first','last','rows','types']`，`symbol` 字段消失；键集断言已进常设真实数据门"
    - "G-03.11-6 —— WR-03：两处 `except _UNUSABLE as exc` 均绑定异常并交给 `_degrade(exc)`，实测损坏/缺失两支各恰好 1 条 WARNING、非整数透传 0 条；WR-04：`_object_payload()`（:283）是顶层形状唯一检查点，`[]` 下 `product_end` 实测抛 shaped `ValueError`"
    - "IN-02 —— `tests/test_crsp_identity.py:1109` 补齐 `set(dropped) == {\"types\",\"rows\",\"first\",\"last\"}`，与 :591 / :1495 两个兄弟完全同强"
    - "IN-03 —— `tests/test_extensibility_contract.py:52-63` 明写这个门只看得见下方四个 legacy market 字面量，并把「门是绿的」与「文件已被证明干净」分开；`FORBIDDEN_SUBSTRINGS` 仍是原四项"
    - "IN-04 —— 模块 docstring 点名的 `test_core_layer_purity_no_market_specific_logic` 实际存在（grep 命中 1）"
    - "IN-01 —— 「三个调用点 / 六条消息」的计数在 `crsp_tickers.py`（:44/:50/:403/:407）、`base/model.py:1311-1314`、`tests/test_model_predict_panel.py:725/:729`、`tests/test_crsp_ticker_sidecar.py:386/:392` 全部一致；实测 `grep -rn '\\.label(' quantlab/` 为 2 处 + `base/model.py` 经 `symbol_labeller` 一处"
  gaps_remaining: []
  regressions:
    - "无。相位目标 12 条真值在 plan 18 重建后的盘上产物上逐条复检仍成立（Part A）"
    - "前置失败集无扩散：本验证独立复跑 D-03.11-12-A 的 9 个文件得 52 failed，逐文件计数（14/11/10/6/5/2/2/1/1）与台账逐字相同，+ `test_factor_kunquant.py` 的 3 条 = 55"
  new_findings:
    - "W-1（台账风险，需裁定）：plans 12-18 的 SUMMARY frontmatter 全部写着 `requirements-completed: [DATA-03]`，其中 plan 18 是最后一个声明 DATA-03 的 plan —— 正是 plan 11 以 T-03.11-38 识别并刻意置空 `[]` 来规避的 shared-ID 门释放条件。`.planning/REQUIREMENTS.md` 目前仍正确（181 行、7 个 `[x]`、DATA-03 仍 `- [ ]`、最后一次改动停在 plan 11 的 f32f6a5），但门的触发条件已重新具备"
    - "W-2（台账不实，已立项未修）：`deferred-items.md` D-03.11-12-A 散文称 55 条前置失败「Every one of the 55 is a ModuleNotFoundError … or the FileNotFoundError twin」，本验证实测错误类分布为 ModuleNotFoundError 26 / FileNotFoundError 17 / AssertionError 8。逐文件计数一致，措辞不实。已由 plan 14 立为 `.planning/WINDOWS.md` 第 29 条（open）"
advisory:
  - finding: "`example/wrds_crsp.md:247-249` 的 `label()` 契约活副本未同步 WR-03 新增的一次性降级 WARNING —— 散文仍只写「永不抛、回落成数字」，读者据此不会知道控制台上现在能区分「旁车损坏」与「本来就没有旁车」"
    category: other
    reason: "plan 17 的 `files_modified` 不含 example/wrds_crsp.md，是刻意的范围边界而非遗漏。散文没有说错，只是少说了一半；建议下次碰这份文档时补一句"
    evidence_status: "none provided"
  - finding: "`03.11-17-SUMMARY.md:102/:165` 记录的 `grep -c '_object_payload()' == 2`，在当前 HEAD 上实测为 3"
    category: other
    reason: "第三处是同一 plan 更晚一次提交（f2379fe「契约散文收口」）加进模块 docstring:37 的引用，不是第三个调用点。实质性质（两个读者、`isinstance(payload, dict)` 计数 1、`self.payload.get(` 计数 0）本验证独立复核全部成立。SUMMARY 里的数字是取自更早一次提交的过期测量"
    evidence_status: "none provided"
  - finding: "编排器交接里「`.planning/WINDOWS.md` 第 28 条（custom store 仍在迁移前 ticker 轴）」这条 carried-forward 项与盘上不符"
    category: other
    reason: "WINDOWS.md:45 第 28 条状态实为 `fixed`（03.11-11 Task 2 操作者裁定删除，已备份至 data/_backup_deleted_custom_store_03.11/，删除后 1d/ 目录内 crsp_symbology_report 命中数为 0）。这是好消息不是 gap：carried-forward 清单应移除这一条"
    evidence_status: "none provided"
behavior_unverified_items:
  - truth: "既有 backtest run 的指纹失效会让 `_compare_fingerprints` 刷一屏 warning（D-12 已接受），且它没有掩盖真实问题（plan 10 backstop）"
    test: "在 plan 18 重建后的 PERMNO 轴 store 上重跑一个既有 backtest run（load 模式），逐条读完 `_compare_fingerprints` 打出的全部 warning"
    expected: "每一条 warning 都能归因到 D-12 已接受的指纹失效（digest / n_symbols 变了），没有一条是别的真实问题被裹在里面"
    why_human: "`quantlab/base/backtest.py:1175` 的 warning 路径在真实 store 上无测试驱动（`tests/test_backtest_rebuild.py:513 test_changed_store_rebuild_warns_and_completes` 只在合成数据上覆盖 warn-and-complete，不回答「真实 store 上这一屏 warning 是否都可归因」）。03.11-UAT.md test 4 至今 `blocked_by: other`，从未跑过；plan 18 又重建了一次 store，这笔债没有缩小"
  - truth: "同一条 backstop 在 plan 11 的重复声明"
    test: "同上"
    expected: "同上"
    why_human: "同上 —— 两次声明、两次都未被人执行；这是上一轮扣掉的两条，本轮仍然扣掉"
human_verification:
  - test: "**裁定（W-1）**：plans 12-18 的 SUMMARY `requirements-completed: [DATA-03]` 要不要按 plan 11 的做法统一置空？"
    expected: "一个明确的处置。相位目标逐字写着「No requirement is COMPLETED here」，plan 11 因此把自己的 `requirements-completed` 置空并在 Deviation 1 里写明理由（T-03.11-38：shared-ID 门会在最后一个声明该 ID 的 plan 产出 SUMMARY 时释放，进而把 DATA-03 翻成 Complete）。plans 12-18 全部照抄了 `[DATA-03]`，而 plan 18 正是最后一个。REQUIREMENTS.md 目前未被改动，但 ship/roll-up 阶段如果读这些字段，DATA-03 会被错误签收"
    why_human: "这是台账处置而不是代码缺陷：要么把 12-18 七份 SUMMARY 的该字段置空并补一行说明，要么操作者明确接受「字段不权威、只有 REQUIREMENTS.md 权威」。两种都自洽，但必须选一个"
  - test: "**执行（UAT test 4，仍 blocked）**：在重建后的 store 上重跑一个既有 backtest run（load 模式），逐条读完 `_compare_fingerprints` 的 warning"
    expected: "每条 warning 都能归因到 D-12 已接受的指纹失效，没有别的真实问题被裹在里面"
    why_human: "见 behavior_unverified_items。这是 134/136 扣掉的两条"
  - test: "**确认（backstop 抽象条目）**：plans 14-18 的 16 条 `verification: backstop` 声明中，有 6 条本验证无法事后观察，按 backstop 规则如实记为「未证」而非「通过」"
    expected: "操作者确认这 6 条不需要额外证据：plan 14 的两条（清缓存动作的串行性与幂等性 —— 动作已完成，事后不可观察）、plan 15 的 concurrency（`_payload` 惰性填充在竞态下最坏重复读盘）、plan 16 的 concurrency、plan 17 的 concurrency 运行时（`_degraded` 读改写非原子）、plan 18 的 idempotency（逐字节确定性，SUMMARY 自述本轮未逐字节比对两代产物）"
    why_human: "backstop 是外生标签，按 honest-verifier 规则不得以「存在 + 接线」充当证据。另外 10 条 backstop 本验证有直接观察或构造证据（见 Part C 表），不在此列"
gaps: []
deferred:
  - truth: "全量 suite 的 55 条前置失败（`ingest_*` 相关）"
    addressed_in: "deferred-items.md D-03.11-12-A"
    evidence: "本验证独立复跑 9 个文件得 52 failed，逐文件计数与台账逐字相同；第 10 个文件 `test_factor_kunquant.py` 的 3 条未跑（KunQuant，见 D-03.11-12-B）。合计 55，与编排器主树测量一致，零新增"
  - truth: "`tests/test_cross_sectional_zscore.py` 在 KunQuant `~MultiThreadExecutor()` 偶发死锁"
    addressed_in: "deferred-items.md D-03.11-12-B"
    evidence: "偶发、前置、与本相位任一 plan 无因果；本验证据此**未**重跑全量 suite，改用编排器主树后合并测量 + 9 文件定向复跑交叉验证"
  - truth: "`FactorConfig.njobs` 默认 128"
    addressed_in: "deferred-items.md D-03.11-12-C"
    evidence: "生产代码、在本相位任一 plan 的 files_modified 之外，需自己的 plan 选替代默认值并重测吞吐"
  - truth: "回归门在主树与 worktree 之间的 ±1"
    addressed_in: "deferred-items.md D-03.11-18-A"
    evidence: "本验证独立复现：`tests/test_entry_point_contracts.py` 按 `REPO_ROOT.glob(\"*.py\")` 读**工作树**参数化，主树多一个未跟踪的 `jerry_query_data.py`（`git status --porcelain` 干净、该文件不在 git 索引内），实测该文件出现在参数化列表中、该文件收集数为 8"
---

# Phase 03.11：CRSP PERMNO 身份轴迁移与 Tiingo 期死代码清除 —— 验证报告（第二轮 gap 收口后）

**相位目标**（ROADMAP.md 逐字）：CRSP 价格面板的 `symbol` 维变成 **int64 PERMNO**，所有只为「让被回收/改名的 ticker 还能当身份轴」而存在的适配一律**删除**而不是藏进兼容分支；`ticker` 完全离开面板，进旁车做 as-of 展示查询。**本相位不完成任何 requirement。**

**验证时间：** 2026-09-22T00:41:18Z
**状态：** human_needed
**重验：** 是 —— 第二轮 gap 收口（plans 14-18）之后
**测量环境：** 主工作树 `/Users/daizhaorong/projects/quantlab`，全部命令以相对路径从仓库根执行，**默认字节码缓存目录**（未使用 `-X pycache_prefix`）。工作树 `git status --porcelain` 为空，HEAD = `809ad54`。

---

## 方法与纪律声明

1. **未采信任何 SUMMARY。** 下表每一条 WR-01/02/03/04 的判定都来自本验证自己写的行为探针（临时脚本，不改仓库），而不是读 SUMMARY 的结论。
2. **字节码纪律。** 开工第一件事是核对 `quantlab/dataset/__pycache__/crsp_tickers.cpython-313.pyc` 的 header：记录的 source mtime/size = `1790034730 / 26506`，与工作树源码**逐字一致**，且缓存内容含本轮新增符号（`_UNUSABLE` / `_degrade` / `_object_payload` 经 import 实测可达）。全部测量在默认缓存目录下取得。
3. **路径纪律。** 未进入任何 worktree，未使用任何绝对路径 verify 命令。
4. **未跑 `tests/test_crsp_rebuild_measurements.py`。** 按交接指令，该门是对真实 store 的破坏性重建。本验证改为：静态核对其内容 + 直接读盘核对它断言的产物 + 只跑其中唯一一条 `tmp_path` 单测（`-k fresh_backup`，1 passed）。
5. **未跑全量 suite。** 按 D-03.11-12-B（KunQuant 偶发死锁，上次挂了 2h02m）与「full run 至多一次」规则，改为定向复跑 D-03.11-12-A 的 9 个文件交叉验证编排器的主树后合并基线。

---

## Goal Achievement

### Part A —— 相位目标可观察真值（12 条，回归复检）

plan 18 在本轮重建了真实 store，所以这 12 条必须在**重建后的盘上产物**上重新观察，不能沿用上一轮的读数。

| # | Truth | 状态 | 本轮实测证据 |
|---|-------|------|------------|
| G1 | 面板 `symbol` 维是 int64 PERMNO | ✓ VERIFIED | 重建后实读 `wrds_crsp_sp500_1d.zarr`：dims 252×520，`symbol` dtype **int64**，值 `[10104 10107 10138 10145 10516 …]` |
| G2 | `permno` 不再是数据变量 | ✓ VERIFIED | `'permno' in ds.data_vars` → **False**，`len(ds.data_vars)` = **27** |
| G3 | 被回收的 ticker 不能把两家公司拼成一列 | ✓ VERIFIED（无回归） | `tests/test_crsp_identity.py` 在四 suite 120 passed 批次内全绿 |
| G4 | 改名不断开身份（13407 跨 FB→META 是一列） | ✓ VERIFIED（无回归） | 同上批次；另本验证行为探针实测区间两端 INCLUSIVE 语义未被本轮改动触碰 |
| G5 | 身份适配机制被**删除**而非藏进兼容分支 | ✓ VERIFIED（无回归） | `grep -rn "NON_COMMON_TICKER_PATTERNS\|exclude_non_common\|is_common_ticker\|symbol_overrides" quantlab/` → **0 命中** |
| G6 | `CrspSymbology` 收缩成纯区间提供者（D-18） | ✓ VERIFIED（无回归） | 本轮未触碰该文件（`git diff --name-only fe54bf1..HEAD` 不含它） |
| G7 | `ticker` 离开面板、进旁车做 as-of | ✓ VERIFIED | 重建后 `'ticker' in ds.variables` → **False**；`{zarr}.crsp_tickers.json` 存在，键为 `{generated_from, intervals, vintage_product_end}`，`vintage_product_end = 2025-12-31` |
| G8 | sidecar 只写面板实际包含的 PERMNO | ✓ VERIFIED | 重建后 `set(intervals 键) == set(面板 symbol 轴)` → **True**，两侧各 520 |
| G9 | int64 硬破坏点修好，`widen_symbol_axis` 不静默清空 | ✓ VERIFIED（无回归） | 本轮未触碰 `quantlab/dataset/backend.py` |
| G10 | `universe_filter.py` 条件 (a) 删除、(b)(c) 保留 | ✓ VERIFIED（无回归） | 同 G5 的 0 命中 |
| G11 | 03.10 locked decision 的反转被记录、03.10 原文未改 | ✓ VERIFIED（无回归） | `roadmap.get-phase 03.11` 输出完整含「Reverses a locked decision of Phase 03.10」段落 |
| G12 | **DATA-03 仍为 Pending**（「No requirement is COMPLETED here」） | ✓ VERIFIED | `.planning/REQUIREMENTS.md`：181 行、7 个 `[x]`、:23 仍 `- [ ] **DATA-03**`、:139 追踪行结尾逐字「Status unchanged: still Pending, still owned by Phase 2」；该文件最后一次改动停在 plan 11 的 `f32f6a5`，plans 12-18 一次都没碰过它 |

**Part A 小结：12/12 成立，零回归。** 相位核心目标在代码与**重建后的盘上产物**两侧都成立。（G12 在代码侧成立，但其 SUMMARY 台账侧有风险，见 W-1。）

### Part B —— plans 01-13 must-haves（91 条，回归复检）

| 来源 | 条数 | 上一轮 | 本轮 | 变化 |
|---|---|---|---|---|
| plans 01-11 | 79 | 77 ✓ / 2 behavior-unverified | 77 ✓ / 2 behavior-unverified | 无 |
| plan 12（G-03.11-3 收口） | 6 | 5 ✓ / **1 ✗** | **6 ✓** | 12-6 由 ✗ 转 ✓ |
| plan 13（G-03.11-1/2 收口） | 6 | 6 ✓ | 6 ✓ | 无 |
| **合计** | **91** | **88 ✓** | **89 ✓ / 2 behavior-unverified** | +1 |

**唯一变化 —— plan 12-6：**

| # | Truth | 状态 | 证据（本轮实测） |
|---|-------|------|------|
| 12-6 | 「永不抛」声明的两个活副本都写明**缺失或损坏**，且声明为真 | ✓ VERIFIED（上一轮 ✗ FAILED） | 上一轮的反例是「深嵌套 JSON 属『解析不了』但实测 `label()` 抛 `RecursionError`」。本轮实测：`'{"a":' * 100000 + '1' + '}' * 100000` → `label([13407], date(2020,1,1))` 返回 **`['13407']`**、不抛；`'[' * 20000 + ']' * 20000` 同样返回 `['13407']`。声明现已为真。两个活副本亦已把这一类写进去：`crsp_tickers.py:416-418`「bytes that never reach a shape at all (undecodable, not JSON, or **nested past the parser's stack**)」、`example/wrds_crsp.md:248-249`「以及**嵌套**深到解析器自己爆栈（`RecursionError` 是 `RuntimeError` 子类，03.11-15 之前从这里逃出去过）」 |

其余 90 条本轮按重验规则做回归检查（存在性 + 基本合理性），未发现任何回退；支撑它们的四个 suite 在默认缓存目录下 **120 passed**。

### Part C —— plans 14-18 must-haves（45 条实质真值 + 16 条 backstop）

> 计分口径：45 条实质真值进分母，16 条 `verification: backstop` 声明按 honest-verifier 规则单列（10 条有直接观察/构造证据，6 条如实记为未证，见人工条目 3）。

#### plan 14（G-03.11-4：投毒字节码）—— 6/6 ✓

| # | Truth | 状态 | 证据（本轮实测） |
|---|-------|------|------|
| 14-1 | 不存在被判为「新鲜」的投毒字节码；默认缓存目录下 `test_crsp_ticker_sidecar.py` 全绿 | ✓ VERIFIED | `.pyc` header 记录 `mtime 1790034730 / size 26506` = 源码实际值，且缓存内容含本轮新增符号；四 suite 默认缓存 **120 passed**（上一轮同一命令在投毒缓存下为 5 failed / 21 passed） |
| 14-2 | 四 gap-closure suite 在默认缓存目录下一次跑出与干净缓存相同的数字 | ✓ VERIFIED | 实跑 **120 passed**，与编排器主树后合并基线**逐字相同**。（plan 14 当时记的是 98；差额是 plans 15-18 新增的用例，非回退） |
| 14-3 | 回归门 FAILED 行集与 D-03.11-12-A 的 55 条逐字相同，无新增 | ✓ VERIFIED | 定向复跑 9 个前置失败文件 → **52 failed, 208 passed, 1 skipped**，逐文件计数 `test_ingest_shells 14 / test_ingest_tiingo_universe_wiring 11 / test_data_dir_cli 10 / test_ingest_conversion_gate 6 / test_volume_guard 5 / test_chunked_ingest 2 / test_spot_dataset 2 / test_entry_point_contracts 1 / test_universe 1`，与台账表逐字相同；+ `test_factor_kunquant.py` 的 3 条 = 55。**但错误类措辞不实，见 W-2** |
| 14-4 | 「等长注入 + 恢复 mtime ⇒ .pyc 不失效」以可执行形式落地 | ✓ VERIFIED | `tests/test_stale_bytecode_lesson.py` 224 行，实跑 **3 passed**：(a) 等长编辑 + 恢复 mtime 保留旧字节码、(b) `touch` 是有效补救、(c) 干净 `pycache_prefix` 也是有效补救且不清除被污染的 `__pycache__` |
| 14-5 | `tests/conftest.py` 有与 macOS OpenMP 守卫并列的散文教训并指向那个证明 | ✓ VERIFIED | conftest.py:43-58「Stale-bytecode trap (phase 03.11, gap G-03.11-4). Prose only, deliberately」，:58 逐字「All three are proved in tests/test_stale_bytecode_lesson.py」，并写明两条安全做法 |
| 14-6 | `quantlab/` 与 `tests/` 不存在那两个等长标识符错字的残留 | ✓ VERIFIED | `grep -rn "tikcer\|paylaod" quantlab/` → **0**。`tests/` 有 3 处命中，全部是 plan 15 之后新增的**字符串字面量**（`raise KeyError("tikcer")` / `raise AttributeError("paylaod")` / 一行解释散文），是 WR-02 子类注入回归的异常消息，不是标识符错字，不可能影响任何运行时行为 |

**prohibitions 核查：** 无 `-X pycache_prefix` / `sys.dont_write_bytecode` / `PYTHONDONTWRITEBYTECODE` 被用作「让它变绿」的手段 —— `grep -rn` 在 `quantlab tests scripts pyproject.toml` 下除 `test_stale_bytecode_lesson.py`（取证用途）外 **0 命中**；`pyproject.toml [tool.pytest.ini_options]` 只有 `pythonpath = ["."]`。`.venv` 未被触碰。

#### plan 15（G-03.11-3：WR-01 / WR-02 / IN-01）—— 10/10 ✓

| # | Truth | 状态 | 证据（本轮实测） |
|---|-------|------|------|
| 15-1 | 深嵌套 JSON 下 `label()` 返回数字不抛；`as_of()` 抛 shaped `ValueError` | ✓ VERIFIED | 两种深嵌套输入（对象嵌套 100000 层 / 数组嵌套 20000 层）实测：`label` → `['13407']`；`as_of` → `ValueError`，消息含类名 `CrspTickerLookup`、旁车绝对路径、`.crsp_*.json` 重建补救（程序化断言 `"rebuild" in str(e) and path in str(e)` → True） |
| 15-2 | `RecursionError` 加在 **`payload` 属性**的 except 元组里，不是 `label()` 的守卫元组 | ✓ VERIFIED | `crsp_tickers.py:206` `except (OSError, ValueError, RecursionError) as exc:` 在 `payload` 属性内；`_UNUSABLE`（:119）不含 `RecursionError`。这正是为什么 `as_of` 的拒绝保持 shaped |
| 15-3 | 有且仅有一个模块级 `_UNUSABLE = (FileNotFoundError, ValueError)`，两处 `except` 共用 | ✓ VERIFIED | :119 定义；`grep -n "_UNUSABLE"` → 定义 1 处 + `except _UNUSABLE as exc`（:450、:483）2 处 + 散文 2 处。方法体内无局部守卫元组字面量（另两个元组 `(OSError, ValueError, RecursionError)` 与 `(TypeError, ValueError)` 是**不同关注点**，:462-478 的注释逐字解释为什么不是第三份拷贝） |
| 15-4 | 本模块内部 bug **上抛**而非静默降级 | ✓ VERIFIED | 行为探针：子类覆写 `as_of` 使其 `raise KeyError("tikcer")`，对一份**完全合法**旁车调 `label([1], …)` → **PROPAGATED KeyError**（上一轮同一实验静默返回 `['13407']`）。回归锁在 `tests/test_crsp_ticker_sidecar.py:688`/`:709` 两条子类注入用例 |
| 15-5 | `label()` / `as_of()` docstring 描述的是现在成立的事 | ✓ VERIFIED | :437-446 逐字说明为什么三个 backstop 类型被移出（「after 03.11-12 preflighted every structural index, a `KeyError`/`AttributeError`/`TypeError` in here can only be a programming bug in THIS module」），:103-113 的 `_UNUSABLE` 注释同款。不再有「Do not leave both」的自相矛盾 |
| 15-6 | `example/wrds_crsp.md:247` 与代码同步，深嵌套一类被明确纳入 | ✓ VERIFIED | :248-249 逐字新增「「解析不了」是三种：字节不是合法 **UTF-8**、字节不是 JSON、以及**嵌套**深到解析器自己爆栈」 |
| 15-7 | IN-01 调用点计数在**五处**改对 | ✓ VERIFIED | `crsp_tickers.py:44/:50`（模块 docstring）、`:403/:407`（`label` docstring）、`base/model.py:1311-1314`、`tests/test_model_predict_panel.py:725/:729`、`tests/test_crsp_ticker_sidecar.py:386/:392` —— 五个文件全部为「三个调用点 / 六条人可见消息」。实测 `grep -rn '\.label(' quantlab/` = 2 处（`masking.py:262`、`engine_vectorbt.py:303`），第三处经 `base/model.py:1319 self.symbol_labeller(...)`，由 `base/backtest.py:1064 model.symbol_labeller = self.ticker_lookup.label` 装配 |
| 15-8 | encoding：非法 UTF-8 → `label` 回落成数字、`as_of` 抛 shaped `ValueError` | ✓ VERIFIED | 行为探针写入 `b'\xff\xfe\x00bad'`：`label([1], …)` → `['1']`；`as_of` → `ValueError` |
| 15-9 | empty：零字节与 `{}` 两种「空」各有结论且不同 | ✓ VERIFIED | 零字节 → `label` 返回 `['1']` 且**打一条 WARNING**（shaped 拒绝被降级捕获）；`{}` → `label` 返回 `['1']` 且 **0 条 WARNING**（「这份旁车不认识任何名字」不是损坏，`_intervals` 的 `.get("intervals", {})` 正常返回） |
| 15-10 | adjacency：区间两端 INCLUSIVE 语义不被触碰 | ✓ VERIFIED | 既有边界用例在 120 passed 批次内全绿；行为探针对合法区间实测 `label([1], 2020-01-01)` → `['AAA']` |

#### plan 16（IN-02 / IN-03 / IN-04）—— 7/7 ✓

| # | Truth | 状态 | 证据（本轮实测） |
|---|-------|------|------|
| 16-1 | 第三处断言补齐 `set(record) == {"types","rows","first","last"}` | ✓ VERIFIED | `tests/test_crsp_identity.py:1108-1113`：`assert "symbol" not in dropped` + `assert set(dropped) == {...}` + `types`/`rows`/`first`/`last` 逐字段断值。与 :590-595（`qqq`）、:1494-1500（`rescued`）两个兄弟**完全同款三层** |
| 16-2 | 加强后，退化成 `{}` 的记录会让这条用例变红 | ✓ VERIFIED | 逻辑必然：`set({}) == {"types","rows","first","last"}` 为 False。plan 16 另以一次抛弃式变异（`cp` 备份 + `git checkout --` 复原，未用 `git stash`）实测并把输出写进 SUMMARY:13/:29 |
| 16-3 | IN-04：模块 docstring 点名的函数名真实存在 | ✓ VERIFIED | docstring:6 写 `test_core_layer_purity_no_market_specific_logic`；`grep -c "def test_core_layer_purity_no_market_specific_logic"` → **1**（定义在 :70） |
| 16-4 | IN-03：注释明写这个门的覆盖面有多窄 | ✓ VERIFIED | :52-63 逐字：「It sees exactly the four legacy market literals listed below, on non-comment lines -- nothing else. It does NOT see either of the two concrete couplings … the named vendor import at `:15` … nor the concrete market class name that appears in prose at `:153`」，并以「"This gate is green" and "this file is proven free of market-specific coupling" are therefore two different claims」收口 |
| 16-5 | `FORBIDDEN_SUBSTRINGS` 仍是原四项，被否决的选项 (a) 未被偷偷执行 | ✓ VERIFIED | :64-69 = `("SpotKlineDataset","StockDataset","crypto_spot","us_equity")`，无 `CrspTickerLookup`；`CORE_LAYER_FILES`（:31-36）仍是含 `quantlab/base/backtest.py` 的四项 |
| 16-6 | empty：`{}` 现在是三处断言各自的反例 | ✓ VERIFIED | 三处均有 `set(...) == {...}`（:591、:1109、:1495） |
| 16-7 | ordering：四个键用**集合**比较，不依赖 JSON 键序；三处写法一致 | ✓ VERIFIED | 三处均为 `set(...) == {"types","rows","first","last"}` |

**prohibitions 核查：** 未把 `CrspTickerLookup` 或任何具名 vendor 类加进 `FORBIDDEN_SUBSTRINGS`（grep 0 命中）；未重构 `base/backtest.py:15`（本轮 `git diff` 不含该文件）；未用放宽断言的方式让用例变绿（方向是从 1 层加强到 3 层）。

#### plan 17（G-03.11-6：WR-03 / WR-04）—— 11/11 ✓

| # | Truth | 状态 | 证据（本轮实测） |
|---|-------|------|------|
| 17-1 | 损坏旁车导致的降级打**恰好一条** WARNING，含类名与原始异常文本；同实例后续调用不再打 | ✓ VERIFIED | 行为探针：损坏旁车 `{"intervals":[1,2,3]}`，同一实例上 `label(range(100))` 连调**两次** → `len(warning_messages) == 1`。消息含 `CrspTickerLookup`、旁车绝对路径、`(ValueError: …)` 原始异常文本 |
| 17-2 | 两处 `except` 都**绑定**异常对象并交给 `_degrade(exc)` | ✓ VERIFIED | :450 `except _UNUSABLE as exc: self._degrade(exc)`；:483 同款。无任何一处把异常整个丢弃 |
| 17-3 | **没有旁车文件**这一支同样打这条告警 | ✓ VERIFIED | 行为探针：指向不存在的路径，`label([13407, 14593], …)` → `['13407','14593']` 且 **1 条 WARNING** |
| 17-4 | 非整数标签透传那一支**不**打告警 | ✓ VERIFIED | 行为探针：合法旁车 + `label(["QQQ","SPY",1], …)` → `['QQQ','SPY','AAA']`，**0 条 WARNING** |
| 17-5 | `_object_payload()` 是顶层形状被检查的**唯一**地方，`_intervals()` 与 `product_end` 都经由它 | ✓ VERIFIED | `grep -c "isinstance(payload, dict)"` → **1**（:305，在 `_object_payload` 内）；`grep -c "self.payload.get("` → **0**；`_object_payload()` 的两个调用点为 :323（`_intervals`）与 :350（`product_end`）。（第三处 grep 命中是 :37 的模块 docstring 引用，非调用点，见 advisory 2） |
| 17-6 | payload 为 `[]` 时 `product_end` 抛 shaped `ValueError` 而非裸 `AttributeError` | ✓ VERIFIED | 行为探针：`[]` 旁车 → `product_end` RAISED **ValueError**，程序化断言消息含类名 + 绝对路径 + `re-convert` → True。上一轮同一输入为 `AttributeError: 'list' object has no attribute 'get'` |
| 17-7 | 模块 leaf 声明与事实一致：module scope 只 import 标准库与 `loguru` | ✓ VERIFIED | :86-93 全部 import 为 `__future__` / `json` / `collections.abc` / `datetime` / `pathlib` / `loguru`。唯一的 project-internal import 在 `beside_store` 函数体内（:165） |
| 17-8 | idempotency：`_degrade` 一次性由 `self._degraded` 实例标志保证 | ✓ VERIFIED | :144 `__init__` 初始化，:272-273 写，无其它读写点；行为探针 100 条记录 × 2 次调用 → 1 条告警 |
| 17-9 | ordering：部分 PERMNO 损坏时逐 PERMNO 降级，位置与长度一一对应，且只一条告警 | ✓ VERIFIED | 行为探针：A 的 span 合法 / B 的 span 缺 `start` → `label([1,2], …)` 返回 **`['AAA','2']`**，**1 条 WARNING** |
| 17-10 | empty：`[]` / `{}` / 零字节三种「空」在**三个**入口上结论各自可见 | ✓ VERIFIED | 行为探针覆盖 `label` / `as_of` / `product_end` 三入口；回归锁在 `tests/test_crsp_ticker_sidecar.py:875`（`product_end` 非对象 payload）、`:899`（不抛裸异常）、`:929`（不可解析旁车） |
| 17-11 | concurrency：`_degraded` 只在 `__init__` 与 `_degrade` 里被写、只在 `_degrade` 里被读，不参与返回值 | ✓ VERIFIED | 源码读证 + `tests/test_crsp_ticker_sidecar.py:811 test_the_degraded_flag_is_a_log_throttle_and_not_a_state_machine` 在 120 passed 批次内全绿 |

**prohibitions 核查：** 非整数透传不打告警 ✓（17-4 实测）；降级不升格成异常、不改返回值 ✓（17-1/17-9 实测返回值与无告警版一致）；不每条记录打一次 ✓（100 条 → 1 条）；未引入第二份 `isinstance` ✓（计数 1）；module scope 无 project-internal import ✓。
**已知刻意偏离（SUMMARY:271-274 如实记录）：** plan 自己的 Task 2 `<behavior>` 回归 2 要求「`MALFORMED_SIDECARS` 三个输入在 `product_end` 上**全部**抛 shaped `ValueError`」，与同 plan 的 prohibition「不得引入第二份 `isinstance`」互斥 —— 三个输入里两个顶层是合法 JSON object，要让它们也在 `product_end` 抛就必须在 `product_end` 里再查 `intervals`。执行者选择守 prohibition、把验收判据收窄为「顶层非对象那一个」，并在 SUMMARY 里写明。**本验证认同这个取舍**：`product_end` 对 `intervals` 不严格是模块 docstring:39-42 明确记录的设计（「a sidecar with a broken interval table can still say honestly which CRSP vintage it was read against」）。

#### plan 18（G-03.11-5：盘上产物 + 常设真实数据门）—— 11/11 ✓

| # | Truth | 状态 | 证据（本轮实测） |
|---|-------|------|------|
| 18-1 | 盘上 `.crsp_filter_report.json` 由当前 HEAD 重生成，每条记录键集恰为 `{types,rows,first,last}` | ✓ VERIFIED | **直接读盘**：`roster_overrides.permnos` 1 条（PERMNO `75154`），键集 `['first','last','rows','types']`，值 `{'first':'2024-01-02','last':'2024-12-31','rows':252,'types':['UG/EQTY/COM/CORP/N']}`。上一轮同一文件里的 `"symbol": "75154"` 已消失。与编排器独立测量一致 |
| 18-2 | 键集断言进**常设**真实数据门，此后每次重建都复检 | ✓ VERIFIED | `tests/test_crsp_rebuild_measurements.py:590 test_the_filter_report_carries_no_redundant_symbol_field`；常量 `BREAKDOWN_RECORD_KEYS = frozenset({"types","rows","first","last"})`（:161）；`pytest --collect-only` 实测该文件 **6 tests collected** |
| 18-3 | raw tier 缺失时 `_data_root()` 以 `pytest.fail` **停下来报告**，不 skip | ✓ VERIFIED | :179-224 三条失败路径全部 `pytest.fail`；`grep` 实测文件内 **0 个** `pytest.skip(` 调用、**0 个** `skipif` 标记（唯二命中在 :30/:182 的散文里，是解释「为什么不用 skip」）。raw tier 实测在位：`wrds/` 下 **132 个 `.pqt`**（按 `month=YYYY-MM` 分区）+ 兄弟目录 `_reference/` 的 6 张参考表 + `manifest.json` |
| 18-4 | 备份写进**新的**代目录，不覆盖 `_backup_pre_03.11` 与 `_backup_pre_03.11_10` | ✓ VERIFIED | `ls -d data/_backup*` → `_backup_deleted_custom_store_03.11` / `_backup_pre_03.11` / `_backup_pre_03.11_10` / `_backup_pre_03.11_18` / `_backup_pre_03.11_18_rerun2`，**五代全部在位**，两个相位前旧代未被覆盖 |
| 18-5 | 这条保护是**前置**的：目标目录在交给 `rebuild()` 之前就已被判定为不存在 | ✓ VERIFIED | 源码顺序：:365 `backup_dir = _fresh_backup_dir(root)` → :370 `assert not backup_dir.exists()` → :386 `rebuilder.rebuild(backup_dir=backup_dir)`。判定整体发生在 `rebuild()` 之前，事后的 mtime / `backup_path:` 两条判据保留但不充当这条保护（:359-362 逐字写明） |
| 18-6 | 前置保护对**任意**已存在代目录成立，不是字面量黑名单 | ✓ VERIFIED | `_fresh_backup_dir`（:239-282）的判据是 `while candidate.exists(): candidate = 下一个名字`，即「目标目录存在」这个一般性质；`BACKUP_GENERATION_BASE = "_backup_pre_03.11_18"` 只是基名 |
| 18-7 | 这条不变量由一个 `tmp_path` 单测常设锁住 | ✓ VERIFIED | **实跑** `uv run pytest tests/test_crsp_rebuild_measurements.py -k fresh_backup -p no:cacheprovider -q` → **1 passed, 5 deselected**。用例 `test_a_fresh_backup_generation_never_lands_on_an_existing_one`（:390）占位一个名字后断言返回值既不等于它也不存在，再占位一次断言继续跨过 —— 不碰真实数据、不跑任何重建 |
| 18-8 | 键集断言**不会空跑通过**：先断记录数 ≥ 1，再逐条断键集 | ✓ VERIFIED | :623-629 `assert len(overrides) >= 1` 在 `_assert_breakdown_shape` 之前，失败消息点名「so the key-set check below would pass without examining anything」 |
| 18-9 | `dropped_permnos` 当前为 `{}`，**如实**记为无从观察而非声称已验证 | ✓ VERIFIED | 盘上实测 `dropped_permnos` **0 条**。代码侧 :643-660 的注释逐字写「the shape check below runs over ZERO records, and is reported as the empty measurement it is rather than allowed to look like a verification」并指向真正覆盖它的 `tests/test_crsp_identity.py:1085-1103`；SUMMARY:416 逐字「本 SUMMARY**不**声称这一支已验证」 |
| 18-10 | adjacency：重建后 75154 的 `first`/`last` 仍是窗口两端、`rows` 仍是 252 | ✓ VERIFIED | 盘上直读 `first='2024-01-02'`、`last='2024-12-31'`、`rows=252` |
| 18-11 | 重建后既有真实数据断言全部仍成立（清洁度四条、int64 轴、symbology 报告不存在、旁车 PERMNO 集合 = 面板轴） | ✓ VERIFIED | 本验证**直接读盘**核对了其中可静态观察的四项：`symbol` dtype **int64**；`.crsp_symbology_report.json` **不存在**；`.crsp_tickers.json` 的 PERMNO 集合与面板轴 **set-equal（各 520）**；四份旁车（`.chunks.json` / `.crsp_adjustment.json` / `.crsp_filter_report.json` / `.crsp_tickers.json`）全部在位。清洁度四条（`adjClose<=0==0`、`close==0==0`、两个 NaN 计数 == `structural_gaps`、`anomaly_flag_true == 13`）只在重建过程的 measurement 对象上产出，按交接指令**未重跑**，依据是 plan 18 执行者本轮对主仓库数据根的**两次**绿色记录（SUMMARY:219 逐字「其余每一个数字逐字相同」）+ 源码里这些断言是等式而非不等式（:453-457 明写「Do NOT relax this to an inequality」）|

**prohibitions 核查：** 无 skip/skipif ✓；备份未指向两个旧代 ✓（五代全在位）；未在已存在代目录上运行 `rebuild()` ✓（前置 assert + tmp_path 单测双锁）；保护是前置而非事后 ✓（源码行序 365 < 370 < 386）；未为腾地方删除任何代目录 ✓；未放宽任何真实数据断言 ✓；未声称 `dropped_permnos` 已验证 ✓；`files_modified` 不含 `.crsp_filter_report.json` / `data/**` ✓（`git diff --name-only fe54bf1..HEAD` 实测仅 `tests/test_crsp_rebuild_measurements.py`）。

#### backstop 声明（16 条，单列不进分母）

| Plan | 条数 | 有直接观察/构造证据 | 如实记为未证（abstain） |
|---|---|---|---|
| 14 | 2 | 0 | 2（清缓存动作的串行性与幂等性 —— 动作已完成，事后不可观察） |
| 15 | 3 | 2（ordering：探针实测一一对位；idempotency：`test_the_sidecar_is_read_once_per_instance` 绿） | 1（concurrency 竞态） |
| 16 | 4 | 3（encoding / idempotency / adjacency —— 三处断言纯读、tmp_path、集合比较，源码可判） | 1（concurrency） |
| 17 | 3 | 2（adjacency 边界用例绿；encoding 由 15-8 回归覆盖） | 1（concurrency 运行时：`_degraded` 读改写非原子） |
| 18 | 4 | 3（ordering 集合比较、encoding 纯 ASCII、concurrency wave 3 串行 —— 源码可判） | 1（idempotency 逐字节确定性，SUMMARY 自述未逐字节比对两代产物） |
| **合计** | **16** | **10** | **6** |

**总分：134/136**（Part B 89 + Part C 45；扣掉的 2 条是 UAT test 4 的两条 behavior-unverified）。

---

### 必需产物

| Artifact | 期望 | 状态 | 细节 |
|---|---|---|---|
| `quantlab/dataset/crsp_tickers.py` | 三入口姿态一致、`_UNUSABLE` 收窄、`_degrade` 可见、`_object_payload` 唯一形状检查 | ✓ VERIFIED | 486 行；四条 WR 全部由本验证行为探针实测关闭；leaf 属性成立 |
| `tests/test_crsp_ticker_sidecar.py` | 深嵌套解析期失败 + 子类注入 + 告警计数三类回归 | ✓ VERIFIED | `warning_messages` fixture（:417）+ 5 条 WR-03 告警计数断言（:531/:746/:767/:786/:808）+ 2 条子类注入（:688/:709）+ 4 条 `product_end` 回归（:863/:875/:899/:929） |
| `tests/test_stale_bytecode_lesson.py` | 等长注入教训的可执行证明 | ✓ VERIFIED | 224 行，3 passed |
| `tests/conftest.py` | 与 macOS OpenMP 守卫并列的散文教训 | ✓ VERIFIED | :43-58 |
| `tests/test_crsp_identity.py` | 三处记录断言同强 | ✓ VERIFIED | :591 / :1109 / :1495 三处 `set(...) == {...}` |
| `tests/test_extensibility_contract.py` | 门的射程被写明、函数名真实、四项字面量未增 | ✓ VERIFIED | :52-63 / :70 / :64-69 |
| `tests/test_crsp_rebuild_measurements.py` | 常设真实数据门 + 前置备份代目录保护 | ✓ VERIFIED | 6 tests collected；`-k fresh_backup` 1 passed；raw tier 前置检查以 `pytest.fail` 停下 |
| `quantlab/base/model.py` | IN-01 计数订正 | ✓ VERIFIED | :1311-1314 |
| `example/wrds_crsp.md` | 「永不抛」活副本与代码同步 | ✓ VERIFIED（含一条 advisory） | :248-249 纳入深嵌套一类；未提 WR-03 新增的降级告警（advisory 1，plan 17 范围外） |
| `data/.../wrds_crsp_sp500_1d.zarr` | int64 PERMNO 轴、无 ticker/permno 变量 | ✓ VERIFIED | 重建后实读 252×520、int64、27 data_vars、无 `ticker`、无 `permno` |
| `data/.../.crsp_filter_report.json` | 无冗余 `symbol` 字段 | ✓ VERIFIED（上一轮 ✗ 陈旧） | 1 条记录，键集 `['first','last','rows','types']` |
| `data/.../.crsp_tickers.json` | PERMNO 集合 = 面板轴 | ✓ VERIFIED | set-equal，各 520；`vintage_product_end = 2025-12-31` |
| `data/_backup_pre_03.11*` | 旧代不被覆盖 | ✓ VERIFIED | 五代全部在位 |

### 关键连接（Key Links）

| From | To | Via | 状态 | 细节 |
|---|---|---|---|---|
| `payload` 属性的 except 元组 | `_intervals()` → `as_of()` → `label()` 两处 `except` | `RecursionError` 在最上游收住 | ✓ WIRED | :206 捕获 → 转成 shaped `ValueError` → 下游 `_UNUSABLE` 接得住。实测 `label` 不抛、`as_of` shaped |
| `_UNUSABLE` 模块常量 | `label()` 的两个 except 站点 | 一份拼写 | ✓ WIRED | :119 → :450 / :483 |
| `_object_payload()` | `_intervals()` 与 `product_end` | 一处检查两处受益 | ✓ WIRED | :283 → :323 / :350；`isinstance(payload, dict)` 计数 1、`self.payload.get(` 计数 0 |
| 两处 `except _UNUSABLE as exc` | `_degrade(exc)` → `self._degraded` → 恰好一条 WARNING | loguru | ✓ WIRED | 实测 1 条 |
| `label()` | 三个**裸**调用点 | `masking.py:262` / `engine_vectorbt.py:303` / `base/model.py` 经 `symbol_labeller` | ✓ WIRED | 第三处装配点实测在 `base/backtest.py:1064` `model.symbol_labeller = self.ticker_lookup.label` |
| `crsp.py:1263-1271 _permno_breakdown` | `:1099 roster_overrides["permnos"]` / `:1116 dropped_permnos` → 盘上 JSON → 常设门键集断言 | 一份渲染两个分支 | ✓ WIRED | 盘上产物键集实测为四键；`test_crsp_identity.py:1109` 守合成 QQQ 那一支 |
| `tests/conftest.py` 散文 | `tests/test_stale_bytecode_lesson.py` 可执行证明 | 散文点名文件、文件证明散文 | ✓ WIRED | conftest:58 逐字点名；该文件 3 passed |
| `_fresh_backup_dir` 判定 | `rebuilder.rebuild(...)` | 前置顺序 | ✓ WIRED | 源码行序 365 < 370 < 386；tmp_path 单测锁住 |

### 数据流追踪（Level 4）

| 值 | 来源 | 是否真实流动 | 状态 |
|---|---|---|---|
| `label()` 返回的 ticker | 盘上 `{zarr}.crsp_tickers.json` → `json.loads` → `_object_payload` → `_intervals` → `as_of` 线性扫描 | 是 —— 探针以真实合法旁车实测返回 `['AAA']`，损坏旁车才回落成数字 | ✓ FLOWING |
| `product_end` | 同一 payload 的 `vintage_product_end` 字段 | 是 —— 盘上实测 `2025-12-31` | ✓ FLOWING |
| `roster_overrides.permnos` 记录 | `crsp.py:_permno_breakdown` 在重建时渲染 → 盘上 JSON | 是 —— 盘上 1 条真实记录（75154，252 行，2024 全年） | ✓ FLOWING |
| `dropped_permnos` | 同一渲染的另一分支 | **本 store 上为空**（`{}`），无从观察 | ⚠️ 空载（已如实记录，非 HOLLOW） |
| `symbol_labeller` | `base/backtest.py:1064` 绑定到 `ticker_lookup.label` | 是 —— 非 `None` 时 `_spell` 直接调用 | ✓ FLOWING |

### 行为抽检（Behavioral Spot-Checks）

| 行为 | 命令 | 结果 | 状态 |
|---|---|---|---|
| 四 gap-closure suite（默认缓存目录） | `uv run pytest -q tests/test_crsp_ticker_sidecar.py tests/test_model_predict_panel.py tests/test_crsp_identity.py tests/test_extensibility_contract.py` | **120 passed, 126 warnings in 14.92s** | ✓ PASS |
| 字节码教训可执行证明 | `uv run pytest -q tests/test_stale_bytecode_lesson.py` | **3 passed in 1.02s** | ✓ PASS |
| 备份代目录前置不变量（tmp_path，不碰真实数据） | `uv run pytest tests/test_crsp_rebuild_measurements.py -k fresh_backup -p no:cacheprovider -q` | **1 passed, 5 deselected** | ✓ PASS |
| 真实数据门收集数 | `uv run pytest --collect-only -q tests/test_crsp_rebuild_measurements.py` | **6 tests collected** | ✓ PASS |
| 前置失败集定向复跑（9 文件） | `uv run pytest -q tests/test_ingest_shells.py …（9 个）` | **52 failed, 208 passed, 1 skipped**；逐文件 14/11/10/6/5/2/2/1/1 | ✓ PASS（与台账逐字相同） |
| 前置失败的错误类分布 | 同上 + `--tb=line` | ModuleNotFoundError **26** / FileNotFoundError **17** / AssertionError **8** | ⚠️ 与 D-03.11-12-A 散文不符（W-2） |
| WR-01 深嵌套 JSON | 行为探针（临时脚本） | `label` → `['13407']` 不抛；`as_of` → shaped `ValueError` | ✓ PASS |
| WR-02 本模块 bug 上抛 | 行为探针（子类注入） | PROPAGATED `KeyError('tikcer')` | ✓ PASS |
| WR-03 告警计数 | 行为探针（loguru sink） | 损坏 1 / 缺失 1 / 混合损坏 1 / 非整数透传 0 | ✓ PASS |
| WR-04 `product_end([])` | 行为探针 | shaped `ValueError` | ✓ PASS |
| `.pyc` 新鲜度 | 读 pyc header 对比源码 stat | mtime/size 一致且内容含本轮新增符号 | ✓ PASS |
| D-03.11-18-A 归因复现 | `pytest --collect-only tests/test_entry_point_contracts.py` | 8 collected，含 `jerry_query_data.py`（未跟踪） | ✓ PASS |
| 全量回归门 | — | **未跑**（D-03.11-12-B 死锁风险 + full-run 至多一次规则）；采信编排器主树后合并测量 **55 failed, 1701 passed, 1 skipped**，并以上面的 9 文件定向复跑交叉验证 | ? SKIP（已交叉验证） |

### Probe 执行

本仓库无 `scripts/*/tests/probe-*.sh` 约定，各 PLAN/SUMMARY 亦未声明 probe。**Step 7c: SKIPPED（无 probe 可跑）。**

### 需求覆盖

| Requirement | Source Plan | 描述 | 状态 | 证据 |
|---|---|---|---|---|
| DATA-03 | 14 / 15 / 16 / 17 / 18（全部声明） | 数据层抽象按市场/频率可扩展，新增市场不改上层 | **Pending（按设计不完成）** | `.planning/REQUIREMENTS.md:23` 仍 `- [ ]`；:139 追踪行结尾「Status unchanged: still Pending, still owned by Phase 2」；相位目标逐字「No requirement is COMPLETED here」。**代码侧正确，SUMMARY 台账侧有风险，见 W-1** |

**孤儿需求检查：** `grep -E "Phase 03.11" .planning/REQUIREMENTS.md` 只映射 DATA-03 一条，无任何 plan 未认领的需求。✓ 无孤儿。

### 反模式扫描

对本轮 `git diff --name-only fe54bf1..HEAD` 列出的全部实现/测试/文档文件做债务标记扫描：

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| （全部 10 个源码/测试/文档文件） | — | `TBD` / `FIXME` / `XXX` / `TODO` / `HACK` / `PLACEHOLDER` | — | **0 命中** |

其它检查：无 `return null` / 空实现 / console-log-only 模式；无 `except Exception`（`crsp_tickers.py` 的三个 except 元组全部窄且各有注释说明关注点）；无 `pytest.skip` 逃逸；无 `-X pycache_prefix` / `dont_write_bytecode` 绕过。

---

## 需要人工的事项

### 1.【裁定，W-1】plans 12-18 的 `requirements-completed: [DATA-03]` 要不要统一置空？

**做什么：** 决定七份 SUMMARY（12/13/14/15/16/17/18）frontmatter 里的 `requirements-completed: [DATA-03]` 如何处置。
**期望：** 一个明确的二选一 —— 要么全部置空 `[]` 并各补一行理由（照 plan 11 的做法），要么操作者明确声明「该字段不权威，只有 `.planning/REQUIREMENTS.md` 权威」。
**为什么需要人：** 这是台账处置不是代码缺陷。plan 11 在自己的 Deviation 1 里逐字记录过这条机制：shared-ID 门会一直压着 DATA-03，**直到最后一个声明它的 plan 产出 SUMMARY**；届时 `requirements.mark-complete` 会把它翻成 Complete，正是 T-03.11-38（severity high）描述的危害。plan 11 因此置空并显式上报。plans 12-18 全部照抄了 `[DATA-03]`，而 **plan 18 正是本相位最后一个**。目前 `.planning/REQUIREMENTS.md` 仍正确（181 行、7 个 `[x]`、DATA-03 仍 `- [ ]`、最后改动停在 plan 11 的 `f32f6a5`），所以损害尚未发生 —— 但触发条件已重新具备，且相位目标逐字写着「No requirement is COMPLETED here」。

### 2.【执行】UAT test 4：`_compare_fingerprints` warning 风暴 backstop（仍 blocked）

**做什么：** 在 plan 18 重建后的 PERMNO 轴 store 上重跑一个既有 backtest run（load 模式），逐条读完 `_compare_fingerprints`（`quantlab/base/backtest.py:1175`）打出的全部 warning。
**期望：** 每一条 warning 都能归因到 D-12 已接受的指纹失效（digest / n_symbols 变了），没有一条是别的真实问题被裹在里面。
**为什么需要人：** 真实 store 上这条路径无测试覆盖 —— `tests/test_backtest_rebuild.py:513 test_changed_store_rebuild_warns_and_completes` 只在合成数据上证明「warn 之后能跑完」，不回答「真实那一屏 warning 是否都可归因」。03.11-UAT.md test 4 至今 `blocked_by: other`，从未跑过；plan 18 又重建了一次 store，这笔债没有缩小。**这是 134/136 扣掉的两条。**

### 3.【确认】6 条 backstop 声明按规则记为「未证」

**做什么：** 确认 plans 14-18 的 16 条 `verification: backstop` 里，有 6 条本验证无法事后观察，如实记为未证而非通过。
**期望：** 操作者确认无需额外证据。清单：plan 14 的 concurrency / idempotency（清缓存动作已完成，事后不可观察）、plan 15 的 concurrency、plan 16 的 concurrency、plan 17 的 concurrency 运行时（`_degraded` 读改写非原子，最坏多打几条告警）、plan 18 的 idempotency（逐字节确定性，SUMMARY 自述本轮未逐字节比对两代产物）。
**为什么需要人：** backstop 是外生标签，按 honest-verifier 规则不得以「存在 + 接线」充当证据。另外 10 条本验证有直接观察或构造证据，已在 Part C 表内计入。

---

## 台账与 advisory

### W-2（台账不实，已立项未修）

`deferred-items.md` D-03.11-12-A 散文逐字称「**Every one of the 55** is a `ModuleNotFoundError: No module named 'ingest_tiingo'` / `'ingest_binance_spot'`, or the `FileNotFoundError` twin of it」。本验证实测错误类分布为 **ModuleNotFoundError 26 / FileNotFoundError 17 / AssertionError 8**。逐文件计数完全一致，**措辞不实**。这一条已由 plan 14 立为 `.planning/WINDOWS.md` 第 29 条（`open`，2026-09-21T22:50:08Z），属于诚实记账的已知未修项，不是新发现 —— 但台账正文本身仍带着这句不成立的话，任何只读 deferred-items.md 的人会被误导。

### Advisory（新范围、无确定性证据，不阻断）

| # | Finding | Category | Why Advisory |
|---|---|---|---|
| 1 | `example/wrds_crsp.md:247-249` 的 `label()` 契约活副本未同步 WR-03 新增的一次性降级 WARNING | other | plan 17 的 `files_modified` 刻意不含该文件。散文没说错，只是少说一半（读者不会知道控制台现在能区分「旁车损坏」与「本来就没有旁车」） |
| 2 | `03.11-17-SUMMARY.md:102/:165` 的 `grep -c '_object_payload()' == 2` 现为 3 | other | 第三处是同 plan 更晚一次提交（`f2379fe`）加进模块 docstring:37 的引用，非调用点。实质性质本验证独立复核全部成立 |
| 3 | 编排器 carried-forward 清单里「WINDOWS 第 28 条（custom store 仍在迁移前 ticker 轴）」与盘上不符 | other | WINDOWS.md:45 第 28 条实为 `fixed`（03.11-11 操作者裁定删除，已备份至 `data/_backup_deleted_custom_store_03.11/`，删除后 1d/ 目录内 `crsp_symbology_report` 命中数 0）。这是好消息，建议从 carried-forward 清单移除 |

---

## Gaps Summary

**上一轮的 4 个 gap 全部关闭，零遗留、零回归。**

- **G-03.11-4（阻断级）** —— 投毒字节码已清除。本验证在默认缓存目录下重新取得的 120 passed 与编排器主树基线逐字相同；`.pyc` header 与源码一致且内容含本轮新增符号。更重要的是，这次教训被做成了**可执行**的（`tests/test_stale_bytecode_lesson.py` 3 passed）而不只是一句散文 —— 这是本轮质量最高的一项交付。
- **G-03.11-3** —— WR-01 与 WR-02 都以本验证自己的行为探针实测关闭。`RecursionError` 被收在 `payload` 属性而不是 `label()` 的元组里，这个位置选择是对的：两个入口同时被覆盖，且 `as_of` 的拒绝保持 shaped。WR-02 按操作者裁定走了「收窄」这条路，并补上了「本模块内部 bug 会上抛」的回归 —— 上一轮那个「合法旁车 + 一个字符错字 → 静默返回数字」的反例现在会 PROPAGATE。
- **G-03.11-6** —— WR-03 的降级告警做对了三件难的事：损坏要说、**缺失也要说**（这是关键，因为「数字」在无旁车 store 上是健康输出）、**非整数透传不说**（否则一批 Tiingo ticker 会把告警刷爆到操作者学会忽略它）。四种情形本验证逐一实测。WR-04 走的是「抽 `_object_payload()` 共用」而不是「再加一个 `isinstance`」，让 docstring 的「唯一一处」真正成真。
- **G-03.11-5** —— 代码与盘上产物重新对齐，且这次不是一次性手工核对：键集断言进了常设真实数据门，raw tier 缺失会 `pytest.fail` 而不是 skip，备份代目录的前置保护另有 `tmp_path` 单测常设锁住（本验证实跑通过）。`dropped_permnos` 那一支空载，代码注释与 SUMMARY 两处都如实写了「无从观察」而不是签收 —— 这正是应该有的姿态。

**相位目标本身（Part A 12/12）在重建后的盘上产物上逐条复检仍成立。** 面板 `symbol` 是 int64 PERMNO、`ticker` 离开面板进旁车、适配机制在 `quantlab/` 下 grep 零命中。

**不判 passed 的唯一原因是人工条目非空：** UAT test 4 仍 blocked（两条 behavior-unverified），加上 W-1 需要一次台账裁定、6 条 backstop 需要一次确认。这三项都不影响代码正确性。

---

_Verified: 2026-09-22T00:41:18Z_
_Verifier: Claude (gsd-verifier)_
_测量环境：主工作树，相对路径，默认字节码缓存目录_

---

## Acknowledged Gaps

记录于 UAT 第三轮（2026-09-21），操作者确认后放行相位完成。

### 1. `03.11-UAT.md` test 4 记为 `skipped`，而 `phase uat-passed` 把任何 skipped 计为未解决

**门的读数：** `blockers: ['03.11-UAT.md: test 4 (skipped)']`

**这是工具约定，不是一条发现。** `gsd-core/bin/lib/uat.cjs:3581-3591` 对 `result: skipped`
只在 `reason` 命中 server / not running / device / build / release 等英文关键词时归到更具体的
类别，否则一律落到 `skipped_unresolved`。test 4 的理由是中文且不属于那几类，因此必然计为阻塞，
与判定是否正确无关。

**为什么不改成 `pass`。** 那等于宣称做过一次从未做过的观察。test 4 要求的观察是
「逐条读完一次既有 backtest run 在重建后 store 上打出的 `_compare_fingerprints` warning」，
这次观察没有发生，也无法发生 —— 前提主语为空集（详见 UAT test 4 的 `reason`：
操作者服务器上 14 个 run 全部读 `us_all.zarr`，本 phase 未触碰；CRSP store 上从无任何 run）。
诚实的记录就是 `skipped` 加完整理由。

**放行依据：** 相位目标 12/12 在重建后的盘上产物上成立；134/136 must-haves 通过；
两条 behavior-unverified 正是本项的两次声明（plan 10 与 plan 11 各一次），随本判定一并收口。

### 2. 6 条 `verification: backstop` 声明保留为「未证」

UAT test 7 操作者确认：plans 14-18 的 16 条 backstop 声明中，6 条事后不可观察
（plan 14 的清缓存串行性与幂等性、plans 15/16/17 的 concurrency、plan 18 的 idempotency），
按 honest-verifier 规则如实记为「未证」而非「通过」，不追加证据。

其中 plan 18 的 idempotency 性质与其余五条不同 —— **它可验证，只是没验**：
两代产物仍在盘上（`data/_backup_pre_03.11_18` 与 `_backup_pre_03.11_18_rerun2`），
逐字节比对做得到。操作者选择不做。方向安全（未证 ≠ 通过）。

### 3. 台账口径

`covered_digest` 于 2026-09-21 按裁定 (a) 刷新（`d337627c…` → `e32834c6…`），
未重跑验证。依据：staleness 源于 #4155 内容指纹而非 mtime；与验证器取数 HEAD `809ad54`
相比仅 7 个 SUMMARY 的一行 frontmatter 变动（`requirements-completed: [DATA-03]` → `[]`），
covered_files 中**零个实现文件**变化。`03.11-UAT.md` 自身在覆盖清单内，故指纹在 UAT 终稿后才计算。

_Acknowledged: 2026-09-21, UAT round 3_
