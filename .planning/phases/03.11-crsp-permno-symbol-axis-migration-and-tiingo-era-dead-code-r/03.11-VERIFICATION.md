---
phase: 03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r
verified: 2026-09-21T21:24:31Z
status: gaps_found
score: 88/91 must-haves verified
covered_files:
  - ".planning/REQUIREMENTS.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-01-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-01-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-02-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-02-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-03-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-03-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-04-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-04-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-05-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-05-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-06-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-06-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-07-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-07-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-08-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-08-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-09-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-09-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-10-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-10-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-11-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-11-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-12-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-12-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-13-PLAN.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-13-SUMMARY.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-REVIEW.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-UAT.md"
  - ".planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/deferred-items.md"
  - "quantlab/acquisition/inspector.py"
  - "quantlab/backtest/engine_vectorbt.py"
  - "quantlab/base/backtest.py"
  - "quantlab/base/config.py"
  - "quantlab/base/constituent.py"
  - "quantlab/base/data.py"
  - "quantlab/base/factor.py"
  - "quantlab/base/model.py"
  - "quantlab/base/rebuild.py"
  - "quantlab/dataset/backend.py"
  - "quantlab/dataset/constituent.py"
  - "quantlab/dataset/crsp.py"
  - "quantlab/dataset/crsp_membership.py"
  - "quantlab/dataset/crsp_rebuild.py"
  - "quantlab/dataset/crsp_symbology.py"
  - "quantlab/dataset/crsp_tickers.py"
  - "quantlab/dataset/masking.py"
  - "quantlab/dataset/stock.py"
  - "quantlab/factor/universe_filter.py"
  - "quantlab/utils/cli.py"
  - "quantlab/utils/symbol_axis.py"
  - "scripts/ingest_wrds_crsp.py"
  - "tests/test_crsp_identity.py"
  - "tests/test_crsp_ticker_sidecar.py"
  - "tests/test_extensibility_contract.py"
  - "tests/test_model_predict_panel.py"
covered_digest: "v1:sha256:055d2520cbbfa94750a58375a57297c40bc165c39f8dfebbbe4af9eda2bda184"
behavior_unverified: 2
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 77/79
  previous_verified: 2026-09-21T11:30:11Z
  gaps_closed:
    - "G-03.11-1 —— `quantlab/base/backtest.py` 已进入 `tests/test_extensibility_contract.py:CORE_LAYER_FILES`（现 4 项），`FORBIDDEN_SUBSTRINGS` 仍为原四项、未被否决的选项 (a) 污染"
    - "G-03.11-2 —— `_permno_breakdown` 的返回字典字面量只剩 `types/rows/first/last`（crsp.py:1263-1271），两个调用点（:1099 / :1116）共用同一渲染；三处断言全部改为 `\"symbol\" not in ...` 正向接管"
    - "UAT test 6 —— WINDOWS.md 22/23/24 已由 `open` 改为 `fixed` 并带证据列"
  gaps_remaining:
    - "G-03.11-3 —— 仅 partially_resolved：三个已复现输入被拦住了，但「永不抛」契约仍有 WR-01 一个可达逃逸口，且 WR-02 证明五元组守卫的方向与其 docstring 相反"
  regressions:
    - "无跨相位回归：plans 01-11 的 12 条相位目标真值逐条复检仍成立（见 Part A 回归列）"
  new_findings:
    - "G-03.11-4（阻断级，环境）：`quantlab/dataset/__pycache__/crsp_tickers.cpython-313.pyc` 是被投毒的陈旧字节码，且其 header 记录的 mtime/size 与当前干净源码逐字相同，Python 判定为「新鲜」而不重编译"
    - "G-03.11-5：盘上 `.crsp_filter_report.json` 仍带着 G-03.11-2 已删除的 `symbol` 字段（代码改了、产物没重生成）"
    - "G-03.11-6：WR-03（降级完全静默）与 WR-04（`product_end` 绕过形状检查）"
gaps:
  - gap_id: G-03.11-3
    truth: "`CrspTickerLookup.label()` 面对缺失或损坏的 sidecar 都不抛错，回落到 PERMNO 自身拼写；`as_of()` 的每一次拒绝都是 shaped 的"
    status: partial
    severity: major
    reason: |
      plan 12 拦住了 UAT 点名的三个输入，但把契约写成了一条更强的声明
      （crsp_tickers.py:250「missing or CORRUPT」、example/wrds_crsp.md:247「缺失或损坏」），
      而这条更强的声明有一个可达的反例（WR-01），并且用来兑现它的守卫元组在同一次提交后
      变成了只会吞掉本模块 bug 的东西（WR-02）。两条本验证均已独立复现，非采信 SUMMARY。
    artifacts:
      - path: "quantlab/dataset/crsp_tickers.py"
        issue: |
          WR-01：`payload` 属性（:118-131）只 catch `(OSError, ValueError)`，`label()` 的
          `unusable` 元组（:271）为 `(FileNotFoundError, ValueError, KeyError, AttributeError,
          TypeError)` —— 两者都不含 `RecursionError`（`RuntimeError` 子类）。深嵌套 JSON 让
          `json.loads` 抛 `RecursionError`，直接穿出 `label()` 与 `as_of()`。
          本验证实测（CrspTickerLookup 构造参数是**旁车文件路径**，不是 zarr 目录；传目录只会
          走到「文件缺失」分支而测不到这个洞）：
            sidecar = '{"a":' * 100000 + '1' + '}' * 100000
            label([13407], date(2020,1,1))  -> RAISED RecursionError
            as_of(13407, date(2020,1,1))    -> RAISED RecursionError（而非 shaped ValueError）
            '[' * 20000 + ']' * 20000       -> 同样两条都 RAISED RecursionError
          三个裸调用点全部无保护，其中 `quantlab/backtest/engine_vectorbt.py:303` 位于
          simulation 中途，`quantlab/dataset/masking.py:262` 与 `quantlab/base/model.py:1315`
          （经 `_spell`）同理。
      - path: "quantlab/dataset/crsp_tickers.py"
        issue: |
          WR-02：docstring（:267-269）逐字声称「`except Exception` is deliberately NOT used …
          so a genuine programming bug in this module still reaches the caller」，但同一次提交
          加了 `_intervals()`/`as_of()` 的结构守卫之后，`KeyError`/`AttributeError`/`TypeError`
          已不可能来自数据损坏，唯一来源就是本模块的 bug —— 元组吞掉的恰好是 docstring 承诺
          会上抛的那一类。本验证实测：对一份**完全合法**的旁车，把 `as_of` 里的
          `span["ticker"]` 改成 `span["tikcer"]`，`label([13407], date(2015,1,1))` 静默返回
          `['13407']` 而不是 `['FB']`；把 `_intervals` 改成 `self.paylaod` 同样静默返回数字。
      - path: "example/wrds_crsp.md"
        issue: ":247「**永不抛**，旁车缺失或损坏——含解析不了与解析得了但形状不对——都原样回落成数字」—— 同一条声明的活副本，被 WR-01 证伪"
      - path: "tests/test_crsp_ticker_sidecar.py"
        issue: ":420 `MALFORMED_SIDECARS` 只有三条（结构损坏），没有「解析阶段就爆栈」这一类，所以 WR-01 不会被现有参数化抓到"
    missing:
      - "WR-01：把 `RecursionError` 加进 `payload` 属性的 except 元组（放在 payload 而不是 label 的元组里，两个入口才都被覆盖，`as_of` 的拒绝也才保持 shaped）"
      - "WR-01：补一条深嵌套 JSON 的回归（`MALFORMED_SIDECARS` 是结构损坏的参数化，这一条是解析期失败，两类要分开）"
      - "WR-02：**需要操作者裁定**——要么把元组收窄为 `(FileNotFoundError, ValueError)` 并提到模块常量（reviewer 建议的 `_UNUSABLE`），要么保留三个 backstop 但把 :267-269 的 docstring 改成它实际成立的说法。REVIEW 明确要求「不要两者都留」"
      - "若选收窄：补一条「本模块内部 bug 会上抛而不是静默返回数字」的回归，否则这条设计意图仍然无人守"
  - gap_id: G-03.11-4
    truth: "本仓库当前任何一次测试运行的结果都反映工作树里的源码"
    status: failed
    severity: blocker
    reason: |
      `quantlab/dataset/__pycache__/crsp_tickers.cpython-313.pyc`（2026-09-21 17:00 本地 ≈ 21:00Z，
      晚于 REVIEW 的 20:55:21Z）里的字节码含有 WR-02 复现时注入的 `span["tikcer"]` 错字，
      而工作树源码是干净的（`git status --porcelain quantlab/dataset/crsp_tickers.py` 为空、
      `grep -rn "tikcer\|paylaod" quantlab/ tests/` 零命中）。
      因为 `ticker` 与 `tikcer` **长度相同**，恢复源码后文件 size 未变，若 mtime 亦被保留，
      CPython 的 mtime+size 失效判据就认为 .pyc 仍然新鲜、不重编译。本验证实测 pyc header：
        pyc records source mtime: 1790024442 size: 14427
        actual source  mtime: 1790024442 size: 14427   -> MATCH -> 判定为新鲜
      后果实测：
        uv run pytest -q tests/test_crsp_ticker_sidecar.py            -> 5 failed, 21 passed
        （同一份源码，改用干净缓存目录 -X pycache_prefix=<tmp> 重跑）  -> 98 passed
      所以现在这个工作树里跑出来的任何绿/红都不可采信，包括 handback 里给定的
      「current main: 55 failed, 1667 passed」——若该次运行发生在 21:00Z 之后，它测的不是 HEAD。
    artifacts:
      - path: "quantlab/dataset/__pycache__/crsp_tickers.cpython-313.pyc"
        issue: "陈旧但被判为新鲜的投毒字节码；源码干净，运行时行为是带 bug 的旧版本"
    missing:
      - "清除全仓字节码缓存：`find . -path ./.venv -prune -o -name '__pycache__' -type d -print0 | xargs -0 rm -rf`"
      - "清除后重跑回归门，重新记录基线与当前两组数字（若原基线运行早于 21:00Z 则只需重跑 current 一侧）"
      - "把「等长注入 + 恢复 → .pyc 不失效」写进复现类操作的注意事项：此后任何源码注入实验要么用子类/猴补丁（如本验证所用），要么在恢复后 `touch` 源文件或删缓存"
  - gap_id: G-03.11-5
    truth: "过滤审计报告里不出现逐字重复 JSON key 的冗余 `symbol` 字段（G-03.11-2 的操作者裁定落到盘上产物）"
    status: partial
    severity: minor
    reason: |
      代码侧已删干净（crsp.py:1263-1271 的字典字面量只剩四个键，两个调用点共用），
      但**盘上产物没有重新生成**，仍带着被裁定删除的字段。本验证直接读盘：
        data/data/us_equity/1d/wrds_crsp_sp500_1d.zarr.crsp_filter_report.json
        roster_overrides.permnos = {"75154": {"first": "2024-01-02", "last": "2024-12-31",
                                              "rows": 252, "symbol": "75154",
                                              "types": ["UG/EQTY/COM/CORP/N"]}}
      `"symbol": "75154"` 与 JSON key 逐字节相同 —— 正是 UAT test 2 裁定要删掉的那个字段。
      （`dropped_permnos` 为 `{}`，所以另一支无从观察。）
      这与本相位自己在 plan 01 立的 W0 规矩同类：盘上产物必须由当前 HEAD 的代码生成，
      否则测量对象与代码不是同一件东西。zarr 面板本身没有受影响（见 Part A G1/G2 回归列）。
    artifacts:
      - path: "data/data/us_equity/1d/wrds_crsp_sp500_1d.zarr.crsp_filter_report.json"
        issue: "`roster_overrides.permnos['75154'].symbol == '75154'` —— plan 13 删除的字段仍在盘上"
    missing:
      - "用 plan 01 的 `CrspStoreRebuilder`（或等价的重新转换）重生成 `.crsp_filter_report.json`，并复核 `roster_overrides.permnos` 的键集为 `{types, rows, first, last}`"
      - "或：明确裁定接受盘上产物滞后于代码，并把这一条写进 deferred-items.md（当前两处都没有记录，属于静默不一致）"
  - gap_id: G-03.11-6
    truth: "`crsp_tickers.py` 的结构守卫在它声称覆盖的范围内真正覆盖，且降级对操作者可见"
    status: partial
    severity: minor
    reason: "REVIEW 的 WR-03 与 WR-04，本验证复现 WR-04，WR-03 为代码读证（两个 `except unusable:` 分支都直接丢弃异常对象，无 logger/计数器）"
    artifacts:
      - path: "quantlab/dataset/crsp_tickers.py"
        issue: |
          WR-04：`product_end`（:192）直接 `self.payload.get(...)`，绕过 `_intervals()` 在
          :158-159 自称「The one place the payload's top-level shape is checked」的检查。
          本验证实测：payload 为 `[]` 时 `product_end` -> `AttributeError: 'list' object has
          no attribute 'get'` —— 正是 plan 12 声称要消灭的裸异常，而 `[]` 就是它自己
          `MALFORMED_SIDECARS` 里的三个输入之一。缓解：`product_end` 在 `quantlab/` 下零生产调用点
          （唯一引用是 tests/test_crsp_ticker_sidecar.py 的那条测试）。
      - path: "quantlab/dataset/crsp_tickers.py"
        issue: |
          WR-03：:272-275 与 :291-294 两处 `except unusable:` 都把异常整个丢弃，没有任何日志。
          而「数字而不是 ticker」同时也是无旁车 store（Tiingo/Alpaca，或 03.11-09 之前的 CRSP store）
          的**正常且受支持**的输出（:254-258 自己这么写的），两种状态在控制台上逐字节相同，
          需要重建的那一种因此不可见。与 `quantlab/base/config.py:142`/`:176` 的
          「never applied silently」姿态相抵。
    missing:
      - "WR-04：让 `product_end` 走同一条形状检查（或抽 `_object_payload()` 让 `_intervals` 与它共用，使 docstring 的「one place」成真）；或按 CLAUDE.md 的 no-backward-compat 直接删掉这个无生产调用点的方法"
      - "WR-03：加一次性降级日志（reviewer 给了 `_degrade()` 方案，`loguru` 是第三方、不违反本模块的 leaf 约束），并在既有参数化里断言 `warning_messages`"
deferred:
  - truth: "全量 suite 的 55 个失败（`ModuleNotFoundError: ingest_tiingo / ingest_binance_spot`）"
    addressed_in: "deferred-items.md D-03.11-12-A"
    evidence: "已证前置存在于相位基线 e95ad6b，与本相位任一 plan 无因果；编排器已逐行 diff 确认基线与当前 FAILED 行集逐字相同"
  - truth: "`tests/test_cross_sectional_zscore.py` 在 KunQuant `~MultiThreadExecutor()` 偶发死锁"
    addressed_in: "deferred-items.md D-03.11-12-B"
    evidence: "前置、间歇性，回归门以 --ignore 规避并已记录"
  - truth: "`FactorConfig.njobs` 默认 128"
    addressed_in: "deferred-items.md D-03.11-12-C"
    evidence: "quantlab/base/config.py:307，超出 12/13 两个 plan 的 files_modified，需要自己的 plan 重新基准测试"
advisory:
  - finding: "`tests/test_extensibility_contract.py` 模块 docstring（:6）点名的函数 `test_no_market_specific_logic_in_core_layers` 不存在，实际函数名是 `test_core_layer_purity_no_market_specific_logic`（:59）"
    category: other
    reason: "REVIEW IN-04。plan 13 就在它下面两行改了「三 → 四个核心层」却没顺手改函数名，按文档名 grep 会一无所获。一行修复，不构成缺口"
    evidence_status: "已读证：docstring :6 与函数定义 :59 不一致"
  - finding: "`tests/test_crsp_identity.py:1099` 这条替换断言弱于它的两个兄弟"
    category: other
    reason: "REVIEW IN-02。另两处（:590、:1481）都把 `\"symbol\" not in ...` 与 `set(record) == {types,rows,first,last}` 配对，这一处只有否定的一半 —— `{}` 也能满足它。plan 13 自己的 patterns-established 警告过「只删不补」，这是它的小号版本"
    evidence_status: "已读证三处断言原文"
  - finding: "`crsp_tickers.py:28-37` 模块 docstring 的「six human-visible points that call it」不准确：`grep -rn '\\.label(' quantlab/` 只有三个调用点（masking.py:262、engine_vectorbt.py:303、base/model.py:1315），另两处（inspector.py、utils/cli.py）只是在散文里提到类名"
    category: other
    reason: "REVIEW IN-01。计数本身是「守卫放在查表层而不是六个调用点」这个设计论证的前提，且 plan 12 把这个数字又复制进了 base/model.py:1310 与 tests/test_model_predict_panel.py"
    evidence_status: "已复现 grep：quantlab/ 下 `.label(` 命中 2 处 + 经 `_spell` 的 model.py 一处"
  - finding: "`tests/test_extensibility_contract.py` 新加的注释说把 `base/backtest.py` 纳入门「green today rather than an owed debt」，容易被读成该文件已受有意义的纯度覆盖"
    category: architectural
    reason: "REVIEW IN-03。该文件仍有两处具体厂商/市场耦合是这个门结构上看不见的（:15 的 `CrspTickerLookup` 具名 import，按裁定 (b) 故意排除；:153 docstring 里的 `USEquityCrossectionSelectStockVectorBt`）。操作者裁定被正确遵守，这只是给下一个读者的提示，不需要改代码"
    evidence_status: "已读证 CORE_LAYER_FILES（4 项）与 FORBIDDEN_SUBSTRINGS（原 4 项，未被污染）"
behavior_unverified_items:
  - truth: "既有 backtest run 的指纹失效会让 `_compare_fingerprints` 刷一屏 warning（D-12 已接受），且它没有掩盖真实问题（plan 10 backstop）"
    test: "在 PERMNO 轴重建后的 store 上重跑一个既有 backtest run（load 模式），读完 `_compare_fingerprints` 打出的全部 warning"
    expected: "每一条 warning 都能归因到 D-12 已接受的指纹失效（digest / n_symbols 变了），没有一条是别的真实问题被裹在里面"
    why_human: "没有任何测试驱动 `quantlab/base/backtest.py:1145-1188` 的 warning 路径；03.11-11-SUMMARY.md:119/:238/:329 自报此项按 operator 类别 auto-approved、无人逐字读过。03.11-UAT.md test 4 至今 `blocked_by: other`，从未跑过"
  - truth: "同一条 backstop 在 plan 11 的重复声明"
    test: "同上"
    expected: "同上"
    why_human: "同上 —— 两次声明、两次都未被人执行；这是上一轮 77/79 扣掉的两条，本轮仍然扣掉"
human_verification:
  - test: "**裁定**（G-03.11-3 / WR-02）：`CrspTickerLookup.label()` 的 `unusable` 元组要收窄成 `(FileNotFoundError, ValueError)`，还是保留五元组、改 docstring :267-269 的说法？"
    expected: "一个明确的二选一。REVIEW 逐字要求「Do not leave both」——现状是元组吞掉了 docstring 承诺会上抛的那一类 bug，两种修法都能让它自洽，但含义不同：收窄 = 把本模块 bug 暴露给调用方（与原设计意图一致，代价是展示层多了一条可能抛出的路径）；改 docstring = 承认这三个类型是 defence-in-depth 的 backstop（代价是接受本模块 bug 会静默成数字）"
    why_human: "这是设计取舍不是缺陷判定。本验证已复现「合法旁车 + 一个字符错字 → 静默返回 ['13407'] 而非 ['FB']」，但哪一边是对的取决于操作者怎么看展示层的失败姿态"
  - test: "**执行**（UAT test 4，仍 blocked）：在 PERMNO 轴重建后的 store 上重跑一个既有 backtest run（load 模式），逐条读完 `_compare_fingerprints` 的 warning"
    expected: "每条 warning 都能归因到 D-12 已接受的指纹失效，没有别的真实问题被裹在里面"
    why_human: "`backtest.py:1145-1188` 无测试覆盖；UAT 本轮无法完成，保持 outstanding。这是 88/91 扣掉的三条里的两条"
  - test: "**裁定**（G-03.11-5）：盘上 `.crsp_filter_report.json` 要不要现在重生成？"
    expected: "要么重跑 `CrspStoreRebuilder` 让产物与 HEAD 一致，要么明确接受滞后并写进 deferred-items.md"
    why_human: "重生成会动到 gitignored 的真实数据目录，需要操作者授权；且需要确认当前 raw tier 仍在位"
---

# Phase 03.11: CRSP PERMNO 身份轴迁移与 Tiingo 时代死代码清除 —— 验证报告（重验）

**Phase Goal（ROADMAP.md:152-157）：** CRSP 价格面板的 `symbol` 维变成 **int64 PERMNO** 而不是每个日期上有效的 ticker；每一处「只为了让被回收/改名的 ticker 还能当身份轴用」的适配都被**删除**而不是藏进兼容分支；`ticker` 完全离开面板，进旁车做 as-of 展示查询。

**Verified:** 2026-09-21T21:24:31Z
**Status:** gaps_found
**Score:** 88/91 must-haves verified（2 条 behavior-unverified）
**Re-verification:** 是 —— 上一份（2026-09-21T11:30:11Z，77/79，human_needed）已被 `verification status` 判为 stale；本轮覆盖 13 个 plan 全集，重点复验 gap-closure 的 12/13 两个 plan 与新增的 03.11-REVIEW.md / 03.11-UAT.md。

> **本轮最重要的一条，先说。** 这个工作树的 `__pycache__` 里有一份**被投毒但被 Python 判为新鲜**的字节码
> （G-03.11-4）。清缓存前跑出来的任何测试结果都不反映 HEAD。本报告里所有「实跑」结论都是在
> `-X pycache_prefix=<干净临时目录>` 下取得的；未加该参数时同一批测试是 **5 failed**。

---

## Goal Achievement

### Part A —— 相位目标可观察真值（12 条，回归复检）

上一轮已逐条验证并通过；本轮按重验规则做回归检查（存在性 + 基本合理性），不重做全量取证。

| # | Truth | 本轮状态 | 回归证据（本轮实测） |
|---|-------|---------|-------------------|
| G1 | 面板 `symbol` 维是 int64 PERMNO | ✓ VERIFIED（无回归） | `quantlab/dataset/crsp.py:568` `frame.with_columns(pl.col("symbol").cast(pl.Int64))` 仍在；盘上 `wrds_crsp_sp500_1d.zarr` 实读 dims 252×520，`symbol` dtype **int64**，值 `[10104 10107 10138 10145 10516 …]` |
| G2 | `permno` 不再是数据变量 | ✓ VERIFIED（无回归） | 盘上 `'permno' in ds.data_vars` → **False**，`len(ds.data_vars)` = 27 |
| G3 | 被回收的 ticker 不能把两家公司拼成一列 | ✓ VERIFIED（无回归） | `tests/test_crsp_tracer.py` 全绿（与 residue/axis/ingest 合跑 **46 passed**） |
| G4 | 改名不断开身份（13407 跨 FB→META 是一列） | ✓ VERIFIED（无回归） | 同上批次 |
| G5 | 身份适配机制被**删除**而非藏进兼容分支 | ✓ VERIFIED（无回归） | `tests/test_no_identity_residue.py` 全绿（同批 46 passed） |
| G6 | `CrspSymbology` 收缩成纯区间提供者（D-18） | ✓ VERIFIED（无回归） | 文件仍 152 行；公开面只有 `SUFFIX_DELIMITER`(:53)、`__init__`(:72)、`symbol_intervals()`(:78) |
| G7 | `ticker` 离开面板、进旁车做 as-of | ✓ VERIFIED（无回归） | 盘上 `'ticker' in ds.variables` → **False**；`{zarr}.crsp_tickers.json` 存在，键为 `{generated_from, intervals, vintage_product_end}` |
| G8 | sidecar 只写面板实际包含的 PERMNO | ✓ VERIFIED（无回归） | sidecar `intervals` 键数 = **520** = 面板 symbol 轴长 |
| G9 | int64 硬破坏点修好，`widen_symbol_axis` 不静默清空 | ✓ VERIFIED（无回归） | `quantlab/dataset/backend.py:13` 引入 `normalize_to_axis_dtype`/`sort_symbol_axis`，:502 在归一化后的值上做 superset 守卫；`tests/test_symbol_axis_widening.py` + `test_widen_crash_residue.py` 在 **158 passed** 批次内全绿 |
| G10 | `universe_filter.py` 条件 (a) 删除、(b)(c) 保留 | ✓ VERIFIED（无回归） | `NON_COMMON_TICKER_PATTERNS` / `exclude_non_common` / `is_common_ticker` 在 `quantlab/` 下命中数 **0** |
| G11 | 03.10 locked decision 的反转被记录、03.10 原文未改 | ✓ VERIFIED（无回归） | `.planning/ROADMAP.md:349`「**REVERSED by Phase 03.11** — the decision above is 03.10's own record and is left word-for-word」；`.planning/STATE.md:306` 完整反转记录在位 |
| G12 | **DATA-03 仍为 Pending**（「No requirement is COMPLETED here」） | ✓ VERIFIED（无回归） | `.planning/REQUIREMENTS.md:23` 仍是 `- [ ] **DATA-03**`；:139 追踪行只**追加**了 advanced-by 段落，结尾逐字「Status unchanged: still Pending, still owned by Phase 2」 |

**Part A 小结：12/12 相位目标真值成立，零回归。** 相位的核心目标——身份轴换成 int64 PERMNO、适配机制删除而非藏起、`ticker` 进旁车——在代码与**盘上产物**两侧都成立。

### Part B —— plan must-haves（91 条）

| 来源 | 条数 | VERIFIED | FAILED | behavior-unverified |
|---|---|---|---|---|
| plans 01-11（上一轮已验，本轮回归） | 79 | 77 | 0 | 2 |
| plan 12（G-03.11-3 收口） | 6 | 5 | 1 | 0 |
| plan 13（G-03.11-1/2 收口） | 6 | 6 | 0 | 0 |
| **合计** | **91** | **88** | **1** | **2** |

#### plan 12 must-haves 逐条

| # | Truth | 状态 | 证据（本轮实测，非采信 SUMMARY） |
|---|-------|------|------|
| 12-1 | `label()` 面对三个已复现的结构损坏输入全部返回数字 | ✓ VERIFIED | 实跑三例：`{"intervals":[1,2,3]}` / `{"intervals":{"13407":[{"ticker":"FB"}]}}` / `[]` → 均为 `['13407']` |
| 12-2 | `as_of()` 在同样三输入下抛**带类名、路径与重建补救**的 `ValueError` | ✓ VERIFIED | 实跑三例均 `ValueError: CrspTickerLookup: the ticker sidecar '<绝对路径>' parsed as JSON but is not shaped like a ticker sidecar (…). … Delete the store together with its '.crsp_*.json' sidecars and re-convert` |
| 12-3 | `label()` 的守卫覆盖**两段**（读 intervals + 逐 PERMNO 的 `as_of`） | ✓ VERIFIED | 代码读证 :272-275 与 :291-294 两处 `except unusable:`；REVIEW 另行逐段移除验证两段皆 load-bearing（去掉第一段放跑 `[1,2,3]`/`[]`，去掉第二段放跑缺 `start` 的 span） |
| 12-4 | `predict_panel` 快乐路径在旁车损坏时仍成功返回、warning 回落成数字 | ✓ VERIFIED | 行为测试存在且实跑通过：`tests/test_model_predict_panel.py:705 test_a_corrupt_ticker_sidecar_leaves_the_warning_working`（:678 另有缺文件版） |
| 12-5 | 缺文件 / JSON 解析失败两类既有行为一字不变 | ✓ VERIFIED | 实跑缺文件：`label([13407,"AAPL"], …)` → `['13407','AAPL']`；`as_of` → `FileNotFoundError`。解析失败由 `payload` 的 `except (OSError, ValueError)` → shaped `ValueError` 覆盖 |
| 12-6 | 「永不抛」声明的两个活副本都写明**缺失或损坏** | ✗ FAILED | 文本确实改了（`crsp_tickers.py:250-253`「missing or CORRUPT -- where corrupt means both halves of it」；`example/wrds_crsp.md:247`「缺失或损坏——含解析不了与解析得了但形状不对」），**但改后的声明被 WR-01 证伪**：深嵌套 JSON 正属「解析不了」，实测 `label()` 抛 `RecursionError`。这条 must-have 的目的是让声明成真，文字达成而实质未达成 → 计为 FAILED，纳入 G-03.11-3 |

#### plan 13 must-haves 逐条

| # | Truth | 状态 | 证据（本轮实测） |
|---|-------|------|------|
| 13-1 | `quantlab/base/backtest.py` 在 `CORE_LAYER_FILES` 里 | ✓ VERIFIED | `tests/test_extensibility_contract.py:31-36` 四项，含 `"quantlab/base/backtest.py"` |
| 13-2 | `FORBIDDEN_SUBSTRINGS` 一字未改，查表类**没有**被加进去 | ✓ VERIFIED | :51-56 仍为 `("SpotKlineDataset","StockDataset","crypto_spot","us_equity")`，无 `CrspTickerLookup`（被否决的选项 (a) 未被执行）；:38-50 的注释完整记录了裁定 (b) 的来由 |
| 13-3 | `_permno_breakdown` 每条记录只有 `types/rows/first/last` | ✓ VERIFIED | `crsp.py:1263-1271` 字典字面量只有四个键；:1226-1252 docstring 逐字说明为什么删（G-03.11-2）以及「若将来要人可读的名字，取数源是 `self._symbology`，**不是** `.crsp_tickers.json`」 |
| 13-4 | 删除同时作用于两个调用点 | ✓ VERIFIED | `grep -n "_permno_breakdown" quantlab/dataset/crsp.py` → :1099（`roster_overrides["permnos"]`）、:1116（`dropped_permnos`）、:1226（定义），两支共用同一渲染 |
| 13-5 | 三处锁死断言全部解掉，各补一条 `"symbol" not in ...` 正向断言 | ✓ VERIFIED | `tests/test_crsp_identity.py:590`、:1099、:1481 三处；:590 与 :1481 另配 `set(...) == {"types","rows","first","last"}` 并逐字段断值（:1099 只有否定一半，见 advisory IN-02） |
| 13-6 | 两份文档字段清单不再点名 `symbol`；三处散文不再引用已删代码但保留设计论点 | ✓ VERIFIED | `example/wrds_crsp.md:322-327`（表格 + 「这份清单里**没有 ticker/名字字段**……」）与 :865-867 两处清单均无 `symbol`；`crsp.py:1294-1311`（`_build_ticker_intervals` docstring）保留了「区间表 vs last-name-wins」全部论点并接上 G-03.11-2 |

### 必需产物

| Artifact | 期望 | 状态 | 细节 |
|---|---|---|---|
| `quantlab/dataset/crsp_tickers.py` | 结构守卫 + 两段 `label()` 回落 | ⚠️ 部分 | 296 行，守卫存在且 load-bearing；但 `RecursionError` 逃逸（WR-01）、元组方向与 docstring 相反（WR-02）、`product_end` 绕过形状检查（WR-04）、降级完全静默（WR-03） |
| `tests/test_crsp_ticker_sidecar.py` | 损坏旁车的参数化回归 | ⚠️ 部分 | `MALFORMED_SIDECARS`（:420）三条 + 两处参数化（:438、:455）实跑通过；缺「解析期爆栈」一类，所以 WR-01 不会被抓到 |
| `tests/test_model_predict_panel.py` | 快乐路径在损坏旁车下仍成功 | ✓ VERIFIED | :678 / :705 两条实跑通过 |
| `tests/test_extensibility_contract.py` | 核心层纯度门覆盖 `base/backtest.py` | ✓ VERIFIED | 四项 CORE_LAYER_FILES，实跑通过（模块 docstring 的函数名过期，见 advisory IN-04） |
| `quantlab/dataset/crsp.py` | `_permno_breakdown` 无 `symbol` 字段 | ✓ VERIFIED | 代码侧已删；**盘上产物未重生成**，见 G-03.11-5 |
| `tests/test_crsp_identity.py` | 三处正向断言接管 | ✓ VERIFIED | :590 / :1099 / :1481 |
| `example/wrds_crsp.md` | 字段清单与「永不抛」声明同步 | ⚠️ 部分 | 字段清单已同步（13-6 通过）；:247 的「永不抛」是被 WR-01 证伪的第三个副本 |
| `data/.../wrds_crsp_sp500_1d.zarr` | int64 PERMNO 轴、无 ticker/permno 变量 | ✓ VERIFIED | 实读 252×520、int64、27 data_vars、无 `ticker` |
| `data/.../.crsp_filter_report.json` | 无冗余 `symbol` 字段 | ✗ 陈旧 | `roster_overrides.permnos['75154'].symbol == '75154'` 仍在盘上（G-03.11-5） |

### 关键连接（Key Links）

| From | To | Via | 状态 | 细节 |
|---|---|---|---|---|
| `_intervals()` | `as_of()` 结构守卫 | `_malformed()` 统一消息 | ✓ WIRED | :170-182 → :217-240 五处 `raise self._malformed(...)` |
| `as_of()` | `label()` 的两段 except | `unusable` 元组 | ⚠️ PARTIAL | 连通且 load-bearing，但元组不含 `RecursionError`（WR-01）、多含三个只会吞 bug 的类型（WR-02） |
| `label()` | `quantlab/base/model.py:1315`（经 `_spell`） | 裸调用，快乐路径 | ⚠️ PARTIAL | 三个已复现输入下不再崩（12-4 实证）；`RecursionError` 下仍会崩 |
| `label()` | `quantlab/backtest/engine_vectorbt.py:303` | 裸调用，simulation 中途 | ⚠️ PARTIAL | 同上；这是 WR-01 最贵的落点 |
| `label()` | `quantlab/dataset/masking.py:262` | 裸调用 | ⚠️ PARTIAL | 同上 |
| `_permno_breakdown` | `dropped_permnos`(:1116) + `roster_overrides["permnos"]`(:1099) | 同一渲染函数 | ✓ WIRED | 一处字典字面量，两支同时生效（by construction） |
| `CORE_LAYER_FILES` | `test_core_layer_purity_no_market_specific_logic`(:59) | 遍历 + 子串扫描 | ✓ WIRED | 门对 `base/backtest.py` 今日为绿，挡的是未来 market-literal 泄漏（覆盖范围之窄见 advisory IN-03） |

### 数据流追踪（Level 4）

| Artifact | 数据变量 | 来源 | 产出真实数据 | 状态 |
|---|---|---|---|---|
| `wrds_crsp_sp500_1d.zarr` | `symbol` 坐标 | `crsp.py:568` 的 `cast(pl.Int64)` ← CRSP `permno` 列 | 是（520 个真实 PERMNO，非占位） | ✓ FLOWING |
| `.crsp_tickers.json` | `intervals` | `crsp.py:_build_ticker_intervals(derived)`（在 `_apply_security_filter` 之后） | 是（520 键 = 面板轴长，13407 有 FB/META 两段） | ✓ FLOWING |
| `.crsp_filter_report.json` | `roster_overrides.permnos` | `crsp.py:1099` → `_permno_breakdown` | 是，但**产物早于当前代码** | ⚠️ STATIC（陈旧，G-03.11-5） |
| `CrspTickerLookup.label()` 输出 | 展示串 | 旁车 as-of 查询，失败回落数字 | 是（缺旁车/三类损坏下回落；`RecursionError` 下抛出） | ⚠️ 见 G-03.11-3 |

### 行为抽检（Behavioral Spot-Checks）

| 行为 | 命令 | 结果 | 状态 |
|---|---|---|---|
| gap-closure 四个 suite（干净字节码缓存） | `uv run python -X pycache_prefix=<tmp> -m pytest -q tests/test_crsp_ticker_sidecar.py tests/test_model_predict_panel.py tests/test_crsp_identity.py tests/test_extensibility_contract.py` | **98 passed** | ✓ PASS |
| 同上，**不**换缓存目录 | `uv run pytest -q tests/test_crsp_ticker_sidecar.py` | **5 failed, 21 passed** | ✗ FAIL（G-03.11-4，非源码缺陷） |
| 身份/轴/摄取回归 | `pytest -q tests/test_no_identity_residue.py tests/test_crsp_tracer.py tests/test_symbol_axis_contract.py tests/test_ingest_wrds_crsp.py` | **46 passed** | ✓ PASS |
| CRSP/universe/backtest 契约回归 | `pytest -q tests/test_crsp_dataset.py tests/test_crsp_symbology.py tests/test_crsp_constituent.py tests/test_crsp_membership.py tests/test_universe_mask.py tests/test_symbol_axis_widening.py tests/test_symbol_coord_encoding.py tests/test_widen_crash_residue.py tests/test_crsp_rebuild.py tests/test_backtest_contracts.py` | **158 passed** | ✓ PASS |
| WR-01 复现 | 深嵌套旁车 → `label([13407], date(2020,1,1))` | **RAISED RecursionError** | ✗ FAIL（已立 gap） |
| WR-02 复现 | 合法旁车 + `span["tikcer"]` 子类注入 → `label(…)` | 静默返回 `['13407']`（应为 `['FB']`） | ✗ FAIL（已立 gap） |
| WR-04 复现 | payload `[]` → `product_end` | **RAISED AttributeError**（裸） | ✗ FAIL（已立 gap） |
| 盘上面板契约 | `xr.open_zarr(...)` 读 dims/dtype/data_vars | 252×520 / int64 / 27 / 无 ticker 无 permno | ✓ PASS |
| 盘上审计报告 | 读 `.crsp_filter_report.json` | `roster_overrides.permnos['75154'].symbol == '75154'` | ✗ FAIL（G-03.11-5） |

> 回归门（基线 `e95ad6b` 55F/1658P/1S vs 当前 main 55F/1667P/1S，FAILED 行集逐字相同）由编排器给定，本验证未重跑 —— 但 **G-03.11-4 要求在清缓存后复核当前 main 一侧**，因为投毒发生在 ≈21:00Z，可能晚于那次运行。

### Probe 执行

本相位未声明 `scripts/*/tests/probe-*.sh` 形态的探针，`find scripts -path '*/tests/probe-*.sh'` 零命中，PLAN/SUMMARY 亦未提及 probe / PASS marker。**Step 7c: SKIPPED（无探针）**，行为证据由上表的实跑测试承担。

### 需求覆盖

| Requirement | 来源 Plan | 描述 | 状态 | 证据 |
|---|---|---|---|---|
| DATA-03 | 03.11-01 … 03.11-13（**全部 13 个 plan 逐个声明 `requirements: [DATA-03]`**） | 数据层抽象按市场/频率可扩展，新增市场不改上层因子/模型/回测 | ✓ SATISFIED（as **advanced**，非 completed） | `.planning/REQUIREMENTS.md:139` 追踪行已追加本相位的 advanced-by 段落，结尾逐字「Status unchanged: still Pending, still owned by Phase 2」；:23 checkbox 仍未勾选 —— 与 ROADMAP:159-160「No requirement is COMPLETED here」一致 |

**孤儿需求检查：** `grep -nE "Phase 03\.11" .planning/REQUIREMENTS.md` 只命中 DATA-03 一行，没有任何「映射到 03.11 但无 plan 认领」的需求。**零孤儿。**

**可追溯性观察（非相位失败）：** 交办说明称 `gsd-tools requirements mark-complete DATA-03` 返回 `not_found`，并推断 DATA-03「未被 REQUIREMENTS.md 跟踪」。**这个推断不成立** —— 本验证实测 DATA-03 确实存在于 `.planning/REQUIREMENTS.md`（:23 的 checkbox 与 :139 的追踪行，后者已记载本相位的贡献）。`mark-complete` 返回 `not_found` 是工具语义：该动词只对**由当前相位拥有**的需求生效，而 DATA-03 的 owner 是 Phase 2。本相位按 ROADMAP 契约本就不该把它标完成，因此这不是缺口、也不是需要修的台账不一致；记录在此只是防止下一轮验证重新推导出错误结论。

### 反模式扫描

扫描范围：gap-closure 区间 `e95ad6b..HEAD` 实际改动的 8 个文件（`git diff --name-only`）。

| 文件 | 行 | 模式 | 严重度 | 影响 |
|---|---|---|---|---|
| `quantlab/dataset/__pycache__/crsp_tickers.cpython-313.pyc` | — | 陈旧但被判新鲜的投毒字节码 | 🛑 Blocker | 任何测试结果都不反映 HEAD（G-03.11-4） |
| `quantlab/dataset/crsp_tickers.py` | 118-131 / 271 | except 元组既漏（`RecursionError`）又多（三个只会吞本模块 bug 的类型） | 🛑 Blocker | 「永不抛」契约有可达逃逸口；三个裸调用点全部建立在该契约上（G-03.11-3） |
| `quantlab/dataset/crsp_tickers.py` | 272-275 / 291-294 | `except unusable:` 丢弃异常对象，零日志 | ⚠️ Warning | 损坏旁车与无旁车在控制台上不可区分（G-03.11-6 / WR-03） |
| `quantlab/dataset/crsp_tickers.py` | 192 | `product_end` 绕过 `_intervals()` 自称唯一的形状检查 | ⚠️ Warning | `[]` payload 下抛裸 `AttributeError`；无生产调用点（G-03.11-6 / WR-04） |
| `tests/test_crsp_identity.py` | 1099 | 替换断言只有否定一半 | ℹ️ Info | `{}` 也能通过；见 advisory IN-02 |
| `tests/test_extensibility_contract.py` | 6 | docstring 点名的函数名不存在 | ℹ️ Info | 见 advisory IN-04 |
| `quantlab/dataset/crsp_tickers.py` | 28-37 | 「six human-visible points that call it」计数不准（实为 3 个调用点） | ℹ️ Info | 该计数被 plan 12 复制进了另两个文件；见 advisory IN-01 |
| `tests/test_extensibility_contract.py` | 38-50 | 「green today rather than an owed debt」易被读成纯度已证 | ℹ️ Info | 见 advisory IN-03 |

**债务标记门（TBD / FIXME / XXX）：** 对 8 个改动文件扫描，**零命中**。无未引用的债务标记。

**安全：** 本区间无新增文件写、网络、认证或反序列化路径；`json.loads` 是唯一解析器，已被 `payload` 守卫包住（补上 WR-01 后闭合）。`_malformed()` 打印旁车绝对路径这一点早前已按 `T-03.11-44` 裁定 accept，本区间未改。无硬编码凭证、无 `eval`、无 subprocess —— 与 CLAUDE.md 的凭证约束一致。

---

## 需要人工的事项

### 1.【裁定】WR-02：`unusable` 元组收窄，还是改 docstring？

**要做的：** 在 `quantlab/dataset/crsp_tickers.py:267-271` 二选一。
**期望结果：** 一个明确裁定。收窄成 `(FileNotFoundError, ValueError)`（并提到模块常量 `_UNUSABLE`，两个 `except` 站点共用一份拼写）= 坚持原设计意图，本模块 bug 上抛给调用方；保留五元组 + 改 docstring = 承认这三个类型是 defence-in-depth backstop，接受本模块 bug 静默成数字。REVIEW 逐字要求「Do not leave both」。
**为什么要人：** 这是设计取舍不是缺陷判定。缺陷事实已复现（合法旁车 + 一个字符错字 → `label` 静默返回 `['13407']` 而非 `['FB']`，`tests/test_crsp_ticker_sidecar.py` 的快乐路径不会变红），但哪一边正确取决于操作者怎么看展示层的失败姿态。

### 2.【执行】UAT test 4：`_compare_fingerprints` warning 风暴 backstop（仍 blocked）

**要做的：** 在 PERMNO 轴重建后的 store 上重跑一个既有 backtest run（load 模式），逐条读完 `_compare_fingerprints`（`quantlab/base/backtest.py:1145-1188`）打出的全部 warning。
**期望结果：** 每条 warning 都能归因到 D-12 已接受的指纹失效（digest / n_symbols 变了），没有一条是别的真实问题被裹在里面。
**为什么要人：** 该路径无任何测试驱动；plan 10 与 plan 11 各把它声明为 `verification: backstop`，`03.11-11-SUMMARY.md:119/:238/:329` 自报 auto-approved、无人逐字读过。`03.11-UAT.md` test 4 至今 `blocked_by: other`，从未执行 —— 这是上一轮 77/79 扣掉的两条，本轮 88/91 里仍然扣着。

### 3.【裁定】G-03.11-5：盘上 `.crsp_filter_report.json` 要不要现在重生成？

**要做的：** 重跑 `CrspStoreRebuilder`（plan 01 建立的两层重建入口）让产物与 HEAD 一致，或明确接受滞后并写进 `deferred-items.md`。
**期望结果：** 二选一，且结果落在文档里而不是留作静默不一致。
**为什么要人：** 重生成会动到 gitignored 的真实数据目录，需要操作者授权，并需确认 raw tier 仍在位。

---

## Gaps Summary

**相位的核心目标达成了。** 12/12 相位目标真值在代码与盘上产物两侧同时成立：`symbol` 轴是 int64 PERMNO（实读 dtype 与 520 个真实 PERMNO 值）、`permno` 与 `ticker` 都已离开面板、适配机制被仓库级零残留门锁住、旁车是区间表而非 last-name-wins、03.10 的反转按契约记录在三处且原文未改、DATA-03 仍为 Pending。gap-closure 的 G-03.11-1 与 G-03.11-2 **确实收口了**，且被否决的选项 (a) 没有被偷偷执行 —— 这两条本轮逐条复验通过。

**没达成的是 G-03.11-3 的那条声明本身。** plan 12 把契约从「缺失时不抛」升级成了「缺失**或损坏**时都不抛」，然后留了一个可达的反例：深嵌套 JSON 让 `json.loads` 抛 `RecursionError`（`RuntimeError` 子类），`payload` 的 `(OSError, ValueError)` 与 `label` 的五元组都不含它，于是它从三个**裸**调用点穿出去，其中 `engine_vectorbt.py:303` 在 simulation 中途。更糟的是方向：同一次提交加的结构守卫让五元组里的 `KeyError`/`AttributeError`/`TypeError` 再也不可能来自数据损坏，唯一来源变成本模块自己的 bug —— 而 docstring 正好声称这个元组的存在理由是让这类 bug 上抛。实测一个字符的错字就能让合法旁车静默降级成数字，而快乐路径测试全绿。**一条宣称「永不抛」却留着一个洞的契约，比一条从未宣称过的更危险**，因为三个调用点都是靠它才不包 try 的。

**另外三条是本轮新发现的，上一份验证里没有。**

其一，也是最该先处理的：这个工作树的 `__pycache__` 被投毒了，而且 Python 认不出来。WR-02 复现时注入的 `span["tikcer"]` 与原文 `span["ticker"]` **等长**，恢复源码后文件 size 不变、mtime 亦未变，CPython 的 mtime+size 失效判据因此判定 `.pyc` 仍然新鲜。结果是：源码干净（`git status` 空、`grep tikcer` 零命中），但每一个 Python 进程 import 到的都是带 bug 的旧字节码 —— 同一批测试不换缓存目录是 5 failed，换成干净 `pycache_prefix` 是 98 passed。**清缓存之前，这个仓库里跑出来的任何绿或红都不可采信**，包括交办里给定的「current main: 55 failed, 1667 passed」（若那次运行晚于 ≈21:00Z）。

其二，G-03.11-2 的裁定只落了一半：代码不再输出 `symbol` 字段，但盘上的 `.crsp_filter_report.json` 没有重新生成，`roster_overrides.permnos['75154'].symbol` 仍然逐字节等于它自己的 key —— 正是操作者裁定要删的那个冗余字段。这与本相位在 plan 01 自己立的 W0 规矩同类：测量对象必须由当前 HEAD 生成。zarr 面板本身没受影响。

其三是同一个模块里的两处小缺陷：损坏旁车的降级**完全静默**（两个 `except` 分支都把异常整个丢掉），而「数字而不是 ticker」恰好也是无旁车 store 的正常输出，两种状态在控制台上逐字节相同 —— 需要重建的那一种因此不可见，这与 `quantlab/base/config.py:142`/`:176` 的「never applied silently」姿态相抵；以及 `product_end` 绕过了 `_intervals()` 自称唯一的形状检查，对 `[]`（它自己 `MALFORMED_SIDECARS` 三个输入之一）仍然抛裸 `AttributeError`（缓解：该方法在 `quantlab/` 下零生产调用点，按 CLAUDE.md 的 no-backward-compat 也可以直接删）。

**四条 gap 按处理顺序：** G-03.11-4（清缓存并重跑回归门，否则后续一切证据都站不住）→ G-03.11-3（WR-01 一行修复 + 一条回归；WR-02 需要操作者先裁定方向）→ G-03.11-6（WR-03 一次性降级日志 + WR-04 一行走同一检查或删掉）→ G-03.11-5（重生成产物或明确记为 deferred）。三条 deferred 项（55 个前置失败、KunQuant teardown 死锁、`njobs=128`）已在 `deferred-items.md` 立项，**不是**本相位缺口，本报告不重复计入。

---

_Verified: 2026-09-21T21:24:31Z_
_Verifier: Claude (gsd-verifier)_
_Mode: re-verification（前一份 2026-09-21T11:30:11Z / 77/79 / human_needed，已 stale）_
