---
phase: 03.11
plan: 11
type: execute
wave: 8
depends_on: ["03.11-10"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [DATA-03]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Phase 03.10 的 locked decision 反转被记录在三处：ROADMAP.md 里 03.10 Identifier 决策**下方**的显式 REVERSED 标注、STATE.md 的一条决策记录、以及 example/wrds_crsp.md（plan 10 已写）—— 03.10 的历史文本本身一字未改（D-08）"
    - "REQUIREMENTS.md 的 DATA-03 只追加一条 advanced-by 记录，状态标记不改、任何计数不动"
    - "D-02 的跨 vendor dtype/交集断言以 checkpoint:decision 交给 operator，不预设它被采纳；未采纳则落成一条 backlog todo（D-21）"
    - "D-20（validate_schema 不给 CRSP 专属 required_columns，numtrd 的 warning 保留）由 operator 在 checkpoint 上确认，理由是消掉它需要动 cleaning.py（D-06 禁止）或一份本 phase 没有证据塑造的列契约"
    - "全套测试相对 03.10 基线（1694 collected）零新增失败"
    - statement: "既有 backtest run 的指纹失效会让 _compare_fingerprints 刷一屏 warning（D-12 已接受），需确认它没有掩盖真实问题"
      verification: backstop
  artifacts:
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/REQUIREMENTS.md
  key_links:
    - "ROADMAP.md 里由 `grep -n '^- Identifier: the panel' .planning/ROADMAP.md` 唯一定位的那一条 03.10 Identifier 决策（一个 3 行 bullet，末行以 `\"no ticker-rename mapping\" todo.` 结束）→ 其下方新增的 REVERSED 标注 → 本 phase 的 D-08"
    - "ROADMAP.md 里 03.11 自己那段已写的反转（`grep -n 'deliberately reverses that'`）→ 无需改动"
    - "REQUIREMENTS.md:139（DATA-03 的 Pending 行）→ 追加 advanced-by 记录"
  prohibitions:
    - "不得编辑 Phase 03.10 的历史决策文本本身 —— 只在其下方加一行显式的 REVERSED 标注（D-08）"
    - "不得改动 REQUIREMENTS.md 里 DATA-03 的状态标记或任何计数"
    - "不得用 sed 范围删除改任何 planning 文档（operator P6：漏匹配会删到 EOF；提交前核对行数）"
    - "不得在 operator 未批准前实现 D-21 的跨 vendor 断言"
    - "不修改 quantlab/dataset/cleaning.py（D-06；这也正是 D-20 选默认项的理由）"
    - "不写任何 config 迁移或兼容层（D-04）"
---

<objective>
收尾之二：把 Phase 03.10 的 locked decision 反转记录下来，把需求台账追加一条，跑完全套回归门，
并把两件**必须由 operator 决定**的事交出去。

**记录反转本身就是一项交付物。** 03.10 的 locked decision 逐字是「the panel `symbol` dimension stays the
**ticker** valid at each date (matching the universes, factors and backtester); **PERMNO is kept as a data
variable**, mapped via the CRSP ticker history.」那个决策在当时已知的信息下是稳的；它被反转，依据是 03.10
交付之后才测到的证据：8,719/36,990 个 ticker（23.6%）被 ≥2 个 PERMNO 用过，18,850/39,506 个 PERMNO（47.7%）
与别人共用过 ticker，而 `resolve_collisions` 的 `group_by(["timestamp","symbol"])` **只看得见同日争用**——
在 2000–2024 窗口里 3,095/3,205 个被回收的 ticker（96.6%）是纯顺序继承，它一个都看不见。

**不靠篡改历史来消除矛盾。** 03.10 的文本一字不改，反转写在它**下方**，并指向本 phase 的 D-08。

Purpose: 让这次反转对后来者是可读的，而不是一处无人解释的前后不一致。
Output: 三处反转记录 + 一条需求台账追加 + 两个 operator 决策 + 全套回归门。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-CONTEXT.md
@.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-RESEARCH.md
@.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-10-SUMMARY.md
</context>

## Artifacts this phase produces

| 符号 | 位置 | 类型 |
|---|---|---|
| 03.10 Identifier 决策下方的 `**REVERSED by Phase 03.11**` 标注 | `.planning/ROADMAP.md` | 文档标注 |
| D-08 决策反转记录 | `.planning/STATE.md` | 决策记录 |
| DATA-03 的 advanced-by 追加 | `.planning/REQUIREMENTS.md` | 台账追加 |
| （条件性）跨 vendor dtype/交集断言 | `quantlab/base/model.py` / `quantlab/base/backtest.py` | 仅在 D-21 选 A 时新增 |

<tasks>

<task type="auto">
  <name>Task 1: 三处反转记录 + 需求台账追加 + 全套回归门</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - Phase 03.10 的 `**User decisions already made**` 一节里的 Identifier 决策。**用 grep 定位，不要按行号**：`grep -n '^- Identifier: the panel' .planning/ROADMAP.md`（全文唯一命中），它是一个 3 行 bullet，末行以 `"no ticker-rename mapping" todo.` 结束。ROADMAP.md 会被 plan-phase 与 `/gsd-phase` 反复编辑，写死的行号会漂；grep 锚不会
    - ⚠ **RESEARCH.md §R10 的表里把这条决策记成 `.planning/ROADMAP.md:276-277`（连同一个 `[VERIFIED]` 标记）——那个行号已经漂了**，`:276-277` 现在落在 03.11 自己的 wave 列表上。按 RESEARCH 的行号插入会把 REVERSED 标注插进完全错误的小节。以本任务的 grep 锚为准，RESEARCH 的行号只当历史记录看
    - Phase 03.11 自己那段已经写了反转的文字：`grep -n 'deliberately reverses that' .planning/ROADMAP.md`（全文唯一命中）—— **无需改动**
    - `.planning/STATE.md` 的决策记录体例（先 `grep -n` 找到既有的决策小节）
    - `.planning/REQUIREMENTS.md:139`（DATA-03 的 `Pending — advanced by Phase 03.10 …` 一行）与 `:23`（DATA-03 的正文）
    - `.planning/phases/03.11-...-r/03.11-CONTEXT.md` 的 D-08 全文（反转的完整理由与测量）
    - `.planning/phases/03.11-...-r/03.11-10-SUMMARY.md`（最终重建的实测数字）
  </read_first>
  <action>
**三处记录，全部用精确替换，改完核对行数**（operator P6：`sed` 范围删除漏匹配会一路删到 EOF，已有真实事故）：

1. **`.planning/ROADMAP.md` 的 03.10 Identifier 决策。先用 `grep -n '^- Identifier: the panel' .planning/ROADMAP.md`
   的锚定位，不要用行号**——本文件被 plan-phase / `/gsd-phase` 反复编辑，行号会漂，锚不会。
   命中必须恰好 1 条；不是 1 条就停下来报告，不要猜。那条 bullet 共 3 行，末行以
   `"no ticker-rename mapping" todo.` 结束。03.10 的决策文本 **一字不改**：在这个 bullet 的**下方**
   （即末行之后、下一条 `- Role:` bullet 之前）插入一行显式标注，形如 `**REVERSED by Phase 03.11**`
   + 一句话理由（被回收的 ticker 会把两家公司拼成一列，而 `resolve_collisions` 只看得见同日争用，
   2000–2024 窗口里 96.6% 的回收是它看不见的顺序继承）+ 指向 03.11 的 D-08。
   **插入用精确替换**：匹配那 3 行原文、替换成「那 3 行原文 + 新增的标注行」，**绝不用 `sed` 范围操作**
   （operator P6：范围删除漏匹配会一路删到 EOF，已有真实事故）。插入前后各跑一次
   `grep -c '^- Identifier: the panel' .planning/ROADMAP.md`（前后都必须是 1）与 `wc -l .planning/ROADMAP.md`，
   再跑 `git diff --numstat -- .planning/ROADMAP.md` 确认**删除列是 0**（纯插入）。三个数都写进 SUMMARY。
2. **03.11 自己那段已经写了反转的文字**（`grep -n 'deliberately reverses that' .planning/ROADMAP.md`，
   全文唯一命中），**无需改动**——执行时确认一遍即可。
3. **`.planning/STATE.md`。** 按本文件既有的决策记录体例加一条：Phase 03.11 反转了 03.10 的身份轴决策
   （D-08），给出证据与新状态（面板 `symbol` = int64 PERMNO，`ticker` 走 `{zarr}.crsp_tickers.json`
   as-of sidecar）。CONTEXT.md 逐字说「Recording the reversal is itself a deliverable」。
4. **`.planning/REQUIREMENTS.md:139`。** DATA-03 现记「Pending — advanced by Phase 03.10, which added a
   second `us_equity`/`1d` vendor …」。**只追加**一条 advanced-by 记录：Phase 03.11 把这个 vendor seam 的
   身份轴换成 int64 PERMNO，使它不再能在 ticker seam 上凭空造出一笔收益。
   **不改状态标记 `[ ]`、不动任何计数、不新建 requirement id。**

**然后跑全套回归门：**

与 03.10 基线（1694 collected）对比，要求**零新增失败**。把 collected 数与 pass / fail / skip 数写进 SUMMARY，
并逐条列出任何失败项与它是否属于已知基线失败。
  </action>
  <verify>
    <automated>uv run pytest -q --ignore=tests/test_factor_hierarchy.py --ignore=tests/test_crsp_rebuild_measurements.py -p no:cacheprovider</automated>
    <fails_when>non-zero exit, or the summary line reports any failed or errored test that is not on the recorded 03.10 baseline failure list, or "collected" reports fewer than 1600 tests</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "REVERSED by Phase 03.11" .planning/ROADMAP.md` 返回 1
    - `grep -c '^- Identifier: the panel' .planning/ROADMAP.md` 返回 1（03.10 的历史 bullet 仍在，一字未改）。**不要用 `grep -c "stays the \*\*ticker\*\* valid at each date"`**：那条模式在 03.11 自己那段反转叙述里也出现，实测返回 2，拿它断言 1 是结构性失败
    - `git diff --numstat -- .planning/ROADMAP.md` 的**删除列为 0**（纯插入；这才是「历史一字未改」的硬不变量，比行数差更可靠），且新增列等于标注实际占的行数并写进 SUMMARY
    - `grep -c "D-08" .planning/STATE.md` ≥ 1
    - `grep -c "Phase 03.11" .planning/REQUIREMENTS.md` ≥ 1
    - `grep -c "DATA-08" .planning/REQUIREMENTS.md` 与改动前相同，且 `grep -c "^- \[x\]" .planning/REQUIREMENTS.md` 与改动前相同（计数与状态未被扰动）
    - `git diff --stat -- .planning/ROADMAP.md .planning/STATE.md .planning/REQUIREMENTS.md` 的增删行数与 SUMMARY 里记录的核对数字一致
    - `uv run pytest -q --ignore=tests/test_factor_hierarchy.py --ignore=tests/test_crsp_rebuild_measurements.py -p no:cacheprovider` 相对 03.10 基线零新增失败
  </acceptance_criteria>
  <done>反转记录在三处可读；需求台账只追加不扰动；全套零新增失败。</done>
  <reversibility rating="reversible">planning 文档追加，git revert 即可。</reversibility>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: operator 决策 —— D-21 跨 vendor 合并断言（顺带确认 D-20）</name>
  <decision>要不要在两个跨 vendor 合并点加一条 dtype / 交集断言？（D-21）</decision>
  <context>
D-02 锁定了「CRSP only，本轮不做跨 vendor 身份工作」——Tiingo / Alpaca / Binance 保留 ticker 轴，
不做 `(ticker, date) → PERMNO` 映射层。Binance 永远不会有 PERMNO，而让非 CRSP vendor 经 CRSP 映射
会让它们变成 CRSP 的下游依赖并引入 as-of 日期的正确性风险。

**已知且已接受的后果：** 把一个 PERMNO 轴的 CRSP 面板与一个 ticker 轴的 Tiingo 因子放进同一个模型，
得到的是**静默的全 NaN 结果而不是报错**。机制：`xr.combine_by_coords`（`base/model.py:207`/`:228`/`:237`）
外连成一个交集为空的并集轴；`base/backtest.py:756-757` 的
`predictions.reindex(symbol=prices.symbol.values)` 随后给出一个空组合回测；全程只有
`backtest/selection.py:163` 一条 warning。

CONTEXT.md 的 D-02 逐字写着「A few-line dtype/intersection assertion at those two merge points would turn
silence into a loud refusal. **Optional task — propose it, let the operator decide; do not assume it is
wanted.**」——所以这个 plan **不预设它被采纳**，把选择交回来。

**顺带请确认 D-20（本 plan 的默认已是选项 B，无需任何改动）：** `validate_schema` 是否给 CRSP 一份专属的
`required_columns`，把 `numtrd` 排除出结构性掩码？`numtrd` 每次转换都 warn 一次——NYSE 不报交易笔数，
所以这是**真实的厂商缺值**而非数据缺陷（两个口径：91,862 修复前全库 / 90,594 修复后 sp500-2024）。
默认 **不改**：消掉这条 warning 要么得动 `cleaning.py`（D-06 明令禁止），要么得写一份本 phase 没有证据去
塑造的 CRSP 专属列契约。
  </context>
  <options>
    <option id="assert-now">
      <name>A —— 现在就加断言</name>
      <pros>把一个静默的全 NaN 回测变成一次响亮的拒绝；D-02 已经点名这是个真实风险；实现约 10–20 行 + 两条测试，形状照 backend.py:470-505 的守卫体例（refusing 开头 + 实测证据 + 补救）</pros>
      <cons>合法的部分重叠场景（例如故意只用交集的一部分标的）会被误拒，需要一个逃生口；本 phase 的 scope 是身份轴重构，这条属于跨 vendor 边界</cons>
    </option>
    <option id="backlog">
      <name>B —— 记成 backlog，不在本 phase 实现</name>
      <pros>保持本 phase 的 scope 纯粹；D-02 已经把这个后果记为「已知且接受」；不冒误拒合法场景的风险</pros>
      <cons>这个静默失败模式会继续存在，直到有人真的踩上去</cons>
    </option>
  </options>
  <resume-signal>回复 `assert-now` 或 `backlog`；并对 D-20 回复「保持默认」或「要改」。两个答复都会作为 D-21 / D-20 写进本 plan 的 SUMMARY。选 `assert-now` 则在本 plan 内追加一个实现任务；选 `backlog` 则在 `.planning/todos/pending/` 落一条 todo。</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: 收尾人工确认 —— 历史未被篡改、文档读起来正确、warning 未掩盖问题、列数差异可解释</name>
  <what-built>
Phase 03.11 的全部交付已落地并自动验证过：
- 盘上 CRSP store 重建为 int64 PERMNO 轴（plan 10 Task 1），四条不变量形式的清洁度断言全绿，
  `.crsp_symbology_report.json` 消失、`.crsp_tickers.json` 出现；
- `example/wrds_crsp.md` 的 16 处已同步（plan 10 Task 2），并核对了 `git diff --stat` 行数；
- 反转记录已写入 `.planning/ROADMAP.md`（标注在 03.10 历史**下方**）与 `.planning/STATE.md`（本 plan Task 1）；
- `.planning/REQUIREMENTS.md` 的 DATA-03 只追加了一条 advanced-by 记录；
- 全套 `uv run pytest -q --ignore=tests/test_factor_hierarchy.py --ignore=tests/test_crsp_rebuild_measurements.py -p no:cacheprovider` 相对 03.10 基线零新增失败。
  </what-built>
  <how-to-verify>
四件事需要人的判断，自动化断言看不见：

1. **03.10 的历史文本一字未改。** 跑 `git diff .planning/ROADMAP.md` 并目视确认：03.10 的 Identifier 决策
   那条 bullet 的 3 行完全保留（末行以 `"no ticker-rename mapping" todo.` 结束），新的
   `**REVERSED by Phase 03.11**` 标注加在它**下方**而不是替换了它。
2. **`example/wrds_crsp.md` 的 16 处同步读起来是否正确。** 文档正确性无法自动断言，需要通读。特别看两处：
   `:82-83`（退市行的哪一半机制还在——符号学的 ticker carry 没了，但 `crsp.py:952-977` 的判决继承还在）
   与 `:189-197`（「ticker 根本不产生，只写 sidecar」现在是主线叙述）。
3. **指纹失效的 warning 是否掩盖了真实问题。** D-12 已接受既有 backtest run 的指纹全部失效
   （`_compare_fingerprints`，`backtest.py:1145-1188` 只 warn 不阻断）。请看一遍全套运行的输出，
   确认这批 warning 没有把别的东西淹掉。
4. **W0 基线的 523 与最终重建的 `symbol_count` 之间的差异解释是否可接受。** 见 plan 10 的 SUMMARY：
   PERMNO 轴下同窗口内的改名会合并两列、ticker 回收会拆成两列，列数可以合法地变化——但解释要成立。
  </how-to-verify>
  <resume-signal>四件都通过则回复 `approved`；任一不通过则描述问题，执行会记进 SUMMARY 并回到对应的 plan。</resume-signal>
</task>

</tasks>

<threat_model>
> ASVS level 1；`security_block_on: high`。

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 历史决策记录 → 当前决策 | 反转必须可读，且不得靠篡改历史来实现 |
| 需求台账 → 交付状态 | 误改状态或计数会让一个未交付的 requirement 看起来已完成 |
| CRSP 面板 ↔ 非 CRSP 因子 | 两种 symbol 语义在同一个模型里相遇的边界（D-21 的对象） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.11-34 | Repudiation | 编辑 03.10 的历史决策文本来「消除矛盾」，反转变得不可追溯 | high | mitigate | 明令不改历史文本，只在下方加标注；验收 grep 要求 03.10 原句仍返回 1；checkpoint 第 1 项要求人目视 `git diff` |
| T-03.11-38 | Tampering | 误改 REQUIREMENTS.md 的状态标记或计数，让一个 Pending 的 requirement 看起来已完成 | high | mitigate | 明令只追加；验收要求 `[x]` 行数与改动前相同 |
| T-03.11-35 | Denial of Service | 用 `sed` 范围删除改 planning 文档，漏匹配后一路删到 EOF | high | mitigate | 明令精确替换；验收要求 `git diff --stat` 的增删行数与 SUMMARY 的核对数字一致（operator P6，已有真实事故记录） |
| T-03.11-39 | Tampering | CRSP 面板与 ticker 轴因子合并得到静默的全 NaN 空组合回测 | medium | transfer | D-02 已把它记为已知且接受的后果；是否现在就加断言由 D-21 的 `checkpoint:decision` 交给 operator，未采纳则落成 backlog todo |
| T-03.11-37 | Repudiation | 指纹失效的 warning 刷屏，掩盖真实问题 | low | accept | D-12 已接受指纹失效；由 checkpoint 第 3 项人工确认它没有淹没别的输出 |
| T-03.11-SC | Tampering | npm/pip/cargo 安装 | low | accept | 本 phase 零安装（RESEARCH `## Package Legitimacy Audit`） |
</threat_model>

<verification>
- `uv run pytest -q --ignore=tests/test_factor_hierarchy.py --ignore=tests/test_crsp_rebuild_measurements.py -p no:cacheprovider` 相对 03.10 基线（1694 collected）零新增失败
- `grep -c "REVERSED by Phase 03.11" .planning/ROADMAP.md` 为 1，且 03.10 的原句仍在
- 两个 checkpoint 的答复都已写进 SUMMARY
</verification>

<success_criteria>
- 反转记录在三处（ROADMAP 标注在历史下方、STATE、`example/wrds_crsp.md`），03.10 历史一字未改
- REQUIREMENTS.md 的 DATA-03 只追加、不改状态、不动计数
- D-20 / D-21 两个 operator 决策已记入 SUMMARY
- 四项人工确认全部通过
- 全套零新增失败
</success_criteria>

<output>
Create `.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-11-SUMMARY.md` when done
</output>
