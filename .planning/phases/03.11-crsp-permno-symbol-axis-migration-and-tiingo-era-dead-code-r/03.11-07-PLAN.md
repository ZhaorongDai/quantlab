---
phase: 03.11
plan: 07
type: execute
wave: 4
depends_on: ["03.11-03", "03.11-04", "03.11-05", "03.11-06"]
files_modified:
  - quantlab/dataset/crsp_symbology.py
  - quantlab/dataset/crsp.py
  - tests/test_crsp_symbology.py
  - tests/test_crsp_identity.py
  - tests/test_no_identity_residue.py
autonomous: true
requirements: [DATA-03]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "CrspSymbology 收缩成一个区间提供者：只剩 symbol_intervals() 及其依赖，label_rows / resolve_collisions / _class_collision_pass / _member_spans / _per_permno / _collision_message / 五个 report 字段全部消失（D-18）"
    - "crsp.py 的 _resolve_identity 全体、symbology_report_path、SYMBOLOGY_REPORT_SUFFIX、_symbology_report 承载字段全部删除（R6 删除清单）"
    - "crsp.py:952-977 的退市判决继承与 crsp.py:228 的 _DELISTING_FLAG 一字未动，plan 03 的防误删断言仍绿（D-11 坑 a）"
    - "_assert_unique_panel_keys 整个方法与其调用点保留，只改两处文案（D-11 坑 d）"
    - "_apply_security_filter 全体、_roster_sources / _roster_exemption / _member_days / _member_intervals、_build_filter_report / _permno_breakdown、调整锚全套、adjustment_sidecar_path 全部保留（D-11 坑 b 职责 2）"
    - "三处指向已删机制的 docstring 被重写而不是照抄：crsp.py:929-934 的最后半句、crsp.py:754-781 的「it now has TWO readers」、crsp.py:1520 的 symbol_overrides 出路（S5）"
    - "一条仓库级 grep gate 锁住死代码零残留，且它计入注释 —— 一条指向已删代码的注释在这个仓库里比在别处更有害"
    - "03.10 的 WR-05 在这里由删除结清（D-07）：resolve_collisions 与 active_over_delisting 整体消失，所以不再需要给它们加那个 rows_dropped_carrying_delisting 计数器；实测规模（1962–2025 共 98 次、96 个 ticker、被丢弃的 delret 中位数 +0.001、低于 −20% 的仅 6 次）记进 SUMMARY 存档"
  artifacts:
    - quantlab/dataset/crsp_symbology.py
    - quantlab/dataset/crsp.py
    - tests/test_no_identity_residue.py
  key_links:
    - "CrspSymbology.symbol_intervals() → plan 09 的 ticker sidecar 唯一数据源（所以它必须活下来）"
    - "crsp.py:_write_identity_reports 的 symbology 分支 → plan 09 换成 ticker sidecar 分支（本 plan 只删，不新建）"
    - "_assert_unique_panel_keys 的新出路文案 → wrds_crsp.py:818-840 的 (permno, dlycaldt) 唯一性断言"
  prohibitions:
    - "不得删除或修改 quantlab/dataset/crsp.py:952-977 的退市判决继承，也不得动 crsp.py:228 的 _DELISTING_FLAG（D-11 坑 a：它与被删的符号学 carry 在 crsp.py:929-934 同一段 docstring 里并排出现）"
    - "不得删除 CrspSymbology.symbol_intervals()、SUFFIX_DELIMITER、_NO_CLASS 与它的 _intervals 缓存哨兵（它是 plan 09 的唯一数据源）"
    - "不得删除 _assert_unique_panel_keys 或其调用点（D-11 坑 d：基类的 dedup_raw_frame(keep='last') 会静默把两家公司合成一条价格序列并留下一个良构面板）"
    - "不得删除 collision_universe 这个配置字段本身（职责 2 还活着；改名属于 plan 08）"
    - "不得删除 _roster_sources / _roster_exemption / _member_days / _member_intervals（D-11 坑 b 职责 2 / operator P8 名册优先于安全过滤器）"
    - "不得删除 _apply_security_filter、_build_filter_report、_permno_breakdown、调整锚全套、adjustment_sidecar_path"
    - "不修改 quantlab/dataset/cleaning.py（D-06）"
    - "不写任何 config 迁移或兼容层（D-04）"
---

<!-- planner-discipline-allow: resolve_collisions, _class_collision_pass, _member_spans, _per_permno, _collision_message, label_rows, _resolve_identity, symbology_report_path, SYMBOLOGY_REPORT_SUFFIX, _symbology_report, nan_adj_at_permno_seam, symbol_overrides, class_suffixed, nonconforming_symbols, delisting_carried, TWO readers, symbol_overrides to separate them, self.report, _DELISTING_FLAG -->

<objective>
删掉因为换轴而失去存在理由的整层适配：符号学里的撞车解决、类别后缀、退市**标签** carry、未贴标丢弃，
以及 `crsp.py` 里的身份解析（含 PERMNO seam 检测与 seam NaN）。

**这是本 phase 最容易出错的一步**，因为要删的代码和必须留的代码在同一个文件、同一段 docstring 里并排出现。
所以本 plan 的每条指令都给行号，并且**点名什么活下来**：
- 两套「delisting carry」里，`crsp_symbology.py:383-407`（给退市行补 **ticker 标签**）删，
  `crsp.py:952-977` + `crsp.py:228` 的 `_DELISTING_FLAG`（让退市行**继承前一日的 security-filter 判决**）
  **必须留**——它是 D-10 的反幸存者偏差，误删的后果是每只退市股带着 delisting return 的最后一天被悄悄丢掉、
  而面板仍然完全良构。
- `collision_universe` 一名两责：撞车 tie-break 那一责死，**显式名册覆盖 `security_filter` 那一责活着**
  （operator P8：名册优先于安全过滤器）。

Purpose: 把「代码回收无法再拼接两家公司」这件事从「多加了一道守卫」变成「那个失败模式在结构上不存在」。
Output: 收缩成单职责区间提供者的 `CrspSymbology` + 清空身份解析的 `crsp.py` + 一条仓库级零残留 gate。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-CONTEXT.md
@.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-RESEARCH.md
@.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-PATTERNS.md
@.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-03-SUMMARY.md
</context>

## Artifacts this phase produces

| 符号 | 位置 | 类型 |
|---|---|---|
| `tests/test_no_identity_residue.py` | 新测试文件（仓库级 grep gate） | — |
| `_assert_unique_panel_keys` 的新出路文案 | `quantlab/dataset/crsp.py` | 文案改写 |

<tasks>

<task type="auto">
  <name>Task 1: CrspSymbology 收缩成区间提供者（D-18）</name>
  <files>quantlab/dataset/crsp_symbology.py, tests/test_crsp_symbology.py</files>
  <read_first>
    - `quantlab/dataset/crsp_symbology.py` 全文（590 行；重点 `:55-112`、`:113-240`、`:241-322`、`:325-455`、`:455-590`）
    - `.planning/phases/03.11-...-r/03.11-RESEARCH.md` 的 `### quantlab/dataset/crsp_symbology.py（590 行）` 删除清单表（按当前 HEAD 重新核验过的行号）与其下方的「活下来的（约 12%）」段落
    - `quantlab/dataset/crsp_symbology.py:155-167`（`symbol_intervals()` 的输出契约 docstring 与 `_intervals` 缓存哨兵 —— **必须留**）
    - `quantlab/dataset/crsp_symbology.py:77`（符号学这一份 `_DELISTING_FLAG` —— 删）与 `quantlab/dataset/crsp.py:226-230`（**另一份，绝不能动**）
    - `tests/test_crsp_symbology.py` 全部 21 个测试名（`grep '^def test_'`）
    - `.planning/phases/03.11-...-r/03.11-PATTERNS.md` 的第 3 节「活下来的是什么」
  </read_first>
  <action>
按 RESEARCH `### quantlab/dataset/crsp_symbology.py` 的删除清单逐行处理：

**删：** `:82`（`_MAX_LISTED_COLLISIONS`）、`:90-111`（`_member_spans`，唯一调用点 `:502` 也在删除范围内）、
`:136-150`（`self.report` 的五个字段初始化）、`:213-220`（overrides 注入的 `replace_strict` + `coalesce`）、
`:222`（`_class_collision_pass` 的调用）、`:230-238`（`nonconforming_symbols` 的计算）、
`:241-322`（`_class_collision_pass` 全体）、`:383-407`（`label_rows` 里的退市 **symbol** carry 三段）、
`:406-408`（两个 report 赋值）、`:409-418`（`unlabelled` 过滤 + warning）、`:428-453`（`_per_permno`）、
`:455-564`（`resolve_collisions`）、`:566-590`（`_collision_message`）、`:77`（符号学这一份 `_DELISTING_FLAG`）、
`:73`（`_OPEN_END`，两个使用点 `:107` / `:276` 都在删除范围内）。

**`label_rows` 整删（D-18）。** 它在价格面板一侧的唯一调用点 `crsp.py:495` 已被 plan 03 移除；它的另一个语义
（给每日行贴 period-correct ticker）正是 ticker sidecar **不需要**的（sidecar 存区间，不存逐行标签）。
删之前**先跑一次** `grep -rn "label_rows" quantlab/ tests/ scripts/ example/` 复核调用点已归零
（已知的另一处 `crsp_membership.py:224` 只是 docstring 提及，且已由 plan 05 改写）。若复核发现第二个真实调用者，
**停下来并在 SUMMARY 里报告**，不要为了删而硬删。

**`_as_date`（`:85`）**：两个调用点 `:106`、`:512` 都在删除范围内，复核后一并删除。

**必须留：** 模块 docstring（**改写**：它现在描述的是一个身份解析器，改成「PERMNO → ticker 区间的提供者」）、
`SUFFIX_DELIMITER`（`:62`）、`_NO_CLASS`（`:67`）、`CrspSymbology.__init__`（去掉 `overrides` 形参与 `report`
初始化，只剩 `security_info` 与 `_intervals` 缓存哨兵；它的 docstring 要从「Turn … into `(permno, symbol,
window)`」改成区间提供者的措辞）、**`symbol_intervals()`（`:155-239` 扣掉 `:213-238`）**。
`symbol_intervals()` 的输出契约 docstring（`:156-165`，逐字给出 `(permno, symbol, start_date, end_date)`、
`permno` Int64 / `symbol` String / 两个日期 Date、按 `(permno, start_date)` 排序、已带缓存）是 plan 09 那个
sidecar 的 schema 依据——**改写而不是删除**，并在其中加一句指明它现在的唯一消费者是 ticker sidecar。

`tests/test_crsp_symbology.py`：21 个测试里 ≥16 个测的是 class share / collision / carry / override / 正则，
随代码删除。**保留并改造**两个：`test_intervals_are_sorted_typed_and_one_row_per_input_interval`（`:201`）与
`test_rename_13407_is_fb_through_2022_06_08_and_meta_after`（`:213`）——它们测的正是 `symbol_intervals()`。
若删完之后文件只剩这两个测试，可以保留文件；plan 09 会把它们迁到 `tests/test_crsp_ticker_sidecar.py` 或
在那里承接同一语义。
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_crsp_symbology.py tests/test_crsp_membership.py -p no:cacheprovider</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line, or the summary line reports any failed or errored test</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "def resolve_collisions" quantlab/dataset/crsp_symbology.py` 返回 0
    - `grep -c "def _class_collision_pass" quantlab/dataset/crsp_symbology.py` 返回 0
    - `grep -c "def label_rows" quantlab/dataset/crsp_symbology.py` 返回 0
    - `grep -c "def symbol_intervals" quantlab/dataset/crsp_symbology.py` 返回 1
    - `grep -c "SUFFIX_DELIMITER" quantlab/dataset/crsp_symbology.py` ≥ 1 且 `grep -c "_NO_CLASS" quantlab/dataset/crsp_symbology.py` ≥ 1
    - `grep -c "self.report" quantlab/dataset/crsp_symbology.py` 返回 0
    - `grep -c "_DELISTING_FLAG" quantlab/dataset/crsp_symbology.py` 返回 0 **且** `grep -c "_DELISTING_FLAG" quantlab/dataset/crsp.py` ≥ 2
    - `wc -l quantlab/dataset/crsp_symbology.py` < 200（590 行收缩到约 12%）
    - `uv run pytest -q tests/test_crsp_symbology.py tests/test_crsp_membership.py -p no:cacheprovider` 全绿
  </acceptance_criteria>
  <done>CrspSymbology 只剩一个职责：给出 PERMNO → ticker 的区间表；符号学那一份退市标志已删，crsp.py 那一份完好。</done>
  <reversibility rating="reversible">删除的是 git 可恢复的代码；删除之前已由 plan 03 断开全部调用点。</reversibility>
</task>

<task type="auto">
  <name>Task 2: crsp.py 的身份解析清空 —— 并逐条点名什么活下来</name>
  <files>quantlab/dataset/crsp.py, tests/test_crsp_identity.py</files>
  <read_first>
    - `quantlab/dataset/crsp.py:216-232`（三个 sidecar suffix 常量与 `_DELISTING_FLAG`）
    - `quantlab/dataset/crsp.py:430-440`（`self._symbology_report` 承载字段的初始化）
    - `quantlab/dataset/crsp.py:485-495`（`CrspSymbology(...)` 的构造，第二个实参要去掉）
    - `quantlab/dataset/crsp.py:655-750`（`_resolve_identity` 全体 + `symbology_report_path()`）
    - `quantlab/dataset/crsp.py:754-781`（`_member_intervals` 的「it now has TWO readers」docstring —— **改写，别删方法**）
    - `quantlab/dataset/crsp.py:788-919`（`_roster_sources` / `_roster_exemption` / `_member_days` —— **全留**）
    - `quantlab/dataset/crsp.py:922-1010`（`_apply_security_filter` 全体；`:929-934` 的 docstring 要改最后半句，`:952-977` 一字不动）
    - `quantlab/dataset/crsp.py:1040-1126`（`_build_filter_report` / `_permno_breakdown` —— **全留**）
    - `quantlab/dataset/crsp.py:1406-1430`（`_write_identity_reports`：本 plan 只删 symbology 分支并改 docstring，**不新建** ticker 分支——那是 plan 09）
    - `quantlab/dataset/crsp.py:1485-1521`（`_assert_unique_panel_keys` —— 留，只改两处文案）
    - `quantlab/acquisition/wrds_crsp.py:818-840`（`(permno, dlycaldt)` 唯一性断言 —— 新文案要指向它）
    - `.planning/phases/03.11-...-r/03.11-RESEARCH.md` 的 `### quantlab/dataset/crsp.py（1521 行）` 删除清单表与「活下来的（关键）」段落，以及 `### (a)` `### (b)` `### (d)` 三小节
  </read_first>
  <action>
**删：**
- `:222-225` 的 `SYMBOLOGY_REPORT_SUFFIX` 常量与它的 `#:` 注释。
- `:436` 的 `self._symbology_report: dict | None = None`（plan 09 会新增一个 ticker sidecar 的承载字段，
  本 plan 只删）。
- `:487-489` 的 `CrspSymbology(reference.table(...), self.config.symbol_overrides)` 去掉第二个实参
  （`symbol_overrides` 这个**配置字段**的删除属于 plan 08）。
- `:662-746` 的 `_resolve_identity` 全体（含撞车调用、seam 检测、seam NaN、`self._symbology_report` 构造）。
  它的调用点已由 plan 03 在 `:578` 移除。
- `:748-750` 的 `symbology_report_path()`。
- `:1423-1428` 的 `_write_identity_reports` 里的 symbology 分支。

**改文案（S5：删代码要同步删注释；这个仓库的 docstring 密度极高且互相引用，留一条指向已删代码的注释比在
别处更有害）：**
1. `:929-934` —— 最后半句逐字是 "immediately after symbology's carry rule had rescued the same row from a
   NULL ticker"。符号学的 carry 已不存在，这半句必须重写：PERMNO 轴下退市行没有「贴不上标签」的问题，
   但它的 type 列仍然会变 NULL，所以判决继承仍然必要。**保持本文件 docstring 的密度体例**：结论一句 +
   为什么这样选 + 被否决的替代方案。
2. `:754-781` 的 `_member_intervals` —— 它逐字说「it now has TWO readers」。撞车 tie-break 那个读者已死，
   现在只剩一个（`_roster_exemption` 的名册豁免）。**整段重写，不要照抄**。方法本身保留。
3. `:1520` —— 逐字是 `f"config.symbol_overrides to separate them."`。那个字段即将删除，而且 PERMNO 轴下
   重复只可能来自 raw tier，所以新出路应指向 `wrds_crsp.py:818-840` 的 `(permno, dlycaldt)` 断言。
   `_assert_unique_panel_keys`（`:1489-1521`）**整个方法与它在 `:1451` 的调用点保留**——它的 docstring 自己
   已经承认是 backstop，而它留着的理由是「被它挡住的错误代价是不可见的」：基类的
   `dedup_raw_frame(keep="last")` 会把两家公司合成一条价格序列并留下一个良构面板。第一段里引用
   `_resolve_identity` 的那句同样要重写。

**逐条点名活下来的（验收会检查）：** `FILTERABLE_COLUMNS`、`SECURITY_FILTER_PRESETS`、`FILTER_REPORT_SUFFIX`、
**`_DELISTING_FLAG`（`:228`）**、`_apply_security_filter` 全体含 `:952-977` 的判决继承、
`_roster_sources` / `_roster_exemption` / `_member_days`、`_member_intervals`、
`_build_filter_report` / `_permno_breakdown`、`_assert_unique_panel_keys`、调整锚全套、`adjustment_sidecar_path`。

`tests/test_crsp_identity.py`（1806 行 / 30 个测试）：撞车 / seam / `symbol_overrides` 相关的删除；
**roster-override（GAP-C）相关的必须全部保留**（operator P8）。保留一条断言：用一个名册 universe 转换后
`.crsp_filter_report.json` 的 `roster_overrides.permnos` 非空——W0 基线里是 `CCL` 一个 PERMNO / 252 行。
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_crsp_identity.py tests/test_crsp_dataset.py tests/test_crsp_tracer.py -p no:cacheprovider</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line, or the summary line reports any failed or errored test</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c "_resolve_identity" quantlab/dataset/crsp.py` 返回 0
    - `grep -c "symbology_report_path" quantlab/dataset/crsp.py` 返回 0
    - `grep -c "SYMBOLOGY_REPORT_SUFFIX" quantlab/dataset/crsp.py` 返回 0
    - `grep -c "_symbology_report" quantlab/dataset/crsp.py` 返回 0
    - `grep -c "TWO readers" quantlab/dataset/crsp.py` 返回 0（文案已重写）
    - `grep -c "symbol_overrides to separate them" quantlab/dataset/crsp.py` 返回 0
    - `grep -c "wrds_crsp.py:818" quantlab/dataset/crsp.py` ≥ 1（新出路指向 raw tier 的唯一性断言）
    - `grep -c "def _assert_unique_panel_keys" quantlab/dataset/crsp.py` 返回 1
    - `grep -c "def _apply_security_filter" quantlab/dataset/crsp.py` 返回 1 且 `grep -c "_DELISTING_FLAG" quantlab/dataset/crsp.py` ≥ 2
    - `grep -c "def _member_days" quantlab/dataset/crsp.py` 返回 1、`grep -c "def _roster_exemption" quantlab/dataset/crsp.py` 返回 1、`grep -c "def _member_intervals" quantlab/dataset/crsp.py` 返回 1
    - `uv run pytest -q tests/test_crsp_dataset.py::test_a_delisted_permno_keeps_its_last_row -p no:cacheprovider` 通过（plan 03 的防误删断言仍绿）
    - `uv run pytest -q tests/test_crsp_identity.py tests/test_crsp_dataset.py tests/test_crsp_tracer.py -p no:cacheprovider` 全绿
  </acceptance_criteria>
  <done>身份解析层清空；三处指向已删机制的 docstring 已重写；名册覆盖与退市判决继承两条必须活下来的机制都有验收断言。</done>
  <reversibility rating="reversible">删除的是 git 可恢复的代码；盘上 store 的重建在 plan 10。</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 仓库级零残留 gate —— 并且它计入注释</name>
  <files>tests/test_no_identity_residue.py</files>
  <read_first>
    - `quantlab/dataset/crsp.py`、`quantlab/dataset/crsp_symbology.py`（Task 1/2 之后的全文）
    - `.planning/phases/03.11-...-r/03.11-RESEARCH.md` 的 `### Validation Architecture` → W4 那两行（grep gate 与防误删断言）
    - 仓库里已有的同类先例：`grep -rln "symbol-absence\|residue\|grep gate" tests/` 先找一下 03.6 SC-3 留下的形状，照它写
  </read_first>
  <behavior>
    - `quantlab/` 下对本 phase 已删符号的命中数为 0，**包括注释里的命中**
    - 被检查的符号至少包含：身份解析入口、撞车解决、类别后缀过程、符号学报告 suffix、以及 plan 08 将要删的两个配置字段（后两个在 plan 08 之前允许非零，由 gate 的分组表达）
    - gate 失败时的消息逐个列出命中的文件与行号，而不是只说「有残留」
    - gate 不扫 `.planning/`、`example/`、`tests/` —— 计划文档与文档正文需要能引用已删机制的名字来解释为什么删
  </behavior>
  <action>
新建 `tests/test_no_identity_residue.py`。它是一条仓库级 gate，形状照 03.6 SC-3 留下的 symbol-absence gate
（先在 `tests/` 里找到那个先例再动手；找不到就按下面的形状写）。

- 一个模块常量列出本 plan 删掉的符号名，一个测试遍历它，对每个名字在 `quantlab/` 下做递归文本搜索，
  断言命中为 0，失败时把 `文件:行号: 行内容` 全部列进 assert 消息。
- **命中判定计入注释。** 这不是疏忽：S5 明确要求删代码时同步删注释，而这个仓库的 docstring 互相引用密度极高
  ——`crsp.py:929-934` 那段把两套 delisting carry 并排提到的 docstring，正是「误删另一半」这个坑的成因。
  一条指向已删代码的注释在这里比在别处更有害。所以 gate 不做 `grep -v '^#'` 过滤，并在测试的 docstring 里
  写明这是刻意的。
- **扫描范围只有 `quantlab/`。** `.planning/`、`example/`、`tests/` 不扫：计划文档要能引用被删机制的名字来
  解释为什么删，文档正文也要能写「这个机制在 03.11 被移除」。
- 把符号分成两组：**本 plan 已删的**（立即断言为 0）与 **plan 08 将删的两个配置字段**（用一个单独的、
  当前 `xfail` 或被跳过的分组表达，并在 plan 08 里翻成正式断言）。分组的目的是让 gate 从现在起就是清单的
  单一真源，而不是在 plan 08 再造一份。
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_no_identity_residue.py -p no:cacheprovider</automated>
    <fails_when>non-zero exit, or "no tests ran" in the summary line, or the summary line reports any failed test</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest -q tests/test_no_identity_residue.py -p no:cacheprovider` 全绿
    - `grep -c "quantlab" tests/test_no_identity_residue.py` ≥ 1 且 `grep -c "'.planning'" tests/test_no_identity_residue.py` 返回 0（扫描范围正确）
    - 该文件的 docstring 里写明「命中判定计入注释，这是刻意的」
    - 人工在 `quantlab/dataset/crsp.py` 临时加一行注释提到某个已删符号，跑 gate 会红（执行时验证一次并在 SUMMARY 里记下，然后撤销）
  </acceptance_criteria>
  <done>死代码零残留有一条会红的仓库级断言，而且它管得住注释。</done>
  <reversibility rating="reversible">纯测试新增。</reversibility>
</task>

</tasks>

<threat_model>
> ASVS level 1；`security_block_on: high`。

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 证券类型列 → 准入判决 | `_apply_security_filter`；退市行的类型列全 NULL，判决继承是它的反幸存者偏差保护 |
| 显式名册 → 准入判决 | GAP-C 的名册覆盖；operator P8「名册优先于安全过滤器」 |
| 面板 (timestamp, symbol) 键 → 去重 | `_assert_unique_panel_keys` 的 backstop 位置 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.11-09 | Tampering | 误删 `crsp.py:952-977` → 退市行的 delisting return 被丢弃，幸存者偏差无声回归 | high | mitigate | prohibitions 逐行点名；验收要求 `_DELISTING_FLAG` 在 `crsp.py` 出现 ≥ 2 次；plan 03 的 `test_a_delisted_permno_keeps_its_last_row` 在本 plan 的验收里单独跑一次 |
| T-03.11-22 | Tampering | 误删 `_assert_unique_panel_keys` → 基类的 `dedup_raw_frame(keep="last")` 静默把两家公司合成一条价格序列，留下一个良构面板 | high | mitigate | prohibitions 点名；验收要求方法与调用点仍在，只改文案 |
| T-03.11-23 | Tampering | 误删名册覆盖（坑 b 职责 2）→ 指数成分被类型过滤挡掉 | high | mitigate | 验收要求 `_member_days` / `_roster_exemption` / `_member_intervals` 三个方法各存在一次；`test_crsp_identity.py` 的 roster-override 测试全部保留 |
| T-03.11-24 | Repudiation | 留下指向已删代码的 docstring，下一个读者按它去找一个不存在的机制 | medium | mitigate | 三处文案逐处点名重写；grep gate **计入注释** |
| T-03.11-SC | Tampering | npm/pip/cargo 安装 | low | accept | 本 phase 零安装（RESEARCH `## Package Legitimacy Audit`） |
</threat_model>

<verification>
- `uv run pytest -q tests/test_crsp_symbology.py tests/test_crsp_identity.py tests/test_crsp_dataset.py tests/test_crsp_tracer.py tests/test_crsp_membership.py tests/test_no_identity_residue.py -p no:cacheprovider` 全绿
- `uv run pytest -q --ignore=tests/test_factor_hierarchy.py --ignore=tests/test_crsp_rebuild_measurements.py -p no:cacheprovider` 相对 03.10 基线零新增失败
</verification>

<success_criteria>
- `crsp_symbology.py` < 200 行且只剩 `symbol_intervals()` 一个公开职责
- `crsp.py` 的身份解析层清空，三处 docstring 已重写
- 退市判决继承、名册覆盖、唯一性 backstop 三条各有验收断言证明它们还在
- 仓库级 grep gate 绿，且能管住注释
</success_criteria>

<output>
Create `.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-07-SUMMARY.md` when done
</output>
