---
phase: 03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r
plan: 07
subsystem: database
tags: [crsp, permno, symbology, dead-code-removal, pytest, polars]

requires:
  - phase: 03.11-03
    provides: "PERMNO 轴落地并断开 _resolve_identity 的调用；把方法体、symbology_report_path、SYMBOLOGY_REPORT_SUFFIX、_symbology_report 留成无调用者的死代码"
  - phase: 03.11-05
    provides: "crsp_membership.py 不再依赖 crsp_symbology，符号学的第二个潜在读者归零"
  - phase: 03.11-06
    provides: "universe_filter 的 ticker 正则已删净，本 plan 的删除面不再与它重叠"
provides:
  - "CrspSymbology 收缩为单职责区间提供者：590 行 -> 152 行，公开面只剩 symbol_intervals()"
  - "crsp.py 的身份解析层清空：_resolve_identity 全体、symbology_report_path、SYMBOLOGY_REPORT_SUFFIX、_symbology_report、_write_identity_reports 的 symbology 分支"
  - "tests/test_no_identity_residue.py —— 仓库级零残留 gate，命中判定计入注释，扫描范围仅 quantlab/"
  - "gate 里 plan 08 的删除清单（symbol_overrides / nan_adj_at_permno_seam）以 strict xfail 表达，成为该清单的单一真源"
  - "三处指向已删机制的 docstring 重写：_apply_security_filter 的退市半句、_member_intervals 的「TWO readers」、_assert_unique_panel_keys 的出路文案（新出路指向 wrds_crsp.py:818-840）"
affects: [03.11-08, 03.11-09, 03.11-10, 03.11-11]

actuals:
  tokens: 22000
  tasks: 3
  commits: 3
  plan_head_before: 0c52a635d0c48520a4e707cc0cea6adf1039f64f

tech-stack:
  added: []
  patterns:
    - "删除式收缩的验收写成「点名什么活下来」而不是只点名什么该死：本 plan 每一条 prohibition 都配了一条 grep 计数或单测"
    - "零残留 gate 刻意不过滤注释，因为本仓库的 docstring 就是设计记录，一条指向已删机制的注释是一句关于系统如何工作的假话"
    - "跨 plan 的删除清单放进 gate 的 strict xfail 分组，而不是写进 SUMMARY 散文：strict xfail 一旦意外变绿就是失败，逼着下一个 plan 回来改这份清单"
    - "gate 的符号入选规则：只收「专属于被删机制」的名字。_as_date 虽然被删，但另有三个模块各自定义同名私有助手，收进来就是狼来了"

key-files:
  created:
    - tests/test_no_identity_residue.py
  modified:
    - quantlab/dataset/crsp_symbology.py
    - quantlab/dataset/crsp.py
    - scripts/ingest_wrds_crsp.py
    - tests/test_crsp_symbology.py
    - tests/test_crsp_identity.py
    - tests/test_crsp_dataset.py
    - tests/test_crsp_tracer.py

key-decisions:
  - "label_rows 整删前先复核调用点归零：quantlab/ 与 scripts/ 的命中全是 docstring，tests/ 的命中全在本 plan 删除的测试里"
  - "CrspSymbology(...) 的第二个实参在 Task 1 就去掉，而不是留到 Task 2：构造器签名变了却不改调用点会让 Task 1 的提交留下一棵坏树"
  - "test_crsp_identity.py 的 29 个测试一个未删：plan 03 已把 7 个机器测试改写成 6 个 PERMNO 轴断言，它们测的是「那个失败形态不可表达」而不是被删的机器"
  - "两个 sidecar 测试把 .crsp_symbology_report.json 写成字面量而不是 import 常量：断言一个文件不存在的测试，不能依赖被测代码同意这个文件名还存在"
  - "gate 只扫 quantlab/，不扫 .planning/ / example/ / tests/：解释一次删除的唯一办法就是叫出死者的名字"
  - "_as_date 不进 gate 清单（wrds_taq.py / wrds_crsp.py 等另有同名私有助手）；_OPEN_END 同理不进，它是个通用哨兵名"

patterns-established:
  - "删代码与删注释同一次提交：S5 在本仓库不是风格要求，crsp.py:929-934 把两套 delisting 机制并排写在一段 docstring 里，正是「误删另一半」这个坑的成因"
  - "无法自动化的删除守卫，用「临时注入残留 -> 看它变红 -> 撤销」实测一次并记录，而不是断言 gate 大概管用"

requirements-completed: []

coverage:
  - id: D1
    description: "CrspSymbology 收缩成单职责区间提供者：590 -> 152 行，resolve_collisions / _class_collision_pass / label_rows / _per_permno / _member_spans / _collision_message / self.report 五字段全部消失，symbol_intervals() / SUFFIX_DELIMITER / _NO_CLASS 保留（D-18）"
    requirement: DATA-03
    verification:
      - kind: other
        ref: "grep -c 'def resolve_collisions|def _class_collision_pass|def label_rows|self.report|_DELISTING_FLAG' quantlab/dataset/crsp_symbology.py == 0 each; 'def symbol_intervals' == 1; wc -l == 152 (< 200)"
        status: pass
      - kind: unit
        ref: "tests/test_crsp_symbology.py#test_intervals_are_sorted_typed_and_one_row_per_input_interval"
        status: pass
      - kind: unit
        ref: "tests/test_crsp_symbology.py#test_rename_13407_is_fb_through_2022_06_08_and_meta_after"
        status: pass
    human_judgment: false
  - id: D2
    description: "crsp.py 的身份解析层清空：_resolve_identity 全体、symbology_report_path、SYMBOLOGY_REPORT_SUFFIX、_symbology_report、_write_identity_reports 的 symbology 分支（R6 删除清单）"
    requirement: DATA-03
    verification:
      - kind: other
        ref: "grep -c '_resolve_identity|symbology_report_path|SYMBOLOGY_REPORT_SUFFIX|_symbology_report' quantlab/dataset/crsp.py == 0 each"
        status: pass
      - kind: integration
        ref: "tests/test_crsp_identity.py#test_no_symbology_report_is_written_on_a_permno_axis"
        status: pass
      - kind: integration
        ref: "tests/test_crsp_dataset.py#test_from_raw_data_leaves_a_complete_sidecar_set"
        status: pass
    human_judgment: false
  - id: D3
    description: "退市判决继承（crsp.py 的 _DELISTING_FLAG + _keep_raw/_carried 表达式）一字未动，plan 03 的防误删断言仍绿（T-03.11-09 / D-11 坑 a）"
    requirement: DATA-03
    verification:
      - kind: integration
        ref: "tests/test_crsp_dataset.py#test_a_delisted_permno_keeps_its_last_row"
        status: pass
      - kind: integration
        ref: "tests/test_crsp_identity.py#test_a_delisting_row_inherits_the_previous_verdict"
        status: pass
      - kind: other
        ref: "git diff 0c52a635..HEAD -- quantlab/dataset/crsp.py 中 _keep_raw / _carried / coalesce / shift(1) 的代码行零改动；grep -c _DELISTING_FLAG quantlab/dataset/crsp.py == 2"
        status: pass
    human_judgment: false
  - id: D4
    description: "_assert_unique_panel_keys 整个方法与其在 _raw_data_to_xr_window 的调用点保留，只改文案；新出路指向 wrds_crsp.py:818-840 而不是即将删除的 config.symbol_overrides（D-11 坑 d / T-03.11-22）"
    requirement: DATA-03
    verification:
      - kind: other
        ref: "grep -c 'def _assert_unique_panel_keys' == 1; 'self._assert_unique_panel_keys' == 1; 'wrds_crsp.py:818' >= 1; 'symbol_overrides to separate them' == 0"
        status: pass
    human_judgment: false
  - id: D5
    description: "名册覆盖（GAP-C / operator P8）全套保留：_apply_security_filter、_roster_sources、_roster_exemption、_member_days、_member_intervals、_build_filter_report、_permno_breakdown、调整锚全套、adjustment_sidecar_path、collision_universe 配置字段（D-11 坑 b 职责 2 / T-03.11-23）"
    requirement: DATA-03
    verification:
      - kind: other
        ref: "每个 'def <name>' 的 grep 计数各为 1（共 15 个符号）；collision_universe 在 crsp.py 出现 11 次"
        status: pass
      - kind: integration
        ref: "tests/test_crsp_identity.py 的五条 GAP-C roster-override 测试（test_a_member_is_not_dropped_by_the_filter_during_its_spell / test_an_explicitly_named_permno_is_not_dropped_by_the_filter / test_without_a_roster_the_filter_still_truncates_the_rejected_era / test_the_filter_report_names_the_roster_rescue / test_every_member_survives_every_preset_on_its_member_dates）"
        status: pass
    human_judgment: false
  - id: D6
    description: "仓库级零残留 gate 绿，且管得住注释；失败时逐条列出 文件:行号: 行内容（T-03.11-24）"
    requirement: DATA-03
    verification:
      - kind: unit
        ref: "tests/test_no_identity_residue.py#test_the_deleted_identity_machinery_leaves_no_residue_in_quantlab"
        status: pass
      - kind: other
        ref: "注入 `# ... see `_resolve_identity` for the seam rule.` 到 crsp.py:227 -> gate 在自己的断言上变红并逐字打印 `quantlab/dataset/crsp.py:227: ...`；`git checkout --` 撤销后复绿（实测记录见下文 TDD Gate Compliance）"
        status: pass
    human_judgment: false
  - id: D7
    description: "三处指向已删机制的 docstring 被重写而不是照抄（S5）"
    requirement: DATA-03
    verification:
      - kind: other
        ref: "grep -c 'TWO readers' == 0; 'symbol_overrides to separate them' == 0; _apply_security_filter 的 'immediately after symbology's carry rule' 半句已不存在"
        status: pass
    human_judgment: true
    rationale: "grep 能证明旧文案消失，不能证明新文案写对了。新写的三段各自给出「结论 + 为什么这样选 + 被否决的替代方案」，是否真的保住了本文件的密度体例、是否对下一个读者有用，是人的判断。"
  - id: D8
    description: "全仓回归相对基线零新增失败"
    requirement: DATA-03
    verification:
      - kind: other
        ref: "uv run pytest -q --ignore=test_factor_hierarchy --ignore=test_crsp_rebuild_measurements --ignore=test_cross_sectional_zscore -> 60 failed / 1617 passed / 1 skipped / 1 xfailed；test_cross_sectional_zscore.py 单跑 9 passed。failed 数与基线逐字相同（60），失败集合按文件与用例名逐条核对一致"
        status: pass
    human_judgment: true
    rationale: "因为环境死锁（见 Issues 第 1 条）这次回归是分两段跑的，不是一条命令跑完的，与基线不同口径。failed 数和失败集合完全对得上，但 passed 的算术差 1 个未能归因（见 Issues 第 2 条）。口径不同的数字要人来判断能不能当作「零新增失败」的证据。"

duration: 69min
completed: 2026-09-21
status: complete
---

# Phase 03.11 Plan 07: 删掉换轴后失去存在理由的整层适配 Summary

**`CrspSymbology` 从 590 行的身份解析器收缩成 152 行的 PERMNO→ticker 区间提供者，`crsp.py` 的身份解析层清空，并留下一条会红、管得住注释、且把 plan 08 的删除清单也一并托管的仓库级零残留 gate。**

## Performance

- **Duration:** 69 min
- **Started:** 2026-09-21T04:49:02Z
- **Completed:** 2026-09-21T05:58:02Z
- **Tasks:** 3
- **Files modified:** 7（新建 1）

## Accomplishments

- **五套机器同时消失，而不是被加了守卫。** 撞车 tie-break、类别后缀过程、退市 **symbol** carry、PERMNO seam 与 seam NaN、符号学报告——它们守的那个失败形态在 PERMNO 轴上不是「不太可能」，是**不可表达**：两家公司没有相遇的格子。`crsp_symbology.py` 590→152 行（删掉 88%），公开面只剩 `symbol_intervals()`。
- **删的和留的在同一个文件、同一段 docstring 里并排出现，全部逐条验过。** 两套 delisting carry 里，符号学那份（给退市行补 **ticker 标签**）删，`crsp.py` 那份（让退市行**继承前一日的 security-filter 判决**）的代码行在整个 diff 里**零改动**——`_keep_raw` / `_carried` / `coalesce` / `shift(1)` 一个字符没变，`_DELISTING_FLAG` 仍出现 2 次，plan 03 的 `test_a_delisted_permno_keeps_its_last_row` 单独跑过一次。`collision_universe` 一名两责里死掉的只有 tie-break 那一责，名册覆盖那一责连同 `_roster_sources` / `_roster_exemption` / `_member_days` / `_member_intervals` 全套还在，五条 GAP-C 测试全绿。
- **零残留 gate 计入注释，而且它自己被实测证明会红。** 往 `crsp.py:227` 注入一行提到 `_resolve_identity` 的注释，gate 在**自己的断言**上变红并逐字打印 `quantlab/dataset/crsp.py:227: # TEMPORARY RESIDUE PROBE: ...`，撤销后复绿。不做 `grep -v '^#'` 是刻意的，并写进了测试的 docstring：这个仓库的 docstring 就是设计记录，而 `_apply_security_filter` 那段把两套 delisting 机制并排提到的 docstring，正是「误删另一半」这个坑的成因。
- **plan 08 的删除清单从今天起只有一个家。** `symbol_overrides` / `nan_adj_at_permno_seam` 放进 gate 的 `xfail(strict=True)` 分组——今天按预期红，plan 08 删掉字段那天它会变成**意外通过**，strict xfail 把意外通过报成失败，于是 plan 08 不来改这份清单就收不了工。清单不会在 plan 08 里被重造一份。
- **D-07 / WR-05 在这里由删除结清。** `resolve_collisions` 与 `active_over_delisting` 整体消失，所以 03.10 要求给它们加的 `rows_dropped_carrying_delisting` 计数器不再需要。实测规模存档：1962–2025 共 **98 次**、涉及 **96 个 ticker**、被丢弃的 `delret` 中位数 **+0.001**、低于 −20% 的仅 **6 次**。

## Task Commits

1. **Task 1: CrspSymbology 收缩成区间提供者（D-18）** — `b29f335` (refactor)
2. **Task 2: crsp.py 的身份解析清空** — `ef2f387` (refactor)
3. **Task 3: 仓库级零残留 gate** — `ebd22e1` (test)

**Plan metadata:** 见本文件的 docs commit。

`commits: 3` 是写 SUMMARY 时用 `git rev-list --count 0c52a635..HEAD` **测**出来的，不含随后那次 docs 提交（与 03.11-02 / 03.11-03 同一口径）。

## TDD Gate Compliance

| Task | RED | GREEN | REFACTOR | Status |
|---|---|---|---|---|
| 1（非 TDD） | — | `b29f335` | — | n/a |
| 2（非 TDD） | — | `ef2f387` | — | n/a |
| 3（`tdd="true"`） | 实测见下 | 行为已由 Task 1/2 交付 | — | Pass（见说明） |

**红证据（本项目是 pytest，按 operator P9 投影真实运行结果，不是 node TAP）。**

Task 3 是一条 **guard / gate**：它保护的状态（零残留）由 Task 1 与 Task 2 交付，所以写完即绿——按 `tdd.md` fail-fast 规则 1，这是「RED 阶段意外变绿」，必须调查而不是放过。调查结论与计划一致：计划本身就把红证据的取法写进了验收条件（"人工在 `quantlab/dataset/crsp.py` 临时加一行注释提到某个已删符号，跑 gate 会红（执行时验证一次并在 SUMMARY 里记下，然后撤销）"）。实测：

注入 `quantlab/dataset/crsp.py:227`：

```python
# TEMPORARY RESIDUE PROBE: see `_resolve_identity` for the seam rule.
```

`uv run pytest -q tests/test_no_identity_residue.py -p no:cacheprovider`：

```
Fx                                                                       [100%]
>       assert residue == [], (
E       AssertionError: phase 03.11 deleted the ticker-identity machinery, but 1 reference(s)
E         to it survive in quantlab/. A reference in a COMMENT counts and is listed here on
E         purpose: ...
E         quantlab/dataset/crsp.py:227: # TEMPORARY RESIDUE PROBE: see `_resolve_identity` for the seam rule.
FAILED tests/test_no_identity_residue.py::test_the_deleted_identity_machinery_leaves_no_residue_in_quantlab
```

红在**自己的断言**上（`residue == []`），不是 collection error、不是 fixture 崩溃（#3770）；`Fx` 的 `x` 是 plan 08 分组的预期 xfail。随后 `git checkout -- quantlab/dataset/crsp.py` 撤销（**未使用 `git stash`**，遵守用户长期规则），复跑 `1 passed, 1 xfailed`。

这次探针同时证明了两件计划要求的事：gate 会红，以及 **gate 管得住注释**——注入的是一行纯注释。

## Files Created/Modified

- `quantlab/dataset/crsp_symbology.py`（590 → 152）— 删 `_MAX_LISTED_COLLISIONS` / `_OPEN_END` / `_as_date` / `_member_spans` / `_class_collision_pass` / `label_rows` / `_per_permno` / `resolve_collisions` / `_collision_message` / 符号学那份 `_DELISTING_FLAG` / `self.report` 五字段 / overrides 注入 / `nonconforming_symbols` 计算；连带去掉已无用的 `loguru` / `datetime` / `TRADEABLE_TICKER_PATTERN` 导入。留 `SUFFIX_DELIMITER`、`_NO_CLASS`、`__init__`（只剩 `security_info` 与 `_intervals` 缓存哨兵）、`symbol_intervals()`。模块 docstring 与 `symbol_intervals()` 的输出契约 docstring **重写**：后者逐字保留四列 / dtype / 排序 / 缓存的陈述（plan 09 sidecar 的 schema 依据），并加了一句指明它现在的唯一消费者是 ticker sidecar，以及为什么 null symbol 要留在表里。
- `quantlab/dataset/crsp.py`（1676 → 1594）— 删 `SYMBOLOGY_REPORT_SUFFIX` 及其 `#:` 注释、`self._symbology_report` 承载字段、`_resolve_identity` 全体（92 行，含撞车调用 / seam 检测 / seam NaN / 报告构造）、`symbology_report_path()`、`_write_identity_reports` 的 symbology 分支、`CrspSymbology(...)` 的第二个实参。重写五处文案：`_derivation` 末尾的「方法体留给 plan 07」注释、`_member_intervals`（TWO readers → 一个读者 + 为什么仍然 memo）、`_apply_security_filter`（退市半句 → 两套机制的区别 + 被否决的「整行豁免」替代方案）、`_assert_unique_panel_keys`（docstring 与 `ValueError` 文案，新出路指向 `wrds_crsp.py:818-840`）、`_admitted_without_ticker`（不再点名 `label_rows`）。
- `scripts/ingest_wrds_crsp.py` — 删掉 `Symbology sidecar:` 那三行打印，它的 `probe_dataset.symbology_report_path()` 是全仓最后一个真实调用者（Deviation 1）。
- `tests/test_crsp_symbology.py`（536 → 118）— 21 个测试留 2 个（`symbol_intervals()` 的 schema 与 FB/META 改名边界）；连带删掉只服务于撞车场景的四组本地 fixture（GOOG_C / BRK_A / LYB / WIN）、`QQQ_OVERRIDES`、`_daily_frame` / `_labelled_frame` / `_member_frame` / `DELISTED_SECINFO_ROWS`。模块 docstring 交代了删了什么、为什么不是覆盖缺口。
- `tests/test_crsp_identity.py` — **29 个测试一个未删**（见 Decisions 3）；删掉零调用者的 `_symbology_report` 助手，两处 suffix 改字面量，四处 docstring 重写。
- `tests/test_crsp_dataset.py` — `_report_paths` 的 symbology suffix 改字面量（并写明为什么），`label_rows` 那条段注释重写。
- `tests/test_crsp_tracer.py` — 回收 ticker 那条的 docstring 不再点名 `CrspSymbology.resolve_collisions`。
- `tests/test_no_identity_residue.py`（新建，158 行）— 见 Accomplishments 第 3、4 条。

## Decisions Made

1. **`CrspSymbology(...)` 的第二个实参在 Task 1 就去掉。** 计划把它列在 Task 2 的删除清单里（`:487-489`），但构造器签名在 Task 1 变了。按计划顺序做会让 `b29f335` 提交出一棵 `crsp.py` 一调用就 `TypeError` 的树。按 Rule 3（blocking，且由本任务的改动直接造成）就地修。
2. **`label_rows` 删之前的复核结果记录在案。** `grep -rn "label_rows" quantlab/ tests/ scripts/ example/` 的 11 条命中里：2 条是 `crsp_symbology.py` 自身、3 条是 `crsp.py` 的 docstring/注释、5 条在本 plan 删除的测试里、1 条是 `test_crsp_dataset.py` 的段注释。**零真实调用者**，与计划的预期一致，没有出现需要停下来报告的第二个调用者。
3. **`test_crsp_identity.py` 一个测试都没删。** 计划写的是「撞车 / seam / `symbol_overrides` 相关的删除」，但 plan 03 已经把那 7 个机器测试改写成 6 个 PERMNO 轴断言（prior-wave context 明确提示按现状读这个文件）。现在它们测的是「回收 ticker 是两列且格子各归各家」「同 PERMNO 改名是一列且跨改名日连续」「两家活着的证券不需要 universe 也能转换」「退市行与新股首行同日各自存活」——测的是**那个失败形态不可表达**，不是被删的机器。删掉它们会丢掉 T-03.11-02 的保护。只改了它们 docstring 里点名已删符号的四处措辞。
4. **两个 sidecar 测试把 `.crsp_symbology_report.json` 写成字面量。** 它们断言这个文件**不存在**，而常量随写入者一起删了。一个断言「文件不存在」的测试不能依赖被测代码同意这个文件名还存在，否则常量一删测试就 `ImportError` 而不是绿。两处都在 docstring 里写明了这个理由。
5. **gate 的符号入选规则是「专属于被删机制」。** `_as_date` 虽然在本 plan 从 `crsp_symbology.py` 删掉，但 `wrds_taq.py` / `wrds_crsp.py` 等另有三个模块各自定义同名私有助手（27 处命中）；`_OPEN_END` 是个通用的开区间哨兵名。两个都**不进**清单，理由写在测试的模块 docstring 里：一条会狼来了的 gate，是人会学着跳过的 gate。
6. **匹配用词边界正则而不是裸 `in`。** `_per_permno` 不能命中 `_admitted_without_ticker` 里的局部变量 `per_permno`，`_as_date` 若入选也不会命中 `_as_datetime`。
7. **`crsp_rebuild.py` 的 `.crsp_symbology_report.json` 字面量保留。** 它在一张「重建时要一并清掉的 sidecar 后缀」清单里，并且原地已有注释说明「后续 plan 停止生成它之后这条仍留在清单上」——一个不再生成的文件仍然可能躺在旧 store 旁边。gate 收的是常量名 `SYMBOLOGY_REPORT_SUFFIX`，不是这个字面量，所以两者不冲突。

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] `scripts/ingest_wrds_crsp.py` 是 `symbology_report_path()` 的真实调用者**

- **Found during:** Task 2（删 `symbology_report_path()` 前的全仓复核）
- **Issue:** 计划的 R6 删除清单说 `symbology_report_path()` 已无调用者。实际 `scripts/ingest_wrds_crsp.py:533` 在转换成功后打印三行 sidecar 路径，其中一行调用它。删掉方法而不动脚本，等于把这条 CLI 路径打成 `AttributeError`。计划的 `files_modified` 不含 `scripts/`。
- **Fix:** 删掉那三行 `print`（`Symbology sidecar: ...`）。另两行（调整锚、security filter）保留——它们指向的 sidecar 还在写。
- **Files modified:** `scripts/ingest_wrds_crsp.py`
- **Verification:** `grep -rn "symbology_report_path" quantlab/ tests/ scripts/` 归零；该脚本的 5 个既有失败（`tests/test_ingest_wrds_crsp.py`，plan 08 的交棒）数量与用例名未变
- **Committed in:** `ef2f387`（Task 2 提交）

**2. [Rule 3 - Blocking] 两个测试文件 import 了本 plan 删除的 `SYMBOLOGY_REPORT_SUFFIX`**

- **Found during:** Task 2
- **Issue:** `tests/test_crsp_dataset.py:1482` 与 `tests/test_crsp_identity.py:808` 从 `quantlab.dataset.crsp` import 这个常量，用来断言 sidecar **不存在**。常量一删，两个文件在 collection 阶段就 `ImportError`——而 `test_crsp_dataset.py` 不在计划的 `files_modified` 里。
- **Fix:** 两处都改成字面量 `".crsp_symbology_report.json"`，并在 docstring 里写明为什么（见 Decisions 4）。顺带删掉 `test_crsp_identity.py` 里零调用者的 `_symbology_report` 助手。
- **Files modified:** `tests/test_crsp_dataset.py`, `tests/test_crsp_identity.py`
- **Verification:** 三文件门 64 passed；六文件门 88 passed + 1 xfailed
- **Committed in:** `ef2f387`

**3. [Rule 2 - Missing Critical] 计划点名的三处 docstring 之外，还有五处指向已删机制**

- **Found during:** Task 2
- **Issue:** 计划的 S5 列了三处（`_apply_security_filter` 的退市半句、`_member_intervals` 的「TWO readers」、`_assert_unique_panel_keys` 的 `symbol_overrides` 出路）。实际还有：`_derivation` 末尾「方法体留给 plan 07」的注释、`_admitted_without_ticker` 的 docstring 与一条行内注释（两处点名 `label_rows`）、`_raw_axes_in_range` 与 `_raw_data_to_xr` 里两句「a run that fails on a symbology collision」、`__init__` 里 `_member_intervals_cache` 的「TWO readers」注释、以及四处测试 docstring。本 plan 的 must-have 把「一条指向已删代码的注释在这个仓库里比在别处更有害」列为正式产出，而 gate 计入注释——留着任何一条，gate 就是红的。
- **Fix:** 全部重写。`_admitted_without_ticker` 的重写最需要小心：它的**存在理由**就是复现已删标签器的谓词来回答一个反事实问题，所以新文案保留了「谓词逐步复制那个已退役的标签器」这个事实，但不再点名一个不存在的符号，并注明本方法是那段行为唯一还需要被陈述的地方。
- **Files modified:** `quantlab/dataset/crsp.py`, `tests/test_crsp_dataset.py`, `tests/test_crsp_identity.py`, `tests/test_crsp_tracer.py`
- **Verification:** `tests/test_no_identity_residue.py` 绿（1 passed, 1 xfailed）
- **Committed in:** `ef2f387` / `ebd22e1`

---

**Total deviations:** 3（2 blocking auto-fixed，1 missing-critical auto-added）
**Impact on plan:** 三条都在计划自己的意图之内（保持树可用、保持 gate 可绿），没有范围蔓延——删除面严格等于计划点名的清单，唯一超出 `files_modified` 的两个文件（`scripts/ingest_wrds_crsp.py`、`tests/test_crsp_dataset.py`）都是「不动就坏」的强制项，且改动各自只有几行。

## Issues Encountered

1. **全仓回归被一个与本 plan 无关的环境死锁挡住，只好分两段跑。** 第一次 `uv run pytest -q --ignore=test_factor_hierarchy --ignore=test_crsp_rebuild_measurements` 跑到 52 分钟仍未结束；`ps` 显示它 CPU 时间只有 2 分 06 秒、状态 `S`、`%CPU 0.1`——不是在算，是**阻塞**。`lsof` 定位到它卡在 `test_cs_zscore_stream.dylib` 上、持有一个 `loky-*` POSIX 信号量，即 KunQuant + joblib/loky 的进程池。同一时刻机器上还有**另外两个 agent 的 pytest 进程分别已经卡了 8 小时 54 分和 11 小时 46 分**，卡在同一个地方。

   处置：只 kill 掉本 agent 自己的那个进程（没有动别人的），把 `tests/test_cross_sectional_zscore.py` 也 `--ignore` 掉重跑，再单独跑该文件。结论是并发争用导致的死锁，与本 plan 无关——本 plan 一行 KunQuant 代码都没碰，该文件单跑 **2.6 秒 9 passed**。

   - 分段回归：`60 failed, 1617 passed, 1 skipped, 1 xfailed`（180.86s）
   - `tests/test_cross_sectional_zscore.py` 单跑：`9 passed`

   **failed 数与基线逐字相同（60）**，且失败集合按文件逐条核对一致：`test_ingest_shells.py` 14、`test_ingest_tiingo_universe_wiring.py` 11、`test_data_dir_cli.py` 10、`test_ingest_conversion_gate.py` 6、`test_volume_guard.py` 5、`test_ingest_wrds_crsp.py` 5、`test_factor_kunquant.py` 3、`test_spot_dataset.py` 2、`test_chunked_ingest.py` 2、`test_universe.py` 1、`test_entry_point_contracts.py` 1。其中 52 条是 `ModuleNotFoundError`（`scripts/` 未进 pythonpath）、3 条是 `KeyError: 'amount'`，合计 55 条长期既有失败；另 5 条全在 `test_ingest_wrds_crsp.py`，即 orchestrator 交给 plan 08 的那批，用例名逐条对上。**本 plan 触碰的 7 个文件没有一个出现在失败集合里。**

2. **passed 的算术差 1 个，未能归因，如实记录。** 预期 `1645 − 19（test_crsp_symbology.py 21→2）+ 1（新 gate）= 1627`，实测 `1617 + 9 = 1626`。已排除的可能：基线那 21 个不是参数化的（`grep -c parametrize` 为 0）、`test_crsp_identity.py` 前后都是 29 个测试、`test_cross_sectional_zscore.py` collect 恰好 9 个且全过、`skipped` 前后都是 1。最可能是分两段跑本身造成的（某个用例的结果依赖整套的执行顺序或共享状态），但没有再花一小时重测基线去证实。**决定性的那个量——失败集合——是逐条核对过的，没有变。** 这条差异写在这里而不是被抹平。

3. **`_apply_security_filter` 的新 docstring 比旧的长。** 旧文案用半句话带过退市继承的理由，而那半句正好挂在已删机制上。重写时按本文件的体例给全了「结论 + 为什么这样选 + 被否决的替代方案」，并明确写出「PERMNO 轴**没有**让这条继承变得多余，而它旁边那条确实被变得多余并删掉了」——这个区别本身就是 D-11 坑 a 的全部内容，值得占篇幅。

## Known Stubs

None。本 plan 没有留下 stub，没有 `skip`，没有未跑的 `<verify>`。

`tests/test_no_identity_residue.py` 里的 `xfail(strict=True)` 分组**不是** stub，是一次有意的跨 plan 交棒：它今天按预期红，plan 08 删掉两个配置字段那天会变成意外通过并因此失败，逼着 plan 08 回来把名字升进正式清单。已按 broken-windows 规则记入 `.planning/WINDOWS.md`（kind: `deviation`），所以即使这份 SUMMARY 滚出上下文，ship 门仍然看得见它。

## Threat Flags

None。本 plan 触碰的面全部在计划自己的 `<threat_model>` 里，四条 `mitigate` 都已落地且各有断言：

| Threat | 落地位置 | 锁 |
|---|---|---|
| T-03.11-09（误删退市判决继承） | `crsp.py` 的 `_keep_raw`/`_carried` 表达式在 diff 里零改动，`_DELISTING_FLAG` 仍 ×2 | `test_a_delisted_permno_keeps_its_last_row`（单独跑过一次）+ `test_a_delisting_row_inherits_the_previous_verdict` |
| T-03.11-22（误删唯一性 backstop） | 方法与调用点各 1 次，只改文案 | `grep -c 'def _assert_unique_panel_keys' == 1` + `'self._assert_unique_panel_keys' == 1` |
| T-03.11-23（误删名册覆盖） | `_member_days` / `_roster_exemption` / `_member_intervals` 各 1 次 | `test_crsp_identity.py` 的五条 GAP-C 测试全部保留且全绿 |
| T-03.11-24（留下指向已删代码的 docstring） | 八处重写（计划点名 3 处 + Deviation 3 的 5 处） | `tests/test_no_identity_residue.py`，**计入注释**，且实测会红 |

T-03.11-SC（包安装）：本 plan 零安装，未新增任何依赖。

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- **plan 08 有一条会自己提醒它的 gate。** `tests/test_no_identity_residue.py` 的 `DELETED_IN_03_11_08` 分组是 `symbol_overrides` / `nan_adj_at_permno_seam` 的单一清单；删完字段后把两个名字移进 `DELETED_IN_03_11_07` 并删掉那个 xfail 测试即可。**不要**只删字段不改清单——strict xfail 会把意外通过报成失败。另外 `collision_universe` 的**改名**也归 plan 08（字段本身在本 plan 保留，职责 2 还活着）。
- **plan 09 的 sidecar schema 已经锁好。** `CrspSymbology.symbol_intervals()` 是它唯一的数据源，四列 / dtype / 排序 / 缓存的契约逐字保留在方法 docstring 里，并由 `test_intervals_are_sorted_typed_and_one_row_per_input_interval` 锁住。`_write_identity_reports` 的方法名保留复数形式就是给这个 sidecar 留位置，docstring 里写明了。`test_crsp_symbology.py` 现在只剩两个测试，plan 09 可以按计划把它们迁到 `tests/test_crsp_ticker_sidecar.py`。
- **plan 10 的 must-have 已提前满足一半。** `{zarr}.crsp_symbology_report.json` 的写入路径从本 plan 起在代码里完全不存在（不是被守卫挡住），两个测试断言它不存在。`crsp_rebuild.py` 的清理清单仍保留那个字面量后缀，这是对的——旧 store 旁边可能还躺着一份。
- **plan 11 要处理 `example/wrds_crsp.md`。** 该文档目前仍把 `resolve_collisions`、撞车拒绝、`crsp_symbology_report.json`、seam 当作在世机制来描述（至少 6 处）。gate 刻意不扫 `example/`，正是为了让文档能写「这个机制在 03.11 被移除」。本 plan 按删除纪律没有越界去改它。
- **环境风险要向 orchestrator 报告：** 机器上有两个其他 agent 的 pytest 进程分别卡死 8h54m / 11h46m 在 KunQuant+loky 上，本 agent 未触碰它们。并发跑全仓 pytest 目前会死锁，后续 wave 的回归门建议串行或预先清理这些僵尸进程。
- 本 plan 全程**未读写**主仓库 `/Users/daizhaorong/projects/quantlab/data`；探针的 A/B 用 `git checkout --` 还原，**未使用 `git stash`**。

---
*Phase: 03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r*
*Completed: 2026-09-21*

## Self-Check: PASSED

- `key-files` 全部在盘上（`created` 1/1、`modified` 7/7 `FOUND`），SUMMARY 本身也在。
- 三个任务提交全部在 `git log 0c52a635..HEAD` 里：`b29f335`、`ef2f387`、`ebd22e1`；`commits: 3` 是用 `rev-list --count` 测出来的。
- 三个任务的 `<acceptance_criteria>` 逐条复跑：Task 1 九条全过（含 `wc -l == 152 < 200`、`crsp.py` 的 `_DELISTING_FLAG >= 2`）；Task 2 十二条全过（含 `test_a_delisted_permno_keeps_its_last_row` 单测）；Task 3 四条全过（含注入残留实测变红并撤销）。
- 计划级 `<verification>` 两条复跑：六文件门 **88 passed + 1 xfailed**；全仓门 **60 failed**，与基线逐字相同，失败集合逐文件逐用例核对一致，本 plan 触碰的文件全部不在其中（口径差异与 1 个未归因的 passed 见 Issues 1/2）。
- prohibitions 逐条核对：退市判决继承代码行零改动、`_assert_unique_panel_keys` 与调用点各 1、`symbol_intervals()`/`SUFFIX_DELIMITER`/`_NO_CLASS`/`_intervals` 哨兵在、`collision_universe` 字段在（11 处）、名册覆盖七个符号各 1、`cleaning.py` 未出现在 `git diff --name-only` 里、零 config 迁移/兼容层。
