---
phase: 03.11
plan: 15
type: execute
wave: 1
depends_on: ["03.11-14"]
files_modified:
  - quantlab/dataset/crsp_tickers.py
  - tests/test_crsp_ticker_sidecar.py
  - quantlab/base/model.py
  - tests/test_model_predict_panel.py
  - example/wrds_crsp.md
autonomous: true
gap_closure: true
gap_ids: [G-03.11-3]
requirements: [DATA-03]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "G-03.11-3 / WR-01：一份深嵌套 JSON 旁车（`'{\"a\":' * 100000 + '1' + '}' * 100000`，或 `'[' * 20000 + ']' * 20000`）下，`label([13407], date(2020,1,1))` 返回 `['13407']` 而**不抛**；同一份旁车下 `as_of(13407, date(2020,1,1))` 抛的是 shaped `ValueError`（含类名、绝对路径、重建补救），而不是裸 `RecursionError`"
    - "G-03.11-3 / WR-01：`RecursionError` 加在 **`payload` 属性**的 except 元组里，不是加在 `label()` 的守卫元组里 —— 两个入口因此都被覆盖，且 `as_of()` 的拒绝保持 shaped"
    - "G-03.11-3 / WR-02（操作者裁定：收窄）：`quantlab/dataset/crsp_tickers.py` 有且仅有一个模块级常量 `_UNUSABLE = (FileNotFoundError, ValueError)`，两处 `except` 站点共用它，方法体内不再有任何局部的守卫元组字面量"
    - "G-03.11-3 / WR-02：本模块内部的编程 bug **会上抛**而不是静默降级成数字 —— 一条回归用子类覆写 `as_of`（或 `_intervals`）使其抛 `KeyError` / `AttributeError`，断言 `label()` 把它原样放过去"
    - "G-03.11-3：`label()` 与 `as_of()` 的 docstring 描述的是现在成立的事 —— 不再宣称那三个被移出元组的类型会被吞掉，也不再宣称一条已被 WR-01 证伪的「永不抛」"
    - "G-03.11-3：`example/wrds_crsp.md:247` 这条「永不抛」的活副本与代码同步，深嵌套 JSON 这一类被明确纳入"
    - "IN-01：`.label()` 的调用点计数在**全部五处**改对 —— `crsp_tickers.py` 两处、`base/model.py` 一处、`tests/test_model_predict_panel.py` 一处、`tests/test_crsp_ticker_sidecar.py` 一处；实测 `grep -rn '\\.label(' quantlab/` 是三个调用点（masking.py / engine_vectorbt.py / 经 `_spell` 的 base/model.py），渲染六条人可见消息"
    - "encoding：旁车按 `encoding=\"utf-8\"` 读入，非法 UTF-8 字节触发的 `UnicodeDecodeError` 是 `ValueError` 子类，因此 `label()` 回落成数字、`as_of()` 抛 shaped `ValueError` —— 由一条回归明写，不再只是推理"
    - "empty：零字节旁车（`json.loads('')` → `JSONDecodeError` ⊂ `ValueError`）与 `{}`（无 `intervals` 键）两种「空」的行为各有一条用例，且两者结论不同：前者是 shaped 拒绝，后者是「这份旁车不认识任何名字」"
    - "adjacency：区间两端 INCLUSIVE 的语义不被本次改动触碰 —— 13407 的 FB 段止于 2022-06-08、META 段起于 2022-06-09，边界日各归一段；既有用例继续全绿即为证据"
    - statement: "ordering — `label()` 输出与输入一一对位、长度相等，且在只有部分 PERMNO 受损时按 PERMNO 逐个降级而非整体降级；本 plan 不改这两条，由既有 suite 全绿背书，逐 PERMNO 混合损坏的正面用例在 03.11-17 补"
      verification: backstop
    - statement: "idempotency — `payload` 的 `None` 哨兵缓存让每个实例最多读盘一次，收窄元组与新增 `RecursionError` 都不改变这一点；既有的 `test_the_sidecar_is_read_once_per_instance` 继续全绿即为证据"
      verification: backstop
    - statement: "concurrency — `label()` 在 `engine_vectorbt.py:303` 位于 simulation 中途、在 `base/model.py:1315` 位于快乐路径；本 plan 不引入新的共享可变状态，`_payload` 的惰性填充在竞态下最坏是重复读盘一次，不会返回错值"
      verification: backstop
  artifacts:
    - quantlab/dataset/crsp_tickers.py
    - tests/test_crsp_ticker_sidecar.py
    - example/wrds_crsp.md
  key_links:
    - "`payload` 属性的 except 元组 → `_intervals()` → `as_of()` → `label()` 的两处 `except _UNUSABLE` —— `RecursionError` 必须在最上游那一处被收住，下游三处才都被覆盖"
    - "`_UNUSABLE` 模块常量 → `label()` 的两个 except 站点 —— 一份拼写，不是两份字面量副本"
    - "`label()` → `quantlab/dataset/masking.py:262` / `quantlab/backtest/engine_vectorbt.py:303` / `quantlab/base/model.py:1315`（经 `_spell`）三个**裸**调用点，全都建立在「永不抛」之上"
  prohibitions:
    - statement: "不得把 `RecursionError` 加进 `label()` 的守卫元组来了事 —— 那样 `as_of()` 仍会抛裸 `RecursionError`，模块 docstring 的「每一次拒绝都是 shaped」依然是假话"
      status: unverified
      verification: flagged
    - statement: "不得同时保留五元组与一份「解释为什么保留」的 docstring —— 03.11-REVIEW.md WR-02 逐字要求「Do not leave both」，操作者已裁定收窄"
      status: unverified
      verification: flagged
    - statement: "不得让展示层给出一个看起来合理、实际是降级产物的答案，而不留下任何可区分的痕迹 —— 本 plan 负责让**本模块的 bug** 不再被静默吞掉；让**数据损坏**的降级可见是 03.11-17 的 WR-03"
      status: unverified
      verification: flagged
    - statement: "不得用 `except Exception` 替代收窄后的元组 —— 那正是原 docstring 明确拒绝的写法，且会把 WR-02 的问题放大而不是缩小"
      status: unverified
      verification: flagged
    - statement: "不得在把「深嵌套 JSON 会爆栈」写进回归时，顺手提高 `sys.setrecursionlimit` —— 那是把一个可观察的拒绝换成一次更贵的崩溃"
      status: unverified
      verification: flagged
  gap_ids: [G-03.11-3]
---

<!-- planner-discipline-allow: RecursionError, _UNUSABLE, FileNotFoundError, ValueError, CrspTickerLookup -->

<assumption_delta_decision>
`assumption-delta scan 03.11` 报 `detected: true`（`kind: pluralization`，命中词 `also`，来自 ROADMAP 散文，与本轮 scope 无关）。
但同一族的问题在**模块层**确实成立，所以照样记一条：

- **现在的主名词：** 「一份不可用的旁车会抛出什么」这件事，此前有**两份**局部字面量副本（`label()` 内的两个 `except` 站点），
  一个未来的第三个展示入口会让它变三份。
- **决定：`promote`。** 把这个概念提升为模块级常量 `_UNUSABLE`，两个 `except` 站点降级为它的引用点。
  不是 add-alongside（保留局部字面量再加一个常量），那会让「哪份是权威」这个问题永远开着。
- 同一条逻辑在 03.11-17 会再走一次（`_object_payload()` 之于「payload 顶层形状在哪里检查」），
  届时同样取 promote。
</assumption_delta_decision>

<objective>
G-03.11-3 上一轮只收了一半：plan 12 拦住了 UAT 点名的三个结构损坏输入，却把契约升格成了一条
更强的声明（「缺失**或损坏**时都不抛」），而这条更强的声明有一个可达的反例，并且用来兑现它的
守卫元组方向与它自己的 docstring 相反。

**WR-01（可达逃逸口）。** 深嵌套 JSON 让 `json.loads` 抛 `RecursionError`——`RuntimeError` 的子类，
既不在 `payload` 属性的 `(OSError, ValueError)` 里，也不在 `label()` 的五元组里。
本相位验证实测：`label([13407], date(2020,1,1))` **RAISED RecursionError**，`as_of(...)` 同样抛出且
**unshaped**，连模块 docstring 的「每一次拒绝都是 shaped」一并证伪。三个**裸**调用点全部无保护，
其中 `quantlab/backtest/engine_vectorbt.py:303` 位于 simulation 中途 —— 这是最贵的那个落点。

**WR-02（守卫方向相反，操作者已裁定：收窄）。** plan 12 在同一次提交里给 `_intervals()` 与 `as_of()`
加了结构守卫之后，`KeyError` / `AttributeError` / `TypeError` 就再也不可能来自数据损坏，
唯一来源变成本模块自己的 bug —— 而 `:267-269` 的 docstring 正好声称这个元组的存在理由是让**这一类**上抛。
实测：对一份**完全合法**的旁车，一个字符的错字就让 `label()` 静默返回 `['13407']` 而不是 `['FB']`，
而快乐路径测试全绿。操作者裁定**收窄**为 `(FileNotFoundError, ValueError)`，并提到模块常量 `_UNUSABLE`。

**IN-01（计数不实）。** 模块 docstring 说有六个调用点，实测是**三个**（`masking.py:262`、
`engine_vectorbt.py:303`、经 `_spell` 的 `base/model.py:1315`），另两处只是在散文里提到类名。
这个数字是「守卫放在查表层而不是每个调用点」这条设计论证的前提，且已被复制进另外三个文件。

Purpose: 让这条被三个裸调用点依赖的契约真正成立，并让它的 docstring 说的是真话。
Output: `_UNUSABLE` 模块常量、`payload` 属性覆盖 `RecursionError`、三条新回归、四个文件里五处计数订正、
一处文档同步。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-REVIEW.md
@.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-VERIFICATION.md
@.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-12-PLAN.md
@quantlab/dataset/crsp_tickers.py
@tests/test_crsp_ticker_sidecar.py
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: WR-01 —— 把「永不抛 / 拒绝必 shaped」这条路从最上游堵死</name>
  <files>quantlab/dataset/crsp_tickers.py, tests/test_crsp_ticker_sidecar.py</files>
  <read_first>
    - `quantlab/dataset/crsp_tickers.py` 全文（296 行）—— 重点是 `:99-132` 的 `payload` 属性、`:155-182` 的 `_intervals()`、`:199-240` 的 `as_of()`、`:242-295` 的 `label()`，以及 `:17-37` 与 `:250-269` 两段把契约写死在散文里的 docstring
    - `.planning/phases/03.11-.../03.11-REVIEW.md` 的 WR-01 一节（lines 98-157）—— 逐字的复现配方、修复位置的理由、以及「CPython 在异常展开后会恢复栈余量，所以在 `RecursionError` 之后再拼消息是安全的」这条实测结论
    - `.planning/phases/03.11-.../03.11-VERIFICATION.md` 的 `gap_id: G-03.11-3` artifacts 第一条（lines 93-107）—— 两份复现载荷的确切字节，以及「`CrspTickerLookup` 的构造参数是**旁车文件路径**、不是 zarr 目录；传目录只会走到『文件缺失』分支而测不到这个洞」这条陷阱
    - `tests/test_crsp_ticker_sidecar.py:403-455` —— `MALFORMED_SIDECARS` 的注释表与两处参数化；新回归要**另起**，不得并进这个参数化
  </read_first>
  <precondition>03.11-14 已清空全树 `__pycache__`；本任务的任何 pytest 结果若取自清场之前，都不是 HEAD 的结果。</precondition>
  <behavior>
    - 新回归 1（label 侧）：写一份深嵌套 JSON 旁车，断言 `label([13407], date(2020, 1, 1))` 返回 `['13407']`，不抛。
    - 新回归 2（as_of 侧）：同一份旁车，断言 `as_of(13407, date(2020, 1, 1))` 抛 `ValueError`，消息里含类名 `CrspTickerLookup:`、旁车的绝对路径、以及 `.crsp_*.json` 这条重建补救 —— 与既有 `test_as_of_refuses_a_structurally_broken_sidecar_with_a_shaped_error` 的三项断言同款。
    - 新回归 3（encoding）：一份**非法 UTF-8 字节**的旁车（以 `write_bytes` 写入，例如一段孤立的 0x80 续字节）——`label()` 返回数字，`as_of()` 抛同款 shaped `ValueError`。
    - 新回归 4（empty）：一份**零字节**旁车 —— `label()` 返回数字，`as_of()` 抛同款 shaped `ValueError`。既有的 `test_a_payload_with_no_intervals_key_keeps_its_current_behaviour`（`{}`）保持原样，两种「空」的结论不同，必须并列可见。
    - 既有全部用例保持绿，特别是 FB→META 边界日那两条（区间两端 INCLUSIVE，本任务不碰）。
  </behavior>
  <action>
**RED 先行。** 先在 `tests/test_crsp_ticker_sidecar.py` 里加上面四条回归，跑一次确认第 1、2 条红
（深嵌套那两条会以 `RecursionError` 而不是断言失败的形式红，这正是 WR-01 的症状，把这次输出抄进 SUMMARY），
第 3、4 条此刻应当已经绿（它们锁的是 `payload` 现有 `(OSError, ValueError)` 已覆盖的行为，
加它们是为了把「已经成立」从推理变成断言）。

四条回归**不要**塞进 `MALFORMED_SIDECARS` 参数化。那个参数化的注释表（`:403-419`）逐字说明它覆盖的是
**结构损坏**——JSON 解析成功、形状不对——并画了「在读 intervals 时炸」与「在逐 PERMNO 的 as_of 里炸」
两条路径。深嵌套 / 非法编码 / 零字节属于**解析阶段**就失败，走的是 `payload` 属性那条更上游的路，
混进去会让那张表说谎。另起一个常量（例如放在 `MALFORMED_SIDECARS` 旁边，注释里明写两类的分界线在
「`json.loads` 有没有返回」）或直接写四个独立用例，任选，但两类必须在文件里肉眼可分。

深嵌套载荷用 VERIFICATION 记录过的其中一份即可（`'[' * 20000 + ']' * 20000` 更短更快，
`'{"a":' * 100000 + '1' + '}' * 100000` 是另一份）。
**不要**用提高解释器递归深度上限的办法绕开它 —— 那是把一次可观察的拒绝换成一次更贵的崩溃，
而且会把这条回归变成对机器栈大小的测量。

**GREEN。** 修改只在 `payload` 属性（`:118-131`）那一个 `except` 子句上：把 `RecursionError` 加进它捕获的类型里。
REVIEW WR-01 的 Fix 段逐字给了这一行，并说明了为什么必须放在这里而不是 `label()` 的元组里 ——
放在 `payload` 上，`as_of()` 的拒绝才保持 shaped，两个入口才都被覆盖。

同时更新两段散文：`payload` 属性自己的 docstring（补一句「解析阶段的失败包含深嵌套导致的栈耗尽」），
以及模块 docstring `:19-27` 那句「All THREE ways the sidecar can fail RAISE, and each refusal is shaped」——
它现在才真的成立，把「bytes are not JSON」这一条展开成「不是 JSON，或嵌套深到解析器自己撑不住」。
  </action>
  <acceptance_criteria>
    - `grep -n 'except (OSError, ValueError, RecursionError) as exc:' quantlab/dataset/crsp_tickers.py` 恰好返回 1 行
    - `grep -c 'RecursionError' quantlab/dataset/crsp_tickers.py` 返回 ≥ 2（一处 except 子句 + 至少一处 docstring 说明）
    - `grep -c 'setrecursionlimit' quantlab/dataset/crsp_tickers.py tests/test_crsp_ticker_sidecar.py` 全部返回 0
    - `uv run pytest -q tests/test_crsp_ticker_sidecar.py` 全绿且用例数比改动前多 4 条以上
    - 深嵌套旁车下 `label([13407], date(2020,1,1))` 返回 `['13407']`；`as_of(13407, date(2020,1,1))` 抛的 `ValueError` 消息同时含 `CrspTickerLookup:`、旁车绝对路径、`.crsp_*.json`
    - `MALFORMED_SIDECARS` 仍恰好三项（新回归没有并进去）：`grep -c 'pytest.param' tests/test_crsp_ticker_sidecar.py` 在该常量块内的计数不变，且该常量的注释表仍只描述结构损坏
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/test_crsp_ticker_sidecar.py -p no:cacheprovider</automated>
    <fails_when>退出码非 0；或输出中出现 `RecursionError`；或摘要行的 passed 数不比改动前的 26 至少多 4</fails_when>
  </verify>
  <done>深嵌套、非法 UTF-8、零字节三类解析期失败下，`label()` 一律回落成数字、`as_of()` 一律抛 shaped `ValueError`；修复落在 `payload` 属性的 except 子句上；两段散文与之同步。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-02 —— 收窄成 `_UNUSABLE`，并补上「本模块 bug 会上抛」的回归</name>
  <files>quantlab/dataset/crsp_tickers.py, tests/test_crsp_ticker_sidecar.py</files>
  <read_first>
    - `quantlab/dataset/crsp_tickers.py:242-295` 的 `label()` 全文，特别是 `:267-269` 那段声称「`except Exception` 被刻意回避、所以本模块的真 bug 仍会到达调用方」的 docstring，以及 `:271` / `:274` / `:293` 三处
    - `.planning/phases/03.11-.../03.11-REVIEW.md` 的 WR-02 一节（lines 161-216）—— 逐条论证为什么那三个类型在结构守卫落地后只可能来自本模块 bug，两次子类注入的实测输出，以及 Fix 段给出的 `_UNUSABLE` 常量与它的 `#:` 注释原文
    - `.planning/phases/03.11-.../03.11-VERIFICATION.md:108-116` 与 `:233-235` —— 操作者裁定的两个选项及其代价，本 plan 执行的是**收窄**那一支
    - `quantlab/dataset/crsp_reference.py` 的 `manifest` 属性 —— 本仓库读 JSON 旁车的同一套范式，`_UNUSABLE` 的注释口吻向它看齐
  </read_first>
  <precondition>Task 1 已合入本 plan 的工作副本：`_UNUSABLE` 要与 `payload` 属性新捕获的 `RecursionError` 保持分工——前者是 `label()` 对**上游已 shaped 的拒绝**的兜底，后者是把原始异常变成 shaped 拒绝的地方。</precondition>
  <reversibility rating="reversible">收窄是一个模块级常量的内容变更；若将来决定改回 defence-in-depth，改的是同一行，不产生迁移成本。操作者已在 03.11-VERIFICATION.md human_verification #1 上裁定，不重新讨论。</reversibility>
  <behavior>
    - 新回归（bug 上抛）：定义一个 `CrspTickerLookup` 的测试内子类，覆写 `as_of` 使其抛 `KeyError`（模拟本模块内部的索引错字）；对一份**完全合法**的旁车调用继承来的 `label()`，断言 `KeyError` **传播出来**，而不是被吞成 `['13407']`。
    - 第二条（另一半路径）：同一手法，子类覆写 `_intervals` 使其抛 `AttributeError`（模拟属性名错字），断言 `label()` 同样放行。
    - 两条都必须用**合法**旁车：拿一份损坏旁车做载体，会让「是守卫吞了它还是数据本来就坏」无法区分。
    - 既有全部用例保持绿 —— 包括三条 `MALFORMED_SIDECARS` 参数化的两处（它们抛的是 `ValueError`，仍在收窄后的元组里）。
  </behavior>
  <action>
**RED 先行。** 先写上面两条回归，跑一次确认它们红（当前五元组会把两种异常都吞掉，`label()` 返回数字，
断言「应当抛出」于是失败）。把这次输出抄进 SUMMARY —— 它是「设计意图此前无人守」的直接证据。

**GREEN。** 在 `quantlab/dataset/crsp_tickers.py` 模块层（`__all__` 之后、`class CrspTickerLookup` 之前）
新增一个常量 `_UNUSABLE`，内容是 `(FileNotFoundError, ValueError)`，并按 REVIEW Fix 段给出的 `#:` 注释
写清楚它为什么恰好是这两个、以及为什么另外三个类型被移出去（两个入口的每一条结构缺陷现在都经
`_malformed` 或 `payload` 汇成这两类；剩下那三类只可能是本模块的 bug，而本模块的 bug 必须到达调用方）。

把 `label()` 里那个方法内的局部守卫元组**整个删掉**，两处 `except` 改为引用 `_UNUSABLE`。
常量前缀下划线、不进 `__all__` —— 它是本模块的内部词汇，不是公开面。

重写 `:267-269` 那段 docstring：它现在要说的是「元组被收窄到守卫真正能产生的两类，所以本模块的
编程 bug 会原样到达调用方」，并点名那条新回归是它的锁。**不要**保留任何解释「为什么还留着三个 backstop」
的句子 —— REVIEW 逐字要求不要两者都留，而裁定是收窄。

顺带检查 `:283-290` 那个 `int(value)` 的窄守卫 `(TypeError, ValueError)`：它是对**调用方传入的标签**
做的类型转换，与旁车无关，是独立的一处、不受本次收窄影响。在它的注释里补半句说明这个区别，
免得下一个读者以为它是第三份漏改的副本。
  </action>
  <acceptance_criteria>
    - `grep -c '_UNUSABLE = (FileNotFoundError, ValueError)' quantlab/dataset/crsp_tickers.py` 返回 1
    - `grep -c 'except _UNUSABLE' quantlab/dataset/crsp_tickers.py` 返回 2（两个站点共用一份拼写）
    - `grep -c 'unusable = (' quantlab/dataset/crsp_tickers.py` 返回 0（方法内的局部元组字面量已不存在）
    - `grep -c '_UNUSABLE' quantlab/dataset/crsp_tickers.py` 返回 ≥ 4（常量定义 + 两处引用 + 至少一处散文）
    - `python3 -c "import ast,sys; src=open('quantlab/dataset/crsp_tickers.py').read(); ast.parse(src)"` 无异常，且 `__all__` 仍只含 `CrspTickerLookup`
    - 子类抛 `KeyError` / `AttributeError` 两条回归全绿（异常传播而非静默降级）
    - `uv run pytest -q tests/test_crsp_ticker_sidecar.py tests/test_model_predict_panel.py` 全绿
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/test_crsp_ticker_sidecar.py tests/test_model_predict_panel.py -p no:cacheprovider</automated>
    <fails_when>退出码非 0，或摘要行出现任何 failed / error 计数</fails_when>
    <automated>grep -c 'except _UNUSABLE' quantlab/dataset/crsp_tickers.py</automated>
    <fails_when>输出不是 2（少于 2 说明有站点漏改，多于 2 说明引入了计划外的第三个站点）</fails_when>
  </verify>
  <done>模块级 `_UNUSABLE = (FileNotFoundError, ValueError)` 被两处 `except` 共用；方法内局部元组消失；两条子类注入回归证明本模块 bug 会上抛；docstring 说的是现在成立的事。</done>
</task>

<task type="auto">
  <name>Task 3: IN-01 计数订正（五处）+ `example/wrds_crsp.md` 的「永不抛」同步</name>
  <files>quantlab/dataset/crsp_tickers.py, quantlab/base/model.py, tests/test_model_predict_panel.py, tests/test_crsp_ticker_sidecar.py, example/wrds_crsp.md</files>
  <read_first>
    - `.planning/phases/03.11-.../03.11-REVIEW.md` 的 IN-01 一节（lines 320-344）—— 它给出的修法是「说『三个调用点，渲染六条人可见消息』，或者把三个按 file:line 列出来、把散文提及另起一句」
    - `quantlab/dataset/crsp_tickers.py:17-37`（模块 docstring 两个入口那一段）与 `:242-249`（`label()` 开头那一段）
    - `quantlab/base/model.py:1303-1313`（`_spell` 的 docstring）
    - `tests/test_model_predict_panel.py:710-727` 与 `tests/test_crsp_ticker_sidecar.py:378-388`
    - `example/wrds_crsp.md:242-250` —— 「永不抛」那条活副本的上下文
    - `quantlab/acquisition/inspector.py:511` 与 `:553`、`quantlab/utils/cli.py` 的 `--symbols` 帮助文案 —— 这两处**只在散文里提到类名**，不构造也不调用，订正后的句子要把它们与真调用点分开说
  </read_first>
  <action>
**计数订正（本 plan 已独立复核，证据写在这里）。**
`grep -rn '\.label(' quantlab/` 实测命中**两行**：`quantlab/dataset/masking.py:262` 与
`quantlab/backtest/engine_vectorbt.py:303`；第三个调用点是 `quantlab/base/model.py:1315`，
经 `self.symbol_labeller(symbols, as_of)` 间接抵达（`_spell` 内）。
`grep -rn 'CrspTickerLookup' quantlab/` 显示 `quantlab/acquisition/inspector.py:511`/`:553` 与
`quantlab/utils/cli.py` 只是在**文案里点名类名**，不构造实例、不调用。
所以正确说法是：**三个调用点，渲染六条人可见消息**。

需要订正的**五处**（本 plan 复核时比 REVIEW 的「三处」多找到两处，一并改，否则改一半就是 plan 13
自己 patterns-established 警告过的「只删不补」的变体）：
`quantlab/dataset/crsp_tickers.py:29`、`quantlab/dataset/crsp_tickers.py:245`、
`quantlab/base/model.py:1311`、`tests/test_model_predict_panel.py:725-726`、
`tests/test_crsp_ticker_sidecar.py:382`。
六条消息的枚举本身**保留**（它是「这些都是让既有消息可读、不是新功能」这条论证的内容），
改的只是把它们称作「调用点」的那个量词。`base/model.py` 与 `test_model_predict_panel.py` 里
「与其给每个展示点各包一个 try，不如把守卫放在查表层」这条论证也**保留**——它在三个调用点上同样成立，
且 `base/model.py` 那一处按 03.11-REVIEW.md 已验证为 docstring-only，本次仍必须是 docstring-only。

**顺带订正的两处相邻计数**（复核时发现，与上面同族）：
`quantlab/dataset/crsp_tickers.py:85` 说若没有 `beside_store` 那个后缀字面量会被重复几次 ——
实测 `grep -rn 'beside_store' quantlab/` 的生产构造点是**两个**（`masking.py:115`、`base/backtest.py:198`）；
`tests/test_crsp_ticker_sidecar.py:496` 是同一条声明的另一份拷贝，数字也不对。两处都按实测数字改。

**文档同步。** `example/wrds_crsp.md:247` 那条「**永不抛**，旁车缺失或损坏——含解析不了与解析得了但
形状不对——都原样回落成数字」现在才真的成立。把「解析不了」这一半展开，明确把
「嵌套深到解析器自己撑不住」和「字节不是合法 UTF-8」纳进去，并在同一句里点出
`as_of` 侧对应的是一次 shaped 拒绝。不要改这一段的其余论证（区间表 vs last-name-wins、
判据是盘上有没有旁车文件而不是面板属于哪个厂商）。
  </action>
  <acceptance_criteria>
    - `grep -n 'six display points\|six human-visible points\|六个展示点' quantlab/dataset/crsp_tickers.py quantlab/base/model.py tests/test_model_predict_panel.py tests/test_crsp_ticker_sidecar.py` 无任何匹配（退出码非 0）
    - `grep -c 'three call sites\|三个调用点' quantlab/dataset/crsp_tickers.py quantlab/base/model.py tests/test_model_predict_panel.py tests/test_crsp_ticker_sidecar.py` 四个文件各返回 ≥ 1
    - `grep -n 'four call sites' quantlab/dataset/crsp_tickers.py` 与 `grep -n 'three display points' tests/test_crsp_ticker_sidecar.py` 均无匹配
    - `git diff --stat quantlab/base/model.py` 显示改动只落在 `_spell` 的 docstring 内：用 `python3 -c` 解析改前改后两版、剥掉全部 docstring 后 `ast.dump` 逐字相同（03.11-REVIEW.md Summary 第一条用的就是这个办法）
    - `grep -c '永不抛' example/wrds_crsp.md` 返回 ≥ 1，且同一段里出现「UTF-8」与「嵌套」两个词
    - `uv run pytest -q tests/test_crsp_ticker_sidecar.py tests/test_model_predict_panel.py tests/test_model_layer.py` 全绿
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q tests/test_crsp_ticker_sidecar.py tests/test_model_predict_panel.py tests/test_model_layer.py -p no:cacheprovider</automated>
    <fails_when>退出码非 0，或摘要行出现任何 failed / error 计数</fails_when>
    <automated>uv run pytest -q tests/test_crsp_ticker_sidecar.py tests/test_model_predict_panel.py tests/test_crsp_identity.py tests/test_extensibility_contract.py -p no:cacheprovider</automated>
    <fails_when>退出码非 0，或 passed 数少于 98 + 本 plan 新增用例数（本 plan 只加不减用例，passed 数下降即为回归）</fails_when>
  </verify>
  <done>四个文件里五处调用点计数与实测一致；两处相邻的构造点计数一并订正；`quantlab/base/model.py` 仍是 docstring-only；`example/wrds_crsp.md` 的「永不抛」与代码同步。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 磁盘上的 `{zarr}.crsp_tickers.json` → `CrspTickerLookup` | 一份可被手工编辑或写坏的文件进入一条**展示**路径；`json.loads` 是这里唯一的解析器 |
| `label()` → 三个**裸**调用点 | 调用方不包 try，完全建立在「永不抛」之上；其中 `engine_vectorbt.py:303` 在 simulation 中途 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.11-52 | Denial of Service | `crsp_tickers.py:118-131` 的 `json.loads` | medium | mitigate | Task 1 把 `RecursionError`（栈耗尽）纳入 `payload` 属性的 except，深嵌套载荷不再能从三个裸调用点穿出；回归以 20000 层嵌套锁住 |
| T-03.11-53 | Information Disclosure | `_malformed()` 打印旁车绝对路径 | low | accept | 沿用 `T-03.11-44` 的既有裁定（本地研究平台，路径是重建补救的必要信息）；本 plan 不改这条消息的内容 |
| T-03.11-56 | Tampering | 收窄后的 `_UNUSABLE` 兜底面 | low | mitigate | 收窄只移除「只可能来自本模块 bug」的三类；两条子类注入回归证明那三类现在会上抛而不是被吞 |
| T-03.11-57 | Spoofing | 降级输出的 PERMNO 数字与正常无旁车输出逐字相同 | medium | transfer | 本 plan 不解决可见性（那是 WR-03）；转交 03.11-17 的 `_degrade` 一次性告警 |

（本 plan 无 npm/pip/cargo 安装动作，依赖集一字未动，故无供应链条目。）
</threat_model>

<verification>
1. 深嵌套 JSON 旁车下 `label()` 返回数字、`as_of()` 抛 shaped `ValueError`。
2. 非法 UTF-8 与零字节旁车下同上；`{}` 的既有行为不变。
3. `_UNUSABLE = (FileNotFoundError, ValueError)` 是模块唯一一份拼写，被两处 `except` 共用。
4. 子类注入 `KeyError` / `AttributeError` 时 `label()` 放行而非降级。
5. 五处调用点计数与 `grep -rn '\.label(' quantlab/` 的实测一致；`quantlab/base/model.py` 仍 docstring-only。
6. 四个 gap-closure suite 在清缓存后的默认缓存目录下全绿，passed 数 ≥ 98 + 本 plan 新增用例数。
</verification>

<success_criteria>
G-03.11-3 关闭：「`label()` 永不抛、`as_of()` 每一次拒绝都是 shaped」这条契约不再有可达反例；
守卫元组的方向与它自己的 docstring 一致；那条被三个裸调用点依赖的声明，说的是真话。
</success_criteria>

## Artifacts this phase produces

| 符号 | 位置 | 性质 |
|---|---|---|
| `_UNUSABLE` | `quantlab/dataset/crsp_tickers.py` 模块层 | 新常量，`(FileNotFoundError, ValueError)`，前缀下划线、不进 `__all__` |
| 深嵌套 JSON 的 `label` / `as_of` 两条回归 | `tests/test_crsp_ticker_sidecar.py` | 新测试函数（名由执行者取，`test_` 前缀，SUMMARY 逐个列出） |
| 非法 UTF-8 旁车回归 | 同上 | 新测试函数 |
| 零字节旁车回归 | 同上 | 新测试函数 |
| 子类注入 `KeyError` → `label()` 放行的回归 | 同上 | 新测试函数 |
| 子类注入 `AttributeError` → `label()` 放行的回归 | 同上 | 新测试函数 |
| （可选）解析期失败载荷常量 | 同上 | 新常量，若执行者选择用参数化而非独立用例；必须与 `MALFORMED_SIDECARS` 分开命名并在注释里写明分界线 |

**本 plan 不新增任何生产符号**（`_UNUSABLE` 是内部常量，`payload` / `label` / `as_of` 的签名一字未动）。
