---
phase: 03.11
plan: 18
type: execute
wave: 3
depends_on: ["03.11-17"]
files_modified:
  - tests/test_crsp_rebuild_measurements.py
autonomous: true
gap_closure: true
gap_ids: [G-03.11-5]
requirements: [DATA-03]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "G-03.11-5：`data/data/us_equity/1d/wrds_crsp_sp500_1d.zarr.crsp_filter_report.json` 由**当前 HEAD** 的代码重新生成，`roster_overrides.permnos` 里每条记录的键集恰好是 `{types, rows, first, last}` —— 那个与自己 JSON key 逐字节相同的 `symbol` 字段从盘上消失"
    - "G-03.11-5：这条键集断言进了**常设**的真实数据门 `tests/test_crsp_rebuild_measurements.py`，此后每一次重建都会复检它 —— 不是一次性的手工核对"
    - "G-03.11-5：raw tier 在任何删除动作之前被确认在位（`data/downloads/us_equity/1d/wrds_crsp/wrds` 的 132 个 `.pqt` + 兄弟目录 `_reference` 的 6 张参考表 + manifest）；缺失时 `_data_root()` 以 `pytest.fail` **停下来报告**，不 skip、不静默跳过"
    - "G-03.11-5：这次重建的备份写进一个**新的**目录，不覆盖 `data/_backup_pre_03.11`（相位前的 pre-fix store）与 `data/_backup_pre_03.11_10`（03.11-10 那一代）—— 被替换掉的这一代是唯一能展示本 gap 的产物，写它的代码已经改了，覆盖即不可复原"
    - "empty：`roster_overrides.permnos` 的键集断言**不会空跑通过** —— 先断记录数 ≥ 1（本 store 实测为 1 条，PERMNO 75154），再逐条断键集"
    - "empty：`dropped_permnos` 当前实测为 `{}`，所以那一支在这个 store 上**无从观察** —— 断言以「逐条检查现有记录 + 打印记录数」的形式写，并在 SUMMARY 里如实说明它这次没有被观察到，而不是声称已验证"
    - "adjacency：重建后 PERMNO 75154 那条记录的 `first` / `last` 仍是窗口两端（`2024-01-02` / `2024-12-31`）、`rows` 仍是 252 —— 删字段没有顺手改动端点语义"
    - "重建后既有的四条真实数据断言全部仍成立：`adjClose <= 0 == 0`、`close == 0 == 0`、两个 NaN 计数 == `structural_gaps`、`anomaly_flag_true == 13`；`symbol` 轴的原始 Zarr dtype 仍是整数；`.crsp_symbology_report.json` 仍不存在；`.crsp_tickers.json` 的 PERMNO 集合仍等于面板轴"
    - statement: "idempotency — 重建对同一份 raw tier 是确定性的，键集断言因此在每一次未来的重建上复检；本 plan 只跑**一次**重建（第二次会再删再转一遍真实 store），所以「连跑两次得到同一键集」这一条是由常设断言承接的推论，不是本轮的实测"
      verification: backstop
    - statement: "ordering — 报告以 `json.dumps` 写、`json.load` 读，键集用集合比较，结论不依赖 JSON 里的键序"
      verification: backstop
    - statement: "encoding — 过滤审计报告是纯 ASCII 的 JSON（PERMNO 数字串、日期串、CRSP type 串），本 plan 不引入任何非 ASCII 字段"
      verification: backstop
    - statement: "concurrency — 这次重建会删除并重写主仓库里的真实 store；它被排在最后一个 wave 且与任何其它 plan 零并发，前三个 wave 的测试全部只用 tmp_path"
      verification: backstop
  artifacts:
    - tests/test_crsp_rebuild_measurements.py
  key_links:
    - "`quantlab/dataset/crsp.py:1263-1271` 的 `_permno_breakdown` 字典字面量 → `crsp.py:1099` 的 `roster_overrides[\"permnos\"]` 与 `:1116` 的 `dropped_permnos` → 盘上 `.crsp_filter_report.json` → 本 plan 的键集断言。代码侧 plan 13 已改，本 plan 补上从代码到盘上产物这最后一段"
    - "`CrspStoreRebuilder.rebuild()` 的 assert → backup → clear → convert 顺序 → raw tier 缺失时在任何删除之前就抛 `FileNotFoundError`"
    - "`EXPECTED_WRITTEN_SIDECARS` 的 mtime 断言 → 每份审计文件描述的都是**这一个**面板，而不是上一个"
  prohibitions:
    - statement: "不得在 raw tier 缺失时静默跳过或 skip —— `_data_run()` 系列路径一律 `pytest.fail`，因为一次 skip 在任何摘要行里都读作 pass，而这个门存在的全部意义就是拒绝假绿"
      status: unverified
      verification: flagged
    - statement: "不得把这次重建的备份指向 `data/_backup_pre_03.11` 或 `data/_backup_pre_03.11_10` —— `backup()` 以 `dirs_exist_ok=True` 复制，指向旧目录会就地覆盖一代不可复原的产物"
      status: unverified
      verification: flagged
    - statement: "不得放宽任何既有的真实数据断言来让重建通过 —— `anomaly_flag_true == 13` 是等式不是不等式，数字变了意味着有事实变了，要去查是哪一件，而不是把断言改宽"
      status: unverified
      verification: flagged
    - statement: "不得声称 `dropped_permnos` 那一支已被验证 —— 它当前是 `{}`，在这个 store 上无从观察；如实说它没被观察到"
      status: unverified
      verification: flagged
    - statement: "不得把 `.crsp_filter_report.json` 或 `data/**` 列进 `files_modified` —— 它们按设计 gitignored，是操作目标不是源码产物"
      status: unverified
      verification: flagged
  gap_ids: [G-03.11-5]
---

<!-- planner-discipline-allow: dropped_permnos, roster_overrides, CrspStoreRebuilder, QUANTLAB_DATA_ROOT -->

<objective>
G-03.11-2 的裁定只落了一半：代码侧 plan 13 已经把那个冗余字段删干净（`quantlab/dataset/crsp.py:1263-1271`
的字典字面量只剩四个键，两个调用点共用同一渲染），但**盘上产物没有重新生成**。本相位验证直接读盘：

```
data/data/us_equity/1d/wrds_crsp_sp500_1d.zarr.crsp_filter_report.json
roster_overrides.permnos = {"75154": {"first": "2024-01-02", "last": "2024-12-31",
                                      "rows": 252, "symbol": "75154",
                                      "types": ["UG/EQTY/COM/CORP/N"]}}
```

`"symbol": "75154"` 与它自己的 JSON key 逐字节相同 —— 正是 UAT test 2 裁定要删掉的那个字段。
**操作者裁定（已锁定）：重新生成盘上产物。**

这与本相位在 plan 01 自己立的 W0 规矩同类：**盘上产物必须由当前 HEAD 的代码生成**，
否则测量对象与代码不是同一件东西。

**为什么排在最后一个 wave。** 这次重建会**删除并重转**主仓库里的真实 store。它与任何其它 plan
零并发不是可选项：`data/` 是 gitignored、在 worktree 里根本不存在，重建走的是主仓库那一棵树，
而前三个 wave 的测试全部只用 `tmp_path`。同时这也是操作者给出的处理顺序里的最后一条。
它与 03.11-17 之间**没有代码依赖**（`crsp_tickers.py` 是读侧、`crsp.py` 是写侧，两个 plan 都不碰后者）；
真正的代码依赖只有 03.11-14 的清缓存。

Purpose: 让盘上的审计产物与 HEAD 一致，并把这条一致性变成每一次未来重建都会复检的常设断言。
Output: `tests/test_crsp_rebuild_measurements.py` 里的两条新断言 + 一个新的备份代目录 +
一份重新生成的 `.crsp_filter_report.json`。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

@.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-VERIFICATION.md
@.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-01-PLAN.md
@.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-13-PLAN.md
@tests/test_crsp_rebuild_measurements.py
@quantlab/dataset/crsp_rebuild.py
</context>

<tasks>

<task type="tracer">
  <name>Task 1: 从 raw tier 到重生成的审计报告，把这条路整个走一遍</name>
  <files>tests/test_crsp_rebuild_measurements.py</files>
  <read_first>
    - `tests/test_crsp_rebuild_measurements.py` 全文（412 行）—— 特别是模块 docstring 说明它为什么被排除在回归门之外、`_data_root()`（`:133-177`，raw tier 缺失时 `pytest.fail` 的那段）、`_input_paths()`（`:180-190`，两个路径陷阱的字面量）、`rebuilt` fixture（`:226-250`，含「备份目录是**按重建代**的，这一点很关键」那段 docstring）、`EXPECTED_WRITTEN_SIDECARS`（`:114-119`）
    - `quantlab/dataset/crsp_rebuild.py:1-60` —— `CRSP_SIDECAR_SUFFIXES` 五项清场清单及其逐项理由，以及「五项必须在重建前全部删除，不只是 store」的论证
    - `quantlab/base/rebuild.py` 的 `rebuild()` —— assert → backup → clear → `_convert()` → `_measure()` 这个固定顺序，以及 `backup()` 用 `dirs_exist_ok=True` 这一点
    - `.planning/phases/03.11-.../03.11-VERIFICATION.md` 的 `gap_id: G-03.11-5` 一节（lines 151-171）—— 实测的盘上原文与两条 `missing:`
    - `quantlab/dataset/crsp.py:1226-1271` —— `_permno_breakdown` 的 docstring 与那个只剩四个键的字典字面量，以及 `:1099` / `:1116` 两个调用点
    - `tests/test_crsp_identity.py:583-595` —— 三处兄弟断言之一的写法，本 plan 的盘上断言向它看齐（`set(record) == {...}` + 逐字段断值）
  </read_first>
  <precondition>CRSP raw tier 在主仓库里在位：`data/downloads/us_equity/1d/wrds_crsp/wrds` 下有 132 个 `.pqt`，兄弟目录 `data/downloads/us_equity/1d/wrds_crsp/_reference` 下有 6 张参考表加 `manifest.json`（本 plan 规划时实测在位，2026-09-21）。缺任一项则 `_data_root()` 会 `pytest.fail` 并逐条列出缺的路径 —— 那是**停下来报告**，不是 skip，执行者据此中止并上报，不要自行造数据或改判据。</precondition>
  <reversibility rating="costly">重建会删除并重转主仓库里的真实 store。有备份因此可复原，但复原要靠备份目录选对：指向一个已有的旧代目录会就地覆盖一代用当前代码**无法再产出**的产物。本任务的第一件事就是给这次重建一个新的代目录。</reversibility>
  <action>
**第一步：取证（在任何删除动作之前）。** 读当前盘上的
`data/data/us_equity/1d/wrds_crsp_sp500_1d.zarr.crsp_filter_report.json`，把
`roster_overrides.permnos` 与 `dropped_permnos` 的原文抄进 SUMMARY —— 这是本 gap 的 RED 证据，
重建之后就再也取不到了。同时确认 `dropped_permnos` 当前是 `{}`（规划时实测），
它决定了 Task 2 里那一支只能如实记为「无从观察」。

**第二步：换一个新的备份代目录。** `rebuilt` fixture（`:247-249`）现在指向
`data/_backup_pre_03.11_10`。fixture 自己的 docstring（`:236-241`）就预言了这个坑：
`backup()` 用 `dirs_exist_ok=True` 复制，两次不同代的重建指向同一个目录会**就地覆盖**旧的那一份。
当前盘上这一代是 plan 01 重建出来、**早于 plan 13 删字段**的那一代 —— 它是唯一能展示 G-03.11-5 的产物，
而写出那个字段的代码已经改了，覆盖即不可复原。
把 fixture 的备份目录改成一个新的代名（沿用 `_10` 那一代的命名法，以本 plan 号为后缀），
并在 fixture docstring 里把这条规则从「这一次为什么换目录」升格成一条常规：
**每当即将被替换掉的那一代是当前代码无法再产出的，就换一个新的代名**；三代（相位前、03.11-10、本次）
因此并存，各自是各自那一代的唯一记录。

**第三步：加键集断言。** 新增一个测试函数，从 `measurement.store_path` 拼出
`.crsp_filter_report.json` 的路径读它，对 `roster_overrides["permnos"]` 做三层断言，
与 `tests/test_crsp_identity.py:583-595` 那三处兄弟同强：
先断记录数 ≥ 1（否则这个循环会空跑通过 —— 这正是 IN-02 那条弱断言的失败模式，不要在盘上复制一遍），
再对每条记录断 `set(record) == {"types", "rows", "first", "last"}`，
再对本 store 已知的那一条（PERMNO 75154）逐字段断值：`rows` 为 252、`first` 为 `2024-01-02`、
`last` 为 `2024-12-31`、`types` 非空。
断言失败消息里带上整个 `roster_overrides["permnos"]`，与兄弟们的 `, report[...]` 习惯一致。
顺带 `print` 出记录数 —— 这个文件的既有风格是把证据 print 出来让 SUMMARY 抄（`:260-266`）。

**第四步：跑一次门。** 用 plan 01 SUMMARY 里已经跑通过的那条命令（它解析出的是主仓库根，
不是 worktree；`data/` gitignored、在 worktree 里不存在，这个区分是这个门防假绿的核心）：
`QUANTLAB_DATA_ROOT="$(dirname "$(git rev-parse --path-format=absolute --git-common-dir)")" uv run pytest -q -s tests/test_crsp_rebuild_measurements.py -p no:cacheprovider`。
把 `-s` 打出来的七行测量证据（`data_root` / `store_path` / `dims` / `metrics` / `removed` /
`backup_path` / `data_vars`）加上 `symbol dtype`、`ticker intervals`、`vintage_product_end`、
`symbol_count` 逐字抄进 SUMMARY。
**只跑一次。** 第二次会把真实 store 再删再转一遍，而它买不到新证据 —— 幂等那一条按 backstop 记。
  </action>
  <acceptance_criteria>
    - 重建**之前**当前盘上 `roster_overrides.permnos["75154"]` 的原文已抄进 SUMMARY，且其中含 `symbol` 字段（RED 证据）
    - `grep -c 'backup_dir=' tests/test_crsp_rebuild_measurements.py` 返回 1（只有一个备份目标，没有分叉）
    - `-s` 输出的 `backup_path:` 那一行指向的目录，既不是 `data/_backup_pre_03.11` 也不是 `data/_backup_pre_03.11_10`（这是运行时观察，不是源码 grep —— fixture 的 docstring 理应点名前两代，所以不能用「源码里不许出现这个名字」当判据）
    - 重建后 `data/_backup_pre_03.11`、`data/_backup_pre_03.11_10` 两个目录仍在位，且它们的 mtime **早于**本次重建开始时刻
    - 重建后 `python3 -c "import json; d=json.load(open('data/data/us_equity/1d/wrds_crsp_sp500_1d.zarr.crsp_filter_report.json')); r=d['roster_overrides']['permnos']; assert len(r) >= 1; [print(k, sorted(v)) for k,v in r.items()]; assert all(set(v) == {'types','rows','first','last'} for v in r.values())"` 无断言失败
    - PERMNO 75154 那条记录：`rows == 252`、`first == "2024-01-02"`、`last == "2024-12-31"`、`types` 非空
    - `QUANTLAB_DATA_ROOT=... uv run pytest -q -s tests/test_crsp_rebuild_measurements.py -p no:cacheprovider` 全绿，用例数比改动前多 1
    - `grep -cE '^[[:space:]]*pytest\.skip\(' tests/test_crsp_rebuild_measurements.py` 返回 0，且 `grep -c 'pytest.mark.skipif' tests/test_crsp_rebuild_measurements.py` 返回 0（缺 raw tier 一律 fail 不 skip，这条既有性质未被本 plan 削弱）。判据取的是**调用形式**而不是裸 token：该文件的模块 docstring 与 `_data_root()` 的 docstring 各有一处散文提及那个动词（`:22`、`:136`），拿裸 token 计数会把它自己的说明文字判成违例
  </acceptance_criteria>
  <verify>
    <automated>QUANTLAB_DATA_ROOT="$(dirname "$(git rev-parse --path-format=absolute --git-common-dir)")" uv run pytest -q -s tests/test_crsp_rebuild_measurements.py -p no:cacheprovider</automated>
    <fails_when>退出码非 0；或输出里出现 `QUANTLAB_DATA_ROOT is not set` / `the CRSP raw tier is incomplete`（raw tier 不在位 —— 这时**中止并上报**，不要改判据）；或 passed 数不比改动前多 1</fails_when>
    <automated>python3 -c "import json,sys; d=json.load(open('data/data/us_equity/1d/wrds_crsp_sp500_1d.zarr.crsp_filter_report.json')); r=d['roster_overrides']['permnos']; sys.exit(0 if r and all(set(v)=={'types','rows','first','last'} for v in r.values()) else 1)"</automated>
    <fails_when>退出码 1（记录为空 —— 空跑通过；或任一条记录的键集不是那四个，例如 `symbol` 仍在盘上）；文件不存在则退出码 1 并带 `FileNotFoundError`，同样算失败</fails_when>
  </verify>
  <done>盘上 `.crsp_filter_report.json` 由当前 HEAD 重新生成，`roster_overrides.permnos` 每条记录的键集恰好四项；两个旧代备份目录原封未动；键集断言进了常设的真实数据门。</done>
</task>

<task type="auto">
  <name>Task 2: 把「另一支没被观察到」如实写下来，而不是让它空跑通过</name>
  <files>tests/test_crsp_rebuild_measurements.py</files>
  <read_first>
    - Task 1 刚加的那条键集断言 —— 本任务把同一套判据扩到 `dropped_permnos` 那一支
    - `quantlab/dataset/crsp.py:1099` 与 `:1116` —— 两个调用点共用同一个 `_permno_breakdown` 渲染，所以两支的记录形状 by construction 相同；这既是「另一支大概率也对」的理由，也正是「所以不能拿它当已验证」的理由
    - `.planning/phases/03.11-.../03.11-VERIFICATION.md:161-165` —— 「`dropped_permnos` 为 `{}`，所以另一支无从观察」这句原文
    - `.planning/phases/03.11-.../03.11-REVIEW.md` 的 IN-02（lines 347-368）—— 「一条 `{}` 也能满足的断言等于没有断言」这条失败模式的完整论证；本任务要防的是它在盘上的翻版
    - `tests/test_crsp_identity.py:1085-1103` —— 合成 store 上**确实有**被丢弃记录的那条用例（QQQ）；它是 `dropped_permnos` 分支真正被覆盖的地方，本任务的散文要指向它
  </read_first>
  <action>
把 Task 1 的判据抽成一个测试内的小 helper（接一个 `{PERMNO: record}` 字典，断每条记录的键集恰好四项），
`roster_overrides["permnos"]` 与 `dropped_permnos` 两支都调用它。

**关键在于两支的「非空」要求不同，且这个不同必须写在代码里而不是留给读者猜：**
- `roster_overrides["permnos"]`：本 store 上**必须非空**（实测 1 条，PERMNO 75154），先断 `len >= 1` 再逐条查。
- `dropped_permnos`：本 store 上实测为 `{}`。**不要**断它非空（那会把一个正确的重建判成失败），
  但也**不要**让一个空循环静默通过。写法是：`print` 出它的记录数，
  并在断言旁的注释里逐字写清楚 —— 这一支在这个 roster/窗口上恰好没有被丢弃的 PERMNO，
  所以这条盘上断言对它是**空载**的；它真正被覆盖的地方是 `tests/test_crsp_identity.py` 里那条合成 store 的 QQQ 用例
  （那里 `dropped_permnos` 确有记录），而两支共用同一个渲染函数是它们形状一致的结构性理由。
  这样下一个读者看到「0 条」时知道这是一次**测量**，不是一次通过。

在 SUMMARY 里同样如实写：本轮观察到的是 `roster_overrides` 那一支；`dropped_permnos` 那一支
在这个 store 上无从观察，未声称已验证。
  </action>
  <acceptance_criteria>
    - `grep -c 'dropped_permnos' tests/test_crsp_rebuild_measurements.py` 返回 ≥ 2（断言引用 + 说明注释）
    - `grep -c 'roster_overrides' tests/test_crsp_rebuild_measurements.py` 返回 ≥ 2
    - 文件里**没有**对 `dropped_permnos` 的非空断言：`grep -c 'assert dropped_permnos\|assert len(dropped' tests/test_crsp_rebuild_measurements.py` 返回 0
    - 两支共用同一个键集 helper：`grep -c 'def _assert_breakdown_shape\|def _assert_record_shape' tests/test_crsp_rebuild_measurements.py` 返回 1（名字由执行者取其一）
    - `-s` 输出里出现 `dropped_permnos` 的记录数（实测应为 0）
    - `QUANTLAB_DATA_ROOT=... uv run pytest -q -s tests/test_crsp_rebuild_measurements.py -p no:cacheprovider` 全绿
  </acceptance_criteria>
  <verify>
    <automated>QUANTLAB_DATA_ROOT="$(dirname "$(git rev-parse --path-format=absolute --git-common-dir)")" uv run pytest -q -s tests/test_crsp_rebuild_measurements.py -p no:cacheprovider</automated>
    <fails_when>退出码非 0；或 `-s` 输出里找不到 `dropped_permnos` 的记录数这一行（说明空载那一支又变回了静默通过）</fails_when>
  </verify>
  <done>两支共用同一个键集判据；`roster_overrides` 那一支断了非空、`dropped_permnos` 那一支把「0 条」打成一次可见的测量并指向它真正被覆盖的地方。</done>
</task>

<task type="auto">
  <name>Task 3: 相位收尾 —— 回归门 + 四个 suite + 逐条对账</name>
  <files>tests/test_crsp_rebuild_measurements.py</files>
  <read_first>
    - `.planning/config.json` 的 `workflow.test_command` —— 回归门的确切拼写（两个 `--ignore`，其中一个正是本 plan 改的这个文件）
    - `.planning/phases/03.11-.../deferred-items.md` 的 D-03.11-12-A / -B / -C —— 三条已立项、本轮一律不碰的前置问题
    - `.planning/phases/03.11-.../03.11-VERIFICATION.md` 的 `gaps:` 全块 —— 四个 gap_id 的每一条 `missing:`，逐条对账用
    - `tests/test_crsp_rebuild_measurements.py:18-45` —— 这个文件为什么被排除在回归门之外的两条理由；本 plan 的改动不得削弱其中任何一条
  </read_first>
  <action>
**回归门。** 跑 `.planning/config.json` 里 `workflow.test_command` 的确切命令。
判据与 03.11-14 Task 3 相同：FAILED 集合里每一条都是 `ingest_tiingo` / `ingest_binance_spot` 的
`ModuleNotFoundError` 或它的 `FileNotFoundError` 孪生，逐文件计数与 D-03.11-12-A 的表一致。
passed 数应当比 03.11-14 那次**更高**（本轮四个 plan 只加用例、不删用例），把差值逐 plan 拆开写进 SUMMARY。
若撞上 D-03.11-12-B 的 KunQuant teardown 间歇性挂起，按 deferred-items 的记录重跑一次即可，不要顺手去修。

**四个 gap-closure suite。** 在默认缓存目录下（03.11-14 之后不需要 `pycache_prefix`）跑
`uv run pytest -q tests/test_crsp_ticker_sidecar.py tests/test_model_predict_panel.py tests/test_crsp_identity.py tests/test_extensibility_contract.py`。

**逐条对账。** 在 SUMMARY 里做一张表，左列是 03.11-VERIFICATION.md 四个 gap_id 下的每一条 `missing:`
（G-03.11-3 四条、G-03.11-4 三条、G-03.11-5 两条、G-03.11-6 两条），
右列是「由哪个 plan 的哪个任务、哪条断言或哪次实跑兑现」。
**out-of-scope 的三项要在表里显式点名为未处理**，而不是从表里消失：
UAT test 4（`_compare_fingerprints` warning 风暴 backstop，仍 `blocked_by: other`，需要人跑一次 load 模式的真实回测）、
以及 deferred-items 的 D-03.11-12-A/B/C。
`G-03.11-5` 的第二条 `missing:`（「或：明确裁定接受盘上滞后并写进 deferred-items.md」）是**二选一的另一支**，
操作者已裁定走第一支（重生成），表里注明它按裁定不适用，不要两支都打勾。

**确认排除性质未被削弱。** 本 plan 改的正是那个被 `--ignore` 排除的文件。
复核它仍然满足排除的两条理由：`_data_root()` 在 `QUANTLAB_DATA_ROOT` 未设时仍 `pytest.fail`（不是 skip），
且 `rebuilt` fixture 仍是 module scope 的破坏性重建。两条只要有一条被削弱，这个门就会在
常规 `pytest` 里开始删真实 store 或永远变红。
  </action>
  <acceptance_criteria>
    - `grep -c 'pytest.fail' tests/test_crsp_rebuild_measurements.py` 返回 ≥ 3（`QUANTLAB_DATA_ROOT` 未设、不是目录、raw tier 不全，三条路径一条不少）
    - `grep -c 'scope="module"' tests/test_crsp_rebuild_measurements.py` 返回 1（`rebuilt` fixture 仍是 module scope）
    - `grep -c 'test_crsp_rebuild_measurements' .planning/config.json` 返回 1（回归门的 `--ignore` 仍点名它）
    - 回归门 FAILED 行集与 D-03.11-12-A 的逐文件表一致；passed 数高于 03.11-14 Task 3 记录的数字，差值已逐 plan 拆解
    - 四个 gap-closure suite 全绿
    - SUMMARY 里有那张「每条 `missing:` → 由谁兑现」的对账表，且 UAT test 4 与 D-03.11-12-A/B/C 在表里被显式标为未处理
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest -q --ignore=tests/test_factor_hierarchy.py --ignore=tests/test_crsp_rebuild_measurements.py</automated>
    <fails_when>FAILED 行集里出现任何一条不是 `ingest_tiingo` / `ingest_binance_spot` 导入失败的条目；或逐文件失败计数与 deferred-items.md D-03.11-12-A 的表不一致；或 passed 数低于 03.11-14 Task 3 记录的数字</fails_when>
    <automated>uv run pytest -q tests/test_crsp_ticker_sidecar.py tests/test_model_predict_panel.py tests/test_crsp_identity.py tests/test_extensibility_contract.py -p no:cacheprovider</automated>
    <fails_when>退出码非 0，或 passed 数少于 98</fails_when>
  </verify>
  <done>回归门无新增失败；四个 suite 全绿；四个 gap 的每一条 `missing:` 都有对应的兑现点，out-of-scope 的四项被显式标为未处理而不是消失。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 主仓库里的真实 store（gitignored）→ 一次破坏性重建 | `rebuild()` 会删掉 store 与它的五个 sidecar；git 救不了它，只有备份能 |
| raw tier / reference tier → 重建的输入 | 输入不全时若继续，产出的是一个「成功」但内容错误的面板 |
| 盘上审计产物 → 任何对它做的测量 | 产物若早于生成它的代码，测量对象与代码不是同一件东西（plan 01 的 W0 规矩） |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.11-64 | Denial of Service | `data/data/us_equity/1d/wrds_crsp_sp500_1d.zarr` 及其五个 sidecar | high | mitigate | `rebuild()` 固定的 assert → backup → clear → convert 顺序（raw tier 缺失时在任何删除之前抛 `FileNotFoundError`）+ Task 1 把备份指向一个**新的**代目录 + 一条断言旧代目录 mtime 早于本次重建 |
| T-03.11-65 | Tampering | `data/_backup_pre_03.11` / `data/_backup_pre_03.11_10` | high | mitigate | `backup()` 的 `dirs_exist_ok=True` 会就地覆盖；Task 1 换新代目录，并把「换代规则」升格成 fixture docstring 里的常规；验收标准逐条断旧目录未被触碰 |
| T-03.11-66 | Repudiation | 一次 skip 被读作 pass | medium | mitigate | `_data_root()` 三条失败路径一律 `pytest.fail`；验收标准断 `pytest.skip` / `skipif` 在本文件里计数为 0 |
| T-03.11-67 | Information Disclosure | `_data_root()` 的失败消息打印绝对路径 | low | accept | 与 `T-03.11-44` 同类裁定：本地单用户研究平台，路径正是「去哪里补 raw tier」这条补救的必要信息；本 plan 不改这些消息 |
| T-03.11-68 | Spoofing | 一个「空载通过」的断言伪装成一次验证 | medium | mitigate | Task 2：`roster_overrides` 那一支断非空，`dropped_permnos` 那一支把 0 条打成可见测量并指向它真正被覆盖的合成用例 |

（本 plan 无 npm/pip/cargo 安装动作，`pyproject.toml` 一字未动，故无供应链条目。重建全程离线 —— `quantlab/dataset/crsp_rebuild.py` 的模块 docstring 与 `quantlab/acquisition/registry.py:565-571` 逐字保证不碰凭证、不碰网络。）
</threat_model>

<verification>
1. 重建前的盘上原文（含那个冗余字段）已作为 RED 证据抄进 SUMMARY。
2. 重建后 `roster_overrides.permnos` 每条记录的键集恰好 `{types, rows, first, last}`；PERMNO 75154 的 `rows`/`first`/`last` 逐字段对上。
3. `data/_backup_pre_03.11` 与 `data/_backup_pre_03.11_10` 原封未动，本次备份写进新的代目录。
4. 既有四条真实数据断言、int64 轴断言、两条 sidecar 断言全部仍绿。
5. `dropped_permnos` 那一支的 0 条被打印成一次测量，且没有被声称为已验证。
6. 回归门无新增失败；四个 gap-closure suite 全绿。
7. 四个 gap 的每一条 `missing:` 在 SUMMARY 的对账表里各有归属；out-of-scope 四项显式标为未处理。
</verification>

<success_criteria>
G-03.11-5 关闭：盘上的审计产物由当前 HEAD 生成，与代码是同一件东西；
而这条一致性不再靠一次手工核对维持 —— 它进了那个每次重建都会跑的真实数据门，
并且在这个 store 上观察不到的那一支被如实标成「未观察」，不是被一个空循环签收。
</success_criteria>

## Artifacts this phase produces

| 符号 / 改动 | 位置 | 性质 |
|---|---|---|
| 键集断言的测试函数 | `tests/test_crsp_rebuild_measurements.py` | 新测试函数（名由执行者取，`test_` 前缀，SUMMARY 逐个列出） |
| 记录形状 helper（`_assert_breakdown_shape` 或同义名） | 同上 | 新的测试内私有 helper，两支共用 |
| 新的备份代目录常量 / 字面量 | 同上，`rebuilt` fixture 内 | 既有 fixture 的一处字面量变更 + docstring 升格为常规 |

**非源码的操作目标（按设计 gitignored，不进 `files_modified`）：**

| 路径 | 动作 |
|---|---|
| `data/data/us_equity/1d/wrds_crsp_sp500_1d.zarr` 及其五个 sidecar | 删除后由当前 HEAD 重新生成 |
| `data/_backup_pre_03.11_18`（新代目录，名字以本 plan 号为准） | 新建：被替换掉的那一代的唯一记录 |
| `data/_backup_pre_03.11`、`data/_backup_pre_03.11_10` | **只读**，必须原封未动 |
| `data/downloads/us_equity/1d/wrds_crsp/{wrds,_reference}` | **只读**，重建的输入 |

**本 plan 不新增、不修改任何生产符号** —— `quantlab/` 下零改动。