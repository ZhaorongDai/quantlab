---
phase: 03.11
plan: 13
type: execute
wave: 2
depends_on: ["03.11-12"]
files_modified:
  - tests/test_extensibility_contract.py
  - quantlab/dataset/crsp.py
  - tests/test_crsp_identity.py
  - tests/test_crsp_ticker_sidecar.py
  - example/wrds_crsp.md
autonomous: true
gap_closure: true
gap_ids: [G-03.11-1, G-03.11-2]
requirements: [DATA-03]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`quantlab/base/backtest.py` 在 `tests/test_extensibility_contract.py:CORE_LAYER_FILES` 里，核心层纯度门覆盖它（G-03.11-1）"
    - "`FORBIDDEN_SUBSTRINGS` 一字未改 —— 那个 CRSP ticker 查表类**没有**被加进去（加进去会立刻变红，那是被否决的选项 a）"
    - "`_permno_breakdown` 输出的每条记录只有 `types` / `rows` / `first` / `last`，`symbol` 字段不存在（G-03.11-2）"
    - "删除同时作用于两个调用点：`crsp.py:1116` 的 `dropped_permnos` 与 `crsp.py:1099` 的 `roster_overrides[\"permnos\"]` —— 它们共用同一个渲染函数"
    - "三处把退化输出锁死的断言全部解掉，并各补一条 `\"symbol\" not in ...` 的正向断言接管它们的位置"
    - "`example/wrds_crsp.md` 的两份字段清单不再点名 `symbol`；`tests/test_crsp_ticker_sidecar.py` 的散文不再引用一行已经不存在的代码，但保留原来的设计论点"
  artifacts:
    - tests/test_extensibility_contract.py
    - quantlab/dataset/crsp.py
    - tests/test_crsp_identity.py
  key_links:
    - "`_permno_breakdown` → `dropped_permnos`（crsp.py:1116）与 `roster_overrides[\"permnos\"]`（crsp.py:1099）两支同时生效"
    - "`CORE_LAYER_FILES` → `test_core_layer_purity_no_market_specific_logic` 的遍历 → 未来 market literal 泄漏进 `base/backtest.py` 时变红"
  prohibitions:
    - "不得把那个 CRSP ticker 查表类加进 `FORBIDDEN_SUBSTRINGS` —— 操作者在 03.11-UAT test 1 上明确否决了选项 (a)（重构 `base/backtest.py:15` 的具名 vendor import）"
    - "不得重构 `quantlab/base/backtest.py` 的 vendor import 本身 —— 裁定是 (b)：接受现状 + 把文件加进门以免继续恶化"
    - "不得用 `CrspTickerLookup` 或任何方式把 ticker 还原进过滤审计报告 —— 操作者明确否决了恢复 ticker；`.crsp_tickers.json` 按设计不含被丢弃的 PERMNO，查它必然为空（若将来改主意，取数源是 `self._symbology`）"
    - "删掉一条断言后必须补一条正向断言 —— 只删不补等于把这个 gap 变成没人守的状态"
    - "不得改 `tests/test_crsp_ticker_sidecar.py:187` 那句「`str(permno)` —— 和 `_permno_breakdown` 用的是同一个拼写」：JSON key 的拼写没变，那句仍然成立"
---

<!-- planner-discipline-allow: CrspTickerLookup, FORBIDDEN_SUBSTRINGS, 被拒绝的类型组合 -->

<objective>
收口两条 minor 裁定，都是「让台账与代码对上」，没有行为变更。

**G-03.11-1（门的覆盖面）。** `quantlab/base/backtest.py:15` 是
`from quantlab.dataset.crsp_tickers import CrspTickerLookup` —— 一个 `base/` 层里的具名 vendor 依赖。
同一个 phase 内 `base/factor.py:80-115` 面对同样的问题选了另一条路（由 dataset 声明常量、base 只读、零 vendor 类名），
一个 phase 里出现了两种模式。操作者裁定 **(b)**：接受这条依赖不重构，
但把文件加进 `CORE_LAYER_FILES`，挡住未来的 market-literal 泄漏。
**现场实测**：`FORBIDDEN_SUBSTRINGS` 当前是 `("SpotKlineDataset","StockDataset","crypto_spot","us_equity")`，
`base/backtest.py` 对这四项命中 0 —— 加进门**今天即为绿**。

**G-03.11-2（冗余字段）。** PERMNO 轴迁移（D-01）之后 `symbol` 列就是 PERMNO 本身，
于是 `_permno_breakdown` 里的 `pl.col("symbol").last()` 退化成逐字节重复 JSON key：
`{"75154": {"symbol": "75154", ...}}`。操作者裁定**删字段**（而不是还原 ticker，也不是保持现状）——
PERMNO(key) + `types` 已经回答了审计问题「丢得对不对」，人可读的 ticker 不是审计正确性的必需；
而保持现状最差，因为那个冗余字段已经被三条测试断言**锁死**了。

Purpose: 把一条掉在地上的交棒（`test_crsp_identity.py` 曾把还原 ticker 交给 plan 09 的 sidecar，
而 plan 09 的五个展示点不含 filter report）就地了结，而不是再交棒一次。
Output: 门多覆盖一个文件 + 一个冗余字段消失 + 三条锁死断言换成三条正向断言 + 四处散文同步。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-UAT.md
@.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-CONTEXT.md
</context>

## 现场测得的行号（UAT 原文有一处差一行，以这里为准）

| 位置 | 内容 |
|---|---|
| `tests/test_extensibility_contract.py:31-35` | `CORE_LAYER_FILES`（三项） |
| `tests/test_extensibility_contract.py:37-42` | `FORBIDDEN_SUBSTRINGS`（四项，**不动**） |
| `quantlab/dataset/crsp.py:1226-1258` | `_permno_breakdown` 全体 |
| `quantlab/dataset/crsp.py:1099` | 调用点 A：`roster_overrides["permnos"]` |
| `quantlab/dataset/crsp.py:1116` | 调用点 B：`dropped_permnos` |
| `tests/test_crsp_identity.py:563` | docstring「a line per dropped PERMNO with its last symbol」 |
| `tests/test_crsp_identity.py:586-588` | 交棒注释（说要等 plan 09 的 sidecar） |
| `tests/test_crsp_identity.py:589` | 锁死断言 1（UAT 写的 588 差一行，588 是注释末行） |
| `tests/test_crsp_identity.py:1097` | 锁死断言 2 |
| `tests/test_crsp_identity.py:1457` | docstring「per PERMNO the symbol, ...」 |
| `tests/test_crsp_identity.py:1474-1476` | 第二份交棒注释 |
| `tests/test_crsp_identity.py:1477` | **锁死断言 3** —— rescued 那一支。UAT 只点了 dropped 的两处，但它同时要求「fix plan 必须覆盖两个调用点」，这就是那句话在测试侧的落点 |
| `example/wrds_crsp.md:325` | `roster_overrides` 字段表的 `permnos` 行 |
| `example/wrds_crsp.md:862` | 同一份描述的第二处副本 |
| `tests/test_crsp_ticker_sidecar.py:10-12` 与 `:166` | 两处散文把 `pl.col("symbol").last()` 当反面教材引用 |

<tasks>

<task type="auto">
  <name>Task 1: G-03.11-1 —— 把 base/backtest.py 纳入核心层纯度门</name>
  <files>tests/test_extensibility_contract.py</files>
  <read_first>
    - `tests/test_extensibility_contract.py:1-42`（模块 docstring 第 5-8 行逐字点了「the three core layers
      (`base/factor.py`, `base/model.py`, `base/backend.py`)」；紧跟着是 `CORE_LAYER_FILES` 与
      `FORBIDDEN_SUBSTRINGS` 两个常量）
    - `tests/test_extensibility_contract.py:44-80`（`test_core_layer_purity_no_market_specific_logic` 的
      docstring 也逐字点了同样三个文件；函数体遍历 `CORE_LAYER_FILES`，并且**排除 `#` 开头的行**，
      所以一条提到被禁名字的注释不会误判成违规）
  </read_first>
  <action>
    1. `CORE_LAYER_FILES` 加一项 `"quantlab/base/backtest.py"`，放在 `"quantlab/base/backend.py"` 之后
       —— 元组顺序就是违规报告的顺序，按层级排读起来最顺。

    2. 模块 docstring 与测试 docstring 两处「the three core layers ...」同步成四个文件。
       两处都要改：只改一处会留下一份说三个、一份说四个的记录。

    3. `FORBIDDEN_SUBSTRINGS` **原样不动**。在该元组正上方加一条注释，记下为什么
       `base/backtest.py:15` 那个具名 vendor import 没有被加进这个集合：
       操作者在 `03.11-UAT.md` test 1 上裁定 **(b)** —— 接受 backtester 对 CRSP ticker 查表类的具名依赖、
       不在本 phase 重构（选项 (a) 被否决），把文件加进门只为挡住**未来**的 market-literal 泄漏。
       注释还要写明一条实测事实：这四个子串在 `base/backtest.py` 上今天命中 0，所以这次扩门不会变红。
       （注释行以 `#` 开头，门本身会跳过它，所以在注释里写出类名是安全的。）

    4. 不要顺手做别的：不重构 `quantlab/base/backtest.py`，不改 `base/factor.py` 的注入模式，
       不给门加豁免机制。这三件都是被否决或不在裁定范围内的。
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_extensibility_contract.py -p no:cacheprovider && OMP_NUM_THREADS=1 uv run python -c "import importlib.util as u; s=u.spec_from_file_location('ec','tests/test_extensibility_contract.py'); m=u.module_from_spec(s); s.loader.exec_module(m); assert 'quantlab/base/backtest.py' in m.CORE_LAYER_FILES, m.CORE_LAYER_FILES; assert len(m.FORBIDDEN_SUBSTRINGS) == 4, m.FORBIDDEN_SUBSTRINGS; assert 'CrspTickerLookup' not in m.FORBIDDEN_SUBSTRINGS, m.FORBIDDEN_SUBSTRINGS; print('gate covers backtest.py; forbidden set unchanged at 4 entries')"</automated>
  </verify>
  <done>
    纯度门遍历四个文件且仍然通过；`FORBIDDEN_SUBSTRINGS` 仍是四项、不含那个查表类名；
    两处 docstring 都说四个文件；`quantlab/base/backtest.py` 本身零改动。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: G-03.11-2 —— 删掉 _permno_breakdown 的 symbol 字段，解掉三处锁死断言</name>
  <files>quantlab/dataset/crsp.py, tests/test_crsp_identity.py</files>
  <read_first>
    - `quantlab/dataset/crsp.py:1226-1258`（`_permno_breakdown` 全体：docstring 首行的契约
      `{PERMNO: {symbol, types, rows, first, last}}`、agg 里的 `pl.col("symbol").last().alias("symbol")`、
      返回 dict 里那个 `"symbol": None if record["symbol"] is None else str(record["symbol"]),` 三元）
    - `quantlab/dataset/crsp.py:1093-1116`（两个调用点：`:1099` 的 `roster_overrides["permnos"]` 与
      `:1116` 的 `dropped_permnos` —— 同一个渲染函数，删字段必然同时作用于两支）
    - `quantlab/dataset/crsp.py:1070-1078`（`_admitted_without_ticker` 上方那段提到
      `dropped_by_type` / `dropped_permnos` 的 docstring —— 读一下确认它不需要跟着改）
    - `tests/test_crsp_identity.py:556-593`（dropped 那一支的完整测试）
    - `tests/test_crsp_identity.py:1088-1098`（第二处 dropped 断言）
    - `tests/test_crsp_identity.py:1453-1487`（rescued 那一支的完整测试）
  </read_first>
  <behavior>
    - `report["dropped_permnos"][PERMNO]` 的键集合恰好是 `{"types", "rows", "first", "last"}`
    - `report["roster_overrides"]["permnos"][PERMNO]` 的键集合同上
    - JSON key 仍然是 `str(permno)`，`types` / `rows` / `first` / `last` 四个值一字不变
    - 既有的两条结构断言仍然成立：`rows_total == rows_kept + rows_dropped`；
      被豁免的 PERMNO 不出现在 `dropped_permnos` 里
    - `tests/test_crsp_dataset.py:1858` 的 `report["dropped_permnos"] == {}` 不受影响
  </behavior>
  <action>
    1. `quantlab/dataset/crsp.py` 的 `_permno_breakdown`：
       - 从 `.agg(...)` 里删掉取最后一个 symbol 的那一行聚合表达式（它现在是这个字段的唯一消费者）
       - 从返回 dict 里删掉整个 `"symbol": ...` 三元
       - docstring 首行契约改成 `{PERMNO: {types, rows, first, last}}`
       - docstring 里补一段**为什么没有 symbol**：PERMNO 轴迁移（D-01）之后 `symbol` 列就是 PERMNO 本身，
         那个字段会逐字节重复 JSON key；操作者在 `03.11-UAT.md` test 2 上裁定删除而不是还原 ticker，
         理由是 key + `types` 已经回答了审计问题「丢得对不对」。
         再补一句给将来改主意的人：真要还原人可读 ticker，取数源是 `self._symbology`，**不是**
         `.crsp_tickers.json` —— 后者按设计只写面板自己的 PERMNO（`_build_ticker_intervals` 在
         `_apply_security_filter` **之后**调用），被丢掉的那些查它必然为空。
         这一句是防止下一个人再走一遍已经被现场否掉的那条弯路。

    2. `tests/test_crsp_identity.py` 三处，每处都是「删断言 + 补正向断言 + 改上方注释/docstring」：
       - `:589` 删掉 `assert qqq["symbol"] == QQQ_PERMNO_TEXT, qqq`，
         换成 `assert "symbol" not in qqq, qqq`；
         `:586-588` 的注释改写成「这个字段已在 G-03.11-2 删除，PERMNO 就是 JSON key」，
         **不要**保留「等 plan 09 的 ticker sidecar 来还原」的说法 —— 那次交棒掉在地上，正是这个 gap 的成因，
         照抄它等于把同一个坑再挖一次；
         `:563` 的 docstring「a line per dropped PERMNO with its last symbol」改成按 PERMNO 键 + 类型组合的说法。
       - `:1097` 删掉那条断言，换成 `assert "symbol" not in report["dropped_permnos"][QQQ_PERMNO_TEXT]`。
       - `:1477` 删掉 `assert rescued["symbol"] == ROSTER_PERMNO, rescued`，
         换成 `assert "symbol" not in rescued, rescued`；
         `:1474-1476` 的注释同样改写；`:1457` docstring 里的「per PERMNO the symbol, the rejected type
         combination and the date range」把 symbol 去掉。
         **这一处是 rescued 分支**，UAT 只点名了 dropped 的两处，但它同时要求覆盖两个调用点。

    3. 在两支里各加一条键集合断言（`set(qqq) == {"types", "rows", "first", "last"}` /
       `set(rescued) == {...}`），让「这个字段回来了」在未来是一次红，而不是没人注意。
  </action>
  <verify>
    <automated>uv run pytest -q tests/test_crsp_identity.py tests/test_crsp_dataset.py -p no:cacheprovider</automated>
  </verify>
  <done>
    两个调用点产出的每条记录键集合都是 `{"types","rows","first","last"}`；三条锁死断言已由三条
    `"symbol" not in ...` 正向断言接管；`tests/test_crsp_identity.py` 与 `tests/test_crsp_dataset.py` 全绿。
  </done>
</task>

<task type="auto">
  <name>Task 3: G-03.11-2 的四处散文同步 + 全量回归</name>
  <files>example/wrds_crsp.md, tests/test_crsp_ticker_sidecar.py</files>
  <read_first>
    - `example/wrds_crsp.md:318-331`（`roster_overrides` 的三行字段表；`:325` 的 `permnos` 行、
      `:329` 那句「被豁免的行不出现在 `dropped_permnos` 里」）
    - `example/wrds_crsp.md:858-866`（同一份字段描述的第二处副本，在 `roster_overrides` 的叙述段里）
    - `tests/test_crsp_ticker_sidecar.py:5-20`（模块 docstring：把 `_permno_breakdown` 的
      last-symbol 聚合当作「刻意不抄的形状」来立论）
    - `tests/test_crsp_ticker_sidecar.py:162-170`（`test_permno_13407_is_two_intervals_fb_then_meta`
      的 docstring：同一个反面引用的第二处）
    - `tests/test_crsp_ticker_sidecar.py:184-188`（`str(permno)` 拼写那句 —— **不要动它**，
      JSON key 的拼写没变，那句仍然成立）
  </read_first>
  <action>
    1. `example/wrds_crsp.md:325` 与 `:862` 两处字段清单：把 `symbol` 从清单里去掉。
       两处都保留「被拒绝的类型组合、行数、首末日期」这三项，并在其中一处点明 JSON key 本身就是 PERMNO
       （这是这次删字段之后审计链上「是谁」的唯一答案）。
       `:329` 那句「被豁免的行不出现在 `dropped_permnos` 里（它没被丢）」本身仍然正确，**不要动**。

    2. `tests/test_crsp_ticker_sidecar.py` 的两处散文：它们引用的那一行聚合代码已经在 Task 2 里被删掉了。
       改写成不再引用一行不存在的代码，但**保留原来的设计论点**：一张「每个 PERMNO 最后一个 ticker」
       的映射会把 13407 的 2012 年答成 META，那正是 D-03 否决 1-D `ticker(symbol)` coord 的同一个缺陷，
       所以旁车是区间表。论点本身仍然成立，只是不能再拿一行已删代码当例子。

    3. 跑全量回归，确认 Task 2 的字段删除没有波及别处（`example/` 里的 JSON 样例、
       `tests/test_crsp_dataset.py`、`tests/test_backtest_*`）。
  </action>
  <verify>
    <automated>uv run python -c "import pathlib; a=[l for l in pathlib.Path('example/wrds_crsp.md').read_text(encoding='utf-8').splitlines() if '被拒绝的类型组合' in l]; assert len(a)==2, a; bad=[l for l in a if 'symbol' in l]; assert not bad, bad; t=pathlib.Path('tests/test_crsp_ticker_sidecar.py').read_text(encoding='utf-8'); c=pathlib.Path('quantlab/dataset/crsp.py').read_text(encoding='utf-8'); lit=chr(34).join(['pl.col(', 'symbol', ').last()']); assert lit not in t, 'the sidecar test still cites a deleted aggregation'; assert lit not in c, 'the aggregation is still in crsp.py'; print('field lists and prose both synced')" && uv run pytest -q --ignore=tests/test_factor_hierarchy.py --ignore=tests/test_crsp_rebuild_measurements.py -p no:cacheprovider</automated>
  </verify>
  <done>
    `example/wrds_crsp.md` 的两份字段清单都不再点名 `symbol`（锚点「被拒绝的类型组合」仍然出现两次）；
    那行 last-symbol 聚合在 `crsp.py` 与 `tests/test_crsp_ticker_sidecar.py` 里都不再出现；
    `:187` 的 `str(permno)` 那句未被改动；全量 pytest 绿。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `crsp_filter_report.json` → 人 | 一份**审计**产物：宇宙缩小了多少、丢了谁、为什么。读者据此判断「丢得对不对」 |
| `CORE_LAYER_FILES` → 核心层纯度门 | 一份**清单**决定哪些文件被检查；不在清单里的文件其违规永远不会变红 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.11-45 | Tampering | `quantlab/base/backtest.py` 未来悄悄泄漏 market literal | medium | mitigate | Task 1 把它加进 `CORE_LAYER_FILES`；实测四个子串今天命中 0，所以这道门立即生效而不是欠着 |
| T-03.11-46 | Elevation of Privilege | 执行者「顺手」把那个查表类加进 `FORBIDDEN_SUBSTRINGS`，等于执行被否决的选项 (a) | high | mitigate | `prohibitions` 明令；Task 1 的 `<automated>` 用**值断言**（不是文本 grep）检查该集合仍是四项且不含那个类名 |
| T-03.11-47 | Repudiation | 只改 `dropped_permnos` 一支、漏掉 `roster_overrides["permnos"]`，审计报告两半不一致 | medium | mitigate | 两支共用同一个渲染函数（结构上不可能只改一支）；Task 2 额外要求 rescued 分支 `:1477` 的断言一起换，并各加一条键集合断言 |
| T-03.11-48 | Repudiation | 删掉断言而不补，gap 从「锁死了错的」变成「没人守」 | medium | mitigate | `prohibitions` 明令；Task 2 要求三处各补一条 `"symbol" not in ...` 与一条键集合断言 |
| T-03.11-49 | Information Disclosure | 有人为了「让报告好读」把 ticker 从 `.crsp_tickers.json` 查回来，得到一份全空的列 | low | mitigate | `prohibitions` 明令；`_permno_breakdown` 的 docstring 写明旁车按设计不含被丢弃的 PERMNO，正确取数源是 `self._symbology` |

**无包管理器安装任务** —— 本 plan 不新增任何 npm/pip/cargo 依赖，`T-{phase}-SC` 不适用。
</threat_model>

<verification>
- `uv run pytest -q tests/test_extensibility_contract.py tests/test_crsp_identity.py tests/test_crsp_dataset.py tests/test_crsp_ticker_sidecar.py -p no:cacheprovider` 全绿
- 值断言：`CORE_LAYER_FILES` 含 `quantlab/base/backtest.py`；`FORBIDDEN_SUBSTRINGS` 仍是四项且不含那个查表类名
- `git diff --stat quantlab/base/backtest.py` 为空（裁定 (b) 不重构）
- 全量 `uv run pytest -q --ignore=tests/test_factor_hierarchy.py --ignore=tests/test_crsp_rebuild_measurements.py` 绿
</verification>

<success_criteria>
- G-03.11-1 的两条 `missing[]` 全部落地：文件进门、被否决的那个子串没进禁止集
- G-03.11-2 的四条 `missing[]` 全部落地：字段删除、两个调用点都覆盖、三处断言解锁并被正向断言接管、文档同步
- 全程零行为变更：`quantlab/base/backtest.py` 未改，过滤逻辑未改，只有一个冗余字段消失
</success_criteria>

<output>
完成后写 `.planning/phases/03.11-crsp-permno-symbol-axis-migration-and-tiingo-era-dead-code-r/03.11-13-SUMMARY.md`
</output>
