---
phase: "03.9"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - quantlab/enums/data.py
  - quantlab/base/config.py
  - quantlab/acquisition/wrds_taq.py
  - quantlab/acquisition/registry.py
  - quantlab/dataset/nbbo_resample.py
  - quantlab/dataset/nbbo.py
  - quantlab/dataset/cleaning.py
  - tests/conftest.py
  - tests/wrds_fixtures.py
  - tests/test_nbbo_dataset.py
  - tests/test_source_registry.py
  - tests/test_acquisition_batching.py
autonomous: true
requirements: [D-01, D-02, D-04, D-06, D-08, D-11, D-12, D-14, D-17, D-18, D-19, D-20, D-22, D-26, D-27, D-28]

estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "One WRDS symbol-day travels the whole path offline: FakeWrdsSession -> registry.run(WRDS_SOURCE) -> raw parquet under .../wrds/data_type=nbbo/date=2024-01-24/symbol=AAPL/ -> registry.convert(WRDS_SOURCE, NbboDatasetConfig(bar_interval='1m')) -> a dense [timestamp, symbol] Zarr store (D-06, D-17)"
    - "Raw keeps EVERY record of the day, pre-market and after-hours included, one row per NBBO record, with taq_date/time_m/time_m_nano/sym_root/sym_suffix and the arrival ordinal wrds_row_ord (D-02, D-04, D-18, D-19)"
    - "The source table is taqm_{YYYY}.complete_nbbo_{YYYYMMDD} (D-01, D-18)"
    - "A 1m panel on 2024-01-24 has exactly 390 labels, first 14:31Z last 21:00Z: right-closed bars labelled at bar end, so a record exactly on an edge belongs to the bar that edge labels (D-22)"
    - "The panel carries exactly the 13 NBBO variables (bid, ask, bid_size, ask_size, mid, spread, spread_bps, imbalance, n_updates, tw_spread, tw_bid_size, tw_ask_size, n_ambiguous_ties), all float64 (D-11, D-12, D-19)"
    - "The bar frequency is NbboDatasetConfig.bar_interval validated against the new BarInterval literal; the locked Frequency literal is unchanged (D-08, D-26)"
    - "SourceDescriptor.config_factory is the classmethod WrdsTaqNbboAcquisition.build_config, not a quantlab/config function (D-27)"
    - "An autouse tripwire makes any psycopg2 connect attempt in the test suite raise, so walking every Acquisition subclass can never reach WRDS (D-28)"
    - "The full suite has no failing test id outside 03.9-BASELINE-FAILURES.txt after this plan (D-28)"
  artifacts:
    - path: "quantlab/acquisition/wrds_taq.py"
      provides: "USERNAME_ENV, WrdsSession, WrdsTaqNbboAcquisition, WRDS_SOURCE"
      contains: "complete_nbbo_"
    - path: "quantlab/dataset/nbbo_resample.py"
      provides: "NbboResampler: event records -> right-closed bar panel"
    - path: "quantlab/dataset/nbbo.py"
      provides: "NbboPanelDataset(StockDataset) with config_cls = NbboDatasetConfig"
    - path: "quantlab/dataset/cleaning.py"
      provides: "NBBO_PANEL_VARIABLES, clean_nbbo_panel"
    - path: "quantlab/enums/data.py"
      provides: "BarInterval, BAR_INTERVAL_SECONDS (Frequency untouched)"
    - path: "quantlab/base/config.py"
      provides: "NbboDatasetConfig(DatasetConfig)"
    - path: "tests/wrds_fixtures.py"
      provides: "FakeWrdsSession, taq_row, TAQ_COLUMNS_2018_ON, TAQ_COLUMNS_PRE_2018, L6_SAMPLE_ROWS"
    - path: "tests/test_nbbo_dataset.py"
      provides: "the tracer test"
  key_links:
    - from: "quantlab/acquisition/wrds_taq.py::WRDS_SOURCE"
      to: "quantlab/dataset/nbbo.py::NbboPanelDataset"
      via: "Capability(market='us_equity', frequency='tick', data_type='nbbo', dataset_cls=NbboPanelDataset) resolved by registry.convert"
    - from: "quantlab/acquisition/registry.py (module bottom)"
      to: "quantlab/acquisition/wrds_taq.py"
      via: "module-object import so a cold registry import enumerates wrds"
    - from: "quantlab/dataset/nbbo.py::_clean"
      to: "quantlab/dataset/cleaning.py::clean_nbbo_panel"
      via: "D-14 override; clean_market_data would raise on the missing OHLCV columns"
    - from: "tests/conftest.py autouse tripwire"
      to: "psycopg2.connect"
      via: "monkeypatch raising AssertionError in every test"
  prohibitions:
    - requirement_id: "D-03"
      category: "safety"
      status: "resolved"
      verification: "test"
      resolution: null
      reason: null
      statement: "MUST NOT aggregate, resample, sort or de-duplicate NBBO records on the WRDS server or in the raw tier; raw is one row per record in arrival order"
    - requirement_id: "D-28"
      category: "safety"
      status: "resolved"
      verification: "test"
      resolution: null
      reason: null
      statement: "MUST NOT let any automated test open a connection to WRDS (each connection can push a Duo prompt to the user's phone)"
---

<objective>
**This is the phase tracer.** Prove the whole architecture on ONE symbol-day before
anything is built sideways: a fake WRDS session serves AAPL's `complete_nbbo` rows
for 2024-01-24, the new vendor provider lands them as raw tick shards, the registry
converts them through a new Dataset class, and a 1m NBBO panel comes out of Zarr
with hand-computed values.

Purpose: every later plan (acquisition hardening, resampler semantics, calendar,
CLI) expands a path that already works end to end. If the layering is wrong, it
shows up here after one commit.

Output: `quantlab/acquisition/wrds_taq.py`, `quantlab/dataset/nbbo_resample.py`,
`quantlab/dataset/nbbo.py`, `clean_nbbo_panel`, `NbboDatasetConfig`, `BarInterval`,
the registry entry, the D-28 test tripwire, and the third vendor wired into the
existing registry and subclass-walk tests so the suite gains no new failure.
</objective>

<assumption_delta_decision>
The assumption-delta detector fired on "resampled locally at a configurable frequency"
(kind `chosen`: a derived value became a parameter).
- Primary noun: the ACQUISITION frequency stays `Frequency` ("tick" for raw NBBO); the
  PANEL bar size becomes a new noun, `BarInterval`, carried by `NbboDatasetConfig.bar_interval`.
- Decision: `add-alongside`, per locked D-26 (the D-08 option "separate panel frequency
  parameter"). Rationale: extending the locked `Frequency` literal would break every table
  keyed on it (RAW_HIVE_KEYS, HIVE_SCHEMA_BY_FREQUENCY, BARS_PER_DAY_BY_FREQUENCY, the
  ingest scripts' choices) for tokens only one Dataset understands.
- Accepted debt: when a second Dataset needs arbitrary intraday bar sizes, `BarInterval`
  should be promoted to a shared panel-interval concept instead of being copied.
- Invariant lock: Task 1's acceptance criteria print `typing.get_args(Frequency)` and
  require `('1d', '1m', 'tick')`.
</assumption_delta_decision>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-CONTEXT.md
@.planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-RESEARCH.md
@.planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-PATTERNS.md
@.planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-VALIDATION.md
@.planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-LIVE-CHECK-1.json
</context>

<environment_notes>
- Run every command from YOUR working-tree root (a worktree if isolated): `uv run pytest -q tests/<file>.py`. Never `cd` into /Users/daizhaorong/projects/quantlab — that is the unmodified main tree and reports a false green.
- A `-k` selector that matches nothing exits 5 with `N deselected`; treat exit 5 as failure.
- Never use `git stash` (user memory). Compare against old code with `git show HEAD:<path>` into a scratch copy.
- The phase gate is "no new failures": the failing-test ids of the full suite must be a subset of `.planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-BASELINE-FAILURES.txt` (55 ids, measured on 9d67af4). Command: `uv run pytest -q --ignore=tests/test_factor_hierarchy.py -p no:cacheprovider > /tmp/full.txt 2>&1; grep '^FAILED' /tmp/full.txt | sed 's/ - .*//' | sort > /tmp/now.txt; comm -13 .planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-BASELINE-FAILURES.txt /tmp/now.txt` — the output must be empty. Use the session scratchpad instead of /tmp if available.
- `uv run` inside a worktree builds its own `.venv`; that is expected.
- The WRDS facts below are LIVE-VERIFIED (03.9-LIVE-CHECK-1/2.json) and supersede RESEARCH.md: there is NO `qu_seqnum`; `time_m_nano` exists only from 2018-01-02; `taqmsec.*` are views, `taqm_{YYYY}.*` are base tables.
</environment_notes>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "one WRDS symbol-day becomes a 1m NBBO panel" — one path only</name>

  <reversibility rating="costly">Implements D-01/D-18 (table choice) and D-19 (wrds_row_ord in the raw schema): both are part of the on-disk raw layout, so changing either later forces a re-pull. Already decided with the user in CONTEXT.md, so no checkpoint:decision is inserted.</reversibility>

  <files>quantlab/enums/data.py, quantlab/base/config.py, quantlab/acquisition/wrds_taq.py, quantlab/acquisition/registry.py, quantlab/dataset/nbbo_resample.py, quantlab/dataset/nbbo.py, quantlab/dataset/cleaning.py, tests/conftest.py, tests/wrds_fixtures.py, tests/test_nbbo_dataset.py</files>

  <read_first>
    - quantlab/acquisition/alpaca.py — whole file: the module-level credential constants (:17-31), the transport class shape (:67-137), class constants (:237-296), `_data_type` (:564-590), the RAW_COLUMNS/RAW_SCHEMA properties (:592-605), `_fetch_page` (:699-891) and the registry block (:894-964). This is the analog.
    - quantlab/base/acquisition.py lines 425-600 (class constants, `_knob`), 748-770 (`_scrub`), 857-1116 (symbol validation, `_session_date`, `_hive_key_expr`, `_write_shard`), 1180-1300 (`_fetch_batch` page loop), 2221-2247 (`_fetch_page` contract)
    - quantlab/acquisition/registry.py lines 55-155 (Capability, SourceDescriptor) and 590-622 (vendor imports at the bottom)
    - quantlab/dataset/stock.py — whole file (the base class NbboPanelDataset extends; `_scan_root`, `_assert_single_vendor_and_drop`, `_scanned_hive_schema`, `has_raw_data`)
    - quantlab/base/data.py lines 312-360 (`from_raw_data`, `_raw_axes_in_range`, `_raw_data_to_xr_window`), 358-640 (`from_raw_data_chunked`), 865-873 (`_widen_fill_values`), 1084-1111 (`_pin_append_dtypes`, `_clean`, `config_cls`)
    - quantlab/dataset/cleaning.py lines 1-40 and 297-353 (`clean_membership_panel`, the precedent for `clean_nbbo_panel`)
    - quantlab/base/constituent.py lines 118-125 (the `_clean` override precedent)
    - quantlab/base/config.py lines 1-53; quantlab/enums/data.py lines 24-28 and 87-139
    - quantlab/config/__init__.py lines 18-98 (`get_data_root`, the downloads/data path convention) — read only; add NOTHING to this module (user memory: no config factories)
    - tests/conftest.py lines 1-160 (import-time rules, `_CREDENTIAL_ENV_NAMES`) and 1036-1128 (`mock_alpaca_client`, the patch-by-dotted-string pattern with the `find_spec` guard)
    - tests/test_acquisition_batching.py lines 330-457 (the subclass walk and the placeholder-token scan: no raise in a concrete subclass may contain the words "not implemented" / "not finished")
    - .planning/phases/03.9-.../03.9-RESEARCH.md § Pattern 4 (resampler algorithm; replace every qu_seqnum with wrds_row_ord) and § Pitfall 8 (join_asof sortedness)
    - .planning/phases/03.9-.../03.9-LIVE-CHECK-1.json keys L2_columns and L6_copy (real column names, order, types, real CSV sample rows)
  </read_first>

  <behavior>
    Tracer test `tests/test_nbbo_dataset.py::test_tracer_one_wrds_symbol_day_lands_raw_and_resamples_to_a_zarr_panel`, written FIRST and seen failing (ImportError / missing module) before the implementation. FakeWrdsSession serves ONE trading day, 2024-01-24 (EST, UTC-5), for AAPL, in this physical order (time_m ET, time_m_nano, bid x size, ask x size):
    - r0 04:00:00.005984, 226, 180 x 100, ask NULL (the real L6 row — pre-market, one-sided)
    - r1 09:29:59.500000, 0, 194.00 x 200, 194.02 x 300 (the seed: last record at or before the 09:30 open)
    - r2 09:30:30.000000, 0, 194.01 x 100, 194.04 x 100
    - r3 09:31:00.000000, 0, 194.02 x 100, 194.03 x 200 (exactly on the 09:31 edge)
    - r4 09:32:15.000000, 500, 194.00 x 400, 194.04 x 100
    - r5 16:00:00.000000, 0, 193.90 x 100, 193.95 x 100 (exactly on the close)
    - r6 16:05:00.000000, 0, 193.80 x 100, 193.85 x 100 (after-hours)
    Expected:
    - Raw: exactly one shard directory `.../wrds/data_type=nbbo/date=2024-01-24/symbol=AAPL/` holding 7 rows (r0..r6 — full day, nothing trimmed); `wrds_row_ord` == [0..6] in that order; r4's `timestamp` == 2024-01-24T14:32:15.000000500 (naive UTC, ns); r0's `best_ask` is null (not 0); the parquet column set equals RAW_COLUMNS minus `symbol` (the hive key carries it); a watermark exists at `.../_watermarks/wrds/nbbo/AAPL.json`.
    - Panel: 390 timestamps, first 2024-01-24T14:31:00, last 2024-01-24T21:00:00; symbol coordinate == ["AAPL"]; the data-variable set equals NBBO_PANEL_VARIABLES; every variable float64.
    - Bar 14:31Z (09:31 ET): n_updates 2 (r2, r3); bid 194.02, ask 194.03, bid_size 100, ask_size 200, mid 194.025, spread 0.01, spread_bps 1e4*0.01/194.025, imbalance -1/3; tw_spread 0.025 (0.02 for 30 s then 0.03 for 30 s), tw_bid_size 150, tw_ask_size 200; n_ambiguous_ties 0.
    - Bar 14:32Z: n_updates 0 and the r3 state carried forward (tw_spread 0.01).
    - Bar 14:33Z: n_updates 1, snapshot r4 (spread 0.04, imbalance 0.6), tw_spread == (0.01*15.0000005 + 0.04*44.9999995)/60 computed in the test from those durations.
    - Bar 21:00Z: n_updates 1 and snapshot r5 (right-closed at the close); r6 contributes to no bar.
    Compare floats with pytest.approx(abs=1e-9).
  </behavior>

  <action>
Build the thinnest production-quality path, in this order. No filtering, no calendar
and no hardening beyond what the one path needs — plans 04/05/06 expand it.

1. **Test safety FIRST (D-28), in `tests/conftest.py`**, before any WRDS class exists:
   - Add an autouse fixture `_forbid_wrds_network(monkeypatch, tmp_path)` that imports `psycopg2` inside its body and monkeypatches BOTH `psycopg2.connect` and `psycopg2._connect` with a function raising `AssertionError("a test tried to open a WRDS/PostgreSQL connection")`. When `importlib.util.find_spec("wrds")` finds the package, also patch `wrds.Connection` and `wrds.sql.Connection` the same way (defence in depth; the provider never uses them). Set `PGPASSFILE` to `tmp_path / "no-such-pgpass"` so no test can read the developer's real `~/.pgpass`.
   - Add an autouse fixture that, after each test, calls `WrdsSession.close_shared()` when `"quantlab.acquisition.wrds_taq"` is already in `sys.modules` (never import it just to clean up), so a shared session never leaks between tests.
   - Append `"WRDS_USERNAME"` to `_CREDENTIAL_ENV_NAMES`.
   - Add a `mock_wrds_session` fixture mirroring `mock_alpaca_client`: import `FakeWrdsSession` from `tests.wrds_fixtures` inside the body, call `FakeWrdsSession.reset()`, patch `quantlab.acquisition.wrds_taq.WrdsSession` by dotted string behind the same `importlib.util.find_spec` guard, set `WRDS_USERNAME` to `test-wrds-user-not-real`, return the class. Keep conftest free of module-scope `quantlab.acquisition.*` imports (its docstring rule).

2. **`tests/wrds_fixtures.py` (new)**: constants `TAQ_COLUMNS_2018_ON = ("date","time_m","time_m_nano","sym_root","sym_suffix","qu_cond","natbbo_ind","qu_source","nbbo_qu_cond","best_bid","best_bidsizeshares","best_ask","best_asksizeshares")` and `TAQ_COLUMNS_PRE_2018` (the same without `time_m_nano`) — exactly LIVE-CHECK-1 L2's names and order; `L6_SAMPLE_ROWS` = the five real L6 CSV rows as dicts. A helper `taq_row(time_m, bid, bid_size, ask, ask_size, *, nano=None, day="2024-01-24", root="AAPL", suffix=None, qu_cond="R", natbbo_ind="4", qu_source="N", nbbo_qu_cond=None) -> dict[str, str | None]` producing the strings COPY CSV would carry (None for NULL). `FakeWrdsSession` with class-level state reset by `reset()`: `trading_days_by_year: dict[int, list[date]]`, `rows: dict[tuple[date, str], list[dict]]` keyed by (day, universe symbol) in PHYSICAL order, `copy_calls: list[dict]`, `connections: int`, `instance`. Methods mirroring the real session's public surface: classmethod `shared()` (reads `WRDS_USERNAME`, raises `RuntimeError` naming it when missing, returns one instance), classmethod `close_shared()`, `trading_days(year) -> list[date]`, `table_columns(day) -> tuple[str, ...]` (2018+ vs pre-2018 by date), `copy_nbbo_csv(day, pairs, columns) -> bytes` (records the call; emits a CSV with a header of exactly `columns`, rows for the requested (root, suffix) pairs in stored order, NULL as empty field — build it with polars `write_csv` over an all-String frame). `reset()` loads a default two-day (2024-01-24, 2024-01-25) AAPL + MSFT data set of a few valid two-sided rows each, so generic walks have data.

3. **`quantlab/enums/data.py`**: beside `Frequency` (which stays byte-identical, D-26) add `BarInterval = Literal["1s","5s","10s","15s","30s","1m","5m","10m","15m","30m"]` and `BAR_INTERVAL_SECONDS: dict[str, int]` with the matching seconds. The token set is chosen so every token divides both a 390-minute regular session and a 210-minute half day; say so in the comment, and that this is the D-08 "separate panel frequency parameter" option per D-26.

4. **`quantlab/base/config.py`**: `@dataclass(kw_only=True) class NbboDatasetConfig(DatasetConfig)` re-declaring defaults `market: Market = "us_equity"`, `frequency: Frequency = "tick"`, `vendor: Vendor | None = "wrds"`, plus `bar_interval: BarInterval = "1m"`, `session_start: str = "09:30"`, `session_end: str = "16:00"` (ET wall clock, D-09 defaults). No other fields in this plan (plan 05 adds the filter knobs).

5. **`quantlab/dataset/cleaning.py`**: `NBBO_PANEL_VARIABLES = ("bid","ask","bid_size","ask_size","mid","spread","spread_bps","imbalance","n_updates","tw_spread","tw_bid_size","tw_ask_size","n_ambiguous_ties")` and `clean_nbbo_panel(data)` modelled line for line on `clean_membership_panel`: exact variable set, every variable float64 with dims ("timestamp","symbol"), strictly increasing timestamp via elementwise compare, returns the data unchanged; raises `ValueError` naming the violation. No `anomaly_flag`, no OHLCV check, no filling of any kind (per D-14; carry-forward is resampler semantics, never cleaning). Keep the module import-free of quantlab.

6. **`quantlab/acquisition/wrds_taq.py` (new)** — module docstring stating the D-18/D-19/D-20 facts. The module must not import the `wrds` package at all.
   - `USERNAME_ENV = "WRDS_USERNAME"` at module level (the alpaca rule: a security control must not live on a patchable class).
   - `class WrdsSession`: class attrs `HOST = "wrds-pgdata.wharton.upenn.edu"`, `PORT = 9737`, `DBNAME = "wrds"`, `SSLMODE = "require"`, `APPLICATION_NAME = "quantlab-wrds-taq"`, `CONNECT_TIMEOUT_SECONDS = 30`; a process-level cache `_shared: dict[str, WrdsSession]`. `__init__(self, username)` makes NO network call. Classmethod `shared()` reads `os.environ.get(USERNAME_ENV)` on every call, raises `RuntimeError` naming `WRDS_USERNAME` (and saying the password is read by libpq from ~/.pgpass, never from config) when empty, else returns the cached instance for that username. Classmethod `close_shared()` closes and clears all. `_connection()` connects lazily ONCE via `psycopg2.connect(host=HOST, port=PORT, dbname=DBNAME, user=username, sslmode=SSLMODE, application_name=..., connect_timeout=...)` — no password argument, host never read from config or env — then `set_session(readonly=True, autocommit=True)` (D-20). Call it through the module attribute `psycopg2.connect` so the tripwire can see it. Static pure builders using `psycopg2.sql`: `table_identifier(day) -> sql.Identifier(f"taqm_{day:%Y}", f"complete_nbbo_{day:%Y%m%d}")`, `where_clause(pairs) -> sql.Composed` = `sym_root = ANY(<Literal list of roots>) AND (sym_root, coalesce(sym_suffix, '')) IN (<Literal pairs>)`, `copy_query(day, pairs, columns)` = `COPY (SELECT <Identifiers> FROM <table> WHERE <where>) TO STDOUT WITH (FORMAT csv, HEADER true)`. The SELECT carries no ordering, grouping or de-duplication clause and no time predicate (D-03/D-04/D-19). Methods `trading_days(year)` (information_schema.tables, `table_schema = %s AND table_name ~ '^complete_nbbo_[0-9]{8}$'`, parse and sort locally), `table_columns(day)` (information_schema.columns, keep server order via `ordinal_position` sorted LOCALLY), `copy_nbbo_csv(day, pairs, columns) -> bytes` (`cursor.copy_expert` into a `tempfile.SpooledTemporaryFile`). The real network methods are exercised only in plan 04's fake-connection tests and the plan-08 live smoke.
   - `class WrdsTaqNbboAcquisition(Acquisition)`: `VENDOR = "wrds"`, `SESSION_TIME_ZONE = "America/New_York"`, `TICK_DATA_TYPES = ("nbbo",)`, `DEFAULT_BATCH_SIZE = 25`, `DEFAULT_MAX_WORKERS = 1`, `CREDENTIAL_ENV_VARS = (USERNAME_ENV,)`, `REDACTION = "<WRDS CREDENTIAL REDACTED>"`, `SCHEMA_PATTERN = "taqm_{year}"`, `TABLE_PATTERN = "complete_nbbo_{ymd}"`, `SUFFIX_DELIMITER = "."`, `DEFAULT_SUBDIR = "wrds_taq"`, `RAW_COLUMNS = ("timestamp","symbol","vendor","taq_date","time_m","time_m_nano","sym_root","sym_suffix","qu_cond","natbbo_ind","qu_source","nbbo_qu_cond","best_bid","best_bidsizeshares","best_ask","best_asksizeshares","wrds_row_ord")` and `RAW_SCHEMA` (timestamp `pl.Datetime("ns")`, taq_date Date, time_m Time, time_m_nano Int16, the two sizes and wrds_row_ord Int64, the two prices Float64, every code and symbol column String). TAQ's `date` column is renamed `taq_date` because `_write_shard` overwrites and drops a raw column named like a hive key.
   - `__init__`: `super().__init__(config)`; resolve `_data_type` eagerly (frequency must be "tick" and `kwargs["data_type"] == "nbbo"`, else `ValueError`, no default guessing — copy alpaca's message style); refuse a `max_workers` knob other than 1 with `ValueError` (one shared connection; every extra connection can push Duo, D-20); then `self._session = WrdsSession.shared()` (module-global lookup at call time so the fixture patch works).
   - Static `symbol_to_pair("BRK.B") -> ("BRK", "B")`, `symbol_to_pair("AAPL") -> ("AAPL", None)`, `pair_to_symbol(root, suffix)` (empty or None suffix -> root) — the D-15 notation; plan 04 adds the refusal of hyphenated symbols.
   - `_trading_days(start_date, end_date)`: union of `self._session.trading_days(year)` over the years of the window, filtered to the window, ascending, cached per window. Never probe calendar days for missing tables.
   - `_fetch_page(symbols, start_date, end_date, page_token)`: `_validate_symbols`; days = `_trading_days`; none -> `(empty RAW_SCHEMA frame, None)`; `day = date.fromisoformat(page_token) if page_token else days[0]` and refuse a token not in `days`; columns = the TAQ columns `self._session.table_columns(day)` reports; `raw = self._session.copy_nbbo_csv(day, pairs, columns)`; parse with `pl.read_csv(..., infer_schema=False)` (every field String, empty field -> null), add `wrds_row_ord = pl.int_range(pl.len())` BEFORE any other operation (arrival order within this (day, batch) query, D-19); cast; `time_m` via `str.to_time` accepting values with and without a fractional part; add a typed-null `time_m_nano` when the table lacks it; `timestamp` = `taq_date` combined with `time_m` plus `time_m_nano` ns, localized `America/New_York` (ambiguous/non_existent "raise"), converted to UTC, zone dropped; `symbol` = `pair_to_symbol`; `vendor` = "wrds"; next token = the next day's ISO date or None. No sort, no dedup, no filter of any record (D-02/D-04/D-05).
   - Classmethod `build_config(symbols, start_date=None, end_date=None, kwargs=None, subdir=DEFAULT_SUBDIR) -> AcquisitionConfig` (D-27): downloads root = `get_data_root() / "downloads" / "us_equity" / "tick" / subdir`; `raw_data_dir_path` terminates at `wrds`, `watermark_path` = sibling `_watermarks/wrds`; kwargs merged with `{"data_type": "nbbo"}`, refusing a conflicting data_type. Constructs `AcquisitionConfig` directly.
   - `WRDS_SOURCE = register_source(SourceDescriptor(vendor="wrds", display_name="WRDS NYSE TAQ millisecond NBBO", acquisition_cls=WrdsTaqNbboAcquisition, config_factory=WrdsTaqNbboAcquisition.build_config, capabilities=(Capability(market="us_equity", frequency="tick", data_type="nbbo", dataset_cls=NbboPanelDataset, earliest_available="2003-09-10", entitlement="WRDS NYSE TAQ millisecond subscription"),), required_env=("WRDS_USERNAME",), universe_categories=("sp500_constituent", "nasdaq100_constituent")))` — `required_env` restated as a literal (D-17).
   - Keep every raise message free of the words the placeholder scan rejects (see read_first).

7. **`quantlab/acquisition/registry.py`**: append `from quantlab.acquisition import wrds_taq as _wrds_taq  # noqa: E402,F401` after the two existing vendor imports (module-object form, per the comment there).

8. **`quantlab/dataset/nbbo_resample.py` (new)**: `class NbboResampler` with `__init__(self, bar_interval)` (validates against `BAR_INTERVAL_SECONDS`) and `resample(records: pl.DataFrame, sessions: pl.DataFrame) -> pl.DataFrame`. `records` columns: symbol, date (session date), timestamp, wrds_row_ord, best_bid, best_bidsizeshares, best_ask, best_asksizeshares. `sessions` columns: date, open, close (naive UTC). Per (symbol, date), never across dates: (a) STABLE sort by (symbol, date, timestamp, wrds_row_ord) — the D-19 total order; (b) `n_ambiguous_ties` per record = size of its (symbol, date, timestamp) group when that group holds more than one distinct (bid, bid size, ask, ask size) state, else 0; (c) keep records at or before close; the seed = last record at or before open, moved to open; drop earlier records; (d) per-bar `n_updates` and `n_ambiguous_ties` = counts of records with open < timestamp <= label and timestamp > previous label; (e) tie collapse `unique(subset=[symbol, date, timestamp], keep="last", maintain_order=True)`; (f) durations to the next record (last: close), exclusive cumulative integrals of spread, bid size, ask size and time; (g) grid edges open + k*interval, labels = edges 1..N (right-closed, labelled at bar end, D-22), N = (close-open)/interval, raising `ValueError` when not an integer; (h) sort BOTH sides by (symbol, date, time) immediately before `join_asof(strategy="backward", by=["symbol","date"])` (Pitfall 8); snapshot = matched record; TW = difference of integrals / difference of covered time; derived `mid`, `spread`, `spread_bps = 1e4*spread/mid`, `imbalance = (bid_size-ask_size)/(bid_size+ask_size)`. Bars with no record at or before the label are null for every state and TW variable. Output long frame: symbol, date, timestamp (= label), the 13 NBBO_PANEL_VARIABLES, all Float64. Docstring: carry-forward here is state semantics (the prevailing NBBO stays in force), deliberately not a cleaning fill.

9. **`quantlab/dataset/nbbo.py` (new)**: `class NbboPanelDataset(StockDataset)` with `config_cls = NbboDatasetConfig`, `DATA_TYPE = "nbbo"`. Config setter (call super first): `TypeError` for a non-NbboDatasetConfig, `ValueError` for frequency != "tick", an unknown bar_interval, or a kwargs data_type other than "nbbo". Override `_tick_data_type` to return `DATA_TYPE`. `_session_dates()` = the `date=` directory names under `_scan_root()` inside [start_date, end_date] (no parquet read). `_session_bounds(dates)` = config session_start/session_end localized in America/New_York per date, converted to naive UTC (DST-correct; plan 06 replaces the body with the XNYS calendar). `_raw_axes_in_range()` = (sorted config.symbols or the `symbol=` directory names, the concatenated labels of all session dates). `_raw_data_to_xr_window(start, end, symbols)`: session dates whose labels intersect [start, end]; scan `str(root / "**" / "*.pqt")` with the inherited hive schema, filter `pl.col("date").is_in(dates)` (by SESSION DATE, never a timestamp window — the pre-open seed must survive), `_assert_single_vendor_and_drop`, restrict to `symbols`, resample, slice labels to [start, end], cross-join the full label grid with the symbol list and left-join so the result is dense, then build an `xr.Dataset` with dims (timestamp, symbol), `timestamp` datetime64[ns], `symbol` built as `np.array([str(s) ...], dtype=object)`; raise `ValueError` for an empty symbol list (empty-store todo). `_raw_data_to_xr()` = the window over the config range. `_clean` -> `clean_nbbo_panel` (D-14). `_widen_fill_values()` -> `{}`.

10. Write the tracer test (behavior block) using `mock_wrds_session`, `registry.run(WRDS_SOURCE, cfg)` with `cfg` from `WrdsTaqNbboAcquisition.build_config` after `quantlab.config.set_data_root(tmp_path)` (the autouse fixture resets it), then `registry.convert(WRDS_SOURCE, NbboDatasetConfig(...), data_type="nbbo", granularity="day")`, then read back with `NbboPanelDataset(config).read().get_xarray_dataset()`.
  </action>

  <verify>
    <automated>uv run pytest -q tests/test_nbbo_dataset.py</automated>
    <fails_when>non-zero exit, or the summary line does not report "1 passed" or more</fails_when>
    <automated>uv run pytest -q tests/test_acquisition_batching.py tests/test_registry_convert.py tests/test_cleaning.py tests/test_stock_dataset.py</automated>
    <fails_when>non-zero exit, or any failed/errored test in the summary line; a failure containing "a test tried to open a WRDS/PostgreSQL connection" means the tripwire caught a real connect and the fake was not installed</fails_when>
  </verify>

  <acceptance_criteria>
    - `uv run pytest -q tests/test_nbbo_dataset.py -k tracer` exits 0 with "1 passed"
    - `uv run python -c "import typing; from quantlab.enums.data import Frequency; print(typing.get_args(Frequency))"` prints `('1d', '1m', 'tick')`
    - `uv run python -c "from quantlab.acquisition.registry import DataSourceRegistry as R; d=R.get('wrds'); c=d.capabilities[0]; print(c.market, c.frequency, c.data_type, c.dataset_cls.__name__, d.required_env)"` prints `us_equity tick nbbo NbboPanelDataset ('WRDS_USERNAME',)`
    - `uv run python -c "import ast,sys; t=ast.parse(open('quantlab/acquisition/wrds_taq.py').read()); m=[n for n in ast.walk(t) if isinstance(n,(ast.Import,ast.ImportFrom)) and any((getattr(n,'module',None) or a.name).split('.')[0]=='wrds' for a in n.names)]; sys.exit(1 if m else 0)"` exits 0
    - `quantlab/dataset/nbbo.py` contains `config_cls = NbboDatasetConfig` and `clean_nbbo_panel`
    - `quantlab/acquisition/registry.py` contains `from quantlab.acquisition import wrds_taq as _wrds_taq`
    - `tests/conftest.py` contains `"WRDS_USERNAME"` inside `_CREDENTIAL_ENV_NAMES` and an autouse fixture patching `psycopg2.connect`
  </acceptance_criteria>

  <done>The tracer test is green: one symbol-day goes fake session -> raw shards -> registry.convert -> Zarr with the hand-computed values, the D-28 tripwire is live in every test, and Frequency is unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the third vendor into the existing registry and subclass-walk suites (no new failures)</name>

  <files>tests/conftest.py, tests/test_source_registry.py, tests/test_acquisition_batching.py</files>

  <read_first>
    - tests/conftest.py lines 1206-1260 (`acquisition_config`)
    - tests/test_source_registry.py lines 60-140 (layout + module-level constant pins), 290-335 (capability triples), 530-560 (sorted enumeration), 630-700 (`_CHILD_CREDENTIALS`, the cold-import child), 770-830 (`test_env_names_are_exactly_what_gates_construction` and `_no_network`), 940-974 (the last-in-file restore check)
    - tests/test_acquisition_batching.py lines 330-457
    - .planning/phases/03.9-.../03.9-BASELINE-FAILURES.txt
  </read_first>

  <action>
Make the suites that enumerate vendors know about `wrds`, by extending them, never by
weakening an assertion.

- `tests/conftest.py::acquisition_config`: make `frequency` and `kwargs` default to a sentinel resolved per vendor: for `vendor="wrds"` an unsupplied frequency becomes "tick" and unsupplied kwargs become `{"data_type": "nbbo"}`; every other vendor keeps "1d" / None exactly as today. Explicit arguments always win. Document why (a WRDS config is only valid as tick/nbbo) in the docstring.
- `tests/test_acquisition_batching.py::test_every_concrete_acquisition_subclass_reaches_the_vendor_via_fetch_batch`: add the `mock_wrds_session` fixture parameter so the walk reaches the WRDS class through the fake. The walk and the placeholder scan otherwise stay unchanged; they must pick the new class up automatically.
- `tests/test_source_registry.py`: update the three literal enumerations to `["alpaca", "tiingo", "wrds"]` (the sorted-order test, the cold-import child's expected stdout `['alpaca', 'tiingo', 'wrds']`, the final restore check); add `"WRDS_USERNAME"` to `_CHILD_CREDENTIALS`; add `"wrds"` to the parametrization of `test_env_names_are_exactly_what_gates_construction` (its real guard runs under the socket tripwire: construction must succeed with the name set and raise `RuntimeError` matching `WRDS_USERNAME` without it); extend the module-level-constant pin test so `WrdsTaqNbboAcquisition.CREDENTIAL_ENV_VARS == (wrds_taq.USERNAME_ENV,)`; add `test_wrds_descriptor_serves_exactly_the_nbbo_capability` asserting the triple set `{("us_equity","tick","nbbo")}`, `dataset_cls is NbboPanelDataset`, `config_factory == WrdsTaqNbboAcquisition.build_config`, `required_env == ("WRDS_USERNAME",)` and `supports("us_equity","tick","quotes") is False`.
- Run the full suite and compare against the baseline (environment_notes). Any failing id outside the baseline is this plan's to fix.
  </action>

  <verify>
    <automated>uv run pytest -q tests/test_source_registry.py tests/test_acquisition_batching.py tests/test_registry_convert.py tests/test_source_inspector.py</automated>
    <fails_when>non-zero exit, or any failed test in the summary line</fails_when>
    <automated>uv run pytest -q --ignore=tests/test_factor_hierarchy.py -p no:cacheprovider</automated>
    <fails_when>any `FAILED` id in the output that is not listed in 03.9-BASELINE-FAILURES.txt (comm -13 against the baseline prints anything), or any `ERROR` line</fails_when>
  </verify>

  <acceptance_criteria>
    - `grep -c '"wrds"' tests/test_source_registry.py` returns at least 4
    - `grep -n 'mock_wrds_session' tests/test_acquisition_batching.py` returns at least one line
    - `comm -13 .planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-BASELINE-FAILURES.txt <sorted FAILED ids of the full run>` prints nothing
  </acceptance_criteria>

  <done>Every vendor-enumerating test knows three vendors, the subclass walk exercises the WRDS class through the fake, and the full suite fails only baseline tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process -> WRDS | a test that reached psycopg2.connect would authenticate as the developer and push Duo |
| process env -> WrdsSession | WRDS_USERNAME is read from the environment; the password stays inside libpq |
| symbol list -> SQL text | universe/CLI symbols become SQL literals |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.9-01 | Denial of Service | tests walking every Acquisition subclass | high | mitigate | autouse `_forbid_wrds_network` patches psycopg2.connect/_connect (+ wrds.Connection) to raise; PGPASSFILE pointed at a tmp path |
| T-03.9-02 | Information Disclosure | AcquisitionConfig / logs | high | mitigate | username only from env in WrdsSession; `CREDENTIAL_ENV_VARS=(USERNAME_ENV,)` feeds `_scrub`; no password parameter anywhere; build_config puts no credential in the config |
| T-03.9-03 | Tampering | copy_query WHERE clause | high | mitigate | roots and pairs passed only as `psycopg2.sql.Literal`; table name only as `sql.Identifier` built from a `date`; `_validate_symbols` runs before any SQL |
| T-03.9-04 | Spoofing | connection host | medium | mitigate | HOST/PORT/DBNAME are class constants passed explicitly; never read from config (plan 04 adds the PGHOSTADDR/PGSERVICE refusal) |
| T-03.9-05 | Tampering | raw record order | medium | mitigate | wrds_row_ord assigned before any frame operation; no server ORDER/GROUP/DISTINCT; stable local sort |
</threat_model>

<verification>
- The tracer test is green in the worktree.
- Full suite: failing ids ⊆ 03.9-BASELINE-FAILURES.txt.
- Frequency literal unchanged; no function added to quantlab/config/__init__.py (`git diff --stat <plan-base-sha>..HEAD -- quantlab/config/__init__.py` prints nothing; `<plan-base-sha>` = the commit this plan started from).
</verification>

<success_criteria>
One WRDS symbol-day reaches a correct 1m NBBO Zarr panel through registry.run and registry.convert, offline, and the suite gains no failure.
</success_criteria>

## Artifacts this plan produces

- `quantlab/acquisition/wrds_taq.py`: `USERNAME_ENV`; `WrdsSession` (`HOST`, `PORT`, `DBNAME`, `SSLMODE`, `APPLICATION_NAME`, `CONNECT_TIMEOUT_SECONDS`, `shared()`, `close_shared()`, `table_identifier()`, `where_clause()`, `copy_query()`, `trading_days()`, `table_columns()`, `copy_nbbo_csv()`); `WrdsTaqNbboAcquisition` (`VENDOR`, `SESSION_TIME_ZONE`, `TICK_DATA_TYPES`, `DEFAULT_BATCH_SIZE=25`, `DEFAULT_MAX_WORKERS=1`, `CREDENTIAL_ENV_VARS`, `REDACTION`, `SCHEMA_PATTERN`, `TABLE_PATTERN`, `SUFFIX_DELIMITER`, `DEFAULT_SUBDIR`, `RAW_COLUMNS`, `RAW_SCHEMA`, `symbol_to_pair()`, `pair_to_symbol()`, `_trading_days()`, `_fetch_page()`, `build_config()`); `WRDS_SOURCE`
- `quantlab/dataset/nbbo_resample.py`: `NbboResampler(bar_interval).resample(records, sessions)`
- `quantlab/dataset/nbbo.py`: `NbboPanelDataset` (`config_cls`, `DATA_TYPE`, `_session_dates()`, `_session_bounds()`, `_raw_axes_in_range()`, `_raw_data_to_xr_window()`, `_raw_data_to_xr()`, `_clean()`, `_widen_fill_values()`)
- `quantlab/dataset/cleaning.py`: `NBBO_PANEL_VARIABLES`, `clean_nbbo_panel()`
- `quantlab/enums/data.py`: `BarInterval`, `BAR_INTERVAL_SECONDS`
- `quantlab/base/config.py`: `NbboDatasetConfig` fields `bar_interval`, `session_start`, `session_end` (+ defaults market/frequency/vendor)
- `quantlab/acquisition/registry.py`: bottom import of `wrds_taq`
- `tests/conftest.py`: `_forbid_wrds_network` (autouse), shared-session cleanup (autouse), `mock_wrds_session`, `WRDS_USERNAME` in `_CREDENTIAL_ENV_NAMES`, per-vendor defaults in `acquisition_config`
- `tests/wrds_fixtures.py`: `FakeWrdsSession`, `taq_row()`, `TAQ_COLUMNS_2018_ON`, `TAQ_COLUMNS_PRE_2018`, `L6_SAMPLE_ROWS`
- `tests/test_nbbo_dataset.py`: `test_tracer_one_wrds_symbol_day_lands_raw_and_resamples_to_a_zarr_panel`
- `tests/test_source_registry.py`: `test_wrds_descriptor_serves_exactly_the_nbbo_capability` + extended enumerations

<output>
Create `.planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-01-SUMMARY.md` when done
</output>
