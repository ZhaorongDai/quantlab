# Phase 03.9: WRDS TAQ Consolidated Quotes to NBBO Zarr Panel - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-19
**Phase:** 03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel
**Areas discussed:** Pre-phase scoping, raw vs aggregated source, resample frequency & fields, session & filtering, symbol mapping & volume guard

---

## Pre-phase scoping (before phase insertion)

| Question | Selected |
|---|---|
| Storage grain | Fixed-frequency NBBO snapshots (later refined: raw NBBO records + local resample) |
| Universe | Reuse existing point-in-time universes |
| Credentials | WRDS account, `WRDS_USERNAME` + `~/.pgpass` |
| Workflow | New GSD phase |

## Raw vs aggregated

The user supplied the WRDS note "A Note on the Use of PROC SQL with TAQ data on WRDS" (saved as
`03.9-REF-wrds-proc-sql-taq-note.md`) and asked whether the raw snapshot data could be pulled directly.

| Option | Description | Selected |
|--------|-------------|----------|
| NBBO record table | WRDS-computed NBBO, one row per NBBO change | ✓ |
| `cqm` full quotes | Every exchange quote, ~10x larger | |
| Both | Two data_types | |

| Option | Description | Selected |
|--------|-------------|----------|
| Raw to parquet + local resample to dense Zarr | Frequency changeable without re-pull | ✓ |
| Raw into Zarr (merge Phase 03.3) | Non-dense event axis | |
| Parquet only this phase | Zarr deferred | |

## Resample frequency & fields

| Question | Options | Selected |
|---|---|---|
| Default frequency | 1m / arbitrary frequency parameter | Arbitrary frequency parameter |
| Bar semantics | Snapshot + time-weighted / snapshot only | Snapshot + time-weighted |
| Fields | bid/ask/sizes; mid/spread/bps; update count; order imbalance | All four |

## Session & filtering

| Question | Selected |
|---|---|
| Session | Raw full day, panel defaults to RTH (configurable) |
| Anomalous quotes | Keep raw, filter in resampler (counted) |
| Empty bars | Carry forward within day, update count 0 |

## Symbol mapping & volume guard

| Question | Selected |
|---|---|
| Symbol mapping | Join sym_root+sym_suffix in universe notation, keep raw columns |
| Volume guard | `count(*)` per daily table before pulling, `--force-volume` override |
| Registry | Register SourceDescriptor + CLI entry |

## Claude's Discretion

Exact NBBO table variant, names, batch size/workers, resampling engine, chunk granularity.

## Deferred Ideas

cqm full quotes and trades; CRSP permno link; raw events in Zarr (Phase 03.3).
