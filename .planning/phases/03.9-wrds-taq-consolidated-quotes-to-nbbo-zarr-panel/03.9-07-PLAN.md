---
phase: "03.9"
plan: "07"
type: execute
wave: 4
depends_on: ["03.9-03", "03.9-04", "03.9-06"]
files_modified:
  - scripts/ingest_wrds_taq.py
  - tests/test_ingest_wrds_taq.py
  - example/wrds_taq.md
  - example/README.md
autonomous: true
requirements: [D-07, D-09, D-15, D-16, D-17, D-20, D-21, D-24, D-26, D-27, D-29]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "scripts/ingest_wrds_taq.py resolves its source as DataSourceRegistry.get('wrds'), names no vendor class, builds the acquisition config through SOURCE.config_factory (the build_config classmethod) and constructs NbboDatasetConfig directly — no quantlab/config factory function is called (D-17, D-27)"
    - "The symbol roster is --symbols or --universe {sp500, nasdaq100} resolved with UniverseCatalog.get_symbols_in_range over [--start-date, --end-date] (interval overlap, dot notation); both dates are required (D-15)"
    - "Before run(...), the script counts rows per trading day and symbol batch with WrdsNbboVolumeProbe and calls SqlVolumeGuard.assert_acquisition_volume_fits(..., force=args.force_volume); a refusal exits non-zero having issued zero COPY queries (D-16, D-24)"
    - "The probe, the pull and the conversion share ONE WrdsSession: a full CLI run against the fake records exactly one connection (D-20)"
    - "An unentitled year stops the script before any count or COPY with a message naming the taqm_YYYY schema (D-21)"
    - "--to-zarr converts through registry.convert with --bar-interval (BarInterval choices, default 1m), --session-start/--session-end and --chunk (default day), and prints the filter-stats sidecar path (D-09, D-26)"
    - "The script accepts no credential argument and prints no credential; WRDS_USERNAME comes from the environment and the password from ~/.pgpass (D-07)"
    - "example/wrds_taq.md documents the pipeline, credentials, Duo behaviour, volume calibration and segmented runs, tie ambiguity before 2018, right-closed labels and the filter sidecar, in Chinese like the other example docs"
  artifacts:
    - path: "scripts/ingest_wrds_taq.py"
      provides: "WRDS TAQ NBBO ingest CLI"
      contains: "assert_acquisition_volume_fits"
    - path: "tests/test_ingest_wrds_taq.py"
      provides: "end-to-end CLI run against FakeWrdsSession + structural locks"
    - path: "example/wrds_taq.md"
      provides: "contract doc"
  key_links:
    - from: "scripts/ingest_wrds_taq.py"
      to: "quantlab/acquisition/sql_volume.py::SqlVolumeGuard.assert_acquisition_volume_fits"
      via: "guard call placed before run(SOURCE, ...)"
    - from: "scripts/ingest_wrds_taq.py"
      to: "quantlab/acquisition/wrds_taq.py::WrdsSession.shared"
      via: "one session for probe, run and teardown (close_shared in finally)"
---

<objective>
Give the user one command that pulls WRDS NBBO for a point-in-time universe and optionally
converts it to a panel: `scripts/ingest_wrds_taq.py`, a thin shell over the registry,
the probe, the SQL guard and `registry.convert`, plus its contract doc.

Purpose: D-17. Everything below the shell already exists and is tested; this plan wires it
in the order that protects the user (entitlement and volume BEFORE any data query, one
Duo push per run) and documents it.

Output: the script, its tests, `example/wrds_taq.md`, an index line in `example/README.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-CONTEXT.md
@.planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-RESEARCH.md
@.planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-03-SUMMARY.md
@.planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-04-SUMMARY.md
@.planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-06-SUMMARY.md
</context>

<environment_notes>
- Run commands from YOUR working-tree root: `uv run pytest -q tests/test_ingest_wrds_taq.py`. Never `cd` to the main-tree absolute path.
- A `-k` selector matching nothing exits 5; treat as failure.
- Existing `tests/test_volume_guard.py`, `tests/test_ingest_shells.py` and `tests/test_data_dir_cli.py` enumerate repo-root script paths that moved to `scripts/` and are part of the 55-failure baseline. Do NOT add the new script to their lists; its structural locks live in `tests/test_ingest_wrds_taq.py` and use the real `scripts/ingest_wrds_taq.py` path.
- Run the script in tests with `runpy.run_path("scripts/ingest_wrds_taq.py", run_name="__main__")` under `monkeypatch.setattr(sys, "argv", [...])`, `--data-dir <tmp_path>`, and the `mock_wrds_session` fixture (it patches `quantlab.acquisition.wrds_taq.WrdsSession`, which the script imports by name at run time).
- User memory: no config factories — construct `NbboDatasetConfig` and `UniverseConfig` directly with paths under `quantlab.config.get_data_root()`; do not call `universe_config()`, `stock_kline_config()` or `stock_acquisition_config()`.
</environment_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scripts/ingest_wrds_taq.py and its end-to-end run against the fake session</name>

  <files>scripts/ingest_wrds_taq.py, tests/test_ingest_wrds_taq.py</files>

  <read_first>
    - scripts/ingest_alpaca.py — whole file (docstring sections, SOURCE resolution, `_build_arg_parser`, `__main__` ordering: `apply_data_dir` first, guard before `run`, conversion branch with `refuse_conversion_without_raw_data` then `convert` then `print_conversion_result`)
    - quantlab/utils/cli.py lines 50-56 (`UNIVERSE_CATEGORY_MAP`), 89-116 (`add_window_args`), 205-300 (`refuse_conversion_without_raw_data`, `add_chunk_args(default=...)`), 371-450 (`resolve_symbols(mode="in_range")`, `add_data_dir_arg`, `apply_data_dir`), 482-530 (`add_volume_guard_args`), `print_sql_volume_estimate`, `print_conversion_result`
    - quantlab/acquisition/wrds_taq.py — `WrdsSession.shared/close_shared`, `WrdsNbboVolumeProbe`, `WrdsTaqNbboAcquisition.build_config`, `DEFAULT_BATCH_SIZE`
    - quantlab/acquisition/sql_volume.py — `SqlVolumeGuard.assert_acquisition_volume_fits`
    - quantlab/acquisition/universe.py lines 1519-1540 (`get_symbols_in_range` ascending-order contract)
    - quantlab/config/__init__.py lines 71-98, 233-250 (data-root layout; read only)
  </read_first>

  <behavior>
    With `mock_wrds_session` serving AAPL and BRK.B for 2024-01-24 and 2024-01-25:
    - `--symbols AAPL,BRK.B --start-date 2024-01-24 --end-date 2024-01-25 --data-dir <tmp> --to-zarr --bar-interval 5m` exits 0; stdout contains the SQL volume estimate, "succeeded", the Zarr path and the filter-stats sidecar path; raw shards exist under `<tmp>/downloads/us_equity/tick/wrds_taq/wrds/data_type=nbbo/`; the store has 156 timestamps and symbols ["AAPL", "BRK.B"]; `FakeWrdsSession.connections == 1`.
    - Without `--to-zarr` the run stops at raw and says so.
    - Fake counts large enough to cross 20 GiB -> the script exits non-zero with the guard's message (naming `--force-volume` and a fitting end date) and `FakeWrdsSession.copy_calls` is empty; the same with `--force-volume` proceeds and prints the forced line.
    - Fake `entitled_years={2024}` and `--start-date 2012-01-03 --end-date 2012-01-04` -> non-zero exit, message names `taqm_2012`, zero count and COPY calls.
    - `WRDS_USERNAME` unset -> non-zero exit with the variable named; a planted `WRDS_USERNAME` value never appears in stdout/stderr of the successful run.
    - `--to-zarr --session-start 04:00 --session-end 20:00 --bar-interval 30m` succeeds (32 labels per day); `--session-start 03:59` exits non-zero naming the 04:00-20:00 range, before any count or COPY call.
    - `--universe nasdaq_all` -> argparse error (exit 2); `--rows-per-symbol-day 100` -> parser error saying WRDS rows are counted server-side; missing `--end-date` -> parser error; `--symbols BRK-B` -> error naming the dot notation.
    - `--universe sp500` resolves through `UniverseCatalog.get_symbols_in_range` (monkeypatch the catalog load to a stub returning ["AAPL", "BRK.B"] and assert it was called with ("sp500_constituent", start, end)).
  </behavior>

  <action>
Tests first (RED: the script does not exist). Then write `scripts/ingest_wrds_taq.py`:
- Module docstring in the ingest_alpaca style with sections: what it does (raw NBBO records from `taqm_{YYYY}.complete_nbbo_{YYYYMMDD}`, optional local resample to a panel); Credentials (WRDS_USERNAME from env, password only in ~/.pgpass via libpq, no credential flag, nothing printed); One Duo push per run (one session shared by probe, pull and conversion; never raise worker counts); Volume (counted per symbol batch with count(*) before any pull, 20 GiB default ceiling, run long ranges as date segments, the refusal names one that fits); Usage examples (`uv run python scripts/ingest_wrds_taq.py --symbols AAPL,MSFT,BRK.B --start-date 2024-01-24 --end-date 2024-01-25 [--to-zarr --bar-interval 1m]`, `--universe sp500 ...`).
- `SOURCE = DataSourceRegistry.get("wrds")`; `UNIVERSES = ("sp500", "nasdaq100")`; `BAR_INTERVALS = typing.get_args(BarInterval)`; `DEFAULT_STORE_TEMPLATE = "wrds_nbbo_{bar_interval}.zarr"`.
- Parser: local `--symbols` and `--universe` (choices UNIVERSES; help: point-in-time constituents resolved by interval overlap over the window, dot notation like BRK.B), `add_window_args(parser)`, `add_volume_guard_args(parser)`, `add_data_dir_arg(parser)`, `--batch-size`, `--refresh`, `add_to_zarr_arg(parser)`, `add_chunk_args(parser, default="day")`, `--bar-interval` (choices BAR_INTERVALS, default "1m"), `--session-start` (default "09:30"), `--session-end` (default "16:00"); their help says the window may be anywhere inside 04:00-20:00 ET and that on half days only regular-hours edges follow the early close (D-29). Validation of the window stays in `XnysSessionCalendar` (one rule, one place): the script constructs `XnysSessionCalendar(args.session_start, args.session_end)` during argument validation, before `WrdsSession.shared()`, and turns its ValueError into `parser.error`, so a bad window costs no WRDS query. No argument accepts a username or password.
- Validation (`parser.error`): exactly one of --symbols/--universe; both dates present; `--rows-per-symbol-day` given -> error; a hyphenated symbol -> error pointing at dot notation.
- `__main__` order: parse; `apply_data_dir(args)`; validate; catalog = `UniverseCatalog.load(UniverseConfig(output_path=..., cache_dir=...))` (paths under `get_data_root() / "data" / "reference"`) only with --universe; `symbols = resolve_symbols(args, catalog, mode="in_range")`; kwargs with batch_size; `acq_config = SOURCE.config_factory(symbols=symbols, start_date=..., end_date=..., kwargs=kwargs)`; `session = WrdsSession.shared()`; inside `try/finally: WrdsSession.close_shared()`: `rows_by_day = WrdsNbboVolumeProbe(session, batch_size=<effective>).count_rows_by_day(symbols, start, end)`; `print_sql_volume_estimate(SqlVolumeGuard(acq_config.kwargs).assert_acquisition_volume_fits(rows_by_day, symbols=len(symbols), start_date=..., end_date=..., force=args.force_volume), forced=args.force_volume)`; `result = run(SOURCE, acq_config, refresh=args.refresh)`; print succeeded/failed counts and the raw root; if `--to-zarr`: build `NbboDatasetConfig(zarr_file_path=str(get_data_root() / "data" / "us_equity" / "tick" / DEFAULT_STORE_TEMPLATE.format(...)), raw_data_dir_path=acq_config.raw_data_dir_path, catalog_path=str(get_data_root() / "data" / "catalog"), start_date=..., end_date=..., bar_interval=..., session_start=..., session_end=...)`, `refuse_conversion_without_raw_data(NbboPanelDataset(replace(ds_config, symbols=None)), result)`, `conversion = convert(SOURCE, ds_config, data_type="nbbo", granularity=args.chunk, on_new_listing=args.on_new_listing)`, `print_conversion_result(conversion)`, print the sidecar path; else print that the run stops at raw. Guard and entitlement errors surface as a non-zero exit with their message (`SystemExit` via `parser.exit(1, message)` or re-raise; the tests assert non-zero and the message text).
  </action>

  <verify>
    <automated>uv run pytest -q tests/test_ingest_wrds_taq.py</automated>
    <fails_when>non-zero exit, or fewer than 9 passed in the summary line</fails_when>
  </verify>

  <acceptance_criteria>
    - `uv run python scripts/ingest_wrds_taq.py --help` exits 0 and lists `--bar-interval`, `--force-volume`, `--to-zarr`, `--chunk`
    - `uv run pytest -q tests/test_ingest_wrds_taq.py -k "one_connection or refus or entitle"` exits 0
    - The end-to-end test asserts `FakeWrdsSession.connections == 1`
  </acceptance_criteria>

  <done>One command pulls, guards, and optionally converts, with one session, and refuses before any data query when entitlement or volume says no.</done>
</task>

<task type="auto">
  <name>Task 2: Structural locks on the script (guard placement, force flag, no credentials, no factories)</name>

  <files>tests/test_ingest_wrds_taq.py</files>

  <read_first>
    - tests/test_volume_guard.py lines 903-1105 (`_main_body`, `_call_linenos`, `_is_guard_call`, `_is_acquisition_construction`, `_fetch_site_linenos`, the force-flag scan) — re-implement the needed helpers locally; do not import from that module
    - tests/test_ingest_shells.py lines 400-480 (the `config_factory` call-site idiom)
    - scripts/ingest_wrds_taq.py (Task 1)
  </read_first>

  <action>
Add AST/source tests over `scripts/ingest_wrds_taq.py`:
`test_the_wrds_guard_is_called_by_name_in_main` (an `assert_acquisition_volume_fits` call in the `__main__` body);
`test_the_wrds_guard_precedes_run_and_any_acquisition_construction` (its line is below every `run(...)` call and every `.acquisition_cls(...)` call, and below the probe's `count_rows_by_day` call);
`test_force_volume_is_a_typed_flag_only` (`force=args.force_volume` and `add_volume_guard_args` appear; none of `FORCE_VOLUME`, `getenv`, `environ.get("FORCE` appear);
`test_the_script_names_no_vendor_class_and_calls_config_factory_once` (`WrdsTaqNbboAcquisition` absent from the source; exactly one `.config_factory(` call);
`test_the_script_calls_no_quantlab_config_factory` (no call to any function defined in `quantlab/config/__init__.py` other than `get_data_root` / `set_data_root`, found by parsing that module's function names);
`test_apply_data_dir_precedes_every_path_derivation` (`apply_data_dir(args)` line is below any `get_data_root()` or `config_factory` call);
`test_no_credential_argument` (no `add_argument` whose flag contains "password", "passwd", "user" or "pgpass");
`test_one_session_per_run` (exactly one `WrdsSession.shared(` call and a `close_shared` inside a `finally`).
Each test fails with a message naming the line numbers it compared.
  </action>

  <verify>
    <automated>uv run pytest -q tests/test_ingest_wrds_taq.py -k "guard or force or vendor or factory or apply_data_dir or credential or session"</automated>
    <fails_when>non-zero exit, exit 5 with "deselected", or fewer than 8 passed</fails_when>
  </verify>

  <acceptance_criteria>
    - Deleting the guard call from a scratch copy of the script (not committed) makes `test_the_wrds_guard_is_called_by_name_in_main` fail — record the mutation result in the SUMMARY
    - `uv run pytest -q tests/test_ingest_wrds_taq.py` exits 0
  </acceptance_criteria>

  <done>The protective ordering and the no-credential/no-factory rules of the script are structural, mutation-checked locks.</done>
</task>

<task type="auto">
  <name>Task 3: example/wrds_taq.md contract doc and README index</name>

  <files>example/wrds_taq.md, example/README.md</files>

  <read_first>
    - example/acquisition.md lines 1-140 and 672-800 (section structure: 一句话 / 不用它会怎样 / 核心概念 / 它是怎么工作的 / 完整例子 / 常见坑)
    - example/README.md (index format)
    - .planning/phases/03.9-.../03.9-CONTEXT.md D-01..D-28 and 03.9-LIVE-CHECK-1/2.json (numbers to quote)
    - scripts/ingest_wrds_taq.py docstring (Task 1)
  </read_first>

  <action>
Write `example/wrds_taq.md` in Chinese following example/acquisition.md's section shape:
一句话; 不用它会怎样 (the concrete failure modes this phase prevents: nbbom missing single-venue NBBO states, PostgreSQL returning no order without ORDER BY and ORDER BY reordering ties, a fixed 16:00 close on half days, Duo push storms, wrds.Connection prompting and breaking under pandas 3); 核心概念 (complete_nbbo vs nbbom, the raw column set and why `date` is stored as `taq_date`, `wrds_row_ord` and the D-19 key, n_ambiguous_ties and the 2018-01-02 nano boundary, right-closed bar-end labels, the session window (default RTH, configurable inside 04:00-20:00 ET per D-29, with the half-day edge rule and its 2024-11-29 examples), carry-forward within a day only, NULL side = NaN, default filters and the sidecar, BarInterval tokens and why they divide 390 and 210 minutes); 它是怎么工作的 (a flow diagram: CLI -> UniverseCatalog -> WrdsSession (one Duo push) -> entitlement -> count probe -> SqlVolumeGuard -> registry.run -> raw shards -> registry.convert -> NbboPanelDataset -> Zarr + sidecar); 凭证 (WRDS_USERNAME, ~/.pgpass line shape `wrds-pgdata.wharton.upenn.edu:9737:wrds:<username>:<password>` with chmod 600, PGHOSTADDR/PGSERVICE refusal, never type a password into the tool); 体量 (live 2024-01-24 calibration: AAPL 1,243,426 rows/day, XOM 760,575, MSFT 669,127, JNJ 157,359, BRK 152,801; full market 313,568,856 rows / 9,703 roots; the 20 GiB ceiling admits only a few S&P 500 days, so run date segments; bytes/row is an assumption until measured); 完整例子 (commands, marked as not executed live unless plan 08 recorded them); 常见坑. Add one index line for `wrds_taq.md` to `example/README.md` in its existing format.
  </action>

  <verify>
    <automated>test -f example/wrds_taq.md && grep -c "wrds_row_ord" example/wrds_taq.md && grep -n "wrds_taq.md" example/README.md</automated>
    <fails_when>non-zero exit, a count of 0, or no README line</fails_when>
  </verify>

  <acceptance_criteria>
    - `example/wrds_taq.md` contains the strings `complete_nbbo`, `wrds_row_ord`, `n_ambiguous_ties`, `WRDS_USERNAME`, `.pgpass`, `--force-volume`, `nbbo_filter_stats.json`
    - `example/README.md` has a line linking `wrds_taq.md`
    - The doc contains no username or password value (only placeholders)
  </acceptance_criteria>

  <done>A reader can run the WRDS pipeline safely and understand every semantic choice from one doc.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator shell -> script | flags and env reach the provider |
| script -> WRDS | the first session opens here (one Duo push) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.9-23 | Information Disclosure | credential in argv / shell history / output | high | mitigate | no credential flag (structural test); env/.pgpass only; test plants WRDS_USERNAME and asserts it never prints |
| T-03.9-24 | Denial of Service | multiple sessions per run (Duo storm) | high | mitigate | one `WrdsSession.shared()` for probe, run and convert; `close_shared` in finally; test asserts one connection |
| T-03.9-25 | Denial of Service | unguarded multi-GB pull | medium | mitigate | probe + guard strictly before run(...); structural ordering test; refusal leaves zero COPY calls |
| T-03.9-26 | Tampering | hyphenated or malformed symbols reaching SQL | medium | mitigate | parser-level refusal of hyphenated symbols plus the provider's `_validate_symbols` |
</threat_model>

<verification>
- `uv run pytest -q tests/test_ingest_wrds_taq.py` green.
- Full suite: failing ids ⊆ 03.9-BASELINE-FAILURES.txt.
</verification>

<success_criteria>
`scripts/ingest_wrds_taq.py` is the safe, documented front door to the WRDS NBBO pipeline.
</success_criteria>

## Artifacts this plan produces

- `scripts/ingest_wrds_taq.py`: `SOURCE`, `UNIVERSES`, `BAR_INTERVALS`, `DEFAULT_STORE_TEMPLATE`, `_build_arg_parser()`; flags `--symbols`, `--universe`, `--start-date`, `--end-date`, `--force-volume`, `--rows-per-symbol-day` (refused), `--data-dir`, `--batch-size`, `--refresh`, `--to-zarr`, `--chunk` (default day), `--on-new-listing`, `--bar-interval`, `--session-start`, `--session-end`
- `tests/test_ingest_wrds_taq.py`
- `example/wrds_taq.md`; index line in `example/README.md`

<output>
Create `.planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-07-SUMMARY.md` when done
</output>
