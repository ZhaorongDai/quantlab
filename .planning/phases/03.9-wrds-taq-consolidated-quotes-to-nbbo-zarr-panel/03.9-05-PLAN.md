---
phase: "03.9"
plan: "05"
type: execute
wave: 2
depends_on: ["03.9-01"]
files_modified:
  - quantlab/dataset/nbbo_resample.py
  - quantlab/dataset/cleaning.py
  - quantlab/base/config.py
  - tests/test_nbbo_resampler.py
  - tests/test_cleaning.py
autonomous: true
requirements: [D-03, D-05, D-10, D-11, D-12, D-13, D-14, D-19, D-22, D-25, D-26]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Filtering is configurable and local: by default crossed quotes (both sides present, bid > ask) and records with a present price <= 0 are dropped, locked quotes (bid == ask) are kept; drop_locked and a keep_qu_cond allow-list are opt-in (D-10)"
    - "A NULL side is 'no quote on that side': that side's price and size are NaN, never 0; spread, mid, spread_bps and imbalance are NaN while a side is missing, and time-weighted averages divide by the time each variable was defined (D-25)"
    - "Every dropped record is counted per (date, symbol) under exactly one reason and returned as a stats frame; a dropped record leaves the previous valid NBBO standing (D-10)"
    - "Record order is the stable total order (date, symbol, timestamp, wrds_row_ord); the snapshot at a bar end is the last record by that order at or before the label; shuffling input rows never changes the output (D-03, D-11, D-19)"
    - "Tied records have zero duration except the last of the tie, so time-weighted spread and depth use only the last state of each instant (D-11)"
    - "n_ambiguous_ties counts, per bar, the records that share their timestamp with a differently-valued record; identical duplicates count 0 (D-19)"
    - "Bars are right-closed and labelled at bar end on a regular grid of bar_interval; a session length that is not a whole number of bars raises (D-22, D-26)"
    - "An empty bar carries the previous state forward with n_updates 0; state never carries across session dates; bars before a day's first valid NBBO are NaN (D-13)"
    - "The panel variable set is exactly NBBO_PANEL_VARIABLES, all float64, and clean_nbbo_panel rejects any deviation without modifying or filling data (D-12, D-14)"
  artifacts:
    - path: "quantlab/dataset/nbbo_resample.py"
      provides: "NbboFilterPolicy, NbboResampler.resample_with_stats"
      contains: "wrds_row_ord"
    - path: "quantlab/base/config.py"
      provides: "NbboDatasetConfig filter fields drop_crossed, drop_locked, drop_nonpositive_price, keep_qu_cond"
    - path: "tests/test_nbbo_resampler.py"
      provides: "hand-computed filter, NULL-side, tie, ambiguity, grid and carry-forward locks"
    - path: "tests/test_cleaning.py"
      provides: "clean_nbbo_panel validation tests"
  key_links:
    - from: "quantlab/dataset/nbbo_resample.py::NbboResampler"
      to: "quantlab/dataset/cleaning.py::NBBO_PANEL_VARIABLES"
      via: "the resampler emits exactly the variable tuple the validator enforces (one definition)"
    - from: "quantlab/base/config.py::NbboDatasetConfig"
      to: "quantlab/dataset/nbbo_resample.py::NbboFilterPolicy.from_config"
      via: "filter knobs are config fields, serialisable with asdict for rebuilds"
  prohibitions:
    - requirement_id: "D-11"
      category: "safety"
      status: "resolved"
      verification: "test"
      resolution: null
      reason: null
      statement: "MUST NOT let a bar's value depend on any record timestamped after its label (no look-ahead into the panel a model trains on)"
    - requirement_id: "D-13"
      category: "safety"
      status: "resolved"
      verification: "test"
      resolution: null
      reason: null
      statement: "MUST NOT fabricate NBBO state: no carry across trading days and no value before the day's first valid record"
---

<objective>
Turn the tracer's happy-path resampler into the full D-10..D-13 / D-19 / D-22 / D-25
semantics, each locked by a hand-computed fixture: configurable filtering with drop
counts, NULL-side handling, the stable total order and tie ambiguity, right-closed labels
on any BarInterval, and within-day-only carry-forward. Also lock `clean_nbbo_panel`.

Purpose: record ordering within ties is the phase's central correctness risk (the user's
WRDS PROC SQL note); the resampler is where that risk turns into numbers.

Output: finished `quantlab/dataset/nbbo_resample.py`, the four filter fields on
`NbboDatasetConfig`, `tests/test_nbbo_resampler.py`, and nbbo tests in `tests/test_cleaning.py`.
Plan 06 wires the policy and stats into the dataset.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-CONTEXT.md
@.planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-RESEARCH.md
@.planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-01-SUMMARY.md
</context>

<environment_notes>
- Run commands from YOUR working-tree root: `uv run pytest -q tests/test_nbbo_resampler.py`. Never `cd` to the main-tree absolute path.
- A `-k` selector matching nothing exits 5; treat as failure.
- Resampler tests build input frames directly (columns: symbol, date, timestamp, wrds_row_ord, best_bid, best_bidsizeshares, best_ask, best_asksizeshares, qu_cond) plus a sessions frame (date, open, close naive UTC); they do not touch WRDS fakes or Zarr.
- Compare floats with `pytest.approx(abs=1e-9)`; write expected TW values in the test as explicit duration-weighted sums so a reader can re-derive them.
- 2016-12-07 and 2024-01-24 are EST days: 09:30 ET = 14:30Z.
- Do NOT modify quantlab/dataset/nbbo.py in this plan (plan 06 owns it); keep `NbboResampler(bar_interval).resample(records, sessions)` working with its plan-01 signature so the tracer stays green.
</environment_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Configurable filters, NULL sides and drop counts (D-05, D-10, D-25)</name>

  <files>quantlab/dataset/nbbo_resample.py, quantlab/base/config.py, tests/test_nbbo_resampler.py</files>

  <read_first>
    - quantlab/dataset/nbbo_resample.py (plan 01)
    - quantlab/base/config.py (NbboDatasetConfig from plan 01)
    - .planning/phases/03.9-.../03.9-CONTEXT.md D-05, D-10, D-25 (live counts: 202 NULL-side rows, 545 crossed, 130,164 locked of 2.98M)
    - .planning/phases/03.9-.../03.9-RESEARCH.md § Pattern 4 step 1
  </read_first>

  <behavior>
    One AAPL session 2024-01-24, 1m bars, seed at 09:29:00 two-sided (100.00 x 100 / 100.02 x 100):
    - NULL side: 09:30:20 bid 100.01 x 200 with ask NULL; 09:30:40 100.00 x 100 / 100.04 x 100. Bar 14:31Z: n_updates 2; snapshot bid 100.00, ask 100.04; tw_spread == (0.02*20 + 0.04*20)/40 (the ask-less 20 s excluded); tw_bid_size == (100*20 + 200*20 + 100*20)/60; tw_ask_size == (100*20 + 100*20)/40. A bar whose last record is ask-less has ask, ask_size, mid, spread, spread_bps, imbalance NaN and bid, bid_size finite. A record with BOTH sides NULL is kept as a state with every price variable NaN.
    - Crossed: 09:30:30 bid 101.00 / ask 100.50 -> dropped by default; that bar's snapshot and TW equal a run without the record; stats `dropped_crossed == 1`; with `drop_crossed=False` it is kept.
    - Locked: 09:30:30 bid 100.01 / ask 100.01 -> kept by default (spread 0, counted in n_updates); with `drop_locked=True` dropped and `dropped_locked == 1`.
    - Non-positive price: bid 0.00 -> dropped (`dropped_nonpositive_price == 1`), previous state stands.
    - `keep_qu_cond=("R",)`: a record with qu_cond "O" dropped (`dropped_condition == 1`); default None keeps it.
    - Each dropped record counted once, precedence nonpositive_price > condition > crossed > locked; stats also carry `records_in`, `one_sided_kept`, `both_null_kept` per (date, symbol).
  </behavior>

  <action>
Tests first (RED). Then:
- `quantlab/base/config.py`: add to `NbboDatasetConfig` `drop_crossed: bool = True`, `drop_locked: bool = False`, `drop_nonpositive_price: bool = True`, `keep_qu_cond: tuple[str, ...] | None = None` (D-10 defaults; config-driven so a rebuild from config.json reproduces the panel).
- `quantlab/dataset/nbbo_resample.py`: frozen dataclass `NbboFilterPolicy` with the same four fields and `from_config(config)`; `NbboResampler.__init__(self, bar_interval, policy=None)` (None -> `NbboFilterPolicy()`); `resample_with_stats(records, sessions) -> tuple[pl.DataFrame, pl.DataFrame]` doing the filter FIRST (before ordering, ties and seeding, so a dropped record never becomes a seed or a tie member), then the plan-01 pipeline; `resample()` returns the panel element only. Present side := price not null. Crossed/locked are evaluated only when both sides are present. Time-weighted averages keep one coverage integral per variable (time the variable was non-null) and divide by it; zero coverage -> null. Stats frame columns: date, symbol, records_in, dropped_nonpositive_price, dropped_condition, dropped_crossed, dropped_locked, one_sided_kept, both_null_kept (Int64).
- Docstring the precedence rule and why a NULL side is NaN, not zero (D-25).
  </action>

  <verify>
    <automated>uv run pytest -q tests/test_nbbo_resampler.py -k "filter or null or crossed or locked or nonpositive or condition"</automated>
    <fails_when>non-zero exit, exit 5 with "deselected", or fewer than 7 passed</fails_when>
  </verify>

  <acceptance_criteria>
    - `grep -n "drop_crossed: bool = True" quantlab/base/config.py` and `grep -n "drop_locked: bool = False" quantlab/base/config.py` each return one line
    - `grep -n "class NbboFilterPolicy" quantlab/dataset/nbbo_resample.py` returns one line
    - `uv run pytest -q tests/test_nbbo_dataset.py -k tracer` still exits 0
  </acceptance_criteria>

  <done>Filters are configurable with the D-10 defaults, NULL sides are NaN with per-variable TW coverage, and every drop is counted once.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Total order, tie collapse and ambiguity count (D-03, D-11, D-19)</name>

  <reversibility rating="costly">D-19's tie-break by the arrival ordinal defines which state a pre-2018 bar reports; changing it later changes historical panels and needs a full re-resample (not a re-pull).</reversibility>

  <files>quantlab/dataset/nbbo_resample.py, tests/test_nbbo_resampler.py</files>

  <read_first>
    - quantlab/dataset/nbbo_resample.py (Task 1 state)
    - .planning/phases/03.9-.../03.9-REF-wrds-proc-sql-taq-note.md (why physical order is the only tie-breaker)
    - .planning/phases/03.9-.../03.9-CONTEXT.md D-19; 03.9-LIVE-CHECK-2.json M3_ties_2016 / M4_tie_content_2016
  </read_first>

  <behavior>
    2016-12-07 AAPL, 1m, no nanoseconds. Seed 09:29:00 S0 = 110.00 x 100 / 110.02 x 100.
    - Bar 14:31Z: three records at 09:30:10.000001 with wrds_row_ord 1, 2, 3 and states Sa (110.01 x 200 / 110.03 x 100), Sb (110.02 x 300 / 110.03 x 100), Sc (110.00 x 100 / 110.03 x 100); then 09:30:40 Sd (110.01 x 100 / 110.02 x 200). Expect n_updates 4, n_ambiguous_ties 3, snapshot Sd, tw_spread == (0.02*10.000001 + 0.03*29.999999 + 0.01*20)/60.
    - Bar 14:32Z: two IDENTICAL records Se (110.02 x 100 / 110.04 x 100) at 09:32:00.000000 exactly -> n_updates 2, n_ambiguous_ties 0, snapshot Se, tw_spread == 0.01 (Se has zero duration inside this bar).
    - Bar 14:33Z: at 09:33:00.000000 exactly, X (110.05 x 100 / 110.06 x 100) ordinal 7 then Y (110.04 x 100 / 110.06 x 100) ordinal 8 -> snapshot Y, n_ambiguous_ties 2.
    - Bar 14:34Z: empty -> carries Y, n_updates 0, n_ambiguous_ties 0.
    - Permutation invariance: 20 seeded shuffles of the input rows give a panel equal to the unshuffled one (`polars.testing.assert_frame_equal`).
    - Ordinal matters: re-labelling the bar-1 tie so Sa carries the highest ordinal changes bar 1's tw_spread to the value computed with Sa's spread — proving the key, not arrival in the frame, decides.
    - A 2024-style fixture with distinct nanoseconds has n_ambiguous_ties 0 on every bar.
  </behavior>

  <action>
Tests first (RED). Plan 01 left this deliberately thin: it sorts by the key but has NO
tie collapse and emits `n_ambiguous_ties` as a constant 0.0, so the tie and ambiguity
tests must fail before this task. Make the order explicit and stable: sort by (symbol, date, timestamp, wrds_row_ord) with
`maintain_order=True`; the tie collapse keeps the last row of each (symbol, date,
timestamp) group by that order; ambiguity = group has more than one distinct
(best_bid, best_bidsizeshares, best_ask, best_asksizeshares) tuple (nulls compared as
values), counted per record and summed per bar alongside n_updates, AFTER filtering.
Never rely on the input frame's row order anywhere. Document in the class docstring that
2018+ data is unique on (symbol, time_m, time_m_nano) (live check L-4: 2,065,354 of
2,065,354) while ~5% of 2016 rows share a microsecond with different content, which is
exactly what n_ambiguous_ties exposes to downstream users.
  </action>

  <verify>
    <automated>uv run pytest -q tests/test_nbbo_resampler.py -k "tie or ambigu or order or permut"</automated>
    <fails_when>non-zero exit, exit 5 with "deselected", or fewer than 5 passed</fails_when>
  </verify>

  <acceptance_criteria>
    - The permutation test runs 20 seeds and asserts full-frame equality
    - The ordinal-mutation test asserts the bar-1 tw_spread changes when the tie ordinals are re-labelled
    - `uv run pytest -q tests/test_nbbo_dataset.py -k tracer` still exits 0
  </acceptance_criteria>

  <done>The snapshot and TW averages are functions of the D-19 key alone, and pre-2018 tie ambiguity is surfaced per bar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Grid, right-closed labels, within-day carry, variable set and clean_nbbo_panel (D-12, D-13, D-14, D-22, D-26)</name>

  <files>quantlab/dataset/nbbo_resample.py, quantlab/dataset/cleaning.py, tests/test_nbbo_resampler.py, tests/test_cleaning.py</files>

  <read_first>
    - quantlab/dataset/nbbo_resample.py (Task 2 state); quantlab/enums/data.py `BAR_INTERVAL_SECONDS`
    - quantlab/dataset/cleaning.py `NBBO_PANEL_VARIABLES`, `clean_nbbo_panel`, and lines 297-353 (`clean_membership_panel`)
    - tests/test_cleaning.py (existing clean_membership_panel tests — mirror their style)
  </read_first>

  <behavior>
    - bar_interval 1s / 5s / 1m / 5m / 30m on a 2024-01-24 regular session -> 23400 / 4680 / 390 / 78 / 13 labels per symbol; first label = open + interval, last = close, constant spacing.
    - A sessions frame whose close is 15:59:59 ET with 1m bars -> ValueError naming the date and the bar interval; `NbboResampler("7m")` -> ValueError listing the accepted tokens.
    - Right-closed: a record exactly at 09:31:00 counts in bar 14:31Z, not 14:32Z; a record at 09:30:00 exactly is the seed, counted in no bar; a record after the close appears in no bar.
    - Two dates: AAPL has state all of 2024-01-24; on 2024-01-25 its first record is 10:00:30 with no pre-open record -> 2024-01-25 bars 14:31Z..15:00Z have NaN states and TW and n_updates 0 (no carry from the previous day); bar 15:01Z has the new state.
    - A symbol whose only records that day are dropped by the filter still gets rows for that date: NaN states, n_updates 0.
    - Output columns are exactly symbol, date, timestamp plus NBBO_PANEL_VARIABLES, every variable Float64; mid == (bid+ask)/2, spread == ask-bid, spread_bps == 1e4*spread/mid, imbalance == (bid_size-ask_size)/(bid_size+ask_size) on a sampled bar.
    - `clean_nbbo_panel`: a valid panel is returned as the same object; a missing variable, an extra variable, an `anomaly_flag` variable, an int64 variable, dims (symbol, timestamp), and a duplicate or decreasing timestamp each raise ValueError naming the problem; it never adds a variable.
  </behavior>

  <action>
Tests first (RED). Fix whatever the tests expose in the grid construction, label slicing,
per-date grouping and output typing. Emit rows for every (symbol, date) present in the
INPUT records (before filtering). Harden `clean_nbbo_panel` only where its tests demand it
and keep `cleaning.py` import-free of quantlab and free of any fill (its module rule).
Add a line to the `cleaning.py` module docstring listing `clean_nbbo_panel` as the fourth
entry point.
  </action>

  <verify>
    <automated>uv run pytest -q tests/test_nbbo_resampler.py tests/test_cleaning.py</automated>
    <fails_when>non-zero exit, or any failed test in the summary line</fails_when>
    <automated>uv run pytest -q tests/test_nbbo_dataset.py tests/test_config_roundtrip.py tests/test_dataset_hierarchy.py</automated>
    <fails_when>non-zero exit, or any failed test in the summary line</fails_when>
  </verify>

  <acceptance_criteria>
    - `uv run pytest -q tests/test_cleaning.py -k nbbo` exits 0 with at least 6 passed
    - `uv run pytest -q tests/test_nbbo_resampler.py -k "interval or label or carry or variables"` exits 0
    - Full suite: failing ids ⊆ 03.9-BASELINE-FAILURES.txt
  </acceptance_criteria>

  <done>Every bar interval yields a correct right-closed grid, state never crosses days, the variable set is fixed and float64, and the validator enforces it without touching data.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw tier -> resampler | raw keeps anomalous records by design (D-05); the resampler is the only filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.9-17 | Tampering | look-ahead into bar values | medium | mitigate | right-closed labels at bar end; as-of backward join; tests place records exactly on edges and after the close |
| T-03.9-18 | Tampering | silent state fabrication | medium | mitigate | per-(symbol, date) grouping; NaN before the first record; no carry across dates; carry-forward documented as state semantics, never in cleaning.py |
| T-03.9-19 | Repudiation | unexplained dropped records | low | mitigate | per-(date, symbol) drop counts by reason returned for the plan-06 sidecar |
</threat_model>

<verification>
- `uv run pytest -q tests/test_nbbo_resampler.py tests/test_cleaning.py tests/test_nbbo_dataset.py` green.
- Full suite: failing ids ⊆ 03.9-BASELINE-FAILURES.txt.
</verification>

<success_criteria>
Every D-10..D-13/D-19/D-22/D-25/D-26 semantic has a hand-computed test, and the tracer is still green.
</success_criteria>

## Artifacts this plan produces

- `quantlab/dataset/nbbo_resample.py`: `NbboFilterPolicy` (`drop_crossed`, `drop_locked`, `drop_nonpositive_price`, `keep_qu_cond`, `from_config()`), `NbboResampler.__init__(bar_interval, policy=None)`, `resample_with_stats()`, `resample()`
- `quantlab/base/config.py`: `NbboDatasetConfig.drop_crossed`, `.drop_locked`, `.drop_nonpositive_price`, `.keep_qu_cond`
- `quantlab/dataset/cleaning.py`: hardened `clean_nbbo_panel()` (docstring lists it)
- `tests/test_nbbo_resampler.py`; nbbo tests in `tests/test_cleaning.py`

<output>
Create `.planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-05-SUMMARY.md` when done
</output>
