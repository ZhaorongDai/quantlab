---
phase: "03.9"
plan: "04"
type: execute
wave: 2
depends_on: ["03.9-01"]
files_modified:
  - quantlab/acquisition/wrds_taq.py
  - tests/wrds_fixtures.py
  - tests/test_wrds_taq_acquisition.py
autonomous: true
requirements: [D-02, D-03, D-05, D-06, D-07, D-15, D-18, D-19, D-20, D-21, D-24, D-25]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A missing WRDS_USERNAME fails construction with a RuntimeError naming the variable; a missing, group/world-readable or non-matching ~/.pgpass (or $PGPASSFILE) fails before psycopg2.connect with an actionable message that never contains the password field (D-07)"
    - "No code path calls input() or getpass.getpass(), and quantlab/acquisition/wrds_taq.py never imports the wrds package (D-07, D-20)"
    - "psycopg2.connect receives host wrds-pgdata.wharton.upenn.edu, port 9737, dbname wrds, sslmode require, the env username and NO password; PGHOST cannot redirect it and a set PGHOSTADDR or PGSERVICE is refused before connecting (D-20)"
    - "One process makes at most one connection: many queries reuse it, and after it breaks the session raises instead of reconnecting (each connection can push Duo) (D-20)"
    - "A broken session or an entitlement failure is a GLOBAL stop (classified 'quota'): the run stops dispatching and no symbol is written to the failure manifest (D-21)"
    - "Before the first COPY, every year in the window is probed with has_schema_privilege('taqm_YYYY','USAGE'); an unentitled year (e.g. 2012) fails the run with a message naming the schema and zero COPY calls (D-21)"
    - "Pre-2018 tables (no time_m_nano) and 2018+ tables produce shards with IDENTICAL columns and dtypes; time_m_nano is a typed null before 2018 (D-18)"
    - "Every server query is SELECT ... WHERE only: no ORDER BY, GROUP BY, DISTINCT, LIMIT or window function in any composed WRDS data/count query; values reach SQL only as psycopg2.sql.Literal (D-03, D-19)"
    - "Raw preserves arrival order and every tied record: wrds_row_ord is 0..n-1 in COPY order per (day, batch) page; pre-2018 same-microsecond records with different content are all kept (D-02, D-19)"
    - "NULL bid/ask sides stay null in raw (not 0); crossed and locked records are kept in raw (D-05, D-25)"
    - "BRK.B is queried as (BRK, B), AAPL as (AAPL, NULL suffix); a returned row outside the requested (root, suffix) pairs fails the batch; a hyphenated symbol is refused (D-15)"
    - "One page = one trading day's complete_nbbo table for one symbol batch; a failure on day k leaves days < k recorded and the next run resumes at day k (D-06)"
    - "WrdsNbboVolumeProbe counts rows per (trading day, symbol batch) with the same WHERE clause the COPY uses and never counts a table without a sym_root predicate (D-16, D-24)"
  artifacts:
    - path: "quantlab/acquisition/wrds_taq.py"
      provides: "WrdsSessionError, WrdsEntitlementError, pgpass pre-check, assert_entitled, count_rows, WrdsNbboVolumeProbe, _run entitlement preflight, _classify_error"
      contains: "has_schema_privilege"
    - path: "tests/test_wrds_taq_acquisition.py"
      provides: "credential, connection, SQL-shape, schema-era, symbol, resume, entitlement and probe tests"
    - path: "tests/wrds_fixtures.py"
      provides: "FakeConnection/FakeCursor, render_composed, FakeWrdsSession entitlement/count/raise_on support"
  key_links:
    - from: "WrdsTaqNbboAcquisition._run"
      to: "WrdsSession.assert_entitled"
      via: "preflight before super()._run dispatches any batch"
    - from: "WrdsNbboVolumeProbe.count_rows_by_day"
      to: "WrdsSession.where_clause"
      via: "the same composed WHERE as copy_query, so counts and pulls cannot drift"
---

<objective>
Harden the tracer's WRDS provider into the production vendor: the credential and
connection contract (D-07, D-20), the entitlement preflight (D-21), the raw schema across
eras and the SQL shape (D-02, D-03, D-18, D-19, D-25), the symbol notation (D-15), day-page
resume (D-06), and the per-batch count probe that feeds the plan-03 guard (D-16, D-24).

Purpose: this is the only code that talks to WRDS. A mistake here either leaks a
credential, spams the user's phone with Duo pushes, or silently corrupts the raw tier that
every later re-resample depends on.

Output: the finished `quantlab/acquisition/wrds_taq.py`, extended fakes, and
`tests/test_wrds_taq_acquisition.py`. Every test is offline.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-CONTEXT.md
@.planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-RESEARCH.md
@.planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-PATTERNS.md
@.planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-LIVE-CHECK-1.json
@.planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-LIVE-CHECK-2.json
@.planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-01-SUMMARY.md
</context>

<environment_notes>
- Run commands from YOUR working-tree root: `uv run pytest -q tests/test_wrds_taq_acquisition.py`. Never `cd` to the main-tree absolute path.
- A `-k` selector matching nothing exits 5; treat as failure.
- The autouse tripwire from plan 01 makes `psycopg2.connect` raise in every test. Tests that exercise the REAL `WrdsSession` install their own `FakeConnection` with `monkeypatch.setattr("psycopg2.connect", ...)` inside the test body; that local override is the only sanctioned way past the tripwire.
- `psycopg2.sql.Composed.as_string()` needs a live connection; tests render composed SQL with `render_composed()` from tests/wrds_fixtures.py instead.
- Never use `git stash`.
</environment_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WrdsSession credential and connection contract (D-07, D-20, D-21 classification)</name>

  <files>quantlab/acquisition/wrds_taq.py, tests/wrds_fixtures.py, tests/test_wrds_taq_acquisition.py</files>

  <read_first>
    - quantlab/acquisition/wrds_taq.py (as landed by plan 01) — `WrdsSession`, `WrdsTaqNbboAcquisition.__init__`
    - quantlab/base/acquisition.py lines 748-860 (`_scrub`, `_classify_error` contract: "quota" = global stop, excluded from the manifest) and 1884-2000 (`_attempt_batch` handling of each status)
    - tests/test_alpaca_acquisition.py lines 296-325 (no credential reaches the config or a scrubbed message)
    - .planning/phases/03.9-.../03.9-RESEARCH.md § Pattern 2 and § Code Examples ".pgpass pre-check"
    - .venv/lib/python3.13/site-packages/wrds/sql.py lines 18-21 (the constants to pin against), 111-135 and 198-215 (the interactive prompts this provider must never reach)
  </read_first>

  <behavior>
    With a local `FakeConnection` (records connect kwargs, `set_session` args, executed SQL, `close()`), `WRDS_USERNAME=test-wrds-user-not-real`, and a tmp `PGPASSFILE`:
    - valid pgpass line `wrds-pgdata.wharton.upenn.edu:9737:wrds:test-wrds-user-not-real:SENTINEL-PW` mode 0600 -> first query connects once; kwargs host/port/dbname/user/sslmode exactly the pinned values; no `password` key; `set_session(readonly=True, autocommit=True)`; three more queries -> still one connect.
    - wildcard line `*:*:*:test-wrds-user-not-real:x` and an escaped `\:` in the password field are accepted.
    - pgpass missing / mode 0644 / no matching line -> RuntimeError before connect (FakeConnection never constructed); messages name the path and the fix (`chmod 600`, the expected line shape with `<password>` as placeholder); `SENTINEL-PW` absent from every message and every loguru record.
    - `PGHOST=evil.example` -> connect still gets the pinned host. `PGHOSTADDR` or `PGSERVICE` set -> RuntimeError naming the variable, no connect.
    - `builtins.input` and `getpass.getpass` monkeypatched to raise -> never triggered on any path above.
    - FakeConnection raising `psycopg2.OperationalError` on a query -> that call raises `WrdsSessionError`; the NEXT call raises `WrdsSessionError` too and `psycopg2.connect` is not called again.
    - `WrdsSession.HOST/PORT/DBNAME` equal both the live literals and `wrds.sql.WRDS_POSTGRES_HOST/PORT/DB` (the test may import wrds.sql; the module under test may not).
    - `_classify_error`: `WrdsSessionError`, `WrdsEntitlementError`, `psycopg2.OperationalError`, `psycopg2.InterfaceError` -> "quota"; `ValueError` / `RuntimeError` -> "failed".
    - AST: `wrds_taq.py` has no import whose top-level module is `wrds`, no name or attribute `Connection` anywhere, and reaches connect only as the attribute `psycopg2.connect`.
  </behavior>

  <action>
Write the tests first (RED), then in `quantlab/acquisition/wrds_taq.py`:
- Add `class WrdsSessionError(RuntimeError)` and `class WrdsEntitlementError(RuntimeError)`.
- `WrdsSession._pgpass_path()` = `$PGPASSFILE` or `~/.pgpass`; `_assert_pgpass_entry()` checks existence, regular file, `st_mode & 0o077 == 0`, and a line whose first four `\:`-aware fields match (host, port, dbname, username) with `*` wildcards; parse only fields 1-4 and never bind field 5 to a name (per D-07). Messages refer to the username as `$WRDS_USERNAME`, never its value.
- `REFUSED_ENV = ("PGHOSTADDR", "PGSERVICE", "PGSERVICEFILE")`: any set -> RuntimeError naming it (libpq would otherwise route the connection or its parameters around the pinned host).
- `_connection()`: refused-env check, pgpass check, then exactly one `psycopg2.connect(...)` per session object (`_connect_attempted` flag set BEFORE the call, so a failed connect is not retried), then `set_session(readonly=True, autocommit=True)`. Wrap every query method so `psycopg2.Error` becomes `WrdsSessionError` (message scrubbed of nothing secret: libpq errors do not carry the password; still never interpolate the pgpass line) and marks the session broken; a broken session raises `WrdsSessionError("the WRDS session broke; re-run to resume ...")` without reconnecting.
- `WrdsTaqNbboAcquisition._classify_error(exc)`: the four classes above -> "quota"; everything else -> base behaviour. Docstring: in this vendor "quota" means "global stop" — a dead single session or a missing entitlement is never one symbol's fault (D-21), and retrying batch by batch would reconnect (Duo push) each time.
- Tests also: a planted `WRDS_USERNAME` value is absent from `str(acq.config.to_dict())` and from `acq._scrub(f"... {value} ...")`'s output.
  </action>

  <verify>
    <automated>uv run pytest -q tests/test_wrds_taq_acquisition.py -k "credential or pgpass or connect or session or classify or ast"</automated>
    <fails_when>non-zero exit, exit 5 with "deselected", or fewer than 10 passed</fails_when>
  </verify>

  <acceptance_criteria>
    - `grep -n "class WrdsSessionError\|class WrdsEntitlementError" quantlab/acquisition/wrds_taq.py` returns two lines
    - `grep -n "PGHOSTADDR" quantlab/acquisition/wrds_taq.py` returns at least one line
    - The AST test in tests/test_wrds_taq_acquisition.py passes (no `wrds` import, no `Connection` name)
    - The RED run output is quoted in the SUMMARY
  </acceptance_criteria>

  <done>WrdsSession cannot prompt, cannot be redirected, connects at most once, never reconnects after a break, and its failures stop the whole run instead of defaming symbols.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Raw schema across eras, SQL shape and symbol notation (D-02, D-03, D-05, D-15, D-18, D-19, D-25)</name>

  <files>quantlab/acquisition/wrds_taq.py, tests/wrds_fixtures.py, tests/test_wrds_taq_acquisition.py</files>

  <read_first>
    - quantlab/acquisition/wrds_taq.py — `WrdsSession.where_clause/copy_query/table_identifier`, `_fetch_page`, `symbol_to_pair`, `pair_to_symbol`
    - quantlab/enums/data.py lines 142-209 (`TRADEABLE_TICKER_PATTERN` — dot and hyphen both admitted there)
    - quantlab/base/acquisition.py lines 990-1116 (`_write_shard`: projection then hive partition)
    - 03.9-LIVE-CHECK-1.json L2_columns (2012/2016/2024 column lists), L6_copy (sample CSV incl. NULL sides), L9_suffix; 03.9-LIVE-CHECK-2.json M3/M4 (2016 ties with different content), M6 (NULL/crossed/locked counts)
  </read_first>

  <behavior>
    - A 2016-12-07 page (FakeWrdsSession columns = TAQ_COLUMNS_PRE_2018) and a 2024-01-24 page produce frames whose `schema` is IDENTICAL (names, order, dtypes); the 2016 `time_m_nano` is all null Int16; the SELECT requested for 2016 omits `time_m_nano`.
    - A 2016 fixture with three AAPL rows at the same `time_m` and different prices, in physical order C, A, B -> all three kept, `wrds_row_ord` 0,1,2 in that order, raw not re-sorted.
    - CSV times with and without a fractional part (`09:30:00` and `09:30:00.026490`) both parse.
    - Composed COPY and count queries (rendered with `render_composed`) contain none of the tokens ORDER BY, GROUP BY, DISTINCT, LIMIT, OVER( (case-insensitive); the table part is `Identifier("taqm_2016", "complete_nbbo_20161207")`; roots arrive as one `Literal` list; the pair list uses `coalesce(sym_suffix, '')`.
    - `symbol_to_pair("BRK.B") == ("BRK", "B")`; `symbol_to_pair("AAPL") == ("AAPL", None)`; a CSV row with empty `sym_suffix` becomes symbol "AAPL"; `BRK`/`B` becomes "BRK.B"; requesting `BRK-B` raises ValueError mentioning the dot notation; a symbol with two dots raises.
    - If the fake returns a `BRK`/`A` row when only `BRK.B` was requested -> the page raises ValueError (the batch fails, nothing written for it).
    - NULL sides: an L6 row with empty ask -> `best_ask` and `best_asksizeshares` null; a crossed row (bid > ask) and a locked row (bid == ask) are present in raw.
    - Timestamp reconstruction: 2024-03-11 09:30:00.000000 nano 7 -> 2024-03-11T13:30:00.000000007 naive UTC; a row whose `date` field differs from the table day -> ValueError.
  </behavior>

  <action>
Tests first (RED). Then in `wrds_taq.py`:
- Build SELECT columns from the INTERSECTION of `WrdsTaqNbboAcquisition.TAQ_COLUMNS` (the 13 live names, class constant) and `session.table_columns(day)`, in TAQ_COLUMNS order; a table lacking a column other than `time_m_nano` raises ValueError naming the table and the missing columns (a schema drift the D-18 evidence did not show must fail loudly, not be null-filled).
- `symbol_to_pair`: refuse any symbol containing "-" (message: WRDS/TAQ queries use the constituent universes' dot notation, e.g. BRK.B), refuse more than one "." ; `_fetch_page` checks every returned (sym_root, coalesce(sym_suffix,'')) against the requested pairs and raises on a stranger; checks every row's `taq_date` equals the table day and the derived ET session date of `timestamp` equals it too.
- Keep `wrds_row_ord` assignment as the first frame operation after parsing; no sort, no unique, no filter on any record anywhere in the acquisition path.
- In `tests/wrds_fixtures.py` add `render_composed(obj) -> str` (walks `psycopg2.sql` SQL / Identifier / Literal / Composed without a connection) and let `FakeWrdsSession.copy_nbbo_csv` build its SQL through the REAL static `WrdsSession.copy_query` and record the rendered text in `copy_calls`, so the shape test covers what the acquisition actually requests.
  </action>

  <verify>
    <automated>uv run pytest -q tests/test_wrds_taq_acquisition.py -k "schema or era or sql or tie or symbol or suffix or null or crossed or timestamp"</automated>
    <fails_when>non-zero exit, exit 5 with "deselected", or fewer than 10 passed</fails_when>
  </verify>

  <acceptance_criteria>
    - The 2016 and 2024 frames compare equal on `frame.schema` in a test
    - No composed WRDS query renders any of ORDER BY / GROUP BY / DISTINCT / LIMIT (test asserts over every recorded copy and count call)
    - `uv run pytest -q tests/test_nbbo_dataset.py -k tracer` still exits 0
  </acceptance_criteria>

  <done>The raw tier is era-stable, order-preserving, tie-preserving and faithful to NULL sides, and the SQL can only ever be a filtered SELECT.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Day-page resume, entitlement preflight and the per-batch count probe (D-06, D-21, D-24)</name>

  <files>quantlab/acquisition/wrds_taq.py, tests/wrds_fixtures.py, tests/test_wrds_taq_acquisition.py</files>

  <read_first>
    - quantlab/base/acquisition.py lines 1180-1300 (`_fetch_batch`, ledger resume point, repeated-token guard), 1348-1540 (`_run` resume loop, manifest writing), 1540-1620 (`_run_once`, `max_workers`)
    - quantlab/base/pageledger.py lines 80-120 (`batch_key`) and 220-240 (`resume_point`)
    - quantlab/acquisition/sql_volume.py (plan 03, if merged; otherwise its plan) — the probe's output feeds `assert_acquisition_volume_fits(rows_by_day, ...)`
    - tests/test_alpaca_acquisition.py lines 602-680 (per-batch failure isolation idiom)
  </read_first>

  <behavior>
    - Window 2024-01-24..2024-01-26 with fake trading days [24, 25, 26] and symbols AAPL, MSFT, batch_size 25 -> 3 COPY calls, one per day, each for both pairs; shards under three `date=` directories; one watermark per symbol.
    - Same window, fake `raise_on={1: RuntimeError("synthetic")}` (day 25) -> run 1: AAPL/MSFT in the manifest (per-batch failure), day 24's page in the ledger; run 2 (raise cleared) -> COPY calls only for days 25 and 26; final rows equal an uninterrupted run's rows.
    - Same window, `raise_on={1: WrdsSessionError(...)}` -> run 1 stops, `last_result.quota_aborted` is True, the manifest has NO entry for AAPL/MSFT; run 2 resumes at day 25.
    - `kwargs={"data_type": "nbbo", "max_workers": 4}` -> ValueError at construction.
    - Entitlement: fake `entitled_years={2016, 2024}`, window 2012-01-03..2012-01-05 -> `download()` raises WrdsEntitlementError naming `taqm_2012` and the subscription; zero COPY calls; no manifest entries. Window spanning 2023-12-29..2024-01-03 checks both 2023 and 2024.
    - Page completeness: with the fake's `count_rows` for day 25 returning one more than the rows its COPY returns -> that page raises ValueError naming the day and both numbers, the batch lands in the manifest, and the next run re-fetches day 25; with `kwargs["verify_page_counts"] = False` no count query is issued during the pull.
    - `WrdsNbboVolumeProbe(session, batch_size=1).count_rows_by_day(["AAPL","BRK.B"], "2024-01-24", "2024-01-25")` -> `{"2024-01-24": n1, "2024-01-25": n2}` summed over two batches per day; every recorded count query renders a `sym_root = ANY(` predicate and the same WHERE text as the COPY for that batch; entitlement is checked first (unentitled -> WrdsEntitlementError, zero counts issued).
  </behavior>

  <action>
Tests first (RED). Then:
- `WrdsSession.has_schema_usage(year)` (`SELECT has_schema_privilege(%s, 'USAGE')`; a missing schema counts as not entitled), `assert_entitled(years)` raising `WrdsEntitlementError` listing every unentitled `taqm_YYYY` and saying the WRDS TAQ subscription does not cover it (D-21), `count_query(day, pairs)` (`SELECT count(*) FROM <table> WHERE <where_clause>`) and `count_rows(day, pairs) -> int`.
- Override `WrdsTaqNbboAcquisition._run(self, symbols, from_watermark)`: `self._session.assert_entitled(years of [config.start_date, config.end_date])` then `return super()._run(symbols, from_watermark)`. The raise propagates out of `download()`/`refresh()` before any batch is dispatched.
- Page completeness check (RESEARCH § Pattern 6 "reuse the counts as a completeness check"): knob `verify_page_counts` (default True) — in `_fetch_page`, `count_rows(day, pairs)` before the COPY and compare with the parsed row count; a mismatch raises ValueError naming the day and both numbers (a per-batch "failed", retried next run). Not adopted from the same section: a JSON cache of probe counts — it only saves re-count seconds on a re-run and adds a second state file beside the watermarks; say so in the probe's docstring.
- `class WrdsNbboVolumeProbe`: `__init__(self, session, batch_size=WrdsTaqNbboAcquisition.DEFAULT_BATCH_SIZE)`; `count_rows_by_day(symbols, start_date, end_date) -> dict[str, int]`: assert entitled, list trading days via the session, and for each day and each consecutive batch of `batch_size` symbols (same chunking as `Acquisition._batches`) add `session.count_rows(day, pairs)`; log progress every N days with loguru. Never issue a count without pairs (ValueError on an empty batch). Docstring: per-batch counts are the D-24 calibration (< 1 s each on Citus columnar chunk-group filters); a whole-table count takes ~76 s and is never issued.
- Extend `FakeWrdsSession`: `entitled_years` (None = all), `raise_on: dict[int, BaseException]` keyed by COPY call index (the failing call is recorded and does not consume data), `count_calls`, `has_schema_usage`, `assert_entitled` (delegating to the REAL `WrdsSession.assert_entitled` logic via a shared helper or by calling the real classmethod-free function — keep one implementation of the message), `count_rows` (row count of the pairs' stored rows).
  </action>

  <verify>
    <automated>uv run pytest -q tests/test_wrds_taq_acquisition.py</automated>
    <fails_when>non-zero exit, or any failed/errored test in the summary line</fails_when>
    <automated>uv run pytest -q tests/test_nbbo_dataset.py tests/test_acquisition_batching.py tests/test_source_registry.py</automated>
    <fails_when>non-zero exit, or any failed test in the summary line</fails_when>
  </verify>

  <acceptance_criteria>
    - `grep -n "class WrdsNbboVolumeProbe" quantlab/acquisition/wrds_taq.py` returns one line
    - `grep -n "has_schema_privilege" quantlab/acquisition/wrds_taq.py` returns at least one line
    - The resume test asserts the second run's COPY calls are exactly days 2024-01-25 and 2024-01-26
    - Full suite: failing ids ⊆ 03.9-BASELINE-FAILURES.txt
  </acceptance_criteria>

  <done>Pulls page by trading day and resume at the failed day; an unentitled year stops the run before any data query; the probe produces per-day counts the guard can price.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process env / ~/.pgpass -> libpq | username from env, password only inside libpq |
| quantlab -> WRDS PostgreSQL (TLS) | every query crosses here; each connection may push Duo |
| symbol list -> SQL | universe or CLI symbols become SQL literals |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.9-10 | Information Disclosure | pgpass pre-check / exception text | high | mitigate | parse fields 1-4 only; messages use `$WRDS_USERNAME` symbolically; sentinel-password test over messages and loguru records; `_scrub` with CREDENTIAL_ENV_VARS |
| T-03.9-11 | Spoofing | connection target via PGHOSTADDR/PGSERVICE | high | mitigate | refuse to connect when either is set; host/port/dbname passed explicitly from pinned constants; PGHOST ignored by construction |
| T-03.9-12 | Denial of Service | Duo push storm (reconnect per batch) | high | mitigate | one connect per session object, flag set before the call, no reconnect after a break; max_workers != 1 refused; session failures classified as a global stop |
| T-03.9-13 | Spoofing | interactive credential prompt | high | mitigate | wrds package never imported; tests make input()/getpass raise on every path |
| T-03.9-14 | Tampering | SQL injection through symbols/dates | high | mitigate | `_validate_symbols` (TRADEABLE_TICKER_PATTERN) + hyphen/dot refusal before SQL; values only as `sql.Literal`, tables only as `sql.Identifier` from a `date` |
| T-03.9-15 | Spoofing | TLS without certificate verification (`sslmode=require`) | medium | accept | locked by D-20 and WRDS's own client uses the same mode; no password crosses in clear (libpq auth over TLS); revisit with verify-full if WRDS publishes a CA bundle |
| T-03.9-16 | Repudiation | failure attribution | low | mitigate | entitlement/session failures never land in `_failures.json`; per-batch data errors do, with scrubbed messages |
</threat_model>

<verification>
- `uv run pytest -q tests/test_wrds_taq_acquisition.py tests/test_nbbo_dataset.py` green.
- Full suite: failing ids ⊆ 03.9-BASELINE-FAILURES.txt (run from the worktree root, then re-run in the main tree after merge).
</verification>

<success_criteria>
The WRDS provider is safe to point at a real account: it cannot prompt, leak, redirect or storm Duo, it fails fast on missing entitlement, and its raw tier is complete, ordered and era-stable.
</success_criteria>

## Artifacts this plan produces

- `quantlab/acquisition/wrds_taq.py`: `WrdsSessionError`, `WrdsEntitlementError`, `WrdsSession.REFUSED_ENV`, `_pgpass_path()`, `_assert_pgpass_entry()`, `has_schema_usage()`, `assert_entitled()`, `count_query()`, `count_rows()`; `WrdsTaqNbboAcquisition.TAQ_COLUMNS`, `_run()` override, `_classify_error()`, `verify_page_counts` kwargs knob (default True); `WrdsNbboVolumeProbe.count_rows_by_day()`
- `tests/wrds_fixtures.py`: `FakeConnection`, `FakeCursor`, `render_composed()`, FakeWrdsSession `entitled_years` / `raise_on` / `count_calls` / `has_schema_usage()` / `assert_entitled()` / `count_rows()`
- `tests/test_wrds_taq_acquisition.py`

<output>
Create `.planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-04-SUMMARY.md` when done
</output>
