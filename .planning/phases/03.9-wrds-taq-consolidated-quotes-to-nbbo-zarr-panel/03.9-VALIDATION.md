---
phase: "03.9"
slug: "wrds-taq-consolidated-quotes-to-nbbo-zarr-panel"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: "2026-09-19"
---

# Phase 03.9 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source: `03.9-RESEARCH.md` § Validation Architecture, amended by the live WRDS checks
> (`03.9-CONTEXT.md` D-18..D-28): `complete_nbbo` has **no `qu_seqnum`**; the tie-breaker is
> `time_m_nano` (2018+) then the arrival ordinal `wrds_row_ord` (all years).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 9.1.1 |
| **Config file** | `pyproject.toml` `[tool.pytest.ini_options]` |
| **Quick run command** | `uv run pytest -q tests/test_nbbo_resampler.py tests/test_wrds_taq_acquisition.py tests/test_nbbo_dataset.py tests/test_sql_volume_guard.py` (exact file names are the planner's; keep all offline) |
| **Full suite command** | `uv run pytest -q --ignore=tests/test_factor_hierarchy.py` |
| **Baseline** | 55 failed / 1250 passed (pre-existing, commit d07f06e moved `ingest_*.py` to `scripts/`); gate = **no new failures** |
| **Estimated runtime** | quick < 30 s; full ~4–5 min |

---

## Sampling Rate

- **After every task commit:** quick run command.
- **After every plan wave:** full suite in the **main tree** after merge, diffed against the 55-failure baseline list (user memory: worktree verify-path trap).
- **Before `/gsd-verify-work`:** no new failures + all new tests green + user-run live smoke.
- **Max feedback latency:** 30 s (quick).

---

## Per-Task Verification Map

Filled by the planner per task. Required coverage (decision → behaviour → test type):

| Decision | Behaviour to lock | Test type |
|----------|-------------------|-----------|
| D-02/D-18 | `_fetch_page` projects to RAW_COLUMNS incl. `wrds_row_ord`; pre-2018 tables (no `time_m_nano`) yield typed-null nano | unit (fake session) |
| D-03/D-19 | server SQL has no ORDER BY / GROUP BY / DISTINCT; `wrds_row_ord` = arrival order; local sort stable on it; per-bar ambiguous-tie count correct on a synthetic tie fixture | unit |
| D-06 | per-day page walk; resume after failure on day k restarts at day k; shard layout `data_type=nbbo/date=/symbol=` | integration (tmp_path) |
| D-07/D-20 | missing `WRDS_USERNAME` → actionable error; no `input`/`getpass`; `wrds.Connection` never used; one connection per run | unit |
| D-21 | unentitled year → run fails fast, not a per-symbol failure | unit |
| D-22/D-26 | right-closed bars labelled at bar end; `bar_interval` validated; grid spacing equals interval | unit |
| D-09/D-23 | session edges from XNYS: DST (2024-03-11), winter, half day (2024-11-29) | unit |
| D-10/D-25 | crossed/zero dropped, locked kept, NULL side → NaN (not 0), drop counts recorded | unit |
| D-11 | hand-computed snapshot + time-weighted spread/depth fixture | unit |
| D-12 | variable set/names | unit |
| D-13 | carry-forward within day, NaN before first NBBO, no carry across days | unit |
| D-14 | NBBO `_clean` validation | unit |
| D-15 | `BRK.B` ↔ (`BRK`,`B`), NULL suffix → root | unit |
| D-16/D-24 | per-batch `count(*)` guard refuses above 20 GiB, `force` passes; guard module imports no acquisition class (AST) | unit |
| D-17/D-27 | registry descriptor + `build_config` classmethod; `convert()` end-to-end from fixture raw | integration |
| D-28 | autouse tripwire: any `psycopg2.connect` in tests raises | guard (every test) |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

### Planner map (plan/task -> decisions -> automated command)

| Plan-Task | Decisions | Automated command | Status |
|-----------|-----------|-------------------|--------|
| 01-1 (tracer) | D-01/02/04/06/08/11/12/14/17/18/19/22/26/27/28 | `uv run pytest -q tests/test_nbbo_dataset.py` | ⬜ |
| 01-2 | D-17/D-28 (suite integration) | `uv run pytest -q tests/test_source_registry.py tests/test_acquisition_batching.py tests/test_registry_convert.py tests/test_source_inspector.py` + full-suite baseline diff | ⬜ |
| 02-1 | D-23 (human-verify package) | human-check (blocking) | ⬜ |
| 02-2 | D-09/D-23 | `uv run pytest -q tests/test_session_calendar.py` | ⬜ |
| 03-1 | D-16/D-24 | `uv run pytest -q tests/test_sql_volume_guard.py` | ⬜ |
| 03-2 | D-16 (renderer) | `uv run pytest -q tests/test_sql_volume_guard.py -k print` | ⬜ |
| 04-1 | D-07/D-20/D-21 | `uv run pytest -q tests/test_wrds_taq_acquisition.py -k "credential or pgpass or connect or session or classify or ast"` | ⬜ |
| 04-2 | D-02/03/05/15/18/19/25 | `uv run pytest -q tests/test_wrds_taq_acquisition.py -k "schema or era or sql or tie or symbol or suffix or null or crossed or timestamp"` | ⬜ |
| 04-3 | D-06/D-21/D-24 | `uv run pytest -q tests/test_wrds_taq_acquisition.py` | ⬜ |
| 05-1 | D-05/D-10/D-25 | `uv run pytest -q tests/test_nbbo_resampler.py -k "filter or null or crossed or locked or nonpositive or condition"` | ⬜ |
| 05-2 | D-03/D-11/D-19 | `uv run pytest -q tests/test_nbbo_resampler.py -k "tie or ambigu or order or permut"` | ⬜ |
| 05-3 | D-12/13/14/22/26 | `uv run pytest -q tests/test_nbbo_resampler.py tests/test_cleaning.py` | ⬜ |
| 06-1 | D-09/D-10/D-23 | `uv run pytest -q tests/test_nbbo_dataset.py -k "half or dst or session or window or policy or tracer"` | ⬜ |
| 06-2 | D-06/10/13/14/17/26 | `uv run pytest -q tests/test_nbbo_dataset.py` | ⬜ |
| 07-1 | D-15/16/17/20/21/24/26/27 | `uv run pytest -q tests/test_ingest_wrds_taq.py` | ⬜ |
| 07-2 | D-07/D-16/D-20 (structural) | `uv run pytest -q tests/test_ingest_wrds_taq.py -k "guard or force or vendor or factory or apply_data_dir or credential or session"` | ⬜ |
| 07-3 | docs | `test -f example/wrds_taq.md && grep -c wrds_row_ord example/wrds_taq.md` | ⬜ |
| 08-1 | D-28 (main-tree gate) | full suite vs 03.9-BASELINE-FAILURES.txt | ⬜ |
| 08-2 | live smoke (Manual-Only table) | human-check (blocking) | ⬜ |

Baseline failing ids: `03.9-BASELINE-FAILURES.txt` (55 ids, measured on 9d67af4 with `uv run pytest -q --ignore=tests/test_factor_hierarchy.py -p no:cacheprovider`: 55 failed / 1250 passed / 1 skipped).

---

## Wave 0 Requirements

- [ ] shared WRDS test fixtures: fake session, synthetic NBBO day frames (ties in shuffled physical order, crossed/locked/zero/NULL-side rows, pre-open/post-close rows, empty symbol-day, pre-2018 shape without `time_m_nano`, DST + half-day dates)
- [ ] `tests/conftest.py`: autouse `psycopg2.connect` / `wrds.Connection` tripwire; `WRDS_USERNAME` in the credential-env list; `PGPASSFILE` → tmp
- [ ] update subclass-walk / registry enumeration tests for the third vendor
- [ ] `exchange_calendars` import smoke (after the dependency checkpoint)

---

## Manual-Only Verifications

| Behavior | Decision | Why Manual | Test Instructions |
|----------|----------|------------|-------------------|
| Real WRDS pull + resume | D-06/D-19/D-20 | needs the user's Duo push | `scripts/ingest_wrds_taq.py` for AAPL, MSFT, BRK.B on 2024-01-24..25; row count equals probe count (AAPL 1,243,426 on 01-24); re-run skips; kill mid day-2 resumes at day 2 |
| Real panel sanity | D-11/D-22 | real data | 1m panel: 390 labels/day, `symbol` dtype str, AAPL spread ≈ 0.01, spot-check one bar vs a manual query |
| Pre-2018 tie flag | D-19 | real data | one 2016 day: ambiguous-tie count > 0 on some bars and 0 on a 2024 day |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30 s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
