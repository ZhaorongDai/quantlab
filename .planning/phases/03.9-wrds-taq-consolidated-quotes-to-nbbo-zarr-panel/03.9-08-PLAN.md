---
phase: "03.9"
plan: "08"
type: execute
wave: 5
depends_on: ["03.9-07"]
files_modified: []
autonomous: false
requirements: [D-06, D-11, D-19, D-20, D-21, D-22, D-24, D-28]
user_setup:
  - service: wrds
    why: "Live WRDS pull for the final smoke (triggers one Duo push)"
    env_vars:
      - name: WRDS_USERNAME
        source: "Your WRDS account username; the password stays in ~/.pgpass (mode 0600, line wrds-pgdata.wharton.upenn.edu:9737:wrds:<username>:<password>)"

estimate:
  tokens: 25000
  raw_tokens: 25000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "In the MAIN tree after all merges, the full suite's failing ids are a subset of 03.9-BASELINE-FAILURES.txt and every new 03.9 test file is green (D-28)"
    - "A real run for AAPL, MSFT, BRK.B over 2024-01-24..2024-01-25 lands raw shards whose AAPL 2024-01-24 row count equals the live count 1,243,426, with one Duo push for the whole run (D-06, D-20)"
    - "Re-running the same command skips via watermarks; a run killed during day 2 resumes at day 2 (D-06)"
    - "--to-zarr --bar-interval 1m gives 390 labels per day ending 21:00Z, a str symbol coordinate and a plausible AAPL spread (about 0.01); one bar's snapshot matches a manual query of the last record at or before its label (D-11, D-22)"
    - "A 2016 day shows n_ambiguous_ties > 0 on some bars while the 2024 days show 0 everywhere; a 2012 window is refused as unentitled (D-19, D-21)"
  artifacts: []
  key_links:
    - from: "scripts/ingest_wrds_taq.py"
      to: "WRDS PostgreSQL (taqm_2024.complete_nbbo_20240124)"
      via: "the one live session opened by the user's run"
---

<objective>
Close the phase with the two checks automation cannot make: the no-new-failures gate
re-run in the MAIN tree after every worktree merge (a worktree green is not the gate),
and the user-run live WRDS smoke from 03.9-VALIDATION.md's Manual-Only table.

Purpose: every earlier test is offline by design (D-28). Only a real pull proves the
schema, ordering, entitlement and volume assumptions against WRDS itself.

Output: no code. The SUMMARY records the gate result, the measured row counts, the
measured raw bytes per row (for recalibrating `SqlVolumeGuard.DEFAULT_BYTES_PER_ROW` in a
follow-up if it differs materially from 30), and the user's observations.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-VALIDATION.md
@.planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-LIVE-CHECK-1.json
@.planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-07-SUMMARY.md
@example/wrds_taq.md
</context>

<environment_notes>
- Task 1 runs in the MAIN working tree after all 03.9 worktrees are merged (user memory: an isolated verify tests unmodified code and reports a false green). Run from the repository root.
- The executor NEVER connects to WRDS. Task 2 is performed by the user in their own shell; each connection can push Duo to their phone.
</environment_notes>

<tasks>

<task type="auto">
  <name>Task 1: Main-tree no-new-failures gate and new-suite check</name>

  <precondition>Plans 03.9-01 through 03.9-07 are merged into the branch this task runs on (their SUMMARY files exist).</precondition>

  <files>(none — verification only)</files>

  <read_first>
    - .planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-BASELINE-FAILURES.txt
    - .planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-VALIDATION.md
  </read_first>

  <action>
From the repository root of the main tree: run the new 03.9 suites (`tests/test_nbbo_dataset.py tests/test_nbbo_resampler.py tests/test_wrds_taq_acquisition.py tests/test_sql_volume_guard.py tests/test_session_calendar.py tests/test_ingest_wrds_taq.py`), then the full suite with `-p no:cacheprovider`, extract the sorted `FAILED` ids and compare with `comm -13` against 03.9-BASELINE-FAILURES.txt. Record pass/fail counts and the comm output in the SUMMARY. Any new failing id blocks Task 2 and is reported as a gap.
  </action>

  <verify>
    <automated>uv run pytest -q tests/test_nbbo_dataset.py tests/test_nbbo_resampler.py tests/test_wrds_taq_acquisition.py tests/test_sql_volume_guard.py tests/test_session_calendar.py tests/test_ingest_wrds_taq.py</automated>
    <fails_when>non-zero exit, or any failed/errored test in the summary line</fails_when>
    <automated>uv run pytest -q --ignore=tests/test_factor_hierarchy.py -p no:cacheprovider</automated>
    <fails_when>`comm -13 03.9-BASELINE-FAILURES.txt <sorted FAILED ids>` prints any line, or any ERROR line appears</fails_when>
  </verify>

  <acceptance_criteria>
    - The six 03.9 test files pass in the main tree
    - The comm comparison prints nothing; the SUMMARY quotes the final "N failed, M passed" line
  </acceptance_criteria>

  <done>The merged main tree has no failure outside the 55-test baseline and all phase tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: User-run live WRDS smoke (one Duo push per command)</name>
  <what-built>The full WRDS TAQ NBBO pipeline: scripts/ingest_wrds_taq.py -> WrdsSession (psycopg2, one connection) -> raw shards -> NbboPanelDataset -> Zarr + filter-stats sidecar.</what-built>
  <how-to-verify>
    Run these yourself (the agent never connects). `export WRDS_USERNAME=<your username>` first; approve the Duo push once per command.
    1. `uv run python scripts/ingest_wrds_taq.py --symbols AAPL,MSFT,BRK.B --start-date 2024-01-24 --end-date 2024-01-25`
       Check: the printed estimate; one Duo push only; shards under `data/downloads/us_equity/tick/wrds_taq/wrds/data_type=nbbo/date=2024-01-24/symbol=AAPL/`; the AAPL 2024-01-24 row count equals 1,243,426 (`uv run python -c "import polars as pl; print(pl.scan_parquet('data/downloads/us_equity/tick/wrds_taq/wrds/data_type=nbbo/date=2024-01-24/symbol=AAPL/*.pqt').select(pl.len()).collect())"`); watermarks under `.../_watermarks/wrds/nbbo/`. Note the AAPL shard size on disk / rows = measured bytes per row.
    2. Re-run the same command: every symbol is skipped as covered. Then delete `_watermarks/wrds/nbbo/` for one symbol, start the command, and interrupt it (Ctrl-C) during day 2; the next run resumes at day 2 (no day-1 COPY in the log).
    3. Add `--to-zarr --bar-interval 1m`: open the store with `uv run python -c "import xarray as xr; d=xr.open_zarr('data/data/us_equity/tick/wrds_nbbo_1m.zarr'); print(d.sizes, d.symbol.values, float(d.spread.sel(symbol='AAPL').median()))"`. Expect 780 timestamps (390 per day, last 21:00), symbols ['AAPL','BRK.B','MSFT'], AAPL median spread about 0.01. Spot-check one bar: its bid/ask equal the last record at or before the label in the raw shard.
    4. `--symbols AAPL --start-date 2016-12-07 --end-date 2016-12-07 --to-zarr` (2016 is entitled): `n_ambiguous_ties` > 0 on some AAPL bars; the 2024 store shows 0 everywhere.
    5. `--symbols AAPL --start-date 2012-01-03 --end-date 2012-01-03`: refused before any count or pull with a message naming `taqm_2012`.
    Report the numbers from 1, 3 and 4 and anything unexpected.
  </how-to-verify>
  <resume-signal>Type "approved" with the measured AAPL row count and bytes/row, or describe the discrepancy.</resume-signal>
  <verify>
    <human-check>The user ran steps 1-5 and reported: row count 1,243,426 for AAPL 2024-01-24, one Duo push per command, resume at day 2, 390 labels/day, ambiguity > 0 only on the 2016 day, and the 2012 refusal.</human-check>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the user's reported numbers (row count, bytes/row, labels per day, median spread, ambiguous-tie observation)
    - A discrepancy is recorded as a gap for /gsd-verify-work, not fixed inside this plan
  </acceptance_criteria>
  <done>The user confirmed the live pipeline matches the offline contract, or the discrepancies are recorded as gaps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user shell -> WRDS | the only place in this phase where a real connection is made |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.9-27 | Denial of Service | Duo pushes during the smoke | low | mitigate | five commands, one session each; the user runs them, never the executor |
| T-03.9-28 | Information Disclosure | pasting command output into the SUMMARY | low | mitigate | the scripts print no credential; the SUMMARY records counts and observations only |
</threat_model>

<verification>
- Task 1 gate green in the main tree.
- User approval of the live smoke recorded.
</verification>

<success_criteria>
The phase passes its no-new-failures gate in the main tree and the user has seen the pipeline work against real WRDS data.
</success_criteria>

## Artifacts this plan produces

- No code or files; the 03.9-08-SUMMARY.md records the main-tree gate result and the live-smoke measurements (AAPL row count, measured raw bytes per row, labels per day, spread, tie ambiguity, entitlement refusal).

<output>
Create `.planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-08-SUMMARY.md` when done
</output>
