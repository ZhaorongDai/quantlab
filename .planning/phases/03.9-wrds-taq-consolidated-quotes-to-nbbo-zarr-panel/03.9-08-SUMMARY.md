---
phase: "03.9"
plan: "08"
status: complete
completed: 2026-09-19
tasks_completed: 2
tasks_total: 2
---

# Plan 03.9-08 Summary — main-tree gate + live WRDS smoke

## Task 1 — no-new-failures gate in the MAIN tree

Run after every worktree merge (plans 01–07), in `/Users/daizhaorong/projects/quantlab` on `main`
at commit `95440c1` + tracking. Because of the pre-existing KunQuant executor deadlock
(`.planning/todos/pending/2026-09-19-kunquant-executor-destructor-hang.md`), the suite was run as
two processes split at `tests/test_cross_sectional_zscore.py`, each under `perl -e 'alarm 1200'`,
excluding `tests/test_factor_hierarchy.py` (pre-existing collection error):

- part A: 2 failed, 489 passed
- part B: 53 failed, 940 passed, 1 skipped
- union of failing ids = 55, **identical to `03.9-BASELINE-FAILURES.txt`; 0 new failures**
- the phase's six test files together: **168 passed**
  (`test_session_calendar`, `test_sql_volume_guard`, `test_wrds_taq_acquisition`,
  `test_nbbo_resampler`, `test_nbbo_dataset`, `test_ingest_wrds_taq`)

## Task 2 — live WRDS smoke (checkpoint:human-verify, gate blocking)

The user approved the orchestrator running the five commands on their behalf and approved each Duo
push (`WRDS_USERNAME=zhaorongdai`, password from `~/.pgpass`). Measurements:

| Step | Result |
|---|---|
| 1. `--symbols AAPL,MSFT,BRK.B --start-date 2024-01-24 --end-date 2024-01-25` | pre-flight `count(*)` = 4,526,360 rows / 0.13 GiB est.; 3 symbols succeeded, 0 failed, 50 s. **AAPL 2024-01-24 = 1,243,426 rows — exactly the live probe count.** MSFT 669,127; BRK.B 152,608 (vs 152,801 for `sym_root='BRK'` — the 193-row difference is BRK.A, so the suffix filter is right). Watermarks at `…/wrds_taq/_watermarks/wrds/nbbo/{SYMBOL}.json` as `{"start_date","last_date"}` intervals. Raw total 44 MB. |
| **bytes/row** | **measured ≈ 9.9–10.6** (AAPL 9.93, MSFT 10.61, BRK.B 9.47) vs the guard's assumed 30 → the guard over-estimates ~3× (conservative; see follow-up) |
| 2. same command again | `0 symbol(s) succeeded, 0 failed` — every symbol skipped by watermark. |
| 3. `--to-zarr --bar-interval 1m` | store `data/data/us_equity/tick/wrds_nbbo_1m_0930-1600.zarr`, 2 windows written, **780 rows = 390 bars/day × 2**; first label 14:31Z, last 21:00Z (right-closed, bar-end, D-22); `symbol` dtype `StringDType`, symbols `['AAPL','BRK.B','MSFT']`; **AAPL median spread 0.01**; 13 variables (`bid, ask, bid_size, ask_size, mid, spread, spread_bps, imbalance, n_updates, n_ambiguous_ties, tw_spread, tw_bid_size, tw_ask_size`). Filter sidecar totals: records_in 4,526,360; dropped_crossed 954; dropped_locked 0 (locked kept per D-10); one_sided_kept 14; both_null_kept 4. |
| 4. `--symbols AAPL 2016-12-07 --to-zarr` (separate `--data-dir`) | 572,110 records → 390 bars; **n_ambiguous_ties total 39,333, all 390 bars > 0, max 385 per bar** — vs 0 everywhere on the 2024 days. Confirms D-19: pre-2018 has no nanosecond tie-breaker, and the count surfaces it. dropped_crossed 361. |
| 5. `--symbols AAPL 2012-01-03` | refused before any count or pull: *"The WRDS account has no access to taqm_2012: its WRDS NYSE TAQ millisecond subscription does not cover that year… nothing was downloaded."* (D-21) |

The plan's "interrupt during day 2 and resume" step was skipped: interrupting an orchestrator-run
command is not reliable in this harness. Per-day resume is covered by offline tests in plan 04.

## Deviations

- Steps 1–5 were run by the orchestrator with the user's explicit go-ahead rather than typed by the
  user; each Duo push was approved by the user on their phone. Every number above is from a real run.
- Steps 4 and 5 used a scratch `--data-dir` (see gap G-03.9-1 below).

## Gaps / follow-ups (not fixed here)

- **G-03.9-1 (low, usability):** the Zarr store name carries `{bar_interval}_{session}` but not the
  symbol roster, so a second run with a different roster hits the ChunkLedger refusal
  (`refusing to resume … pinned symbol axis has 1 symbol(s) but the ledger … was written against 3`).
  The refusal is correct (it prevents silent column misalignment) but the message should suggest
  `--data-dir` or a roster-aware store name.
- **G-03.9-2 (low, cost):** a fully-covered re-run still executes the per-batch `count(*)` probe
  before discovering every symbol is watermark-skipped. Probe after the coverage filter.
- **G-03.9-3 (low, accuracy):** `SqlVolumeGuard.DEFAULT_BYTES_PER_ROW = 30` vs measured ~10 for NBBO
  shards. Consider a per-data-type default (~12 with headroom) now that a real measurement exists.

## Self-Check: PASSED
