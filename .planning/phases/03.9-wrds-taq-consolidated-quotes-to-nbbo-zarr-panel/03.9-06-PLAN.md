---
phase: "03.9"
plan: "06"
type: execute
wave: 3
depends_on: ["03.9-02", "03.9-05"]
files_modified:
  - quantlab/dataset/nbbo.py
  - tests/test_nbbo_dataset.py
autonomous: true
requirements: [D-06, D-09, D-10, D-13, D-14, D-17, D-22, D-23, D-26, D-29]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "NbboPanelDataset takes every session's open/close from XnysSessionCalendar: a 2024-11-29 1m panel has 210 labels ending 18:00Z and ignores the 13:00-16:00 ET quotes that raw holds; a 2024-03-11 panel starts at 13:31Z (D-09, D-23)"
    - "A raw date=... directory that is not an XNYS session fails the conversion with a ValueError naming the date (D-23)"
    - "An extended window (e.g. session_start 04:00, session_end 20:00) flows through the dataset: 960 1m labels per regular day ending at 20:00 ET (01:00Z next UTC day in winter), pre-market and after-hours quotes become bar state, the window is seeded from the last valid NBBO at or before its start, and on 2024-11-29 the extended window is not truncated at 13:00 (D-29)"
    - "An extended-window panel is identical whether converted with granularity 'day' or 'month', even though one session's labels span two UTC calendar days (D-29, D-06)"
    - "The filter knobs on NbboDatasetConfig reach the resampler through NbboFilterPolicy.from_config (D-10)"
    - "Drop counts are written per (session date, symbol) and as totals to {zarr_file_path}.nbbo_filter_stats.json, a sibling of the store (never inside it, never under the raw root), merged across chunk windows and runs, and exposed in-process as last_filter_stats (D-10)"
    - "A two-day, two-symbol (AAPL, BRK.B) conversion through registry.convert gives the same store for granularity 'day' and 'month'; a resumed run skips recorded windows; the symbol coordinate is stored as variable-length strings (D-06, D-17)"
    - "A symbol with no raw record on a session date is NaN for every variable that day, counts included; state never crosses dates in the stored panel (D-13)"
    - "load_dataset_from_config(dataset.get_config()) rebuilds an NbboPanelDataset with an NbboDatasetConfig carrying bar_interval, session window and filter knobs (D-26)"
  artifacts:
    - path: "quantlab/dataset/nbbo.py"
      provides: "XNYS-backed _session_bounds, policy wiring, filter-stats sidecar, FILTER_STATS_SUFFIX, last_filter_stats"
      contains: "XnysSessionCalendar"
    - path: "tests/test_nbbo_dataset.py"
      provides: "half-day, DST, non-session, multi-day chunked, resume, sidecar, encoding and rebuild tests"
  key_links:
    - from: "quantlab/dataset/nbbo.py::NbboPanelDataset._session_bounds"
      to: "quantlab/dataset/session_calendar.py::XnysSessionCalendar.session_bounds"
      via: "config.session_start / config.session_end"
    - from: "quantlab/dataset/nbbo.py::_raw_data_to_xr_window"
      to: "quantlab/dataset/nbbo_resample.py::NbboResampler.resample_with_stats"
      via: "policy from config; stats merged into the sidecar"
---

<objective>
Finish the NBBO Dataset: replace the tracer's fixed localized window with the XNYS
calendar (half days, DST, non-sessions), wire the filter policy and its drop counts into
a sidecar, and prove the chunked, resumable, multi-day, multi-symbol conversion through
`registry.convert`.

Purpose: plan 02 built the calendar and plan 05 the semantics; this plan is where a user's
`--to-zarr` run actually produces a trustworthy multi-day panel.

Output: finished `quantlab/dataset/nbbo.py` and the dataset-level tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-CONTEXT.md
@.planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-RESEARCH.md
@.planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-02-SUMMARY.md
@.planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-05-SUMMARY.md
</context>

<environment_notes>
- Run commands from YOUR working-tree root: `uv run pytest -q tests/test_nbbo_dataset.py`. Never `cd` to the main-tree absolute path.
- A `-k` selector matching nothing exits 5; treat as failure.
- Build raw fixtures through the real acquisition path (`mock_wrds_session` + `registry.run(WRDS_SOURCE, ...)`) so the shards are exactly what production writes; set the data root with `quantlab.config.set_data_root(tmp_path)`.
- Symbol-encoding helpers are imported as `from conftest import stored_symbol_encoding` (the repo's convention, see tests/test_symbol_coord_encoding.py).
- `exchange_calendars` exists only after plan 02 merged; if `uv run python -c "import exchange_calendars"` fails, stop and report — do not re-add the dependency here.
</environment_notes>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: XNYS session bounds and filter-policy wiring in NbboPanelDataset (D-09, D-10, D-23)</name>

  <precondition>`uv run python -c "from quantlab.dataset.session_calendar import XnysSessionCalendar"` and `uv run python -c "from quantlab.dataset.nbbo_resample import NbboFilterPolicy"` both exit 0 (plans 02 and 05 merged).</precondition>

  <files>quantlab/dataset/nbbo.py, tests/test_nbbo_dataset.py</files>

  <read_first>
    - quantlab/dataset/nbbo.py (plan 01) — `_session_bounds`, config setter, `_raw_axes_in_range`, `_raw_data_to_xr_window`
    - quantlab/dataset/session_calendar.py (plan 02) — `XnysSessionCalendar(session_start, session_end).session_bounds(dates)` contract: accepts windows inside 04:00-20:00 ET, refuses non-sessions, clips only regular-hours edges to the exchange open/close (half days), leaves extended-hours edges unchanged, omits empty clipped windows
    - .planning/phases/03.9-.../03.9-CONTEXT.md D-29 (user decision on the extended window and the half-day edge rule)
    - quantlab/dataset/nbbo_resample.py (plan 05) — `NbboFilterPolicy.from_config`, `resample_with_stats`
    - tests/test_nbbo_dataset.py (the tracer test — must stay green)
  </read_first>

  <behavior>
    - Raw for AAPL on 2024-11-29 with records at 12:59:30 ET and 14:00:00 ET -> 1m panel for that date has 210 labels, last 18:00Z; the 14:00 ET record changes no bar; the 18:00Z bar carries the 12:59:30 state.
    - Raw on 2024-03-11 -> first label 13:31Z, 390 labels.
    - A raw `date=2024-11-28` directory (Thanksgiving) -> `from_raw_data_chunked` raises ValueError naming 2024-11-28 before writing anything.
    - `NbboDatasetConfig(session_start="10:00", session_end="15:30")` on 2024-01-24 -> labels 15:01Z..20:30Z (330); `session_start="03:59"` or `session_end="20:01"` -> ValueError at dataset construction.
    - Extended window (D-29): `NbboDatasetConfig(session_start="04:00", session_end="20:00")` on 2024-01-24 with raw records at 04:00:00.005984 ET (the real L6 one-sided row), 07:15:00 ET and 17:30:00 ET -> 960 labels 09:01Z..2024-01-25T01:00Z; the first bar (09:01Z) carries the 04:00:00.005984 state (bid 180, ask NaN, spread NaN, n_updates 1); the 07:15 and 17:30 records each count in their bar and carry forward; a second fixture with a raw record at 03:59:59 ET and none until 05:00 ET shows bars 09:01Z..10:00Z carrying the 03:59:59 state (the seed), exactly as the pre-open record seeds RTH.
    - Extended window on 2024-11-29 -> 960 labels ending 2024-11-30T01:00Z; a 14:00 ET record changes the bar it falls in (post-close state, not dropped); the same raw under the default RTH config gives 210 labels ending 18:00Z and the 14:00 ET record changes no bar.
    - `NbboDatasetConfig(drop_crossed=False)` keeps a crossed raw record in the panel (its bar's snapshot is the crossed state), default config drops it.
    - The tracer test still passes unchanged.
  </behavior>

  <action>
Tests first (RED). In `quantlab/dataset/nbbo.py`: construct one `XnysSessionCalendar(config.session_start, config.session_end)` per dataset (lazily; construction validates the window, so a bad window fails at dataset construction via the config setter) and make `_session_bounds(dates)` delegate to its `session_bounds`; DELETE the plan-01 localized-window body so exactly one implementation of session edges exists. `_session_dates()` keeps listing raw `date=` directories; dates the calendar omits (empty capped window) contribute no labels. Build the resampler as `NbboResampler(config.bar_interval, NbboFilterPolicy.from_config(config))` and call `resample_with_stats`. Update the class docstring: session edges come from XNYS (D-23); the window defaults to 09:30-16:00 ET (D-09) and may be set anywhere inside 04:00-20:00 ET (D-29); on half days only regular-hours edges are clipped to the early close, extended-hours edges are unchanged; every window is seeded from the last valid NBBO at or before its start, because the raw scan is by session date and raw holds the whole 04:00-20:00 day (D-04). Nothing in the dataset may assume labels fall on the session date's UTC calendar day: an extended close lands on the next UTC day.
  </action>

  <verify>
    <automated>uv run pytest -q tests/test_nbbo_dataset.py -k "half or dst or session or window or policy or tracer or extended"</automated>
    <fails_when>non-zero exit, exit 5 with "deselected", or fewer than 8 passed</fails_when>
  </verify>

  <acceptance_criteria>
    - `grep -n "XnysSessionCalendar" quantlab/dataset/nbbo.py` returns at least one line
    - `grep -n "ZoneInfo\|replace_time_zone" quantlab/dataset/nbbo.py` returns no line (session localization lives only in session_calendar.py)
    - `grep -n "NbboFilterPolicy.from_config" quantlab/dataset/nbbo.py` returns one line
  </acceptance_criteria>

  <done>The panel's session edges are XNYS-correct on half days and DST days, non-sessions are refused, and the config's filter knobs are honoured.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Multi-day chunked conversion, filter-stats sidecar, encoding and rebuild (D-06, D-10, D-13, D-14, D-17, D-26)</name>

  <files>quantlab/dataset/nbbo.py, tests/test_nbbo_dataset.py</files>

  <read_first>
    - quantlab/base/data.py lines 358-640 (`from_raw_data_chunked`: axes, windows, ledger, `_clean`, `widen_and_append`, `last_chunk_result`) and 865-873 (`_widen_fill_values`)
    - quantlab/base/chunking.py lines 145-200 (`plan_from_timestamps`, `ChunkLedger.default_path` = `<store>.chunks.json`)
    - quantlab/utils/atomic.py `write_json_atomically`
    - quantlab/utils/module.py lines 47-75 (`_config_cls_of`, `load_dataset_from_config`)
    - quantlab/acquisition/registry.py lines 425-588 (`convert`)
    - tests/conftest.py lines 1350-1420 (`stored_symbol_encoding`)
  </read_first>

  <behavior>
    Raw via the real acquisition path: AAPL and BRK.B on 2024-01-24 and 2024-01-25 (BRK.B has NO rows on 2024-01-25), bar_interval "5m".
    - `convert(WRDS_SOURCE, cfg, data_type="nbbo", granularity="day")` -> store with 2*78 timestamps, symbols ["AAPL", "BRK.B"]; BRK.B on 2024-01-25 is NaN for all 13 variables including n_updates; AAPL's first 2024-01-25 bar does not carry 2024-01-24 state when 2024-01-25 has no pre-open record.
    - The same with granularity "month" into another store -> `xr.testing.assert_identical` on the two datasets.
    - Extended window (`session_start="04:00"`, `session_end="20:00"`, bar_interval "30m"): granularity "day" and "month" stores are identical although each session's last labels fall on the next UTC calendar day (D-29).
    - A second `convert` over the same config -> `ConversionResult.resumed` True, windows_written 0, store unchanged.
    - `stored_symbol_encoding(store) == "variable_length"`.
    - Sidecar `<store>.nbbo_filter_stats.json` exists beside the store, not inside the store directory and not under the raw root; its `by_session["2024-01-24"]["AAPL"]` counts match a planted crossed record (`dropped_crossed == 1`); `totals` equal the sum over sessions; it is the same after the resumed run; `dataset.last_filter_stats` equals the file content after a run.
    - `load_dataset_from_config(NbboPanelDataset(cfg).get_config())` returns an `NbboPanelDataset` whose config is an `NbboDatasetConfig` with the same bar_interval, session window and filter fields.
    - `from_raw_data_chunked(on_new_listing="widen")` after adding MSFT raw for 2024-01-25 widens the store without raising (the `{}` widen fill values).
    - A config whose symbols tuple is empty -> ValueError before any store is created (no empty store).
  </behavior>

  <action>
Tests first (RED). Then in `quantlab/dataset/nbbo.py`:
- `FILTER_STATS_SUFFIX = ".nbbo_filter_stats.json"`; `filter_stats_path` property = `f"{config.zarr_file_path}{FILTER_STATS_SUFFIX}"`.
- After each window's resample, merge that window's per-(date, symbol) stats into the sidecar: read the existing JSON (absent = empty), overwrite the entries for the window's session dates, recompute `totals`, record the filter policy and bar_interval under `config`, write with `write_json_atomically(path, payload, indent=2, sort_keys=True)`; keep the merged dict on `self.last_filter_stats`. Windows that the ledger skips leave the sidecar untouched.
- Make sure the dense window build reindexes to the pinned symbol list so a symbol absent on a date is all-NaN (counts included), and that `_raw_axes_in_range` refuses an empty symbol list with ValueError.
- Keep `_widen_fill_values()` returning `{}` and add a docstring line why (no `anomaly_flag` in this panel; counts for a newly widened symbol stay NaN over its pre-listing history, which is correct).
- Update the module docstring with the chunking advice: sub-minute bars should convert with `granularity="day"` (a 1s S&P 500 day is ~11.7M bar-rows).
  </action>

  <verify>
    <automated>uv run pytest -q tests/test_nbbo_dataset.py</automated>
    <fails_when>non-zero exit, or any failed test in the summary line</fails_when>
    <automated>uv run pytest -q tests/test_registry_convert.py tests/test_chunked_ingest.py tests/test_symbol_axis_widening.py</automated>
    <fails_when>any failed id not listed in 03.9-BASELINE-FAILURES.txt</fails_when>
  </verify>

  <acceptance_criteria>
    - `grep -n "FILTER_STATS_SUFFIX" quantlab/dataset/nbbo.py` returns at least one line
    - The day-vs-month identity test and the resume test pass
    - Full suite: failing ids ⊆ 03.9-BASELINE-FAILURES.txt
  </acceptance_criteria>

  <done>A multi-day, multi-symbol NBBO conversion is chunked, resumable, granularity-independent, rebuildable from its config, and leaves an inspectable drop-count sidecar beside the store.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| raw tier -> Zarr panel | the panel is what factors and models consume |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.9-20 | Tampering | half-day after-hours quotes entering the session | medium | mitigate | XNYS early-close cap; test with a 14:00 ET record on 2024-11-29 |
| T-03.9-21 | Tampering | sidecar written into the raw root breaking the parquet scan | low | mitigate | sidecar path is a sibling of the Zarr store; test asserts it is under neither the store nor the raw root |
| T-03.9-22 | Denial of Service | an empty store with a float64 symbol axis | low | mitigate | refuse an empty symbol list before writing (empty-store todo) |
</threat_model>

<verification>
- `uv run pytest -q tests/test_nbbo_dataset.py tests/test_session_calendar.py tests/test_nbbo_resampler.py` green.
- Full suite: failing ids ⊆ 03.9-BASELINE-FAILURES.txt.
</verification>

<success_criteria>
The NBBO Dataset converts real-shaped raw into a correct, resumable, XNYS-aware multi-day panel with inspectable drop counts.
</success_criteria>

## Artifacts this plan produces

- `quantlab/dataset/nbbo.py`: XNYS-backed `_session_bounds()`, `FILTER_STATS_SUFFIX`, `filter_stats_path`, `last_filter_stats`, policy wiring, sidecar merge
- `tests/test_nbbo_dataset.py`: half-day, DST, non-session, window, policy, multi-day, granularity-identity, resume, sidecar, encoding, rebuild and widen tests

<output>
Create `.planning/phases/03.9-wrds-taq-consolidated-quotes-to-nbbo-zarr-panel/03.9-06-SUMMARY.md` when done
</output>
