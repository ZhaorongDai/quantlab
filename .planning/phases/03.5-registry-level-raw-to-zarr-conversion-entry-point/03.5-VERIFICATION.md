---
phase: 03.5-registry-level-raw-to-zarr-conversion-entry-point
verified: 2026-09-12T02:51:04Z
status: human_needed
score: 7/7 roadmap success criteria verified; 40/40 plan must-have truths verified
covered_files:
  - ".planning/REQUIREMENTS.md"
  - ".planning/ROADMAP.md"
  - "cal.py"
  - "ingest_alpaca.py"
  - "ingest_tiingo.py"
  - "ingest_us_equity.py"
  - "main.py"
  - "quantlab/acquisition/alpaca.py"
  - "quantlab/acquisition/registry.py"
  - "quantlab/acquisition/tiingo.py"
  - "quantlab/acquisition/universe.py"
  - "quantlab/base/chunking.py"
  - "quantlab/base/config.py"
  - "quantlab/base/data.py"
  - "quantlab/base/progress.py"
  - "quantlab/config/__init__.py"
  - "quantlab/dataset/spot.py"
  - "quantlab/utils/cli.py"
  - "tests/test_chunked_ingest.py"
  - "tests/test_chunked_panel_estimate.py"
  - "tests/test_dataset_hierarchy.py"
  - "tests/test_entry_point_contracts.py"
  - "tests/test_ingest_conversion_gate.py"
  - "tests/test_ingest_shells.py"
  - "tests/test_registry_convert.py"
  - "tests/test_spot_dataset.py"
  - "tests/test_volume_guard.py"
covered_digest: "v1:sha256:8f1575e35e46b936eec1851db15a21c734c1d2069129c0a87b0d93f12d701611"
behavior_unverified: 0
overrides_applied: 0
coincidental_reliance_items:
  - truth: "A window that plans to zero chunks returns chunks == [], max_chunk is None and max_chunk_bytes == 0 without raising, and print_chunk_report renders that report without a KeyError or a TypeError (plan 02, probe row DATA-08/empty)"
    reason: fixture-only
    harden: "The empty-chunks branch is reachable ONLY by monkeypatching plan_calendar — no valid (start, end, granularity) triple produces zero windows. The test establishes a precondition the production path cannot. Either declare the branch defensive-only in the estimator docstring, or drop it and let plan_calendar's non-empty postcondition be the declared precondition."
human_verification:
  - test: "Have the out-of-repo `quantlab-console` import `quantlab.acquisition.registry.convert()` and `UniverseCatalog.estimate_chunked_panel()`, render a ConversionResult and a chunk report in its own UI, and drive one conversion to completion and one to cancellation from the console's own CancelToken."
    expected: "The console converts an acquired raw tier without importing StockDataset, TiingoAcquisition or any ingest_*.py; CVT-02's 'guard and predicted peak visible in the UI' is satisfiable from the returned dict; a console-issued cancel stops the run at a window boundary and the next run resumes."
    why_human: "The phase goal's stated first consumer lives in a second repository this verification cannot reach. Every in-repo proof is a shell that this repo also wrote; only an out-of-repo caller falsifies 'the contract is not written for it'."
  - test: "Start two `convert()` runs against the SAME zarr_file_path concurrently (two processes, same DatasetConfig) and inspect the store and the chunk ledger afterwards."
    expected: "Determine and record what actually happens — duplicated windows, a ledger/store disagreement caught by assert_consistent, or corruption. The phase DECLARES this path unguarded (plan 06 backstop truth: 'two concurrent convert() runs against ONE Zarr store are not serialised and this phase adds no lock')."
    why_human: "Verified structurally — no FileLock/flock/threading.Lock exists in base/data.py, base/chunking.py or registry.py, so the declaration is accurate. But NO test observes the race outcome, and the consequence (a silently mis-ordered append) is exactly the class ChunkLedger exists to refuse. Needs a human decision: accept the documented hazard, or schedule a lock."
  - test: "On a machine holding a PRE-03.5 Tiingo or Alpaca Zarr store (written by the old whole-window `from_raw_data()` path, so it has no chunk-ledger sidecar), run `uv run python ingest_tiingo.py --to-zarr`."
    expected: "ChunkLedger.assert_consistent refuses with 'a store exists at ... but there is no chunk ledger at ...'. Confirm the operator-facing remedy ('Delete the store to rebuild it, or restore the ledger') is acceptable, or commission the migration note (WR-04)."
    why_human: "Reproducing needs a real legacy store on disk; this worktree has no data/ directory. The refusal path is confirmed by reading quantlab/base/chunking.py:338-345, but whether the migration cost is acceptable is the developer's call, not the verifier's."
advisory:
  - finding: "REQUIREMENTS.md Coverage block states 'v1 requirements: 26 total' while the v1 sections actually list 28"
    category: other
    reason: "Plan 03 correctly incremented 24 -> 26 (a +2 delta matching DATA-07/DATA-08), but the 24 baseline was ALREADY 2 short of the real 26 before this phase — confirmed by counting `git show 4f7371d^:.planning/REQUIREMENTS.md`, which lists 26 while claiming 24. The drift is INHERITED, not introduced here. Plan 03's own verify (`grep -E 'v1 requirements: 26 total' | wc -l -> 2`) checked that the number was WRITTEN, not that it was CORRECT. All 28 v1 IDs do appear in the traceability table, so no requirement is actually unmapped."
    evidence_status: "confirmed by count; pre-existing, out of this phase's scope"
---

# Phase 03.5: Registry-level raw→Zarr Conversion Entry Point — Verification Report

**Phase Goal:** A caller that is not one of quantlab's own scripts — the out-of-repo
`quantlab-console` first, but the contract is not written for it — can convert an acquired raw
tier into the Zarr tier through the registry, and can ask what peak RAM that conversion predicts
BEFORE anything is allocated.

**Verified:** 2026-09-12T02:51:04Z
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

**Verdict: goal achieved, with follow-up debt.** Both halves of the goal are real, wired and
behaviourally exercised in the codebase, not merely claimed in a SUMMARY. Three items need a
human because they live outside this repository or outside what a test currently observes.

### Observable Truths — ROADMAP Success Criteria

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC-1 | A conversion can be started through the registry without naming a vendor class, a `Dataset` subclass, or an `ingest_*.py` script at the call site | ✓ VERIFIED | `convert(descriptor, dataset_config, ...)` at `quantlab/acquisition/registry.py:425`, module-level, placed after `run()` and before the bottom vendor-import block per D-03. Behaviourally exercised: I resolved `DataSourceRegistry.get("tiingo")` and enumerated `Capability.dataset_cls` in a fresh process naming no Dataset subclass. `tests/test_registry_convert.py::test_convert_writes_a_real_zarr_store_and_returns_a_conversion_result` and `::test_convert_reaches_the_second_vendor_through_the_same_entry_point` pass. |
| SC-2 | `registry.run()` remains acquisition-only; conversion is a SEPARATE call, never a flag on it | ✓ VERIFIED | `run()` (`registry.py:355`) returns `AcquisitionResult` and its body contains no conversion call; `convert()` is a separate module-level function. Behaviourally pinned by `test_run_still_returns_an_acquisition_result_and_converts_nothing` (passes). |
| SC-3 | There is no `mode` parameter at all — an ABSENT one — and `convert()` does chunked conversion and only chunked conversion (D-06) | ✓ VERIFIED | `inspect.signature(convert).parameters` = `['descriptor','dataset_config','data_type','granularity','on_new_listing','predicted_peak_bytes','reporter','cancel']` — no `mode`. `grep -r ConversionMode` over all `*.py` returns **zero** hits repo-wide (the `Literal["whole-window","chunked"]` type is gone, not merely unused). `add_to_zarr_arg` (`utils/cli.py:167`) takes only `parser`; the two-arm `_TO_ZARR_HELP` dict is gone and one help text survives. |
| SC-4 | The caller can ask what peak a given (roster, window) conversion predicts, WITHOUT starting it — the estimate is a value the caller can render, not a line this layer prints | ✓ VERIFIED | **Behavioural spot-check, run live:** `estimate_chunked_panel("(explicit --symbols list)", "2000-01-01","2024-12-31", granularity="year", bars_per_day=390)` on a 5000-symbol roster returned a **dict** with all **25** chunks present and **all 25 marked over budget**, without raising — which is precisely the case D-10 said the raise-inside-the-loop could not answer. `max_chunk_bytes` = 47,361,600,000. Zero `print(`/`logger.` statements inside the estimator; `grep -rn '^\s*print('` over `quantlab/acquisition/` and `quantlab/base/` returns **nothing**; the renderer `print_chunk_report` lives at `quantlab/utils/cli.py:801` (D-13). |
| SC-5 | A guard refusal names the remedy that would fit rather than only refusing | ✓ VERIFIED | **Behavioural:** `assert_chunked_panel_fits(...)` raised, and the message contains `"Pass a finer --chunk (year -> quarter -> month)"` byte-identically (`universe.py:2057`); every over-budget chunk additionally carries its own `remedy` field (`universe.py:1969`, `_CHUNK_REMEDY` at `:1847`). Pinned by `test_the_refusal_still_names_the_finer_chunk_remedy`. |
| SC-6 | All three ingest shells reach conversion through this entry point; the `--chunk`/`--on-new-listing` divergence disappears. Fourth shell INCOMPLETE by decision (D-09) | ✓ VERIFIED | `ingest_us_equity.py:562`, `ingest_tiingo.py:290`, `ingest_alpaca.py:520` each call `convert(SOURCE, ds_config, granularity=args.chunk, on_new_listing=args.on_new_listing, ...)`. All three call `add_chunk_args(parser)` (`:350` / `:164` / `:317`) from the single definition at `utils/cli.py:255`, so both flags are shared by construction. `test_the_three_conversion_flags_are_shared_by_every_us_equity_shell` pins this structurally via `parser._option_string_actions`. **D-09 carve-out honoured and recorded:** `ingest_binance_spot.py:111` still reads `SpotKlineDataset(config).from_raw_data().save()` — untouched, as decided; the ROADMAP text states the incompleteness in its own SC-6. This is a recorded decision, not a gap. |
| SC-7 | Tick stays refused, not silently converted | ✓ VERIFIED | **Behavioural:** `convert(alpaca_descriptor, tick_config, data_type="trades")` raised `ValueError` naming phase 03.3; the same for `data_type=None`. Alpaca's capability rows enumerate as `1d/bars -> StockDataset`, `1m/bars -> StockDataset`, `tick/quotes -> None`, `tick/trades -> None` — the refusal IS the absent field (D-01). `inspect.getsource(convert)` contains **no** `"tick"` literal, so the refusal is data-driven rather than a frequency branch. |

**Score:** 7/7 roadmap Success Criteria verified (0 present-but-behavior-unverified).

### Observable Truths — Plan `must_haves`

| Plan | Truths / Prohibitions | Status | Evidence |
|------|----------------------|--------|----------|
| 01 | 5 truths, 2 prohibitions | ✓ ALL VERIFIED | `ConversionResult` at `base/data.py:34` (base layer, per D-04) carrying zarr/ledger paths, windows planned/written/skipped, rows, pinned symbols, observed + predicted peak, `resumed`, `cancelled`, `rebuild_rolled_back`. `DatasetConfig.market` reinstated at `base/config.py:43` — and deliberately NOT on `ConstituentDatasetConfig`, which the docstring justifies. Idempotency pinned by `test_a_second_convert_over_the_same_window_writes_nothing_twice`. **Prohibition 1 (no nearest-match fallback):** `if capability.dataset_cls is None: raise` — refusal, never substitution. **Prohibition 2 (convert() must NOT acquire a self-protecting guard):** `convert()`'s full body read — it contains no `assert_*_panel_fits` call. D-11's recorded risk is intact, not silently retired. |
| 02 | 12 truths | ✓ ALL VERIFIED | See SC-4/SC-5 above, plus: `bars_per_day` is keyword-only on both chunked guards and the identity default is pinned **by construction** — live check confirmed `estimate_chunked_panel(...) == estimate_chunked_panel(..., bars_per_day=1)` and `chunks[0]["dense_bytes"]` at 390 is **exactly** 390× the default. The arithmetic is genuinely **MOVED, not re-derived**: diffing the pre-phase in-loop body against `universe.py:1945-1971` shows `trading_days = max(round(...))` identical and `dense_cells` gaining only `* bars_per_day`, so the `round()` half-to-even tie-breaking claim is verified by code identity rather than needing an unreachable tie input. Tie-break is **explicitly documented** at `universe.py:1973-1975` ("`max()` returns the FIRST maximal element … Stated because the tie-break is depended on"), not left to whatever `max()` happened to return. |
| 03 | 5 truths | ✓ ALL VERIFIED (1 advisory) | DATA-07/DATA-08 are in the `### Data` section (`REQUIREMENTS.md:27-28`) in CONTEXT.md's Chinese wording verbatim, in the traceability table mapped to `Phase 03.5` (`:112-113`), and the Coverage block was incremented by exactly +2. ROADMAP 03.5's Goal and SC-3/SC-4/SC-6 are rewritten to record the D-06/D-09 supersessions, and the Requirements line reads plain `DATA-07, DATA-08` with no `(to be added in discuss-phase)` parenthetical. **Advisory:** the Coverage *absolute* is 26 vs an actual 28 — inherited drift, see `advisory:` frontmatter. |
| 04 | 5 truths, 1 prohibition | ✓ ALL VERIFIED | `_raw_data_to_xr_window` is `@abstractmethod` on `MarketDataset` (`base/data.py:1423-1429`) and `BaseDataset` keeps its concrete default (`:437`). The warn-and-degrade block is GONE — the chunk loop now calls `self._raw_data_to_xr_window(...)` unconditionally at `base/data.py:759`. `tests/test_dataset_hierarchy.py:153-156` asserts `BaseDataset.__abstractmethods__ == frozenset({"_raw_data_to_xr"})` AND the three-element `MarketDataset`-minus-`BaseDataset` set — both pinned assertions updated deliberately as D-08 required. `SpotKlineDataset._raw_data_to_xr_window` (`dataset/spot.py:60`) carries the inherited body plus a docstring that says out loud it is "a KNOWN non-memory-bounded implementation". **Prohibition (no silent degradation):** satisfied structurally — a `MarketDataset` subclass omitting the seam cannot be instantiated at all. |
| 05 | 7 truths | ✓ ALL VERIFIED | Shells delegate (SC-6 above). `assert_chunked_panel_fits` replaces `assert_dense_panel_fits` in tiingo (`:241`) and alpaca (`:445`). **Alpaca's minute sizing survives:** `bars_per_day=pricing.BARS_PER_DAY_BY_FREQUENCY[args.frequency]` at `ingest_alpaca.py:453`. **The AST detectors kept their teeth:** `tests/test_volume_guard.py:1379` recognises a bare `ast.Name` call to `convert` as a densifying site, and asserts `densifying >= 2`. The THIRD detector the plan never named — `tests/test_ingest_conversion_gate.py:347` with the stricter `assert densifying == 3` (`:385`) — also recognises `convert()`; both pass. `estimate_chunked_panel` is in the guard-siblings tuple at `test_volume_guard.py:842`. |
| 06 | 6 truths (1 backstop) | ✓ 5 VERIFIED, backstop split | `reporter`/`cancel` are keyword-only `None`-defaulted on `convert()`, forwarded as call arguments into `from_raw_data_chunked` (`registry.py:568-572`). `EVENT_KINDS` gained the four conversion kinds (`base/progress.py:80-83`). `_emit_progress` (`base/data.py:660-261`) catches `Exception`, logs at warning and continues — the never-raises contract, pinned by `test_a_reporter_that_raises_on_every_event_cannot_end_the_conversion`. Cancel is read as **the first thing each iteration does** (`base/data.py:699`), i.e. at a window boundary. `test_neither_handle_is_ever_written_onto_the_config` pins the no-config-assignment prohibition. `run()`'s three-modes docstring sentence is de-staled. **Backstop truth:** its resume and cancel-with-no-partial-window clauses ARE directly observed (`test_a_cancel_after_two_of_four_windows_stops_there_and_resumes` asserts `cancelled is True`, `windows_written == 2`, store holds exactly `2*2` timestamps and no partial third, then the second run reports `windows_skipped == 2, resumed is True`). Its **concurrency clause has no observing test** → routed to human verification. |

**RED→GREEN evidence (plan 06).** The executor reported that `gsd-tools check tdd-red-evidence`
cannot parse pytest output and that RED was produced through a throwaway TAP plugin. The sequence
is nonetheless **genuinely evidenced from git history, independent of that tooling**:
`git show fb85f0b:quantlab/base/data.py` shows `from_raw_data_chunked(self, granularity,
ledger_path, append_dim, on_new_listing)` — **no `reporter`, no `cancel`** — while the 318 lines
of tests added in that same commit call those keywords. Those six tests could only have raised
`TypeError` at that commit. The keyword count in `base/data.py` goes 2 → 14 at the GREEN commit
`f3960f1`.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `quantlab/acquisition/registry.py` | module-level `convert()` + `Capability.dataset_cls` | ✓ VERIFIED | `def convert(` at `:425`; `dataset_cls: type[MarketDataset] \| None = None` at `:109` — on `Capability`, not `SourceDescriptor` (D-01). |
| `quantlab/base/data.py` | `ConversionResult`, `last_chunk_result`, abstract seam, reporter/cancel, rollback | ✓ VERIFIED | `class ConversionResult` at `:34`; `@abstractmethod _raw_data_to_xr_window` at `:1423`; `_restore_rebuild_asides(..., reason="cancelled")` at `:919`. |
| `quantlab/base/config.py` | `DatasetConfig.market` | ✓ VERIFIED | `market: Market` at `:43` on `DatasetConfig`; absent from `ConstituentDatasetConfig` by design. |
| `quantlab/base/progress.py` | four conversion `EVENT_KINDS` | ✓ VERIFIED | `:80-83`. |
| `quantlab/acquisition/universe.py` | `estimate_chunked_panel` + thin `assert_chunked_panel_fits` wrapper | ✓ VERIFIED | `:1851` and `:1986`; the wrapper's first statement is `self.estimate_chunked_panel(...)` — one copy of the arithmetic. |
| `quantlab/utils/cli.py` | `print_chunk_report`, collapsed `add_to_zarr_arg`, `_ExplicitSymbolCatalog._validate_category` | ✓ VERIFIED | `:801`, `:167`, `:598`. |
| `quantlab/dataset/spot.py` | explicit `_raw_data_to_xr_window` | ✓ VERIFIED | `:60`, honest docstring. |
| `ingest_{us_equity,tiingo,alpaca}.py` | conversion delegated, chunk flags, chunked guard | ✓ VERIFIED | See SC-6. |
| `.planning/REQUIREMENTS.md` | DATA-07/DATA-08 in three places | ✓ VERIFIED | `:27-28`, `:112-113`, Coverage `:132`. |
| `tests/test_registry_convert.py` | end-to-end + refusals + reporter/cancel (min 120 lines) | ✓ VERIFIED | 18 tests, all passing. |
| `tests/test_chunked_panel_estimate.py` | boundary/precision/adjacency/empty/ordering/intraday/identity (min 120 lines) | ✓ VERIFIED | 18 tests, all passing. |

### Key Link Verification

| From | To | Via | Status |
|------|----|-----|--------|
| `acquisition/registry.py` | `base/data.py` | `from quantlab.base.data import ConversionResult, MarketDataset` (`:50`) — same direction as `AcquisitionResult` | ✓ WIRED |
| `acquisition/tiingo.py` / `alpaca.py` | `dataset/stock.py` | `Capability(dataset_cls=StockDataset)` — direct class reference, no second registry | ✓ WIRED (confirmed by live capability enumeration) |
| `acquisition/registry.py` | `base/config.py` | `dataset_config.market` as first element of the lookup key | ✓ WIRED (`registry.py:506`) |
| `acquisition/universe.py` → itself | `assert_chunked_panel_fits` → `estimate_chunked_panel` | ✓ WIRED (`universe.py:2023`) |
| `ingest_*.py` | `utils/cli.py` | `print_chunk_report(catalog.assert_chunked_panel_fits(...))` — the guard stays an ARGUMENT | ✓ WIRED (all three) |
| `ingest_{tiingo,alpaca,us_equity}.py` | `acquisition/registry.py` | `convert(SOURCE, ds_config, ...)` | ✓ WIRED |
| `ingest_alpaca.py` | `acquisition/universe.py` | `bars_per_day=pricing.BARS_PER_DAY_BY_FREQUENCY[args.frequency]` | ✓ WIRED (`:453`) |
| `acquisition/registry.py` | `base/data.py` | `reporter=reporter, cancel=cancel` forwarded into the chunk loop | ✓ WIRED (`:570-571`) |
| `base/data.py` | `base/progress.py` | `ProgressEvent` emitted, `CancelToken.is_cancelled()` read; `progress.py` is a leaf → no cycle | ✓ WIRED |
| `dataset/spot.py` | `base/data.py` | satisfies `MarketDataset`'s new abstract member, keeping the class instantiable | ✓ WIRED |
| `.planning/ROADMAP.md` | `.planning/REQUIREMENTS.md` | `DATA-07, DATA-08` now resolve to real entries | ✓ WIRED |

### Data-Flow Trace (Level 4)

| Artifact | Value | Source | Real data? | Status |
|----------|-------|--------|-----------|--------|
| `ConversionResult.windows_written` / `rows_written` / `peak_window_bytes` | counted INSIDE the chunk loop (`base/data.py:672-676`), never reconstructed from the ledger afterwards | live loop accounting | Yes | ✓ FLOWING |
| `ConversionResult.resumed` | `windows_skipped > 0`, derived from real ledger skips | `ChunkLedger` | Yes | ✓ FLOWING |
| `ConversionResult.predicted_peak_bytes` | echoed from the caller via `dataclasses.replace` | caller's own estimate | Yes (by contract) | ✓ FLOWING — see IN-02 |
| `estimate_chunked_panel()["chunks"][n]["dense_bytes"]` | `pinned_symbols * trading_days * bars_per_day * num_variables * bytes_per_value` | real roster size + real calendar | Yes — live run produced 47.4 GB on a 5000-symbol/390-bpd year, not a constant | ✓ FLOWING |
| `Capability.dataset_cls` | direct class reference on descriptor rows | vendor modules | Yes | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full workspace suite (run once) | `uv run pytest -q` | `801 passed, 442 warnings in 52.57s` | ✓ PASS — independently confirms the orchestrator's claim |
| Phase-owned test files | `uv run pytest tests/test_registry_convert.py tests/test_chunked_panel_estimate.py tests/test_ingest_shells.py tests/test_volume_guard.py tests/test_dataset_hierarchy.py tests/test_entry_point_contracts.py tests/test_ingest_conversion_gate.py -q` | `116 passed` | ✓ PASS |
| CR-01 rollback invariant (behaviour-dependent) | `uv run pytest tests/test_chunked_ingest.py -q -k "rebuild or cancel"` | `5 passed` (incl. `test_a_cancelled_rebuild_restores_the_original_store`) | ✓ PASS |
| SC-4: estimator answers without starting and without raising | live `estimate_chunked_panel` on 5000 symbols × 25 years × bars_per_day=390 | dict, 25 chunks, 25 over budget, every one carrying a remedy, no raise | ✓ PASS |
| SC-5: refusal names the remedy | live `assert_chunked_panel_fits` on the same input | raised; message contains the byte-identical remedy sentence | ✓ PASS |
| D-10 amendment: identity default + 390× exactness | live equality + multiplication checks | `identity default equal: True`; `390x exact: True` | ✓ PASS |
| CR-02 regression: explicit-symbol sentinel reaches the chunked guard | live `estimate_chunked_panel(EXPLICIT_SYMBOLS_CATEGORY, ...)` | accepted, no "Unknown universe category" | ✓ PASS |
| SC-7: tick refused programmatically | live `convert(alpaca, tick_cfg, data_type="trades")` and `data_type=None` | both raised, both name phase 03.3 | ✓ PASS |
| SC-3: no mode parameter | `inspect.signature(convert)` + repo-wide `grep -r ConversionMode` | no `mode`; zero `ConversionMode` hits | ✓ PASS |
| CR-03: entry points import | `ast.parse` of `main.py`/`cal.py` + `test_entry_point_contracts.py` (12 root scripts) | parse ok; all parametrised cases pass | ✓ PASS |
| SC-4 layering: no prints below `utils/cli.py` | `grep -rn '^\s*print(' quantlab/acquisition/ quantlab/base/` | no output | ✓ PASS |
| Concurrency lock absence (backstop clause) | `grep -rn 'FileLock\|flock\|threading.Lock' base/data.py base/chunking.py registry.py` | no output — declaration accurate | ✓ PASS (structural); race outcome → human |

### Probe Execution

| Probe | Command | Result | Status |
|-------|---------|--------|--------|
| — | — | No `scripts/*/tests/probe-*.sh` exist and no PLAN/SUMMARY declares one | ? SKIPPED (no probes in this project) |

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|--------------|-------------|--------|----------|
| DATA-07 | 01, 03, 04, 05, 06 | 外部调用方可以不指名 vendor 类、`Dataset` 子类或 `ingest_*.py` 脚本，就通过注册表把 raw parquet 层转换为 Zarr 层，并拿到一个描述本次转换结果的对象 | ✓ SATISFIED | `registry.convert()` + `ConversionResult` carrying windows written/skipped, pinned symbols, both paths, `resumed`, `cancelled`. Registered in `REQUIREMENTS.md:27` and mapped to Phase 03.5 at `:112`. |
| DATA-08 | 02, 03, 05 | 任何调用方可以在任何内存被分配之前，问出一次转换的预测峰值内存与每个超预算窗口的补救建议；该答案是一个可被调用方渲染的值 | ✓ SATISFIED | `estimate_chunked_panel()` returns a dict, never raises for budget, completes the loop, per-chunk `remedy`; rendering lives in `utils/cli.py:print_chunk_report`. Registered at `:28`, mapped at `:113`. |

**Orphaned requirements:** none. `grep -E "Phase 03.5" .planning/REQUIREMENTS.md` returns exactly
DATA-07 and DATA-08, both claimed by plans.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | `TBD` / `FIXME` / `XXX` / `HACK` / `PLACEHOLDER` / `TODO` scan across all 16 phase-modified source files | — | **CLEAN — zero hits.** No unreferenced debt markers; the debt-marker gate does not fire. |
| — | — | Hardcoded credentials scan across `quantlab/`, `ingest_*.py`, `main.py`, `cal.py` | — | CLEAN — no `api_key = "…"` / `secret = "…"` literals. CLAUDE.md's credential constraint holds. |
| `quantlab/base/data.py` | 855-879 | `conversion_finished` is emitted INSIDE the `try:` block, so an exception in the loop bypasses it; `reporter.close()` is never called on any path | ⚠️ Warning | **WR-01 REPRODUCED.** `Acquisition` does call `self._active_reporter.close()` (`base/acquisition.py:713`), so this is a real asymmetry between the two loops. A console attaching a reporter to `convert()` gets `conversion_started` with no terminator on failure, and never gets a release signal. Observability debt on the error path; does not prevent conversion. |
| `quantlab/utils/cli.py` | 887-920 | `print_conversion_result` renders neither `cancelled` nor `rebuild_rolled_back` | ⚠️ Warning | **WR-02 REPRODUCED** — `grep -n 'cancelled\|rebuild_rolled_back' quantlab/utils/cli.py` returns **nothing**. A rolled-back rebuild would print `windows_written`/`rows_written` for work that was thrown away. Mitigating fact I verified: **no shell passes `cancel=` to `convert()`**, so the misrender is unreachable from the CLI today and needs an in-process caller to trigger. The `ConversionResult` object — what the external caller actually consumes — carries both flags correctly. |
| `quantlab/base/progress.py` | 212-235 | `TqdmProgressReporter.emit` handles only `run_started`/`batch_completed`/`quota_exhausted`/`run_finished` | ⚠️ Warning | **WR-03 REPRODUCED.** The default reporter is blind to all four conversion kinds. Mitigating facts I verified: the chunk loop deliberately instantiates **no** default reporter (`_emit_progress` docstring states this), and `CallbackProgressReporter` forwards every event — so the console's path works. Debt only for an in-repo caller who attaches the tqdm reporter to a conversion. |
| `quantlab/base/chunking.py` | 338-345 | A pre-03.5 store with no chunk-ledger sidecar is refused by `assert_consistent` | ⚠️ Warning | **WR-04 REPRODUCED.** Direct consequence of the recorded D-06/D-07 collapse to chunked-only. The refusal is **loud and names a remedy** ("Delete the store to rebuild it, or restore the ledger"), so it is a migration burden, not a silent break, and it does not affect the NEW caller the phase goal is about. Migration note still owed → human verification item 3. |
| `quantlab/base/data.py` | 1423-1429 | `MarketDataset`'s abstract stub shadows `BaseDataset`'s working default in the MRO | ℹ️ Info | **IN-01 — latent, not live.** I traced every `super()._raw_data_to_xr_window(...)` call site (`tests/test_chunked_ingest.py:127,141`, `tests/test_dataset_update.py:148`): all are on `_SpyStockDataset`, which subclasses `StockDataset`, so `super()` resolves to StockDataset's working override. No current caller reaches the `...` stub. |
| `quantlab/acquisition/registry.py` | 580-582 | `dataclasses.replace(...)` returns a new object; `dataset.last_chunk_result` keeps `predicted_peak_bytes=None` | ℹ️ Info | **IN-02 confirmed.** Two objects describe one run and differ. Harmless today (`convert()` drops the dataset); a trap for a future caller that wants the dataset too. |
| `ingest_us_equity.py` | 506-510 | sizes its chunk guard on `args.category` rather than the `--limit`-aware `pricing` view | ℹ️ Info | **IN-03 confirmed.** Error direction is **conservative** — the full category is ≥ the limited roster, so it over-estimates and may over-refuse; it cannot under-refuse. Inconsistent with the other two shells, worth fixing, not a safety gap. |
| `quantlab/base/data.py` | 162, 192, 419 | `#:` comments still describe a `_reset_symbols()` → `read()` path deleted by `df7bfe9` | ℹ️ Info | WINDOWS.md entries 16 confirmed open and accurately worded. |
| `quantlab/acquisition/inspector.py` | 99 | `_RawTierReader._reset_symbols` overrides a method that no longer exists on `BaseDataset` | ℹ️ Info | WINDOWS.md entry 17 confirmed — inert zombie override, and the class docstring (`:79`, `:87`) still describes a construction-time `from_raw_data()` fallback that no longer happens. Out of every 03.5 plan's `files_modified`. |

**WINDOWS.md ledger:** entries 14, 15, 16, 17 (all phase `03.5`, all `open`) were read and each is
**accurately characterised** — the zero-coverage regression guard, the un-run `--dry-run` verify,
the stale `#:` comments, and the zombie override. All four trace to `df7bfe9` deleting
`_reset_symbols` without cleaning up its documentation. Nothing in the ledger is mis-stated or
under-stated.

### Recorded Decisions — NOT gaps

Per the phase's LOCKED CONTEXT, the following are deliberate, documented non-deliveries. I
verified each is genuinely a decision that landed in the ROADMAP text, not a quiet omission:

1. **SC-6's fourth shell (D-09).** `ingest_binance_spot.py` does not join the entry point.
   Verified correct and correctly justified: `SpotKlineDataset` does not bound the densify
   (its own docstring says so), and binance is not a registered source — `registry.py`'s bottom
   block imports only `alpaca` and `tiingo`, so `convert()` structurally cannot reach it. The
   ROADMAP SC-6 text states the incompleteness in its own words.
2. **`convert()` is not self-protecting (D-11).** Verified as an intentional absence: the guard
   is at all three call sites and nowhere inside `convert()`. Plan 01 encoded this as an explicit
   *prohibition*, so a later "helpful" guard inside `convert()` would now fail a test rather than
   silently retire D-11's recorded risk. This is the right shape for an accepted risk.
3. **Three modes → one (D-06).** SC-2's second half and SC-3 were rewritten, not left standing.
   `ConversionMode` is gone from the repository entirely.

### Human Verification Required

See the `human_verification` frontmatter block. Three items:

1. **The out-of-repo `quantlab-console` actually consuming the entry point.** Every in-repo proof
   is a shell this same repo wrote. Only an external caller falsifies "the contract is not written
   for it".
2. **Concurrent `convert()` runs against one Zarr store.** The phase *declares* this unguarded and
   the declaration is structurally accurate (no lock exists). No test observes the outcome. Needs
   a developer decision: accept the documented hazard or schedule a lock.
3. **A pre-03.5 store's migration path (WR-04).** Needs a real legacy store on disk; the refusal
   path is confirmed by reading the code, but whether the "delete and rebuild" cost is acceptable
   is the developer's call.

### Gaps Summary

**No gaps block the phase goal.** Both halves of the goal are delivered, wired, and
behaviourally exercised:

- *"…can convert an acquired raw tier into the Zarr tier through the registry"* — `convert()`
  exists at the registry level, resolves its target from capability data rather than a branch,
  writes a real Zarr store, is idempotent across runs, refuses tick honestly, and returns a
  renderable value object. Proven by 18 passing tests plus live in-process checks.
- *"…can ask what peak RAM that conversion predicts BEFORE anything is allocated"* —
  `estimate_chunked_panel()` answers without raising and without starting, completes the loop so
  the case that matters (every window over budget) is answerable, and carries a remedy per
  over-budget window. Proven live on a 5000-symbol × 25-year × 390-bars/day input.

What remains is **follow-up debt, all of it in the reporting/rendering periphery rather than the
entry point itself**: an unbalanced reporter lifecycle on the exception path (WR-01), a CLI
renderer that ignores two ConversionResult flags (WR-02), a default reporter blind to conversion
events (WR-03), and an owed migration note for pre-03.5 stores (WR-04). None of the four prevents
the goal; all four deserve a follow-up plan. The three Critical review findings were reproduced,
fixed, merged (`3bfb1c6`, `08304cc`, `efef254`) and are now covered by passing tests — I verified
each fix in the code rather than trusting the commit messages.

The status is `human_needed` rather than `passed` solely because three checks cannot be closed
inside this repository.

---

_Verified: 2026-09-11T23:05:00Z_
_Verifier: Claude (gsd-verifier)_
