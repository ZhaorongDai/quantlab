---
phase: "03.5-registry-level-raw-to-zarr-conversion-entry-point"
plan: "05"
type: execute
wave: 2
depends_on: ["03.5-01", "03.5-02"]
files_modified:
  - ingest_us_equity.py
  - ingest_tiingo.py
  - ingest_alpaca.py
  - quantlab/utils/cli.py
  - tests/test_volume_guard.py
  - tests/test_ingest_shells.py
autonomous: true
requirements: [DATA-07, DATA-08]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "All three US-equity ingest shells reach conversion through registry.convert() rather than each constructing StockDataset and calling a densify method themselves — they stay the registry's in-repo consumers and live proof the programmatic path works (SC-6, 03.4 D-15 role b)"
    - "The --chunk / --on-new-listing divergence between ingest_us_equity.py and the other two is gone: all three carry both flags, as an automatic consequence of sharing one conversion path rather than as new surface grown on the shells (SC-6, D-07)"
    - "ingest_tiingo.py and ingest_alpaca.py size their conversion with assert_chunked_panel_fits rather than assert_dense_panel_fits, because the whole-window path they guarded no longer exists (D-07)"
    - "The RAM guard still precedes the densification in every shell: the AST ordering assertion in tests/test_volume_guard.py recognises convert() as a densifying call, so delegation cannot make that test silently cover nothing"
    - "Alpaca's parser-level tick refusal stays where it is and fires before any work — convert()'s programmatic refusal is the second layer for non-CLI callers, not a replacement for the first"
  artifacts:
    - path: "ingest_tiingo.py"
      provides: "conversion delegated to registry.convert(), chunked guard, --chunk/--on-new-listing"
      contains: "convert("
    - path: "ingest_alpaca.py"
      provides: "conversion delegated to registry.convert(), chunked guard, --chunk/--on-new-listing, tick refusal preserved"
      contains: "convert("
    - path: "ingest_us_equity.py"
      provides: "conversion delegated to registry.convert() and the ConversionResult rendered"
      contains: "convert("
    - path: "tests/test_volume_guard.py"
      provides: "a densify detector that recognises the delegated conversion call, so the guard-precedes-densify assertion keeps its teeth"
      contains: "convert"
  key_links:
    - from: "ingest_tiingo.py"
      to: "quantlab/acquisition/registry.py"
      via: "convert(SOURCE, ds_config, ...) replaces the direct StockDataset construction and whole-window densify"
      pattern: "convert\\(\\s*SOURCE"
    - from: "ingest_alpaca.py"
      to: "quantlab/acquisition/universe.py"
      via: "assert_chunked_panel_fits replaces assert_dense_panel_fits now that the whole-window path is gone"
      pattern: "assert_chunked_panel_fits\\("
---

<objective>
Make the three US-equity shells consumers of the entry point rather than three independent
implementations of it.

Today each shell constructs `StockDataset` itself and calls a densify method directly:
`ingest_us_equity.py` the chunked one, `ingest_tiingo.py` and `ingest_alpaca.py` the whole-window
one. That divergence is what SC-6 is about, and it is also what keeps the programmatic path
unproven — an entry point with no in-repo caller is an entry point nobody has run.

With one conversion mode (D-06), delegation makes the `--chunk` / `--on-new-listing` divergence
disappear automatically rather than by growing new surface on the shells, and it switches the two
whole-window shells onto the per-chunk guard, which is the only guard left.

Purpose: DATA-07 and DATA-08 reaching real callers; SC-6.

Output: three shells that call `convert()`, two shells that gained the chunk flags, and two AST
test suites re-expressed so they keep pinning what they were written to pin.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.5-registry-level-raw-to-zarr-conversion-entry-point/03.5-CONTEXT.md
@.planning/phases/03.5-registry-level-raw-to-zarr-conversion-entry-point/03.5-PATTERNS.md
@.planning/phases/03.5-registry-level-raw-to-zarr-conversion-entry-point/03.5-01-SUMMARY.md
@.planning/phases/03.5-registry-level-raw-to-zarr-conversion-entry-point/03.5-02-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols this phase creates (full list in `03.5-01-PLAN.md`). Plan 05 creates no new public symbol;
it rewires three existing entry points onto `convert()`, `assert_chunked_panel_fits` and
`print_chunk_report`.

## Planner notes carried forward

- **The `symbols=None` probe feeding `refuse_conversion_without_raw_data` STAYS IN THE SHELLS.**
  The pattern map asked for this to be decided deliberately rather than drifted into. It is
  decided: that helper takes an `AcquisitionResult`, and `convert()` has none — `run()` is
  acquisition-only (03.4 D-14) and `convert()` is a separate call precisely so a conversion does
  not require an acquisition to have just happened. Moving the probe inside `convert()` would
  reintroduce that coupling in the one function built to remove it. All three shells keep their
  existing `refuse_conversion_without_raw_data(StockDataset(replace(ds_config, symbols=None)),
  result)` line, unchanged, immediately before the delegated call.
- **A test WILL go quiet if this plan is careless, and that is the point of Task 3.**
  `tests/test_volume_guard.py::test_every_entry_point_that_densifies_guards_the_dense_panels_ram`
  detects a densification by AST — an attribute call named `from_raw_data` or
  `from_raw_data_chunked` — and closes with `assert densifying >= 2` whose own message says "if a
  door stopped densifying, say so here rather than letting this test silently cover nothing". After
  delegation NO shell names either method, so the counter reaches 0 and that assertion fails. It is
  the test doing exactly what it was written to do. Teach the detector about the delegated call;
  do not weaken the threshold.
- **Alpaca's parser-level tick refusal is NOT replaced.** `ingest_alpaca.py:321-333` fires during
  argument parsing, before a single byte moves. `convert()`'s `dataset_cls is None` raise (Plan 01
  Task 2) is the second, programmatic layer for callers that never touch argparse. Both exist on
  purpose.
- **`ingest_binance_spot.py` is untouched by this plan** (D-09) — it is the fourth converting shell
  the roadmap never names, and it cannot reach `convert()` because binance is not a registered
  source.

<tasks>

<task type="auto">
  <name>Task 1: ingest_us_equity.py delegates its conversion to convert() and renders the result</name>

  <files>ingest_us_equity.py</files>

  <read_first>
    - `ingest_us_equity.py:504-570` — the guard call site inside `if args.to_zarr:` and the
      conversion block below it. This is the shell whose shape the other two adopt, so read it
      first and in full, including the two comments explaining WHY the guard is checked before the
      download and why the dataset config carries `symbols=None`.
    - `ingest_us_equity.py:360-375` — `_build_arg_parser`'s `add_to_zarr_arg` and `add_chunk_args`
      calls as Plan 02 Task 3 left them.
    - `quantlab/acquisition/registry.py` — `convert()`'s final signature and its `ConversionResult`
      return, as Plan 01 left them. Read the docstring: it states that `convert()` does not run the
      guard (D-11), which is why the shell's guard call stays exactly where it is and why removing
      it would silently retire a recorded, developer-accepted risk.
    - `quantlab/base/data.py` — `ConversionResult`'s field names, so the rendered lines name real
      fields rather than invented ones.
    - `quantlab/utils/cli.py` — `refuse_conversion_without_raw_data` and `print_chunk_report`.
    - `tests/test_volume_guard.py:1422-1450` —
      `test_the_chunked_panel_guard_keeps_both_of_its_call_sites`, which asserts EXACTLY two
      `assert_chunked_panel_fits` call sites in this file. Both stay; do not add a third here.
  </read_first>

  <action>
Replace this shell's direct conversion with the delegated call. The guard call site at `:504-516`
does not move and does not change — it stays inside `if args.to_zarr:`, before `run(...)`, wrapping
`catalog.assert_chunked_panel_fits(...)` as the argument to `print_chunk_report`, so a refusal
still arrives before a multi-hour backfill and the AST ordering assertions still read a guard line
number below every densify line number.

In the conversion block at `:540-566`: keep the `refuse_conversion_without_raw_data(StockDataset(
replace(ds_config, symbols=None)), result)` line and its comment exactly as they are — see the
planner note above for why that probe stays in the shell. Then replace the three lines that
construct `StockDataset(ds_config)`, announce the conversion and call `from_raw_data_chunked(...)`
with a single `convert(SOURCE, ds_config, granularity=args.chunk,
on_new_listing=args.on_new_listing)` whose return value is bound to a local. Keep the announcement
print before the call — it tells the user a long operation is starting — and keep the
"Zarr store written at:" line after it, but source the path from the returned result rather than
from `ds_config`, so what is printed is what was actually written.

Render the rest of the `ConversionResult` in the same block: windows written and skipped, the
pinned symbol count, and whether the run resumed. This is the first in-repo proof that the result
object is renderable without reading disk, which is half of DATA-07's point. Follow this
repository's convention and put any multi-line rendering in `quantlab/utils/cli.py` beside
`print_chunk_report` rather than growing a private printer back into the shell; a two- or
three-line inline print in the shell is acceptable if that is all it takes.

Pass the predicted peak through if it is cheap to do so: the guard call above already returns the
report dict, and its `max_chunk_bytes` is exactly `convert()`'s `predicted_peak_bytes` parameter.
Binding that dict to a local at the guard call site and forwarding the value makes the result
object carry the full story — the prediction and the observed peak side by side — in the one place
a user looks after a run.

Import `convert` from `quantlab.acquisition.registry` alongside the existing `run` import.
`StockDataset` stays imported: the `symbols=None` probe still constructs one.
  </action>

  <verify>
    <automated>uv run pytest tests/test_volume_guard.py tests/test_ingest_shells.py tests/test_ingest_conversion_gate.py -q</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line. Expect the densify-detector test to FAIL here until Task 3 — record that it failed for that reason rather than weakening it</fails_when>
    <automated>uv run python -c "import ast; t=ast.parse(open('ingest_us_equity.py').read()); calls=[n for n in ast.walk(t) if isinstance(n,ast.Call)]; conv=[n for n in calls if isinstance(n.func,ast.Name) and n.func.id=='convert']; densify=[n for n in calls if isinstance(n.func,ast.Attribute) and n.func.attr in ('from_raw_data','from_raw_data_chunked')]; assert len(conv)==1, [n.lineno for n in conv]; assert not densify, [n.lineno for n in densify]; print('delegated ok')"</automated>
    <fails_when>non-zero exit (the AssertionError prints the offending line numbers), or stdout does not contain "delegated ok"</fails_when>
    <automated>uv run python -c "import ast; t=ast.parse(open('ingest_us_equity.py').read()); sites=[n.lineno for n in ast.walk(t) if isinstance(n,ast.Call) and isinstance(n.func,ast.Attribute) and n.func.attr=='assert_chunked_panel_fits']; assert len(sites)==2, sites; print('guard sites', sites)"</automated>
    <fails_when>non-zero exit — the two existing guard call sites must both survive, and no third may appear</fails_when>
    <automated>uv run python ingest_us_equity.py --dry-run --symbols AAPL --start-date 2024-01-01 --end-date 2024-01-31</automated>
    <fails_when>non-zero exit, or stderr contains a traceback</fails_when>
  </verify>

  <acceptance_criteria>
    - `ingest_us_equity.py` contains exactly one `convert(` call and zero `from_raw_data` /
      `from_raw_data_chunked` attribute calls (pinned by AST, not substring — the module docstring
      names those methods in prose).
    - The `refuse_conversion_without_raw_data(...)` probe survives:
      `grep -c 'refuse_conversion_without_raw_data' ingest_us_equity.py` prints 2 (the import and
      the call).
    - Both `assert_chunked_panel_fits` call sites survive and no third is added.
    - The "Zarr store written at:" line prints the path from the returned result.
    - The run reports windows written, windows skipped and the pinned symbol count from the
      returned object.
    - `uv run python ingest_us_equity.py --help` exits 0.
  </acceptance_criteria>

  <done>
The shell that already had the chunked path now reaches it through the registry, and the
`ConversionResult` it gets back is what it prints.
  </done>
</task>

<task type="auto">
  <name>Task 2: ingest_tiingo.py and ingest_alpaca.py adopt the same shape — chunked guard, chunk flags, delegated conversion</name>

  <files>ingest_tiingo.py, ingest_alpaca.py, quantlab/utils/cli.py</files>

  <read_first>
    - `ingest_us_equity.py:504-566` AS TASK 1 LEFT IT — this is the shape being adopted. Read the
      finished version, not the original.
    - `ingest_tiingo.py:195-250` — the `if args.to_zarr:` guard block with its long CR-03 comment
      (the parts of it that argue for the WHOLE-WINDOW form are now false and must be rewritten,
      not deleted wholesale — the sibling-guard argument and the "conditioned on --to-zarr" reason
      both still stand), the `assert_dense_panel_fits` call at `:216`, and the conversion block at
      `:233-250`.
    - `ingest_alpaca.py:378-470` — the guard block with its 25-line comment, the
      `assert_dense_panel_fits` call at `:412` with its `num_variables` and `bars_per_day`
      arguments, the tick branch at `:438-450` (which STAYS), and the conversion block at
      `:452-470`.
    - `ingest_alpaca.py:321-333` — the parser-level tick refusal. It STAYS. See the planner note.
    - `quantlab/utils/cli.py` — `add_chunk_args` (`:281-326`), which already declares both flags
      with derived `choices`, and `print_chunk_report` as Plan 02 Task 2 left it.
    - `quantlab/acquisition/universe.py` — `assert_chunked_panel_fits`'s signature as Plan 02 left
      it, in particular that it has `granularity` but NO `bars_per_day` parameter. Decide
      deliberately how `ingest_alpaca.py`'s minute frequency is sized under the chunked guard and
      state the decision in the shell's comment; if the chunked guard cannot express the minute
      case, say so in the summary rather than silently sizing a minute fetch as if it were daily.
    - `tests/test_volume_guard.py:1387-1420` —
      `test_the_intraday_ram_guard_is_sized_on_the_timestamp_axis_not_the_day`, which is about
      `assert_dense_panel_fits` and the 390-rows-per-minute-session fact. Read it before changing
      how alpaca sizes anything.
  </read_first>

  <action>
Give both shells the shape `ingest_us_equity.py` now has. Three changes each.

GUARD. Replace `pricing.assert_dense_panel_fits(...)` with
`print_chunk_report(pricing.assert_chunked_panel_fits(category, guard_start, guard_end,
granularity=args.chunk, ...))`, keeping the call INSIDE the existing `if args.to_zarr:` condition
and at its current POSITION before `run(...)` — both properties are pinned by AST assertions in
`tests/test_volume_guard.py` and both are still correct. Rewrite the surrounding comment rather
than deleting it: the halves that still hold are that this guard is a SIBLING of the
volume guard (that one bounds disk bytes, requests and wall clock; this one bounds the dense panel's
RAM) and that it is conditioned on `--to-zarr` because it measures a densification that no longer
always happens. The half that is now false — that this door densifies the WHOLE window with no
chunking, so the whole-window form is the one that applies — is replaced by D-07's fact: there is
one conversion path and therefore one guard.

For `ingest_alpaca.py` specifically, its current call passes `num_variables` computed from the
vendor's own bar width and `bars_per_day` from `BARS_PER_DAY_BY_FREQUENCY` — a guard that
overstates refuses fetches that would have been fine, which is how a guard gets deleted, and a
guard that understates admits the fetch it exists to refuse. Carry `num_variables` across.
`bars_per_day` has no counterpart on the chunked guard's signature; read that signature and decide,
then WRITE THE DECISION DOWN in the shell's comment and in the plan summary. If sizing a minute
window correctly needs the chunked guard to learn about intraday bar counts, that is a real finding
about D-07's consequences — report it rather than silently letting a minute run be sized as daily.

FLAGS. Add `add_chunk_args(parser)` to each shell's `_build_arg_parser`, beside the existing
`add_to_zarr_arg(parser)` call, exactly as `ingest_us_equity.py:371` does. Both flags already exist
with derived choices; this is one line per shell, and it is what makes SC-6's "the divergence
disappears" an automatic consequence of delegation rather than new surface.

CONVERSION. In each shell's `if args.to_zarr:` conversion block: keep the
`refuse_conversion_without_raw_data(StockDataset(replace(ds_config, symbols=None)), result)` line
and its comment untouched; replace the `StockDataset(ds_config)` construction and the
`from_raw_data().save()` call with `convert(SOURCE, ds_config, granularity=args.chunk,
on_new_listing=args.on_new_listing)`; render the returned result the way Task 1 renders it, sourcing
the written path from the result. In `ingest_alpaca.py` the tick branch above stays exactly as it
is — it prints that the run stops at raw and names phase 03.3, and it is reached before the
`elif args.to_zarr:` arm. Import `convert` alongside `run` in both.

The module docstrings of both shells describe a whole-window conversion. Amend them in this same
edit — this repository treats a stale docstring as a defect, and these two become false the moment
the call changes.
  </action>

  <verify>
    <automated>uv run python -c "import ast
for path in ('ingest_tiingo.py','ingest_alpaca.py','ingest_us_equity.py'):
    t=ast.parse(open(path).read())
    calls=[n for n in ast.walk(t) if isinstance(n,ast.Call)]
    conv=[n for n in calls if isinstance(n.func,ast.Name) and n.func.id=='convert']
    dense=[n for n in calls if isinstance(n.func,ast.Attribute) and n.func.attr=='assert_dense_panel_fits']
    chunked=[n for n in calls if isinstance(n.func,ast.Attribute) and n.func.attr=='assert_chunked_panel_fits']
    densify=[n for n in calls if isinstance(n.func,ast.Attribute) and n.func.attr in ('from_raw_data','from_raw_data_chunked')]
    assert conv, path+': no convert() call'
    assert not dense, path+': whole-window guard survived at '+str([n.lineno for n in dense])
    assert chunked, path+': no chunked guard'
    assert not densify, path+': direct densify survived at '+str([n.lineno for n in densify])
    assert min(n.lineno for n in chunked) < min(n.lineno for n in conv), path+': the guard no longer precedes the conversion'
print('all three delegated')"</automated>
    <fails_when>non-zero exit (the AssertionError names the file and the line numbers), or stdout does not contain "all three delegated"</fails_when>
    <automated>uv run python -c "import subprocess,sys
for s in ('ingest_tiingo.py','ingest_alpaca.py','ingest_us_equity.py'):
    o=subprocess.run([sys.executable,s,'--help'],capture_output=True,text=True)
    assert o.returncode==0, (s,o.stderr[-400:])
    for flag in ('--to-zarr','--chunk','--on-new-listing'):
        assert flag in o.stdout, (s,flag)
print('flags uniform')"</automated>
    <fails_when>non-zero exit (the AssertionError names the shell and the missing flag), or stdout does not contain "flags uniform"</fails_when>
    <automated>uv run python -c "import subprocess,sys; o=subprocess.run([sys.executable,'ingest_alpaca.py','--frequency','tick','--data-type','quotes','--to-zarr','--symbols','AAPL'],capture_output=True,text=True); assert o.returncode!=0, 'the parser-level tick refusal is gone'; assert '03.3' in (o.stderr+o.stdout), o.stderr[-400:]; print('tick refusal intact')"</automated>
    <fails_when>non-zero exit from the harness itself, or stdout does not contain "tick refusal intact"</fails_when>
    <automated>uv run pytest tests/test_ingest_tiingo_universe_wiring.py tests/test_ingest_conversion_gate.py tests/test_alpaca_acquisition.py -q</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line</fails_when>
  </verify>

  <acceptance_criteria>
    - No shell contains an `assert_dense_panel_fits` call; all three contain
      `assert_chunked_panel_fits` and exactly one `convert(` call each in their conversion path.
    - All three shells accept `--to-zarr`, `--chunk` and `--on-new-listing`.
    - In every shell the chunked guard's line number is below the `convert()` call's line number.
    - `ingest_alpaca.py --frequency tick --to-zarr` still exits non-zero with a message naming
      `03.3`.
    - Each shell still calls `refuse_conversion_without_raw_data(...)`:
      `grep -c refuse_conversion_without_raw_data ingest_tiingo.py ingest_alpaca.py` prints 2 for
      each file (import plus call).
    - Both amended module docstrings describe the chunked, resumable conversion rather than a
      whole-window one.
    - The alpaca minute-sizing decision is written down in the shell's comment AND in the plan
      summary.
  </acceptance_criteria>

  <done>
All three US-equity shells reach conversion through one entry point, size it with one guard, and
offer the same two conversion knobs — and the tick door still refuses before anything moves.
  </done>
</task>

<task type="auto">
  <name>Task 3: Re-express the two AST test suites so they keep pinning what they were written to pin</name>

  <files>tests/test_volume_guard.py, tests/test_ingest_shells.py</files>

  <read_first>
    - `tests/test_volume_guard.py:1318-1385` —
      `test_every_entry_point_that_densifies_guards_the_dense_panels_ram` in full: the
      `_RAM_GUARD_NAMES` frozenset, the `_is_densify` predicate matching an ATTRIBUTE call named
      `from_raw_data` / `from_raw_data_chunked`, the guard-precedes-densify line-number assertion,
      and the closing `assert densifying >= 2` whose message explicitly says a door that stopped
      densifying must be SAID rather than left to silently cover nothing. Read its docstring too —
      it records that the predecessor test got this wrong by pinning itself to one script.
    - `tests/test_volume_guard.py:1422-1450` —
      `test_the_chunked_panel_guard_keeps_both_of_its_call_sites`, which counts exactly two call
      sites in `ingest_us_equity.py`.
    - `tests/test_ingest_shells.py:560-690` — `_US_EQUITY_FLAGS`, the `_keyword` / `_calls_named`
      helpers, and `test_us_equity_keeps_every_capability_that_makes_it_distinct` with its
      seven-difference docstring. The parenthetical at `:618-621` — that the `--to-zarr` flag is
      shared with the other two shells but the chunking is not — becomes false the moment Task 2
      lands.
    - `ingest_tiingo.py`, `ingest_alpaca.py`, `ingest_us_equity.py` AS TASKS 1 AND 2 LEFT THEM.
  </read_first>

  <action>
Two tests are now saying something untrue about the code. Fix what they assert, not what they are
for.

FIRST, `tests/test_volume_guard.py`'s densify detector. Delegation means no shell names
`from_raw_data` or `from_raw_data_chunked` any more, so `densifying` reaches 0 and the closing
`assert densifying >= 2` fails. Teach `_is_densify` about the delegated call: a bare `Name` call to
`convert` is now a densification for this test's purposes, because it is the call that reaches the
allocation. Keep the attribute arms too — a shell that goes back to calling a densify method
directly must still be caught. Do NOT lower the `>= 2` threshold and do NOT narrow the test to the
shells that happen to remain; the property it protects is "any entry point that reaches a
densification names a RAM guard first, and names it FIRST", and that property is unchanged.
Update the test's docstring to record that the densification is now reached through the registry
entry point, so the next reader understands why a bare-name call counts.

Then re-check `test_the_chunked_panel_guard_keeps_both_of_its_call_sites`. It asserts exactly two
`assert_chunked_panel_fits` call sites in `ingest_us_equity.py`; Task 1 kept both and added none,
so this should pass untouched. If it does not, the cause is a change Task 1 made, and the fix is in
`ingest_us_equity.py`, not here.

SECOND, `tests/test_ingest_shells.py`'s capability pin. The parenthetical claiming the chunking is
not shared is now false — all three shells share the chunked path. Re-express the docstring's list
of what makes `ingest_us_equity.py` distinct: the survivorship-correct in-range roster, the
independent `us_all` watermark tree and Zarr store, the deliberate `symbols=None` dataset config,
the credential-free `--dry-run`, the only in-repo route to watermark stamping, and the
quota/concurrency knobs. That is six, not seven — the chunked conversion left the list because it
stopped being a difference. If you find a seventh that genuinely survives, name it and keep the
count at seven; if you do not, say six and say why one left.

Adjust `_US_EQUITY_FLAGS` accordingly: `--to-zarr`, `--chunk` and `--on-new-listing` are now on all
three shells, so they no longer evidence distinctness. The flag-presence assertion in that test is
still worth keeping for the flags that ARE distinct (`--dry-run`, `--stamp-legacy-watermarks`,
`--limit`, `--legacy-watermarks` and the quota/concurrency four); moving the three shared flags out
of the tuple is the honest edit. Consider adding a small positive assertion in their place — that
all three shells' parsers accept the three shared flags — so the sharing itself is pinned rather
than merely un-pinned. The three AST arms (`mode="in_range"`, `subdir=DEFAULT_SUBDIR`,
`symbols=None`) are untouched: all three are still real distinctions.
  </action>

  <verify>
    <automated>uv run pytest tests/test_volume_guard.py tests/test_ingest_shells.py -q</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line</fails_when>
    <automated>uv run python -c "import ast,inspect; import tests.test_volume_guard as m" 2>/dev/null || uv run python -c "src=open('tests/test_volume_guard.py').read(); i=src.index('def test_every_entry_point_that_densifies'); j=src.index('def test_', i+10); body=src[i:j]; assert 'convert' in body, 'the densify detector was not taught about the delegated call'; assert 'densifying >= 2' in body, 'the threshold was weakened instead of the detector being fixed'; print('detector ok')"</automated>
    <fails_when>non-zero exit, or stdout does not contain "detector ok"</fails_when>
    <automated>uv run python -c "src=open('tests/test_volume_guard.py').read(); i=src.index('def test_every_entry_point_that_densifies'); j=src.index('def test_', i+10); body=src[i:j]; assert 'from_raw_data_chunked' in body, 'the attribute arms were dropped — a shell going back to a direct densify would no longer be caught'; print('arms kept')"</automated>
    <fails_when>non-zero exit, or stdout does not contain "arms kept"</fails_when>
    <automated>uv run pytest -q</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line, or the passed count is lower than the count recorded in 03.5-02-SUMMARY.md</fails_when>
  </verify>

  <acceptance_criteria>
    - `test_every_entry_point_that_densifies_guards_the_dense_panels_ram` passes, with its
      `densifying >= 2` threshold intact and both attribute arms still present.
    - That test's docstring explains that the densification is now reached through the registry
      entry point.
    - `test_us_equity_keeps_every_capability_that_makes_it_distinct` passes, its docstring no
      longer claims the chunking is unshared, and its count matches the list it enumerates.
    - `_US_EQUITY_FLAGS` no longer contains the three now-shared flags, and the sharing is asserted
      somewhere rather than merely dropped.
    - The three AST arms of that test survive:
      `uv run python -c "t=open('tests/test_ingest_shells.py').read(); [__import__('sys').exit('lost: '+p) for p in ('in_range','DEFAULT_SUBDIR','dataset_symbols') if p not in t]; print('arms intact')"`
      prints `arms intact`.
    - `uv run pytest -q` exits 0 across the whole suite.
  </acceptance_criteria>

  <done>
Both AST suites still refuse the regressions they were written to refuse, and neither passes by
covering nothing.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator argv → three CLI shells | `--to-zarr`, `--chunk`, `--on-new-listing`, `--frequency` and the roster flags. |
| shell → `convert()` | The shells become the entry point's in-repo callers and the live proof the programmatic path works (03.4 D-15 role b). |
| shell → `assert_chunked_panel_fits` | The shells are where D-11 places the RAM guard, since `convert()` does not run it. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.5-14 | Denial of Service | a shell that reaches `convert()` with no RAM guard in front of it | high | mitigate | D-11 puts the guard at the call site, which makes a forgotten guard a real OOM rather than a caught error. Mitigated by the AST assertion that every entry point reaching a densification names a RAM guard FIRST — which is exactly why Task 3 teaches that detector about the delegated call instead of letting it silently count zero doors. |
| T-03.5-15 | Tampering | `ingest_alpaca.py`'s minute-frequency sizing under the chunked guard | high | mitigate | The whole-window guard takes `bars_per_day`; the chunked guard does not. A minute window sized as if it were daily understates the allocation by ~390x and admits the exact fetch the guard exists to refuse — the documented CR-03 failure. Task 2 requires the sizing decision to be written down in the shell and in the summary rather than defaulted into, and requires the finding to be reported if the chunked guard cannot express it. |
| T-03.5-16 | Elevation of Privilege | tick conversion reaching a dense panel | medium | mitigate | Two independent refusals survive: the parser-level one in `ingest_alpaca.py` (fires before any work) and `convert()`'s `dataset_cls is None` raise (fires for non-CLI callers). Task 2 keeps the first and Plan 01 Task 2 built the second; the CLI refusal is pinned by a runnable check in this plan's verify. |
| T-03.5-17 | Information Disclosure | rendered `ConversionResult` lines in shell output | low | mitigate | The rendered fields are counts, booleans and the store path the operator supplied. No vendor response body, no credential, no environment value reaches a print — `ConversionResult`'s field set is fixed by Plan 01 and contains no free-text vendor string. |
| T-03.5-SC | Tampering | npm/pip/cargo installs | low | accept | This plan adds no package-manager install task and no new dependency; `pyproject.toml` and `uv.lock` are not in `files_modified`. The package-legitimacy gate is not applicable. |
</threat_model>

<verification>
- `uv run pytest -q` exits 0 with a passed count no lower than the pre-plan baseline.
- All three shells respond to `--help` with exit 0 and expose `--to-zarr`, `--chunk` and
  `--on-new-listing`.
- No shell contains an `assert_dense_panel_fits` call or a direct `from_raw_data*` call.
- `ingest_binance_spot.py` is unchanged.
</verification>

<success_criteria>
- SC-6 is satisfied for the three shells the roadmap names, with the fourth shell's exclusion
  recorded rather than hidden (D-09; the ROADMAP text itself is Plan 03's).
- D-07's shell consequences are complete: one guard, both chunk flags everywhere, no whole-window
  guard left in an ingest path.
- The entry point has real in-repo callers, which is what makes DATA-07 more than a signature.
</success_criteria>

<output>
Create `.planning/phases/03.5-registry-level-raw-to-zarr-conversion-entry-point/03.5-05-SUMMARY.md` when done
</output>
