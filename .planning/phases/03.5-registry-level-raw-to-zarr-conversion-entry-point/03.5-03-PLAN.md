---
phase: "03.5-registry-level-raw-to-zarr-conversion-entry-point"
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [DATA-07, DATA-08]

estimate:
  tokens: 16000
  raw_tokens: 16000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "DATA-07 and DATA-08 exist in .planning/REQUIREMENTS.md — in the Data section, in the traceability table mapped to Phase 03.5, and counted in the Coverage block — so this phase's requirement IDs resolve instead of dangling as a discuss-phase proposal"
    - "ROADMAP 03.5's Goal and Success Criteria no longer claim a conversion mode is chosen, that three modes carry three differently-sized RAM guards, or that an omitted mode raises: D-06 superseded all three claims and they are rewritten rather than left standing in contradiction"
    - "ROADMAP 03.5 records that SC-6's 'all three ingest shells' is INCOMPLETE by decision: ingest_binance_spot.py is a FOURTH converting shell the roadmap never named and that deliberately does not join the entry point (D-09)"
    - "REQUIREMENTS.md stays in Chinese, using CONTEXT.md's proposed DATA-07/DATA-08 wording verbatim"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "DATA-07 and DATA-08 as first-class v1 requirements with phase mapping and updated coverage counts"
      contains: "DATA-07"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 03.5 Goal and Success Criteria consistent with D-06/D-07/D-09"
      contains: "Phase 03.5"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "Phase 03.5's Requirements line names DATA-07, DATA-08 and both now resolve to real entries"
      pattern: "DATA-07, DATA-08"
---

<objective>
Close the two documentation obligations this phase owes, both of which are currently false
statements sitting in the planning tree.

First: ROADMAP 03.5 lists `DATA-07, DATA-08 (to be added in discuss-phase)` and neither exists in
`.planning/REQUIREMENTS.md`. Discuss-phase left them as PROPOSALS in CONTEXT.md. This phase's own
requirement IDs therefore dangle.

Second: D-06 superseded ROADMAP 03.5's Goal clause and Success Criteria 2 and 3 outright, and
reduced SC-4. CONTEXT.md states the obligation plainly — *"ACTION REQUIRED OUTSIDE THIS DOCUMENT:
ROADMAP 03.5's SC-2 and SC-3 must be rewritten, not left standing in contradiction — the same
handling 03.4 D-12 prescribed for the superseded boundary contract."*

Purpose: a roadmap that argues with its own phase context is the thing a later reader trusts and
is wrong about. The verification step for this phase reads those criteria.

Output: DATA-07/DATA-08 in REQUIREMENTS.md; a Phase 03.5 section in ROADMAP.md that states what
this phase actually delivers.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03.5-registry-level-raw-to-zarr-conversion-entry-point/03.5-CONTEXT.md
</context>

## Artifacts this phase produces

Symbols this phase creates (full list in `03.5-01-PLAN.md`). Plan 03 creates:

- requirement ID `DATA-07` in `.planning/REQUIREMENTS.md`
- requirement ID `DATA-08` in `.planning/REQUIREMENTS.md`

<tasks>

<task type="auto">
  <name>Task 1: Write DATA-07 and DATA-08 into REQUIREMENTS.md</name>

  <files>.planning/REQUIREMENTS.md</files>

  <read_first>
    - `.planning/REQUIREMENTS.md` in full (134 lines). Note the three places a requirement
      appears: the `### Data（数据获取与预处理）` bullet list at `:19-26`, the traceability table
      at `:97-124` (`| Requirement | Phase | Status |`), and the `**Coverage:**` block at
      `:126-130` which today reads 24 total / 24 mapped / 0 unmapped.
    - `.planning/phases/03.5-registry-level-raw-to-zarr-conversion-entry-point/03.5-CONTEXT.md`
      section "Requirements DATA-07 / DATA-08 — PROPOSED, NOT YET IN REQUIREMENTS.md" — the two
      Chinese sentences are the wording to use VERBATIM.
    - The existing DATA-05 and DATA-06 entries, which are the format to match: a `- [ ] **ID**: `
      prefix, Chinese prose, and `[x]` only once the requirement is met.
  </read_first>

  <action>
Add DATA-07 and DATA-08 to `.planning/REQUIREMENTS.md` in all three places a requirement must
appear.

In the `### Data（数据获取与预处理）` list, append two entries after DATA-06, unchecked
(`- [ ]`), carrying CONTEXT.md's proposed wording verbatim. DATA-07 is the one about an external
caller converting a persisted raw parquet tier to Zarr through the registry without naming a
vendor class, a `Dataset` subclass or an `ingest_*.py` script, and receiving an object describing
the conversion (windows written and skipped, pinned symbol count, storage paths, whether it
resumed or was cancelled). DATA-08 is the one about any caller being able to ask, before any
memory is allocated, for the predicted peak memory of a conversion and the remedy for each
over-budget window — and that the answer is a value the caller renders rather than a log line this
layer prints. The file is written in Chinese and must stay so; do not translate, paraphrase or
"improve" the wording — it is the developer's, approved in discuss-phase.

In the traceability table, insert two rows after `| DATA-06 | Phase 03.1 | Complete |`, both
mapped to `Phase 03.5` with status `Pending`.

In the `**Coverage:**` block, raise the two counts from 24 to 26 (`v1 requirements: 26 total`,
`Mapped to phases: 26 (all v1 requirements covered)`); `Unmapped: 0` is unchanged. Update the
`*Last updated:*` footer line to today's date with a short note that 03.5 added DATA-07/DATA-08.

Do not renumber, reword or re-check any existing requirement. This task ADDS two entries and
adjusts two integers; anything else appearing in the file is out of scope.
  </action>

  <verify>
    <automated>grep -c '\*\*DATA-07\*\*\|\*\*DATA-08\*\*' .planning/REQUIREMENTS.md</automated>
    <fails_when>the printed count is not 2 (0 means the bullets were never added; more than 2 means a duplicate entry)</fails_when>
    <automated>grep -E '^\| DATA-0[78] \| Phase 03\.5 \|' .planning/REQUIREMENTS.md | wc -l</automated>
    <fails_when>the printed count is not 2 — a missing row means the requirement is defined but unmapped, which the Coverage block would then contradict</fails_when>
    <automated>grep -E 'v1 requirements: 26 total|Mapped to phases: 26' .planning/REQUIREMENTS.md | wc -l</automated>
    <fails_when>the printed count is not 2 — a stale count means the coverage block disagrees with the table above it</fails_when>
    <automated>uv run python -c "n=sum(1 for _ in open('.planning/REQUIREMENTS.md')); assert 136 <= n <= 142, n; print('line count', n)"</automated>
    <fails_when>non-zero exit — the file was 134 lines before this task, so a count outside 136-142 means edits beyond the two bullets, two table rows and two counts</fails_when>
  </verify>

  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` contains a `- [ ] **DATA-07**:` bullet and a `- [ ] **DATA-08**:`
      bullet, both in the `### Data（数据获取与预处理）` section, both in Chinese.
    - The traceability table contains `| DATA-07 | Phase 03.5 | Pending |` and
      `| DATA-08 | Phase 03.5 | Pending |`.
    - The Coverage block reads 26 total and 26 mapped.
    - No existing requirement was renumbered, reworded or re-checked:
      `uv run python -c "t=open('.planning/REQUIREMENTS.md').read(); [__import__('sys').exit('missing '+i) for i in ('DATA-01','DATA-02','DATA-03','DATA-04','DATA-05','DATA-06','SEC-01','QUAL-02') if '**'+i+'**' not in t]; assert '[x] **DATA-05**' in t and '[x] **DATA-06**' in t; print('existing intact')"`
      prints `existing intact`.
  </acceptance_criteria>

  <done>
This phase's two requirement IDs resolve to real, phase-mapped, Chinese-language entries in
REQUIREMENTS.md instead of to a proposal inside a CONTEXT.md.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite ROADMAP 03.5's Goal and the Success Criteria D-06 superseded</name>

  <files>.planning/ROADMAP.md</files>

  <read_first>
    - `.planning/ROADMAP.md` § "Phase 03.5: Registry-level raw→Zarr Conversion Entry Point
      (INSERTED)" — the Goal line, `**Mode:** mvp`, the Requirements line, and all seven Success
      Criteria. Read the whole section before editing any part of it.
    - `.planning/phases/03.5-registry-level-raw-to-zarr-conversion-entry-point/03.5-CONTEXT.md`
      decisions D-06 (what is superseded and the stale-arithmetic reason why), D-07 (the SC-6
      consequence), D-09 (the fourth shell and the scope-honesty instruction), D-10 (what SC-4
      reduces to), and D-11 (answerable, not enforced).
    - `.planning/ROADMAP.md` § "Phase 03.4" — look at how 03.4 D-12's superseded boundary contract
      was handled there. CONTEXT.md names that as the precedent for this edit; match its handling
      rather than inventing a new one.
  </read_first>

  <action>
Rewrite the parts of ROADMAP 03.5 that D-06, D-09 and D-10 falsified. Rewrite them — do not append
a correction beneath a claim that stays. A roadmap that states both halves of a contradiction is
worse than one that states the wrong half, because a reader cannot tell which survived.

The GOAL line currently says the caller converts "choosing the conversion mode explicitly and
being told which RAM guard that choice implies and what peak it predicts BEFORE anything is
allocated." There is no mode to choose and one guard to imply (D-06). Rewrite the goal so it says
what the phase delivers: a caller that is not one of quantlab's own scripts converts an acquired
raw tier into the Zarr tier through the registry, and can ask what peak RAM that conversion
predicts before anything is allocated. Keep the existing framing of who the caller is (the
out-of-repo `quantlab-console` first, but the contract is not written for it) — that part is
untouched and is the phase's reason to exist.

SUCCESS CRITERION 2: keep its first half, which still stands and still matters — `registry.run()`
remains acquisition-only and conversion is a SEPARATE call, never a flag on it (03.4 D-14). Delete
the second half, the clause about three modes carrying three differently-sized RAM guards. State
in its place why a separate call still matters without that clause: folding conversion into `run()`
would make an acquisition-only contract into a conversion-sometimes contract, which is what D-14
separated.

SUCCESS CRITERION 3: delete it entirely and replace it with what D-06 actually decided. Today it
requires the mode to be an explicit argument with NO default that raises when omitted. There is no
mode parameter at all — not a defaulted one, an absent one — and `convert()` does chunked
conversion and only chunked conversion. Record, briefly, why the roadmap's "three" did not survive
contact: it was stale arithmetic, counting three SCRIPTS at a moment when `G-03.4-1b` had already
collapsed two of them onto one whole-window path, and the repo's own vocabulary had already
settled at two before the developer removed the second.

SUCCESS CRITERION 4: reduce it. "Which guard a given (mode, roster, window) selects" loses its
object with one mode — there is one guard. What survives is "what peak does it predict", answered
WITHOUT starting the conversion, as a value the caller renders rather than a line this layer
prints. Add the D-11 half that the roadmap's own wording already implied and that this phase reads
literally: the guard is ANSWERABLE before the run, not ENFORCED by it, and the callers own the
asking.

SUCCESS CRITERION 6: keep the criterion, and add one sentence of scope honesty per D-09.
`ingest_binance_spot.py` is a FOURTH converting shell that this roadmap never named and that
deliberately does NOT join the entry point this phase, because `SpotKlineDataset` does not override
the chunked densify seam — under chunked-only it would densify the whole range once per window for
identical peak RAM plus a ledger plus N redundant densifications. Binance is also not a registered
source at all, so that shell cannot reach the entry point regardless. "One conversion entry point"
is therefore INCOMPLETE this phase, by decision. Say so; do not paper over it.

SUCCESS CRITERIA 1, 5 and 7 are untouched.

Leave the `**Plans:**` count and the plan checklist under this phase exactly as the planner wrote
them — that section is maintained by the planning workflow, not by this task. Do not touch any
other phase's section.
  </action>

  <verify>
    <automated>uv run python -c "t=open('.planning/ROADMAP.md').read(); i=t.index('### Phase 03.5:'); j=t.index('### Phase 4:', i); s=t[i:j]; bad=[p for p in ('three genuinely different modes','three differently-sized RAM guards','The mode is an explicit argument','which guard a given (mode','choosing the conversion mode explicitly') if p in s]; assert not bad, bad; print('superseded claims removed')"</automated>
    <fails_when>non-zero exit (the AssertionError prints the surviving claim), or stdout does not contain "superseded claims removed"</fails_when>
    <automated>uv run python -c "t=open('.planning/ROADMAP.md').read(); i=t.index('### Phase 03.5:'); j=t.index('### Phase 4:', i); s=t[i:j]; assert 'ingest_binance_spot.py' in s, 'SC-6 scope honesty (D-09) is missing'; assert 'registry.run()' in s or 'run()' in s, 'SC-2 first half was deleted with its second half'; print('rewrite ok')"</automated>
    <fails_when>non-zero exit, or stdout does not contain "rewrite ok"</fails_when>
    <automated>uv run python -c "t=open('.planning/ROADMAP.md').read(); import re; n=len(re.findall(r'^### Phase ', t, re.M)); assert n >= 12, n; print('phase count', n)"</automated>
    <fails_when>the printed phase count is below 12 — a whole-file rewrite of ROADMAP.md that dropped sections outside the 03.5 edit window</fails_when>
    <automated>uv run python -c "t=open('.planning/ROADMAP.md').read(); i=t.index('### Phase 03.5:'); j=t.index('### Phase 4:', i); print('03.5 section lines', t[i:j].count(chr(10)))"</automated>
    <fails_when>the printed count is outside 60-95 — the section (including the plan checklist beneath it) was 72 lines before this task, so a count outside that band means the edit escaped its section or swallowed the next one</fails_when>
  </verify>

  <acceptance_criteria>
    - The Phase 03.5 section contains none of the superseded phrases (pinned by the runtime check
      above, which prints the offender rather than merely failing).
    - Success Criterion 2 still asserts that `run()` remains acquisition-only and that conversion
      is a separate call — the surviving half was kept, not deleted along with the other.
    - Success Criterion 3 states that there is no mode parameter at all and that `convert()` does
      chunked conversion only.
    - Success Criterion 4 asks only what peak is predicted, and says the guard is answerable
      before the run rather than enforced by it.
    - Success Criterion 6 names `ingest_binance_spot.py` and says "one conversion entry point" is
      incomplete this phase by decision.
    - Success Criteria 1, 5 and 7 survive verbatim:
      `uv run python -c "t=open('.planning/ROADMAP.md').read(); [__import__('sys').exit('lost: '+p) for p in ('without naming a vendor class','names the remedy that would fit','Tick stays refused') if p not in t]; print('1/5/7 intact')"`
      prints `1/5/7 intact`.
    - `ROADMAP.md` still contains every other phase section (pinned by the phase-count check).
  </acceptance_criteria>

  <done>
A reader of ROADMAP 03.5 learns what this phase delivers, including what it deliberately does not
deliver, with no criterion left standing that its own phase context already retired.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning documents → future readers and agents | These two files are what later phases, the verifier and the out-of-repo console's roadmap read as authoritative. A false statement here propagates as a design decision. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.5-08 | Repudiation | ROADMAP 03.5 Success Criteria | medium | mitigate | A superseded criterion left in place makes it impossible to say afterwards which contract the phase was actually built against — the failure D-06 explicitly calls out by pointing at 03.4 D-12's handling. Mitigated by rewriting in place rather than appending a correction, and by the runtime check that fails on any surviving superseded phrase. |
| T-03.5-09 | Tampering | whole-file rewrite of ROADMAP.md losing unrelated phases | medium | mitigate | The task instructs a scoped section edit, and the phase-count check plus the section-line bound make an escaped edit fail loudly rather than silently deleting another phase's plan checklist. |
| T-03.5-10 | Information Disclosure | requirement text in REQUIREMENTS.md | low | accept | Both requirements describe an in-process API contract. Neither names a credential, an env var value, a host or an endpoint; the repository's `SEC-01` constraint is untouched by this plan. |
| T-03.5-SC | Tampering | npm/pip/cargo installs | low | accept | This plan modifies only Markdown under `.planning/`; it adds no dependency and runs no package manager. The package-legitimacy gate is not applicable. |
</threat_model>

<verification>
- Both runtime checks over `.planning/ROADMAP.md` exit 0.
- `grep -c` over `.planning/REQUIREMENTS.md` reports exactly two DATA-07/DATA-08 bullets and two
  traceability rows.
- The ROADMAP 03.5 section line count and the REQUIREMENTS.md line count both stay inside their
  stated bounds, so neither edit escaped its scope.
</verification>

<success_criteria>
- The phase's requirement IDs DATA-07 and DATA-08 resolve in REQUIREMENTS.md.
- ROADMAP 03.5 states the contract this phase actually implements, including the deliberate
  incompleteness D-09 recorded.
</success_criteria>

<output>
Create `.planning/phases/03.5-registry-level-raw-to-zarr-conversion-entry-point/03.5-03-SUMMARY.md` when done
</output>
