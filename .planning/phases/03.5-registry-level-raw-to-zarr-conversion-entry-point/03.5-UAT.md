---
status: testing
phase: 03.5-registry-level-raw-to-zarr-conversion-entry-point
source: [03.5-VERIFICATION.md]
started: 2026-09-12T02:54:02Z
updated: 2026-09-12T02:54:02Z
---

## Current Test

number: 1
name: The out-of-repo quantlab-console consumes convert() and estimate_chunked_panel()
expected: |
  The console converts an acquired raw tier without importing StockDataset,
  TiingoAcquisition or any ingest_*.py; CVT-02's "guard and predicted peak visible
  in the UI" is satisfiable from the returned dict; a console-issued cancel stops
  the run at a window boundary and the next run resumes.
awaiting: user response

## Tests

### 1. The out-of-repo quantlab-console consumes convert() and estimate_chunked_panel()
expected: Have the console import `quantlab.acquisition.registry.convert()` and
  `UniverseCatalog.estimate_chunked_panel()`, render a ConversionResult and a chunk report in
  its own UI, and drive one conversion to completion and one to cancellation from the console's
  own CancelToken. The console must not import StockDataset, TiingoAcquisition or any
  ingest_*.py. A console-issued cancel must stop at a window boundary, and the next run must
  resume.
why_human: The phase goal's stated first consumer lives in a second repository this
  verification cannot reach. Every in-repo proof is a shell that this repo also wrote; only an
  out-of-repo caller falsifies "the contract is not written for it".
result: [pending]

### 2. Concurrent convert() runs against ONE Zarr store
expected: Start two `convert()` runs against the SAME zarr_file_path concurrently (two
  processes, same DatasetConfig) and inspect the store and chunk ledger afterwards. Record what
  actually happens — duplicated windows, a ledger/store disagreement caught by
  assert_consistent, or corruption. Then DECIDE: accept the documented hazard, or schedule a
  lock.
why_human: Verified structurally — no FileLock/flock/threading.Lock exists in base/data.py,
  base/chunking.py or registry.py, so plan 06's backstop declaration ("two concurrent convert()
  runs against ONE Zarr store are not serialised and this phase adds no lock") is accurate. But
  NO test observes the race outcome, and the consequence (a silently mis-ordered append) is
  exactly the class ChunkLedger exists to refuse. The accept-or-lock call is the developer's.
result: [pending]

### 3. Pre-03.5 Zarr store migration path (WR-04)
expected: On a machine holding a PRE-03.5 Tiingo or Alpaca Zarr store (written by the old
  whole-window `from_raw_data()` path, so it has no chunk-ledger sidecar), run
  `uv run python ingest_tiingo.py --to-zarr`. ChunkLedger.assert_consistent should refuse with
  "a store exists at ... but there is no chunk ledger at ...". Confirm the operator-facing
  remedy ("Delete the store to rebuild it, or restore the ledger") is acceptable, or commission
  the migration note.
why_human: Reproducing needs a real legacy store on disk; no data/ directory is present here.
  The refusal path is confirmed by reading quantlab/base/chunking.py:338-345, but whether the
  migration cost is acceptable is the developer's call, not the verifier's.
result: [pending]

## Summary

total: 3
passed: 0
issues: 0
pending: 3
skipped: 0
blocked: 0

## Gaps
