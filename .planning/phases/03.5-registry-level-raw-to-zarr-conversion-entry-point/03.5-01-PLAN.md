---
phase: "03.5-registry-level-raw-to-zarr-conversion-entry-point"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - quantlab/base/config.py
  - quantlab/config/__init__.py
  - quantlab/base/data.py
  - quantlab/acquisition/registry.py
  - quantlab/acquisition/tiingo.py
  - quantlab/acquisition/alpaca.py
  - tests/conftest.py
  - tests/test_registry_convert.py
  - test.py
  - tests/test_alpaca_acquisition.py
  - tests/test_chunked_ingest.py
  - tests/test_config_paths.py
  - tests/test_construction_raw_fallback.py
  - tests/test_dataset_hierarchy.py
  - tests/test_dataset_update.py
  - tests/test_dataset_update_evidence.py
  - tests/test_extensibility_contract.py
  - tests/test_ingest_conversion_gate.py
  - tests/test_raw_hive_layout.py
  - tests/test_source_inspector.py
  - tests/test_spot_dataset.py
  - tests/test_stock_dataset.py
  - tests/test_symbol_coord_encoding.py
autonomous: true
requirements: [DATA-07]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "An in-process caller holding only a SourceDescriptor and a DatasetConfig can convert an already-persisted raw parquet tier into a Zarr store through quantlab.acquisition.registry.convert(), without naming StockDataset, TiingoAcquisition or any ingest_*.py at the call site (DATA-07, D-03)"
    - "convert() returns a ConversionResult describing windows written/skipped, the pinned symbol count, the Zarr and ledger paths, and whether the run resumed — so the caller renders the outcome without reading disk (D-04)"
    - "A capability whose dataset_cls is None refuses the conversion with a message naming Phase 03.3, and the refusal is the ABSENCE of a conversion target rather than a frequency literal in convert()'s body (D-01, SC-7)"
    - "Calling convert() twice over the same window writes no window twice: the second call reports windows_written == 0, windows_skipped equal to the first call's windows_written, and resumed is True (probe row DATA-07/idempotency)"
    - "registry.run() still returns an AcquisitionResult and still performs no conversion — convert() is a SEPARATE module-level function, never a flag on run() (03.4 D-14, SC-2 first half)"
  prohibitions:
    - "convert() must NOT fall back to a nearest-match Dataset subclass when the matched capability carries no conversion target — a quotes/trades stream silently flattened onto a dense [timestamp, symbol] grid produces a plausible-looking panel that is scientifically wrong, and a wrong panel that loads is worse than a refusal"
    - "convert() must NOT acquire a self-protecting RAM guard call in this phase — D-11 placed the guard at every call site deliberately and recorded the residual risk; adding one inside convert() would retire that recorded risk silently instead of by decision"
  artifacts:
    - path: "quantlab/acquisition/registry.py"
      provides: "module-level convert() entry point and Capability.dataset_cls"
      contains: "def convert("
    - path: "quantlab/base/data.py"
      provides: "ConversionResult value object and BaseDataset.last_chunk_result"
      contains: "class ConversionResult"
    - path: "quantlab/base/config.py"
      provides: "DatasetConfig.market — the first field of the capability lookup key"
      contains: "market: Market"
    - path: "tests/test_registry_convert.py"
      provides: "end-to-end proof that convert() writes a real Zarr store and returns a ConversionResult, plus the three capability-resolution refusals"
      min_lines: 120
  key_links:
    - from: "quantlab/acquisition/registry.py"
      to: "quantlab/base/data.py"
      via: "convert() imports ConversionResult from base/, the same direction run() imports AcquisitionResult (registry.py:47)"
      pattern: "from quantlab.base.data import .*ConversionResult"
    - from: "quantlab/acquisition/tiingo.py"
      to: "quantlab/dataset/stock.py"
      via: "Capability(dataset_cls=StockDataset) — a direct class reference (03.4 D-03), precedent quantlab/acquisition/inspector.py:71"
      pattern: "dataset_cls=StockDataset"
    - from: "quantlab/acquisition/registry.py"
      to: "quantlab/base/config.py"
      via: "convert() reads dataset_config.market as the first element of the (market, frequency, data_type) capability lookup key (D-02)"
      pattern: "\\.market"
---

<objective>
Land the phase's tracer: one caller reaches `registry.convert()` for ONE vendor capability
(Tiingo `us_equity`/`1d`) and gets a real Zarr write plus a `ConversionResult` back — through
every layer the phase touches (config → registry → base → dataset → store), with nothing
stubbed on the happy path.

Then expand that proven slice sideways to the second vendor: Alpaca's four capability rows, and
the three capability-resolution outcomes a real caller hits (no match, ambiguous match, and the
tick refusal that is data rather than a branch).

Purpose: DATA-07. The out-of-repo `quantlab-console` (its Phase 8 `CVT-01`) names this entry
point by hand as its unmet upstream precondition; today the only way to convert is to run one of
quantlab's own shells by hand.

Output: `DatasetConfig.market` reinstated, `Capability.dataset_cls`, `ConversionResult`,
`convert()`, `BaseDataset.last_chunk_result`, and `tests/test_registry_convert.py`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.5-registry-level-raw-to-zarr-conversion-entry-point/03.5-CONTEXT.md
@.planning/phases/03.5-registry-level-raw-to-zarr-conversion-entry-point/03.5-PATTERNS.md
@.planning/phases/03.4-data-source-registry/03.4-CONTEXT.md
</context>

## Line numbers are navigation aids, not addresses

Every `quantlab/...:NNN` reference in this plan was resolved against the WORKING TREE at plan time
(commit `329fb50` plus the developer's uncommitted edits). Those edits were still in flight while
this plan was written — `quantlab/base/data.py`, `quantlab/dataset/stock.py` and
`quantlab/config/__init__.py` all differed from `HEAD`, and two symbols had already moved
(`BaseDataset._raw_data_to_xr_window` 429 to 420, `StockDataset._raw_data_to_xr_window` 477 to
486). **Locate every target by SYMBOL NAME first and treat the line number as a hint.** If a cited
line does not hold what this plan says it holds, re-derive it with `grep -n` and note the drift in
the summary; do not edit the line the number points at.

**One in-flight edit changes an argument this plan relies on.** At plan time the working tree had
an uncommitted change to `BaseDataset._reset_symbols`'s missing-store branch, replacing the
fall-back-to-`from_raw_data()` behaviour with a raised `FileNotFoundError`. Several comments in the
ingest shells justify their `symbols=None` probe by that fall-back ("`_reset_symbols()` ... falls
back to `from_raw_data()` — a full densification at CONSTRUCTION time"). **Read
`BaseDataset._reset_symbols` before relying on that rationale.** If the fall-back is gone, the
probe may still be correct for other reasons but its stated reason is stale — report that in the
summary rather than deleting the probe or silently keeping a false comment.

## Artifacts this phase produces

New symbols and files created by Phase 03.5 (this list exists so a drift check does not flag a
symbol that did not exist before this phase). Plan 01 creates the ones marked **[01]**:

- **[01]** `quantlab.acquisition.registry.convert()` — module-level conversion entry point
- **[01]** `quantlab.acquisition.registry.Capability.dataset_cls` — new optional field
- **[01]** `quantlab.acquisition.registry.SourceDescriptor.capabilities_for()` — lookup helper
- **[01]** `quantlab.base.data.ConversionResult` — frozen dataclass; fields `zarr_path`,
  `ledger_path`, `granularity`, `pinned_symbols`, `windows_planned`, `windows_written`,
  `windows_skipped`, `rows_written`, `peak_window_bytes`, `predicted_peak_bytes`, `resumed`,
  `cancelled`
- **[01]** `quantlab.base.data.BaseDataset.last_chunk_result` — instance attribute
- **[01]** `quantlab.base.config.DatasetConfig.market` — REINSTATED field (existed before
  commit `441e630`)
- **[01]** `tests/test_registry_convert.py` — new test module
- [02] `quantlab.acquisition.universe.UniverseCatalog.estimate_chunked_panel()`
- [02] `quantlab.utils.cli.print_chunk_report()`
- [03] requirement IDs `DATA-07`, `DATA-08` in `.planning/REQUIREMENTS.md`
- [04] abstract `quantlab.base.data.MarketDataset._raw_data_to_xr_window`
- [04] `quantlab.dataset.spot.SpotKlineDataset._raw_data_to_xr_window`
- [06] `reporter` / `cancel` keyword parameters on `BaseDataset.from_raw_data_chunked()`

## Planner notes carried forward (read before reviewing this plan)

- **NOTE FOR PLAN-CHECKER — do not flag:** under D-01 a second `market → Dataset` registry was
  considered and REJECTED, because ROADMAP 03.4 explicitly forbids inventing a second registry
  style. `dataset_cls` on `Capability` is the accepted answer.
- **D-01 reversibility: costly** (carried from CONTEXT.md, not re-derived). Once
  `quantlab-console` reads `dataset_cls` off capabilities, relocating the field changes
  enumeration call sites in a second repository. Flagged, not gated — no `checkpoint:decision`,
  because `costly` is not `one-way`.
- **VERIFIED during planning, so the executor does not have to re-derive it:**
  `tests/test_source_registry.py::test_capabilities_match_the_vendor_class_constants`
  (`tests/test_source_registry.py:360-400`) compares capability ATTRIBUTES against
  `TIMEFRAME_MAP` / `TICK_DATA_TYPES` / `ENDPOINT_MAP` — it never constructs a `Capability(...)`
  object. Adding an optional field with a default therefore requires **no change to that test**.
  `quantlab/acquisition/alpaca.py:914-916` said to check this; it is checked.
- **VERIFIED during planning:** `Market` is already imported at `quantlab/base/config.py:4`
  (`from quantlab.enums.data import Frequency, Market, Vendor`), so D-02 needs no import change.
- **D-11's accepted risk, recorded not hidden:** `convert()` does NOT run the RAM guard. The
  guarantee lives at every call site. A new integrator who forgets to ask goes straight to OOM.
  The developer accepted this; revisit only if that failure is observed.
- **Canon-referral breadcrumbs (prohibition probe, dropped rather than minted):** credential text
  leaking into a result object or log line → canon (OWASP A09 logging); path traversal via a
  caller-supplied `zarr_file_path` → canon (path traversal). Both are handled in
  `<threat_model>` below instead.

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "convert one Tiingo capability to Zarr through the registry" — one path only</name>

  <reversibility rating="costly">D-01 puts `dataset_cls` on `Capability`. Once the out-of-repo
  `quantlab-console` enumerates capabilities and reads that field, relocating it changes call
  sites in a second repository. Rating carried from 03.5-CONTEXT.md D-01, not re-derived.</reversibility>

  <files>quantlab/base/config.py, quantlab/config/__init__.py, quantlab/base/data.py, quantlab/acquisition/registry.py, quantlab/acquisition/tiingo.py, tests/conftest.py, tests/test_registry_convert.py, test.py, tests/test_alpaca_acquisition.py, tests/test_chunked_ingest.py, tests/test_config_paths.py, tests/test_construction_raw_fallback.py, tests/test_dataset_hierarchy.py, tests/test_dataset_update.py, tests/test_dataset_update_evidence.py, tests/test_extensibility_contract.py, tests/test_ingest_conversion_gate.py, tests/test_raw_hive_layout.py, tests/test_source_inspector.py, tests/test_spot_dataset.py, tests/test_stock_dataset.py, tests/test_symbol_coord_encoding.py</files>

  <read_first>
    - `quantlab/acquisition/registry.py` — the whole file. `Capability` at `:53` (its docstring
      carries the rule that DECIDES D-01 — cite it, do not restate it), `SourceDescriptor` at
      `:89`, `supports()` at `:134`, `run()` at `:302` (the signature and body shape `convert()`
      mirrors; body at `:359-363`), and the vendor-import banner at `:366-396` whose imports must
      stay LAST (03.4 D-07).
    - `quantlab/base/acquisition.py:95-134` — `AcquisitionResult`, and specifically `:113-118`,
      the "defined HERE in base/ because the base layer must not import the acquisition package"
      argument `ConversionResult` reuses.
    - `quantlab/base/data.py:86-96` (`BaseDataset.__init__`, whose two-line ordering is
      load-bearing), `:520-716` (`from_raw_data_chunked` — the loop `convert()` wraps, the pinned
      symbol axis, the `ChunkLedger` resume, `on_new_listing`).
    - `quantlab/base/config.py:1-31` — `BaseDatasetConfig` / `DatasetConfig`; `Market` is already
      imported at `:4`. `AcquisitionConfig` at `:38-50` is the analog where `market` is the FIRST
      field.
    - `quantlab/acquisition/inspector.py:71-75` — the verified precedent that
      `quantlab/acquisition/*` may import `quantlab.dataset.stock` with no cycle.
    - `quantlab/dataset/stock.py:477-497` — `StockDataset._raw_data_to_xr_window`, the override
      that makes chunked conversion a genuine memory win for US equity.
    - `tests/test_chunked_ingest.py:1-120` — the working recipe for a real offline chunked run
      (raw parquet fixture tree, vendor-terminated `raw_data_dir_path`, pinned symbol axis).
    - `tests/conftest.py:680-923` — `spot_kline_zarr`, `stock_pqt_row`, `write_stock_pqt`,
      `stock_zarr`; the last two construct `DatasetConfig(...)` directly and are two of the
      `market=` restoration sites.
    - `.planning/phases/03.5-registry-level-raw-to-zarr-conversion-entry-point/03.5-PATTERNS.md`
      sections "module-level `convert()`", "`Capability.dataset_cls`", "`ConversionResult`" and
      "reinstate `DatasetConfig.market`".
    - Run `git show 441e630` and read the diff — it is the literal checklist for the `market`
      restoration. Restore ONLY the `market:` field and the `market=` / `"market"` /
      `cfg.market` lines; `441e630` also reflowed formatting and trimmed docstrings, and that
      reflow is NOT reverted.
  </read_first>

  <action>
Wire ONE entry point — Tiingo `us_equity`/`1d` — through every layer to a real Zarr write. No
other vendor, no reporter, no cancel, no guard. Four layers, in this order:

(1) CONFIG LAYER, per D-02. Reinstate `market: Market` on `DatasetConfig` in
`quantlab/base/config.py`, placed beside `frequency` to mirror `AcquisitionConfig`. Required, no
default, `kw_only=True` so ordering is free. It stays OFF `BaseDatasetConfig` — a constituent
panel has no market. Then walk `git show 441e630` backwards and restore every call site: both
factories in `quantlab/config/__init__.py` (`spot_kline_config` and `stock_kline_config` already
TAKE a `market` parameter and use it for path construction via `_market_downloads_root`, so each
is one `market=market,` line), the two direct `DatasetConfig(...)` constructions in
`tests/conftest.py` (`spot_kline_zarr` and `stock_zarr`), `test.py`, and the thirteen test
modules the commit touched. Two of those are assertions rather than constructions:
`tests/test_config_paths.py` regains its two `cfg.market` equality assertions, and
`tests/test_dataset_hierarchy.py` regains the `"market"` entry in `MARKET_ONLY_CONFIG_FIELDS`
(that set is what keeps the field off the shared base). Restoring the field is the whole point
of this layer: it makes the capability lookup key exactly `Capability`'s own key, per D-02.

(2) REGISTRY DATA LAYER, per D-01. Add `dataset_cls: type[MarketDataset] | None = None` to
`Capability`, appended AFTER the existing optional fields (`earliest_available`, `entitlement`)
so every existing positional or keyword construction is unchanged, with a `#:` comment in the
same shape as those two saying what it holds and that `None` IS the SC-7 refusal expressed as
data. Annotate with the ABC `MarketDataset` imported from `quantlab.base.data`, following the
rule `SourceDescriptor.acquisition_cls` states at `registry.py:106-110` — that keeps the TOP of
`registry.py` vendor-free. In `quantlab/acquisition/tiingo.py`, fill the single capability row
(`tiingo.py:333-335`) with `dataset_cls=StockDataset`, importing `StockDataset` from
`quantlab.dataset.stock` at the top of that module. Leave `alpaca.py` for Task 2.

(3) BASE LAYER, per D-04. Add a frozen dataclass `ConversionResult` to `quantlab/base/data.py`,
placed above `BaseDataset`, carrying: `zarr_path: str`, `ledger_path: str`, `granularity: str`,
`pinned_symbols: int`, `windows_planned: int`, `windows_written: int`, `windows_skipped: int`,
`rows_written: int`, `peak_window_bytes: int | None`, `predicted_peak_bytes: int | None = None`,
`resumed: bool = False`, `cancelled: bool = False`. Its docstring cites — does not re-derive —
`AcquisitionResult`'s `base/acquisition.py:113-118` argument for living in `base/`. State
plainly that `peak_window_bytes` is the OBSERVED peak (`window.nbytes` of the largest window this
run actually materialised, `None` when no window was written) while `predicted_peak_bytes` is the
caller's own pre-flight estimate echoed back, because D-11 puts the guard at the call site and
`convert()` has no roster category to size against. Then make
`BaseDataset.from_raw_data_chunked()` record its outcome: initialise
`self.last_chunk_result: ConversionResult | None = None` as the FIRST line of
`BaseDataset.__init__` (the existing two-line ordering comment below it still governs those two
lines), count skipped versus written windows and appended rows inside the existing loop, track
the largest `window.nbytes`, set `resumed=True` when at least one ledger-recorded window was
skipped, and assign `self.last_chunk_result` before returning `self`. This mirrors
`Acquisition.last_result`, which `run()` reads at `registry.py:363` — the same handoff, one
layer down. The method still returns `Self`; no existing caller changes.

(4) ENTRY POINT, per D-03. Add `capabilities_for(self, market, frequency, data_type=None) ->
tuple[Capability, ...]` to `SourceDescriptor` directly after `supports()`, returning every
matching capability with the same "`data_type=None` means do not care" semantics `supports()`
already documents, and re-express `supports()` as the boolean of that tuple so the predicate
exists once. Then add a module-level
`convert(descriptor, dataset_config, *, data_type=None, granularity="year",
on_new_listing="refuse", predicted_peak_bytes=None) -> ConversionResult` to
`quantlab/acquisition/registry.py`, inserted AFTER `run()`'s body ends at `:363` and BEFORE the
vendor-import banner at `:366` — the only position that is both adjacent to `run()` (making 03.4
D-14's "two calls, never a flag" visible while reading) and compatible with imports-must-be-last.
Its body: resolve capabilities for `(dataset_config.market, dataset_config.frequency,
data_type)`; on the happy path read `capability.dataset_cls`, construct
`dataset_cls(dataset_config)`, call `from_raw_data_chunked(granularity=granularity,
on_new_listing=on_new_listing)`, and return the dataset's `last_chunk_result` with
`predicted_peak_bytes` attached via `dataclasses.replace`. `convert()` WRITES, period — it does
not return an unsaved object, which is the asymmetry between `from_raw_data()` (returns `Self`,
leaves writing to `.save()`) and `from_raw_data_chunked()` (writes inside its loop) that this
entry point exists to normalise. Task 2 adds the three refusal arms; for this task a single
unmatched or `None`-target capability may raise a bare `ValueError` naming the tuple, which Task
2 replaces with the real messages. Import `ConversionResult` from `quantlab.base.data` at the top
of `registry.py`, the same direction `AcquisitionResult` is already imported at `registry.py:47`.
`convert()`'s docstring follows `run()`'s house voice — bolded claim headers, decision IDs
inline, and an explicit statement of what it does NOT do — and it must state the D-11 admission:
this function does not run the RAM guard; the caller does, and a caller that forgets goes to OOM.

(5) THE TRACER TEST. Create `tests/test_registry_convert.py` with one end-to-end test that builds
a raw Tiingo-shaped parquet tree with `write_stock_pqt` / `stock_pqt_row` under a
vendor-terminated `raw_data_dir_path` (copy the layout from `tests/test_chunked_ingest.py` —
`StockDataset._scan_raw` asserts `Path(raw_data_dir_path).name == vendor`), constructs a
`DatasetConfig` with `market="us_equity"`, calls `convert(TIINGO_SOURCE, config)` and asserts:
the Zarr store exists on disk and opens with the expected `timestamp`/`symbol` axes; the returned
object is a `ConversionResult`; `windows_written` equals the planned window count and
`windows_skipped` is 0; `pinned_symbols` equals the fixture roster length; `zarr_path` equals
`config.zarr_file_path`. Add a second test in the same module that calls `convert()` a SECOND
time on the same config and asserts `windows_written == 0`, `windows_skipped` equals the first
call's `windows_written`, and `resumed is True` — that is the DATA-07 idempotency probe row, and
it must be a real assertion rather than prose. Offline: no credential, no network, `tmp_path`
only.
  </action>

  <verify>
    <automated>uv run pytest tests/test_registry_convert.py -q</automated>
    <fails_when>non-zero exit, or "failed"/"error" appears in the pytest summary line, or the summary reports fewer than 2 passed</fails_when>
    <automated>uv run pytest -q</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line (a missed `market=` restoration site surfaces here as a TypeError about a required keyword argument)</fails_when>
    <automated>uv run python -c "import inspect, quantlab.acquisition.registry as r; src=inspect.getsource(r); assert src.index('def convert(') > src.index('def run('), 'convert() must sit after run()'; assert src.index('def convert(') < src.index('from quantlab.acquisition import alpaca'), 'convert() must sit above the vendor-import banner'; print('placement ok')"</automated>
    <fails_when>non-zero exit, or stdout does not contain "placement ok"</fails_when>
    <automated>uv run python -c "import inspect, quantlab.acquisition.registry as r; from quantlab.base.data import ConversionResult; assert 'ConversionResult' in inspect.signature(r.convert).return_annotation or r.convert.__annotations__.get('return') is ConversionResult; assert list(inspect.signature(r.convert).parameters)[:2] == ['descriptor','dataset_config']; print('signature ok')"</automated>
    <fails_when>non-zero exit, or stdout does not contain "signature ok"</fails_when>
  </verify>

  <acceptance_criteria>
    - `quantlab/acquisition/registry.py` contains the literal `def convert(`.
    - `quantlab/base/data.py` contains the literal `class ConversionResult`.
    - `quantlab/base/config.py`'s `DatasetConfig` has a required `market` field:
      `uv run python -c "import dataclasses as d; from quantlab.base.config import DatasetConfig, BaseDatasetConfig; f={x.name:x for x in d.fields(DatasetConfig)}; assert 'market' in f; assert f['market'].default is d.MISSING; assert 'market' not in {x.name for x in d.fields(BaseDatasetConfig)}; print('ok')"` prints `ok`.
    - `uv run python -c "from quantlab.acquisition.registry import DataSourceRegistry; from quantlab.dataset.stock import StockDataset; c=DataSourceRegistry.get('tiingo').capabilities[0]; assert c.dataset_cls is StockDataset; print('ok')"` prints `ok`.
    - `uv run python -c "from quantlab.acquisition.registry import DataSourceRegistry as R; d=R.get('tiingo'); assert d.capabilities_for('us_equity','1d'); assert d.capabilities_for('us_equity','tick') == (); assert d.supports('us_equity','1d') and not d.supports('us_equity','tick'); print('ok')"` prints `ok`.
    - `uv run pytest -q` exits 0 with the same or a higher passed count than before this task
      (baseline at plan time: 745 tests collected).
    - `tests/test_registry_convert.py` asserts a Zarr store exists on disk after `convert()` —
      not merely that a `ConversionResult` was returned.
    - The restoration added `market` lines and nothing else: every file `git show 441e630` names
      as having lost a `market=` / `"market"` / `cfg.market` line carries it again, verified by
      `grep -rlc 'market' quantlab/config/__init__.py tests/conftest.py test.py tests/test_alpaca_acquisition.py tests/test_chunked_ingest.py tests/test_config_paths.py tests/test_construction_raw_fallback.py tests/test_dataset_hierarchy.py tests/test_dataset_update.py tests/test_dataset_update_evidence.py tests/test_extensibility_contract.py tests/test_ingest_conversion_gate.py tests/test_raw_hive_layout.py tests/test_source_inspector.py tests/test_spot_dataset.py tests/test_stock_dataset.py tests/test_symbol_coord_encoding.py`
      printing a non-zero count for each, and by `uv run pytest -q` exiting 0 (a missed site is a
      TypeError about a required keyword argument, never a silent pass).
  </acceptance_criteria>

  <done>
`convert(TIINGO_SOURCE, dataset_config)` writes a real Zarr store and returns a populated
`ConversionResult`; a second identical call skips every window and reports `resumed is True`; the
full suite is green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Alpaca's four capability rows and the three ways a capability lookup can refuse</name>

  <files>quantlab/acquisition/alpaca.py, quantlab/acquisition/registry.py, tests/test_registry_convert.py</files>

  <read_first>
    - `quantlab/acquisition/alpaca.py:900-930` — `ALPACA_SOURCE`, the four `Capability(...)` rows
      and the `#:` comment above them explaining why the list is not a cross-product. The new
      comment about the two deliberately-empty `dataset_cls` fields goes in that voice.
    - `ingest_alpaca.py:321-335` — the existing parser-level tick refusal. Its message is the
      VOICE the programmatic refusal copies: it names the irregular event axis, says the dense
      `[timestamp, symbol]` panel cannot express it, names phase 03.3 and D-18, and tells the user
      what the deliverable actually is.
    - `tests/test_source_registry.py:360-400` —
      `test_capabilities_match_the_vendor_class_constants`. Confirm for yourself what the plan
      already verified: it reads capability ATTRIBUTES, never constructs `Capability(...)`, so it
      needs no change.
    - `quantlab/acquisition/registry.py` — `convert()` as Task 1 left it, and
      `SourceDescriptor.capabilities_for`.
    - `tests/test_registry_convert.py` — the module Task 1 created.
  </read_first>

  <behavior>
    - Given the Alpaca descriptor and a `DatasetConfig(market="us_equity", frequency="1d")`,
      `convert()` resolves to `StockDataset` and converts.
    - Given `frequency="tick"` (either `data_type`), `convert()` raises `ValueError` whose message
      names `03.3` and the irregular event axis, and no Zarr store is created.
    - Given a `(market, frequency)` no capability serves, `convert()` raises `ValueError` naming
      the requested tuple and enumerating the capabilities the descriptor does serve.
    - Given a `(market, frequency)` matching two capabilities with two DIFFERENT conversion
      targets and no `data_type` supplied, `convert()` raises `ValueError` telling the caller to
      pass `data_type=` and naming the candidates. (Exercised with a synthetic descriptor through
      the `isolated_registry` fixture — no live vendor has this shape today, which is exactly why
      it needs a test rather than a comment.)
    - Given `frequency="tick"`, `capabilities_for` still MATCHES the rows — the refusal comes from
      the absence of a conversion target, not from the lookup failing.
  </behavior>

  <action>
Fill the Alpaca rows and make `convert()`'s three refusal arms real, replacing Task 1's
placeholder raise.

In `quantlab/acquisition/alpaca.py:918-923`, give the two `data_type="bars"` rows (`1d` and `1m`)
`dataset_cls=StockDataset`, importing `StockDataset` from `quantlab.dataset.stock` at the top of
the module. LEAVE the two `tick` rows at the `None` default and add a `#:` comment above the
tuple, in the voice of the existing comment at `alpaca.py:911-917`, stating that the two empty
fields are the refusal expressed as data (D-01/SC-7) and that Phase 03.3 turns the refusal off by
filling two fields rather than by deleting a branch.

In `quantlab/acquisition/registry.py`, replace `convert()`'s placeholder raise with three named
outcomes, all `ValueError`, all built from the descriptor and the requested tuple with no vendor
literal anywhere in the body:

- NO MATCH — the matched tuple is empty. The message names `descriptor.display_name`, the
  requested `(market, frequency, data_type)`, and the capabilities the descriptor DOES serve
  (rendered from `descriptor.capabilities`), and says that adding one is adding a `Capability`
  row beside the vendor class rather than a branch here.
- AMBIGUOUS — two or more matches carry two or more DISTINCT `dataset_cls` values and no
  `data_type` was supplied. The message names how many matched, lists their `data_type` values,
  and tells the caller to pass `data_type=` to disambiguate. This is the case D-02's whole
  argument is about — a vendor serving two markets or two shapes at one frequency — so it refuses
  rather than picking the first row.
- NO CONVERSION TARGET (SC-7) — exactly one target resolved and it is `None`. Copy the voice of
  `ingest_alpaca.py:321-333`: name the capability, say the raw-to-xarray conversion for it needs
  an irregular event axis the dense `[timestamp, symbol]` panel cannot express, name phase 03.3
  and D-18, and say the raw shards are the deliverable for this capability. Keep the existing
  parser-level refusal in `ingest_alpaca.py` untouched — it fires before any work is done; this
  is the second, programmatic layer for callers that never touch argparse.

Add tests to `tests/test_registry_convert.py` covering all five behaviours above. Write the
assertions FIRST and watch them fail before filling the rows — the tick arm in particular passes
trivially against Task 1's placeholder raise if the message is not asserted, so assert on the
substrings `03.3` and the event-axis phrase, not merely on `ValueError`. Use the
`isolated_registry` fixture (`tests/conftest.py:60`) to register the synthetic two-target
descriptor for the ambiguity case; that fixture restores `DataSourceRegistry.SOURCES` by
rebinding the saved tuple object on teardown, so the fake never leaks into a later test.
  </action>

  <verify>
    <automated>uv run pytest tests/test_registry_convert.py tests/test_source_registry.py -q</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line</fails_when>
    <automated>uv run python -c "from quantlab.acquisition.registry import DataSourceRegistry as R; from quantlab.dataset.stock import StockDataset; caps=R.get('alpaca').capabilities; bars=[c for c in caps if c.data_type=='bars']; tick=[c for c in caps if c.frequency=='tick']; assert len(bars)==2 and all(c.dataset_cls is StockDataset for c in bars); assert len(tick)==2 and all(c.dataset_cls is None for c in tick); print('rows ok')"</automated>
    <fails_when>non-zero exit, or stdout does not contain "rows ok"</fails_when>
    <automated>uv run python -c "import inspect, quantlab.acquisition.registry as r; src=inspect.getsource(r.convert); assert 'tiingo' not in src.lower() and 'alpaca' not in src.lower(), 'convert() must carry no vendor literal'; assert 'tick' not in src.lower().split('\"\"\"')[-1], 'convert() must not branch on a frequency literal outside its docstring'; print('no vendor literal')"</automated>
    <fails_when>non-zero exit, or stdout does not contain "no vendor literal"</fails_when>
    <automated>uv run pytest -q</automated>
    <fails_when>non-zero exit, or "failed"/"error" in the pytest summary line</fails_when>
  </verify>

  <acceptance_criteria>
    - All four Alpaca capability rows exist and `dataset_cls` is `StockDataset` on exactly the two
      `bars` rows and `None` on exactly the two `tick` rows (pinned by the runtime check above).
    - The SC-7 refusal message contains the literal `03.3` and names the irregular event axis;
      asserted by a test, not by inspection.
    - `convert()`'s source contains no vendor name and no frequency literal outside its docstring.
    - `tests/test_source_registry.py` is UNCHANGED — adding an optional `Capability` field does
      not touch it: `grep -c 'Capability(' tests/test_source_registry.py` prints 0, proving that
      file still pins capability ATTRIBUTES rather than constructing `Capability` objects.
    - `ingest_alpaca.py`'s parser-level tick refusal is untouched:
      `grep -c 'to-zarr is not available with --frequency tick' ingest_alpaca.py` prints 1.
    - `uv run pytest -q` exits 0.
  </acceptance_criteria>

  <done>
Both vendors carry conversion targets where a target exists and `None` where none can exist yet;
`convert()` refuses in three distinguishable, tested ways, and the tick refusal is the absence of
a target rather than a branch.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| out-of-repo in-process caller → `convert()` | `quantlab-console` (a separate repository) imports quantlab and calls `convert()` with a `DatasetConfig` it constructed. Same process, same privileges, but not this repository's code. |
| `DatasetConfig` paths → filesystem | `zarr_file_path`, `raw_data_dir_path` and the derived `ChunkLedger` sidecar path are caller-supplied strings that this phase writes to. |
| `ConversionResult` → caller's renderer | A new egress path for strings out of quantlab into a UI the repository does not control. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.5-01 | Tampering | `convert()` → `DatasetConfig.zarr_file_path` / `raw_data_dir_path` | medium | accept | `convert()` introduces NO new path input: it consumes a `DatasetConfig` the caller already constructs today and could hand straight to `StockDataset(...)`. Both sides run in one process at one privilege level, so canonicalising here would move no boundary. ASVS L1 does not require path canonicalisation for a same-privilege in-process API. Re-open if a future phase accepts a config from an untrusted transport. |
| T-03.5-02 | Information Disclosure | `ConversionResult` fields, `convert()` refusal messages | high | mitigate | `ConversionResult` carries only paths, integer counts and booleans — no vendor response body, no exception text, no environment value. `convert()` constructs NO vendor client and reads NO credential (it constructs a `Dataset`, not an `Acquisition`), so there is no credential in scope to leak. Pinned by the acceptance criterion that the result type's fields are exactly the declared set. |
| T-03.5-03 | Denial of Service | `convert()` with no RAM guard (D-11) | medium | accept | Developer-accepted and recorded in D-11: the guard lives at every call site, not inside `convert()`. Plan 02 gives out-of-repo callers `estimate_chunked_panel()` — a non-raising way to ask before allocating — which is the mitigation that IS in scope. Residual risk: a new integrator who never asks reaches OOM. Revisit if observed. |
| T-03.5-04 | Spoofing | `Capability.dataset_cls` as a direct class reference | low | accept | A direct class reference (03.4 D-03), not a dotted path resolved at runtime through `get_cls_from_path`. There is no string an attacker could influence into becoming an imported class. |
| T-03.5-SC | Tampering | npm/pip/cargo installs | low | accept | This plan adds NO package-manager install task and no new dependency — `pyproject.toml` and `uv.lock` are not in `files_modified`. The package-legitimacy gate is therefore not applicable; no `## Package Legitimacy Audit` is required. Re-open the gate if any task in this phase grows an install step. |
</threat_model>

<verification>
- `uv run pytest -q` exits 0 over the whole suite.
- `uv run pytest tests/test_registry_convert.py -q` exits 0 and reports at least 6 passed.
- A cold `uv run python -c "from quantlab.acquisition.registry import convert, DataSourceRegistry; print([d.vendor for d in DataSourceRegistry.all()])"` prints `['alpaca', 'tiingo']` — the bottom vendor-import block still runs after `convert()`'s definition.
</verification>

<success_criteria>
- DATA-07 is reachable: a caller with a descriptor and a `DatasetConfig` converts to Zarr without
  naming a vendor class, a `Dataset` subclass, or an `ingest_*.py` script.
- SC-1 (conversion startable through the registry) and SC-7 (tick stays refused) are satisfied by
  this plan alone.
- SC-2's surviving first half holds: `run()` is untouched and conversion is a separate function.
</success_criteria>

<output>
Create `.planning/phases/03.5-registry-level-raw-to-zarr-conversion-entry-point/03.5-01-SUMMARY.md` when done
</output>
