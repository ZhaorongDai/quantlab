---
phase: 03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse
plan: 11
type: execute
wave: 6
depends_on: ["03.7-02", "03.7-10"]
files_modified:
  - quantlab/utils/module.py
  - tests/test_backtest_rebuild.py
autonomous: true
requirements: [CFG-01, BT-01]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "load_backtester_from_config(config) rebuilds the backtester class, its CrossSectionBacktestConfig, the price dataset, the model with its factors and labels, and every parameter from a persisted config.json dict, without mutating the input (D-25, D-26)"
    - "A rebuilt backtester's get_config() equals the original's; the data_fingerprint record moves onto expected_fingerprint instead of into the config (D-25, D-26, D-27)"
    - "load_backtester_from_config refuses a class that is not a BaseBacktester subclass with a TypeError naming it, before any dataset or model is built (D-25)"
    - "Loading a run's config.json and calling run() reproduces identical target weights and an identical equity curve, for model_mode 'load' and for 'train' (D-25)"
    - "Loading a run_cv run's config.json and calling run_cv() reproduces the identical stitched equity curve (D-25)"
    - "An unchanged store re-runs without a fingerprint warning; a changed store re-runs with a warning naming the dataset and still completes (D-27)"
    - "The rebuild path never routes through quantlab/config factories (D-32)"
  artifacts:
    - path: "quantlab/utils/module.py"
      provides: "load_backtester_from_config"
      contains: "def load_backtester_from_config"
    - path: "tests/test_backtest_rebuild.py"
      provides: "config round-trip and identical re-run locks"
  key_links:
    - from: "quantlab/utils/module.py"
      to: "quantlab/base/backtest.py"
      via: "lazy import of BaseBacktester for the subclass check; expected_fingerprint assignment"
      pattern: "expected_fingerprint"
    - from: "quantlab/utils/module.py"
      to: "quantlab/utils/module.py"
      via: "reuses load_dataset_from_config and load_model_from_config for the nested configs"
      pattern: "load_model_from_config\\("
---

<objective>
Close the reproducibility loop D-25 asks for: a backtest's persisted `config.json` rebuilds everything the run needed and re-runs it identically. That covers datasets, factors, labels, the model and its checkpoint reference, and every parameter. The data fingerprint is compared on the way (D-27).

Purpose: CFG-01. The user said "回测器也是从一个回测器的 config 直接构建所有需要的东西" (the backtester is also built directly from a backtester config), and this plan makes that literally true and proves it end to end for run and run_cv.

Output: `load_backtester_from_config` in `quantlab/utils/module.py` and `tests/test_backtest_rebuild.py`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-CONTEXT.md
@.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-RESEARCH.md
@.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-02-SUMMARY.md
@.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-10-SUMMARY.md
@quantlab/utils/module.py

The 03.7-02 SUMMARY is referenced for the config_cls loaders this plan composes; the 03.7-10 SUMMARY for the final get_config and run_cv shapes.

Standing constraints for every task in this plan:
- The user has uncommitted edits in `quantlab/enums/data.py`, `quantlab/factor/alpha158.py`, `quantlab/label/fret.py`. Never edit them and never `git add` them; commit only this plan's files, by explicit path.
- Never use `git stash`.
- Scope fence: this plan owns ONLY `quantlab/utils/module.py` and `tests/test_backtest_rebuild.py`. If a rebuild test exposes a gap in `BaseBacktester.get_config()`, record it in the SUMMARY as a deviation and fix it in `quantlab/base/backtest.py` only within this plan's commit, naming that extra file explicitly in the commit and SUMMARY. No other plan edits that file in this wave.
- `quantlab/utils/module.py` must not import the backtest layer at module import time. The BaseBacktester import is local to the function, which is the exception the 03.7-01 layering lock already allows for this file.
- Model-training reruns inside one second collide on the model layer's second-resolution project directory name (`BaseModel.train`). Train-mode tests therefore wait just over one second between the two runs, with a comment naming that cause.
- Configs are constructed directly, never through quantlab/config factories (D-32).
- A lock that passes on arrival is mutation-verified in a scratch copy and the original copied back; record each in the SUMMARY.
- The full-suite gate is `bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh`.
- Spec-less probe fallback: skipped (no requirement IDs to probe); recorded as a visible choice.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: load_backtester_from_config rebuilds the whole backtester from its JSON</name>
  <reversibility rating="costly">config.json is a persisted, user-facing format (D-25). The loader's field mapping becomes the compatibility surface for every stored run. The user already decided this, so no checkpoint.</reversibility>
  <files>quantlab/utils/module.py, tests/test_backtest_rebuild.py</files>
  <read_first>
    - quantlab/utils/module.py (get_cls_from_path, _config_cls_of, load_dataset_from_config, load_factor_from_config, load_model_from_config)
    - quantlab/base/backtest.py (get_config, expected_fingerprint, __init__ ordering; read only unless the scope-fence exception applies)
    - quantlab/base/config.py (BacktestConfig/CrossSectionBacktestConfig fields and to_dict)
    - tests/backtest_fixtures.py (write_price_store, make_model, train_checkpoint; fixture classes resolve by the dotted path `tests.backtest_fixtures.*`)
    - tests/test_config_roundtrip.py (JSON-normalized equality style from 03.7-02)
    - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-RESEARCH.md (Pitfalls 8 and 9; Security Domain: arbitrary class import)
  </read_first>
  <behavior>
    - `test_rebuilt_backtester_has_the_same_class_config_class_and_config`: `get_config()` passes through JSON, then `load_backtester_from_config`. The rebuilt object is a USEquityCrossectionSelectStockVectorBt, `type(config)` is CrossSectionBacktestConfig, `type(config.model.config)` is MLConfig, and the JSON-normalized `get_config()` equals the original's.
    - `test_rebuild_from_a_run_config_moves_the_fingerprint_to_expected`: from a real run's config.json, the rebuilt object's `expected_fingerprint` equals the file's `data_fingerprint`, and the rebuilt `get_config()` has no data_fingerprint key before it runs.
    - `test_loader_does_not_mutate_its_input`: a deep copy before equals the dict after.
    - `test_non_backtester_class_is_refused_before_building_anything`: `name` points at `quantlab.dataset.stock.StockDataset`. TypeError names that path, and spies on load_dataset_from_config and load_model_from_config record zero calls.
    - `test_backtester_rebuild_imports_no_config_factories`: an AST check that tests/test_backtest_rebuild.py and quantlab/utils/module.py import nothing resolving to quantlab.config.
  </behavior>
  <action>
Write the five tests first in `tests/test_backtest_rebuild.py`. Its English module docstring states that it locks D-25, D-26 (backtester part) and D-27 on rebuild. It says fixture classes must live in an importable module (`tests.backtest_fixtures`), because a class defined in `__main__` cannot be rebuilt by dotted path. Add an autouse offline-wandb fixture.

Implement `load_backtester_from_config(config: dict)` in `quantlab/utils/module.py`, with an English docstring:
1. `config = copy.deepcopy(config)`.
2. `expected = config.pop("data_fingerprint", None)`: a record of what the original run read, not a config field.
3. Local import `from quantlab.base.backtest import BaseBacktester`.
4. `cls = get_cls_from_path(config["name"])`. If it is not a type or not a subclass of BaseBacktester, raise TypeError naming `config["name"]`, before touching any nested config. This is the RESEARCH Security Domain mitigation against a tampered name.
5. `config["price_dataset"] = load_dataset_from_config(config["price_dataset"])` and `config["model"] = load_model_from_config(config["model"])`.
6. `config["benchmark_dataset"]` becomes `load_dataset_from_config(...)` when present and not None, else None.
7. `backtester = cls(_config_cls_of(cls)(**config))`, then `backtester.expected_fingerprint = expected`, and return it.

The docstring also states the trust stance: a config JSON is trusted local input, like a checkpoint.

Mutation-verify in a scratch copy: move the subclass check after the nested loads; the refusal spy test goes red. Copy the original back.
  </action>
  <verify>
    <automated>uv run pytest tests/test_backtest_rebuild.py -q -k "rebuilt or moves or mutate or refused or imports"</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line, or fewer than 5 passed</fails_when>
  </verify>
  <acceptance_criteria>
    - The verify command exits 0 with "5 passed".
    - `grep -c "def load_backtester_from_config" quantlab/utils/module.py` prints 1.
    - `uv run python -c "import ast,pathlib; t=ast.parse(pathlib.Path('quantlab/utils/module.py').read_text()); top=[n for n in t.body if isinstance(n,(ast.Import,ast.ImportFrom))]; mods={getattr(n,'module',None) for n in top}|{a.name for n in top if isinstance(n,ast.Import) for a in n.names}; assert not any(m and 'backtest' in m for m in mods), mods; print('no module-level backtest import')"` prints "no module-level backtest import".
    - `uv run pytest tests/test_backtest_contracts.py tests/test_config_roundtrip.py tests/test_model_hierarchy.py -q` exits 0.
    - The SUMMARY records the observed-red mutation.
  </acceptance_criteria>
  <done>A persisted backtest config rebuilds the full backtester with the right classes, refuses non-backtester classes before building anything, and carries the fingerprint forward for comparison.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: A rebuilt config re-runs identically, for run and run_cv, and notices changed data</name>
  <files>tests/test_backtest_rebuild.py</files>
  <read_first>
    - tests/test_backtest_rebuild.py (after Task 1)
    - tests/test_backtest_run_cv.py (the train_cv fixture shape from 03.7-10)
    - quantlab/base/backtest.py (run, run_cv, _compare_fingerprints; read only unless the scope-fence exception applies)
    - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-CONTEXT.md (D-25, D-27)
  </read_first>
  <behavior>
    - `test_load_mode_rebuild_reproduces_weights_and_equity`: run once, `json.loads((run_dir / "config.json").read_text())`, rebuild, run again. The two runs' weights.zarr are identical after loading, their equity `value` arrays are exactly equal, metrics whole "Total Return [%]" is equal, and no fingerprint warning appears.
    - `test_train_mode_rebuild_retrains_and_reproduces_weights_and_equity`: same for model_mode "train", waiting just over one second between runs because the model layer names project directories to the second.
    - `test_run_cv_rebuild_reproduces_the_stitched_curve`: run_cv, rebuild from its config.json, run_cv again, and the stitched equity values and stitched weights are identical.
    - `test_changed_store_rebuild_warns_and_completes`: after the first run, overwrite one adjusted close value in the price store (open the Zarr in "r+" mode). The rebuilt run() logs a warning naming "price_dataset" and returns a result.
  </behavior>
  <action>
Add the four tests to `tests/test_backtest_rebuild.py`.
- Compare weights with `xr.testing.assert_identical` on the two loaded `weights.zarr` datasets.
- Compare equity with `numpy.testing.assert_array_equal` on the `value` variables.
- Overwrite the store value through `xarray.open_zarr` plus a direct zarr array write, or by rewriting the whole synthetic store with one changed cell via the fixture writer. Either way, the fingerprint must see the change; record which one you used.

If a test fails because `BaseBacktester.get_config()` omits something `run()` depends on, fix it under the scope-fence exception and record it. Examples: a selector parameter missing from the dict, or checkpoint or cv_project_dir not persisted.

Mutation-verify in a scratch copy of quantlab/utils/module.py: drop the `expected_fingerprint` assignment; the changed-store test goes red, because no warning appears. Copy the original back. Then run the baseline gate.
  </action>
  <verify>
    <automated>uv run pytest tests/test_backtest_rebuild.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line, or fewer than 9 passed</fails_when>
    <automated>bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh</automated>
    <fails_when>non-zero exit, or output containing "NEW FAILURE(S)", "NEW ERROR(S)" or "NO 'passed' SUMMARY LINE"</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_backtest_rebuild.py -q` exits 0 with "9 passed".
    - `bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh` exits 0.
    - The SUMMARY records the store-change method used, any scope-fence exception taken, and the observed-red mutation.
  </acceptance_criteria>
  <done>A stored backtest config re-runs to identical weights and equity for load, train and CV modes, and a data change between runs is announced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.json on disk → `load_backtester_from_config` | a persisted dict names classes to import and paths to read/deserialize |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.7-35 | Tampering / Elevation of Privilege | `get_cls_from_path(config["name"])` | medium | mitigate | `issubclass(cls, BaseBacktester)` check before any nested config is built; spy test proves nothing else is constructed first; configs are documented as trusted local input |
| T-03.7-36 | Repudiation | re-run silently differing from the original | high | mitigate | Identical-rerun tests for load/train/CV plus fingerprint comparison with a warning on changed data (D-27) |
| T-03.7-37 | Tampering | checkpoint path inside a rebuilt config → deserialization | medium | accept | Same trusted-local-file stance as `BaseModel.load`, with its suffix check |
</threat_model>

<verification>
- `uv run pytest tests/test_backtest_rebuild.py tests/test_backtest_contracts.py tests/test_config_roundtrip.py -q` exits 0.
- `bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh` exits 0.
- Spec-less probe fallback: skipped (no requirement IDs to probe). Recorded here as a visible choice.
</verification>

<success_criteria>
- D-25 holds end to end for run and run_cv, in load and train modes.
- D-26's backtester round trip is regression-locked; D-27 comparison fires on changed data.
- No failures beyond the pre-phase baseline.
</success_criteria>

<output>
Create `.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-11-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Symbols created by this plan:

- `quantlab/utils/module.py`: `load_backtester_from_config`
- `tests/test_backtest_rebuild.py`: `test_rebuilt_backtester_has_the_same_class_config_class_and_config`, `test_rebuild_from_a_run_config_moves_the_fingerprint_to_expected`, `test_loader_does_not_mutate_its_input`, `test_non_backtester_class_is_refused_before_building_anything`, `test_backtester_rebuild_imports_no_config_factories`, `test_load_mode_rebuild_reproduces_weights_and_equity`, `test_train_mode_rebuild_retrains_and_reproduces_weights_and_equity`, `test_run_cv_rebuild_reproduces_the_stitched_curve`, `test_changed_store_rebuild_warns_and_completes`
