---
phase: 03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse
plan: 13
type: execute
wave: 7
depends_on: ["03.7-10", "03.7-11"]
files_modified:
  - example/backtest.md
  - example/README.md
  - example/model.md
autonomous: true
requirements: [BT-01, CFG-01, CLEAN-02]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "example/backtest.md explains, in example/model.md's style, the hierarchy (D-01), the run()/run_cv() template (D-02, D-16, D-35), the weights contract (D-03), price columns and t+1 fills (D-04, D-05), the universe and delisting rule (D-06, D-07), selection (D-09..D-12), model preparation and warm-up (D-13..D-15), in/out-of-sample reporting (D-17, D-22, D-34), rebalancing and costs (D-18..D-20), the run directory (D-23, D-24, D-28) and rebuild with fingerprints (D-25..D-27)"
    - "example/backtest.md states that short-side returns are optimistic because no borrow cost is modelled (D-21), and that benchmark comparison is not available until directly-downloaded index price data exists (D-08)"
    - "Every config in example/backtest.md is constructed directly (DatasetConfig, PolarsFactorConfig, MLConfig, CrossSectionBacktestConfig), never through the config factories package (D-32)"
    - "The minimal example in example/backtest.md runs offline on a synthetic store, and its pasted output is the real output; any snippet not run is labelled 此例未实际运行 (example/README.md convention)"
    - "example/model.md documents predict_panel with the MLP and RNN adapters (D-29, D-33) and the versioned cv_folds.json (D-30, D-36), and its old backtest-skeleton section is kept but marked as deleted in phase 03.7 (D-37)"
    - "example/README.md lists backtest.md and marks the backtest-skeleton row as deleted in phase 03.7 (D-37)"
  artifacts:
    - path: "example/backtest.md"
      provides: "the backtester guide with a runnable minimal example"
      contains: "## 最小可运行例子"
    - path: "example/model.md"
      provides: "predict_panel and cv_folds.json sections; annotated skeleton section"
      contains: "predict_panel"
    - path: "example/README.md"
      provides: "index row for backtest.md"
      contains: "backtest.md"
  key_links:
    - from: "example/README.md"
      to: "example/backtest.md"
      via: "index table link"
      pattern: "\\(backtest\\.md\\)"
    - from: "example/backtest.md"
      to: "quantlab/backtest/us_equity.py"
      via: "the runnable example constructs USEquityCrossectionSelectStockVectorBt directly"
      pattern: "USEquityCrossectionSelectStockVectorBt\\("
---

<objective>
Write the user-facing guide to the new backtester layer, `example/backtest.md`, with a minimal example that genuinely runs offline. Update the model guide for the two model-layer additions (predict_panel with head adapters, and cv_folds.json) and mark the deleted in-model backtest hooks as superseded (D-37).

Purpose: this repository's `example/` directory is how a reader learns to use and extend a layer. Its convention is that pasted output is real, and a backtester nobody can drive from the docs is not delivered.
Output: `example/backtest.md` (new), `example/model.md` and `example/README.md` updates.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-CONTEXT.md
@.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-RESEARCH.md
@.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-10-SUMMARY.md
@.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-11-SUMMARY.md
@example/model.md
@example/README.md

The 03.7-10 and 03.7-11 SUMMARYs are referenced for the final public shapes (run_cv outputs, rebuild loader) the guide documents.

Standing constraints for every task in this plan:
- The user has uncommitted edits in `quantlab/enums/data.py`, `quantlab/factor/alpha158.py`, `quantlab/label/fret.py`. Never edit them and never `git add` them; commit only this plan's files, by explicit path.
- Never use `git stash`.
- The docs are Chinese prose in example/model.md's style: a `> 代码位置：` blockquote, `---` separators, "一句话" (one-line summary), "常见坑" (common pitfalls), "已知的不完整之处" (known gaps).
- Superseded text is kept and annotated, never deleted.
- Examples construct every config directly; the guide never imports the config factories package (D-32). It says the macOS `OMP_NUM_THREADS=1` rule applies when torch and xgboost share a process.
- `README.md`, `CLAUDE.md` and `.planning/codebase` belong to plan 03.7-12 in this wave; do not edit them here.
- The full-suite gate is `bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh`.
- Spec-less probe fallback: skipped (no requirement IDs to probe); recorded as a visible choice.
</context>

<tasks>

<task type="auto">
  <name>Task 1: example/backtest.md — the backtester guide with an example that really runs</name>
  <files>example/backtest.md</files>
  <read_first>
    - example/model.md (lines 1-45 header and summary style; the "最小示例（真的跑过）" and "真实数据用法（此例未实际运行）" sections at ~634 and ~795 for the ran/not-ran labelling convention; "macOS / Linux：OpenMP 冲突" at ~957)
    - example/README.md (lines 1-8: the convention that pasted outputs are real)
    - quantlab/base/backtest.py, quantlab/base/config.py (BacktestConfig, CrossSectionBacktestConfig), quantlab/backtest/us_equity.py, quantlab/backtest/selection.py, quantlab/backtest/engine_vectorbt.py
    - quantlab/utils/module.py (load_backtester_from_config)
    - tests/backtest_fixtures.py (a known-good synthetic store writer and Polars factor/label shapes to adapt inline; the guide must not import from tests/)
    - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-CONTEXT.md (all decisions; Deferred Ideas)
    - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-RESEARCH.md (Common Pitfalls for the 常见坑 section)
  </read_first>
  <action>
Create `example/backtest.md` with these sections, in order.

1. A `> 代码位置：` blockquote listing `quantlab/base/backtest.py`, `quantlab/base/config.py` (BacktestConfig, CrossSectionBacktestConfig), `quantlab/backtest/engine_vectorbt.py`, `quantlab/backtest/selection.py`, `quantlab/backtest/us_equity.py`, and `quantlab/utils/module.py` (load_backtester_from_config).

2. "一句话" (one-line summary).

3. "类层次" (class hierarchy): the engine varies by inheritance and market/selection by composition. Explain BaseBacktester -> VectorBtBacktester -> USEquityCrossectionSelectStockVectorBt, and name the reserved sibling names (D-01).

4. "run() 的七步" and "run_cv()": the D-02 template, then run_cv's manifest, per-fold and stitched outputs (D-16, D-35, D-36).

5. "权重契约" (the weights contract, D-03): NaN on non-rebalance bars, 0.0 for unselected symbols, gross <= 100%, and why NaN must never appear on a rebalance bar.

6. "价格与成交" (prices and fills, D-04, D-05): the adjusted columns come from the MARKET spec; a signal at the bar-t close fills at the bar t+1 open; target percent is marked at the order price.

7. "标的池与退市" (universe and delisting, D-06, D-07): what a liquidation record looks like.

8. "选股规则" (selection rules, D-09..D-12, D-18): long_only and long_short books, eligibility, short books, the rebalance schedule, and the last-bar rule.

9. "模型准备与日期对齐" (model preparation and date alignment, D-13..D-15): train vs load, warm-up in bars, the read-cache refresh, and DL checkpoints needing a collected panel.

10. "样本内外" (in-sample vs out-of-sample, D-17, D-22, D-34): the label-horizon extension, why slices come from one simulation, and the turnover definition.

11. "费用与假设" (costs and assumptions, D-19..D-21): the defaults. Include the explicit sentence that no borrow or short-financing cost is modelled, so short-side returns are optimistic.

12. "输出目录" (the run directory, D-23, D-24, D-27, D-28): every file, fingerprint.json, and the wandb switch.

13. "从 config 重建与复现" (rebuild from config and reproduce, D-25, D-26, D-27): load_backtester_from_config, and why factor and model classes must live in an importable module rather than `__main__`.

14. "## 最小可运行例子" (minimal runnable example). Exactly one fenced python block that:
- writes a small synthetic business-day US-equity Zarr store into a `tempfile.mkdtemp()` directory;
- defines a tiny Polars past-return factor and forward-return label inline, adapted from tests/backtest_fixtures.py;
- builds `DatasetConfig`, `PolarsFactorConfig`, `MLConfig` and `CrossSectionBacktestConfig` directly;
- trains a shipped `XGBoostRegressor` with a handful of boosting rounds in model_mode "train", after setting `os.environ["OMP_NUM_THREADS"] = "1"` and `os.environ["WANDB_MODE"] = "disabled"` before importing quantlab;
- runs `USEquityCrossectionSelectStockVectorBt(config).run()`;
- prints the run directory's file names and three metrics ("Total Return [%]", "Sharpe Ratio", turnover mean).

Run that exact block offline, then paste its real stdout below it under "### 真实输出" (real output), replacing the temp path with a placeholder and saying so.

15. "真实数据用法（此例未实际运行）" (real-data usage, not run): a short snippet pointing the price dataset at `data/data/us_equity/1d/us_all.zarr`. Note that the local store holds only about 21 bars, too short for a real warm-up.

16. "benchmark" (D-08): the config slot exists, and a non-None value raises until directly-downloaded index price data exists.

17. "常见坑" (common pitfalls), from RESEARCH: the read cache, NaN-price group freeze, NaN weights on rebalance bars, the Portfolio not being sliceable, nanosecond fold dates, and the last-bar rule.

18. "已知的不完整之处" (known gaps): benchmark comparison, borrow costs, staggered rebalancing, optimizers beyond equal-weight TopN, and the event-driven engine. These are all deferred per CONTEXT; list them as deferred, not as promises.

Cite decision ids inline where each rule is stated.
  </action>
  <verify>
    <automated>ROOT="$(git rev-parse --show-toplevel)" && T="$(mktemp -d)" && awk '/^## 最小可运行例子/{f=1} f&&/^```python/{c=1;next} c&&/^```/{exit} c' "$ROOT/example/backtest.md" > "$T/example.py" && test -s "$T/example.py" && (cd "$T" && OMP_NUM_THREADS=1 WANDB_MODE=disabled uv run --project "$ROOT" python example.py) | tee "$T/out.txt" && grep -q "Total Return" "$T/out.txt" && echo example-ran</automated>
    <fails_when>non-zero exit, an empty extracted example.py, a Python traceback in the output, or "example-ran" not printed</fails_when>
    <automated>uv run python -c "import pathlib,re; t=pathlib.Path('example/backtest.md').read_text(); need=['## 最小可运行例子','### 真实输出','D-21','D-08','D-03','D-25','CrossSectionBacktestConfig(','load_backtester_from_config','OMP_NUM_THREADS']; miss=[n for n in need if n not in t]; assert not miss, miss; code=re.findall(r'\x60\x60\x60python(.*?)\x60\x60\x60', t, re.S); assert not any(re.search(r'^\s*(from|import)\s+quantlab\.config\b', c, re.M) for c in code), 'factory import in example'; print('doc-ok')"</automated>
    <fails_when>non-zero exit, or "doc-ok" not printed (a required section or decision citation is missing, or a code block imports the config factories package)</fails_when>
  </verify>
  <acceptance_criteria>
    - The first verify command prints "example-ran" (the extracted example runs offline and prints Total Return).
    - The second verify command prints "doc-ok".
    - The SUMMARY records the exact command used to run the example, and confirms the pasted output is from that run.
  </acceptance_criteria>
  <done>example/backtest.md documents every locked 03.7 decision a user touches, and its minimal example runs offline with its real output pasted.</done>
</task>

<task type="auto">
  <name>Task 2: Model guide and index — predict_panel, cv_folds.json, and the deleted skeleton marked superseded</name>
  <files>example/model.md, example/README.md</files>
  <read_first>
    - example/model.md (header blockquote lines 1-10; "吐：预测" at ~66; "ML 交叉验证" at ~923; "已知的不完整之处" item "2/3/4" at ~1107-1152)
    - example/README.md (the 第三步 table and the "回测骨架" row at ~54)
    - quantlab/base/model.py (predict_panel, _predict_panel_array, CV_FOLDS_FILENAME, CV_FOLDS_FORMAT_VERSION)
    - quantlab/dl_model/mlp.py, quantlab/dl_model/rnn.py, quantlab/dl_model/rnn_classification.py (the three adapters)
    - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-05-SUMMARY.md (the MLP premise finding)
    - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-07-SUMMARY.md (the manifest and golden accommodation)
  </read_first>
  <action>
In `example/model.md`:

(a) Add `predict_panel` and `cv_folds.json` to the header blockquote's description of `quantlab/base/model.py`.

(b) Add a section "## 面板预测：predict_panel" after "### 吐：预测" (D-29, D-33). It states:
- predict_panel takes and returns a (timestamp, symbol) xr.Dataset, one variable per label in declared order.
- Rows whose features are all NaN predict NaN.
- The single `_predict_panel_array` hook is how a head adapts. MLPRegressor flattens to its `[T, S*F]` inference input and reshapes back; this corrects D-33's premise that MLP needed no adapter. RNNRegressor uses the direct-prediction channels. RNNClassifier yields per-label P(up), a probability and not a return.
- A tuple-returning head without an adapter raises.

(c) Add a subsection "### cv_folds.json" under "### ML 交叉验证" (D-30, D-36). It gives the location (the train_cv project directory), the shape `{"format_version": 1, "folds": [...]}`, that folds equals the returned list, that non-finite metrics become null, and that run_cv rejects unknown versions. Link example/backtest.md.

(d) In "已知的不完整之处", item "2/3/4. 回测骨架", insert a dated first paragraph, keeping all original text below it. It says that in phase 03.7 (D-37) the model-layer backtest hooks were deleted together with their tests, that backtesting lives in `quantlab/backtest/`, and that readers should see backtest.md. Add the same short note wherever the old test names are listed as current locks in that item.

In `example/README.md`:

(e) Add a row for [backtest.md](backtest.md) to the "第三步" table, or as a new "第四步：回测" table directly after it. The row says what it covers and when to read it.

(f) Annotate the "回测骨架" row in the 写文档时发现的缺陷 table the way the other resolved rows are annotated: strike the old description and append that it was deleted in phase 03.7 (D-37) and that backtesting lives in quantlab/backtest/. Link backtest.md.
  </action>
  <verify>
    <automated>uv run python -c "import pathlib; m=pathlib.Path('example/model.md').read_text(); r=pathlib.Path('example/README.md').read_text(); need_m=['## 面板预测：predict_panel','### cv_folds.json','format_version','_predict_panel_array','D-37','D-33','backtest.md']; need_r=['(backtest.md)','D-37']; miss=[n for n in need_m if n not in m]+[n for n in need_r if n not in r]; assert not miss, miss; print('model-docs-ok')"</automated>
    <fails_when>non-zero exit, or "model-docs-ok" not printed</fails_when>
    <automated>bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh</automated>
    <fails_when>non-zero exit, or output containing "NEW FAILURE(S)", "NEW ERROR(S)" or "NO 'passed' SUMMARY LINE"</fails_when>
  </verify>
  <acceptance_criteria>
    - The first verify command prints "model-docs-ok".
    - Before committing, `git diff HEAD -- example/model.md > "$TMPDIR/model_md.diff"` exits 0. Then `grep -c '^-[^-]' "$TMPDIR/model_md.diff"` prints a number no larger than 5, meaning the original skeleton-section prose was annotated rather than removed.
    - `bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh` exits 0.
  </acceptance_criteria>
  <done>The model guide documents predict_panel and cv_folds.json, the deleted skeleton is marked superseded instead of erased, and the index links the new backtest guide.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → users copying examples | examples are pasted and run by readers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.7-40 | Information Disclosure | example snippets | low | mitigate | Examples use synthetic temp stores and `WANDB_MODE=disabled`; no credential or API key appears; real-data snippet is path-only and labelled not run |
| T-03.7-41 | Repudiation (misleading docs) | fabricated example output | low | mitigate | The example block is extracted and executed by the verify command; pasted output must come from that run |
</threat_model>

<verification>
- The example-extraction command prints "example-ran"; the two doc checks print "doc-ok" and "model-docs-ok".
- `bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh` exits 0.
- Spec-less probe fallback: skipped (no requirement IDs to probe). Recorded here as a visible choice.
</verification>

<success_criteria>
- A reader can drive run() from example/backtest.md alone, offline, with real pasted output.
- D-21 and D-08 limitations and D-32 direct construction are stated where users look.
- D-29/D-30/D-33/D-36 are documented in the model guide; D-37 is annotated as superseded.
- No failures beyond the pre-phase baseline.
</success_criteria>

<output>
Create `.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-13-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Files and sections created by this plan:

- `example/backtest.md` (new) with sections 类层次, run() 的七步, run_cv(), 权重契约, 价格与成交, 标的池与退市, 选股规则, 模型准备与日期对齐, 样本内外, 费用与假设, 输出目录, 从 config 重建与复现, 最小可运行例子 (with 真实输出), 真实数据用法（此例未实际运行）, benchmark, 常见坑, 已知的不完整之处
- `example/model.md`: sections `## 面板预测：predict_panel` and `### cv_folds.json`; a dated D-37 annotation in 已知的不完整之处
- `example/README.md`: index row linking `backtest.md`; D-37 annotation on the 回测骨架 row
