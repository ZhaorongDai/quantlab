---
status: diagnosed
phase: 03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse
source: [03.7-VERIFICATION.md, 03.7-REVIEW-FIX.md]
started: 2026-09-15T04:20:00Z
updated: 2026-09-15T12:10:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Open a plain run() report.html in a browser
expected: Equity and drawdown panels on a shared time axis. Drawdown matches equity.zarr. In-sample bars are shaded, and after CR-01 intraday tails are shaded too. The short-side note is present. Run the offline example in example/backtest.md, then open `<run_dir>/report.html`.
result: pass

### 2. Open a stitched run_cv() report.html in a browser
expected: One continuous equity/drawdown curve across all folds, with no capital reset at fold boundaries. There is NO in-sample shading; this known limitation is disclosed in metrics.json `notes` and in example/backtest.md. Per-fold outputs are under `folds/fold_{i}/`.
result: pass

### 3. Read the rewritten architecture prose
expected: CLAUDE.md, .planning/codebase/STACK.md, STRUCTURE.md, ARCHITECTURE.md, example/backtest.md and example/model.md describe the backtester layer as it now exists (BaseBacktester → VectorBtBacktester → USEquityCrossectionSelectStockVectorBt, plus load_backtester_from_config), including the dated correction paragraphs the review fixes added. None of them presents vecbt/bt.py or the deleted in-model backtest hooks as current. Older stale Nautilus/ML-training statements are tracked in deferred-items.md and are out of scope.
result: pass

### 4. Decide whether CLEAN-02 stays marked Complete
expected: |
  You confirm or revert the CLEAN-02 checkbox in .planning/REQUIREMENTS.md.
  For Complete: both examples the requirement names are resolved (vecbt/bt.py is retired, and
  binance parsing lives only in quantlab/utils/binance.py).
  For partial: ROADMAP scopes 03.7 to "CLEAN-02 (partial)", and root-level ad hoc scripts remain
  (test.py, cal.py, jerry_query_data.py, test_nt.ipynb, train_model.py). Phase 7 / QUAL-02 owns
  the wider cleanup.
  BT-01 and CFG-01 are already reverted to Partial; the verifier confirmed that matches the evidence.
result: pass
note: "User kept CLEAN-02 marked Complete."

### 5. Accept CR-02's annualization convention for bars longer than one day
expected: You accept that bars longer than a day are annualized as min(252, 365.25 / days) bars per year (weekly about 52.2, 31-day about 11.8), with daily and intraday bars unchanged. The rejected alternatives were a 5/7 trading-day scaling (weekly 50.4) and raising for multi-day bars. Cost: a series sampled every N trading days would be annualized about 40% high, and nothing in the project produces that today.
result: skipped
reason: "Deferred follow-up: user chose option a — derive annualization from DatasetConfig.frequency instead of the timestamp-diff mode — to be done when the Frequency enum gains multi-day (calendar week/month) frequencies. Current enum is 1d/1m/tick, so no exposure today."

### 6. Accept CR-01's exact-bar in-sample boundary and its metrics.json endpoint format
expected: You accept that in-sample/out-of-sample membership is decided by exact bar timestamp, using the same pandas slice the model layer uses. metrics.json range endpoints are written as plain dates for midnight bars and as full ISO timestamps otherwise, so daily output is unchanged.
result: pass

### 7. Accept WR-01's checkpoint-vs-config policy, and decide on the spurious run_cv warning
expected: |
  You accept these three rules:
  - a checkpoint trained on different factors or labels (name or order) raises;
  - a checkpoint whose recorded train dates differ from config.model warns, and the checkpoint's dates are used;
  - a checkpoint with no config.json beside it warns and continues.
  Known defect found in re-verification (no numbers affected): every normal run_cv fold logs a
  spurious "config.model says … using the checkpoint's dates" warning, 24 of them across 3 tests.
  run_cv compares against config.model even though it uses manifest dates, and the dates are
  compared as strings, so '2024-01-01' and a nanosecond string count as different.
  The proposed fix: skip the config.model comparison inside run_cv, and compare dates as timestamps.
result: issue
reported: "修复"
severity: minor
note: "User accepted the three WR-01 rules and asked for the spurious run_cv warning to be fixed (the proposed fix)."

### 8. Accept WR-02's DL symbol-universe policy
expected: You accept that DL heads raise when a training symbol is missing from the prediction panel, and drop unseen symbols with a warning, so those symbols are never traded. This makes a DL model's tradable universe narrower than D-06's "every symbol in the price dataset". ML heads are unaffected because they predict each symbol independently.
result: pass
note: "User accepted the WR-02 rules, then asked to check whether the data ordering ML and DL heads depend on is pinned. Two unpinned spots were found and recorded as gaps G-03.7-8 and G-03.7-9."

### 9. Accept WR-05/06/07 strictness
expected: |
  You accept three strictness changes:
  - a backtester config missing any field refuses to rebuild, so adding a config field later means migrating stored runs (model/factor/dataset loaders still fill defaults);
  - to_jsonable raises on unknown types instead of stringifying them;
  - rebuilding a run saved before the fixes warns once about the new fingerprint entries.
result: pass

## Summary

total: 9
passed: 7
issues: 1
pending: 0
skipped: 1
blocked: 0

## Gaps

- gap_id: G-03.7-7
  truth: "A normal run_cv (checkpoints and cv_folds.json from the same train_cv) logs no checkpoint-vs-config date-mismatch warning; date mismatch checks compare timestamps, not strings"
  status: failed
  reason: "User reported: 修复 — every run_cv fold logs a spurious 'config.model says ... using the checkpoint's dates' warning (8 of 8 folds reproduced in a scratchpad 8-fold run; 24 across 3 tests in re-verification). No numbers affected."
  severity: minor
  test: 7
  root_cause: "Two compounding causes. (1) run_cv (quantlab/base/backtest.py:432) calls _checkpoint_train_bounds, a run()-load-mode helper that compares the checkpoint's recorded train dates with config.model and warns; run_cv discards that and uses manifest dates, and each fold trains on its own window, so folds 1..N warn even with a correct comparison. (2) Both date checks (:799 vs config.model, :434-435 vs manifest) compare tuple(map(str, ...)); train_cv writes np.datetime_as_string nanosecond strings (model.py:514-525, 540-541) while config.model carries plain ISO dates, so equal instants mismatch (fold 0, and run() load mode false positives). Numbers unaffected. Caveat: plain Timestamp equality treats '2024-02-09' and '2024-02-09T00:00:00.000000000' as equal although as slice endpoints they select different intraday bars."
  artifacts:
    - path: "quantlab/base/backtest.py:432"
      issue: "run_cv calls _checkpoint_train_bounds and inherits its config.model comparison/warning though it uses manifest dates"
    - path: "quantlab/base/backtest.py:799"
      issue: "train dates vs config.model compared as strings; false positive in run() load mode too"
    - path: "quantlab/base/backtest.py:434-435"
      issue: "checkpoint vs manifest dates compared as strings (latent)"
    - path: "tests/test_backtest_run_cv.py"
      issue: "no assertion that a normal run_cv logs zero WR-01 warnings"
    - path: "tests/test_backtest_dates.py:614-647"
      issue: "WR-01 tests never mix date string formats"
  missing:
    - "Split _checkpoint_train_bounds into a pure recorded-bounds reader and a config.model comparison used only by _prepare_model (run() load mode); run_cv uses the reader plus its manifest check"
    - "One shared non-string date comparison at :799 and :434 (decide: timestamp equality vs resolving each endpoint to the bars it selects on the price calendar, as _training_window does)"
    - "Endpoint comparison must use slice semantics, not bare Timestamp equality (verified in UAT follow-up): on intraday bars end='2024-02-09' selects through 2024-02-09 16:00 while '2024-02-09T00:00' and the ns string select through 2024-02-08 16:00, yet pd.Timestamp equality calls them equal. Recommended: resolve both endpoints to the bars they select on the price calendar via slice_indexer, as _training_window does. Note dataset start/end accept only YYYY-MM-DD and the backtester's _iso_date truncates its own start/end, so only model train_*/test_* dates carry a time part."
    - "Regression tests: cv_project run_cv logs 0 WR-01 warnings; mixed-format equal dates (plain vs ns string, plain vs T00:00:00) log 0; genuinely stale config.model dates still log exactly 1; genuine checkpoint-vs-manifest mismatch still warns; the three WR-01 rules unchanged"
  debug_session: ".planning/debug/run-cv-spurious-checkpoint-date-warning.md"

- gap_id: G-03.7-8
  truth: "DL predict_panel puts every prediction on the symbol coordinate whose features produced it, regardless of the order of the recorded training symbol list"
  status: failed
  reason: "User reported: 记录 — found while checking data-ordering guarantees after test 8. predict_panel takes output coords from feats after _align_prediction_symbols (.sel in trained-list order, quantlab/base/model.py:385/405), but to_array re-sorts by symbol (model.py:272), so an unsorted trained list mislabels every prediction. The trained list is self.symbols in raw backend order (model.py:137) and train() does not force collect() (model.py:447). Reproduced: trained list ['S2','S0','S1'] -> all 3 coords carry another symbol's prediction (scratchpad order_probe.py). Latent today because collect() sorts; existing tests use an already-sorted SYMBOLS."
  severity: major
  test: 8
  root_cause: "AND-gate of two causes. (1) DLModel._align_prediction_symbols (quantlab/base/model.py:1045) returns feats.sel(symbol=trained) in trained_on.symbols order and predict_panel takes output coords from that panel (:405), but to_array (:269-276) re-sorts the symbol axis before building the array — values follow sorted order (which is also what DLModel._fit has always trained on, to_tensor sorted since 6b3c603), coords follow the record order. (2) _save_model (:299) records self.symbols in raw data_backend order, not the sorted layout training used; only collect() sorts the backend, and train()/train_cv() (incl. parallel folds) accept a backend filled by to_internal without collect(); a hand-edited trained_on.symbols is an equivalent trigger (load() reads it verbatim, :340-350). Reproduced: unsorted record mislabels exactly the positions where record order != sorted order; train()/train_cv() on an unsorted backend write an unsorted record while the network sees sorted input; an MLP checkpoint trained by HEAD with an unsorted record gets every coord wrong. Latent on every collect() path and in all 134 related tests (sorted SYMBOLS, position-invariant LinearDLHead). numpy vs Python string sort eliminated; head adapters add no reorder. Sorted order is the correct contract: re-sorting after alignment fixes existing and new checkpoints; keeping trained order end-to-end corrupts values of existing position-sensitive checkpoints and changes DL training."
  artifacts:
    - path: "quantlab/base/model.py:1007-1045"
      issue: "DLModel._align_prediction_symbols selects in record order; docstring (:1019) wrongly claims the network sees that layout"
    - path: "quantlab/base/model.py:385-407"
      issue: "predict_panel coords come from the aligned panel that to_array then re-sorts"
    - path: "quantlab/base/model.py:299"
      issue: "_save_model records backend-order symbols instead of the sorted training layout"
    - path: "example/model.md:159"
      issue: "promises output coords 'in training order', contradicting :147 (sorted panel)"
    - path: "tests/test_model_predict_panel.py"
      issue: "WR-02 tests use sorted SYMBOLS and position-invariant LinearDLHead, so cannot see the bug"
  missing:
    - "Keep to_array's symbol sort and DL training unchanged; after the membership check select trained symbols in sorted order (sel(symbol=sorted(trained)) or .sortby('symbol')) — fixes existing checkpoints"
    - "Optionally re-sort in BaseModel.predict_panel right before to_array and take coords from the exact panel to_array consumes, so no hook can reintroduce the mismatch"
    - "_save_model records sorted(symbols) so trained_on describes training truthfully"
    - "Fix _align_prediction_symbols docstring, example/model.md:159 and the 'training order' wording in 03.7-REVIEW-FIX.md WR-02"
    - "Regression tests with a position-sensitive head (MLP or RNN) and an unsorted record (trained without collect() or hand-edited): assert each coord's value equals the module run on the sorted layout; boundaries: one swap, fully reversed, single symbol; train() and train_cv() on an unsorted backend record sorted symbols"
  debug_session: ".planning/debug/dl-predict-panel-symbol-coord-misalignment.md"
- gap_id: G-03.7-9
  truth: "BaseModel.load() refuses a checkpoint whose recorded factor/label variable names or order differ from the model config, so predict_panel outside the backtester cannot silently feed permuted inputs"
  status: failed
  reason: "User reported: 记录 — found while checking data-ordering guarantees after test 8. _save_model writes trained_on.factor_names/label_names (model.py:300) but nothing ever reads them; only the backtester's _assert_checkpoint_variables checks, and it reads the config field factors[].factor_names rather than trained_on (quantlab/base/backtest.py:863-904). A direct model.load() + predict_panel with a reordered factor config predicts on permuted features without error (xgboost DMatrix has no feature_names, only the count is checked)."
  severity: major
  test: 8
  root_cause: "Confirmed by repro for both heads and for labels as well as factors. (1) BaseModel.load() (quantlab/base/model.py:312-338) reads only trained_on.symbols and never compares trained_on.factor_names/label_names with get_factor_names()/get_label_names(). (2) Neither head validates variable identity: XGBoostRegressor trains on nameless DMatrix (xgb.py:227,234) and predicts via inplace_predict(numpy) (xgb.py:283), count-only; DL heads load a positional state_dict, shape-only. Reordered factors or labels load and predict silently (XGB ret_30 corr +0.046; labels corr -0.017; LinearDLHead corr +0.021). (3) The only name check, BaseBacktester._assert_checkpoint_variables (backtest.py:874-904), lives outside the model layer and compares the factor CONFIG field factors[].factor_names (Factor.get_factor_names = config.factor_names, user-suppliable, not rewritten on the FactorPolars read path) instead of trained_on (written by the same BaseModel.get_factor_names()/_get_factor_names() calls that built the training arrays) — measured false positive (refuses a model's own checkpoint) and false negative (accepts permuted inputs after derived names drift)."
  artifacts:
    - path: "quantlab/base/model.py:312-350"
      issue: "load()/_read_trained_symbols ignore trained_on.factor_names and label_names; _save_model writes them but nothing reads them"
    - path: "quantlab/base/backtest.py:862-904"
      issue: "_saved_variable_names/_assert_checkpoint_variables compare against factors[].factor_names config field, not trained_on; only runs in the backtester"
    - path: "quantlab/base/factor.py:186,196"
      issue: "two name sources: get_factor_names returns config.factor_names, training uses _get_factor_names()"
    - path: "quantlab/ml_model/xgb.py:227,234,283"
      issue: "DMatrix without feature_names; inplace_predict on numpy validates only column count"
    - path: "train_model.py:58"
      issue: "direct load() outside the backtester on a pre-trained_on checkpoint (legacy path)"
  missing:
    - "One model-level check, e.g. BaseModel._assert_trained_variables(path): reads the sidecar itself (no data), compares trained_on.factor_names/label_names with get_factor_names()/get_label_names(); called in load() after the suffix check and before _read_checkpoint (so DL count mismatches get a named ValueError, not torch RuntimeError)"
    - "Policy: trained_on present -> ValueError naming both lists and the path; no trained_on but legacy factors[].factor_names -> warn it is a weaker record, compare against it, still raise on mismatch; no record -> one warning and continue"
    - "Backtester delegates: _load_model_checkpoint calls the model-level check before DL feature collection; remove the backtester's own comparison (single source of truth); keep 'was trained on' + path message (tests/test_backtest_dates.py:574) and exactly one 'has no config.json' warning (test_backtest_dates.py:650-673)"
    - "User addition (XGBoost feature_names): pass feature_names=get_factor_names() to DMatrix at train/val; in _forward either compare self.model.feature_names with get_factor_names() before inplace_predict or predict through a named DMatrix (inplace_predict on numpy alone does not validate); names persist through MlBackend joblib; old nameless checkpoints stay unvalidated by xgboost"
    - "User addition (W&B feature importance): in XGBoostRegressor._fit_model after xgb.train, write Booster.get_score(importance_type) keyed by factor name, zero-filled for never-split factors, to recorder.summary (not recorder.log — would break tests/test_xgb_model.py:293-296; not MLModel._evaluate — would break tests/test_ml_models.py:281); xgboost rejects names with '[', ']', '<' or duplicates — all 82 Alpha101, 169 Alpha158Stock and repo Polars/label names comply"
    - "Regression tests: reordered factors and reordered labels raise at load() for an ML head and a DL head; legacy-record and no-record paths; XGBoost name validation and importance summary keys"
  debug_session: ".planning/debug/model-load-skips-variable-order-check.md"

## Deferred Follow-Ups

- test: 5
  idea: "a — derive bars-per-year from the declared DatasetConfig.frequency (calendar-defined values such as 1w / 1M) instead of inferring bar_interval from the timestamp-diff mode (quantlab/backtest/engine_vectorbt.py:95, MarketSpec.year_freq in quantlab/base/backtest.py). Do it together with extending the Frequency Literal in quantlab/enums/data.py."
  deferred_at: 2026-09-15
