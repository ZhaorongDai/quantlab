---
status: testing
phase: 03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse
source: [03.7-VERIFICATION.md]
started: 2026-09-15T04:20:00Z
updated: 2026-09-15T04:20:00Z
---

## Current Test

number: 1
name: Open a plain run() report.html in a browser
expected: |
  Two stacked panels share one time axis: equity on top, drawdown below. The drawdown values
  match equity.zarr (running-max drawdown, <= 0). The in-sample period is shaded grey when the
  backtest window overlaps the model's effective training window. The short-side note says
  borrow costs are not modelled, so short returns are optimistic.
  Known limitations (already disclosed): a one-bar in-sample overlap draws a zero-width band,
  and on intraday data the bars after noon on the last in-sample day fall outside the band.
awaiting: user response

## Tests

### 1. Open a plain run() report.html in a browser
expected: Equity and drawdown panels on a shared time axis. Drawdown matches equity.zarr. In-sample bars are shaded. The short-side note is present. (Run the offline example in example/backtest.md, then open `<run_dir>/report.html`.)
result: [pending]

### 2. Open a stitched run_cv() report.html in a browser
expected: One continuous equity/drawdown curve across all folds, with no jump or capital reset at fold boundaries. There is NO in-sample shading, which is a known limitation disclosed in metrics.json `notes` and in example/backtest.md. Per-fold outputs live under `folds/fold_{i}/`.
result: [pending]

### 3. Read the rewritten architecture prose
expected: CLAUDE.md, .planning/codebase/STACK.md, STRUCTURE.md, ARCHITECTURE.md and example/backtest.md describe the backtester layer as it actually exists: BaseBacktester → VectorBtBacktester → USEquityCrossectionSelectStockVectorBt, plus load_backtester_from_config. None of them still presents vecbt/bt.py or the deleted in-model backtest hooks as current. Stale Nautilus-strategy and ML-training statements that predate this phase are tracked in deferred-items.md and are out of scope here.
result: [pending]

### 4. Decide whether CLEAN-02 stays marked Complete
expected: |
  The user confirms or reverts the CLEAN-02 checkbox in .planning/REQUIREMENTS.md.
  For Complete: both named examples are resolved (vecbt/bt.py retired; binance parsing lives only
  in quantlab/utils/binance.py).
  For partial: ROADMAP scopes 03.7 to "CLEAN-02 (partial)", and root-level ad hoc scripts remain
  (test.py, cal.py, jerry_query_data.py, test_nt.ipynb, train_model.py) against the requirement's
  general "clean up temporary scripts" clause (Phase 7 / QUAL-02 already owns the wider cleanup).
  Note: BT-01 and CFG-01 were reverted to Pending by the orchestrator. The verifier found them
  over-claimed, because Phase 6 still owns the target-holdings entry point and the single-config
  end-to-end pipeline.
result: [pending]

## Summary

total: 4
passed: 0
issues: 0
pending: 4
skipped: 0
blocked: 0

## Gaps
