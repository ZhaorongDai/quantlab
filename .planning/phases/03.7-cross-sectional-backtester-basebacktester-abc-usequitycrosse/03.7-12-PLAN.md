---
phase: 03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse
plan: 12
type: execute
wave: 7
depends_on: ["03.7-10", "03.7-11"]
files_modified:
  - quantlab/vecbt/__init__.py
  - quantlab/vecbt/bt.py
  - train_model.py
  - README.md
  - CLAUDE.md
  - .planning/codebase/STACK.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/ARCHITECTURE.md
  - tests/test_backtest_contracts.py
autonomous: true
requirements: [CLEAN-02]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The quantlab/vecbt package no longer exists on disk (both tracked files and its __pycache__), importlib cannot find it, and a test keeps it retired (D-31, CLEAN-02)"
    - "No live code and no current-state document points at the retired helper: train_model.py's commented import line is gone, and README.md, CLAUDE.md and .planning/codebase describe quantlab/backtest/ and quantlab/base/backtest.py instead; any remaining mention is explicitly marked retired (D-31)"
    - "CLAUDE.md and .planning/codebase no longer describe the deleted model-layer backtest guard as current (D-37)"
    - "The vectorbt import-scope lock no longer carries a temporary allowance: quantlab/backtest/engine_vectorbt.py is the only quantlab module importing vectorbt (D-31, layering)"
  artifacts:
    - path: "tests/test_backtest_contracts.py"
      provides: "vecbt retirement lock; vectorbt import scope without allowance"
      contains: "def test_vecbt_package_is_retired"
    - path: "README.md"
      provides: "module list describing quantlab/backtest/"
      contains: "quantlab/backtest/"
    - path: "CLAUDE.md"
      provides: "stack and architecture sections describing the backtester layer"
      contains: "quantlab/backtest/"
  key_links:
    - from: "tests/test_backtest_contracts.py"
      to: "quantlab/"
      via: "find_spec lookup and directory-existence check for the retired package"
      pattern: "find_spec"
---

<!-- planner-discipline-allow: vecbt -->

<objective>
Retire `quantlab/vecbt/bt.py` (REQUIREMENTS CLEAN-02, D-31) now that `quantlab/backtest/` delivers a working vectorbt backtest. Delete the package, remove or redirect every live reference, and update the project's current-state maps (README.md, CLAUDE.md, .planning/codebase) so they describe the backtester layer this phase built. Also drop the stale claims about the model-layer backtest guard deleted by D-37.

Purpose: a broken helper that still appears in the architecture map is the first thing a new reader tries to use.
Output: the package deleted, the retirement lock, and the corrected maps.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-CONTEXT.md
@.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-RESEARCH.md
@.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-10-SUMMARY.md
@CLAUDE.md
@README.md

The 03.7-10 SUMMARY is referenced for the final class and file names the maps must describe.

Standing constraints for every task in this plan:
- The user has uncommitted edits in `quantlab/enums/data.py`, `quantlab/factor/alpha158.py`, `quantlab/label/fret.py`. Never edit them and never `git add` them; commit only this plan's files, by explicit path.
- Never use `git stash`.
- History documents are NOT rewritten: `.planning/quick/*`, `.planning/phases/0*` (other phases), `.planning/REQUIREMENTS.md`, `.planning/PROJECT.md` and `.planning/ROADMAP.md` keep their historical mentions.
- `example/model.md` and `example/README.md` belong to plan 03.7-13 in this same wave; do not edit them here.
- CLAUDE.md carries GSD-managed section markers; edit only the text inside the stack and architecture sections that mentions the retired helper or the deleted guard, keeping every marker line intact.
- The full-suite gate is `bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh`.
- Spec-less probe fallback: skipped (no requirement IDs to probe); recorded as a visible choice.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete the vecbt package, drop its last code reference, lock it retired</name>
  <files>quantlab/vecbt/__init__.py, quantlab/vecbt/bt.py, train_model.py, README.md, tests/test_backtest_contracts.py</files>
  <read_first>
    - quantlab/vecbt/bt.py (the helper being retired)
    - train_model.py (line 8: the commented import of the retired helper)
    - README.md (lines 215-250: the module list, including the stale Nautilus `backtest/` bullet and the vecbt bullet)
    - tests/test_backtest_contracts.py (test_vectorbt_is_imported_only_by_the_engine and its temporary allowance comment)
    - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-RESEARCH.md (Runtime State Inventory: editable install maps only the top package, so deleting the subpackage takes effect immediately; egg-info is gitignored)
  </read_first>
  <behavior>
    - `test_vecbt_package_is_retired`: `importlib.util.find_spec("quantlab.vecbt") is None`, and the directory `quantlab/vecbt` does not exist under the repo root.
    - `test_vectorbt_is_imported_only_by_the_engine` now allows exactly one quantlab module: quantlab/backtest/engine_vectorbt.py.
  </behavior>
  <action>
Write the retirement test and tighten the vectorbt import-scope test in `tests/test_backtest_contracts.py` first. Remove the allowance for the retired package and its comment. Run them and see the retirement test fail.

Then:
- `git rm quantlab/vecbt/__init__.py quantlab/vecbt/bt.py`, and remove the leftover untracked `quantlab/vecbt/__pycache__` directory from disk so the directory is gone.
- Delete the single commented import line referencing the retired helper in `train_model.py` (line 8), and change nothing else in that root script.
- In `README.md`, replace the stale `backtest/` Nautilus Strategy bullet (its file was deleted 2026-09-07) and the retired-helper bullet with one bullet. It describes `quantlab/backtest/`: `engine_vectorbt.py` VectorBtBacktester (vectorbt from_orders, target percent, t+1 open fills), `selection.py` CrossSectionTopNSelector, and `us_equity.py` USEquityCrossectionSelectStockVectorBt, on top of `quantlab/base/backtest.py` BaseBacktester with run and run_cv. Point to example/backtest.md.

Mutation-verify: temporarily recreate an empty `quantlab/vecbt/__init__.py` in the working tree (not committed); the retirement test goes red. Delete it again.
  </action>
  <verify>
    <automated>uv run pytest tests/test_backtest_contracts.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line, or fewer than 8 passed</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_backtest_contracts.py -q` exits 0 with "8 passed".
    - `test ! -e quantlab/vecbt` exits 0.
    - `git grep -n "vecbt" -- quantlab train_model.py README.md` prints nothing.
    - `uv run pytest tests/test_entry_point_contracts.py -q` shows no failure beyond its 1 baseline failure (the root-script scan still parses train_model.py).
  </acceptance_criteria>
  <done>The retired helper is gone from code and README, and a test keeps it gone.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the current-state maps in CLAUDE.md and .planning/codebase</name>
  <files>CLAUDE.md, .planning/codebase/STACK.md, .planning/codebase/STRUCTURE.md, .planning/codebase/ARCHITECTURE.md</files>
  <read_first>
    - CLAUDE.md (stack section lines ~22-74 and architecture section lines ~82-182: every line mentioning the retired helper, the Backtest layer, the layering chain, the error-handling example about the model fit guard, and the "Broken/incomplete backtest helper" anti-pattern heading)
    - .planning/codebase/STACK.md (lines 27-60)
    - .planning/codebase/STRUCTURE.md (lines 35-100 and 150-165)
    - .planning/codebase/ARCHITECTURE.md (lines 15-25, 65-80, 100-120, 135-145, 185-215)
    - quantlab/base/backtest.py and quantlab/backtest/*.py (the real class names, to describe them accurately)
  </read_first>
  <action>
Update each current-state statement in place so it describes the code as it now is. Where the retired helper or the deleted guard is named, keep one short parenthetical marking it retired in phase 03.7 (D-31, or D-37 for the guard). Concretely:

1. The vectorbt stack bullets in CLAUDE.md and STACK.md say vectorbt is used by `quantlab/backtest/engine_vectorbt.py` (`Portfolio.from_orders`, target-percent weights) and by the ad hoc `test.py`. The retired helper is noted as retired.

2. The component table row for the retired helper (CLAUDE.md; ARCHITECTURE.md) becomes rows for `BaseBacktester` (`quantlab/base/backtest.py`), `VectorBtBacktester` (`quantlab/backtest/engine_vectorbt.py`), `CrossSectionTopNSelector` (`quantlab/backtest/selection.py`) and `USEquityCrossectionSelectStockVectorBt` (`quantlab/backtest/us_equity.py`).

3. The Backtest layer's Purpose/Location bullets describe run() (model backtest) and run_cv() (model-CV backtest), D-03 target weights, t+1 open fills, and the run directory. They state that the event-driven path is still reserved for Phase 6.

4. The layering chain reads base -> dataset/factor/label -> dl_model/ml_model -> backtest (`quantlab/backtest/`).

5. The error-handling example that cites the model-layer fit guard (deleted by D-37) is replaced by a still-true example: `BaseBacktester`'s config setter raises NotImplementedError for a benchmark dataset (D-08).

6. The "Broken/incomplete backtest helper committed as-is" anti-pattern section gets a first line marking it resolved: the helper was retired in phase 03.7 (D-31). The original description stays below it (superseded-wording convention).

7. STRUCTURE.md's directory tree and "where to add a new backtest" guidance point at `quantlab/backtest/`: a new engine subclasses `BaseBacktester`, and a new selection style subclasses `VectorBtBacktester` (composition of MarketSpec and selector).

Keep every `<!-- GSD:... -->` marker line in CLAUDE.md byte-identical.
  </action>
  <verify>
    <automated>T="${TMPDIR:-/tmp}" && { test ! -e quantlab/vecbt; } && { git grep -n 'vecbt' -- CLAUDE.md .planning/codebase > "$T/vecbt_refs.txt"; rc=$?; test "$rc" -le 1; } && ! grep -vqi 'retired' "$T/vecbt_refs.txt" && git show HEAD:CLAUDE.md > "$T/claude_head.md" && test "$(grep -c '<!-- GSD:' CLAUDE.md)" = "$(grep -c '<!-- GSD:' "$T/claude_head.md")" && grep -q 'quantlab/backtest/' CLAUDE.md && grep -q 'quantlab/backtest/' .planning/codebase/ARCHITECTURE.md && echo maps-ok</automated>
    <fails_when>non-zero exit, or "maps-ok" is not printed (git grep exiting above 1 counts as failure; exit 1 only means no match)</fails_when>
    <automated>bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh</automated>
    <fails_when>non-zero exit, or output containing "NEW FAILURE(S)", "NEW ERROR(S)" or "NO 'passed' SUMMARY LINE"</fails_when>
  </verify>
  <acceptance_criteria>
    - The first verify command prints "maps-ok".
    - `git grep -n "_fit(backtest" -- CLAUDE.md .planning/codebase > "${TMPDIR:-/tmp}/fit_refs.txt"` exits 0 or 1 (1 means no match), and then `grep -vi -e retired -e deleted "${TMPDIR:-/tmp}/fit_refs.txt"` prints nothing.
    - `grep -c "USEquityCrossectionSelectStockVectorBt" CLAUDE.md` prints at least 1.
    - The number of `<!-- GSD:` marker lines in CLAUDE.md is unchanged from HEAD.
    - `bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh` exits 0.
  </acceptance_criteria>
  <done>CLAUDE.md and the codebase maps describe the backtester layer that exists, mark the retired helper and deleted guard as retired, and keep the GSD markers intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → future agents and readers | CLAUDE.md is loaded as instructions; a stale pointer sends work to a broken helper |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.7-38 | Tampering (misdirection) | stale CLAUDE.md architecture text | low | mitigate | Current-state lines rewritten to the real backtester layer; retired items explicitly marked; marker lines preserved |
| T-03.7-39 | Denial of Service | a stale import of the deleted package | low | mitigate | `git grep` shows no live reference; `test_vecbt_package_is_retired` locks the deletion |
</threat_model>

<verification>
- `uv run pytest tests/test_backtest_contracts.py -q` exits 0.
- `test ! -e quantlab/vecbt` exits 0.
- `bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh` exits 0.
- Spec-less probe fallback: skipped (no requirement IDs to probe). Recorded here as a visible choice.
</verification>

<success_criteria>
- CLEAN-02's named item (the vectorbt signal helper) is retired and locked out (D-31).
- README.md, CLAUDE.md and .planning/codebase describe `quantlab/backtest/`; D-37's deleted guard is no longer described as current.
- No failures beyond the pre-phase baseline.
</success_criteria>

<output>
Create `.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-12-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Symbols created by this plan:

- `tests/test_backtest_contracts.py`: `test_vecbt_package_is_retired`
- Deleted: `quantlab/vecbt/__init__.py`, `quantlab/vecbt/bt.py` (with `backtest_from_signals`)
