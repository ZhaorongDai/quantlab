---
phase: 03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse
plan: 09
type: execute
wave: 4
depends_on: ["03.7-08"]
files_modified:
  - quantlab/base/backtest.py
  - quantlab/utils/fingerprint.py
  - quantlab/utils/backtest_report.py
  - tests/test_backtest_persistence.py
autonomous: true
requirements: [BT-01, CFG-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Each run directory output_dir/{class}_{timestamp}/ holds config.json, weights.zarr, equity.zarr (value, returns), liquidations.json, metrics.json, report.html and fingerprint.json, and an existing directory is never overwritten (D-24)"
    - "weights.zarr re-opens identical to the in-memory target weights, and every JSON artifact is strict JSON written atomically (D-03, D-24)"
    - "fingerprint.json records, per dataset used (including warm-up), the time range, timestamp count, symbol count, variables and a sha256 over NaN-canonical values; the price dataset covers the fill and valuation columns, KunQuant factor datasets their data_columns, Polars factor datasets every data variable (D-27)"
    - "When expected_fingerprint is set and a dataset's digest, range or counts differ, run() logs a warning naming that dataset and the differing fields and still completes (D-27)"
    - "get_config() carries a top-level data_fingerprint record once a run has computed one, so the persisted config.json lets a rebuild compare (D-25, D-27)"
    - "report.html is a plotly page with an equity curve and a drawdown curve on a shared time axis, the in-sample range shaded when present, no benchmark trace, and the note that short-side returns are optimistic because no borrow cost is modelled (D-23, D-21, D-08)"
    - "metrics.json carries the same D-21 note (D-21)"
    - "use_wandb=False never calls wandb.init; use_wandb=True logs metrics and the report to a separate backtest run and finishes it (D-28)"
  artifacts:
    - path: "quantlab/utils/fingerprint.py"
      provides: "dataset_fingerprint (sha256, NaN-canonical)"
      contains: "def dataset_fingerprint"
    - path: "quantlab/utils/backtest_report.py"
      provides: "write_backtest_report (plotly equity + drawdown HTML)"
      contains: "def write_backtest_report"
    - path: "quantlab/base/backtest.py"
      provides: "complete run-dir persistence, fingerprint record and comparison, report notes, optional wandb"
      contains: "expected_fingerprint"
    - path: "tests/test_backtest_persistence.py"
      provides: "run-dir, fingerprint, report and wandb locks"
  key_links:
    - from: "quantlab/base/backtest.py"
      to: "quantlab/utils/fingerprint.py"
      via: "price and factor dataset fingerprints recorded during run()"
      pattern: "dataset_fingerprint\\("
    - from: "quantlab/base/backtest.py"
      to: "quantlab/utils/backtest_report.py"
      via: "_report_and_persist writes report.html"
      pattern: "write_backtest_report\\("
---

<objective>
Complete the run's persistent record:
- every D-24 artifact in the run directory;
- a data fingerprint recorded, and compared when a rebuild supplies the expected one (D-27);
- the interactive plotly report with in-sample shading and the short-side disclosure (D-23, D-21);
- the optional wandb switch (D-28).

Purpose: CFG-01 reproducibility needs more than the config. Tiingo re-bases adjusted prices after new dividends and stores get appended, so a re-run must detect that its data changed rather than silently disagree.

Output: `quantlab/utils/fingerprint.py`, `quantlab/utils/backtest_report.py`, persistence, fingerprint and wandb wiring in `quantlab/base/backtest.py`, and `tests/test_backtest_persistence.py`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-CONTEXT.md
@.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-RESEARCH.md
@.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-08-SUMMARY.md
@quantlab/base/backtest.py
@quantlab/base/model.py

The 03.7-08 SUMMARY is referenced for the metrics dict shape (`in_sample_range`) the report shades.

Standing constraints for every task in this plan:
- The user has uncommitted edits in `quantlab/enums/data.py`, `quantlab/factor/alpha158.py`, `quantlab/label/fret.py`. Never edit them and never `git add` them; commit only this plan's files, by explicit path.
- Never use `git stash`.
- `quantlab/utils/fingerprint.py` and `quantlab/utils/backtest_report.py` are leaf utilities: they import numpy, pandas, xarray, hashlib, plotly only, no `quantlab.*` module, and have English docstrings. `quantlab/base/backtest.py` docstrings are Chinese.
- Tests keep `WANDB_MODE=disabled`; wandb calls are asserted through a monkeypatched recorder, never over the network.
- A lock that passes on arrival is mutation-verified in a scratch copy and the original copied back; record each in the SUMMARY.
- The full-suite gate is `bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh`.
- Spec-less probe fallback: skipped (no requirement IDs to probe); recorded as a visible choice.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete run directory and the data fingerprint — record, persist, compare</name>
  <reversibility rating="costly">The run-directory layout and the fingerprint.json and config.json shapes become persisted, user-facing formats that rebuilds read (D-24, D-25, D-27). The user already decided the contents, so no checkpoint.</reversibility>
  <files>quantlab/base/backtest.py, quantlab/utils/fingerprint.py, tests/test_backtest_persistence.py</files>
  <read_first>
    - quantlab/base/backtest.py (_load_prices, _align_and_predict, _refresh_factor_reads, get_config, _report_and_persist, __init__)
    - quantlab/base/config.py (FactorConfig.data_columns vs PolarsFactorConfig; read only)
    - quantlab/utils/atomic.py and quantlab/utils/jsonable.py
    - quantlab/dataset/backend.py (lines 125-131: XrBackend.write mode="w")
    - tests/backtest_fixtures.py (write_price_store, train_checkpoint, make_model)
    - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-RESEARCH.md (Code Examples: Data fingerprint; Pitfall 10)
    - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-CONTEXT.md (D-24, D-27)
  </read_first>
  <behavior>
    - `test_run_directory_holds_every_d24_artifact`: the run_dir entry set is exactly {config.json, weights.zarr, equity.zarr, liquidations.json, metrics.json, report.html, fingerprint.json}, and no `*.tmp` file remains.
    - `test_weights_zarr_round_trips_identically`: `xr.open_zarr(run_dir / "weights.zarr")` loaded equals `result.weights` (xr.testing.assert_identical after load).
    - `test_equity_zarr_has_value_and_returns_on_timestamp`.
    - `test_every_json_artifact_is_strict_json`: config, metrics, liquidations and fingerprint all parse under a `parse_constant` that raises.
    - `test_existing_run_directory_is_never_overwritten`: `_run_dir_name` is monkeypatched to a fixed name whose directory already exists, and run() raises RuntimeError naming it with the existing directory's contents unchanged.
    - `test_fingerprint_is_stable_and_nan_canonical`: `dataset_fingerprint` of the same store opened twice is equal. Two arrays differing only in NaN payload bits or in -0.0 vs 0.0 give equal digests, and a single changed finite value changes the digest.
    - `test_fingerprint_json_covers_price_and_factor_datasets`: keys `price_dataset` and one per factor. The price entry's variables are the MARKET fill and valuation columns; a Polars factor entry's variables are every data variable of its dataset. Each entry has start, end, n_timestamps, n_symbols, algorithm "sha256" and digest.
    - `test_get_config_carries_the_data_fingerprint_after_a_run`.
    - `test_matching_expected_fingerprint_logs_no_warning` and `test_changed_store_logs_a_fingerprint_warning_and_completes`: set `expected_fingerprint` from a first run, then overwrite one adjusted close value in the Zarr store; a second backtester run warns naming `price_dataset` and "digest", and still returns a result.
  </behavior>
  <action>
Write the ten tests first in `tests/test_backtest_persistence.py`. Its English module docstring states that it locks D-24 and D-27 and why the fingerprint must be NaN-canonical: two reads of one store can carry NaNs with different payload bits. Add an autouse offline-wandb fixture and a loguru list sink.

(1) `quantlab/utils/fingerprint.py`: `dataset_fingerprint(ds: xr.Dataset, variables: list[str]) -> dict`, following RESEARCH Code Examples: Data fingerprint.
- Sort by timestamp and symbol.
- Hash the int64 nanosecond timestamps, then the NUL-joined symbol names.
- For each variable in sorted order, hash its name and its float64 values after canonicalizing NaN (`np.where(np.isnan(a), np.nan, a)`) and adding 0.0 (turning -0.0 into 0.0).
- Return {"algorithm": "sha256", "digest", "variables" (sorted), "start", "end" (ISO strings), "n_timestamps", "n_symbols"}.
- A missing variable raises ValueError naming it.

(2) `quantlab/base/backtest.py`:
- `__init__` sets `self.expected_fingerprint: dict | None = None` and `self._fingerprints: dict = {}` BEFORE assigning config.
- `_record_price_fingerprint(self, prices)` records under key "price_dataset" over the MARKET columns. Call it from `_load_prices`.
- `_record_factor_fingerprints(self)`: for each factor at index i, record under key `f"factor[{i}]:{factor.class_name}"` over `factor.config.dataset.get_xarray_dataset()`. Variables are `factor.config.data_columns` when the factor config is a `FactorConfig`, otherwise every data variable (Polars factors consume the whole lazyframe). Call it at the end of `_align_and_predict`, after the refreshed reads, so the range includes the warm-up (D-27).
- `_compare_fingerprints(self)` runs when `expected_fingerprint` is not None. Any key present on one side only, or any difference in digest/start/end/n_timestamps/n_symbols, produces one logger.warning naming the key and the differing fields. The run continues. Call it right after both records exist.
- `get_config()` adds top-level `"data_fingerprint": dict(self._fingerprints)` when non-empty.
- `_report_and_persist` additionally writes `liquidations.json` (to_jsonable of simulation.liquidations) and `fingerprint.json` (to_jsonable of the fingerprints), each through `write_json_atomically(..., indent=2)`. Keep the existing refuse-if-exists RuntimeError and the microsecond directory name.
- report.html is written by Task 2. Until Task 2 lands, the run-directory test expects it, so do Task 2 in the same session before committing Task 1, or mark that one test xfail(strict=True) in the Task 1 commit and remove the mark in Task 2.

Mutation-verify in a scratch copy: drop the NaN canonicalization; the NaN-payload assertion goes red. Copy the original back.
  </action>
  <verify>
    <automated>uv run pytest tests/test_backtest_persistence.py -q -k "not report and not wandb"</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line, or "no tests ran"</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_backtest_persistence.py -q -k "not report and not wandb"` exits 0 with at least 9 passed (the run-directory test may be xfail until Task 2).
    - `grep -c "def dataset_fingerprint" quantlab/utils/fingerprint.py` prints 1.
    - `uv run python -c "import ast,pathlib; t=ast.parse(pathlib.Path('quantlab/utils/fingerprint.py').read_text()); mods={n.module for n in ast.walk(t) if isinstance(n,ast.ImportFrom) and n.module}|{a.name for n in ast.walk(t) if isinstance(n,ast.Import) for a in n.names}; assert not any(m.startswith('quantlab') for m in mods), mods; print('leaf')"` prints "leaf".
    - The SUMMARY records the observed-red mutation.
  </acceptance_criteria>
  <done>A run records and persists fingerprints of every dataset it read, a changed store is detected with a warning, and the config carries the record a rebuild needs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HTML report with in-sample shading and the short-side note, plus the optional wandb run</name>
  <files>quantlab/base/backtest.py, quantlab/utils/backtest_report.py, tests/test_backtest_persistence.py</files>
  <read_first>
    - quantlab/base/backtest.py (_report_and_persist and the metrics `in_sample_range` key from 03.7-08)
    - quantlab/base/model.py (lines 572-575: _init_wandb, the wandb pattern to mirror)
    - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-RESEARCH.md (Code Examples: Plotly report; probe showing add_vrect gives 2 shapes on a 2-row subplot)
    - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-CONTEXT.md (D-08, D-21, D-23, D-28)
  </read_first>
  <behavior>
    - `test_report_has_equity_and_drawdown_and_shades_the_in_sample_range`: with an overlapping window, report.html contains trace names "equity" and "drawdown", a rect shape, and the short-side note text.
    - `test_report_without_in_sample_overlap_has_no_shaded_range`: no rect shape.
    - `test_report_and_metrics_carry_no_benchmark`: report.html contains no "benchmark" trace name, and metrics.json has no "benchmark" key (D-08).
    - `test_metrics_json_carries_the_short_side_note` (D-21).
    - `test_wandb_is_never_initialized_when_disabled`: `wandb.init` monkeypatched to raise, and run() with use_wandb False succeeds.
    - `test_wandb_logs_metrics_and_report_to_a_separate_backtest_run`: use_wandb True with a recorder. `wandb.init` is called once with project f"{class_name}_backtest" and name equal to the run_dir name; the summary receives flattened numeric keys prefixed "whole/"; `log` receives a "report" entry; `finish` is called once.
  </behavior>
  <action>
Write the six tests first, then implement.

(1) `quantlab/utils/backtest_report.py`: `write_backtest_report(value: xr.DataArray, path, *, in_sample_range: tuple[str, str] | None, notes: list[str], title: str) -> None`.
- Convert to pandas inside the function.
- `make_subplots(rows=2, cols=1, shared_xaxes=True, row_heights=[0.7, 0.3])`.
- A Scatter named "equity" in row 1 and a Scatter named "drawdown" in row 2, where drawdown = value / running max - 1.
- When in_sample_range is given, `fig.add_vrect(x0=..., x1=..., row="all", col=1, fillcolor="grey", opacity=0.2, line_width=0)`.
- The notes as a paper-referenced annotation below the plot.
- `fig.write_html(path, include_plotlyjs="cdn")`. The cdn choice keeps each run directory small; say so in the docstring. It is plot-styling discretion.

(2) `quantlab/base/backtest.py`:
- Add `_report_notes(self) -> list[str]`, whose default returns the D-21 note: no borrow or short-financing cost is modelled, so short-side returns are optimistic. An engine that models borrow costs overrides it.
- `_compute_metrics` (or run(), right after) adds `"notes": self._report_notes()` to metrics.
- `_report_and_persist` calls `write_backtest_report(simulation.value, run_dir / "report.html", in_sample_range=metrics.get("in_sample_range"), notes=self._report_notes(), title=run_dir.name)`.
- Add `_log_to_wandb(self, run_dir, metrics)`, called only when `self.config.use_wandb` (D-28):
  - `run = wandb.init(project=f"{self.class_name}_backtest", name=run_dir.name, config=to_jsonable(self.get_config()))`.
  - `run.summary.update(...)` with every finite numeric leaf of metrics["whole"], metrics["in_sample"] and metrics["out_of_sample"], flattened as `whole/<key>`, `in_sample/<key>`, `out_of_sample/<key>`. Nested turnover dicts become `whole/turnover/<key>`.
  - `run.log({"report": wandb.Html((run_dir / "report.html").read_text())})`, then `run.finish()`.
  - Import wandb at module top (the model layer already does), keeping it off the default path by the flag alone.
- No benchmark trace and no benchmark key when benchmark is None (D-08).

Mutation-verify in a scratch copy: call `_log_to_wandb` unconditionally; the disabled test goes red. Copy the original back. Remove any Task 1 xfail mark. Run the baseline gate.
  </action>
  <verify>
    <automated>uv run pytest tests/test_backtest_persistence.py tests/test_backtest_metrics.py tests/test_backtest_run.py -q</automated>
    <fails_when>non-zero exit, or "failed", "error" or "xfail" in the pytest summary line, or "no tests ran"</fails_when>
    <automated>bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh</automated>
    <fails_when>non-zero exit, or output containing "NEW FAILURE(S)", "NEW ERROR(S)" or "NO 'passed' SUMMARY LINE"</fails_when>
    <human-check>Open report.html from a synthetic overlapping run in a browser: the equity panel on top, the drawdown panel below on the same time axis, the in-sample range shaded grey, and the short-side note legible (VALIDATION manual-only row, D-23).</human-check>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_backtest_persistence.py -q` exits 0 with "16 passed" and no xfail.
    - `grep -c "def write_backtest_report" quantlab/utils/backtest_report.py` prints 1, and `grep -c "def _log_to_wandb" quantlab/base/backtest.py` prints 1.
    - `bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh` exits 0.
    - The SUMMARY records the observed-red mutation and whether the manual report check was performed.
  </acceptance_criteria>
  <done>Every run directory holds the interactive report with in-sample shading and the short-side note, metrics carry the note, and wandb logging is available behind an off-by-default switch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backtester → filesystem | run-directory artifacts are the durable record a rebuild trusts |
| backtester → wandb (opt-in) | metrics and the config leave the machine only when use_wandb is True |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.7-28 | Repudiation | silent data drift between runs (Tiingo re-basing, appended stores) | high | mitigate | NaN-canonical sha256 fingerprint of every dataset read (D-27), persisted and compared with a warning on rebuild |
| T-03.7-29 | Tampering | overwriting a prior run's artifacts | medium | mitigate | Refuse an existing run directory; atomic JSON writes; locked by `test_existing_run_directory_is_never_overwritten` |
| T-03.7-30 | Information Disclosure | config and metrics sent to wandb | low | mitigate | `use_wandb` defaults False (D-28); a test proves `wandb.init` is never called when disabled; no credential is part of the config |
| T-03.7-31 | Information Disclosure | report.html pulls plotly.js from a CDN when opened | low | accept | The page contains only local backtest numbers; the viewer's browser fetch reveals nothing about the data |
</threat_model>

<verification>
- `uv run pytest tests/test_backtest_persistence.py tests/test_backtest_metrics.py tests/test_backtest_run.py -q` exits 0.
- `bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh` exits 0.
- Manual: report.html visual check (VALIDATION manual-only row).
- Spec-less probe fallback: skipped (no requirement IDs to probe). Recorded here as a visible choice.
</verification>

<success_criteria>
- D-23, D-24, D-27 and D-28 are delivered and locked; D-21 is stated in both report and metrics; D-08 keeps the benchmark absent.
- No failures beyond the pre-phase baseline.
</success_criteria>

<output>
Create `.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-09-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Symbols and files created by this plan:

- `quantlab/utils/fingerprint.py`: `dataset_fingerprint`
- `quantlab/utils/backtest_report.py`: `write_backtest_report`
- `quantlab/base/backtest.py`: `BaseBacktester.expected_fingerprint`, `BaseBacktester._fingerprints`, `_record_price_fingerprint`, `_record_factor_fingerprints`, `_compare_fingerprints`, `_report_notes`, `_log_to_wandb`; `get_config()` top-level key `data_fingerprint`; metrics key `notes`
- Run-directory files: `liquidations.json`, `fingerprint.json`, `report.html` (alongside the existing `config.json`, `weights.zarr`, `equity.zarr`, `metrics.json`)
- `tests/test_backtest_persistence.py`: `test_run_directory_holds_every_d24_artifact`, `test_weights_zarr_round_trips_identically`, `test_equity_zarr_has_value_and_returns_on_timestamp`, `test_every_json_artifact_is_strict_json`, `test_existing_run_directory_is_never_overwritten`, `test_fingerprint_is_stable_and_nan_canonical`, `test_fingerprint_json_covers_price_and_factor_datasets`, `test_get_config_carries_the_data_fingerprint_after_a_run`, `test_matching_expected_fingerprint_logs_no_warning`, `test_changed_store_logs_a_fingerprint_warning_and_completes`, `test_report_has_equity_and_drawdown_and_shades_the_in_sample_range`, `test_report_without_in_sample_overlap_has_no_shaded_range`, `test_report_and_metrics_carry_no_benchmark`, `test_metrics_json_carries_the_short_side_note`, `test_wandb_is_never_initialized_when_disabled`, `test_wandb_logs_metrics_and_report_to_a_separate_backtest_run`
