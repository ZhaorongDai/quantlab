---
phase: 03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse
plan: 04
type: execute
wave: 2
depends_on: ["03.7-01"]
files_modified:
  - quantlab/backtest/engine_vectorbt.py
  - quantlab/backtest/us_equity.py
  - tests/test_backtest_engine.py
autonomous: true
requirements: [BT-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A bar-t target weight fills at bar t+1's fill-column price, slipped by `slippage` against the trade, with fees = size x fill price x fees (D-05, D-19)"
    - "A long position flips to short in one rebalance under direction='both' (D-05)"
    - "Order sizes are fractional (D-20); CrossSectionBacktestConfig defaults are fees 0.0005, slippage 0.0005 and init_cash 1_000_000 (D-19)"
    - "When a held symbol's price turns NaN, prices are forward-filled, the position is force-liquidated at the next rebalance at its last price, every other symbol keeps rebalancing, and SimulationResult.liquidations records symbol, signal timestamp, fill timestamp and price (D-07)"
    - "A rebalance row mixing finite weights and NaN is refused at the engine boundary before vectorbt runs (RESEARCH Pitfall 3)"
    - "A symbol with leading NaN prices that was never held fills normally once it lists (D-07 boundary)"
    - "Engine stats never contain a benchmark metric, _simulate_benchmark returns None, and a non-None benchmark_dataset is refused at construction naming D-08 (D-08)"
    - "The engine class docstring states that no borrow or short-financing cost is modelled, so short-side returns are optimistic (D-21)"
    - "US equity uses the adjusted fill/valuation columns and annualizes daily bars with 252 days and 1-minute bars with 252 x 390 minutes (D-04)"
    - "An unknown score_label fails at backtester construction, before any training (D-11)"
    - "A new VectorBtBacktester sibling with its own _generate_signals runs end to end without any change to BaseBacktester (D-01)"
  artifacts:
    - path: "quantlab/backtest/engine_vectorbt.py"
      provides: "forced-liquidation records, NaN-row refusal, benchmark hook"
      contains: "liquidations"
    - path: "quantlab/backtest/us_equity.py"
      provides: "US_EQUITY_MARKET spec and construction-time score-label check"
      contains: "US_EQUITY_MARKET"
    - path: "tests/test_backtest_engine.py"
      provides: "engine fact, delisting, market spec, construction and sibling-extensibility locks"
  key_links:
    - from: "quantlab/backtest/engine_vectorbt.py"
      to: "quantlab/base/backtest.py"
      via: "_simulate returns SimulationResult with liquidations populated"
      pattern: "SimulationResult\\("
    - from: "tests/test_backtest_engine.py"
      to: "quantlab/backtest/engine_vectorbt.py"
      via: "a test-local VectorBtBacktester subclass proves D-01 extensibility"
      pattern: "VectorBtBacktester\\)"
---

<objective>
Harden the vectorbt engine layer and the US-equity concrete class, and lock the engine facts CONTEXT verified by probe: t+1-open fills, flips, fees, slippage and fractional sizes (D-05, D-19, D-20). Also cover the delisting rule with forced-liquidation records (D-07), the benchmark hook's deferred state (D-08), the short-side disclosure (D-21), the market spec (D-04), construction-time label validation (D-11), and that a sibling backtester needs no base-class change (D-01).

Purpose: the engine decides every P&L number. RESEARCH found that vectorbt silently freezes a whole group on one NaN price and silently holds on a NaN weight; both must fail loudly or be handled.
Output: `quantlab/backtest/engine_vectorbt.py` (liquidation detection, boundary assertion), `quantlab/backtest/us_equity.py`, `tests/test_backtest_engine.py`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-CONTEXT.md
@.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-RESEARCH.md
@.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-01-SUMMARY.md
@quantlab/backtest/engine_vectorbt.py
@quantlab/backtest/us_equity.py
@quantlab/base/backtest.py
@tests/backtest_fixtures.py

The 03.7-01 SUMMARY is referenced because this plan extends the engine and fixtures that plan created.

Standing constraints for every task in this plan:
- The user has uncommitted edits in `quantlab/enums/data.py`, `quantlab/factor/alpha158.py`, `quantlab/label/fret.py`. Never edit them and never `git add` them; commit only this plan's files, by explicit path.
- Never use `git stash`.
- Scope fence: this plan owns ONLY `quantlab/backtest/engine_vectorbt.py`, `quantlab/backtest/us_equity.py` and `tests/test_backtest_engine.py`. `quantlab/base/backtest.py` belongs to 03.7-06 this wave and `quantlab/backtest/selection.py` to 03.7-03, so read both but do not edit them. If a defect there blocks a test, record it in the SUMMARY as a deviation instead of editing.
- `tests/test_backtest_contracts.py` is created by 03.7-14 in this same wave, so it does not exist in this plan's tree; never run it here. The D-04 rule (no price-column literal inside a method body) is checked instead by the inline AST command in Task 3's acceptance criteria, and 03.7-08 runs the whole contracts file in wave 3.
- Inline `uv run python -c` commands that import `quantlab.base.model` with vectorbt set `OMP_NUM_THREADS=1`.
- A lock that passes on arrival is mutation-verified in a scratch copy and the original copied back; record each in the SUMMARY.
- The full-suite gate is `bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh`.
- Spec-less probe fallback: skipped (no requirement IDs to probe); recorded as a visible choice.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Engine facts — t+1 open fills, flips, fees, slippage, fractional sizes, defaults</name>
  <files>quantlab/backtest/engine_vectorbt.py, tests/test_backtest_engine.py</files>
  <read_first>
    - quantlab/backtest/engine_vectorbt.py (as the tracer wrote it)
    - quantlab/base/backtest.py (SimulationResult fields; the config setter validation; read only)
    - tests/backtest_fixtures.py (write_price_store, make_model, make_stock_dataset)
    - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-RESEARCH.md (Pattern 1; Pitfalls 4, 5, 6; probe facts P1-P5)
    - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-CONTEXT.md (D-05, D-19, D-20)
  </read_first>
  <behavior>
    - A module helper `_backtester(tmp_path, **overrides)` builds a USEquityCrossectionSelectStockVectorBt over a fixture store and a fixture model with model_mode="load" and a checkpoint path string. The path need not exist, because these tests call `_simulate` directly and never `run()`.
    - A second helper `_panel(fill, valuation, timestamps, symbols)` builds the prices xr.Dataset under the backtester's MARKET column names, and `_weights(rows)` builds the weight Dataset.
    - `test_bar_t_weight_fills_at_bar_t_plus_1_open`: 3 bars x 1 symbol with target 1.0 on bar 0. The only order's timestamp is bar 1 and its price equals the bar-1 fill price exactly (fees=0, slippage=0).
    - `test_slippage_moves_buys_up_and_sells_down`: slippage=0.01. The buy fill price is open x 1.01, and the later sell (target 0.0) fill price is open x 0.99.
    - `test_fees_are_size_times_price_times_rate`: fees=0.001. Each order's fees equal size x price x 0.001 within 1e-9 relative.
    - `test_long_to_short_flip_executes_in_one_rebalance`: target +1.0 then -1.0 on a 2-symbol panel. After the flip fill bar the symbol's cumulative signed order size is negative.
    - `test_order_sizes_are_fractional`: prices chosen so the target cannot be met in whole shares, and at least one order size is non-integer.
    - `test_config_defaults_match_d19`: CrossSectionBacktestConfig built without fees/slippage/init_cash has 0.0005, 0.0005 and 1_000_000.0.
    - `test_first_equity_value_is_init_cash`: value at the first bar equals init_cash.
  </behavior>
  <action>
Create `tests/test_backtest_engine.py`.
- The English module docstring states that it locks the vectorbt engine facts CONTEXT verified by probe (D-05, D-19, D-20), the delisting rule (D-07), the benchmark hook (D-08) and D-01 extensibility. It explains that every assertion is on vectorbt's own order records, so a change in engine semantics turns them red.
- Add an autouse offline-wandb fixture.
- Add the helpers and seven tests above. Build tiny hand-made price panels as `xr.Dataset`s with the fill and valuation variables named from `USEquityCrossectionSelectStockVectorBt.MARKET`, never with literal column names. That keeps D-04's single source of truth.

Run the tests. Fix defects inside `quantlab/backtest/engine_vectorbt.py` only:
- The orders Dataset must map vectorbt's `records_readable` columns Timestamp/Column/Size/Price/Fees/Side onto timestamp/symbol/size/price/fees/side.
- Side must stay the readable "Buy"/"Sell" string, so a signed size is computable as +size for Buy and -size for Sell.
- Add a private helper `_signed_order_sizes(orders) -> xr.DataArray` that returns (timestamp, symbol) cumulative signed size; Task 2 reuses it.
- Keep `from_orders(..., size=w.shift(1), size_type="targetpercent", direction="both", group_by=True, cash_sharing=True, call_seq="auto")`.

Mutation-verify in a scratch copy:
- Remove the one-bar shift; the t+1 test goes red.
- Pass direction="longonly"; the flip test goes red.
Copy the original back.
  </action>
  <verify>
    <automated>uv run pytest tests/test_backtest_engine.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line, or fewer than 7 passed</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_backtest_engine.py -q` exits 0 with "7 passed".
    - `grep -c "def _signed_order_sizes" quantlab/backtest/engine_vectorbt.py` prints 1.
    - `grep -c 'size_type="targetpercent"' quantlab/backtest/engine_vectorbt.py` prints 1.
    - The SUMMARY records the two mutations (shift removed, longonly) and their observed red.
  </acceptance_criteria>
  <done>D-05 fill timing and flips, D-19 fees/slippage/defaults and D-20 fractional sizes are locked on vectorbt's own order records.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delisting — forward-filled prices, forced liquidation records, no group freeze, NaN-row refusal</name>
  <files>quantlab/backtest/engine_vectorbt.py, tests/test_backtest_engine.py</files>
  <read_first>
    - quantlab/backtest/engine_vectorbt.py (after Task 1)
    - quantlab/backtest/selection.py (eligibility uses the raw next-bar fill price; read only)
    - tests/backtest_fixtures.py (write_price_store delist_at / list_at)
    - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-RESEARCH.md (Pitfalls 2 and 3; probes P4, P4b, P4c, P10a-c)
    - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-CONTEXT.md (D-07)
  </read_first>
  <behavior>
    - `test_held_symbol_that_delists_is_liquidated_at_its_last_price_and_recorded`: symbol A is held from a rebalance on bar 0 (fill bar 1), and its fill and valuation go NaN from bar 3. A rebalance signal on bar 4 targets A=0.0 and B=1.0.
      - `simulation.liquidations` contains exactly one record: symbol "A", signal_timestamp bar 4, fill_timestamp bar 5, and price equal to A's last finite fill price.
      - A vectorbt sell order for A exists at bar 5.
    - `test_delisting_does_not_freeze_other_symbols`: same panel. B has a Buy order at bar 5 and a later rebalance on bar 8 still produces orders. This is the group-wide freeze regression from RESEARCH Pitfall 2.
    - `test_symbol_listing_late_fills_normally_once_listed`: C is NaN for bars 0-3 and never held before. A target on C at bar 5 fills at bar 6, and no liquidation is recorded for C.
    - `test_rebalance_row_mixing_nan_and_finite_is_refused_before_simulating`: a weight row [1.0, NaN] raises ValueError naming that timestamp, and vectorbt's from_orders is never called (monkeypatched spy).
    - `test_end_to_end_delisting_run_records_the_liquidation`: through `run()` on a fixture store with `delist_at` for a symbol the model selects. The run completes, `result.simulation.liquidations` is non-empty, and every record has keys {symbol, signal_timestamp, fill_timestamp, price}.
  </behavior>
  <action>
Write the five tests first, then implement in `quantlab/backtest/engine_vectorbt.py`. At the top of `_simulate`, before any pandas conversion, add a boundary check (RESEARCH Pitfall 3): every weight row must be either all-NaN or all-finite, otherwise raise ValueError naming the first offending timestamp. The base contract check already exists, but the engine re-asserts because a future caller may reach `_simulate` directly.

After the vectorbt run, build the forced-liquidation records (D-07):
- The signal rows are the weight rows that are finite.
- For each signal row at index t with t + 1 < n_bars, read the held position just before the fill as the cumulative signed order size per symbol up to and including bar t, via `_signed_order_sizes`.
- Record every symbol whose held position is nonzero and whose RAW (not forward-filled) fill price at t + 1 is NaN, as a dict: symbol (str), signal_timestamp and fill_timestamp (pandas Timestamps), and price (float, the forward-filled fill price at t + 1).
- Holdings come from order records, not a portfolio accessor, so the logic stays engine-neutral and does not depend on how vectorbt groups assets.
- Log one info line per recorded liquidation, and put the records on `SimulationResult.liquidations`.
- Extend the class docstring (Chinese) with D-07: why prices are forward-filled (one NaN-priced holding silently freezes every later rebalance of the whole group, RESEARCH Pitfall 2) and what a record holds.

Mutation-verify in a scratch copy:
- Drop the ffill on the fill frame; the freeze test goes red.
- Use the ffilled price for the NaN test; the record test goes red.
Copy the original back.
  </action>
  <verify>
    <automated>uv run pytest tests/test_backtest_engine.py tests/test_backtest_run.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line, or fewer than 13 passed</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_backtest_engine.py -q` exits 0 with "12 passed".
    - `uv run pytest tests/test_backtest_run.py -q` exits 0.
    - `grep -c "fill_timestamp" quantlab/backtest/engine_vectorbt.py` prints at least 1, and `grep -c "D-07" quantlab/backtest/engine_vectorbt.py` prints at least 1.
    - The SUMMARY records the two mutations and their observed red.
  </acceptance_criteria>
  <done>A delisted holding is force-liquidated at its last price and recorded. Other symbols keep rebalancing, a NaN-mixed rebalance row is refused before simulating, and a late listing trades normally.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Market spec, construction-time checks, benchmark deferral, short-side disclosure, sibling extensibility</name>
  <files>quantlab/backtest/us_equity.py, quantlab/backtest/engine_vectorbt.py, tests/test_backtest_engine.py</files>
  <read_first>
    - quantlab/backtest/us_equity.py (US_EQUITY_MARKET, _validate_config, _generate_signals)
    - quantlab/base/backtest.py (MarketSpec.year_freq, the config setter's D-08 rejection; read only)
    - quantlab/backtest/selection.py (rebalance_mask; read only)
    - tests/backtest_fixtures.py (make_model, train_checkpoint)
    - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-CONTEXT.md (D-01, D-04, D-08, D-11, D-21)
    - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-RESEARCH.md (Pitfall 6: year_freq and benchmark_return)
  </read_first>
  <behavior>
    - `test_us_equity_year_freq_daily_is_252_days_and_minute_is_252_sessions`: `US_EQUITY_MARKET.year_freq(np.timedelta64(1, "D"))` == `pd.Timedelta(days=252)`, and `year_freq(np.timedelta64(1, "m"))` == `pd.Timedelta(minutes=252 * 390)`.
    - `test_us_equity_market_uses_adjusted_columns`: the spec's fill_price_column and valuation_price_column are the adjusted Tiingo EOD open and close names, and both appear in `quantlab.enums.data.TiingoColumns.EOD` (read-only import).
    - `test_unknown_score_label_fails_at_construction_before_training`: ValueError at construction, `model.model` is still None, and no joblib file exists under model_save_dir.
    - `test_benchmark_dataset_is_refused_naming_d08`: a non-None benchmark_dataset raises NotImplementedError whose message contains "D-08".
    - `test_engine_stats_carry_no_benchmark_metric_and_warn_nothing`: after a real `run()`, the "whole" metrics keys contain no "Benchmark" key, and pytest's recwarn records no UserWarning mentioning "benchmark_rets".
    - `test_simulate_benchmark_returns_none`.
    - `test_a_sibling_engine_subclass_runs_without_touching_the_base`: a test-local `EqualWeightEveryone(VectorBtBacktester)` with `config_cls = CrossSectionBacktestConfig`, `MARKET = US_EQUITY_MARKET`, and a `_generate_signals` that gives 1/n on `rebalance_mask` rows and NaN elsewhere. It runs `run()` to completion on a trained fixture checkpoint, and every rebalance row equals 1/n.
  </behavior>
  <action>
Write the seven tests first, then fix defects only in `quantlab/backtest/us_equity.py` and `quantlab/backtest/engine_vectorbt.py`.

Confirm and, if needed, rewrite the engine class docstring so it states D-21 explicitly, citing "D-21": no borrow or short-financing cost is modelled and short-side returns are therefore optimistic. The test file does not assert docstring prose; the acceptance criterion greps it.

Confirm the US_EQUITY_MARKET constant carries a Chinese comment explaining D-04: the adjusted columns are split- and dividend-adjusted, and unadjusted open/close are never used for P&L.

The `EqualWeightEveryone` sibling is defined inside the test module only. Its purpose is to prove D-01: a new engine-layer sibling needs only `config_cls`, `MARKET` and `_generate_signals`, and BaseBacktester is not edited.

Mutation-verify in a scratch copy of us_equity.py: move `resolve_score_label` out of `_validate_config` into `_generate_signals`; the construction test goes red. Copy the original back. Then run the baseline gate.
  </action>
  <verify>
    <automated>uv run pytest tests/test_backtest_engine.py tests/test_backtest_run.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line, or fewer than 20 passed</fails_when>
    <automated>bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh</automated>
    <fails_when>non-zero exit, or output containing "NEW FAILURE(S)", "NEW ERROR(S)" or "NO 'passed' SUMMARY LINE"</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_backtest_engine.py -q` exits 0 with "19 passed".
    - `grep -c "D-21" quantlab/backtest/engine_vectorbt.py` prints at least 1, and `grep -c "D-04" quantlab/backtest/us_equity.py` prints at least 1.
    - `OMP_NUM_THREADS=1 uv run python -c "import ast,pathlib; from quantlab.backtest.us_equity import US_EQUITY_MARKET as M; names={M.fill_price_column,M.valuation_price_column}; files=[pathlib.Path('quantlab/base/backtest.py'),*sorted(pathlib.Path('quantlab/backtest').glob('*.py'))]; bad=[(str(f),n.lineno) for f in files for fn in ast.walk(ast.parse(f.read_text())) if isinstance(fn,(ast.FunctionDef,ast.AsyncFunctionDef)) for n in ast.walk(fn) if isinstance(n,ast.Constant) and isinstance(n.value,str) and n.value in names]; assert not bad, bad; print('no price-column literal in a method body')"` prints "no price-column literal in a method body". The D-04 literal rule still holds after any us_equity.py edit; this replaces running the contracts file, which 03.7-14 creates in this same wave.
    - `bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh` exits 0.
  </acceptance_criteria>
  <done>The market spec, the D-11 construction check, the D-08 deferral, the D-21 disclosure and D-01 sibling extensibility are locked, with no failures beyond the baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| price store → engine | vendor prices (with NaN from delistings or late listings) drive fills and valuation |
| weights → engine | any producer of D-03 weights (selector today, optimizer in Phase 5) feeds vectorbt |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.7-12 | Tampering (result integrity) | `VectorBtBacktester._simulate` group freeze | high | mitigate | Forward-fill fill and valuation frames (D-07), record forced liquidations from raw NaN prices, regression test that other symbols keep rebalancing after a delisting |
| T-03.7-13 | Tampering (result integrity) | NaN-mixed weight rows | medium | mitigate | Boundary ValueError before from_orders, with a spy test proving vectorbt is never reached |
| T-03.7-14 | Repudiation (misleading metrics) | short-side returns | low | mitigate | D-21 disclosure in the engine docstring here; report and metrics notes in 03.7-09; docs in 03.7-13 |
</threat_model>

<verification>
- `uv run pytest tests/test_backtest_engine.py tests/test_backtest_run.py -q` exits 0, and Task 3's inline AST command confirms the D-04 literal rule. The contracts file belongs to 03.7-14 in this wave.
- `bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh` exits 0.
- Spec-less probe fallback: skipped (no requirement IDs to probe). Recorded here as a visible choice.
</verification>

<success_criteria>
- D-05, D-07, D-19, D-20 are locked on vectorbt's own records; D-01, D-04, D-08, D-11, D-21 are locked or stated.
- The group-wide NaN freeze cannot silently return.
- No failures beyond the pre-phase baseline.
</success_criteria>

<output>
Create `.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-04-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Symbols created by this plan:

- `quantlab/backtest/engine_vectorbt.py`: `VectorBtBacktester._signed_order_sizes`; forced-liquidation record dict keys `symbol`, `signal_timestamp`, `fill_timestamp`, `price`
- `tests/test_backtest_engine.py`: helpers `_backtester`, `_panel`, `_weights`; test-local class `EqualWeightEveryone`; tests `test_bar_t_weight_fills_at_bar_t_plus_1_open`, `test_slippage_moves_buys_up_and_sells_down`, `test_fees_are_size_times_price_times_rate`, `test_long_to_short_flip_executes_in_one_rebalance`, `test_order_sizes_are_fractional`, `test_config_defaults_match_d19`, `test_first_equity_value_is_init_cash`, `test_held_symbol_that_delists_is_liquidated_at_its_last_price_and_recorded`, `test_delisting_does_not_freeze_other_symbols`, `test_symbol_listing_late_fills_normally_once_listed`, `test_rebalance_row_mixing_nan_and_finite_is_refused_before_simulating`, `test_end_to_end_delisting_run_records_the_liquidation`, `test_us_equity_year_freq_daily_is_252_days_and_minute_is_252_sessions`, `test_us_equity_market_uses_adjusted_columns`, `test_unknown_score_label_fails_at_construction_before_training`, `test_benchmark_dataset_is_refused_naming_d08`, `test_engine_stats_carry_no_benchmark_metric_and_warn_nothing`, `test_simulate_benchmark_returns_none`, `test_a_sibling_engine_subclass_runs_without_touching_the_base`
