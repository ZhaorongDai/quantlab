---
phase: 03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse
plan: 06
type: execute
wave: 2
depends_on: ["03.7-01", "03.7-02"]
files_modified:
  - quantlab/base/backtest.py
  - tests/test_backtest_dates.py
autonomous: true
requirements: [BT-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The warm-up start is max(factor.config.window) bars before the window start on the price dataset's own timestamp axis, so across a weekend it spans more calendar days than the window (D-15)"
    - "When the price calendar has fewer bars than the warm-up needs, the warm-up clamps to the first bar and logs a warning naming the shortfall (D-15)"
    - "After run(), every factor's config.start_date is the warm-up start and config.end_date the backtest end, and the returned predictions cover exactly the window bars (D-14, D-15)"
    - "A model trained first (its collect narrowed to its own dates) still predicts over the widened warm-up range, for factor_data_strategy 'cal' and 'read', because every re-dated dataset and factor read is refreshed (D-14, RESEARCH Pitfall 1)"
    - "A prediction panel missing a price-dataset symbol yields 0.0 weight for that symbol on rebalance rows: the universe is every price symbol (D-06)"
    - "model_mode='train' trains on the model's own train/test dates and leaves them unchanged; model_mode='load' loads the checkpoint, and a DL head's feature panel is collected before load (D-13, RESEARCH Pitfall 11)"
    - "Fold-style date strings such as '2026-08-07T00:00:00.000000000' normalize to ISO dates through one helper (RESEARCH Pitfall 10)"
  artifacts:
    - path: "quantlab/base/backtest.py"
      provides: "_refresh_factor_reads, _iso_date, DL-aware _prepare_model, hardened _warmup_start/_align_and_predict"
      contains: "def _refresh_factor_reads"
    - path: "tests/test_backtest_dates.py"
      provides: "warm-up, re-dating, cache-refresh, universe and model-preparation locks"
  key_links:
    - from: "quantlab/base/backtest.py"
      to: "quantlab/base/factor.py"
      via: "_refresh_factor_reads calls factor.read(overwrite=True) for the read strategy"
      pattern: "read\\(overwrite=True\\)"
    - from: "quantlab/base/backtest.py"
      to: "quantlab/base/model.py"
      via: "DL heads get data_backend.to_internal(features) before load"
      pattern: "to_internal\\("
---

<objective>
Make the first two steps of the D-02 template, model preparation and date alignment, correct in every mode:
- bar-accurate warm-up on the price calendar (D-15);
- factor re-dating that survives the XrBackend read cache for both factor data strategies (D-14, RESEARCH Pitfall 1);
- the full-universe reindex (D-06);
- train vs load model preparation, including DL heads that need a collected panel before load (D-13, Pitfall 11);
- one date-normalization helper for fold dates (Pitfall 10).

Purpose: a stale read cache or calendar-day arithmetic silently shrinks the warm-up or empties the first rebalance, and nothing downstream would notice.
Output: hardened `quantlab/base/backtest.py` steps and `tests/test_backtest_dates.py`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-CONTEXT.md
@.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-RESEARCH.md
@.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-01-SUMMARY.md
@.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-02-SUMMARY.md
@quantlab/base/backtest.py
@tests/backtest_fixtures.py

The 03.7-01 SUMMARY is referenced because this plan hardens the template steps it created; the 03.7-02 SUMMARY because this plan consumes `Factor.read(overwrite=True)`.

Standing constraints for every task in this plan:
- The user has uncommitted edits in `quantlab/enums/data.py`, `quantlab/factor/alpha158.py`, `quantlab/label/fret.py`. Never edit them and never `git add` them; commit only this plan's files, by explicit path.
- Never use `git stash`.
- Scope fence: this plan owns ONLY `quantlab/base/backtest.py` and `tests/test_backtest_dates.py`. `tests/backtest_fixtures.py` is read-only here, so define any extra test double (the tiny DL head) inside the test file.
- Date alignment works by mutating factor configs (D-14). Never build a parallel slicing or fetching path.
- Configs are constructed directly, never through the factories in `quantlab/config/__init__.py` (D-32).
- A lock that passes on arrival is mutation-verified in a scratch copy and the original copied back; record each in the SUMMARY.
- The full-suite gate is `bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh`.
- Spec-less probe fallback: skipped (no requirement IDs to probe); recorded as a visible choice.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bar-accurate warm-up and cache-proof re-dating for both factor data strategies, plus the full-universe reindex</name>
  <files>quantlab/base/backtest.py, tests/test_backtest_dates.py</files>
  <read_first>
    - quantlab/base/backtest.py (_price_calendar, _warmup_start, _align_and_predict, run)
    - quantlab/base/factor.py (Factor.read(overwrite=...) from 03.7-02, _reset_dataset_config, FactorPolars.cal, save)
    - quantlab/base/model.py (_collect_all_features and its factor_data_strategy branch)
    - quantlab/dataset/backend.py (lines 15-27: the read cache early-return)
    - tests/backtest_fixtures.py (write_price_store, PastReturnFactor, make_model, train_checkpoint)
    - tests/test_read_cache_refresh.py (the cache semantics 03.7-02 locked)
    - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-RESEARCH.md (Pitfalls 1, 10, 14)
  </read_first>
  <behavior>
    - `test_warmup_counts_bars_on_the_price_calendar_not_calendar_days`: business-day store, factor window 5, window start on a Monday. After run(), `factor.config.start_date` is the ISO date of the Monday one week earlier (5 bars = 7 calendar days); calendar-day arithmetic would give the preceding Wednesday.
    - `test_warmup_clamps_to_first_bar_and_warns_on_short_history`: window start at bar 2 with window 5. The warm-up equals the store's first date, and a WARNING mentions the shortfall.
    - `test_factor_dates_are_pushed_and_predictions_cover_exactly_the_window`: every factor's end_date equals the backtest end, and `result.predictions.timestamp` equals the window bars exactly.
    - `test_read_strategy_predicts_over_the_widened_range_after_training`: factor stores saved over the full range; MLConfig factor_data_strategy="read"; model dates start after the backtest warm-up. In train mode, the window's first bar gets finite predictions for every listed symbol. Verified as a mutation: without the factor read refresh those predictions are NaN.
    - `test_cal_strategy_predicts_over_the_widened_range_after_training`: same shape for "cal". Verified as a mutation: without the dataset refresh the first rebalance is flat.
    - `test_symbol_absent_from_predictions_gets_zero_weight_on_rebalance_rows`: the factor's dataset config uses `symbols=` excluding one price symbol. That symbol exists in `result.weights` with 0.0 on rebalance rows and NaN elsewhere (D-06).
    - `test_iso_date_normalizes_fold_style_strings`: `_iso_date("2026-08-07T00:00:00.000000000")`, `_iso_date(np.datetime64("2026-08-07"))` and `_iso_date(pd.Timestamp("2026-08-07 15:30"))` all return "2026-08-07".
  </behavior>
  <action>
Write the seven tests first in `tests/test_backtest_dates.py`. Its English module docstring states that it locks D-06, D-14 and D-15 and the read-cache trap. It explains why each would go red, citing RESEARCH Pitfall 1's probe: 6 bars, still 6 after widening, 10 with overwrite. Add an autouse offline-wandb fixture and a loguru list sink for warnings.

Implement in `quantlab/base/backtest.py`:

(a) A static method `_iso_date(value) -> str` returning `pd.Timestamp(str(value)).strftime("%Y-%m-%d")`. Use `str(value)` because `pd.Timestamp` rejects `numpy.str_` (RESEARCH Pitfall 10). Every date this module writes into a dataset or factor config goes through it.

(b) `_refresh_factor_reads(self, factor)`:
- Always call `factor.config.dataset.read(overwrite=True)`.
- When `self.config.model.config.factor_data_strategy == "read"`, also call `factor.read(overwrite=True)`, the 03.7-02 flag.
- `_align_and_predict` calls it for every factor right after `_reset_dataset_config()`, replacing the tracer's inline dataset read.
- Its Chinese docstring explains the cache: the XrBackend read returns early once loaded and `_filter`/`_auto_filter` narrow in place, so a re-dated config would otherwise keep reading the earlier narrower window.

(c) Keep `_warmup_start` counting bars with `np.searchsorted` on the calendar and returning `_iso_date(...)`. Its warning names the requested bars, the available bars and the clamped start.

(d) Confirm run() reindexes predictions onto `prices.symbol` and `prices.timestamp` before `_generate_signals` (D-06), so a symbol missing from predictions scores NaN, is ineligible and gets 0.0.

Mutation-verify in a scratch copy:
- Remove the `factor.read(overwrite=True)` branch; the read-strategy test goes red.
- Replace the calendar lookup with `pd.Timestamp(start) - pd.DateOffset(days=window)`; the bar-count test goes red.
Copy the original back.
  </action>
  <verify>
    <automated>uv run pytest tests/test_backtest_dates.py tests/test_backtest_run.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line, or fewer than 8 passed</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_backtest_dates.py -q` exits 0 with "7 passed".
    - `uv run pytest tests/test_backtest_run.py -q` exits 0.
    - `grep -c "def _refresh_factor_reads" quantlab/base/backtest.py` prints 1, and `grep -c "def _iso_date" quantlab/base/backtest.py` prints 1.
    - `grep -vE '^\s*#' quantlab/base/backtest.py | grep -c "DateOffset"` prints 0 (no calendar-day warm-up arithmetic in executable code).
    - The SUMMARY records the two mutations and their observed red.
  </acceptance_criteria>
  <done>The warm-up is bar-accurate and clamped with a warning, re-dated reads are refreshed for both strategies, the universe is every price symbol, and fold-style dates normalize.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Model preparation — train on the model's own dates, load ML and DL checkpoints</name>
  <files>quantlab/base/backtest.py, tests/test_backtest_dates.py</files>
  <read_first>
    - quantlab/base/backtest.py (_prepare_model after Task 1; the config setter's model_mode/checkpoint validation)
    - quantlab/base/model.py (load, DLModel._read_checkpoint needing num_symbols from data_backend at lines 836-843; MLModel._read_checkpoint at 1116-1119; train)
    - tests/test_model_hierarchy.py (lines 120-140: LinearDLHead, the smallest DLModel)
    - tests/backtest_fixtures.py (make_model, train_checkpoint)
    - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-CONTEXT.md (D-13)
    - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-RESEARCH.md (Pitfall 11)
  </read_first>
  <behavior>
    - `test_train_mode_uses_the_models_own_dates_and_leaves_them_unchanged`: model_mode="train" with model dates distinct from the backtest window. After run(), `model.config.train_start/train_end/test_start/test_end` equal their original values, exactly one `*.joblib` exists under model_save_dir, and the first window bar's predictions are finite.
    - `test_load_mode_with_missing_checkpoint_file_fails_before_predicting`: a checkpoint path that does not exist raises FileNotFoundError, and a spy on `model.predict_panel` records zero calls.
    - `test_load_mode_without_checkpoint_is_refused_at_construction`: model_mode="load" with neither checkpoint nor cv_project_dir raises ValueError at construction.
    - `test_dl_head_loads_after_its_feature_panel_is_collected`: a test-local `TinyLinearDLHead(DLModel)` (nn.Linear on the last axis, trains one epoch) gets a `.pth` checkpoint via collect() and train(). A fresh head instance with the same config, in model_mode="load", completes run(). Verified as a mutation: without the pre-load collection, load fails inside `_read_checkpoint`.
  </behavior>
  <action>
Write the four tests first, then change `_prepare_model` in `quantlab/base/backtest.py`:
- model_mode "train": call `model.collect()` then `model.train()`, never writing the backtest dates into the model's train/test dates (D-13).
- model_mode "load": first check `Path(checkpoint).exists()` and raise FileNotFoundError naming the path, before any feature work. Then, only for a `DLModel` instance (import DLModel from `quantlab.base.model`), call `model.data_backend.to_internal(model._collect_all_features())`, because `DLModel._read_checkpoint` sizes the network from `num_symbols` read off that backend (RESEARCH Pitfall 11). MLModel checkpoints skip this. Finally call `model.load(checkpoint)`.
- Its Chinese docstring states why each branch exists.

The construction-time check already exists from the tracer. Update the message only if needed, so it states that "load" needs a checkpoint path: `run_cv` needs a cv_project_dir instead, and 03.7-10 relaxes the rule to either one. Write it so the relaxation is a one-line change.

Mutation-verify in a scratch copy: remove the DL pre-load collection; the DL test goes red. Copy the original back. Then run the baseline gate.
  </action>
  <verify>
    <automated>uv run pytest tests/test_backtest_dates.py tests/test_backtest_run.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line, or fewer than 12 passed</fails_when>
    <automated>bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh</automated>
    <fails_when>non-zero exit, or output containing "NEW FAILURE(S)", "NEW ERROR(S)" or "NO 'passed' SUMMARY LINE"</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_backtest_dates.py -q` exits 0 with "11 passed".
    - `grep -c "isinstance(model, DLModel)\|isinstance(self.config.model, DLModel)" quantlab/base/backtest.py` prints at least 1.
    - `bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh` exits 0.
    - The SUMMARY records the DL mutation and its observed red.
  </acceptance_criteria>
  <done>Train mode respects the model's own dates. Load mode fails fast on a missing file, and DL checkpoints load after their panel is collected. The full suite shows no failures beyond the baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config dates → dataset/factor configs | user or persisted dates are written straight into dataset configs (bypassing the setter's ISO normalization) |
| checkpoint path → deserialization | load mode opens a user-supplied path |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.7-18 | Tampering (result integrity) | stale read cache after re-dating | high | mitigate | `_refresh_factor_reads` (dataset always; factor store for the read strategy), mutation-verified tests for both strategies |
| T-03.7-19 | Tampering (input validation) | non-ISO dates written into dataset configs | medium | mitigate | Every written date goes through `_iso_date`; lexicographic comparisons downstream stay correct |
| T-03.7-20 | Denial of Service | load mode on a missing checkpoint after expensive feature work | low | mitigate | Existence check before any feature collection |
| T-03.7-21 | Tampering | pickle/torch checkpoint deserialization | medium | accept | Trusted-local-file stance documented on the model layer; suffix check in `BaseModel.load` |
</threat_model>

<verification>
- `uv run pytest tests/test_backtest_dates.py tests/test_backtest_run.py -q` exits 0.
- `bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh` exits 0.
- Spec-less probe fallback: skipped (no requirement IDs to probe). Recorded here as a visible choice.
</verification>

<success_criteria>
- D-06, D-13, D-14 and D-15 hold under both factor data strategies and both model modes, including DL load.
- The read-cache and calendar-day traps are locked by mutation-verified tests.
- No failures beyond the pre-phase baseline.
</success_criteria>

<output>
Create `.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-06-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Symbols created by this plan:

- `quantlab/base/backtest.py`: `BaseBacktester._iso_date`, `BaseBacktester._refresh_factor_reads` (and the DL-aware branch of `_prepare_model`)
- `tests/test_backtest_dates.py`: test-local `TinyLinearDLHead`; tests `test_warmup_counts_bars_on_the_price_calendar_not_calendar_days`, `test_warmup_clamps_to_first_bar_and_warns_on_short_history`, `test_factor_dates_are_pushed_and_predictions_cover_exactly_the_window`, `test_read_strategy_predicts_over_the_widened_range_after_training`, `test_cal_strategy_predicts_over_the_widened_range_after_training`, `test_symbol_absent_from_predictions_gets_zero_weight_on_rebalance_rows`, `test_iso_date_normalizes_fold_style_strings`, `test_train_mode_uses_the_models_own_dates_and_leaves_them_unchanged`, `test_load_mode_with_missing_checkpoint_file_fails_before_predicting`, `test_load_mode_without_checkpoint_is_refused_at_construction`, `test_dl_head_loads_after_its_feature_panel_is_collected`
