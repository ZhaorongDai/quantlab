---
phase: 03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse
plan: 12
subsystem: backtest
tags: [cleanup, vecbt, vectorbt, docs, claude-md, codebase-map, deletion-lock, mutation-testing, tdd]

requires:
  - phase: 03.7-10
    provides: run_cv and the final class and file names the maps describe
  - phase: 03.7-11
    provides: load_backtester_from_config
  - phase: 03.7-13
    provides: example/backtest.md, which the README bullet links
  - phase: 03.7-14
    provides: the vectorbt import-scope lock with its temporary quantlab/vecbt exemption
provides:
  - quantlab/vecbt/ deleted (both tracked files and the untracked __pycache__)
  - tests/test_backtest_contracts.py::test_vecbt_package_is_retired
  - test_vectorbt_is_imported_only_by_the_engine with no exemption
  - README.md, CLAUDE.md and .planning/codebase describe quantlab/backtest/ and quantlab/base/backtest.py
affects: [phase-5-portfolio-optimizer, phase-6-backtest]

actuals:
  tokens: 10231    # chars/4 over git diff f3e9166..cb94827 (40927 chars)
  tasks: 2
  commits: 4       # MEASURED: git rev-list --count f3e9166..HEAD before the docs commit
plan_head_before: f3e91665e612b92ecd54227151aa5879e1930c6c

tech-stack:
  added: []
  patterns:
    - "A retirement lock pairs find_spec with a filesystem existence check, and each arm is proven by its own mutation"
    - "Current-state docs name a retired or deleted item only inside a parenthetical that says retired/deleted, so a grep gate can tell history from a live pointer"

key-files:
  created:
    - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/deferred-items.md
  modified:
    - tests/test_backtest_contracts.py
    - train_model.py
    - README.md
    - CLAUDE.md
    - .planning/codebase/STACK.md
    - .planning/codebase/STRUCTURE.md
    - .planning/codebase/ARCHITECTURE.md
  deleted:
    - quantlab/vecbt/__init__.py
    - quantlab/vecbt/bt.py

key-decisions:
  - "A directory holding only __pycache__ is a namespace package, so find_spec('quantlab.vecbt') returns a spec for it; the existence arm is still needed because a non-package entry at that path is invisible to find_spec"
  - "The README's stale Nautilus backtest/ bullet and the vecbt bullet became one quantlab/backtest/ bullet, as the plan asked"
  - "CLAUDE.md's error-handling example now cites BaseBacktester's D-08 benchmark NotImplementedError, with a parenthetical that the DLModel._fit(backtest=True) guard was deleted (D-37)"
  - "Stale Nautilus-strategy statements elsewhere in the maps (outside the backtest/vecbt passages this plan rewrites) were logged to deferred-items.md, not fixed"

patterns-established:
  - "When a deleted package's lock passes on arrival in a given shape, find the shape that escapes one arm and plant it"

requirements-completed: [CLEAN-02]

coverage:
  - id: D1
    description: "quantlab/vecbt is deleted from git and from disk, and test_vecbt_package_is_retired keeps it gone (find_spec is None and the path does not exist, with a positive control on quantlab.backtest)"
    requirement: CLEAN-02
    verification:
      - kind: unit
        ref: "tests/test_backtest_contracts.py#test_vecbt_package_is_retired"
        status: pass
      - kind: other
        ref: "RED at 6b90e0c: find_spec returned the real package spec; gsd-tools check tdd-red-evidence RED_EVIDENCE_OK"
        status: pass
      - kind: other
        ref: "mutations M1 (empty __init__.py), M2 (__pycache__-only directory), M4 (plain file at quantlab/vecbt, find_spec None): all RED"
        status: pass
    human_judgment: false
  - id: D2
    description: "The vectorbt import-scope lock has no exemption: only quantlab/backtest/engine_vectorbt.py may import vectorbt"
    verification:
      - kind: unit
        ref: "tests/test_backtest_contracts.py#test_vectorbt_is_imported_only_by_the_engine"
        status: pass
      - kind: other
        ref: "RED at 6b90e0c: {'quantlab/vecbt/bt.py': ['vectorbt']}; RED_EVIDENCE_OK. Mutation M3 (import vectorbt in selection.py): RED"
        status: pass
    human_judgment: false
  - id: D3
    description: "No live code references the retired helper: train_model.py's commented import is gone and the root-script scan still parses it"
    requirement: CLEAN-02
    verification:
      - kind: other
        ref: "git grep -n vecbt -- quantlab train_model.py README.md -> no output (rc 1); no (from|import) vecbt anywhere in *.py/*.ipynb"
        status: pass
      - kind: unit
        ref: "uv run pytest tests/test_entry_point_contracts.py -q -> 1 failed (baseline test_the_import_smoke_test_covers_the_ingest_shells), 6 passed, 1 skipped"
        status: pass
    human_judgment: false
  - id: D4
    description: "README.md, CLAUDE.md and .planning/codebase describe quantlab/backtest/ and quantlab/base/backtest.py; every remaining vecbt mention is marked retired, the D-37 guard mention is marked deleted, and the GSD markers are unchanged"
    requirement: CLEAN-02
    verification:
      - kind: other
        ref: "plan Task 2 verify 1 -> maps-ok; _fit(backtest refs all contain 'deleted'; grep -c USEquityCrossectionSelectStockVectorBt CLAUDE.md = 2; 14 marker lines byte-identical to HEAD; README links example/backtest.md"
        status: pass
    human_judgment: true
    rationale: "The grep gates prove pointers and markers, not that the rewritten prose describes the backtester accurately and readably; a reader should confirm it"
  - id: D5
    description: "No failures beyond the 56-test pre-phase baseline"
    verification:
      - kind: other
        ref: "bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh -> 56 failed, 962 passed, 1 skipped; exit 0"
        status: pass
    human_judgment: false

duration: 9min
completed: 2026-09-15
status: complete
---

# Phase 03.7 Plan 12: Retire vecbt and Correct the Current-State Maps Summary

**`quantlab/vecbt/` is gone from git and from disk. A two-arm lock (`find_spec` plus a filesystem check) keeps it gone, and the vectorbt import-scope lock no longer exempts it. README.md, CLAUDE.md and the three codebase maps now describe `BaseBacktester` → `VectorBtBacktester` → `USEquityCrossectionSelectStockVectorBt` with `run()`/`run_cv()` and `load_backtester_from_config`. Every remaining mention of the retired helper or the D-37 guard is marked retired or deleted. Four mutations each turned a lock red, and the gate shows no new failures.**

## Performance

- **Duration:** 9 min
- **Started:** 2026-09-15T07:44:06Z
- **Completed:** 2026-09-15T07:53:38Z
- **Tasks:** 2
- **Files modified:** 9 in the task commits (2 deleted, 7 modified; 90 insertions, 74 deletions), plus `deferred-items.md` in the docs commit

## Accomplishments

- **Deletion (D-31, CLEAN-02).** `git rm` removed `quantlab/vecbt/__init__.py` and `quantlab/vecbt/bt.py`, and the untracked `__pycache__` was deleted, so `test ! -e quantlab/vecbt` holds. The editable install maps only the top `quantlab` package, so the removal takes effect without `uv sync`.
- **Locks in `tests/test_backtest_contracts.py`.**
  - `test_vecbt_package_is_retired` checks two things: `importlib.util.find_spec("quantlab.vecbt") is None`, and the path does not exist. A positive control confirms `find_spec("quantlab.backtest")` is not None.
  - `test_vectorbt_is_imported_only_by_the_engine` lost its `legacy_vecbt` exemption and the TEMPORARY ALLOWANCE comment. Its positive control on the engine is unchanged.
  - The module docstring now lists the retirement lock.
- **`train_model.py`:** only the commented `# from vecbt.bt import ...` line was deleted.
- **`README.md`:** the `backtest/` Nautilus bullet (its file was deleted 2026-09-07) and the vecbt bullet became one `quantlab/backtest/` bullet. It covers `BaseBacktester` with `run()`/`run_cv()`, `VectorBtBacktester` (`from_orders`, target percent, t+1 open fills), `CrossSectionTopNSelector`, `USEquityCrossectionSelectStockVectorBt` and `load_backtester_from_config`, and links `example/backtest.md`.
- **`CLAUDE.md`** (text inside the stack and architecture sections only; all 14 `<!-- GSD:` lines are byte-identical):
  - both vectorbt stack bullets;
  - four component rows in place of `backtest_from_signals`;
  - the Backtest layer's Purpose / Location / Depends on / Used by, where the event-driven path is still reserved for Phase 6;
  - the layering chain, which now ends at `backtest` (`quantlab/backtest/`);
  - a "Resolved" line under the anti-pattern heading;
  - the error-handling example, which now cites `BaseBacktester`'s D-08 benchmark `NotImplementedError`, with the D-37 deletion noted.
- **`.planning/codebase/`:**
  - STACK.md gets the same two vectorbt bullets.
  - STRUCTURE.md: the directory tree lists `quantlab/backtest/*` and `base/backtest.py`; the `backtest/` purpose section is rewritten; "New backtest" guidance says a new engine subclasses `BaseBacktester` and a new selection style or market subclasses `VectorBtBacktester`.
  - ARCHITECTURE.md: the system diagram, the four component rows, a "file deleted 2026-09-07" note on the `Test` row, the overall pattern line, the Backtest layer and the layering chain are updated. The anti-pattern section is marked resolved, and its original text is kept and labelled as history.
  - Each map gets a dated footer line.

## Task Commits

1. **Task 1 RED: retirement lock, allowance removed** - `6b90e0c` (test)
2. **Task 1 GREEN: package deleted, train_model.py and README.md updated** - `48880e4` (feat)
3. **Task 1 fix: retirement-lock docstring corrected after mutation M2** - `d083dac` (fix)
4. **Task 2: CLAUDE.md and the three codebase maps** - `cb94827` (docs)

**Plan metadata:** recorded in the docs commit that follows this SUMMARY.

### Scope fence (`git diff --stat f3e9166..cb94827`)

```
 .planning/codebase/ARCHITECTURE.md | 36 +++++++++++++++++++++---------------
 .planning/codebase/STACK.md        |  5 +++--
 .planning/codebase/STRUCTURE.md    | 27 +++++++++++++++------------
 CLAUDE.md                          | 22 +++++++++++++---------
 README.md                          | 13 ++++++++++---
 quantlab/vecbt/__init__.py         |  0
 quantlab/vecbt/bt.py               | 23 -----------------------
 tests/test_backtest_contracts.py   | 36 ++++++++++++++++++++++++++++--------
 train_model.py                     |  2 --
```

Exactly the plan's `files_modified`. The user's uncommitted edits in `quantlab/enums/data.py`, `quantlab/factor/alpha158.py` and `quantlab/label/fret.py` were never edited, staged or committed. `git stash` was not used. Every commit staged explicit paths, and the only deletions (the two vecbt files) are intentional.

## TDD Gate Compliance

| Task | RED | GREEN | REFACTOR | Status |
|------|-----|-------|----------|--------|
| 1 | `6b90e0c` | `48880e4` | none (`d083dac` is a docstring fix) | Pass |
| 2 | n/a (docs task, not `tdd="true"`) | `cb94827` | none | n/a |

**Task 1 RED.** The real run was `uv run pytest tests/test_backtest_contracts.py -rA -q -p no:cacheprovider` against the tree that still held `quantlab/vecbt`. It exited 1 with 8 tests: 2 failed, 6 passed.
- `test_vecbt_package_is_retired` failed with `AssertionError: assert ModuleSpec(name='quantlab.vecbt', ... origin=.../quantlab/vecbt/__init__.py) is None`.
- `test_vectorbt_is_imported_only_by_the_engine` failed with `AssertionError: {'quantlab/vecbt/bt.py': ['vectorbt']}`.

The scratchpad script `red_evidence_12.py` projected that run's PASSED/FAILED node ids into TAP. Nothing was synthesized. `gsd-tools check tdd-red-evidence` returned `RED_EVIDENCE_OK` for both target tests.

## Mutation Verification

Each mutation was planted in the working tree, run against one test, then removed. `quantlab/backtest/selection.py` was restored from a scratchpad copy and byte-compared against `git show HEAD:`. After all four, `quantlab/vecbt` was absent and the contracts file passed 8/8.

| # | Planted defect | Lock | Result |
|---|---|---|---|
| M1 | empty `quantlab/vecbt/__init__.py` (plan mutation) | retired | RED: `find_spec` returned a `SourceFileLoader` spec |
| M2 | `quantlab/vecbt/__pycache__/bt.cpython-313.pyc` only | retired | RED, but on the `find_spec` arm: a `__pycache__`-only directory is a namespace package (`loader=None`, `_NamespacePath`) |
| M3 | `import vectorbt as vbt` appended to `quantlab/backtest/selection.py` | vectorbt scope | RED: `{'quantlab/backtest/selection.py': ['vectorbt']}` |
| M4 | plain empty file at `quantlab/vecbt` (`find_spec` prints None) | retired | RED on the existence arm alone: `assert not True where True = exists()` |

The first mutation attempt never ran: zsh does not word-split an unquoted `$P` holding the pytest command, so each line failed with `command not found`. It was re-run with the command spelled out, and those results are the ones above.

## Decisions Made

See `key-decisions` in the frontmatter.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Retirement-lock docstring made a false claim about `find_spec`**
- **Found during:** Task 1, mutation M2
- **Issue:** The docstring as first committed said a directory holding only `__pycache__` is "invisible to `find_spec`". M2 showed Python resolves such a directory as a namespace package, so `find_spec` catches it. The claim would mislead anyone reasoning about which arm is needed.
- **Fix:** The docstring now says both a restored `__init__.py` and a `__pycache__`-only directory are caught by `find_spec`, and that the existence arm additionally catches a non-package entry at that path. M4 was added to prove that arm on its own. No assertion changed.
- **Files modified:** tests/test_backtest_contracts.py
- **Verification:** 8 passed; M4 RED on the existence arm.
- **Committed in:** `d083dac`

**2. [Rule 1 - Accuracy] ARCHITECTURE.md `Test` (Strategy) row annotated as deleted**
- **Found during:** Task 2
- **Issue:** The component table this plan rewrites still presented the Nautilus `Test` strategy (deleted 2026-09-07) as a current component, directly above the new backtester rows.
- **Fix:** Added "(file deleted 2026-09-07)" to that row and kept the rest of the row. The other stale Nautilus statements, outside the passages this plan rewrites, were logged in `deferred-items.md` rather than fixed.
- **Files modified:** .planning/codebase/ARCHITECTURE.md
- **Committed in:** `cb94827`

---

**Total deviations:** 2 auto-fixed (1 false test docstring, 1 stale table row)
**Impact on plan:** Both stay inside `files_modified`. No assertion was loosened and no code behaviour changed.

## Issues Encountered

- **zsh word splitting** voided the first mutation run, as described above. No result from that run was used.
- **Out-of-scope stale statements** (logged in `deferred-items.md`, not fixed):
  - STACK.md lines 31/47 and ARCHITECTURE.md's "Live Prediction Path" section still describe the deleted Nautilus strategy;
  - ARCHITECTURE.md's error-handling pattern still says ML training is unimplemented;
  - CLAUDE.md mentions `predictions_history` "in the live strategy".
- **Historical mentions left as found:** the three docstrings plan 05 recorded (`quantlab/dl_model/rnn.py:236`, `quantlab/utils/nautilus.py:33`, `tests/test_dl_models.py:686`) contain no `vecbt` substring, so no acceptance grep hit them. `example/*.md` was not touched, since it belongs to 03.7-13.
- Spec-less probe fallback: skipped (no requirement IDs to probe), recorded as the plan asks.

## Known Stubs

None. The diff adds no TODO, FIXME, placeholder, skip or xfail.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- CLEAN-02's named item (the vectorbt signal helper) is retired and locked out. This is the last 03.7 plan without a SUMMARY; all 14 plans now have one.
- **Baseline gate after `cb94827`:** 56 failed / 962 passed / 1 skipped (exit 0). That is the 56-failure baseline, with passed up by exactly the one new lock (961 → 962).
- The rewritten CLAUDE.md is loaded as instructions in later sessions. It now points at `quantlab/backtest/` and `example/backtest.md`, not a broken helper.

---
*Phase: 03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse*
*Completed: 2026-09-15*

## Self-Check: PASSED

- Files: `quantlab/vecbt` is absent; `tests/test_backtest_contracts.py`, `train_model.py`, `README.md`, `CLAUDE.md` and the three codebase maps are committed, and `git status --short` shows only the user's three pre-existing edits.
- Commits: `6b90e0c`, `48880e4`, `d083dac` and `cb94827` appear in `git log`; `git rev-list --count f3e9166..HEAD` = 4 before this docs commit.
- Acceptance:
  - `tests/test_backtest_contracts.py`: 8 passed;
  - `git grep vecbt -- quantlab train_model.py README.md` printed nothing;
  - the README links `example/backtest.md`;
  - the entry-point contracts file shows only its 1 baseline failure;
  - Task 2 verify printed `maps-ok`;
  - the `_fit(backtest` refs are all marked deleted;
  - the `USEquityCrossectionSelectStockVectorBt` count is 2;
  - the marker count is unchanged;
  - the baseline gate exits 0.
