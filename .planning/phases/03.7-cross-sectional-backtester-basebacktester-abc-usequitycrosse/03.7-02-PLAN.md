---
phase: 03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - quantlab/utils/module.py
  - quantlab/base/factor.py
  - quantlab/base/data.py
  - quantlab/base/constituent.py
  - tests/test_config_roundtrip.py
  - tests/test_read_cache_refresh.py
autonomous: true
requirements: [CFG-01, BT-01]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A PolarsFactorConfig dict rebuilds into its FactorPolars class with a PolarsFactorConfig, with no TypeError about mode/data_columns; get_config -> JSON -> load_factor_from_config -> get_config is identical (D-26)"
    - "A KunQuant factor (Alpha101Stock) and a MarketDataset (StockDataset) round-trip identically through the same loaders (D-26)"
    - "A ConstituentDatasetConfig dict rebuilds into SP500ConstituentDataset with a ConstituentDatasetConfig, with no TypeError about cache_dir (D-26)"
    - "load_dataset_from_config, load_factor_from_config and load_model_from_config never mutate the dict they are given (RESEARCH Pitfall 9)"
    - "A class that declares no config_cls is refused by the loaders with a TypeError naming the class; config_cls is a plain class attribute, so BaseDataset and Factor test subclasses stay instantiable (D-26)"
    - "Factor.read(overwrite=True) re-opens the factor store so widened dates take effect; the default read() keeps its cached semantics (D-14, RESEARCH Pitfall 1)"
    - "Every round-trip test constructs configs directly, never through quantlab/config factories (D-32)"
  artifacts:
    - path: "quantlab/utils/module.py"
      provides: "config_cls-based, non-mutating load_dataset_from_config / load_factor_from_config / load_model_from_config"
      contains: "config_cls"
    - path: "quantlab/base/factor.py"
      provides: "FactorKunQuant.config_cls, FactorPolars.config_cls, Factor.read(overwrite=)"
      contains: "config_cls = PolarsFactorConfig"
    - path: "quantlab/base/data.py"
      provides: "MarketDataset.config_cls"
      contains: "config_cls = DatasetConfig"
    - path: "quantlab/base/constituent.py"
      provides: "IndexConstituentDataset.config_cls"
      contains: "config_cls = ConstituentDatasetConfig"
    - path: "tests/test_config_roundtrip.py"
      provides: "KunQuant, Polars, market-dataset and constituent-dataset round-trip regression tests"
    - path: "tests/test_read_cache_refresh.py"
      provides: "read-cache refresh locks for Factor.read and BaseDataset.read"
  key_links:
    - from: "quantlab/utils/module.py"
      to: "quantlab/base/factor.py"
      via: "load_factor_from_config builds cls.config_cls(**config)"
      pattern: "config_cls\\(\\*\\*"
    - from: "quantlab/base/factor.py"
      to: "quantlab/dataset/backend.py"
      via: "Factor.read forwards overwrite to XrBackend.read"
      pattern: "overwrite=overwrite"
---

<objective>
Fix the config-rebuild gap so a persisted dataset or factor config rebuilds with the config class its class declares. This closes the verified `PolarsFactorConfig` and `ConstituentDatasetConfig` TypeErrors, and the fix is locked with round-trip regression tests (D-26). Also give `Factor.read` the `overwrite` flag the backtester's re-dated reads need (D-14, RESEARCH Pitfall 1).

Purpose: CFG-01 reproducibility. The backtest config rebuild (plan 03.7-11) nests dataset and factor configs, so it can only be as correct as these loaders.
Output: `config_cls` class attributes on the four intermediate dataset/factor classes, non-mutating loaders in `quantlab/utils/module.py`, `Factor.read(overwrite=...)`, and two test files.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-CONTEXT.md
@.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-RESEARCH.md
@.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-PATTERNS.md
@quantlab/utils/module.py
@quantlab/base/factor.py

Standing constraints for every task in this plan:
- The user has uncommitted edits in `quantlab/enums/data.py`, `quantlab/factor/alpha158.py`, `quantlab/label/fret.py`. Never edit them, never `git add` them; commit only the files each task lists, by explicit path.
- Never use `git stash`; use `git show HEAD:<file>` or a scratch copy for A/B comparisons.
- The full-suite gate is `bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh`: no failures beyond the pre-phase baseline.
- A lock that passes on arrival is mutation-verified: break the specific line in a scratch copy, see the test go red, copy the original back, and record it in the SUMMARY.
- STATE.md recorded in Phase 03 that `Factor.config_cls` was deliberately NOT added then, deferred until a reload path needed it. D-26 is that need, so this plan adds it.
- Spec-less probe fallback: skipped. This phase has no requirement IDs to probe; recorded as a visible choice.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Loaders build cls.config_cls(**config) without mutating their input — Polars factors and constituent datasets rebuild</name>
  <files>quantlab/utils/module.py, quantlab/base/factor.py, quantlab/base/data.py, quantlab/base/constituent.py, tests/test_config_roundtrip.py</files>
  <read_first>
    - quantlab/utils/module.py (whole file: the two hardcoded loaders and load_model_from_config)
    - quantlab/base/factor.py (FactorKunQuant at line 194, FactorPolars at line 349, the config imports at lines 15-19)
    - quantlab/base/data.py (lines 82-110 BaseDataset header; lines 1103-1110 MarketDataset and its `config: DatasetConfig` narrowing)
    - quantlab/base/constituent.py (lines 25-85: IndexConstituentDataset, its config property and setter)
    - quantlab/base/config.py (DatasetConfig, ConstituentDatasetConfig, FactorConfig, PolarsFactorConfig field sets)
    - quantlab/factor/alpha101.py (the Alpha101Stock class and the adjusted input names its graph reads)
    - quantlab/factor/momentum.py (Momentum, a shipped FactorPolars)
    - quantlab/dataset/constituent.py (SP500ConstituentDataset)
    - tests/conftest.py (lines 703-800 spot_kline_zarr; lines 865-927 stock_zarr)
    - tests/test_factor_polars.py (lines 48-83: `_momentum_config` builds a PolarsFactorConfig directly)
    - tests/test_constituent_panel.py (lines 18-37: an offline ConstituentDatasetConfig)
    - tests/test_model_hierarchy.py (lines 270-330: loader tests that monkeypatch `module_utils.load_factor_from_config` and must stay green)
  </read_first>
  <behavior>
    - `test_polars_factor_round_trips_with_its_own_config_class`: build Momentum over `spot_kline_zarr`. `get_config()` goes through `json.loads(json.dumps(cfg, default=str))`, then `load_factor_from_config`. The rebuilt object is a Momentum whose `type(config)` is PolarsFactorConfig, and its JSON-normalized `get_config()` equals the original's.
    - `test_kunquant_factor_round_trips`: same shape for Alpha101Stock over `stock_zarr`, with FactorConfig(mode="batch", data_columns=("adjHigh","adjLow","adjClose","adjOpen","adjVolume")). The rebuilt `type(config)` is FactorConfig.
    - `test_market_dataset_round_trips`: StockDataset rebuilds with `type(config)` DatasetConfig and an identical normalized config.
    - `test_constituent_dataset_round_trips`: SP500ConstituentDataset with an offline ConstituentDatasetConfig under tmp_path rebuilds with `type(config)` ConstituentDatasetConfig. No network is touched.
    - `test_loaders_do_not_mutate_their_input`: a deep copy of each input dict taken before load_dataset_from_config, load_factor_from_config and load_model_from_config equals the dict after. The model case uses the monkeypatched factor loader from tests/test_model_hierarchy.py.
    - `test_a_class_without_config_cls_is_refused_by_name`: monkeypatch `get_cls_from_path` to return a class with no config_cls. Both dataset and factor loaders raise TypeError whose message contains that class's name.
    - `test_config_cls_is_a_plain_class_attribute`: the four class attributes resolve as MarketDataset->DatasetConfig, IndexConstituentDataset->ConstituentDatasetConfig, FactorKunQuant->FactorConfig and FactorPolars->PolarsFactorConfig, and "config_cls" is absent from BaseDataset.__abstractmethods__ and Factor.__abstractmethods__.
  </behavior>
  <action>
Write the failing tests first in `tests/test_config_roundtrip.py`. Its English module docstring names D-26 and the two TypeErrors reproduced in CONTEXT. It explains that the tests JSON-normalize both sides because tuples serialize to lists, and that configs are constructed directly, never through `quantlab/config/__init__.py` factories (D-32). Run them and see the Polars and constituent cases fail with those TypeErrors. Then implement:

(1) Plain class attributes, never abstract properties. Tests subclass BaseDataset and Factor directly (tests/test_rebuild_aside_recovery.py, test_backend_indexes.py, test_dataset_hierarchy.py, test_chunked_ingest.py, test_factor_save_mode.py, test_factor_update.py), and an abstract member would make those classes uninstantiable.
- `FactorKunQuant.config_cls = FactorConfig` and `FactorPolars.config_cls = PolarsFactorConfig`, both in quantlab/base/factor.py, where both config classes are already imported.
- `MarketDataset.config_cls = DatasetConfig` in quantlab/base/data.py, beside the existing `config: DatasetConfig` narrowing.
- `IndexConstituentDataset.config_cls = ConstituentDatasetConfig` in quantlab/base/constituent.py.
Give each a one-line comment naming D-26 and pointing at `quantlab/utils/module.py`.

(2) `quantlab/utils/module.py`:
- Add `import copy` and remove the now-unused `from quantlab.base.config import DatasetConfig, FactorConfig`.
- Add one private helper, `_config_cls_of(cls) -> type`. It returns `cls.config_cls` when that attribute is a type, and otherwise raises TypeError naming `cls.__qualname__`, saying the class declares no config_cls and so cannot be rebuilt from a config dict.
- `load_dataset_from_config(config)`: take `config = copy.deepcopy(config)`, resolve `cls = get_cls_from_path(config["name"])`, and return `cls(_config_cls_of(cls)(**config))`.
- `load_factor_from_config(config)`: deep-copy, replace `config["dataset"]` with `load_dataset_from_config(config["dataset"])`, then build the same way.
- `load_model_from_config(config)`: add the same deep copy as the first statement and keep the rest of the body. It must still call `load_factor_from_config` through the module global, because tests/test_model_hierarchy.py monkeypatches that name.
- Write English docstrings on both changed loaders, naming D-26 and RESEARCH Pitfall 9 (callers reuse the dict they saved).

Mutation-verify two cases:
- Revert load_factor_from_config to a hardcoded FactorConfig in a scratch copy; the Polars test goes red.
- Remove the deep copy; the no-mutation test goes red.
Restore both by copying the originals back.
  </action>
  <verify>
    <automated>uv run pytest tests/test_config_roundtrip.py tests/test_model_hierarchy.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line, or "no tests ran"</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_config_roundtrip.py tests/test_model_hierarchy.py -q` exits 0, with at least 7 passed from tests/test_config_roundtrip.py.
    - `grep -c "config_cls = " quantlab/base/factor.py` prints 2; `grep -c "config_cls = DatasetConfig" quantlab/base/data.py` prints 1; `grep -c "config_cls = ConstituentDatasetConfig" quantlab/base/constituent.py` prints 1.
    - `grep -vE '^\s*#' quantlab/utils/module.py | grep -c "DatasetConfig(\*\*config)\|FactorConfig(\*\*config)"` prints 0 (no hardcoded config class left in executable code).
    - `uv run python -c "import ast,pathlib; t=ast.parse(pathlib.Path('tests/test_config_roundtrip.py').read_text()); mods={n.module for n in ast.walk(t) if isinstance(n,ast.ImportFrom) and n.module}|{a.name for n in ast.walk(t) if isinstance(n,ast.Import) for a in n.names}; assert not any(m=='quantlab.config' or m.startswith('quantlab.config.') for m in mods), mods; print('no factory imports')"` prints "no factory imports".
    - The SUMMARY records two observed-red mutations (hardcoded FactorConfig; removed deep copy).
  </acceptance_criteria>
  <done>Polars factors, KunQuant factors, market datasets and constituent datasets all rebuild from JSON with their declared config classes. Loaders no longer mutate their input, and every behavior is regression-locked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Factor.read(overwrite=True) makes re-dated reads take effect</name>
  <files>quantlab/base/factor.py, tests/test_read_cache_refresh.py</files>
  <read_first>
    - quantlab/base/factor.py (Factor.read at lines 110-113, Factor._auto_filter, FactorPolars.cal, Factor.save)
    - quantlab/dataset/backend.py (lines 15-27: `XrBackend.read(path, overwrite=False)` returns early when data is already loaded)
    - quantlab/base/data.py (lines 288-291: `BaseDataset.read(**kwargs)` already forwards overwrite, then `_filter()` narrows in place)
    - tests/test_factor_polars.py (lines 48-83: `_momentum_config` with explicit window/start_date/end_date)
    - tests/conftest.py (lines 703-800: spot_kline_zarr)
    - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-RESEARCH.md (Pitfall 1 and its probe: 6 bars, still 6 after widening, 10 with overwrite)
  </read_first>
  <behavior>
    - `test_factor_read_keeps_the_cached_narrow_window_by_default`: save a Momentum factor store over the full spot_kline_zarr range with `cal().save(mode="w")`. A new factor instance with a narrow start_date reads N1 timestamps. After widening `config.start_date`, `read()` still returns N1. This pins today's cache semantics, so changing the default is a visible decision.
    - `test_factor_read_overwrite_true_honours_widened_dates`: same setup, but `read(overwrite=True)` after widening returns more than N1 timestamps and equals a fresh instance's read over the widened range.
    - `test_dataset_read_overwrite_true_honours_widened_dates`: SpotKlineDataset read narrow, then widen `config.start_date` (ISO string); `read()` keeps the narrow count and `read(overwrite=True)` gives the widened count.
  </behavior>
  <action>
Write the three tests first in `tests/test_read_cache_refresh.py`. Its English module docstring explains the cache trap: `XrBackend.read` returns early once data is loaded, and `_filter`/`_auto_filter` narrow in place. It explains why the backtester's D-14 re-dating needs an explicit refresh, citing RESEARCH Pitfall 1. Run them and see `test_factor_read_overwrite_true_honours_widened_dates` fail (Factor.read accepts no argument today).

Then change `Factor.read` in quantlab/base/factor.py to `read(self, overwrite: bool = False) -> Self`. The body forwards to `self.data_backend.read(self.config.file_path, overwrite=overwrite)` and then calls `self._auto_filter()`. Add a short English docstring saying:
- The default keeps the cached narrow read.
- `overwrite=True` re-opens the store, and is required after mutating `config.start_date`/`end_date` (the backtester's D-14 path).

Do not change `BaseDataset.read`; it already forwards `**kwargs`, and the dataset test locks that. When widening dates in the tests, write ISO `YYYY-MM-DD` strings directly onto the config, because the dataset config setter only normalizes on whole-config assignment. Mutation-verify by dropping `overwrite=overwrite` from the forward in a scratch copy: the overwrite test goes red. Restore by copying the original back.
  </action>
  <verify>
    <automated>uv run pytest tests/test_read_cache_refresh.py tests/test_factor_polars.py tests/test_factor_save_mode.py tests/test_factor_update.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line, or "no tests ran"</fails_when>
    <automated>bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh</automated>
    <fails_when>non-zero exit, or output containing "NEW FAILURE(S)", "NEW ERROR(S)" or "NO 'passed' SUMMARY LINE"</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_read_cache_refresh.py tests/test_factor_polars.py tests/test_factor_save_mode.py tests/test_factor_update.py -q` exits 0, including "3 passed" worth of tests from tests/test_read_cache_refresh.py.
    - `grep -c "def read(self, overwrite: bool = False)" quantlab/base/factor.py` prints 1.
    - `bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh` exits 0.
    - The SUMMARY records the observed-red mutation (overwrite not forwarded).
  </acceptance_criteria>
  <done>A re-dated factor or dataset read returns the widened window when `overwrite=True` is passed. The default cached behaviour is pinned, and the full suite shows no failures beyond the baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config JSON → `get_cls_from_path` | a persisted dict names the class to import and instantiate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.7-07 | Tampering / Elevation of Privilege | `load_dataset_from_config`, `load_factor_from_config` | medium | accept | Config JSON is trusted local input, the same stance as joblib checkpoints (`MLModel` docstring); `_config_cls_of` refuses any class that declares no `config_cls`, which also stops an arbitrary non-quantlab callable from being instantiated with a config dict; the backtester loader adds a `BaseBacktester` subclass check in plan 03.7-11 |
| T-03.7-08 | Tampering (data integrity) | loaders mutating caller dicts | low | mitigate | `copy.deepcopy` as each loader's first statement, locked by `test_loaders_do_not_mutate_their_input` |
| T-03.7-09 | Tampering (result integrity) | stale read cache after re-dating | medium | mitigate | `Factor.read(overwrite=True)`, locked by `tests/test_read_cache_refresh.py`; the backtester uses it in plan 03.7-06 |
</threat_model>

<verification>
- `uv run pytest tests/test_config_roundtrip.py tests/test_read_cache_refresh.py tests/test_model_hierarchy.py -q` exits 0.
- `bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh` exits 0.
- Spec-less probe fallback: skipped (no requirement IDs to probe). Recorded here as a visible choice.
</verification>

<success_criteria>
- The two TypeErrors reproduced in CONTEXT D-26 no longer occur, and KunQuant/Polars/market/constituent round trips are regression-locked.
- Loaders are non-mutating and refuse classes without `config_cls` by name.
- `Factor.read(overwrite=True)` exists with its cache semantics locked in both directions.
- No failures beyond the pre-phase baseline.
</success_criteria>

<output>
Create `.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-02-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Symbols created by this plan:

- `quantlab/utils/module.py`: `_config_cls_of`
- `quantlab/base/factor.py`: `FactorKunQuant.config_cls`, `FactorPolars.config_cls`, the `overwrite` parameter of `Factor.read`
- `quantlab/base/data.py`: `MarketDataset.config_cls`
- `quantlab/base/constituent.py`: `IndexConstituentDataset.config_cls`
- `tests/test_config_roundtrip.py`: `test_polars_factor_round_trips_with_its_own_config_class`, `test_kunquant_factor_round_trips`, `test_market_dataset_round_trips`, `test_constituent_dataset_round_trips`, `test_loaders_do_not_mutate_their_input`, `test_a_class_without_config_cls_is_refused_by_name`, `test_config_cls_is_a_plain_class_attribute`
- `tests/test_read_cache_refresh.py`: `test_factor_read_keeps_the_cached_narrow_window_by_default`, `test_factor_read_overwrite_true_honours_widened_dates`, `test_dataset_read_overwrite_true_honours_widened_dates`
