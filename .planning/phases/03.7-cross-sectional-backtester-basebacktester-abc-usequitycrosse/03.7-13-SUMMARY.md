---
phase: 03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse
plan: 13
subsystem: docs
tags: [backtest, vectorbt, xarray, xgboost, polars, docs, predict_panel, cv_folds]

requires:
  - phase: 03.7-10
    provides: run_cv() with manifest validation, contiguity assertion, stitched simulation and CV run directory
  - phase: 03.7-11
    provides: load_backtester_from_config and identical re-run guarantees
  - phase: 03.7-05
    provides: predict_panel head adapters (MLP, RNNRegressor, RNNClassifier)
  - phase: 03.7-07
    provides: cv_folds.json manifest and D-37 hook deletion
provides:
  - example/backtest.md, the user-facing guide to the backtester layer with an offline example that really runs
  - example/model.md sections for predict_panel and cv_folds.json, plus dated D-37 annotations
  - example/README.md index row for backtest.md and an annotated backtest-skeleton defect row
affects: [03.7-12, phase-5-portfolio-optimizer, phase-6-end-to-end-backtest]

actuals:
  tokens: 16700    # bytes/4 over the realized diff (66915 bytes; Chinese text is 3 bytes per char, so chars/4 is lower)
  tasks: 2
  commits: 2
plan_head_before: 39e7d1c409105877116453b1fe48e03979d8bd73

tech-stack:
  added: []
  patterns:
    - "Docs example is extracted from the markdown and executed by the verify command; pasted stdout is compared against that run"
    - "History annotations cite the commit that actually removed code, not the phase that owns the decision"

key-files:
  created:
    - example/backtest.md
  modified:
    - example/model.md
    - example/README.md

key-decisions:
  - "The minimal example defines its Polars factor and label inline for single-file readability, and the guide says its run directory therefore cannot be rebuilt in another process (verified: AttributeError: module '__main__' has no attribute 'PastReturn')"
  - "The D-37 annotation distinguishes three removals: _do_vecbt / BaseModel._vecbt went in the user's commit d07f06e on 2026-09-14 (before 03.7); RNNClassifier._vecbt in 03.7-05; the backtest flag, backtest_data and _reset_backtest_dataset_config in 03.7-07"
  - "The predict_panel section sits after the whole 它吃什么、吐什么 section, not directly under ### 吐：预测, so the existing ### subsections keep their parent; 吐：预测 gets a dated pointer instead"

patterns-established:
  - "Backtest docs state limitations where users look: D-21 optimistic short side, D-08 benchmark unavailable, stitched report unshaded, day-granular fold contiguity"

requirements-completed: [BT-01, CFG-01, CLEAN-02]

coverage:
  - id: D1
    description: "example/backtest.md documents the hierarchy, run()/run_cv(), weights contract, prices and t+1 fills, universe and delisting, selection, model preparation and warm-up, in/out-of-sample reporting, costs, run directory and rebuild with fingerprints, citing decision ids"
    requirement: "BT-01"
    verification:
      - kind: other
        ref: "uv run python -c <plan Task 1 doc check> -> doc-ok"
        status: pass
    human_judgment: true
    rationale: "The structural check proves required sections and citations exist; whether the prose explains the backtester correctly and readably is a reader judgment"
  - id: D2
    description: "The minimal example in example/backtest.md runs offline on a synthetic store and its pasted stdout is the real output"
    requirement: "CFG-01"
    verification:
      - kind: other
        ref: "awk-extract ## 最小可运行例子 python block -> OMP_NUM_THREADS=1 WANDB_MODE=disabled uv run --project <root> python example.py -> example-ran"
        status: pass
      - kind: other
        ref: "cmp extracted block vs executed scratchpad script (identical); diff of stdout vs pasted output identical except run dir name"
        status: pass
    human_judgment: false
  - id: D3
    description: "example/model.md documents predict_panel with the MLP/RNN adapters and cv_folds.json; the deleted backtest skeleton is annotated, not erased; README indexes backtest.md and annotates the skeleton row"
    requirement: "CLEAN-02"
    verification:
      - kind: other
        ref: "uv run python -c <plan Task 2 doc check> -> model-docs-ok"
        status: pass
      - kind: other
        ref: "git diff HEAD -- example/model.md | grep -c '^-[^-]' -> 1 (limit 5)"
        status: pass
      - kind: integration
        ref: "bash .planning/phases/03.7-.../03.7-baseline-gate.sh -> 56 failed / 961 passed / 1 skipped, no new failures"
        status: pass
    human_judgment: true
    rationale: "Accuracy of the Chinese prose against the code and history was checked by the author; a reader should confirm it reads correctly"

duration: 15min
completed: 2026-09-15
status: complete
---

# Phase 03.7 Plan 13: Backtester Guide and Model-Doc Updates Summary

**`example/backtest.md` explains the whole backtester layer in example/model.md's style, and its minimal example trains a 20-round XGBoostRegressor on a synthetic Polars factor, then runs a long_short TopN vectorbt backtest offline; the pasted stdout is from that run and was re-produced by extracting the block from the markdown. The model guide now documents `predict_panel` (with the MLP premise correction) and `cv_folds.json`, and the deleted in-model backtest hooks are annotated with the commits that actually removed them.**

## Performance

- **Duration:** 15 min
- **Started:** 2026-09-15T07:22:58Z
- **Completed:** 2026-09-15T07:37:59Z
- **Tasks:** 2
- **Files modified:** 3 (1 created, 2 modified; 917 insertions, 2 deletions)

## Accomplishments

- **`example/backtest.md`** (new), with all 18 planned sections. The class hierarchy table gives the exact abstract sets that `tests/test_backtest_contracts.py` locks. The `run()` table maps each D-02 step to its real method, and the `run_cv()` section covers the validation order, contiguity-before-load, the per-fold D-17 split, the single stitched simulation, the CV run-directory table, the `stitched` / `folds` / `notes` metrics shape, and the unshaded stitched report. Further sections cover:
  - the D-03 weights contract and why NaN on a rebalance row blocks the rebalance;
  - `US_EQUITY_MARKET` and the exact `from_orders` call, including valuation at the order price, fee/slippage semantics and `year_freq`;
  - the delisting rule, with a liquidation record as `to_jsonable` really renders it;
  - the selection rules table;
  - train vs load mode, DL checkpoint collection, the read cache and bar-counted warm-up;
  - the D-17 training window, the metrics keys and the turnover definition;
  - costs, including the explicit D-21 sentence and the literal `notes` text;
  - every run-directory file, the fingerprint layout, and the fact that `data_fingerprint` is not a `BacktestConfig` field;
  - `load_backtester_from_config`, with the D-27 warning and the train-mode one-second collision;
  - the `__main__` rebuild failure, with real output from a second process;
  - a real-data snippet labelled 此例未实际运行 (the local store has about 21 bars);
  - benchmark (D-08), 10 common pitfalls, and 8 deferred gaps.
- **The minimal example** constructs `DatasetConfig`, `PolarsFactorConfig`, `MLConfig` and `CrossSectionBacktestConfig` directly (D-32). It sets `OMP_NUM_THREADS=1` and `WANDB_MODE=disabled` before importing quantlab. The backtest window starts on the bar after `train_end`, so the output shows a real D-17 overlap: training window `2024-01-01..2024-05-24`, in-sample `2024-05-20..2024-05-24`. The first rebalance puts ±1/6 on six names. The stderr section is summarised, and the D-17 warning is quoted verbatim.
- **`example/model.md`:**
  - the header blockquote names `predict_panel`, `_predict_panel_array`, `cv_folds.json` and the two constants;
  - a new `## 面板预测：predict_panel` section covers the contract, the all-NaN mask and why it exists, a per-head adapter table, the MLP correction to D-33 with the real `mat1 and mat2` error, a labelled not-run usage snippet, and the locks;
  - a new `### cv_folds.json` subsection covers location, shape (marked as structure, not output), strict JSON with null metrics, zero folds, why the file is versioned, what `run_cv` rejects, and the golden accommodation;
  - dated notes were added to 吐：预测, to item 2/3/4 (at its top and beside the old lock-test list), and to items 5 and 8. The original text is kept; the diff deletes one line, the header line it extends.
- **`example/README.md`:** a 第四步：回测 table with the `backtest.md` row, and the 回测骨架 defect row struck through with a note on what was deleted, when, and the current lock.

## Task Commits

1. **Task 1: example/backtest.md with a runnable offline example** - `13c094f` (docs)
2. **Task 2: predict_panel and cv_folds.json in model.md; README index; D-37 annotations** - `ddcf23b` (docs)

**Plan metadata:** recorded in the docs commit that follows this SUMMARY.

## Files Created/Modified

- `example/backtest.md`: the backtester guide, with the runnable example and its real output
- `example/model.md`: header, `## 面板预测：predict_panel`, `### cv_folds.json`, and dated annotations
- `example/README.md`: fourth-step index table and the annotated skeleton row

## Example execution record

- **Command used for the pasted output.** Run from a scratchpad directory outside the repo: `OMP_NUM_THREADS=1 WANDB_MODE=disabled uv run --project /Users/daizhaorong/projects/quantlab python backtest_example.py` (exit 0; stdout and stderr captured to separate files). The stdout pasted under `### 真实输出` is that file, verbatim. The script prints only the run directory's name, never the temp path, so no substitution was needed; only the command line's repo path is abbreviated, and the doc says so.
- **Re-verification through the plan's own command.** The plan's awk extraction pulled the `## 最小可运行例子` python block into a fresh `mktemp -d` and ran it. It printed `example-ran`.
  - `cmp` showed the extracted block is byte-identical to the executed script.
  - `diff` of the two stdouts, excluding the run-dir line, was empty: Total Return 3.2102, Sharpe 1.0364 and turnover 1.2695 all reproduced.
  - Zero tracebacks appeared in stderr.
- **Rebuild probe.** A second script (`rebuild_probe.py`) read the run's `config.json` in a new process and called `load_backtester_from_config`. It printed the four lines quoted in the guide, ending in `AttributeError: module '__main__' has no attribute 'PastReturn'`.

## Decisions Made

See `key-decisions` in the frontmatter.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Accuracy] D-37 annotation distinguishes who deleted what**
- **Found during:** Task 2
- **Issue:** The plan asks for a note saying the model-layer backtest hooks "were deleted together with their tests" in phase 03.7. `git log -S` shows `_do_vecbt`, `BaseModel._vecbt` and their two locks (`test_do_vecbt_says_it_is_unbuilt_instead_of_returning_none`, `test_vecbt_stub_is_still_a_stub_and_names_phase_6`) were removed by the user's commit `d07f06e` on 2026-09-14, before 03.7. Attributing those removals to 03.7 would be false.
- **Fix:** The note and the README row name `d07f06e` for those two hooks, 03.7-05 for `RNNClassifier._vecbt`, and 03.7-07 for the rest. They also name the current locks, `test_stale_backtest_hooks_are_deleted` and `test_rnn_classifier_vecbt_is_deleted`.
- **Files modified:** example/model.md, example/README.md
- **Committed in:** `ddcf23b`

**2. [Rule 3 - Structure] `## 面板预测：predict_panel` placed after the parent section, not directly under `### 吐：预测`**
- **Found during:** Task 2
- **Issue:** A `##` heading inserted directly after `### 吐：预测` would have re-parented `### 张量形状` and `### 为什么不经 DataFrame` under the new section.
- **Fix:** The section goes after the whole 它吃什么、吐什么 section, and 吐：预测 gets a dated pointer to it. The required heading text is unchanged.
- **Files modified:** example/model.md
- **Committed in:** `ddcf23b`

**3. [Rule 2 - Completeness] Two extra dated notes beyond the plan (items 5 and 8)**
- **Found during:** Task 2
- **Issue:** Item 5 still said MLP inference has no path from a 3-D panel, and item 8 said nothing does "xarray to prediction" in one call. `predict_panel` now covers both, so a reader following those items would be misled.
- **Fix:** Added dated notes pointing to the new section. The original text was kept.
- **Files modified:** example/model.md
- **Committed in:** `ddcf23b`

---

**Total deviations:** 3 auto-fixed (1 accuracy, 1 structure, 1 completeness)
**Impact on plan:** All within the plan's three files. No code changed.

## Issues Encountered

- zsh leaked into verification twice without affecting any result:
  - an unquoted `--include=*.py` glob failed, and the grep was re-run quoted;
  - `${PIPESTATUS[0]}` printed empty after the gate, whose own summary line reported "no failures beyond the 56-test pre-phase baseline".
- The first verify command's wrapper exited 1 only because my extra `grep -c Traceback` found zero matches. `example-ran` had already printed.

## Verification Results

- Task 1 verify 1: `example-ran` (extracted block, fresh temp dir, offline).
- Task 1 verify 2: `doc-ok`.
- Task 2 verify 1: `model-docs-ok`.
- `git diff HEAD -- example/model.md` exited 0 and `grep -c '^-[^-]'` returned 1 (limit 5).
- `03.7-baseline-gate.sh`: 56 failed, 961 passed, 1 skipped, "no failures beyond the 56-test pre-phase baseline". The counts are identical to the wave-6 run, so the docs plan changed nothing.
- Key links: `example/README.md` contains `(backtest.md)`, and `example/backtest.md` contains `USEquityCrossectionSelectStockVectorBt(`.
- Spec-less probe fallback: skipped (no requirement IDs to probe). This was a deliberate, recorded choice.
- The user's uncommitted edits in `quantlab/enums/data.py`, `quantlab/factor/alpha158.py` and `quantlab/label/fret.py` were never edited, staged or committed. `git stash` was not used. Both commits staged files by explicit path.

## User Setup Required

None. No external service configuration is required.

## Next Phase Readiness

- Plan 03.7-12 can link `example/backtest.md` from `README.md` and `CLAUDE.md`, and delete `quantlab/vecbt/`. Nothing in these three docs imports or references `quantlab.vecbt`.
- The README's 想直接上手扩展 list was not extended: the backtest example is a usage example, not a minimal subclass.

---
*Phase: 03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse*
*Completed: 2026-09-15*

## Self-Check: PASSED

- Files: `example/backtest.md`, `example/model.md` and `example/README.md` exist and are committed; the working tree matches HEAD for all three.
- Commits: `13c094f` and `ddcf23b` appear in `git log`, and `git rev-list --count 39e7d1c..HEAD` = 2 before this docs commit.
