---
phase: 03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse
plan: 03
type: execute
wave: 2
depends_on: ["03.7-01"]
files_modified:
  - quantlab/backtest/selection.py
  - tests/test_backtest_selection.py
autonomous: true
requirements: [BT-01]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "On every rebalance row, symbols the selector does not pick hold exactly 0.0, and every non-rebalance row is all-NaN; the invariant holds on randomized panels (D-03, RESEARCH Pitfall 3)"
    - "long_only gives top_n equal weights summing to 1; long_short gives top_n longs summing to +0.5 and top_n shorts summing to -0.5 with no shared symbol (gross 1, net 0) (D-09, D-10)"
    - "A symbol with a NaN score or a NaN next-bar fill price is never selected; when fewer than top_n remain the book splits equally among what is available and a warning names the bar (D-12)"
    - "Equal scores resolve by symbol-axis order, so one panel always yields one set of weights (D-25 reproducibility)"
    - "Rebalance bars are the first window bar and every rebalance_periods bars after it, and never the window's last bar (D-18, RESEARCH Pitfall 13)"
    - "score_label defaults to the model's first label; an unknown label raises listing the known label names (D-11)"
  artifacts:
    - path: "quantlab/backtest/selection.py"
      provides: "hardened CrossSectionTopNSelector, rebalance_mask, resolve_score_label"
      contains: "class CrossSectionTopNSelector"
    - path: "tests/test_backtest_selection.py"
      provides: "book construction, eligibility, tie, schedule and invariant locks"
  key_links:
    - from: "tests/test_backtest_selection.py"
      to: "quantlab/backtest/selection.py"
      via: "direct calls to select / rebalance_mask / resolve_score_label on hand-built panels"
      pattern: "from quantlab\\.backtest\\.selection import"
---

<objective>
Harden the cross-sectional TopN selector the tracer introduced and lock every selection rule with pure tests on hand-built panels. The rules are the book construction (D-09, D-10), eligibility and short books (D-12), deterministic ties, the rebalance schedule (D-18), score-label resolution (D-11) and the D-03 weights contract, checked on randomized panels.

Purpose: target weights are the Phase 5 optimizer interface (D-03). Every rule here decides what the backtest actually trades, and none of it needs vectorbt to test.
Output: `quantlab/backtest/selection.py` (defects found by the tests fixed in place), `tests/test_backtest_selection.py`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-CONTEXT.md
@.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-RESEARCH.md
@.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-01-SUMMARY.md
@quantlab/backtest/selection.py

The 03.7-01 SUMMARY is referenced because this plan tests the selector interface that plan created (`select(scores, next_fill_price, rebalance)`).

Standing constraints for every task in this plan:
- The user has uncommitted edits in `quantlab/enums/data.py`, `quantlab/factor/alpha158.py`, `quantlab/label/fret.py`. Never edit them and never `git add` them; commit only this plan's files, by explicit path.
- Never use `git stash`.
- Scope fence: this plan owns ONLY `quantlab/backtest/selection.py` and `tests/test_backtest_selection.py`. Plans 03.7-04, 03.7-05, 03.7-06 and 03.7-14 run in the same wave on other files. Do not change the `select` / `rebalance_mask` / `resolve_score_label` signatures, because `quantlab/backtest/us_equity.py` (owned by 03.7-04 this wave) calls them.
- Capture loguru warnings with a list sink: `logger.add(sink_list.append, level="WARNING")`, removed in a finally block.
- A lock that passes on arrival is mutation-verified in a scratch copy and the original copied back; record each in the SUMMARY.
- The full-suite gate is `bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh`.
- Spec-less probe fallback: skipped (no requirement IDs to probe); recorded as a visible choice.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Book construction, eligibility, short books and ties are exact</name>
  <files>quantlab/backtest/selection.py, tests/test_backtest_selection.py</files>
  <read_first>
    - quantlab/backtest/selection.py (as the tracer wrote it)
    - quantlab/backtest/us_equity.py (how _generate_signals calls select, read only)
    - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-CONTEXT.md (D-03, D-09, D-10, D-12)
    - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-RESEARCH.md (Pattern 2; Pitfall 3; Assumption A6)
    - tests/test_metrics.py (pure-function test style on [T, S] numpy panels)
  </read_first>
  <behavior>
    - `test_long_only_top_n_equal_weights_sum_to_one`: 6 symbols, distinct scores, top_n=2. The two highest scores get 0.5 each, the other four get exactly 0.0, and the row sums to 1.0.
    - `test_long_short_books_are_half_each_and_disjoint`: top_n=2. The top two get +0.25, the bottom two get -0.25, the middle two get 0.0, the absolute sum is 1.0, the sum is 0.0, and no symbol is in both books.
    - `test_nan_score_is_never_selected`: the highest raw score is NaN, so the next finite scores are chosen.
    - `test_nan_next_fill_price_is_never_selected`: the top-scored symbol has NaN next_fill_price (delisted at t+1) and gets 0.0.
    - `test_short_long_only_book_splits_among_available_and_warns`: 1 eligible symbol with top_n=2 gets 1.0, and one WARNING names the bar's timestamp.
    - `test_long_short_with_fewer_than_twice_top_n_eligible_stays_disjoint`: 3 eligible with top_n=2 gives k=1 per book (+0.5 / -0.5), one symbol at 0.0, and a warning.
    - `test_no_eligible_symbol_gives_a_flat_row_not_nan`: every score NaN on a rebalance bar gives a row of all 0.0 (never NaN) and a warning.
    - `test_equal_scores_resolve_by_symbol_axis_order`: all scores equal, top_n=2, long_only picks the first two symbols on the axis; two calls give identical arrays.
    - `test_non_rebalance_rows_are_all_nan`: a 4-bar panel with mask [True, False, True, False] has rows 1 and 3 all NaN.
  </behavior>
  <action>
Write the nine tests first in `tests/test_backtest_selection.py`.
- The English module docstring states that this file locks D-03, D-09, D-10, D-12 and deterministic ties. It explains why each rule matters: a NaN on a rebalance bar means "hold" in vectorbt and silently blocks the rebalance (RESEARCH Pitfall 3), and overlapping long/short books would net a symbol to zero while double-counting gross.
- Build scores and next_fill_price as `xr.DataArray`s on ("timestamp", "symbol") by hand, with a business-day timestamp coord and six symbol names. No store, no model, no vectorbt.
- Call `CrossSectionTopNSelector(direction=..., top_n=...).select(scores, next_fill_price, rebalance)` directly.

Run the tests against the tracer's selector. Fix any defect they expose inside `quantlab/backtest/selection.py` without changing a public signature. The intended semantics, unchanged from the tracer:
- 0.0 for unselected symbols on rebalance rows.
- Eligible means finite score AND finite next_fill_price.
- Ranking uses a stable argsort on the negated score.
- long_only uses k = min(top_n, n_eligible) at 1/k.
- long_short uses k = min(top_n, n_eligible // 2) at +0.5/k and -0.5/k, from opposite ends of the eligible ranking.
- A warning fires whenever k < top_n; k == 0 gives an all-0.0 row.

Keep or add Chinese docstrings on `select` that state these rules and cite D-09/D-12.

Mutation-verify in a scratch copy:
- Write NaN instead of 0.0 for unselected symbols; `test_long_only...` or `test_no_eligible...` goes red.
- Drop the next_fill_price term from eligibility; `test_nan_next_fill_price...` goes red.
- Use a non-stable sort kind; the tie test goes red, or record that it stayed green and strengthen it with a permuted input.
Copy the original back after each.
  </action>
  <verify>
    <automated>uv run pytest tests/test_backtest_selection.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line, or fewer than 9 passed</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_backtest_selection.py -q` exits 0 with "9 passed".
    - `uv run pytest tests/test_backtest_run.py -q` still exits 0 (the tracer path is intact).
    - `grep -c 'kind="stable"' quantlab/backtest/selection.py` prints at least 1.
    - The SUMMARY records the three mutations and the observed red (or the strengthening made).
  </acceptance_criteria>
  <done>Every D-09/D-10/D-12 book rule and the deterministic tie rule is locked by a mutation-verified pure test, and the tracer run still passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rebalance schedule, score-label resolution and the D-03 invariant under random panels</name>
  <files>quantlab/backtest/selection.py, tests/test_backtest_selection.py</files>
  <read_first>
    - quantlab/backtest/selection.py (rebalance_mask, resolve_score_label, CrossSectionTopNSelector.__post_init__)
    - quantlab/base/config.py (CrossSectionBacktestConfig field set; read only)
    - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-CONTEXT.md (D-03, D-10, D-11, D-18)
    - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-RESEARCH.md (Pitfall 13)
  </read_first>
  <behavior>
    - `test_rebalance_mask_anchors_at_first_bar_and_steps_by_period`: n_bars=21, p=5 gives True exactly at indices 0, 5, 10, 15. Index 20 is a multiple of 5 but the last bar, so it is False.
    - `test_rebalance_mask_period_one_is_every_bar_but_the_last`: n_bars=4, p=1 gives [True, True, True, False].
    - `test_rebalance_mask_period_longer_than_window_rebalances_once`: n_bars=4, p=10 gives [True, False, False, False].
    - `test_rebalance_mask_rejects_non_positive_period`: p=0 and p=-1 raise ValueError.
    - `test_score_label_defaults_to_first_label`: resolve_score_label(None, ["ret_5", "ret_1"]) returns "ret_5" (declared order, not alphabetical).
    - `test_unknown_score_label_raises_listing_known_labels`: ValueError whose message contains both known names.
    - `test_selector_rejects_bad_parameters`: top_n=0 and direction="short_only" raise ValueError at construction.
    - `test_cross_section_config_has_no_quantile_mode`: no field of CrossSectionBacktestConfig contains "quantile", and `top_n` is a declared field (D-10).
    - `test_weights_contract_holds_on_random_panels`: 50 seeded panels (T=12, S=8, random NaN holes in scores and next fill, random p in 1..4, both directions, random top_n in 1..5). Every non-rebalance row is all-NaN; every rebalance row is all-finite with sum(|w|) <= 1 + 1e-12; long_short rows have net sum within 1e-12 of 0.0 whenever any weight is nonzero.
  </behavior>
  <action>
Add the nine tests to `tests/test_backtest_selection.py` below Task 1's. Write them first and run them against the current selector module. Fix defects inside `quantlab/backtest/selection.py` only, keeping signatures.
- `rebalance_mask` must set True at multiples of rebalance_periods from index 0 (D-18: the anchor is the first window bar), force the last index False (Pitfall 13), and raise ValueError for rebalance_periods < 1.
- `resolve_score_label` returns the first declared label when None (D-11), raises ValueError listing the known names for an unknown label, and raises ValueError for an empty label list.

The randomized invariant test uses `numpy.random.default_rng(seed)` per iteration so a failure message can print the seed, direction, top_n and p that broke it. It asserts the three D-03 properties directly.

Mutation-verify in a scratch copy:
- Remove the last-bar exclusion; the schedule test goes red.
- Return label_names[-1] as the default; the default-label test goes red.
Copy originals back. Then run the full-suite baseline gate.
  </action>
  <verify>
    <automated>uv run pytest tests/test_backtest_selection.py tests/test_backtest_run.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line, or fewer than 19 passed</fails_when>
    <automated>bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh</automated>
    <fails_when>non-zero exit, or output containing "NEW FAILURE(S)", "NEW ERROR(S)" or "NO 'passed' SUMMARY LINE"</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_backtest_selection.py -q` exits 0 with "18 passed".
    - `bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh` exits 0.
    - The scope fence held: for each commit sha this plan records in its SUMMARY, `git show --name-only --format= <sha>` lists only `quantlab/backtest/selection.py` and/or `tests/test_backtest_selection.py` (the SUMMARY pastes that output per sha).
    - The SUMMARY records the two mutations and their observed red.
  </acceptance_criteria>
  <done>The D-18 schedule, D-11 label resolution, D-10 fixed top_n and the D-03 invariant are locked. The randomized invariant covers both directions, and the full suite shows no failures beyond the baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| model predictions → selector | model output (possibly NaN or inf) decides what the backtest trades |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.7-10 | Tampering (result integrity) | `CrossSectionTopNSelector.select` | medium | mitigate | Non-finite scores and NaN next-bar fills are ineligible (D-12); unselected symbols get 0.0, not NaN (Pitfall 3); randomized D-03 invariant test |
| T-03.7-11 | Repudiation (irreproducible runs) | tie ordering | low | mitigate | Stable argsort by symbol-axis order, locked by `test_equal_scores_resolve_by_symbol_axis_order` |
</threat_model>

<verification>
- `uv run pytest tests/test_backtest_selection.py tests/test_backtest_run.py -q` exits 0.
- `bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh` exits 0.
- Spec-less probe fallback: skipped (no requirement IDs to probe). Recorded here as a visible choice.
</verification>

<success_criteria>
- D-03, D-09, D-10, D-11, D-12 and D-18 are each locked by a pure, mutation-verified test.
- Selection is deterministic under ties.
- No failures beyond the pre-phase baseline.
</success_criteria>

<output>
Create `.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Symbols created by this plan (no new production symbols; the selector module already exists from 03.7-01):

- `tests/test_backtest_selection.py`: `test_long_only_top_n_equal_weights_sum_to_one`, `test_long_short_books_are_half_each_and_disjoint`, `test_nan_score_is_never_selected`, `test_nan_next_fill_price_is_never_selected`, `test_short_long_only_book_splits_among_available_and_warns`, `test_long_short_with_fewer_than_twice_top_n_eligible_stays_disjoint`, `test_no_eligible_symbol_gives_a_flat_row_not_nan`, `test_equal_scores_resolve_by_symbol_axis_order`, `test_non_rebalance_rows_are_all_nan`, `test_rebalance_mask_anchors_at_first_bar_and_steps_by_period`, `test_rebalance_mask_period_one_is_every_bar_but_the_last`, `test_rebalance_mask_period_longer_than_window_rebalances_once`, `test_rebalance_mask_rejects_non_positive_period`, `test_score_label_defaults_to_first_label`, `test_unknown_score_label_raises_listing_known_labels`, `test_selector_rejects_bad_parameters`, `test_cross_section_config_has_no_quantile_mode`, `test_weights_contract_holds_on_random_panels`
