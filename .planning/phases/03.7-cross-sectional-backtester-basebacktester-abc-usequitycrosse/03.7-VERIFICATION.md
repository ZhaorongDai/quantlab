---
phase: 03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse
verified: 2026-09-15T14:25:00Z
status: human_needed
score: 8/8 goal truths verified (99/99 plan must-have truths regression-checked; 11/11 review-fix truths verified; 0 behavior-unverified)
covered_files:
  - .planning/REQUIREMENTS.md
  - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-01-PLAN.md
  - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-01-SUMMARY.md
  - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-02-PLAN.md
  - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-02-SUMMARY.md
  - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-03-PLAN.md
  - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-03-SUMMARY.md
  - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-04-PLAN.md
  - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-04-SUMMARY.md
  - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-05-PLAN.md
  - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-05-SUMMARY.md
  - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-06-PLAN.md
  - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-06-SUMMARY.md
  - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-07-PLAN.md
  - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-07-SUMMARY.md
  - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-08-PLAN.md
  - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-08-SUMMARY.md
  - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-09-PLAN.md
  - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-09-SUMMARY.md
  - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-10-PLAN.md
  - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-10-SUMMARY.md
  - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-11-PLAN.md
  - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-11-SUMMARY.md
  - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-12-PLAN.md
  - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-12-SUMMARY.md
  - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-13-PLAN.md
  - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-13-SUMMARY.md
  - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-14-PLAN.md
  - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-14-SUMMARY.md
  - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-CONTEXT.md
  - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-REVIEW-FIX.md
  - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-REVIEW.md
  - CLAUDE.md
  - example/backtest.md
  - example/model.md
  - quantlab/backtest/engine_vectorbt.py
  - quantlab/backtest/selection.py
  - quantlab/backtest/us_equity.py
  - quantlab/base/backtest.py
  - quantlab/base/config.py
  - quantlab/base/model.py
  - quantlab/utils/backtest_report.py
  - quantlab/utils/fingerprint.py
  - quantlab/utils/jsonable.py
  - quantlab/utils/module.py
  - tests/test_backtest_contracts.py
  - tests/test_backtest_dates.py
  - tests/test_backtest_engine.py
  - tests/test_backtest_metrics.py
  - tests/test_backtest_persistence.py
  - tests/test_backtest_rebuild.py
  - tests/test_backtest_run_cv.py
  - tests/test_backtest_selection.py
  - tests/test_jsonable.py
  - tests/test_ml_models.py
  - tests/test_model_cv_manifest.py
  - tests/test_model_predict_panel.py
covered_digest: "v1:sha256:69364037dbfd1f53c2545b8277209d5c7ea15cdabad2f30998ba26ae06dbfa74"
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 8/8 goal truths (99/99 plan truths)
  gaps_closed: []
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Open report.html from a plain run() in a browser (run example/backtest.md's minimal example, open <run_dir>/report.html)"
    expected: "Equity over drawdown on a shared time axis; drawdown matches equity.zarr; in-sample band 2024-05-20..2024-05-24 shaded grey; short-side optimism note legible; no benchmark trace. Known limitation still open: a one-bar in-sample overlap draws a zero-width band. The earlier 'intraday bars after midnight on the last in-sample day fall outside the band' limitation no longer applies, because CR-01 persists full ISO timestamps for intraday bar labels"
    why_human: "Visual layout; the page loads plotly.js from a CDN"
  - test: "Open report.html from a run_cv() run directory in a browser"
    expected: "One continuous stitched equity/drawdown curve with no capital reset at fold boundaries and no in-sample shading (disclosed limitation); the note pointing at metrics stitched.in_sample_ranges is legible; per-fold outputs under folds/fold_{i}/"
    why_human: "Visual layout"
  - test: "Read the architecture prose (CLAUDE.md backtester sections, .planning/codebase/{ARCHITECTURE,STACK,STRUCTURE}.md) and example/backtest.md + example/model.md including the eight dated 2026-09-15 code-review correction blocks"
    expected: "Accurate, readable description of BaseBacktester -> VectorBtBacktester -> USEquityCrossectionSelectStockVectorBt and load_backtester_from_config; vecbt mentions read as history; each struck-through superseded sentence is clearly paired with its correction"
    why_human: "Prose quality and readability"
  - test: "Decide whether CLEAN-02 stays [x] Complete in .planning/REQUIREMENTS.md"
    expected: "Keep Complete (both named examples resolved) or revert to partial (ROADMAP scopes 03.7 to 'CLEAN-02 (partial)'; root-level ad hoc scripts test.py, cal.py, jerry_query_data.py, test_nt.ipynb, train_model.py remain). BT-01 and CFG-01 are already [ ] / Partial, which matches the evidence"
    why_human: "Traceability judgment call; the verifier does not edit requirements"
  - test: "CR-02: accept the annualization convention for bars longer than one day"
    expected: "bars/year = min(252, 365.25 / bar_days). Verifier-measured values: 1D 252, 2D 182.625, 7D 52.179, 30D 12.175, 31D 11.782, 91D 4.014; intraday unchanged (1min 98280, 5min 19656). Confirm this calendar-span reading, or pick the review's trading-day conversion (weekly 50.4) or an explicit per-market bars_per_year override. Documented cost: a 'every N trading days' resampled series (mode interval 2-3D) is annualized ~40% high"
    why_human: "A metric-definition choice with no locked decision behind it (D-18 only says the backtester is frequency-agnostic); it changes Sharpe/Sortino/Calmar/annualized return/annualized turnover for every non-daily run"
  - test: "CR-01: accept the exact-bar in-sample boundary semantics"
    expected: "The training segment is located with the same pandas slice_indexer the model layer's sel(slice(train_start, train_end)) uses. A date-string train_end covers the whole session and a 'T13:00' train_end stops at 13:00. The horizon is counted in bars from the last training bar, so intraday horizon bars on the next session are now in-sample. Endpoints are written as a date for a midnight bar and full ISO otherwise. Daily output is unchanged: the verifier reproduced the guide example's ranges exactly. Fold contiguity and test-segment selection in run_cv remain day-based (IN-05, fail-closed)"
    why_human: "Changes which intraday bars count as out-of-sample. Consistent with D-17, but the endpoint-label format is part of the persisted metrics.json (D-25-style format), so the user should accept it"
  - test: "WR-01: accept the raise-vs-warn choices, and decide on the spurious run_cv warning"
    expected: "Accept or change:<br>- checkpoint factor/label variable names or order differ from config.model: raises ValueError before any feature work;<br>- load-mode run() where the checkpoint's recorded train dates differ from config.model: warns and uses the checkpoint's dates for D-17;<br>- run_cv: uses the manifest dates (D-16);<br>- no config.json beside the checkpoint: warns and continues.<br>ALSO DECIDE: every fold of a NORMAL run_cv currently logs a false warning, '... but config.model says train_start=..., train_end=...; using the checkpoint's dates for the effective training window'. run_cv does not use those dates; it uses the manifest's. Recommended fix: do not call _checkpoint_train_bounds' config.model comparison from run_cv, and compare dates as timestamps rather than str(). Verifier observed 8 such warnings per run_cv call across 3 tests"
    why_human: "Policy choice (raise vs warn) plus a misleading-log defect whose fix is a small follow-up; numbers are unaffected"
  - test: "WR-02: accept the DL-head symbol alignment policy"
    expected: "For DLModel heads, predict_panel raises ValueError when any training symbol is missing from the panel (including same-count, different-member panels). Symbols the model never saw are dropped with a warning, and after the backtester's reindex they are NaN and never selectable. The consequence: for DL heads, a ticker added to the store after training is never traded, even though D-06 says the universe is every symbol in the price dataset. ML heads (XGBoost) are unaffected. Verifier checked that a delisted or not-yet-listed symbol stays on the feature panel's symbol axis over later or earlier windows, so ordinary delistings do not trigger the raise"
    why_human: "Narrows the effective D-06 universe for DL heads; a positional DL net cannot score unseen symbols, so this is a product decision (accept, or retrain on the wider universe)"
  - test: "WR-05 / WR-06 / WR-07: accept the reproducibility strictness"
    expected: "Accept or change:<br>- WR-06: load_backtester_from_config refuses a config.json missing any config field. Consequence: adding any field to BacktestConfig/CrossSectionBacktestConfig later makes every stored run's config.json unrebuildable until migrated (D-25 already marks this format costly to change);<br>- WR-07: to_jsonable raises TypeError on unknown types instead of persisting str();<br>- WR-05: rebuilding a run persisted before this fix warns once that the new fingerprint keys are absent (read strategy: factor_store[i]; train mode: train_factor[i]/train_label[i] or *_store[i])"
    why_human: "Backward-compatibility policy for persisted, user-facing formats; the fail-loud direction is consistent with D-25/D-27, but it is a trade-off the user owns"
---

# Phase 03.7: Cross-sectional backtester Verification Report

**Phase Goal:** Users can turn a trained return model into a reproducible cross-sectional US-equity backtest. `run()` handles a model backtest and `run_cv()` a model-CV backtest over each fold's out-of-sample segment. The engine is vectorbt, with t+1 open fills and long-only/long-short TopN target weights. Every run reports in/out-of-sample results, persists its artifacts and data fingerprints, and rebuilds from its own config. The legacy `vecbt/bt.py` is retired.
**Verified:** 2026-09-15T14:25:00Z
**Status:** human_needed
**Re-verification:** Yes. This run follows code-review fixes 8780ee2..7c8f737 (CR-01, CR-02, WR-01..WR-09) and report commit 13259be.

## Previous verification

**Previous result** (2026-09-15T04:10Z): `human_needed`, 8/8 goal truths, 99/99 plan truths, no gaps. It raised four human items (REQUIREMENTS checkbox decision, two report.html visual checks, architecture prose) and carried these warnings:
- (a) no test caught a dropped `init_cash` (mutation M4 stayed green);
- (b) stitched run_cv report has no in-sample shading;
- (c) single-bar band has zero width, and intraday in-sample tails fell outside the band;
- (d) intraday run_cv folds are judged by day;
- (e) BT-01/CFG-01 were over-claimed as Complete.

**What changed since then:**
- **(e):** the orchestrator reverted BT-01/CFG-01 to Partial. REQUIREMENTS.md now shows `[ ]` and "Partial (03.7: …)" for both, which matches the evidence.
- **(a):** now closed (M4 is red; see below).
- **(c):** half closed. The intraday tail is now covered with exact bar labels, but the single-bar zero-width band remains.
- **(b) and (d):** unchanged (IN-04 and IN-05, out of the fix scope).

## Evidence base (the verifier's own runs in this session)

1. **Phase gate re-run on main.** `bash 03.7-baseline-gate.sh` gave `56 failed, 1001 passed, 1 skipped, 942 warnings in 113.28s` and `03.7 baseline gate: no failures beyond the 56-test pre-phase baseline`, exit 0. This matches the orchestrator's run. The previous verification had 962 passed; the extra passes are the fix regression tests.

2. **In-memory mutation sampling at HEAD** (`scratchpad/verifier_mutate2.py`). Each mutation patches already-imported objects in a fresh process and runs only that fix's regression tests. No repo file was edited and no stash was used. CR-01 swaps in the pre-fix methods compiled from `git show 8780ee2^:…`.

   | Mutation | What it undoes | Target tests | Result |
   |---|---|---|---|
   | CR01 | parent `_training_window`, `_split_window`, `_in_ranges`, `_period_record_stats`, engine `_period_returns_stats` | 3 CR-01 tests (6 cases) | **RED**, 6 failed |
   | CR02 | old `year_freq` (252 × day/interval) | `test_us_equity_year_freq_multi_day_bars_are_calendar_spans` | **RED** |
   | WR01 | variable check no-op; dates taken from config.model | refuse-other-variables and use-checkpoint-dates tests | **RED**, 3 failed |
   | WR02 | DL `_align_prediction_symbols` becomes the base no-op | refuse-missing-symbol and align-onto-training-symbols tests | **RED**, 3 failed |
   | WR03 | `_resolve_fold_checkpoint` returns the recorded path | resolves-inside-project-dir test | **RED**, 2 failed |
   | WR05 | `_record_training_fingerprints` no-op | train-mode fingerprint test | **RED** |
   | WR06 | completeness check disabled (`if missing:` becomes `if False:`) | refuse-missing-field [4 cases] plus non-default round-trip | **RED**, 4 failed; the round-trip lock passes, as designed |
   | M4 (previous warning a) | `BacktestConfig.to_dict` drops `init_cash` | all of test_backtest_rebuild.py | **RED**, 10 failed (was GREEN before the fixes) |
   | WR08 | old non-staged persist (mkdir final, write in place) | interrupted-persist tests for run and run_cv | **RED**, 2 failed |
   | WR09 | `_align_to_scores` becomes identity (positional pairing) | label-not-position and other-labels-refused tests | **RED**, 2 failed |

3. **Independent long_short probe re-run** (`scratchpad/verifier_probe2.py`). The scenario is the same as the earlier `verifier_probe.py`, run against the fixed code:
   - **Weights:** rebalance rows are 0,3,…,30 of 34, and non-rebalance rows are all-NaN. Every rebalance row has books of +0.5/−0.5. CCC holds 0.0 on every rebalance row after it delists. **Unchanged.**
   - **Fills and fees:** the first order is on window bar 1 at adjOpen[t+1], and fees equal |size|×price×0.001. **Unchanged.**
   - **In-sample split:** `training_window` ('2024-01-01', '2024-02-05'), `in_sample_range` ('2024-01-31', '2024-02-05'), out-of-sample from 2024-02-06, and the overlap warning is emitted. **Identical to the previous probe**, so CR-01 did not move daily numbers.
   - **Fingerprint:** keys `price_dataset` (adjClose, adjOpen) and `factor[0]`. The factor start is 2024-01-23, before the bar-19 warm-up start. **Unchanged.** There is no `factor_store` key, correctly, because this is a cal strategy.
   - **Rebuild:** `load_backtester_from_config(config.json).run()` gives identical weights and equity, with no fingerprint or WR warnings. **Unchanged.**
   - **What differs, all as the fixes intend:**
     - the checkpoint's config.json now carries `trained_on`;
     - `checkpoint`/`output_dir` in config.json are absolute;
     - `output_dir` holds only the final run dir, with no `.partial`;
     - load-mode metrics have no `trained_checkpoint`;
     - removing `init_cash` from the JSON is now **refused** with `ValueError … missing field(s) ['init_cash']`, where it previously defaulted silently to 1,000,000;
     - a model whose config dates are stale (train_end bar 10) warns WR-01 and still gets `training_window` ('2024-01-01', '2024-02-05') and identical weights, because the checkpoint's recorded dates are used;
     - bars/year: 1D 252, 2D 182.625, 7D 52.179, 30D 12.175, 31D 11.782, 91D 4.014, 1min 98280, 5min 19656.

4. **Guide example re-run.** The "最小可运行例子" Python block (example/backtest.md:518-679) is byte-identical to the previous extraction. Run with `OMP_NUM_THREADS=1 WANDB_MODE=disabled uv run --project <repo>`, its **stdout matches the pasted "真实输出" exactly except the run-dir name**:
   - training_window ('2024-01-01', '2024-05-24'), in-sample ('2024-05-20', '2024-05-24'), OOS [('2024-05-27', '2024-08-09')];
   - Total Return 3.2102, Sharpe 1.0364, turnover 1.2695, OOS −0.0442;
   - 105 orders, identical first-rebalance weights, identical notes.

   Stderr is 13 lines (3 ZarrUserWarnings, 6 INFO, one D-17 WARNING), the same as documented. No WR-01/WR-02 warning appeared.

5. **Warning capture during normal run_cv** (`scratchpad/verifier_cv_warnings.py`). Three standard run_cv tests produced 24 WR-01 warnings, 8 per run_cv call, all of the same form: `checkpoint <path> was trained on 2024-01-09T00:00:00.000000000..2024-02-19T00:00:00.000000000 (its config.json), but config.model says train_start='2024-01-01', train_end='2024-02-09'; using the checkpoint's dates for the effective training window (D-17, WR-01)`.
   - **Cause:** `run_cv` calls `_checkpoint_train_bounds` (base/backtest.py:432), which compares against `config.model` and logs the claim.
   - **What actually happens:** run_cv passes `fold["_train_bounds"]`, the manifest's dates (line 448). The warning's claim is false.
   - **Manifest-vs-checkpoint warning:** did not fire, because the formats match.
   - **Impact:** a misleading log line only; numbers are unaffected. See Warnings.

6. **Symbol-axis check for WR-02** (`scratchpad/verifier_symaxis.py`). The feature panel, via the Polars fixture factor, keeps all 6 store symbols over a window after CCC delists (bars 30..45) and before FFF lists (bars 0..15). Ordinary listings and delistings therefore do not trigger the new "missing training symbol" raise.

## Goal Achievement

### Observable Truths (roadmap goal, decomposed)

| # | Truth | Status | Evidence (re-verification) |
|---|-------|--------|----------------------------|
| G1 | `run()` turns a trained/loaded return model into a cross-sectional US-equity backtest | ✓ VERIFIED | Template `run()` at base/backtest.py:338. `_prepare_model` now returns the D-17 bounds: checkpoint dates in load mode, `model.config` dates in train mode (D-13 unchanged). `_load_model_checkpoint` checks variables before any feature work. Probe, rebuild and guide example all run. `tests/test_backtest_run.py` is green in the gate |
| G2 | `run_cv()` backtests each fold's out-of-sample segment from `cv_folds.json` and produces a stitched curve | ✓ VERIFIED | `run_cv` at :377:<br>- each fold's checkpoint is resolved inside `cv_project_dir` (`_resolve_fold_checkpoint` :607, WR-03, mutation red);<br>- per-fold D-17 uses the raw manifest bounds (`_train_bounds`);<br>- contiguity is still asserted before simulation;<br>- one stitched simulation.<br>The run_cv suite is green in the gate. **Warning:** a spurious per-fold WR-01 log line (evidence 5) |
| G3 | The engine is vectorbt with t+1 open fills | ✓ VERIFIED | engine_vectorbt.py `from_orders(..., size=w.shift(1))` is unchanged by the fixes. The only engine change is exact-Timestamp return slicing (CR-01). Probe: first fill on bar t+1 at adjOpen. Contracts test unchanged since d083dac (plan 12) and green; vectorbt is imported only in engine_vectorbt.py |
| G4 | Long-only / long-short TopN target weights (D-03 contract) | ✓ VERIFIED | selection.py `_align_to_scores` (:69, WR-09) makes fill-price pairing label-based and raises on label-set differences. The D-03 output contract is unchanged. Probe books are exact. WR09 mutation is red |
| G5 | Every run reports in-sample / out-of-sample results | ✓ VERIFIED | `_training_window` (:1233) uses the model layer's own `slice_indexer`. `_split_window` (:1286), `_in_ranges`, the open-trade count and the engine slice all compare exact bar timestamps (CR-01, mutation red, 6 cases). Daily ranges are identical in the probe and example. CR-02 annualization changes only intervals above 1D (mutation red). **Semantics routed to human (items 5, 6)** |
| G6 | Every run persists its artifacts and data fingerprints | ✓ VERIFIED | `_persist_run_dir` (:1627, WR-08) stages to `.{name}.partial` and renames on completion; mutation red. All 7 D-24 artifacts are present and no `.partial` remains (probe). Fingerprints now also cover the factor store (read strategy) and training data (train mode) (`_record_factor_fingerprints` :1037, `_record_training_fingerprints` :1061, WR-05, mutation red). `to_jsonable` raises instead of stringifying (jsonable.py:121, WR-07; 7 tests in the new tests/test_jsonable.py) |
| G7 | Every run rebuilds from its own config | ✓ VERIFIED | `load_backtester_from_config` (utils/module.py):<br>- pops the `data_fingerprint` and `trained_checkpoint` records;<br>- refuses a config missing any field (:166, WR-06);<br>- config path fields are absolute (WR-03).<br>Probe rebuild is identical. Previous warning (a) is closed: M4 is now red (10 failed). Train mode records `trained_checkpoint` for exact replay (WR-04), and `train()` returns an absolute Path; model project names are microsecond-precise with a `_n` suffix |
| G8 | Legacy `vecbt/bt.py` is retired | ✓ VERIFIED | `quantlab/vecbt` is still absent. No fix touched retirement; `test_vecbt_package_is_retired` is green in the gate |

**Score:** 8/8 goal truths verified (0 present, behavior-unverified).

### Review-fix truths (added by this re-verification)

| Fix | Fix real in code | Regression test goes red without it | Consistent with CONTEXT.md | Status |
|-----|------------------|-------------------------------------|-----------------------------|--------|
| CR-01 exact bar-time in-sample split | yes (backtest.py:1233-1330, engine slice) | yes (mutation CR01) | D-17 (bars counted, single simulation sliced) holds; daily output unchanged; run_cv test segments/contiguity still day-based (IN-05, fail-closed) | ✓ VERIFIED; semantics → human item 6 |
| CR-02 multi-day annualization | yes (`year_freq` :43, `CALENDAR_DAYS_PER_YEAR`) | yes | No locked decision on the >1D convention; D-18 (frequency-agnostic) favours not raising | ✓ VERIFIED; convention → human item 5 |
| WR-01 checkpoint record check | yes (:784, :874) | yes | D-13 (train mode uses the model's dates) unchanged. Load mode uses the checkpoint's recorded dates, which fits D-17's intent. run_cv keeps the manifest dates (D-16). **Spurious run_cv warning found** | ✓ VERIFIED with ⚠️ Warning; policy → human item 7 |
| WR-02 DL symbol alignment | yes (model.py:1007, `trained_on` record, `_read_checkpoint` sizes from the record) | yes | Tension with D-06 for DL heads only: unseen symbols are unscoreable, and NaN means ineligible per D-12. No locked decision is contradicted | ✓ VERIFIED; policy → human item 8 |
| WR-03 absolute / project-relative paths | yes (setter; `_resolve_fold_checkpoint`; `_train_one_fold` `.absolute()`) | yes | D-36 v1 manifest format unchanged (paths only become absolute) | ✓ VERIFIED |
| WR-04 trained_checkpoint + collision-free names | yes (model.py:430, :447; backtest `_trained_checkpoint`) | not sampled (the fixer reports 2/2 red); both tests green in the gate | D-24 artifact set unchanged; recorded as a record, not a config field (so WR-06 does not break) | ✓ VERIFIED |
| WR-05 fingerprint coverage | yes | yes | D-27 warn-and-continue kept; old runs warn once | ✓ VERIFIED; policy → human item 9 |
| WR-06 missing-field refusal | yes (module.py:166) | yes; M4 also red now | D-25 "all parameters identical" supports raising; adding future fields needs a migration | ✓ VERIFIED; policy → human item 9 |
| WR-07 lossless-or-loud JSON | yes (jsonable.py) | not sampled (the fixer reports 6/7 red); tests green in the gate | D-36 format unchanged. The only persisted callers are backtest artifacts and the CV manifest. `DLModel._fit` returns None, so DL folds carry no metrics; ML metrics pass in the CV tests | ✓ VERIFIED |
| WR-08 staged run dir | yes (:1627); wandb logs after the rename against the final dir | yes | D-24 `{class}_{timestamp}` naming unchanged | ✓ VERIFIED |
| WR-09 label alignment in selector | yes (selection.py:69) | yes | D-03/D-12 unchanged | ✓ VERIFIED |

**Contract locks:** `git diff 8780ee2^..7c8f737 -- tests/test_backtest_contracts.py` is empty; the file was last changed in d083dac (plan 12). It passes in the gate. `tests/test_model_hierarchy.py` passes too (`_align_prediction_symbols` is non-abstract, so the abstract sets are unchanged). The only deliberately updated existing locks are `test_backtest_rebuild.py` (`time.sleep` removed) and `test_model_without_train_dates_warns_and_records_null` (deletes the checkpoint record first). Both follow directly from WR-04/WR-01, and neither loosens a behavioural assertion. No locked decision D-01..D-37 is contradicted.

### Plan must-have truths (99, regression check)

All 14 plans' truths remain supported. Each of their test files is green inside the single gate run; the probe and the guide example reproduce the previous numbers exactly. No regression was found.

### Required Artifacts

| Artifact | Status | Details |
|----------|--------|---------|
| `quantlab/base/backtest.py` | ✓ VERIFIED | The fixes add private helpers only (`_bar_label`, `_label_ns`, `_slice_bound`, `_checkpoint_train_bounds`, `_read_checkpoint_config`, `_assert_checkpoint_variables`, `_resolve_fold_checkpoint`, `_record_training_fingerprints`, `_persist_run_dir`). The public surface is still `run`, `run_cv` and `get_config` (contracts test) |
| `quantlab/base/model.py` | ✓ VERIFIED | `TRAINED_ON_KEY`, `_read_trained_symbols`, `_align_prediction_symbols` (base no-op, DL override), `_new_project_name`; `train() -> Path`; absolute fold checkpoints |
| `quantlab/backtest/selection.py` | ✓ VERIFIED | `_align_to_scores` |
| `quantlab/backtest/engine_vectorbt.py` | ✓ VERIFIED | exact-Timestamp return slicing |
| `quantlab/utils/module.py` | ✓ VERIFIED | missing-field refusal; `trained_on` / `trained_checkpoint` records popped |
| `quantlab/utils/jsonable.py` | ✓ VERIFIED | arrays/Series/Index/sets → lists; key-collision ValueError; unknown-type TypeError |
| `tests/test_jsonable.py` (new) | ✓ VERIFIED | 7 tests, green in the gate |
| `example/backtest.md`, `example/model.md` | ✓ VERIFIED | Superseded sentences are struck through and paired with dated corrections (D-18 convention); the runnable example still reproduces its pasted output |

### Key Link Verification

| From | To | Via | Status |
|------|----|-----|--------|
| base/backtest.py `run` | checkpoint `config.json` | `_prepare_model` → `_load_model_checkpoint` → `_read_checkpoint_config` / `_checkpoint_train_bounds` → `_backtest_window(*train_bounds)` | WIRED (probe stale-dates case) |
| base/backtest.py `run_cv` | manifest raw bounds | `fold["_train_bounds"]` → `_backtest_window` | WIRED |
| base/backtest.py `run_cv` | `cv_project_dir` layout | `_resolve_fold_checkpoint` | WIRED (mutation WR03 red) |
| base/model.py `_save_model` | `DLModel.predict_panel` | `trained_on.symbols` → `load` → `_trained_symbols` → `_align_prediction_symbols` | WIRED (mutation WR02 red) |
| base/backtest.py `_prepare_model` (train) | fingerprint.json | `_record_training_fingerprints` before `model.train()` | WIRED (mutation WR05 red) |
| base/backtest.py `_prepare_model` (train) | config.json / metrics.json | `model.train()` return → `_trained_checkpoint` → `get_config` / metrics | WIRED (gate test) |
| `_report_and_persist` / `_persist_cv` | output_dir | `_persist_run_dir` staging + rename; `_log_to_wandb` runs after and reads the final dir | WIRED |
| utils/module.py loader | `fields(config_cls)` | completeness check before building nested objects | WIRED (mutation WR06 red) |

### Data-Flow Trace (Level 4)

| Artifact | Data | Source | Real data | Status |
|----------|------|--------|-----------|--------|
| metrics.json ranges | `training_window` / `in_sample_range` / `out_of_sample_ranges` | calendar `slice_indexer` over the price store + checkpoint/manifest train bounds | Yes (probe and example values reproduced) | ✓ FLOWING |
| metrics.json stats | Sharpe etc. | `pf.stats()` / sliced `pf.returns()` with `year_freq` | Yes (example Sharpe 1.0364 reproduced) | ✓ FLOWING |
| fingerprint.json | digests incl. new keys | price, factor dataset, factor store, training data | Yes (WR-05 tests edit stored values and see the warning) | ✓ FLOWING |
| checkpoint config.json `trained_on` | symbols | training panel `self.symbols` | Yes (probe: present) | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full-suite gate | `bash 03.7-baseline-gate.sh` | 56 failed / 1001 passed / 1 skipped; no failures beyond baseline; exit 0 | ✓ PASS |
| Fix locks catch reverts | `uv run python scratchpad/verifier_mutate2.py {CR01,CR02,WR01,WR02,WR03,WR05,WR06,M4,WR08,WR09}` | all 10 RED | ✓ PASS |
| Independent long_short probe + rebuild | `PYTHONPATH=<repo> uv run python scratchpad/verifier_probe2.py` | all prior values identical; rebuild identical; init_cash removal refused | ✓ PASS |
| Guide example | extracted block run outside the repo | stdout identical to pasted output (run-dir name excepted) | ✓ PASS |
| run_cv log cleanliness | `uv run python scratchpad/verifier_cv_warnings.py` | 24 false WR-01 "using the checkpoint's dates" warnings across 3 normal run_cv calls | ⚠️ WARNING |

### Probe Execution

Step 7c: SKIPPED. The phase declares no `scripts/*/tests/probe-*.sh`; the phase gate script was executed instead (above).

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| BT-01 (vectorbt part) | 01-11, 13, 14 | vectorbt backtest: target holdings in, equity curve + key metrics out | ✓ SATISFIED for this phase's scope | REQUIREMENTS.md now `[ ]` and "Partial (03.7: vectorbt backtester; target-holdings entry + end-to-end pipeline still Phase 6)". That matches the evidence: there is no public entry point for externally supplied holdings, and Phase 6 SC1 is still open. The fixes strengthen the vectorbt part (CR-02 metrics, WR-09 selector) |
| CFG-01 (partial) | 02, 09, 11, 13 | stages config-driven, same config reproduces the same experiment | ✓ SATISFIED for backtest scope | REQUIREMENTS.md `[ ]` / Partial, matching. The fixes close reproducibility holes: WR-06 missing field, WR-03 cwd paths, WR-04 exact checkpoint replay, WR-05 fingerprint coverage, WR-08 half-written runs. Phase 6 SC2 (single end-to-end config) remains open |
| CLEAN-02 (partial) | 05, 07, 12, 13 | delete test code, temp scripts, redundant implementations | ✓ SATISFIED for the named examples; checkbox `[x]` pending the user (human item 4) | Unchanged by the fixes |

No orphaned requirements: REQUIREMENTS.md maps no other ID to 03.7, and every plan's `requirements` lists only these three.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| all fix-modified source/test/doc files | - | TBD/FIXME/XXX | none found | Debt-marker gate clean |
| quantlab/base/backtest.py | 432 (calls :784) | `run_cv` calls `_checkpoint_train_bounds`, which compares a fold checkpoint's dates to `config.model` and logs "using the checkpoint's dates" although run_cv uses the manifest's | ⚠️ Warning | Misleading log on every fold of every normal run_cv; numbers correct. No test locks warning absence |
| quantlab/base/backtest.py | 784-806 | Date comparison is `str()` equality; a train_cv fold checkpoint records `2024-01-01T00:00:00.000000000` while a hand-built `config.model` usually says `2024-01-01` | ⚠️ Warning (inferred from code and observed formats) | Load-mode `run()` on a CV fold checkpoint with semantically equal dates warns that they differ. Same root cause and same fix as the row above: compare as timestamps |
| quantlab/base/backtest.py | 1627-1661 | A SIGKILL mid-persist leaves a hidden `.{name}.partial` directory (BaseException cleanup cannot run) | ℹ️ Info | Hidden, never confused with a completed run; names are microsecond-unique |

### Human Verification Required

Items 1-4 carry forward from 03.7-UAT.md; item 1's expected text is updated for CR-01. Items 5-9 come from choices the fixes introduced. The full test / expected / why_human text for each is in the frontmatter `human_verification` list, which the orchestrator can copy into 03.7-UAT.md.

1. **Plain run() report.html in a browser.**
   - *Expect:* equity over drawdown, grey band 2024-05-20..2024-05-24, short-side note.
   - *Limitation still open:* a one-bar overlap draws a zero-width band. The intraday-tail limitation is gone.
2. **Stitched run_cv() report.html in a browser.**
   - *Expect:* one continuous curve with no shading (disclosed).
3. **Architecture prose.**
   - *Expect:* CLAUDE.md, the codebase maps and example/backtest.md + model.md, including the new dated correction blocks, read accurately.
4. **CLEAN-02 checkbox.**
   - *Decide:* keep `[x]` or revert to partial.
   - *Context:* BT-01/CFG-01 are already Partial.
5. **CR-02 annualization convention.**
   - *Rule:* `min(252, 365.25/days)`, which gives weekly 52.18, 31D 11.78 and 2D 182.6.
   - *Decide:* accept, or choose a trading-day conversion or a per-market override.
6. **CR-01 intraday in-sample semantics.**
   - *Behaviour:* exact model-layer slice, horizon in bars, bar-label endpoints in metrics.json; daily unchanged.
   - *Decide:* accept.
7. **WR-01 policy, plus the spurious run_cv warning.**
   - *Policy:* variable mismatch raises; date mismatch warns and uses the checkpoint's dates; missing config.json warns.
   - *Decide:* whether to fix the false per-fold warning. The recommended fix is small: skip the config.model comparison inside run_cv and compare dates as timestamps.
8. **WR-02 DL-head symbol policy.**
   - *Behaviour:* a missing training symbol raises; an unseen symbol is dropped and never traded.
   - *Decide:* accept for DL heads, knowing it narrows the D-06 universe.
9. **WR-05/06/07 strictness.**
   - *Behaviour:* a missing config field refuses to rebuild, so future config fields need a migration. Unknown JSON types raise. Old runs warn once about new fingerprint keys.
   - *Decide:* accept.

### Gaps Summary

No blocking gaps. All 11 critical and warning review fixes are real in code, and each sampled regression test goes red when its fix is reverted in memory (9 of 11 sampled, plus M4). No contract lock or locked decision was loosened.

Behaviour against the goal holds:
- The independent probe and the guide example reproduce the previous daily numbers exactly.
- Rebuild is still bit-identical.
- The earlier rebuild blind spot (`init_cash`) is now closed.

The status stays `human_needed`:
- 4 carried UAT items;
- 5 new decisions on semantics or policy introduced by CR-01, CR-02, WR-01, WR-02 and WR-05/06/07.

**Warnings to carry forward** (no gap plan required):
1. **Spurious WR-01 warning.** A false per-fold warning in normal `run_cv`, and format-sensitive `str()` date comparison in `_checkpoint_train_bounds`. Misleading logs only; a quick fix is suggested in human item 7.
2. **Report shading.** The stitched run_cv report has no in-sample shading, and a single-bar band has zero width (IN-04, unchanged).
3. **Intraday run_cv folds.** Fold contiguity and test-segment selection are day-based (IN-05, fail-closed, unchanged).

---

_Verified: 2026-09-15T14:25:00Z_
_Verifier: Claude (gsd-verifier)_
