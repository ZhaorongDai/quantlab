---
phase: 03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse
plan: 14
type: execute
wave: 2
depends_on: ["03.7-01"]
files_modified:
  - tests/test_backtest_contracts.py
coupling_justified:
  - "03.7-03: 03 changes only selection.py behaviour and adds no import and no public member; these locks read that file and hold in either merge order"
  - "03.7-04: 04 adds only private engine members and does not run this test file (it checks the D-04 rule with an inline AST command instead); these locks hold in either merge order"
  - "03.7-05: 05 adds head adapters under quantlab/dl_model and imports nothing from the backtest layer; the layering scan holds in either merge order"
  - "03.7-06: 06 adds only private BaseBacktester helpers (_iso_date, _refresh_factor_reads) and no abstract or public member; the exact sets hold in either merge order"
autonomous: true
requirements: [BT-01]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The engine-by-inheritance hierarchy is locked by exact abstract-method sets: BaseBacktester {config_cls, _generate_signals, _simulate, _simulate_benchmark, _engine_stats}, VectorBtBacktester {config_cls, _generate_signals}, USEquityCrossectionSelectStockVectorBt empty (D-01)"
    - "No class above BaseBacktester in the concrete MRO defines run or any public callable of its own, so run() stays the single template (D-02)"
    - "A backtester given the wrong config class raises TypeError naming CrossSectionBacktestConfig before any dataset or model attribute is read (D-01)"
    - "No string constant inside any function body of quantlab/base/backtest.py or quantlab/backtest/*.py equals a MARKET price column name; the module-level spec is their only home (D-04)"
    - "No import in the backtest layer, tests/backtest_fixtures.py or tests/test_backtest_*.py resolves to quantlab.config, relative spellings included (D-32)"
    - "No quantlab module outside quantlab/backtest/, quantlab/base/backtest.py and quantlab/utils/module.py imports the backtest layer, and only quantlab/backtest/engine_vectorbt.py imports vectorbt, with a quantlab/vecbt allowance that names plan 03.7-12 (layering, D-31)"
    - "Every lock is mutation-verified: a planted defect turns it red, and the SUMMARY records the red run"
  artifacts:
    - path: "tests/test_backtest_contracts.py"
      provides: "hierarchy, public-surface, type-check-first, D-04 literal, D-32 and layering locks"
      contains: "def _resolved_imports"
  key_links:
    - from: "tests/test_backtest_contracts.py"
      to: "quantlab/base/backtest.py"
      via: "exact __abstractmethods__ sets and the MRO __dict__ scan"
      pattern: "__abstractmethods__"
    - from: "tests/test_backtest_contracts.py"
      to: "quantlab/backtest/us_equity.py"
      via: "function-body AST walk against US_EQUITY_MARKET's column names"
      pattern: "US_EQUITY_MARKET"
---

<objective>
Lock the architecture the 03.7-01 tracer built, so later plans cannot erode it silently:
- the engine-by-inheritance hierarchy (D-01);
- run() as the single template (D-02);
- price column names only in the market spec (D-04);
- no config-factory imports (D-32);
- one-directional layering, with vectorbt confined to the engine module.

Purpose: `tests/test_model_hierarchy.py` keeps the model layer's shape honest, and this file does the same for the backtest layer. These locks were Task 2 of 03.7-01. They became their own plan because the combined estimate (110000 tokens) exceeded the 100000-token smart-zone budget: `estimate-check` gave 1.10x, with calibration factor 1 and 0 samples.
Output: `tests/test_backtest_contracts.py` with seven mutation-verified contract tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-CONTEXT.md
@.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-VALIDATION.md
@.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-01-SUMMARY.md
@quantlab/base/backtest.py
@quantlab/backtest/engine_vectorbt.py
@quantlab/backtest/selection.py
@quantlab/backtest/us_equity.py
@tests/test_model_hierarchy.py

The 03.7-01 SUMMARY is referenced because every lock here inspects classes and modules that plan created.

Standing constraints for every task in this plan:
- The user has uncommitted edits in `quantlab/enums/data.py`, `quantlab/factor/alpha158.py`, `quantlab/label/fret.py`. Never edit them and never `git add` them; commit only this plan's file, by explicit path.
- Never use `git stash`. For a before/after comparison use `git show HEAD:<file>` or a scratch copy.
- Scope fence: this plan owns ONLY `tests/test_backtest_contracts.py`. The other wave-2 plans own the source files: `quantlab/backtest/selection.py` belongs to 03.7-03, `quantlab/backtest/engine_vectorbt.py` and `quantlab/backtest/us_equity.py` to 03.7-04, and `quantlab/base/backtest.py` to 03.7-06. Read them and never edit them.
- A lock that is red on arrival means the defect is in 03.7-01 code. Record the lock and the offending file and line in the SUMMARY, and stop instead of editing a file another plan owns.
- The locks must hold in either merge order with the other wave-2 plans. Those plans add only private members and import nothing from the backtest layer, as the frontmatter's `coupling_justified` entries record. So assert architectural invariants (abstract sets, public surface, imports, literals), never the presence or absence of a non-abstract private helper.
- A lock that passes on arrival is mutation-verified. Copy the source file aside, introduce the specific defect, run the one test and see it red, then copy the original back. Record each mutation in the SUMMARY.
- The full-suite gate is `bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh`: no failures beyond the 56-test pre-phase baseline. It is never "exit 0 of pytest".
- Spec-less probe fallback: skipped (no requirement IDs to probe); recorded as a visible choice.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the class surface — exact abstract sets, run() only on the base, type check first</name>
  <files>tests/test_backtest_contracts.py</files>
  <read_first>
    - tests/test_model_hierarchy.py (the model-layer contract style: exact __abstractmethods__ sets, "no class above the base redefines a public method", deletion locks via hasattr)
    - quantlab/base/backtest.py (BaseBacktester abstract hooks, run, and the first statement of the config setter, as 03.7-01 wrote them)
    - quantlab/backtest/engine_vectorbt.py and quantlab/backtest/us_equity.py (VectorBtBacktester and USEquityCrossectionSelectStockVectorBt, as 03.7-01 wrote them)
    - quantlab/base/config.py (BacktestConfig required fields, to build a plain one)
    - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-CONTEXT.md (D-01, D-02)
  </read_first>
  <behavior>
    - `test_abstract_method_sets_are_exact` checks three sets. `BaseBacktester.__abstractmethods__` == {config_cls, _generate_signals, _simulate, _simulate_benchmark, _engine_stats}; `VectorBtBacktester.__abstractmethods__` == {config_cls, _generate_signals}; `USEquityCrossectionSelectStockVectorBt.__abstractmethods__` is empty (D-01).
    - `test_run_lives_only_on_base_backtester` fails if any class in the concrete MRO above BaseBacktester has "run" in its `__dict__`, or defines any public (non-underscore) callable of its own (D-02).
    - `test_backtest_configs_are_constructed_with_their_own_classes`: USEquityCrossectionSelectStockVectorBt given a plain BacktestConfig raises TypeError naming CrossSectionBacktestConfig before the model or dataset is touched (type check first).
  </behavior>
  <action>
Create `tests/test_backtest_contracts.py`. Its English module docstring states that this file is the backtest layer's equivalent of `tests/test_model_hierarchy.py`. It lists each locked property with the decision it enforces (D-01, D-02, D-04, D-32, layering) and explains why each would go red. Task 2 adds the static locks, but the docstring lists them from the start.

Write the three tests in the behavior block.

For the type-check-first test, build a plain `BacktestConfig` (not the cross-sectional subclass) whose `price_dataset` and `model` are both bare `object()` instances. Then assert that constructing `USEquityCrossectionSelectStockVectorBt` with it raises TypeError whose message names `CrossSectionBacktestConfig`. Bare objects prove the check fires before any dataset or model attribute is read, because an attribute access would raise AttributeError instead.

For the public-surface test, walk `USEquityCrossectionSelectStockVectorBt.__mro__` up to, but excluding, BaseBacktester. For each class, fail if `vars(cls)` has "run", or has any non-underscore name bound to a function, classmethod, staticmethod or property.

All three pass on arrival, so mutation-verify each one:
- Add a `run` override that only calls super() to us_equity.py; the public-surface test goes red.
- Remove the abstractmethod decorator from `_simulate_benchmark` in base/backtest.py; the exact-set test goes red.
- Move the config-class isinstance check to the end of the base config setter; the type-check-first test goes red, because a different exception or message fires first.

Restore each file by copying the original back, never with git stash. Record the three observed red runs in the SUMMARY.
  </action>
  <verify>
    <automated>uv run pytest tests/test_backtest_contracts.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line, or fewer than 3 passed</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_backtest_contracts.py -q` exits 0, and its summary shows "3 passed".
    - `git status --short -- quantlab/backtest quantlab/base` prints nothing (every mutation was restored).
    - The SUMMARY records three mutation runs (run override, abstract decorator removed, type check moved), each observed red and then restored.
  </acceptance_criteria>
  <done>The D-01 hierarchy, the D-02 single run() template and type-check-first construction are each locked by a mutation-verified test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the source rules — price-column literals, factory imports, layering, vectorbt import scope</name>
  <files>tests/test_backtest_contracts.py</files>
  <read_first>
    - tests/test_backtest_contracts.py (after Task 1)
    - quantlab/backtest/us_equity.py (US_EQUITY_MARKET, the only allowed home of the column names)
    - quantlab/utils/module.py (the one lower-layer file allowed to import the backtest layer, locally inside a function, for plan 03.7-11's loader)
    - .planning/STATE.md (Decisions: the AST import resolver lesson — relative spellings defeat substring scans; "a lock that passes on arrival is mutation-verified")
    - .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-VALIDATION.md (D-32 and Layering rows)
  </read_first>
  <behavior>
    - `test_price_column_literals_never_appear_inside_a_method_body` walks every FunctionDef/AsyncFunctionDef body in quantlab/base/backtest.py and quantlab/backtest/*.py and fails if any string constant equals one of the two adjusted column names. The module-level US_EQUITY_MARKET constant stays allowed (D-04).
    - `test_backtest_layer_never_imports_config_factories` uses an AST import resolver that resolves relative imports against the file's package. It covers quantlab/base/backtest.py, quantlab/backtest/*.py, tests/backtest_fixtures.py and tests/test_backtest_*.py, and fails if any import resolves to `quantlab.config` or a submodule of it (D-32).
    - `test_no_lower_layer_imports_the_backtest_layer` uses the same resolver over every quantlab/**/*.py except quantlab/backtest/, quantlab/base/backtest.py and quantlab/utils/module.py. It fails on any import resolving to quantlab.backtest or quantlab.base.backtest.
    - `test_vectorbt_is_imported_only_by_the_engine` fails if any quantlab/**/*.py other than quantlab/backtest/engine_vectorbt.py imports vectorbt. The files under quantlab/vecbt/ are allowed until plan 03.7-12 retires that package, and the allowance names that plan in a comment.
  </behavior>
  <action>
Add the four tests in the behavior block to `tests/test_backtest_contracts.py`.

Read the two column names from `US_EQUITY_MARKET.fill_price_column` and `US_EQUITY_MARKET.valuation_price_column` rather than spelling them out in the test. The lock then tracks the spec, which D-04 makes the single source of truth.

Write the AST import resolver as one private helper, `_resolved_imports(path) -> set[str]`:
- `ast.Import` contributes each alias name.
- `ast.ImportFrom` with level 0 contributes `module`.
- `ast.ImportFrom` with level >= 1 resolves against the file's dotted package, derived from its path relative to the repo root: strip `level` trailing components, then append `module`.
- For `from X import name`, add both "X" and "X.name", so `from quantlab import config` is caught.

After each test passes, mutation-verify it:
- Temporarily add a method to us_equity.py that returns the fill-column literal; the D-04 test goes red.
- Add an import of the config factories package to selection.py; the D-32 test goes red.
- Add a relative import of the backtest package to a file under quantlab/base; the layering test goes red.
- Add an import of vectorbt to selection.py; the vectorbt-scope test goes red.

Restore each file by copying the original back, never with git stash. Record the four observed red runs in the SUMMARY.
  </action>
  <verify>
    <automated>uv run pytest tests/test_backtest_contracts.py -q</automated>
    <fails_when>non-zero exit, or "failed" or "error" in the pytest summary line, or fewer than 7 passed</fails_when>
    <automated>bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh</automated>
    <fails_when>non-zero exit, or output containing "NEW FAILURE(S)", "NEW ERROR(S)" or "NO 'passed' SUMMARY LINE"</fails_when>
  </verify>
  <acceptance_criteria>
    - `uv run pytest tests/test_backtest_contracts.py -q` exits 0, and its summary shows "7 passed".
    - `bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh` exits 0 and prints "03.7 baseline gate: no failures beyond the 56-test pre-phase baseline".
    - `grep -c "def _resolved_imports" tests/test_backtest_contracts.py` prints 1.
    - `git status --short -- quantlab/backtest quantlab/base` prints nothing (every mutation was restored).
    - The SUMMARY records four mutation runs (D-04 literal, D-32 import, layering relative import, vectorbt import), each observed red and then restored.
  </acceptance_criteria>
  <done>The D-04 column-literal rule, the D-32 factory ban, layering and the vectorbt import scope are each locked by a mutation-verified test. The full suite shows no failures beyond the baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| later plans and edits → backtest architecture | every later 03.7 plan, and any future change, edits the classes and modules these tests inspect |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.7-42 | Tampering (architecture erosion) | a subclass overriding `run` or growing its own public surface | medium | mitigate | Exact `__abstractmethods__` sets and the MRO `__dict__` scan, each mutation-verified |
| T-03.7-43 | Tampering (result integrity) | a hardcoded price column inside a method, bypassing the MARKET spec | medium | mitigate | AST walk of every function body against the spec's column names (D-04), mutation-verified |
| T-03.7-44 | Tampering (layering) | a relative-import spelling that a substring scan would miss | low | mitigate | `_resolved_imports` resolves relative imports against the file's package before the D-32, layering and vectorbt-scope checks run |
</threat_model>

<verification>
- `uv run pytest tests/test_backtest_contracts.py -q` exits 0 with "7 passed".
- `bash .planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-baseline-gate.sh` exits 0.
- Spec-less probe fallback: skipped (no requirement IDs to probe). Recorded here as a visible choice.
</verification>

<success_criteria>
- The hierarchy (D-01), the single run() implementation (D-02), the D-04 column-name rule, the D-32 factory ban and one-directional layering are locked by mutation-verified tests.
- No failures beyond the 56-test pre-phase baseline.
</success_criteria>

<output>
Create `.planning/phases/03.7-cross-sectional-backtester-basebacktester-abc-usequitycrosse/03.7-14-SUMMARY.md` when done
</output>

## Artifacts this phase produces

Symbols created by this plan (they were Task 2 of 03.7-01 until that plan was split for context budget):

- `tests/test_backtest_contracts.py`: `_resolved_imports`; tests `test_abstract_method_sets_are_exact`, `test_run_lives_only_on_base_backtester`, `test_backtest_configs_are_constructed_with_their_own_classes`, `test_price_column_literals_never_appear_inside_a_method_body`, `test_backtest_layer_never_imports_config_factories`, `test_no_lower_layer_imports_the_backtest_layer`, `test_vectorbt_is_imported_only_by_the_engine`
</output>
