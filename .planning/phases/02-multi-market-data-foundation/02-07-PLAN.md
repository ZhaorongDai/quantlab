---
phase: 02-multi-market-data-foundation
plan: 07
type: execute
wave: 4
depends_on: ["02-02", "02-03", "02-04"]
files_modified: [dataset/stock.py, tests/test_stock_dataset.py, ingest_tiingo.py, README.md]
autonomous: true
requirements: [DATA-01]

must_haves:
  truths:
    - "StockDataset._raw_data_to_xr() no longer crashes when raw Tiingo parquet files contain overlapping/duplicate (timestamp, symbol) rows"
    - "A full pipeline of TiingoAcquisition.download() (mocked client) -> StockDataset.from_raw_data().save() -> StockDataset(...).read() round-trips a [timestamp, symbol] xr.Dataset through a real (tmp-path) Zarr store"
    - "The persisted dataset carries the anomaly_flag variable (from clean_market_data) and preserves NaN gaps without forward-filling"
    - "A user can run a single documented command, authenticated via TIINGO_API_KEY, to pull US equities daily data from Tiingo and see it persisted as xr.Dataset/Zarr"
  artifacts:
    - path: "dataset/stock.py"
      provides: "StockDataset._raw_data_to_xr() with dedup_raw_frame() applied before to_xarray()"
      contains: "dedup_raw_frame("
    - path: "ingest_tiingo.py"
      provides: "Thin, documented top-level CLI wiring TiingoAcquisition + StockDataset end-to-end, matching the cal.py/train_model.py entry-point convention"
      contains: "TiingoAcquisition"
    - path: "tests/test_stock_dataset.py"
      provides: "Unit test for dedup + full mocked-network integration test proving DATA-01 end-to-end"
  key_links:
    - from: "dataset/stock.py:StockDataset._raw_data_to_xr"
      to: "dataset/cleaning.py:dedup_raw_frame"
      via: "direct call before set_index/.to_xarray()"
      pattern: "dedup_raw_frame\\("
    - from: "ingest_tiingo.py"
      to: "acquisition/tiingo.py:TiingoAcquisition"
      via: "import and .download()/.refresh() call"
      pattern: "from acquisition.tiingo import TiingoAcquisition"
    - from: "ingest_tiingo.py"
      to: "dataset/stock.py:StockDataset"
      via: "import and .from_raw_data().save() call"
      pattern: "StockDataset\\("
---

<objective>
Complete the US-equities Tiingo vertical slice (DATA-01) by wiring Wave 3's `TiingoAcquisition` (02-04) to `StockDataset` end-to-end: insert `dedup_raw_frame()` into `StockDataset._raw_data_to_xr()` (D-05, matching the identical insertion pattern already applied to `SpotKlineDataset` in 02-05), prove the full `TiingoAcquisition.download()` (mocked) -> `StockDataset.from_raw_data().save()` -> `.read()` round trip through a real tmp-path Zarr store, and ship the documented, user-runnable `ingest_tiingo.py` entry point ROADMAP Success Criterion 1 requires ("a documented command/script to pull US equities daily data from Tiingo... and see it persisted... in Zarr").

This is the final plan of the phase's Tiingo slice -- after this lands, DATA-01 is fully satisfied: a real, testable, end-to-end path from Tiingo's API shape through to a Zarr-persisted `xr.Dataset`.

Purpose: DATA-01, and the user's explicitly stated top priority for this phase.
Output: `StockDataset` deduplicates before `to_xarray()`; a full mocked-network integration test; `ingest_tiingo.py`; README documents it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-multi-market-data-foundation/02-CONTEXT.md
@.planning/phases/02-multi-market-data-foundation/02-PATTERNS.md
@dataset/stock.py
</context>

<interfaces>
Current `StockDataset._raw_data_to_xr()` tail (`dataset/stock.py`, already read in full): after
`data = data.filter(...)` on start/end date, `data = data.sort(by=["timestamp", "symbol"])`,
then `data = data.collect().to_pandas().set_index(["timestamp", "symbol"]); return
data.to_xarray()`. Insert `data = dedup_raw_frame(data, keep="last")` between the `.sort(...)`
line and `.collect()` -- the identical insertion point pattern `02-05` already applied to
`SpotKlineDataset`.

`acquisition/tiingo.py:TiingoAcquisition` (landed by `02-04`) writes raw parquet files to
`{config.raw_data_dir_path}/{symbol}/data.pqt`. `StockDataset._raw_data_to_xr()` discovers
files via `get_pqt_files(self.config.raw_data_dir_path)` (`utils/file.py`, already
`Path(dir_path).rglob("*.pqt")` -- recursive), so `TiingoAcquisition`'s output directory
structure is already directly compatible with `StockDataset`'s discovery logic -- no change to
`get_pqt_files`/`file_date_filter` is needed.

`config/__init__.py:stock_kline_config()` and `stock_acquisition_config()` (landed by `02-02`)
both default `raw_data_dir_path` to the identical `_market_downloads_root("us_equity", "1d") /
"nasdaq_data"` path -- so a `TiingoAcquisition` built from `stock_acquisition_config(...)` and
a `StockDataset` built from `stock_kline_config(...)` naturally point at the same directory
when given the same `market`/`frequency` (defaults already do this).

`tests/conftest.py` (`02-01`) provides `mock_tiingo_client`/`tiingo_json_response` -- reuse
these directly for the integration test in Task 2 rather than hand-rolling a second mock.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Insert dedup_raw_frame() into StockDataset._raw_data_to_xr() (D-05)</name>
  <files>dataset/stock.py, tests/test_stock_dataset.py</files>
  <read_first>dataset/stock.py, dataset/cleaning.py</read_first>
  <behavior>
    - Test 1: A `StockDataset` built from a `tmp_path` directory containing two parquet files
      for the same symbol with one overlapping `(timestamp, symbol)` row (simulating an
      overlapping `download()`+`refresh()` range) converts successfully via
      `.from_raw_data()` without raising `ValueError` (the non-unique-MultiIndex crash), and
      the resulting dataset has exactly one row for that timestamp.
    - Test 2: A `StockDataset` built from non-overlapping parquet files converts unchanged
      (same row count) -- regression proving the dedup insertion doesn't alter clean data.
  </behavior>
  <action>
  In `dataset/stock.py`, add `from dataset.cleaning import dedup_raw_frame` to the imports.
  Inside `_raw_data_to_xr()`, insert `data = dedup_raw_frame(data, keep="last")` immediately
  after the existing `data = data.sort(by=["timestamp", "symbol"])` line and before
  `data = data.collect().to_pandas().set_index(["timestamp", "symbol"])`. Do not touch any
  other line of this method. Write `tests/test_stock_dataset.py`'s first two tests covering
  the behaviors above, writing synthetic parquet fixtures directly with `polars` (columns:
  `timestamp`, `symbol`, `open`, `high`, `low`, `close`, `volume`, `adjOpen`, `adjHigh`,
  `adjLow`, `adjClose`, `adjVolume`) under `tmp_path / "{symbol}" / "data.pqt"`.
  </action>
  <acceptance_criteria>
    - `dataset/stock.py:StockDataset._raw_data_to_xr` contains the literal string `dedup_raw_frame(data, keep="last")`, positioned between the existing `.sort(...)` call and `.collect()`.
    - No other line in `_raw_data_to_xr()` changed.
    - `uv run pytest tests/test_stock_dataset.py -k dedup -v` shows 2 passed.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_stock_dataset.py -k dedup -v</automated>
  </verify>
  <done>StockDataset deduplicates overlapping (timestamp, symbol) rows before conversion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Full mocked-network integration test proving DATA-01 end-to-end</name>
  <files>tests/test_stock_dataset.py</files>
  <read_first>acquisition/tiingo.py, dataset/stock.py, tests/conftest.py</read_first>
  <behavior>
    - Test 3: Using the `mock_tiingo_client` fixture, construct a `stock_acquisition_config`-
      style `AcquisitionConfig` and `stock_kline_config`-style `DatasetConfig` both pointed at
      the same `tmp_path`-derived `raw_data_dir_path` (per this plan's `<interfaces>` note on
      shared defaults). Call `TiingoAcquisition(acq_config).download(["AAPL"])`. Then construct
      `StockDataset(ds_config)` and call `.from_raw_data().save()` against a `tmp_path`-derived
      `zarr_file_path`. Construct a second `StockDataset(ds_config)` and call `.read()`. Assert
      the resulting `xr.Dataset` (`get_xarray_dataset()`) has `timestamp`/`symbol` dims,
      contains an `adjClose` variable with the values from `tiingo_json_response`, contains an
      `anomaly_flag` variable (proving `clean_market_data()` from `02-03` ran), and that any
      `(timestamp, symbol)` combination absent from the fetched data is `NaN` (not
      forward-filled) in the read-back result.
  </behavior>
  <action>
  Add the integration test described above to `tests/test_stock_dataset.py`, reusing
  `mock_tiingo_client`/`tiingo_json_response` from `tests/conftest.py` (no live network call).
  This is the strongest available proof of DATA-01: a real `TiingoAcquisition` writes real
  parquet files that a real `StockDataset` reads, cleans, and persists to a real (tmp-path)
  Zarr store, then reads back -- only the outermost HTTP call is mocked.
  </action>
  <acceptance_criteria>
    - The test constructs both `AcquisitionConfig` and `DatasetConfig` with matching `raw_data_dir_path`.
    - The test asserts presence of `anomaly_flag` in the read-back dataset.
    - `uv run pytest tests/test_stock_dataset.py -v` shows all tests (dedup + integration) passed.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_stock_dataset.py -v</automated>
  </verify>
  <done>A full TiingoAcquisition-to-Zarr round trip is proven end-to-end with zero live network access, exercising every layer this phase built for the US-equities slice.</done>
</task>

<task type="auto">
  <name>Task 3: Documented ingest_tiingo.py entry-point script + README update</name>
  <files>ingest_tiingo.py, README.md</files>
  <read_first>cal.py, README.md, config/__init__.py</read_first>
  <action>
  Create a new top-level `ingest_tiingo.py`, matching `cal.py`'s established thin-script
  convention (no classes defined in the script; a module docstring documenting usage,
  including the `TIINGO_API_KEY` requirement; an `argparse.ArgumentParser` with `--symbols`
  (required, comma-separated), `--start-date`, `--end-date`, and a `--refresh` flag
  (`action="store_true"`) meaning "call `.refresh()` instead of `.download()`"). Under `if
  __name__ == "__main__":`: parse `symbols` into a tuple; build `acq_config =
  stock_acquisition_config(symbols=symbols, start_date=..., end_date=...)` and `acquisition =
  TiingoAcquisition(acq_config)`; call `acquisition.refresh()` if `--refresh` else
  `acquisition.download()`; then build `ds = StockDataset(stock_kline_config(symbols=list(
  symbols), start_date=..., end_date=...))` and call `ds.from_raw_data().save()`. The script
  must never print or log the raw `TIINGO_API_KEY` value or the `TiingoClient` config dict --
  only symbol lists and date ranges may be logged/printed.

  Update `README.md`'s "Entry Points" section: add a bullet for `ingest_tiingo.py` (full
  Tiingo-to-Zarr pipeline, requires `TIINGO_API_KEY`, supports `--refresh` for incremental
  updates) and update the existing `TIINGO_API_KEY` bullet under "Environment Variables" to
  note it is now also required by `ingest_tiingo.py` (in addition to the existing
  `scripts/download_stock_data_from_tiingo.py` reference).
  </action>
  <acceptance_criteria>
    - `TIINGO_API_KEY=test uv run python ingest_tiingo.py --help` exits 0 and prints usage mentioning `--symbols` and `--refresh`.
    - `ingest_tiingo.py` contains no line printing/logging `os.environ["TIINGO_API_KEY"]` or the raw key value (grep: `grep -n "TIINGO_API_KEY" ingest_tiingo.py` shows it only used inside the imported `TiingoAcquisition`/`stock_acquisition_config` calls, never printed directly in this script).
    - `README.md`'s Entry Points section lists `ingest_tiingo.py`; Environment Variables section mentions it needs `TIINGO_API_KEY`.
  </acceptance_criteria>
  <verify>
    <automated>TIINGO_API_KEY=test uv run python ingest_tiingo.py --help</automated>
  </verify>
  <done>A single documented command, authenticated via TIINGO_API_KEY, pulls US-equities daily data from Tiingo and persists it as an xr.Dataset in Zarr, with --refresh supporting incremental updates -- ROADMAP Success Criterion 1 fully satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `ingest_tiingo.py` CLI process <-> local shell environment | `TIINGO_API_KEY` crosses from the shell environment into the process; the script itself must never re-expose it |
| Locally-written Tiingo raw parquet files <-> internal xr.Dataset representation | Vendor JSON converted and written by `02-04`'s `TiingoAcquisition`, read here by `StockDataset` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07-01 | Tampering (data integrity, non-adversarial) | `dataset/stock.py:StockDataset._raw_data_to_xr` | mitigate | `dedup_raw_frame()` inserted before `.to_xarray()` prevents a crash from overlapping `download()`/`refresh()` date ranges |
| T-02-07-02 | Information Disclosure | `ingest_tiingo.py` | mitigate | The script never prints/logs the raw `TIINGO_API_KEY` value or the `TiingoClient` config dict -- only symbol lists and date ranges are ever logged, enforced by Task 3's grep acceptance criterion |

</threat_model>

<verification>
Run `uv run pytest tests/ -v` (full suite across all seven Phase 2 plans) -- all tests green, confirming the complete US-equities and Binance slices, the shared cleaning module, and the extensibility contract all pass together.
</verification>

<success_criteria>
- `StockDataset` no longer crashes on overlapping/duplicate raw Tiingo rows.
- A full mocked-network integration test proves the TiingoAcquisition -> StockDataset -> Zarr round trip end-to-end.
- A single documented command (`ingest_tiingo.py`) pulls US-equities daily data from Tiingo and persists it as xr.Dataset/Zarr, with incremental refresh support.
- README documents the new entry point and its credential requirement.
</success_criteria>

<output>
Create `.planning/phases/02-multi-market-data-foundation/02-07-SUMMARY.md` when done
</output>
