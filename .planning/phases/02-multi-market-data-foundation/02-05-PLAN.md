---
phase: 02-multi-market-data-foundation
plan: 05
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified: [dataset/spot.py, ingest_binance_spot.py, tests/test_spot_dataset.py, README.md]
autonomous: true
requirements: [DATA-02]

must_haves:
  truths:
    - "SpotKlineDataset._raw_data_to_xr() no longer crashes when raw CSVs contain overlapping/duplicate (timestamp, symbol) rows near a month boundary"
    - "A user can run a documented top-level command to rebuild the Binance spot-kline Zarr store from locally-dropped CSVs through the same Dataset/DataBackend abstraction used by US equities"
    - "No new Binance downloader/network call exists anywhere in this plan's changes (D-03) -- the manual-CSV-drop workflow is unchanged"
  artifacts:
    - path: "dataset/spot.py"
      provides: "SpotKlineDataset._raw_data_to_xr() with dedup_raw_frame() applied before to_xarray()"
      contains: "dedup_raw_frame("
    - path: "ingest_binance_spot.py"
      provides: "Thin, documented top-level CLI entry point rebuilding the Binance spot-kline Zarr store via spot_kline_config()+SpotKlineDataset, matching the existing cal.py/train_model.py entry-point convention"
    - path: "tests/test_spot_dataset.py"
      provides: "Unit test proving dedup + correct xr.Dataset conversion from synthetic fixture CSVs with duplicate rows"
  key_links:
    - from: "dataset/spot.py:SpotKlineDataset._raw_data_to_xr"
      to: "dataset/cleaning.py:dedup_raw_frame"
      via: "direct call before set_index/.to_xarray()"
      pattern: "dedup_raw_frame\\("
    - from: "ingest_binance_spot.py"
      to: "config/__init__.py:spot_kline_config"
      via: "import and call"
      pattern: "from config import spot_kline_config"
---

<objective>
Retrofit `SpotKlineDataset`/`spot_kline_config()` onto the new `market`/`frequency` config schema landed in Wave 2 (CONTEXT.md D-04) and insert the shared `dedup_raw_frame()` call before `SpotKlineDataset._raw_data_to_xr()`'s final `.to_xarray()` conversion (D-05, matching the same pattern the Tiingo/StockDataset slice applies). Per the user's explicit priority (CONTEXT.md D-03), this plan does NOT add any new Binance downloader or network call -- Binance keeps its current manual-CSV-drop workflow. `spot_kline_config()` itself was already retrofitted onto the `market`/`frequency` schema by `02-02`; this plan's job is the `SpotKlineDataset` conversion-logic change plus a documented, user-runnable command that proves the retrofit works end-to-end (ROADMAP Success Criterion 2).

Purpose: DATA-02 -- the user can pull/refresh Binance spot kline data through the same `Dataset`/`DataBackend` abstraction into the same Zarr storage format US equities use.
Output: `SpotKlineDataset` deduplicates before crashing on `to_xarray()`; a new `ingest_binance_spot.py` thin entry-point script; `README.md` documents it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-multi-market-data-foundation/02-CONTEXT.md
@.planning/phases/02-multi-market-data-foundation/02-PATTERNS.md
@dataset/spot.py
</context>

<interfaces>
Current `SpotKlineDataset._raw_data_to_xr()` tail (`dataset/spot.py`, already read in full):
values end with `res = pl.concat(dfs)`, then `res = res.rename({"Open time": "timestamp"})`,
then `res = res.sort(by=["timestamp", "symbol"])`, then
`res = res.collect().to_pandas().set_index(["timestamp", "symbol"]); return res.to_xarray()`.
The fix is a single inserted line calling `dataset.cleaning.dedup_raw_frame(res, keep="last")`
on the `pl.LazyFrame` between the `.sort(...)` call and `.collect()` -- matching the exact
insertion point 02-RESEARCH.md's Pattern 2 documents (dedup must run before `.to_xarray()`, not
after, since `base/data.py:Dataset.from_raw_data()`'s centralized `clean_market_data()` hook
(landed by `02-03`) only sees the already-converted `xr.Dataset`, by which point a
non-unique-index crash has already happened).

`config/__init__.py:spot_kline_config()` (retrofitted by `02-02`) now accepts `market:
Market = "crypto_spot"` and `frequency: Frequency = "1d"` keyword params and derives
`raw_data_dir_path`/`zarr_file_path` via the `data/{market}/{frequency}/...` convention -- no
further config change is needed in this plan, only the import site (`ingest_binance_spot.py`)
needs to exist.

`cal.py` (existing top-level entry point, read in full) shows the established thin-script
convention this plan's new `ingest_binance_spot.py` must match: import from `config`/
`dataset.*`, construct one config object, call one or two chained methods under
`if __name__ == "__main__":`, no classes defined in the script itself.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Insert dedup_raw_frame() into SpotKlineDataset._raw_data_to_xr() (D-05)</name>
  <files>dataset/spot.py, tests/test_spot_dataset.py</files>
  <read_first>dataset/spot.py, dataset/cleaning.py</read_first>
  <behavior>
    - Test 1: Using `write_binance_csv` (from `tests/conftest.py`, landed by `02-01`) to write
      two monthly CSV files whose date ranges overlap by one day (simulating the real-world
      Binance monthly-CSV-near-month-boundary duplicate risk documented in 02-RESEARCH.md),
      constructing a `SpotKlineDataset` pointed at that `tmp_path` via a `DatasetConfig`
      (`market="crypto_spot", frequency="1d"`), and calling `.from_raw_data()` succeeds (does
      NOT raise `ValueError: cannot convert a DataFrame with a non-unique MultiIndex into
      xarray`) and the resulting dataset has exactly one row per overlapping
      `(timestamp, symbol)` pair.
    - Test 2: A `SpotKlineDataset` built from CSVs with no duplicate rows converts to an
      `xr.Dataset` with the expected `[timestamp, symbol]` dims and an unchanged row count
      (regression -- dedup insertion must not alter non-duplicate data).
  </behavior>
  <action>
  In `dataset/spot.py`, add `from dataset.cleaning import dedup_raw_frame` to the imports.
  Inside `_raw_data_to_xr()`, insert `res = dedup_raw_frame(res, keep="last")` immediately
  after `res = res.sort(by=["timestamp", "symbol"])` and before
  `res = res.collect().to_pandas().set_index(["timestamp", "symbol"])`. Do not touch any other
  line of this method -- this is a config/cleaning-insertion change only, not a rewrite of the
  CSV-discovery or column-mapping logic (D-04's own scope boundary). Write
  `tests/test_spot_dataset.py` covering both behaviors above using `write_binance_csv` from
  `tests/conftest.py`.
  </action>
  <acceptance_criteria>
    - `dataset/spot.py:SpotKlineDataset._raw_data_to_xr` contains the literal string `dedup_raw_frame(res, keep="last")`, positioned between the existing `.sort(...)` call and `.collect()`.
    - No other line in `_raw_data_to_xr()` changed (diff review: only the import line and the one inserted line are new).
    - `uv run pytest tests/test_spot_dataset.py -v` shows 2 passed.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_spot_dataset.py -v</automated>
  </verify>
  <done>SpotKlineDataset deduplicates overlapping (timestamp, symbol) rows before conversion, preventing the to_xarray() crash, with zero change to unrelated conversion logic.</done>
</task>

<task type="auto">
  <name>Task 2: Documented ingest_binance_spot.py entry-point script + README update</name>
  <files>ingest_binance_spot.py, README.md</files>
  <read_first>cal.py, README.md, config/__init__.py</read_first>
  <action>
  Create a new top-level `ingest_binance_spot.py`, matching `cal.py`'s established thin-script
  convention (no classes defined in the script; import from `config`/`dataset.spot`; a module
  docstring documenting usage; an `argparse.ArgumentParser` with `--symbols` (comma-separated,
  optional -- omit to process every symbol found under `raw_data_dir_path`), `--start-date`,
  `--end-date` options; under `if __name__ == "__main__":`, build `SpotKlineDataset(
  spot_kline_config(symbols=..., start_date=..., end_date=...))` and call
  `.from_raw_data().save()`). The module docstring must explicitly state this script rebuilds
  the Zarr store from CSVs that must already exist locally under `raw_data_dir_path`
  (`{QUANTLAB_DATA_DIR}/downloads/crypto_spot/1d/spot/monthly/klines/` by default) -- it does
  NOT download anything (D-03: no new Binance downloader in this phase).

  Update `README.md`'s "Entry Points" section: add a bullet for `ingest_binance_spot.py`
  describing what it does (rebuild the Binance spot-kline Zarr store from locally-dropped
  monthly CSVs, no network access) directly below the existing `get_binance_instruments.py`
  bullet, matching the existing bullet-list format and level of detail.
  </action>
  <acceptance_criteria>
    - `uv run python ingest_binance_spot.py --help` exits 0 and prints usage mentioning `--symbols`.
    - `ingest_binance_spot.py` contains no `requests.get(`/`urllib`/HTTP-client import of any kind (grep: `grep -iE "requests|urllib|httpx|aiohttp" ingest_binance_spot.py` returns zero matches) -- proves D-03 compliance (no new network fetch).
    - `README.md`'s Entry Points section lists `ingest_binance_spot.py`.
  </acceptance_criteria>
  <verify>
    <automated>uv run python ingest_binance_spot.py --help</automated>
  </verify>
  <done>A documented, runnable top-level command rebuilds the Binance spot-kline Zarr store through the Dataset/DataBackend abstraction, with no new network-fetch code, and README documents it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Locally-dropped Binance CSV files <-> internal xr.Dataset representation | Raw vendor data of unverified integrity (manual drop workflow, no acquisition-layer validation before this point) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Tampering (data integrity, non-adversarial) | `dataset/spot.py:SpotKlineDataset._raw_data_to_xr` | mitigate | `dedup_raw_frame()` inserted before `.to_xarray()` prevents the documented non-unique-MultiIndex crash from overlapping monthly CSV files near month boundaries |
| T-02-05-02 | Tampering | `ingest_binance_spot.py` / `raw_data_dir_path` | accept | The raw CSV directory is a locally-configured path (via `spot_kline_config()`'s `_market_downloads_root()` helper), not a user/network-supplied value at runtime -- low severity per 02-RESEARCH.md Security Domain; no new validation needed beyond what `02-02` already established |

</threat_model>

<verification>
Run `uv run pytest tests/test_spot_dataset.py tests/test_cleaning.py tests/test_config_paths.py -v` -- all tests green. Manually confirm `uv run python ingest_binance_spot.py --help` prints usage.
</verification>

<success_criteria>
- `SpotKlineDataset` no longer crashes on overlapping/duplicate raw rows.
- A documented top-level script rebuilds the Binance Zarr store via the shared Dataset/DataBackend abstraction, with zero new network code.
- README documents the new entry point.
</success_criteria>

<output>
Create `.planning/phases/02-multi-market-data-foundation/02-05-SUMMARY.md` when done
</output>
