---
phase: 02-multi-market-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [pyproject.toml, uv.lock, tests/conftest.py, tests/test_backend_overwrite.py, dataset/backend.py]
autonomous: true
requirements: [DATA-01]

must_haves:
  truths:
    - "Running `uv run pytest tests/ -q` executes successfully (pytest is installed and discoverable) even though most feature test files do not exist yet"
    - "Calling XrBackend.write() twice against the same Zarr path no longer raises FileExistsError"
    - "A regression test locks in the mode=\"w\" fix so a future edit cannot silently reintroduce Pitfall 1"
  artifacts:
    - path: "tests/conftest.py"
      provides: "Shared pytest fixtures: synthetic Binance CSV rows/writer, synthetic Tiingo JSON response, mocked TiingoClient (no live network calls)"
    - path: "tests/test_backend_overwrite.py"
      provides: "Regression test proving XrBackend.write() supports being called twice against the same path"
    - path: "dataset/backend.py"
      provides: "XrBackend.write() defaulting to mode=\"w\" via kwargs.setdefault"
      contains: "kwargs.setdefault(\"mode\", \"w\")"
  key_links:
    - from: "tests/test_backend_overwrite.py"
      to: "dataset/backend.py:XrBackend.write"
      via: "direct call, twice, against the same tmp_path"
      pattern: "XrBackend\\(\\)\\.write"
---

<objective>
Stand up the project's first pytest test infrastructure (framework, shared fixtures) and fix the one empirically-verified defect (RESEARCH.md Pitfall 1) that blocks every later incremental-refresh task in this phase: `XrBackend.write()` crashes with `FileExistsError` the second time it is called against the same Zarr path, because it never passes `mode` to `xr.Dataset.to_zarr()`.

This plan is the mandatory Wave 0 prerequisite (per 02-VALIDATION.md) that every other Phase 2 plan's `<verify><automated>` command depends on — no other plan in this phase can run `uv run pytest ...` until this one lands. It intentionally does NOT create the five other feature-specific test files (`test_tiingo_acquisition.py`, `test_stock_dataset.py`, `test_spot_dataset.py`, `test_cleaning.py`, `test_extensibility_contract.py`) — those are created with real content by the plan that implements each feature (Waves 2-4), not stubbed empty here, to avoid double-touching the same file across waves for no reason.

Purpose: Every later plan's automated verification depends on pytest existing; the incremental-refresh capability locked in CONTEXT.md D-10 is impossible without the Zarr overwrite fix.
Output: `pytest` installed as a dev dependency; `tests/conftest.py` with reusable fixtures; `tests/test_backend_overwrite.py` passing; `dataset/backend.py:XrBackend.write()` fixed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-multi-market-data-foundation/02-RESEARCH.md
@.planning/phases/02-multi-market-data-foundation/02-VALIDATION.md
@enums/data.py
</context>

<interfaces>
<!-- Fixtures this plan creates that later plans' test files will import via pytest's
     automatic conftest discovery (no explicit import needed — pytest fixtures are
     injected by name as test function parameters). -->

Fixture contract for `tests/conftest.py` (consumed by 02-04, 02-05, 02-07's test files):

- `binance_csv_rows() -> list[list]`: returns a small list of raw row-tuples matching the
  column order of `enums.data.BinanceCSVHeaders.SPOT` (12 columns: `Open time, Open, High,
  Low, Close, Volume, Close time, Quote asset volume, Number of trades, Taker buy base asset
  volume, Taker buy quote asset volume, Ignore`), with `Open time` as millisecond epoch ints.
- `write_binance_csv(tmp_path) -> Callable[[str, str, list | None], Path]`: a factory fixture
  — call it as `write_binance_csv(symbol="BTCUSDT", year_month="2024-01", rows=None)` to write
  a headerless CSV file at `{tmp_path}/{symbol}-1d-{year_month}.csv` using `binance_csv_rows()`
  as the default row data if `rows` is not given; returns the written `Path`.
- `tiingo_json_response() -> list[dict]`: a small list of dicts matching the field set
  `open,high,low,close,volume,adjOpen,adjHigh,adjLow,adjClose,adjVolume,divCash,splitFactor,date`
  (the `columns=` list a future `TiingoAcquisition` will request explicitly — see
  02-RESEARCH.md Pitfall 3), with `date` as ISO-8601 strings (e.g. `"2024-01-02T00:00:00.000Z"`).
- `mock_tiingo_client(monkeypatch, tiingo_json_response) -> type`: returns a
  `FakeTiingoClient` class and, as a side effect, calls
  `monkeypatch.setattr("acquisition.tiingo.TiingoClient", FakeTiingoClient)` from INSIDE the
  fixture function body (never at module level in `conftest.py`) so `conftest.py` has zero
  import-time dependency on `acquisition.tiingo`, which does not exist until 02-04 lands —
  `conftest.py` must still collect cleanly today. Also calls
  `monkeypatch.setenv("TIINGO_API_KEY", "test-key-not-real")` so downstream tests never need a
  real credential. Records every `get_ticker_price(...)` call's kwargs on
  `FakeTiingoClient.calls` (a list) so tests can assert on `startDate`/`columns`/`frequency`
  arguments passed by `TiingoAcquisition`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install pytest and create shared test fixtures</name>
  <files>pyproject.toml, uv.lock, tests/conftest.py</files>
  <read_first>pyproject.toml, enums/data.py, .planning/phases/02-multi-market-data-foundation/02-VALIDATION.md</read_first>
  <action>
  Run `uv add --dev pytest` from the repo root (this is the standard, ubiquitous `pytest-dev`-maintained package already named as this project's test framework in 02-RESEARCH.md's Validation Architecture section — classify as [VERIFIED], no legitimacy checkpoint needed; confirm the resulting `pyproject.toml` gained a `[dependency-groups]` `dev = ["pytest>=..."]` block and `uv.lock` was regenerated without error).

  Create `tests/conftest.py` implementing exactly the four fixtures documented in this plan's `<interfaces>` block above: `binance_csv_rows`, `write_binance_csv`, `tiingo_json_response`, `mock_tiingo_client`. Use `pytest.fixture` decorators, `tmp_path` (pytest's built-in fixture) inside `write_binance_csv`, and `monkeypatch` inside `mock_tiingo_client`. `mock_tiingo_client` must NOT import `acquisition.tiingo` at module level in `conftest.py` — only reference the dotted string `"acquisition.tiingo.TiingoClient"` inside `monkeypatch.setattr(...)` at fixture-call time, so `conftest.py` itself has zero import-time dependency on code that doesn't exist yet (02-04 creates `acquisition/tiingo.py`).
  </action>
  <acceptance_criteria>
    - `pyproject.toml` contains a dev dependency group listing `pytest`.
    - `uv run python -c "import pytest"` succeeds.
    - `tests/conftest.py` defines exactly the four fixtures named above with the signatures described in `<interfaces>`.
    - `tests/conftest.py` has no top-level `import acquisition...` statement (grep confirms: `grep -v '^#' tests/conftest.py | grep -c 'import acquisition'` returns `0`).
    - `uv run pytest tests/ --collect-only -q` exits 0 (collects zero tests so far, but does not error).
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/ --collect-only -q</automated>
  </verify>
  <done>pytest is an installed dev dependency; tests/conftest.py exists with the four documented fixtures and collects without import errors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix XrBackend.write() FileExistsError on re-run (RESEARCH.md Pitfall 1)</name>
  <files>dataset/backend.py, tests/test_backend_overwrite.py</files>
  <read_first>dataset/backend.py</read_first>
  <behavior>
    - Test 1: Calling `XrBackend().write(path, ...)` once against a fresh `tmp_path / "test.zarr"` with a small 2x2 `xr.Dataset` succeeds and the store is readable back via `xr.open_dataset(path)`.
    - Test 2: Calling `XrBackend().write(path, ...)` a second time against the SAME path (fresh `XrBackend` instance, same or different in-memory `xr.Dataset`) does NOT raise `FileExistsError` — this is the exact regression RESEARCH.md's Pitfall 1 documents (`self.data.to_zarr(path)` with no `mode` kwarg defaults to Zarr's `mode="w-"`).
    - Test 3: An explicit caller-supplied `mode="a"` (or any other explicit `mode=`) is still respected and NOT silently overridden by the new default (`kwargs.setdefault` semantics, not unconditional override) — matches `base/factor.py:FactorKunQuant.save()`'s existing `mode: Literal["a", "w"] = "a"` explicit-pass-through usage, which must remain unaffected.
  </behavior>
  <action>
  In `dataset/backend.py:XrBackend.write()`, insert `kwargs.setdefault("mode", "w")` immediately before the existing `self.data.to_zarr(path, **kwargs)` call (after the `Path(path).parent.mkdir(...)` block). Do not change `PlBackend.write()` — parquet overwrite has no equivalent defect (per 02-RESEARCH.md Common Pitfalls). Write `tests/test_backend_overwrite.py` covering the three behaviors above using `tmp_path` and a small hand-built `xr.Dataset` (e.g. `xr.Dataset({"close": (["timestamp", "symbol"], [[1.0, 2.0]])}, coords={"timestamp": [...], "symbol": ["A", "B"]})`) — write RED (test against the unfixed method, confirm `FileExistsError` is raised) before applying the one-line fix, then GREEN (fix applied, all three tests pass).
  </action>
  <acceptance_criteria>
    - `dataset/backend.py:XrBackend.write` contains the literal string `kwargs.setdefault("mode", "w")`.
    - `PlBackend.write` is byte-for-byte unchanged.
    - `uv run pytest tests/test_backend_overwrite.py -v` shows 3 passed.
  </acceptance_criteria>
  <verify>
    <automated>uv run pytest tests/test_backend_overwrite.py -v</automated>
  </verify>
  <done>XrBackend.write() can be called twice against the same Zarr path without raising; a committed regression test locks this in; explicit mode= kwargs still pass through unmodified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local process <-> local Zarr filesystem store | Repeated writes to the same on-disk path by a daily-refresh workflow — the exact scenario Pitfall 1 breaks today |
| Local dev environment <-> PyPI package registry | New dev-only dependency (`pytest`) installed via `uv add --dev` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | `dataset/backend.py:XrBackend.write` | mitigate | `kwargs.setdefault("mode", "w")` makes re-runs idempotent-by-overwrite instead of crashing; a committed regression test (`tests/test_backend_overwrite.py`) prevents silent regression of this fix in future edits |
| T-02-01-SC | Tampering (supply chain) | `pyproject.toml` dev dependency install (`uv add --dev pytest`) | accept | `pytest` is a [VERIFIED] mainstream package (PyPI `pytest`, maintained by the `pytest-dev` organization, already named as this project's test framework choice in 02-RESEARCH.md's own Validation Architecture section) — no [ASSUMED]/[SUS] classification applies, no blocking-human-verify checkpoint required |

</threat_model>

<verification>
Run `uv run pytest tests/ -v` — expect `test_backend_overwrite.py`'s 3 tests to pass and collection of `tests/conftest.py` to succeed with zero errors (other test files do not exist yet in this plan).
</verification>

<success_criteria>
- `uv run pytest tests/ -q` runs successfully (installed, collects, executes).
- `XrBackend.write()` no longer raises `FileExistsError` on a second call to the same path.
- `tests/conftest.py` exposes the four fixtures documented in `<interfaces>`, ready for 02-04/02-05/02-07 to consume.
</success_criteria>

<output>
Create `.planning/phases/02-multi-market-data-foundation/02-01-SUMMARY.md` when done
</output>
