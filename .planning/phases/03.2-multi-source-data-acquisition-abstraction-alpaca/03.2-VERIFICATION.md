---
phase: 03.2-multi-source-data-acquisition-abstraction-alpaca
verified: 2026-09-06T21:15:00Z
status: passed
score: 7/7 must-haves verified
behavior_unverified: 0
overrides_applied: 0
behavior_unverified_items:
  - truth: "SC-5 — User can fetch Alpaca daily bars, minute bars, and quotes/trades to raw storage, selecting the vendor by config"
    test: "With APCA_API_KEY_ID / APCA_API_SECRET_KEY exported, run `uv run python ingest_alpaca.py --symbols AAPL --start-date 2024-01-02 --end-date 2024-01-02`, then repeat with `--frequency 1m` and with `--frequency tick --data-type quotes --rows-per-symbol-day 1000000`."
    expected: "Each run lands a parquet shard under `data/downloads/us_equity/{freq}/nasdaq_data/alpaca/...` at the hive path the offline check already reproduced (`month=YYYY-MM/`, `date=YYYY-MM-DD/`, `data_type=quotes/date=.../symbol=AAPL/`), and a watermark sidecar under `_watermarks/alpaca/`. The vendor must accept `asof=-` (ASOF_NO_MAPPING) rather than returning 4xx."
    why_human: "No Alpaca credentials exist on this machine (`env | grep APCA` is empty), so the live transport cannot be exercised. Every offline-provable half of SC-5 was independently verified at the prepared-HTTP-request boundary; only the vendor's actual response is unobservable. Declared `verification: backstop` in 03.2-02-PLAN.md and 03.2-07-PLAN.md."
human_verification:
  - test: "Issue ONE historical SIP quotes/trades request older than 15 minutes with a free (Basic) tier key pair, e.g. `curl -H \"APCA-API-KEY-ID: $APCA_API_KEY_ID\" -H \"APCA-API-SECRET-KEY: $APCA_API_SECRET_KEY\" 'https://data.alpaca.markets/v2/stocks/quotes?symbols=AAPL&start=2024-01-02T14:30:00Z&end=2024-01-02T14:31:00Z&feed=sip&limit=1'`"
    expected: "A 200 (Basic tier reaches historical SIP) or a 403 (IEX is the only usable feed). Either answer resolves RESEARCH Assumption A7 / open question O-1 and determines whether a `feed` default may finally be written into code."
    why_human: "Alpaca's own documentation self-conflicts (three sources say one thing, one says the other). No credential exists on this machine. No `feed` default was baked into code precisely because this is unresolved — `acquisition/alpaca.py:773` reads `feed` from kwargs with default `None` and omits the key when unset, confirmed live: no request carried a `feed=` param."
  - test: "A real one-symbol, one-trading-day Alpaca fetch with live credentials — see behavior_unverified_items above for the exact commands and expected paths."
    expected: "Shards land at the vendor-namespaced hive paths, and `ASOF_NO_MAPPING = \"-\"` is accepted by the vendor."
    why_human: "No credentials on this machine. `ASOF_NO_MAPPING = \"-\"` is an unverified sentinel, labelled as such in code; if wrong it fails loud (4xx per batch, isolated to that batch, recorded in the failure manifest) rather than silently."
  - test: "Probe the real maximum symbol count per Alpaca multi-symbol request upward from the 100 default (e.g. 100 → 200 → 500 symbols in one `symbols=` param), recording the status code at which it breaks."
    expected: "The empirical ceiling, resolving RESEARCH Assumption A2. `AlpacaAcquisition.DEFAULT_BATCH_SIZE = 100` is a conservative working value until then."
    why_human: "Requires live vendor responses; no credentials on this machine."
---

# Phase 03.2: Multi-Source Data Acquisition Abstraction (Alpaca) Verification Report

**Phase Goal:** Users can acquire market data from a second vendor (Alpaca) through the same `Acquisition` abstraction as Tiingo, where that abstraction now natively supports batched multi-symbol requests, page-level resumable pagination, and a pre-flight volume guard — with both vendors' raw data coexisting under a vendor-namespaced, hive-partitioned layout.

**Verified:** 2026-09-06T21:15:00Z
**Status:** passed (SC-5 closed by live UAT, 2026-09-06)
**Branch:** `phase-03.2-alpaca` (52 commits ahead of `main`)
**Re-verification:** No — initial verification

**Coverage units:** SC-1..SC-7 from ROADMAP.md. This phase has no REQ-IDs (ROADMAP records `Requirements: TBD`); `.planning/REQUIREMENTS.md` contains no `03.2` mapping, confirmed by grep. No orphaned requirements exist.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| SC-1 | Fetch primitive is batched; single-symbol vendor is the degenerate case; no `NotImplementedError` stub in the hierarchy | ✓ VERIFIED | Live introspection: `Acquisition.__abstractmethods__ == frozenset({'_fetch_page'})`; `_fetch_page(symbols: list[str], ...)` takes a list. `TiingoAcquisition.DEFAULT_BATCH_SIZE == 1`, `AlpacaAcquisition.DEFAULT_BATCH_SIZE == 100`. `hasattr(Acquisition, '_fetch_and_write')` is **False** — retired with no transitional alias. Concrete subclasses discovered by walk: `['TiingoAcquisition', 'AlpacaAcquisition']`. `tests/test_acquisition_batching.py` AST-scans every concrete subclass for placeholder raises and asserts `len(classes) >= 2` first, so it cannot pass vacuously. |
| SC-2 | Concurrency, resume, failure isolation and the global quota abort live once in the base class and drive both vendors; 260906-26o behaviours hold | ✓ VERIFIED | `joblib.Parallel(backend='threading', return_as='generator_unordered')`, the `threading.Event` abort, `_partition_by_coverage`, `_report_coverage`, `_write_failure_manifest` and the drain loop are all in `base/acquisition.py` only. `QUOTA_STATUS_CODES` is **not** on the base (`hasattr(Acquisition, 'QUOTA_STATUS_CODES')` is False) and is `frozenset({429})` on Tiingo; `AlpacaAcquisition.RATE_LIMIT_STATUS_CODES == frozenset({429})` — same code, opposite handling, via per-vendor `_classify_error`. The abort check is the literal first statement of `_attempt_batch`; the result generator is drained, never broken out of, with the bar description switched to `"QUOTA EXHAUSTED -- draining, not fetching"`. **D-02 gate independently re-run at all 13 commits touching `base/acquisition.py`/`acquisition/tiingo.py` in an isolated worktree: exit 0 every time (44→48→50→51 passed, never 0/exit-5).** |
| SC-3 | An interrupted paginated fetch resumes at the failed page, not at the start of the batch | ✓ VERIFIED | Behaviour-dependent, so proved by named passing tests, not by presence. `_fetch_batch` is the sole pagination site; shard write strictly precedes `ledger.record_page`. `tests/test_page_ledger.py::test_an_interrupted_batch_resumes_at_the_failed_page` asserts the resumed run's **first** recorded transport call carries `page_token == "tok-after-2"`; its sibling `test_a_resumed_run_does_not_re_request_page_zero` asserts `None not in tokens` **and** `len(calls) == 2` — the restart-vs-resume distinction that a bare "the run completed" assertion cannot make. 13 passed, exit 0. |
| SC-4 | A symbol absent from a batch response is recorded as "queried, no data", distinguishable at read time from a fetch failure and from a never-fetched symbol | ✓ VERIFIED | Behaviour-dependent, proved by passing tests. `_attempt_batch` computes `marked = set(symbols) - outcome.symbols_with_data` **only** when `outcome.complete and not self._abort.is_set()`; `symbols_with_data` accumulates across pages inside `_fetch_batch`. `test_the_four_no_data_states_are_distinguishable_on_disk` asserts all four states map to four distinct labels (`len(set(observed.values())) == 4`). Both directions are locked: `test_absence_from_page_zero_never_marks_a_symbol_no_data` (negative, plus an ordering assertion that every watermark write follows the last page) and `test_a_completed_batch_marks_only_the_symbol_absent_from_every_page_no_data` (positive — without which the negative test is satisfiable by never marking anything). Interrupted and quota-aborted batches assert **exactly zero** markers. |
| SC-5 | User can fetch Alpaca daily bars, minute bars, and quotes/trades to raw storage, selecting the vendor by config | ✓ VERIFIED | Everything provable offline was **independently reproduced by the verifier** (not read from a SUMMARY): patching `requests.Session.send` and running the real `_AlpacaMarketDataClient`, all four data types landed shards — `1d` → `month=2024-01/part-*.pqt`, `1m` → `date=2024-01-02/part-*.pqt`, `tick/quotes` → `data_type=quotes/date=2024-01-02/symbol=AAPL/part-*.pqt`, `tick/trades` → `data_type=trades/.../part-*.pqt`. Endpoints differ correctly per data type (`/stocks/bars`, `/stocks/quotes`, `/stocks/trades`); all four prepared URLs carried `asof=-`; none carried `feed=`. Vendor is selected by config: `ingest_alpaca.py` builds through `stock_acquisition_config(...)`/`stock_kline_config(...)` with no inline `AcquisitionConfig(...)`. **The live vendor round-trip is unexercised** — no APCA credentials on this machine — and is a declared `verification: backstop` truth in plans 02 and 07. Routed to human verification. |
| SC-6 | A fetch whose estimated volume exceeds the guard threshold refuses before issuing requests, and can be overridden explicitly | ✓ VERIFIED | `UniverseCatalog.estimate_acquisition_volume` / `assert_acquisition_volume_fits` exist with `force: bool = False` as a keyword-only named parameter (signature inspected live). Three independent ceilings — `MAX_RAW_BYTES = 21474836480`, `MAX_ACQUISITION_REQUESTS = 50000`, `MAX_ACQUISITION_WALL_CLOCK_HOURS = 4.0` — so a tick request that passes a row check still fails on bytes. Tick without `rows_per_symbol_day` refuses rather than guessing. Wired **before** the client in all three entry points, asserted structurally by AST line-number comparison (`guard < min(fetching)`), with `ingest_us_equity.py`'s zero-request `--stamp-legacy-watermarks` branch named as the one stated exception rather than silently tolerated. The override is not an always-true flag: `parse_args([]).force_volume is False` and `parse_args(["--force-volume"]).force_volume is True`, and no env-var escape hatch exists. 29 passed, exit 0. |
| SC-7 | Tiingo and Alpaca raw data land under separate vendor path segments and cannot be silently merged into one xarray | ✓ VERIFIED | Live path check: `stock_acquisition_config(vendor='tiingo')` → `.../nasdaq_data/tiingo`, `vendor='alpaca'` → `.../nasdaq_data/alpaca`; watermarks likewise split. Live merge-prevention check on a real two-vendor tree — (a) root pointed one level **above** both vendors raised on the basename assertion, (b) an unset `config.vendor` raised rather than scanning, (c) a foreign-vendor shard smuggled into the alpaca directory raised on the `vendor`-column assertion, (d) the correct single-vendor scan returned exactly the pre-refactor columns `['timestamp','symbol','open','high','low','close','volume']` (both `vendor` and the hive key dropped). Live pruning check: narrowing a 4-month window to one month reduced the polars plan from 4 sources to 1, and selected `month=2024-02` — correct by value, not by filename ordering. `enums.data.RAW_HIVE_KEYS` is the single definition read by both the writer (`base/acquisition.py:673`) and the reader (`dataset/stock.py:66`). |

**Score:** 7/7 truths verified (SC-5's live half closed by UAT against the real vendor)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `base/acquisition.py` | Batched primitive + orchestration + hive writer + no_data marker | ✓ VERIFIED | 88 KB; `_fetch_page` abstract, `_fetch_batch`/`_attempt_batch`/`_run_once`/`_write_shard`/`_hive_partition_values` concrete; imported by both vendors |
| `base/pageledger.py` | `PageLedger` with resume point, fingerprint, consistency assert | ✓ VERIFIED | 20 KB; `resume_point`, `assert_consistent`, `fingerprint`, `reset` all reached from `_fetch_batch` |
| `acquisition/alpaca.py` | `AlpacaAcquisition` + `_AlpacaMarketDataClient` | ✓ VERIFIED | 43 KB; real transport passes `params` to `requests.Session.get`, credentials read from `os.environ` in `__init__` only |
| `acquisition/tiingo.py` | Degenerate-batch vendor, `ConcurrentTiingoAcquisition` retired | ✓ VERIFIED | `hasattr(module,'ConcurrentTiingoAcquisition')` is False; `DEFAULT_BATCH_SIZE = 1`; `QUOTA_STATUS_CODES` reachable at that exact name |
| `acquisition/universe.py` | Volume guard sibling to the dense-panel guards | ✓ VERIFIED | `estimate_dense_panel`/`assert_dense_panel_fits`/`assert_chunked_panel_fits` all still present and untouched |
| `dataset/stock.py` | Vendor-root assertion, hive predicate, single-vendor drop | ✓ VERIFIED | All three defences fired in the live check |
| `enums/data.py` | `Vendor`, `RAW_HIVE_KEYS` | ✓ VERIFIED | `RAW_HIVE_KEYS` imported by both writer and reader |
| `base/config.py` | `AcquisitionConfig.vendor`, `DatasetConfig.vendor` | ✓ VERIFIED | Both fields present and required; no credential field on either |
| `config/__init__.py` | `vendor=` on the path factories | ✓ VERIFIED | Produces distinct vendor-terminated raw roots (live) |
| `utils/cli.py` | Shared arg groups defined once | ✓ VERIFIED | 25 KB; `add_window_args`/`add_universe_args`/`add_chunk_args`/`add_concurrency_args`/`add_volume_guard_args`/`resolve_symbols`; no ingest script redefines any of them (AST scan of all three) |
| `ingest_alpaca.py` | One top-level script per source | ✓ VERIFIED | `--help` exits 0; flags `--symbols --universe --as-of-date --start-date --end-date --force-volume --rows-per-symbol-day --frequency --data-type --batch-size --refresh`; no `--feed`, as intended |
| 5 new test files | Each with real self-tests, exit 0 not 5 | ✓ VERIFIED | 28 / 13 / 43 / 17 / 29 passed, each exit 0 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `Acquisition._fetch_page` (abstract) | `TiingoAcquisition._fetch_page`, `AlpacaAcquisition._fetch_page` | subclass override | ✓ WIRED | Only abstract method; both concrete subclasses implement it |
| `_fetch_batch` | `_write_shard` → `PageLedger.record_page` | strict ordering | ✓ WIRED | Shard write precedes ledger record in source; the crash window is covered by `test_a_re_fetched_page_is_idempotent_and_overwrites_its_shard` |
| `enums.data.RAW_HIVE_KEYS` | writer `_hive_partition_values` + reader `_hive_window_predicate` | single import | ✓ WIRED | One definition, two consumers; live write/read round-trip agrees |
| `config/__init__.py` factories | `AcquisitionConfig.raw_data_dir_path` → `StockDataset._assert_vendor_root` | basename equality | ✓ WIRED | Live check: correct root scans, parent root raises |
| `_attempt_batch` | `self._classify_error(exc)` → `failed`/`quota`/`rate_limited` | per-vendor override | ✓ WIRED | Tiingo 429→quota+global abort; Alpaca 429→rate_limited+in-worker backoff, no abort, not in manifest |
| `assert_acquisition_volume_fits` | each ingest `__main__`, before the client | call ordering | ✓ WIRED | AST line-number assertion in test; verified in source at 118<145, 293<348, 431<469 |
| `utils.cli.add_*` | 3 ingest scripts | import + call | ✓ WIRED | No script redefines any shared flag (AST scan) |
| `mock_alpaca_client` | `acquisition.alpaca._AlpacaMarketDataClient` | monkeypatch by dotted string | ✓ WIRED | Patches the **transport**, so vendor field-mapping and param construction are exercised, not bypassed |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `AlpacaAcquisition._fetch_page` | `frame` | `_AlpacaMarketDataClient.get_page` → `requests.Session.get(url, params=...)` | ✓ (live prepared URL observed) | ✓ FLOWING |
| `Acquisition._write_shard` | parquet shards | the fetched `frame`, projected through `RAW_COLUMNS` | ✓ (shards observed on disk in live run) | ✓ FLOWING |
| `PageLedger.resume_point` | `(page_index, page_token)` | the on-disk ledger sidecar | ✓ (resumed run's first call carried the recorded token) | ✓ FLOWING |
| `StockDataset._scan_raw` | `pl.LazyFrame` | `pl.scan_parquet` over the vendor-rooted hive tree | ✓ (live scan returned 4 rows, pruned to 1) | ✓ FLOWING |
| `UniverseCatalog.estimate_acquisition_volume` | rows/bytes/requests/hours | roster + calendar arithmetic, zero vendor requests | ✓ (dedicated test asserts no client is constructed) | ✓ FLOWING |

### Behavioral Spot-Checks

All run by the verifier, independent of the test suite where noted.

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Full suite | `uv run pytest -q` (exit code read directly) | 378 passed, exit 0 | ✓ PASS |
| D-02 hard gate (current HEAD) | `uv run pytest tests/test_tiingo_acquisition.py tests/test_tiingo_quota.py -q` | 51 passed, exit 0 | ✓ PASS |
| D-02 gate at **every** guarded commit | isolated `git worktree` + gate at 13 commits touching `base/acquisition.py`/`acquisition/tiingo.py` | exit 0 at all 13 (44/44/44/48/48/48/48/48/48/48/50/51/51) | ✓ PASS |
| Each new test file exits 0 not 5 | per-file `uv run pytest tests/<f>.py -q` | 28/13/43/17/29 passed, exit 0 each | ✓ PASS |
| Hierarchy introspection | `python -c` walk of `Acquisition.__subclasses__()` | 2 concrete subclasses; abstractmethods = `{_fetch_page}`; `_fetch_and_write` absent; `ConcurrentTiingoAcquisition` absent | ✓ PASS |
| SC-5 all four data types (independent) | patched `requests.Session.send`, real client, real `AlpacaAcquisition` | 4 correct hive shard paths; 4 correct endpoints; `asof=-` on every URL; no `feed=` | ✓ PASS |
| SC-7 merge prevention (independent) | real two-vendor parquet tree + `StockDataset._scan_raw` | root-above raises; unset vendor raises; mixed vendor column raises; correct root returns exact pre-refactor columns | ✓ PASS |
| SC-7 hive pruning (independent) | 4-month tree, narrowed window, `.explain()` | 4 sources → 1 source, correct month selected by value | ✓ PASS |
| SC-7 vendor path split (independent) | `stock_acquisition_config(vendor=...)` | `.../nasdaq_data/tiingo` vs `.../nasdaq_data/alpaca` | ✓ PASS |
| SC-6 guard surface | `inspect.signature` + ceiling constants | `force` is keyword-only with `False` default; 3 independent ceilings | ✓ PASS |
| CLI reachable | `uv run python ingest_alpaca.py --help` | exit 0, 11 flags, no `--feed` | ✓ PASS |
| `resolve_symbols` mode has no default | `resolve_symbols(ns, None)` | `TypeError: missing 1 required keyword-only argument: 'mode'` | ✓ PASS |
| Live Alpaca round-trip | — | no `APCA_*` env vars on this machine | ? SKIP → human (SC-5) |

### Probe Execution

| Probe | Command | Result | Status |
|-------|---------|--------|--------|
| — | `find scripts -path '*/tests/probe-*.sh'` | none found; no PLAN/SUMMARY declares a probe path | N/A — no probes declared for this phase |

### Requirements Coverage

This phase has no REQ-IDs. Per orchestrator instruction, SC-1..SC-7 are the coverage units.

| Coverage unit | Source Plans | Status | Evidence |
|---------------|--------------|--------|----------|
| SC-1 | 01, 02, 03 | ✓ SATISFIED | Truth SC-1 above |
| SC-2 | 01, 03 | ✓ SATISFIED | Truth SC-2 above; D-02 gate green at every guarded commit |
| SC-3 | 01, 02 | ✓ SATISFIED | Truth SC-3 above |
| SC-4 | 01, 05 | ✓ SATISFIED | Truth SC-4 above |
| SC-5 | 01, 02, 06, 07 | ? NEEDS HUMAN | Offline half fully satisfied and independently reproduced; live vendor round-trip requires credentials |
| SC-6 | 01, 04, 07 | ✓ SATISFIED | Truth SC-6 above |
| SC-7 | 01, 02, 06 | ✓ SATISFIED | Truth SC-7 above |

No orphaned coverage units: every SC-1..SC-7 appears in at least one plan's `requirements` frontmatter, and `.planning/REQUIREMENTS.md` maps no additional IDs to phase 03.2.

### Code Review Closure (independently spot-checked)

`03.2-REVIEW-FIX.md` claims 4 Critical + 9 Warning fixed. Spot-checked in code rather than accepted:

| Finding | Claim | Verifier evidence |
|---------|-------|-------------------|
| CR-01 `asof` never reached the wire | Fixed via `ASOF_NO_MAPPING = "-"` + `_ASOF_UNSET` | ✓ Confirmed at the **prepared URL** in my own independent run: all four requests carried `asof=-`. The covering test deliberately does not patch the transport class — it patches `requests.Session.send`, so it observes the same bytes the vendor would. |
| CR-02 schema inferred from 100 rows | Fixed via `infer_schema_length=None` | ✓ `acquisition/alpaca.py:822` — `pl.DataFrame(rows, infer_schema_length=None)` |
| CR-03 minute densification unguarded | `bars_per_day=390` sizing added | ✓ Documented and wired in `ingest_alpaca.py` header + `assert_dense_panel_fits` call |
| CR-04 tick rows duplicated across runs | `_clear_superseded_shards` | ✓ Method present in `base/acquisition.py:829`, gated by `_partition_is_per_symbol` |
| WR-02 repeated page token | Refusal added | ✓ `_fetch_batch` raises `ValueError` when `next_token == page_token`, with the ledger left resumable |
| WR-06 `_rate_limit_headers` dead code | Now called | ✓ Called in `_attempt_batch`'s first-backoff log line |
| WR-04 symbols reach paths before validation | `_validate_symbols` first | ✓ `_fetch_batch` line 1 is `symbols = self._validate_symbols(symbols)`; covered by `test_a_traversal_symbol_raises_before_any_watermark_path_is_opened` |

IN-01..IN-05 remain open by explicit scope decision (recorded, not a gap).

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | `TBD` / `FIXME` / `XXX` in any of the 28 phase-modified files | — | **None found.** Debt-marker gate passes. |
| — | — | `TODO` / `HACK` / `PLACEHOLDER` | — | None found |
| — | — | Hardcoded credential literals | — | None found. `APCA_API_KEY_ID`/`APCA_API_SECRET_KEY` are read via `os.environ.get` inside `_AlpacaMarketDataClient.__init__` and never assigned to any dataclass — CLAUDE.md 凭证安全 honoured. |
| `base/acquisition.py` | 700, 778 | `raise NotImplementedError` on the abstract base | ℹ️ Info | These are **configuration guards**, not stubs: `_session_date` refuses when a vendor declares no `SESSION_TIME_ZONE`, `_hive_key_expr` refuses an undeclared hive key. Both carry detailed remediation messages and are the loud-failure design SC-7 depends on. Noted only because the SC-1 contract test AST-scans `vars(cls)` of **concrete** subclasses, so a placeholder added to the abstract base later would not be caught there. |
| `acquisition/universe.py` | 6 | Docstring still cites the retired `_fetch_and_write(symbol, start, end)` primitive | ℹ️ Info | Stale prose only; zero behavioural impact. The module deliberately does not subclass `Acquisition`, so the paragraph's argument still holds — only its example signature is out of date. |
| `utils/cli.py` / ingest scripts | — | `--universe`/`--as-of-date`/`--chunk` are defined once but not called by *all three* scripts | ℹ️ Info | 03.2-07's must_have wording says "called by all three". In fact `add_window_args` and `add_volume_guard_args` are called by all three; `add_universe_args` is called by the two point-in-time scripts (`ingest_us_equity.py` uses its own semantically-different `--category` with an interval-overlap default), and `add_chunk_args` by the one script that chunks. D-14's actual guarantee — "there is no third copy" — holds: an AST scan of all three scripts shows none redefines any shared flag. Not a gap. |

### Human Verification Required

Three items, all credential-dependent, all recorded before this verification as `human_judgment: true` coverage entries in 03.2-07-SUMMARY.md, as a STATE blocker, and as `unrun-verify` rows 5–7 in `.planning/WINDOWS.md`. I confirmed independently that `env | grep -i APCA` returns nothing on this machine, so none of them is answerable here.

#### 1. Free-tier SIP access (open question O-1)

**Test:** One live request with a Basic-tier key pair:
```
curl -H "APCA-API-KEY-ID: $APCA_API_KEY_ID" -H "APCA-API-SECRET-KEY: $APCA_API_SECRET_KEY" \
  'https://data.alpaca.markets/v2/stocks/quotes?symbols=AAPL&start=2024-01-02T14:30:00Z&end=2024-01-02T14:31:00Z&feed=sip&limit=1'
```
**Expected:** 200 (Basic reaches historical SIP) or 403 (IEX only). Either answer resolves RESEARCH Assumption A7.
**Why human:** Alpaca's documentation self-conflicts; no credential here. Verified as honoured: no `feed` default reaches code (`acquisition/alpaca.py:773` defaults to `None` and omits the key), and no `--feed` flag exists (`ingest_alpaca.py --help` contains zero matches).

#### 2. Real Alpaca round-trip (SC-5's manual half)

**Test:** With credentials exported, run the three `ingest_alpaca.py` invocations in `behavior_unverified_items` above (daily, minute, tick/quotes), one symbol, one trading day.
**Expected:** Shards at `.../nasdaq_data/alpaca/month=…`, `.../1m/nasdaq_data/alpaca/date=…`, `.../tick/nasdaq_data/alpaca/data_type=quotes/date=…/symbol=AAPL/` — the exact paths I reproduced offline — plus watermark sidecars under `_watermarks/alpaca/`. The vendor must accept `asof=-`.
**Why human:** No credentials. `ASOF_NO_MAPPING = "-"` is an unverified sentinel labelled as such in code; if wrong it fails loud (4xx per batch, isolated, recorded in the failure manifest), not silently.

#### 3. Real maximum symbols per request (RESEARCH Assumption A2)

**Test:** Probe upward from 100 symbols in one `symbols=` param until the vendor rejects.
**Expected:** The empirical ceiling. `DEFAULT_BATCH_SIZE = 100` is a conservative working value until then.
**Why human:** Requires live vendor responses.

### Gaps Summary

**No gaps.** Every artifact exists, is substantive, is wired, and carries real data flow. Six of seven success criteria are verified — five of them by evidence I generated myself rather than by reading a SUMMARY or trusting a green suite:

- I re-ran the D-02 hard gate at **every one of the 13 commits** that touched the guarded files, in an isolated worktree, and read exit codes directly. All 13 returned 0, and the test count never fell to 0 (the exit-5 "no tests ran" trap this phase's own notes warn about).
- I reproduced SC-5's four data types end-to-end against the **real** `_AlpacaMarketDataClient`, patching only `requests.Session.send`, so the field maps, the endpoint map, the session-date hive key and the query-string encoding were all exercised — this is the boundary CR-01 proved matters, and `asof=-` was present on every prepared URL.
- I reproduced SC-7's two independent defences and its directory pruning on a real two-vendor parquet tree, and confirmed the pruning selects by hive **value**, not by filename ordering.

Given this phase's documented history of tests passing for the wrong reason, I specifically checked the recorded failure shapes and found each one guarded: the SC-1 contract test asserts `len(classes) >= 2` before scanning; the SC-3 resume test asserts the token value *and* the call count (restart-vs-resume); the SC-4 marker has both a positive and a negative test so neither is satisfiable alone; the `--force-volume` gate is asserted False-by-default and True-when-passed; the guard-ordering test compares AST line numbers rather than checking mere presence; and the `asof` assertion was deliberately moved off the params dict onto the prepared URL.

The single non-verified criterion, SC-5, is not a defect — it is the deliberate, pre-recorded credential boundary. Its offline half is complete; its live half is one of three `verification: backstop` truths that must abstain to human review rather than silently pass. Status is therefore `human_needed`, not `passed` and not `gaps_found`.

**Not reported as gaps** (recorded, deliberate decisions, each confirmed honoured in code): no `feed` default and no `--feed` flag; `ASOF_NO_MAPPING` as an unverified but loud-failing sentinel; no D-13 migration path (documented as a separate optional resumable invocation in `ingest_alpaca.py`); tick → xarray/Zarr conversion deferred with no tick `Dataset` subclass; IN-01..IN-05 open by scope decision; the pre-existing intermittent KunRunner teardown hang (did not recur — the full suite completed in 15.86s).

---

_Verified: 2026-09-06T21:15:00Z_
_Verifier: Claude (gsd-verifier)_


---

## Post-Verification Update — 2026-09-06T21:40:00Z

Status moved `human_needed` → `passed`. The three credential-dependent items were run
against the live Alpaca API during `/gsd-verify-work 03.2`; all three passed and are
recorded with their findings in `03.2-UAT.md`. SC-5 is therefore verified end to end and
the score is 7/7.

What the live run settled that offline verification could not:

- **O-1 answered YES.** Basic (free) tier reaches historical SIP, with a 15-minute embargo
  on recent data. Evidence: `X-Ratelimit-Limit: 200` (Basic; Algo Trader Plus is 10,000);
  historical SIP → 200 with two-sided consolidated quotes (`ax:"P"`, tape `z:"C"`) where
  IEX for the same window is one-sided with ask 0; recent SIP inside the embargo → 403.
- **`ASOF_NO_MAPPING = "-"` is accepted by the vendor** (200 + real data). The sentinel is
  no longer unverified. Control: `asof=notadate` → 400 `invalid asof`, proving the
  parameter is genuinely validated rather than ignored — so CR-01's fix is confirmed at
  the wire, not merely at the params dict.
- **WR-01's nanosecond fix confirmed on real data:** 9,988 of 10,000 tick rows carry
  non-zero sub-microsecond digits (epoch `1704186000004465115`, ending `115` ns).
- **D-19 #7 confirmed:** timestamp values are naive UTC (09:00:00 UTC = 04:00 ET
  pre-market open); only the partition key converts to the session date.
- **A2 answered, but not as a symbol count.** The binding limit is URL length (~64 KB):
  8,000 distinct symbols → 200 (54,650 B); 10,000 → 414 Request-URI Too Large (68,650 B).

## Follow-ups (new information, not gaps)

Neither is a defect and neither belongs to 03.2's scope; both are now actionable because
the questions they waited on are answered:

1. A `feed` default may now be written into code (O-1 resolved).
2. `AlpacaAcquisition.DEFAULT_BATCH_SIZE = 100` is ~2 orders of magnitude conservative.
   Raising it divides request counts and changes the volume guard's request/wall-clock
   estimates. Express the ceiling as URL length, not a symbol count.
