---
phase: "03.8"
plan: "04"
type: execute
wave: 4
depends_on: ["03.8-03"]
files_modified:
  - quantlab/utils/backtest_report.py
  - tests/test_backtest_report.py
autonomous: true
requirements: [BT-01]
gap_closure: true
gap_ids: [G-03.8-1]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The year-by-month heatmap uses a red-green diverging scale: a loss month is red, a gain month is green, and a flat month is a neutral grey. Green means up and red means down (the Western convention, per the user's 2026-09-19 clarification of G-03.8-1)"
    - "`zmid=0.0` stays on the heatmap, so zero always maps to the grey midpoint of the scale whatever the run's range. A run with only gains therefore renders every cell in the green half"
    - "Months the run did not cover stay null and render blank: the colour change does not turn on `connectgaps`, and the existing null-cell locks stay green without being edited"
    - "Each monthly bar is coloured by its own sign: above 0 takes the gain colour, below 0 the loss colour, exactly 0 the neutral colour. The zero rule follows the heatmap, where z=0 lands on the grey midpoint"
    - "The bars and the heatmap draw their colours from ONE set of module-level constants (`GAIN_COLOUR`, `LOSS_COLOUR`, `NEUTRAL_COLOUR`). A test reads the heatmap's own colorscale stops back from the page and compares them with the bar colours, so the two panels cannot drift apart"
    - "Nothing else on the page changes: the main figure still draws exactly five traces, the pixel budget holds, the bar row stays (D-04), the run directory still holds seven files, and the module is still a leaf"
  artifacts:
    - path: "quantlab/utils/backtest_report.py"
      provides: "GAIN_COLOUR / LOSS_COLOUR / NEUTRAL_COLOUR, RETURN_COLOURSCALE, _sign_colour, the heatmap on RETURN_COLOURSCALE, bars with per-sign marker colours"
      contains: "RETURN_COLOURSCALE"
    - path: "tests/test_backtest_report.py"
      provides: "the colour locks: hue direction of each colorscale stop, zmid, bar colour matched to value sign and to the heatmap's stops"
      contains: "_rgb"
  key_links:
    - from: "quantlab/utils/backtest_report.py::GAIN_COLOUR / LOSS_COLOUR / NEUTRAL_COLOUR"
      to: "quantlab/utils/backtest_report.py::_monthly_heatmap_div"
      via: "RETURN_COLOURSCALE is built from the three constants and passed as the heatmap's `colorscale`, with zmid=0.0"
    - from: "quantlab/utils/backtest_report.py::GAIN_COLOUR / LOSS_COLOUR / NEUTRAL_COLOUR"
      to: "quantlab/utils/backtest_report.py::_add_monthly_returns"
      via: "`_sign_colour` maps each compounded monthly value to one of the three constants, which become the bar trace's `marker.color` list"
---

<objective>
Close UAT gap G-03.8-1. The user reported "热力图的颜色应该使用红绿" (the heatmap should
use red and green). Their binding clarification: green is a gain and red is a loss (the
Western convention), and the monthly bar row must be coloured the same way by sign so it
matches the heatmap. Keep the `zmid=0` diverging centre, and keep uncovered cells blank.

Purpose: the phase goal promises a reader who can "see how each month of each year
performed" at a glance. The UAT passed everything else on the page (rendering, month
alignment, year order, blank cells, the delta column). Only the palette failed: the heatmap
is on plotly's named red-blue scale, whose top end is blue, and the bars share one untinted
default colour.

Output: two edits to the leaf module (three shared colour constants plus the colorscale
built from them, and per-sign bar colours) and their locks in `tests/test_backtest_report.py`.
The page gains no file, no trace and no figure.

**Scope fence.** `example/backtest.md` is NOT edited. It never describes the heatmap's
palette, so there is nothing to sync. Its `report.html` row at line 447 is out of date in
other ways (no delta column, no heatmap, wrong page order), but that is REVIEW finding
WR-02, a separate item. Do not fold it in here. `tests/test_backtest_persistence.py` is not
edited either: the real run calls the same leaf function, so the leaf locks cover it, and
the persistence suite is re-run only as a regression gate.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03.8-backtest-report-in-out-of-sample-delta-column-monthly-return/03.8-UAT.md
@.planning/phases/03.8-backtest-report-in-out-of-sample-delta-column-monthly-return/03.8-03-SUMMARY.md
@quantlab/utils/backtest_report.py
</context>

<environment_notes>
- **Never** wrap a verify command in `timeout` or `gtimeout`. Neither exists on this machine.
- A `-k` selector that matches nothing exits **5** and prints `N deselected`. It never prints
  `no tests ran`. Measured 2026-09-19 before this plan: `uv run pytest
  tests/test_backtest_report.py -q -k colour` gives `72 deselected`, because no current test
  name contains `colour`. Every new test in this plan MUST carry `colour` in its name so the
  selector is meaningful.
- Baseline measured 2026-09-19: `uv run pytest tests/test_backtest_report.py -q` gives
  `72 passed`.
- Worktrees are enabled. Every verify command below uses a RELATIVE path. Run it from YOUR
  working root, and never `cd` into `/Users/daizhaorong/projects/quantlab`. From an isolated
  worktree that path would test the unchanged main tree and return a meaningless green.
  Before the first test run, confirm that `uv run python -c "import quantlab, sys;
  print(quantlab.__file__)"` resolves inside your own working root.
- The phase gate is the full suite, re-run in the MAIN tree after the merge.
- Verified against the installed plotly 5.24.1. A list colorscale serializes into the
  embedded JSON unchanged, as `[[0.0,"#e03b30"],[0.5,"#f0efec"],[1.0,"#1b8a5a"]]`, and a
  per-point bar colour list serializes as `"marker":{"color":[...]}`. A NAMED scale such as
  the current red-blue one serializes EXPANDED, as `rgb(r,g,b)` strings (its top stop is
  `rgb(5,48,97)`, a blue). The test helper must therefore parse both `#rrggbb` and
  `rgb(r,g,b)`, so the RED run fails on a real assertion instead of on a parse error.
</environment_notes>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer - shared gain/loss/neutral colours drive the heatmap's diverging scale end to end</name>
  <files>quantlab/utils/backtest_report.py, tests/test_backtest_report.py</files>

  <read_first>
    - quantlab/utils/backtest_report.py lines 1-70 (the module docstring; item 5 describes the heatmap)
    - quantlab/utils/backtest_report.py lines 276-285 (`SPAN_COLOUR` and its comment block). This is the house style for a module-level colour constant: British `COLOUR` spelling, a `#:` comment that says WHY the value is what it is
    - quantlab/utils/backtest_report.py lines 417-498 (`MONTH_LABELS`, `HEATMAP_CAPTION`, `_monthly_grid`, `_monthly_heatmap_div`). Line 483 holds the named red-blue colorscale this task replaces
    - tests/test_backtest_report.py lines 561-627 (`HEATMAP`, `_second_figure`, `_second_figure_traces`, `_dated_returns`, `_multi_year_returns`, `_non_null_cells`). The new test reuses these helpers and adds no new parser
    - tests/test_backtest_report.py lines 663-688 (the two null-cell locks). They must stay green WITHOUT being edited, because they are the proof that blank cells survive the palette change
  </read_first>

  <behavior>
    - `_write(tmp_path, returns=_multi_year_returns())` gives a page whose second figure's
      heatmap trace has `colorscale` with exactly three stops, at positions `[0.0, 0.5, 1.0]`.
    - The stop at 0.0 (the most negative value) is a RED: its red channel is greater than
      both its green and its blue channel.
    - The stop at 1.0 (the most positive value) is a GREEN: its green channel is greater
      than both its red and its blue channel.
    - The stop at 0.5 (zero, because of zmid) is a light NEUTRAL GREY: its largest and
      smallest channels differ by at most 16, and its smallest channel is at least 200.
    - The heatmap trace's `zmid` is `0` (equal to 0.0).
    - The heatmap trace does NOT set `connectgaps` to true: `heatmap.get("connectgaps")` is
      None or False. connectgaps would interpolate across the null cells, and the user
      requires uncovered months to stay blank.
    - RED first: the test fails against the current code on the GREEN assertion for the
      1.0 stop. The named red-blue scale's top stop is `rgb(5,48,97)`, which is blue.
  </behavior>

  <action>
Per UAT gap G-03.8-1 and the user's binding clarification (green is a gain, red is a loss,
keep `zmid=0`, keep blank cells), put the three colours in one place and build the heatmap's
scale from them. The bars will use the same colours in Task 2.

**Test first (RED).** Add a module-level test helper `_rgb(colour)` to
`tests/test_backtest_report.py`, next to the heatmap helpers. It returns an `(r, g, b)` tuple
of ints and accepts both `#rrggbb` and `rgb(r, g, b)` (optional whitespace after commas).
Plotly expands named scales into `rgb(...)` strings, so the RED run must reach the colour
assertions instead of dying in the parser. Use the already-imported `re`; import nothing new.

Then add `test_the_heatmap_colours_losses_red_gains_green_and_zero_grey`. It asserts every
bullet in `<behavior>` against `_second_figure_traces(html)[HEATMAP]`, where `html` is
`_write(tmp_path, returns=_multi_year_returns())`. Give it a docstring that cites G-03.8-1
and explains the design: the hue checks assert the DIRECTION of the scale (red at the low
end, green at the high end), not particular hex values, so a later re-tune of the shades
stays green while a swapped pair goes red. Run `uv run pytest tests/test_backtest_report.py
-q -k colour` and confirm it FAILS on the green assertion for the 1.0 stop. Record that
failure in the summary.

**Implementation (GREEN).** In `quantlab/utils/backtest_report.py`, add three module-level
constants in a block placed just before `MONTH_LABELS`, in the `SPAN_COLOUR` style:
`GAIN_COLOUR = "#1b8a5a"`, `LOSS_COLOUR = "#e03b30"` and `NEUTRAL_COLOUR = "#f0efec"`.
Then add `RETURN_COLOURSCALE`, a tuple of three `(position, colour)` pairs:
`(0.0, LOSS_COLOUR)`, `(0.5, NEUTRAL_COLOUR)`, `(1.0, GAIN_COLOUR)`.

The `#:` comment must state these facts:
- Green means up and red means down. The user chose this Western convention on 2026-09-19
  (G-03.8-1); it is deliberately NOT the East-Asian red-up convention.
- The bars and the heatmap both read these constants, so the two panels of the same numbers
  cannot disagree about colour.
- The midpoint is a neutral grey, not a hue. A diverging scale's centre must read as
  "nothing happened", and `zmid=0.0` pins zero to it.
- The two poles have matched luminance (about 0.19 each), so neither sign looks heavier.
  The pair passes the dataviz palette validator, including red-green colour-vision
  separation (deutan dE 9.4, above the 8 target). This matters because red/green is the
  classic colour-blind confusion pair. Sign is also carried without colour: bars point up
  or down, and every heatmap cell states its percentage on hover.

In `_monthly_heatmap_div`, replace the named colorscale argument with
`colorscale=RETURN_COLOURSCALE`. Keep `zmid=0.0`, `colorbar={"tickformat": ".1%"}`,
`hoverongaps=False`, the hovertemplate, `name`, `x`, `y`, `z` and the whole layout exactly
as they are. Do not add `connectgaps`, `zmin` or `zmax`: zmid already centres the scale
symmetrically, and pinning bounds would undo that.

Update the prose to match. Add one sentence to `_monthly_heatmap_div`'s docstring stating
the palette and pointing at `RETURN_COLOURSCALE`. In the module docstring, extend item 5 to
say the heatmap is red for a loss, green for a gain and grey at zero.

Make no project-internal import: the module stays a leaf (locked by
`test_the_module_is_a_leaf`).
  </action>

  <verify>
    <automated>uv run pytest tests/test_backtest_report.py -q -k colour</automated>
    <fails_when>exit code 5 with `deselected` and no `passed` (the new test's name lacks `colour`), or any non-zero exit from an assertion failure</fails_when>
    <automated>uv run pytest tests/test_backtest_report.py -q</automated>
    <fails_when>non-zero exit, or fewer than 73 passed (72 baseline + 1 new). A red null-cell lock means the palette change disturbed the blank cells</fails_when>
  </verify>

  <acceptance_criteria>
    - `grep -c '^GAIN_COLOUR = "#1b8a5a"' quantlab/utils/backtest_report.py` prints 1
    - `grep -c '^LOSS_COLOUR = "#e03b30"' quantlab/utils/backtest_report.py` prints 1
    - `grep -c '^NEUTRAL_COLOUR = "#f0efec"' quantlab/utils/backtest_report.py` prints 1
    - `grep -c 'colorscale=RETURN_COLOURSCALE' quantlab/utils/backtest_report.py` prints at least 1
    - `grep -c 'zmid=0.0' quantlab/utils/backtest_report.py` prints at least 1 (the heatmap call keeps it; the comment block may also mention it)
    - `grep -c 'def _rgb' tests/test_backtest_report.py` prints 1
    - `grep -c 'def test_the_heatmap_colours_losses_red_gains_green_and_zero_grey' tests/test_backtest_report.py` prints 1
    - The summary records the RED run: the failing assertion, and the `rgb(5,48,97)` top stop it saw
    - `test_the_heatmap_leaves_the_months_before_a_november_start_null` and `test_the_heatmap_leaves_the_months_after_a_february_end_null` pass with no edit: `git diff "$PLAN_BASE"..HEAD -- tests/test_backtest_report.py` shows no hunk inside either function, where `PLAN_BASE` is the SHA that `git rev-parse HEAD` printed before this plan's first commit (record it at the start of the task)
  </acceptance_criteria>

  <done>The report's heatmap renders red for a loss month, green for a gain month and grey at zero. The scale is centred on 0 and uncovered months stay blank. The colours come from three module constants that Task 2's bars will reuse, and a hue-direction lock goes red if the scale is ever reversed or goes back to red-blue.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Colour each monthly bar by its sign, from the same constants as the heatmap</name>
  <files>quantlab/utils/backtest_report.py, tests/test_backtest_report.py</files>

  <read_first>
    - quantlab/utils/backtest_report.py lines 366-414 (`_monthly_series` and `_add_monthly_returns`). `_monthly_series` drops NaN before compounding, so every value `_add_monthly_returns` sees is finite
    - quantlab/utils/backtest_report.py lines 288-315 (`_add_drawdown_span`'s docstring on why this module never raises: the report is written inside the run's staging directory, so an exception deletes the ENTIRE run)
    - tests/test_backtest_report.py lines 473-477 and 532-558 (`_traces`, which parses the FIRST figure, and the three existing monthly-bar tests that must stay green)
    - tests/test_backtest_report.py lines 715-727 (`test_the_heatmap_cells_equal_the_monthly_bars`, the existing cross-figure lock that the new consistency test mirrors)
    - tests/test_backtest_report.py lines 786-803 (`test_the_figure_draws_exactly_these_five_traces`). Colouring the bars must not add a trace
  </read_first>

  <behavior>
    - Returns on 2024-01-10 (+0.05), 2024-02-12 (-0.03) and 2024-03-11 (exactly 0.0), built
      with `_dated_returns`, give three bars. Their `marker.color` list equals `[the
      heatmap's 1.0 stop colour, its 0.0 stop colour, its 0.5 stop colour]`, in that order.
      Both sides are read from the SAME page: bars through `_traces(html)["monthly_return"]`,
      stops through `_second_figure_traces(html)[HEATMAP]["colorscale"]`. The test therefore
      proves the two panels share colours without importing the module's constants.
    - For a longer mixed-sign series (one return per month, 2023-01 to 2024-12, alternating
      in sign, with at least one exact 0.0 month), every bar's colour matches its own `y`
      value's sign under the same three-way rule, and the colour list is exactly as long
      as `y`.
    - A bar with `y > 0` never takes the loss colour, and a bar with `y < 0` never takes
      the gain colour. This follows from the equality above and needs no separate assertion.
    - The three existing monthly-bar tests, the exactly-five-traces lock and
      `test_the_heatmap_cells_equal_the_monthly_bars` stay green unchanged.
    - RED first: with the current code, `bars.get("marker", {}).get("color")` is None, so
      the equality assertion fails with no KeyError.
  </behavior>

  <action>
Per G-03.8-1 (the user wants the monthly bar row coloured green/red by sign, consistent with
the heatmap), give the bars the heatmap's colours.

**Test first (RED).** Add two tests to `tests/test_backtest_report.py`, next to the Task 1
colour test. Both names must contain `colour`:
- `test_each_monthly_bar_takes_the_heatmap_colour_of_its_sign` covers the three-month
  case in `<behavior>`.
- `test_every_monthly_bar_colour_follows_the_sign_of_its_value` covers the long
  mixed-sign series.

Read the bar colours with `.get` chains, so the RED run fails on an assertion rather than a
KeyError. Build the expected colours from the heatmap stops of the same page. The tests must
not import the module's colour constants: comparing against the other figure is what locks
the two panels together, and the file's only import from the report module stays the
existing `write_backtest_report` line. Run `uv run pytest tests/test_backtest_report.py -q -k colour` and
confirm the two new tests FAIL (the Task 1 test stays green). Record that in the summary.

**Implementation (GREEN).** In `quantlab/utils/backtest_report.py`, add a private helper
`_sign_colour(value: float) -> str` next to the colour constants. It returns `GAIN_COLOUR`
when `value > 0`, `LOSS_COLOUR` when `value < 0`, and `NEUTRAL_COLOUR` otherwise. Its
docstring must state the zero rule and the reason for it: a month that compounds to exactly
0.0 (a month spent wholly in cash, for example) lands on the heatmap's grey midpoint under
`zmid=0`, so the bar takes the same grey. The bar has zero height, so the grey is invisible
there, but pinning the rule keeps the two panels identical by construction. The docstring
must also say that a NaN fails both comparisons and falls through to neutral rather than
raising. `_monthly_series` already drops NaN, so none should arrive, but this module must
never raise: the report is written inside the run's staging directory. Use no `try/except`
and no dict lookups that could KeyError.

In `_add_monthly_returns`, pass the bar trace `marker={"color": [...]}`, where the list holds
`_sign_colour(value)` for each value of `monthly.values`, in the same order as `y`. Leave
`x`, `y`, `name="monthly_return"`, the hovertemplate and `row=3, col=1` untouched. Add no
trace and do not change the layout. D-04 keeps the bar row; this task only tints it.

Update the prose. `_add_monthly_returns`' docstring says each bar is coloured by sign from
the constants the heatmap uses. The module docstring's item 4 says the monthly-return bars
are green for a gain and red for a loss, matching the heatmap below.

The module stays a leaf. `example/backtest.md` stays untouched (see the objective's scope
fence).
  </action>

  <verify>
    <automated>uv run pytest tests/test_backtest_report.py -q -k colour</automated>
    <fails_when>exit code 5 with `deselected` and no `passed`, or fewer than 3 passed (1 from Task 1 + 2 new), or any assertion failure</fails_when>
    <automated>uv run pytest tests/test_backtest_report.py -q</automated>
    <fails_when>non-zero exit, or fewer than 75 passed (72 baseline + 3 new)</fails_when>
    <automated>uv run pytest tests/test_backtest_persistence.py tests/test_backtest_run.py tests/test_backtest_run_cv.py tests/test_backtest_metrics.py tests/test_backtest_report.py tests/test_backtest_engine.py tests/test_backtest_contracts.py -q</automated>
    <fails_when>non-zero exit, or the summary reports any failed test. The persistence suite covers the real run's report.html and the seven-file run directory</fails_when>
    <automated>uv run python -m compileall -q quantlab</automated>
    <fails_when>non-zero exit</fails_when>
    <human-check>Re-run UAT test 1 for G-03.8-1. Open a real `report.html` in a browser; the UAT used the `overlap_run` artifact from `tests/test_backtest_persistence.py`, and any real run directory also works. Confirm that loss months are red and gain months green in BOTH the heatmap and the monthly bar row, that a near-zero heatmap cell reads as a light neutral grey, and that uncovered months are still blank. Tests lock the stop colours and the sign-to-colour mapping, but they cannot judge whether the page reads well. This is recorded as a human check rather than a blocking checkpoint, matching 03.8-03's precedent and the user's preference that routine GSD steps run without stopping. Put the outcome in the summary; the orchestrator's `/gsd-verify-work` re-run of the UAT is the binding confirmation.</human-check>
  </verify>

  <acceptance_criteria>
    - `grep -c 'def _sign_colour' quantlab/utils/backtest_report.py` prints 1
    - `grep -c '_sign_colour(' quantlab/utils/backtest_report.py` prints at least 2 (the definition plus the call in `_add_monthly_returns`)
    - `grep -c 'def test_each_monthly_bar_takes_the_heatmap_colour_of_its_sign' tests/test_backtest_report.py` prints 1
    - `grep -c 'def test_every_monthly_bar_colour_follows_the_sign_of_its_value' tests/test_backtest_report.py` prints 1
    - `grep -E '^from quantlab.utils.backtest_report import' tests/test_backtest_report.py` prints exactly one line, `from quantlab.utils.backtest_report import write_backtest_report` (the tests read colours from the rendered page, never from the module)
    - `git diff --name-only "$PLAN_BASE"..HEAD -- . ':!.planning'` (with `PLAN_BASE` as recorded in Task 1) lists only `quantlab/utils/backtest_report.py` and `tests/test_backtest_report.py`
    - The summary records the RED run of the two new tests and the human-check outcome
  </acceptance_criteria>

  <done>Every monthly bar is green for a gain, red for a loss and grey at exactly zero, using the same three colours as the heatmap's stops, and a test reads both figures from one page to prove it. The report, backtest and persistence suites are green, the page still has five main-figure traces, and the run directory still holds seven files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| report rendering -> staging directory | The report is written inside the run's `.partial` staging directory, so ANY exception deletes every artifact of a run that already succeeded |
| module constants -> plotly JSON | Colour strings enter the embedded plotly payload. They are literals defined in the module, never caller-supplied text |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.8-04-01 | Denial of Service | `_sign_colour` and the colour list in `_add_monthly_returns` | high | mitigate | Pure `>` / `<` comparisons on a float with a neutral fall-through. A NaN fails both and returns `NEUTRAL_COLOUR` instead of raising. No dict lookup, no `try/except`, no new input path. Locked by the exact-zero-month test and by the existing no-returns / one-month / all-NaN tests, which must stay green |
| T-03.8-04-02 | Tampering | integrity of the displayed signal: bar and heatmap colours disagreeing | medium | mitigate | One constant set feeds both panels. `test_each_monthly_bar_takes_the_heatmap_colour_of_its_sign` reads the heatmap's stops and the bar colours from the SAME rendered page and asserts they match by sign, and the hue-direction lock goes red on a swapped or reverted scale |
| T-03.8-04-03 | Tampering | colour strings reaching the plotly JSON | low | accept | The values are module-level hex literals. No caller or run data can reach them, and plotly serializes them into its own JSON |
| T-03.8-SC | Tampering | package-manager installs | n/a | accept | No package installed. The change uses only the already-imported `plotly.graph_objects`, and no `uv add` or `pyproject.toml` change is made |
</threat_model>

<verification>
- `uv run pytest tests/test_backtest_report.py -q -k colour` exits 0 with 3 passed
- `uv run pytest tests/test_backtest_report.py -q` exits 0 with at least 75 passed
- The seven-suite command (persistence, run, run_cv, metrics, report, engine, contracts) exits 0
- `uv run python -m compileall -q quantlab` exits 0
- Both RED runs are recorded: Task 1 failed on the blue top stop, and Task 2's two tests failed on the missing bar colours
- The human check of a rendered `report.html` is recorded
- After the merge, the orchestrator re-runs the full `workflow.test_command` in the MAIN tree
</verification>

<success_criteria>
- G-03.8-1 closed: the heatmap is red for a loss and green for a gain, with a grey midpoint at zero (zmid=0 kept)
- The monthly bars are coloured by sign with the same constants, so the two panels agree by construction and by test
- Uncovered heatmap months are still blank, and the null-cell locks pass unedited
- No trace, figure, file or project-internal import was added; D-04's bar row stays
- `example/backtest.md` untouched (WR-02 is a separate item)
</success_criteria>

<output>
Create `.planning/phases/03.8-backtest-report-in-out-of-sample-delta-column-monthly-return/03.8-04-SUMMARY.md` when done.
Record: the two RED runs (failing assertion and observed value), the final pass counts
worktree-local, the human-check verdict, and that G-03.8-1 is ready for UAT re-test.
</output>
