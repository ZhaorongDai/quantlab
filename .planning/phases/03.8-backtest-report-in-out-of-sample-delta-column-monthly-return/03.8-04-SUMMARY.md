---
phase: "03.8"
plan: "04"
subsystem: backtest-report
status: complete
gap_closure: true
gap_ids: [G-03.8-1]
tags: [report, plotly, heatmap, palette, gap-closure]
requires: ["03.8-03"]
provides:
  - "GAIN_COLOUR / LOSS_COLOUR / NEUTRAL_COLOUR module constants"
  - "RETURN_COLOURSCALE (red -> grey -> green) on the year-by-month heatmap, zmid=0.0 kept"
  - "_sign_colour and per-sign marker colours on the monthly bar row"
affects: [quantlab/utils/backtest_report.py, tests/test_backtest_report.py]
tech-stack:
  added: []
  patterns: ["one module-level colour set feeds both panels; tests compare the panels on the rendered page, not against imported constants"]
key-files:
  created: []
  modified:
    - quantlab/utils/backtest_report.py
    - tests/test_backtest_report.py
decisions:
  - "Western convention: green is a gain, red is a loss (user, 2026-09-19, G-03.8-1)"
  - "Heatmap and bars share GAIN/LOSS/NEUTRAL constants; a month that is exactly 0.0 is neutral grey in both panels"
  - "The hue lock checks the scale's direction (red low, green high, light grey middle), not hex values"
metrics:
  duration: "~12 min"
  completed: 2026-09-19
actuals:
  tokens: 6000
  tasks: 2
  commits: 4
plan_head_before: 39958881a063c69b5f72eeab200dd9aa7fd59718
---

# Phase 03.8 Plan 04: Red-green report palette (G-03.8-1) Summary

The heatmap now uses a red-grey-green diverging scale (`RETURN_COLOURSCALE`) instead of plotly's red-blue scale, and each monthly bar is coloured by its sign. Both panels take their colours from the same three module constants (`#1b8a5a` gain, `#e03b30` loss, `#f0efec` neutral). Months outside the run stay blank.

## Tasks

| Task | Name | Commits |
|------|------|---------|
| 1 (tracer, TDD) | Shared colours drive the heatmap's diverging scale | a6a5fd6 (RED test), 3057ed3 (GREEN feat) |
| 2 (TDD) | Colour each monthly bar by sign from the same constants | 665c39e (RED test), 3719368 (GREEN feat) |

## TDD evidence (real pytest runs, worktree-local)

- **Task 1 RED** (`uv run pytest tests/test_backtest_report.py -q -k colour`): `1 failed, 72 deselected`.
  `AssertionError: the 1.0 (gain) stop is not green: [1.0, 'rgb(5,48,97)']` / `assert (48 > 5 and 48 > 97)`.
  The named RdBu scale's top stop was expanded to `rgb(5,48,97)`, which is blue, as the plan predicted.
- **Task 1 GREEN**: the full report suite gave `73 passed`. The tracer gate re-ran verify end to end and it passed, so the task expanded.
- **Task 2 RED**: `2 failed, 1 passed, 72 deselected`. Both new tests failed on
  `assert None == ['#1b8a5a', '#e03b30', '#f0efec']` and `isinstance(None, list)`, because the bar trace had no `marker.color`. They failed on the assertion, not with a KeyError.
- **Task 2 GREEN**: `-k colour` gave `3 passed, 72 deselected`, and the full report suite gave `75 passed`.

## Verification (worktree-local)

- `uv run pytest tests/test_backtest_report.py -q`: **75 passed** (72 baseline + 3 new).
- Seven-suite regression (persistence, run, run_cv, metrics, report, engine, contracts): **182 passed** (329 warnings, all pre-existing).
- `uv run python -m compileall -q quantlab`: ok.
- `quantlab.__file__` resolved inside the worktree before the first test run.
- Acceptance greps all match. The test file has exactly one `from quantlab.utils.backtest_report import write_backtest_report`. `git diff 3995888..HEAD` touches only the two planned files. The test-file hunks are pure additions (at @@ 616 and @@ 727), so the two null-cell locks are unedited and still pass.

## Human check (G-03.8-1 visual re-test)

**Not done in this worktree.** Plotly image export (`kaleido`) is not installed, and installing packages is out of bounds, so the page could not be rendered for viewing here. The rendered page's JSON was checked instead: the colorscale reads `[[0.0,"#e03b30"],[0.5,"#f0efec"],[1.0,"#1b8a5a"]]` and the bar colours match each bar's sign. The binding check is the orchestrator's `/gsd-verify-work` UAT re-run: open a real `report.html` (for example the `overlap_run` artifact from `tests/test_backtest_persistence.py`) and confirm three things. Loss months are red and gain months green in both the heatmap and the bar row. A near-zero cell reads as light grey. Uncovered months are blank. **G-03.8-1 is ready for UAT re-test.**

## Deviations from Plan

**1. [Rule 1 - test ordering] The Task 1 test checks the end stops' hue before the stop count**
- **Found during:** the Task 1 RED run
- **Issue:** Asserting positions `[0.0, 0.5, 1.0]` first made the RED run fail on the stop count, because RdBu expands to 11 stops. The plan requires the RED failure to land on the green assertion for the top stop.
- **Fix:** The test checks the hue of `stops[0]` and `stops[-1]` first, then the position list, then the grey midpoint. It checks the same things as before.
- **Commit:** a6a5fd6

No other deviations. `example/backtest.md` and `tests/test_backtest_persistence.py` were not touched (scope fence; WR-02 stays separate).

## Threat mitigations

- T-03.8-04-01: `_sign_colour` uses only `>` / `<` comparisons and falls through to neutral, with no dict lookup and no try/except. A NaN returns neutral instead of raising. The existing no-returns, one-month and all-NaN tests still pass.
- T-03.8-04-02: the bar colours are read from the same page as the heatmap stops and must match (`test_each_monthly_bar_takes_the_heatmap_colour_of_its_sign`, `test_every_monthly_bar_colour_follows_the_sign_of_its_value`). The hue-direction lock catches a swapped or reverted scale.

## Known Stubs

None.

## Self-Check: PASSED

- FOUND: quantlab/utils/backtest_report.py, tests/test_backtest_report.py
- FOUND commits: a6a5fd6, 3057ed3, 665c39e, 3719368
