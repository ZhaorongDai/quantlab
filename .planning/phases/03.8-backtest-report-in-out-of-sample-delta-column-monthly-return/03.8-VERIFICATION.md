---
phase: 03.8-backtest-report-in-out-of-sample-delta-column-monthly-return
verified: 2026-09-18T18:59:52Z
status: human_needed
score: 26/26 plan must-have truths verified; 4/5 goal truths verified, 1 routed to human (visual reading of the heatmap)
covered_files:
  - ".planning/REQUIREMENTS.md"
  - ".planning/phases/03.8-backtest-report-in-out-of-sample-delta-column-monthly-return/03.8-01-PLAN.md"
  - ".planning/phases/03.8-backtest-report-in-out-of-sample-delta-column-monthly-return/03.8-01-SUMMARY.md"
  - ".planning/phases/03.8-backtest-report-in-out-of-sample-delta-column-monthly-return/03.8-02-PLAN.md"
  - ".planning/phases/03.8-backtest-report-in-out-of-sample-delta-column-monthly-return/03.8-02-SUMMARY.md"
  - ".planning/phases/03.8-backtest-report-in-out-of-sample-delta-column-monthly-return/03.8-03-PLAN.md"
  - ".planning/phases/03.8-backtest-report-in-out-of-sample-delta-column-monthly-return/03.8-03-SUMMARY.md"
  - "example/backtest.md"
  - "quantlab/backtest/engine_vectorbt.py"
  - "quantlab/base/backtest.py"
  - "quantlab/utils/backtest_report.py"
  - "tests/test_backtest_engine.py"
  - "tests/test_backtest_metrics.py"
  - "tests/test_backtest_persistence.py"
  - "tests/test_backtest_report.py"
  - "tests/test_backtest_run_cv.py"
covered_digest: "v1:sha256:987435684bff607ac862d61f64b68eb93eb6f98cc14ae11a067672f52b2ed07e"
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "在浏览器中打开一次真实回测生成的 report.html（例如 tests/test_backtest_persistence.py 的 overlap_run 产物，或任一真实 run 目录）"
    expected: "热力图能渲染；每个格子的年月与上方月度柱对齐；RdBu 色阶正负两个方向都可读（zmid=0）；最早年份在最上面；未覆盖月份为空白而非 0；差值列 out_of_sample - in_sample 在表格中可读"
    why_human: "测试只锁定了格子数值、trace 名、年/月标签与像素预算，无法判断页面是否'一眼可读'——而阶段目标的措辞正是 at a glance。03.8-03-PLAN 以 <human-check> 显式把这一项推迟到阶段末，03.8-03-SUMMARY 记录其尚未执行。"
---

# Phase 03.8: 回测报告——样本内外差值列、月度收益热力图、持仓级交易统计 验证报告

**Phase Goal:** 报告读者能一眼比较样本内与样本外、看到每年每月的表现、并信任交易统计。指标表新增纯按值类型驱动（无硬编码指标清单）的 `out_of_sample - in_sample` 差值列；年 x 月收益热力图与保留的月度柱并列；交易指标整体切换到持仓视图（每个标的一次建仓到清空算一笔，消除部分减仓造成的约 6.5 点胜率高估），并在 `whole` 块新增 `order_count`。
**Verified:** 2026-09-18T18:59:52Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths（目标级，来自 ROADMAP goal）

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | 指标表有 `out_of_sample - in_sample` 差值列，纯按类型驱动、无硬编码指标清单 | ✓ VERIFIED | `quantlab/utils/backtest_report.py:571-599` `_delta`（先排除 bool，再 `numbers.Real` + `math.isfinite`，无任何指标名）；`_metrics_section:633-647` 仅在 in_sample 与 out_of_sample 两键都存在时加第 4 列；真实 run 普查 `test_report_delta_census_pins_which_rows_carry_a_number` 同时锁定"有数"集合与"破折号"集合 |
| 2 | 年 x 月热力图加入，月度柱保留 | ✓ VERIFIED（数值层面） | `_monthly_series`（共享）→ `_add_monthly_returns`（柱，row=3 保留）与 `_monthly_heatmap_div`（`go.Heatmap`，第二个 div，`include_plotlyjs=False`），`write_backtest_report:237-239` 接入 `_document`；`test_report_carries_the_monthly_heatmap_in_a_second_div` 在真实 run 上断言 `Plotly.newPlot(` 恰好 2 次、热力图非空格子集合 == 月度柱月份集合 |
| 3 | 交易指标整体为持仓视图，消除部分减仓造成的胜率高估 | ✓ VERIFIED | `engine_vectorbt.py:329` 单次 `replace(trades_type="positions").stats()`；`:174` `SimulationResult.trades` 取自 `pf.positions.records_readable`；quantlab/ 下已无 `TRADE_STATS_METRICS` / `"positions"` 嵌套块残留；`test_the_fixture_really_diverges_lot_level_from_position_level` 证明减仓夹具上 lot 级与持仓级胜率确实不同且报告的是持仓级 |
| 4 | `whole` 块有 `order_count`，成交活动仍被报告 | ✓ VERIFIED | `quantlab/base/backtest.py:1570` `whole["order_count"] = int(simulation.orders.sizes.get("order", 0))`；`test_slice_order_counts_partition_the_whole_run` 断言 whole == in + out == 订单记录数；`test_whole_order_count_is_zero_for_an_order_less_simulation` 覆盖空订单 |
| 5 | 读者能"一眼"读懂（热力图可读、与柱对齐、色阶两向可读） | ? UNCERTAIN → 人工 | 视觉可读性无法程序化判断；PLAN 03 的 `<human-check>` 显式推迟，SUMMARY 03 记录未执行 |

### Plan must_haves（26 条）

| Plan | Truths | Status | 关键证据 |
|------|--------|--------|----------|
| 03.8-01 | 8/8 | ✓ | 持仓视图（engine 测试 3 条）；无嵌套 `positions`（`test_the_whole_block_carries_the_trade_metrics_with_no_nested_positions_block`、`test_metrics_json_carries_one_trade_view_and_no_nested_positions_block`）；`SimulationResult.trades` 来自 positions；对账恒等式以统一口径恢复而非删断言（`test_backtest_metrics.py:582-586`）；`whole.order_count` 分区恒等式；14 个组合级指标值不变（`test_the_portfolio_level_metrics_are_unchanged_by_the_positions_switch`，从未 replace 的组合现场重算对比）；无新抽象钩子（`test_abstract_method_sets_are_exact` 单独复跑通过）；运行目录 7 个文件名精确集合（四处锁均绿） |
| 03.8-02 | 9/9 | ✓ | 表头陈述算式（`DELTA_HEADER`）；类型谓词含比率指标；喂原始 pandas/numpy 对象的叶子锁（Timedelta/Timestamp/NaT/np.float64 nan/np.int64/np.float32）；bool 对为破折号；单边键与 None 块为破折号不抛异常；仅 whole 时无第 4 列；D-05 仅报告（真实 run 断言 metrics.json 无该串，wandb `_flatten_numeric` 只读 metrics 对象，差值从不进入该对象）；叶子模块（`import numbers`，无 numpy/quantlab 导入，`test_the_module_is_a_leaf` 通过）；通用性锁 `test_a_mapping_with_every_shipped_key_deleted_still_renders` 通过 |
| 03.8-03 | 9/9 | ✓ | 热力图与柱并存；独立 `go.Figure` 第二 div；共享 `_monthly_series`（`test_the_heatmap_cells_equal_the_monthly_bars`）；`period.month - 1` 以值层面锁定（`test_the_heatmap_puts_a_march_return_in_the_march_column` 等）；未覆盖月份为 None（11 月起始 / 2 月结束两条锁）；空/None 收益无 div 且页面仍写出；两个测试文件均有 `_second_figure_traces`；主图五 trace 与像素预算锁保持绿；运行目录文件集不变 |

**Score:** 26/26 plan truths verified；目标级 4/5 verified，1 项（视觉可读性）转人工。behavior_unverified = 0。

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `quantlab/backtest/engine_vectorbt.py` | 单次 `replace(trades_type="positions")`；trades 来自 positions；删除 `TRADE_STATS_METRICS` | ✓ VERIFIED | :174, :329；grep 无残留 |
| `quantlab/base/backtest.py` | `whole["order_count"]` | ✓ VERIFIED | :1570 |
| `quantlab/utils/backtest_report.py` | `_delta`、`DELTA_HEADER`、第 4 列；`_monthly_series`/`_monthly_grid`/`_monthly_heatmap_div`；`go.Heatmap` | ✓ VERIFIED | :98, :366-498, :571-647, :687-720 |
| `tests/test_backtest_engine.py` | 一套口径的锁（减仓夹具） | ✓ VERIFIED | :753-850 |
| `tests/test_backtest_metrics.py` | order_count 分区与恢复的交易计数恒等式 | ✓ VERIFIED | :347, :541-586 |
| `tests/test_backtest_report.py` | 差值叶子锁、`_second_figure_traces`、月份对齐锁 | ✓ VERIFIED | :328-461, :585-786 |
| `tests/test_backtest_persistence.py` | 真实 run 差值普查、真实 run 热力图第二 div | ✓ VERIFIED | :745, :762, :875 |
| `example/backtest.md` | 描述一套交易口径 + order_count | ✓ VERIFIED（但见 WR-01/WR-02） | :342, :355-373 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `engine_vectorbt.py::_simulate` | `base/backtest.py::_period_record_stats` | `SimulationResult.trades` 持仓口径 | ✓ WIRED | 对账恒等式 `closed 相加 == whole Total Closed Trades` 在测试中通过 |
| `_compute_metrics` | `_flatten_numeric` | 泛型摊平 | ✓ WIRED | `_flatten_numeric` 收 int → `whole/order_count` 进入 wandb summary |
| `_delta` | `_cell` | 返回 None 由 `_cell` 渲染破折号 | ✓ WIRED | `_metrics_section:640` `_cell(delta)` |
| `_report_and_persist` | `_metrics_section` | 原始 metrics 对象 | ✓ WIRED | `backtest.py:1804-1808`：`to_jsonable(metrics)` 只写 metrics.json，`write_backtest_report` 收原对象 |
| `_add_monthly_returns` | `_monthly_series` | 共享 helper | ✓ WIRED | :401 |
| `_monthly_heatmap_div` | `_document` | 第二 plotly 片段插入主图与指标表之间 | ✓ WIRED | :237-239, :715-719；`test_the_heatmap_div_sits_between_the_figure_and_the_metrics_table` |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| 热力图 | `z` | `simulation.returns` → `_monthly_series` → `_monthly_grid` | 是（真实 run 测试断言非空格子 == 柱月份） | ✓ FLOWING |
| 差值列 | `delta` | 原始 in_sample/out_of_sample 块 | 是（真实 run 普查锁定有数行集合） | ✓ FLOWING |
| `whole.order_count` | orders 维度大小 | vectorbt 订单记录 | 是 | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| 7 个回测套件 + universe 套件 | `uv run pytest -q tests/test_backtest_{persistence,run,run_cv,metrics,report,engine,contracts}.py tests/test_universe_filtered_factor.py` | 263 passed，pytest exit 0 | ✓ PASS |
| 叶子模块 / 抽象钩子集合 | `uv run pytest -q tests/test_backtest_report.py::test_the_module_is_a_leaf tests/test_backtest_contracts.py -k "leaf or abstract_method_sets_are_exact"` | 2 passed | ✓ PASS |

与编排者测得的 263 一致。全量套件中 55 个失败经编排者证明与阶段前集合完全相同，不属本阶段回归，未重复运行。

### Probe Execution

Step 7c: SKIPPED（本阶段计划未声明 probe 脚本）。

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| BT-01 | 03.8-01/02/03 | 基于 vectorbt 的向量化回测打通：输入目标持仓，输出回测结果（收益曲线、关键指标等） | ✓ 已说明（相邻，未关闭） | 本阶段加强"关键指标/回测结果"；REQUIREMENTS.md:153 仍为 Partial（Phase 6），符合 ROADMAP"sharpens, does not close" 的表述 |

无孤立需求：REQUIREMENTS.md 中没有映射到 03.8 的 REQ-ID。

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `quantlab/base/backtest.py` / `example/backtest.md` | 1491-1493 / 355-356 | REVIEW WR-01：声称段末未平仓数"同理"逐段相加等于 whole，实际不成立（测试只锁最后一段 == whole） | ⚠️ Warning | 文档错误；不影响数值，但可能误导后续维护者"修"正确代码 |
| `example/backtest.md` | 447 | REVIEW WR-02：report.html 描述未提差值列与热力图，页面顺序写成"先表后图" | ⚠️ Warning | 文档与实际页面不符；不影响报告本身 |
| `quantlab/utils/backtest_report.py` | 592-599 | REVIEW IN-02：`math.isfinite` 对超大 int / Fraction 抛 OverflowError，与"never raise"承诺不符 | ℹ️ Info | 真实指标不会出现；理论上可删 run 目录 |
| `quantlab/utils/backtest_report.py` | `_cell` | deferred-items：非 float 子类的 numpy 浮点绕过 `.6g`/NaN 处理（阶段前已存在） | ℹ️ Info | 真实 metrics 为 float64，未出现 |

无 TBD/FIXME/XXX 债务标记（三个实现文件 grep 为空）。两个 Warning 均为文档/注释问题，不削弱任何 must_have：代码行为与测试锁都是正确的。

### Human Verification Required

### 1. 热力图与差值列的浏览器目视检查

**Test:** 在浏览器中打开一份真实回测的 `report.html`。
**Expected:** 热力图能渲染；格子月份与上方月度柱对齐；RdBu 色阶正负方向都可读；最早年份在最上；未覆盖月份为空白；差值列清晰可读。
**Why human:** 测试锁定了数值、标签与像素预算，但无法判断"一眼可读"。PLAN 03 以 `<human-check>` 推迟到阶段末，SUMMARY 03 明确记录未执行。

### Gaps Summary

没有阻塞性缺口。三项交付（持仓级交易统计 + `whole.order_count`、类型驱动的差值列、第二 div 热力图）都在代码中实际存在，已接入，并由行为测试（含真实 run 层面的锁）证明。D-01..D-05 都得到遵守：差值谓词纯按类型判断，作用于原始对象；持仓口径全页统一，嵌套块已删除；D-03 的 NaT 问题按约定留在范围外；月度柱保留；差值只出现在报告里。

状态为 `human_needed`，原因只有一个：热力图在浏览器中的视觉可读性。建议另开 quick task 修正 WR-01（对账声明写错）和 WR-02（report.html 描述过时），不阻塞本阶段。

---

_Verified: 2026-09-18T18:59:52Z_
_Verifier: Claude (gsd-verifier)_
