---
phase: 03.8-backtest-report-in-out-of-sample-delta-column-monthly-return
verified: 2026-09-19T15:15:00Z
status: passed
score: 32/32 plan must-have truths verified; 6/6 goal truths verified (including G-03.8-1 palette, checked on a rendered page)
covered_files:
  - ".planning/REQUIREMENTS.md"
  - ".planning/phases/03.8-backtest-report-in-out-of-sample-delta-column-monthly-return/03.8-01-PLAN.md"
  - ".planning/phases/03.8-backtest-report-in-out-of-sample-delta-column-monthly-return/03.8-01-SUMMARY.md"
  - ".planning/phases/03.8-backtest-report-in-out-of-sample-delta-column-monthly-return/03.8-02-PLAN.md"
  - ".planning/phases/03.8-backtest-report-in-out-of-sample-delta-column-monthly-return/03.8-02-SUMMARY.md"
  - ".planning/phases/03.8-backtest-report-in-out-of-sample-delta-column-monthly-return/03.8-03-PLAN.md"
  - ".planning/phases/03.8-backtest-report-in-out-of-sample-delta-column-monthly-return/03.8-03-SUMMARY.md"
  - ".planning/phases/03.8-backtest-report-in-out-of-sample-delta-column-monthly-return/03.8-04-PLAN.md"
  - ".planning/phases/03.8-backtest-report-in-out-of-sample-delta-column-monthly-return/03.8-04-SUMMARY.md"
  - "example/backtest.md"
  - "quantlab/backtest/engine_vectorbt.py"
  - "quantlab/base/backtest.py"
  - "quantlab/utils/backtest_report.py"
  - "tests/test_backtest_engine.py"
  - "tests/test_backtest_metrics.py"
  - "tests/test_backtest_persistence.py"
  - "tests/test_backtest_report.py"
  - "tests/test_backtest_run_cv.py"
covered_digest: "v1:sha256:25f8b8a370cb8c5e0afac74890f5f26d337aaf71be58383158f53c84df6b7b76"
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: "26/26 plan truths; 4/5 goal truths (1 routed to human)"
  gaps_closed:
    - "G-03.8-1: the year-by-month heatmap uses red for losses, green for gains and grey at zero, and the monthly bars are coloured by sign from the same constants"
    - "Goal truth 5 (readable at a glance): the UAT passed every other item, and the palette is now confirmed on a rendered page"
  gaps_remaining: []
  regressions: []
---

# Phase 03.8: 回测报告：样本内外差值列、月度收益热力图、持仓级交易统计（验证报告）

**Phase Goal:** 报告读者一眼就能比较样本内与样本外、看到每年每个月的表现，并且可以信任交易统计。具体包括四项：
- 指标表新增 `out_of_sample - in_sample` 差值列，完全按值的类型决定是否计算，不维护硬编码的指标清单；
- 新增年 × 月收益热力图，原有的月度柱保留；
- 交易指标全部改用持仓口径，消除部分减仓导致的约 6.5 个点的胜率高估；
- `whole` 块新增 `order_count`。

（以上目标取自 ROADMAP 在 dc5909d 时的版本。当前 ROADMAP.md 已经没有这一节，见下文“阶段外问题”。）

**Verified:** 2026-09-19T15:15:00Z
**Status:** passed
**Re-verification:** 是。起因是 UAT 发现的缺口 G-03.8-1，由缺口补丁计划 03.8-04 关闭。

## Goal Achievement

### Observable Truths（目标级）

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | 指标表有 `out_of_sample - in_sample` 差值列，完全按类型驱动 | ✓ VERIFIED（回归检查） | `backtest_report.py:100` `DELTA_HEADER`，`:636` `_delta`。report 与 persistence 两个套件均为绿 |
| 2 | 新增年 × 月热力图，月度柱保留 | ✓ VERIFIED（回归检查） | `:368` `_monthly_series`、`:510` `_monthly_heatmap_div`、`:752` `_document`。渲染截图中两个面板都在，并且逐月对齐 |
| 3 | 交易指标统一为持仓口径 | ✓ VERIFIED（回归检查） | `engine_vectorbt.py:329` `replace(trades_type="positions")`，`:174` `pf.positions.records_readable`。`TRADE_STATS_METRICS` 在 quantlab/ 下无残留 |
| 4 | `whole.order_count` | ✓ VERIFIED（回归检查） | `base/backtest.py:1570` |
| 5 | 热力图配色为红绿（G-03.8-1）：亏损红、盈利绿、零点灰；月度柱按正负号用同一组颜色 | ✓ VERIFIED | `backtest_report.py:442-452` 定义 `GAIN/LOSS/NEUTRAL_COLOUR` 和 `RETURN_COLOURSCALE`；`:456` `_sign_colour`；`:415` 柱子的 `marker.color`；`:548-549` 热力图 `colorscale=RETURN_COLOURSCALE, zmid=0.0`。3 个 `colour` 测试通过。**渲染核对**：headless Chrome 截图见下文，两个面板都是红跌绿涨 |
| 6 | 读者一眼能读懂：热力图渲染正常、与月度柱对齐、年份顺序正确、未覆盖月份留空、差值列可读 | ✓ VERIFIED | 03.8-UAT 测试 1 中，除配色外各项均由用户确认通过。配色这一项现已在渲染页上核对，详见下文 |

**Score:** 目标级 6/6。计划级 must-have 共 32 条（01:8、02:9、03:9、04:6），全部 verified。behavior_unverified = 0。

### Plan 03.8-04 must_haves（本轮完整核对）

| Truth | Status | Evidence |
|-------|--------|----------|
| 红绿发散色阶：亏损红、盈利绿、零点灰（西式约定） | ✓ | `RETURN_COLOURSCALE = ((0.0,LOSS),(0.5,NEUTRAL),(1.0,GAIN))`。`test_the_heatmap_colours_losses_red_gains_green_and_zero_grey` 断言的是色相方向，没有写死 hex |
| 保留 `zmid=0.0` | ✓ | `:549`。测试中断言 `heatmap["zmid"] == 0` |
| 未覆盖月份仍为 null、渲染为空白，`connectgaps` 未打开 | ✓ | 代码中没有 `connectgaps`；测试断言 `not heatmap.get("connectgaps")`。按 SUMMARY 所述，两个空格锁的 diff 只有新增、没有修改，且两者都通过。截图中 2023-01/02、2024-09..12 为空白 |
| 柱子按自身正负号着色，恰好为 0 时取中性色 | ✓ | `_sign_colour` 只做 `>`/`<` 比较，NaN 落到中性色。`test_every_monthly_bar_colour_follows_the_sign_of_its_value` 覆盖 24 个月，含两个零值 |
| 两个面板共用一组常量，测试从页面上的热力图 stops 反读颜色来比较 | ✓ | `_sign_colours_of_the_heatmap` 从第二个 figure 的 colorscale 读出颜色，再与柱子的 `marker.color` 比较 |
| 页面其他部分不变（主图 5 个 trace、像素预算、柱行、7 个文件、叶子模块） | ✓ | diff 3995888..HEAD 只涉及两个计划内文件。report、persistence、contracts 套件均为绿 |

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `quantlab/utils/backtest_report.py` | 三个颜色常量、`RETURN_COLOURSCALE`、`_sign_colour`、热力图与柱子接线 | ✓ VERIFIED | `:442-475`、`:415`、`:548` |
| `tests/test_backtest_report.py` | `_rgb`、色相方向锁、跨面板颜色锁 | ✓ VERIFIED | 新增的 3 个测试和 2 个 helper 都在 |
| 01-03 的产物（engine、base/backtest、各测试文件、example/backtest.md） | 与上轮一致 | ✓ VERIFIED（回归检查） | grep 与测试结果都与上轮一致 |

### Key Link Verification

| From | To | Via | Status |
|------|----|-----|--------|
| `GAIN/LOSS/NEUTRAL_COLOUR` | `_monthly_heatmap_div` | `RETURN_COLOURSCALE` → `colorscale=`，`zmid=0.0` | ✓ WIRED |
| `GAIN/LOSS/NEUTRAL_COLOUR` | `_add_monthly_returns` | `_sign_colour` → `marker={"color": [...]}` | ✓ WIRED |
| 上轮的 6 条连接（positions trades → 分段统计、`_delta` → `_cell`、`_monthly_series` → 柱与热力图、第二个 div → `_document` 等） | — | — | ✓ WIRED（回归检查） |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| 柱子颜色 | `marker.color` | `_monthly_series(returns).values` → `_sign_colour` | 是：每根柱子的颜色由自身数值决定 | ✓ FLOWING |
| 热力图颜色 | `z` + colorscale | `_monthly_grid` | 是 | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| 6 个回测套件（主树） | `uv run pytest -q tests/test_backtest_{report,persistence,engine,metrics,run_cv,contracts}.py` | 181 passed | ✓ PASS |
| `tests/test_backtest_run.py` | `uv run pytest -q tests/test_backtest_run.py` | 1 passed | ✓ PASS |
| 配色锁 | `uv run pytest -q tests/test_backtest_report.py -k colour` | 3 passed, 72 deselected | ✓ PASS |
| 在主树中导入 | `python -c "import quantlab; print(quantlab.__file__)"` | 解析到 `/Users/daizhaorong/projects/quantlab/quantlab/__init__.py` | ✓ PASS（不是 worktree 假绿） |
| 渲染页面目视核对 | 调用 `write_backtest_report` 生成 18 个月的混合收益，其中 2023-09 为全零月、2024-02 为近零月；把 plotly.js 内联后，用 headless Chrome 截图（不安装任何包） | 柱子：盈利月实心绿、亏损月实心红。热力图：红 → 灰 → 绿。2023-05 最深绿、2024-04 红，两个面板逐月一致。2023-09 为灰；未覆盖月份透出绘图区浅蓝灰背景 | ✓ PASS |

全量套件的结果由编排者提供，本轮没有重复运行：merge 后 55 failed / 1250 passed，merge 前 55 failed / 1246 passed，两次的失败列表完全相同，都是已存在的失败，与 03.8 无关。

### Probe Execution

Step 7c：SKIPPED。本阶段没有声明 probe。

### Requirements Coverage

| Requirement | Source Plan | Status | Evidence |
|-------------|-------------|--------|----------|
| BT-01（相邻） | 03.8-01..04 | ✓ 已说明 | 本阶段加强的是“关键指标 / 回测结果”，不关闭 BT-01（Partial，归 Phase 6）。没有映射到本阶段的孤立需求 |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `quantlab/base/backtest.py` / `example/backtest.md` | 1492 / 356 | WR-01（上轮遗留）：文档写“段末未平仓数同理逐段相加”，这个说法不成立 | ⚠️ Warning | 只是文档错误，数值正确 |
| `example/backtest.md` | 447 | WR-02（上轮遗留，本轮范围扩大）：`report.html` 的描述缺差值列、缺热力图、页面顺序写错、也没写配色约定 | ⚠️ Warning | 文档过时；03.8-04 的 scope fence 明确排除了这一项 |
| `quantlab/utils/backtest_report.py` | 404-406 | IN-06：docstring 说柱子与热力图“同色”，实际只是色相一致，深浅不一致。截图可以证实：+2% 的月份柱子是实心绿，热力图格子只是浅绿 | ℹ️ Info | 措辞需要改 |
| `quantlab/utils/backtest_report.py` | 444 | IN-07：零值灰 `#f0efec` 与空白格背景 `#E5ECF6` 对比度低。截图中勉强能分辨，一个偏暖、一个偏冷，但不醒目 | ℹ️ Info | 原来的 RdBu 中点也有同样的问题，不是本轮回归 |
| `quantlab/utils/backtest_report.py` | 662 / `_cell` | IN-02 / IN-03（上轮遗留） | ℹ️ Info | 真实指标中不会触发 |

两个实现文件中没有 TBD、FIXME、XXX。

### 阶段外问题（需要编排者或开发者处理，不影响本阶段结论）

**ROADMAP.md 被截断。** commit `bfea39d docs(03.9): capture phase context` 从 `.planning/ROADMAP.md` 删掉了 924 行：`### Phase` 小节从 16 个降到 4 个，Phase 03.8、03.7 以及 03.1–03.6 等小节都被删了。现在 `roadmap.get-phase 03.8` 查不到这个阶段，所以本报告的目标文本取自 `git show dc5909d:.planning/ROADMAP.md`。在把 03.8 标记为完成之前，应当从 dc5909d 恢复 ROADMAP.md，再叠加 d046062 对 03.9 的 2 行修改，并把 03.8-04 勾上。否则阶段完成工具和进度统计会读到残缺的路线图。

### Human Verification Required

无。上轮留下的人工项（热力图在浏览器中是否可读）已经关闭：UAT 中用户已确认除配色外的各项，配色本轮在 headless Chrome 渲染的页面上核对过。如果用户希望按自己的审美再确认一次（例如 IN-07 的零值与空白格对比度），属于可选操作，不阻塞本阶段。

### Gaps Summary

没有缺口。G-03.8-1 已在代码中关闭：三个共享颜色常量同时驱动热力图色阶和柱子的逐根颜色；`zmid=0` 和空白格保持不变；色相方向和跨面板一致性都有测试锁定，并在渲染页面上得到印证。01-03 的交付没有回归。

后续建议另开 quick task 处理 WR-01、WR-02 和 IN-06（都是文档或措辞问题）；IN-07 可选。

---

_Verified: 2026-09-19T15:15:00Z_
_Verifier: Claude (gsd-verifier)_
