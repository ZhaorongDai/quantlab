---
phase: "03.8"
plan: "03"
type: execute
wave: 3
depends_on: ["03.8-02"]
files_modified:
  - quantlab/utils/backtest_report.py
  - tests/test_backtest_report.py
  - tests/test_backtest_persistence.py
autonomous: true
requirements: [BT-01]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The report carries a year-by-month heatmap of compounded monthly returns, ADDED BESIDE the existing monthly bar row, which stays (D-04)"
    - "The heatmap is its own go.Figure rendered as a SECOND div with include_plotlyjs=False — never a fourth subplot row, which would bind the three datetime axes to a categorical month axis via matches='x4' (RESEARCH Pitfall 6)"
    - "The heatmap and the bar row consume ONE shared monthly-series helper, so the two panels of the same numbers cannot drift apart"
    - "A return in a known month lands in that month's column: month indexing is `period.month - 1`, and the lock asserts the x label of the single non-null cell rather than the grid's shape (RESEARCH Pitfall 7 — the off-by-one is otherwise invisible)"
    - "Months the run did not cover stay None, serialize to JSON null and render as empty cells — never as a fabricated 0.0"
    - "Empty or None returns produce NO heatmap div and the page is still written"
    - "`_second_figure_traces` exists in the test files, so the heatmap is reachable by a lock: both existing trace parsers anchor on the FIRST `Plotly.newPlot(` and are blind to it"
    - "The existing main-figure locks stay green untouched: the exactly-five-traces set and the axis-title pixel budget"
    - "The run directory still holds exactly the same seven file names — the heatmap lives inside report.html, never as a sibling file"
  artifacts:
    - path: "quantlab/utils/backtest_report.py"
      provides: "_monthly_series shared helper, _monthly_grid, _monthly_heatmap_div, and the second-div insertion point in _document"
      contains: "go.Heatmap"
    - path: "tests/test_backtest_report.py"
      provides: "_second_figure_traces parser and the month/year alignment locks"
      contains: "_second_figure_traces"
    - path: "tests/test_backtest_persistence.py"
      provides: "the real-run lock that report.html carries the heatmap in a second div"
  key_links:
    - from: "quantlab/utils/backtest_report.py::_add_monthly_returns"
      to: "quantlab/utils/backtest_report.py::_monthly_series"
      via: "both panels compound the same series through one helper — asserted equal in a test"
    - from: "quantlab/utils/backtest_report.py::_monthly_heatmap_div"
      to: "quantlab/utils/backtest_report.py::_document"
      via: "a second plotly fragment inserted verbatim between the main figure and the metrics table"
---

<objective>
Add a year-by-month heatmap of compounded monthly returns to the report, beside the
existing monthly bar row.

Purpose: the bars answer "when did P&L land" on the shared time axis; the grid answers
"which months of which years were good". D-04 keeps both — they answer different
questions and the cost is about 8 KB.

Output: one new div in `report.html`; three new leaf helpers; one new test parser without
which the chart would ship with zero coverage. No new file in the run directory.

Depends on plan 02 only because both modify `quantlab/utils/backtest_report.py`,
`tests/test_backtest_report.py` and `tests/test_backtest_persistence.py`; same-wave plans
must have zero file overlap. There is no logical dependency between the heatmap and the
delta column.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03.8-backtest-report-in-out-of-sample-delta-column-monthly-return/CONTEXT.md
@.planning/phases/03.8-backtest-report-in-out-of-sample-delta-column-monthly-return/03.8-RESEARCH.md
@.planning/phases/03.8-backtest-report-in-out-of-sample-delta-column-monthly-return/03.8-PATTERNS.md
@.planning/phases/03.8-backtest-report-in-out-of-sample-delta-column-monthly-return/03.8-VALIDATION.md
@.planning/phases/03.8-backtest-report-in-out-of-sample-delta-column-monthly-return/03.8-02-SUMMARY.md
</context>

<environment_notes>
Same as plans 01 and 02:
- **Never** wrap a verify command in `timeout` / `gtimeout` — neither exists on this machine.
- **MEASURED (2026-09-16):** a `-k` selector matching nothing exits **5**; pytest 9.1.1 prints
  `N deselected` and never the literal `no tests ran`. Verified:
  `uv run pytest tests/test_backtest_report.py -q -k heatmap` -> exit 5,
  `37 deselected in 0.02s`. Every `-k` `<fails_when>` below names exit 5 / `deselected`.
- Worktrees are enabled. The phase gate is the full suite re-run in the MAIN tree after merge.
</environment_notes>

<tasks>

<task type="auto">
  <name>Task 1: Extract the shared monthly-series helper without changing the bar panel</name>
  <files>quantlab/utils/backtest_report.py, tests/test_backtest_report.py</files>

  <read_first>
    - quantlab/utils/backtest_report.py lines 351-376 — `_add_monthly_returns` in full, including the docstring explaining why `index.to_period("M")` is used rather than a resample alias
    - tests/test_backtest_report.py lines 355-381 — `test_monthly_returns_are_grouped_by_calendar_month`, `test_a_one_month_run_is_a_single_bar`, `test_no_returns_means_no_monthly_trace`, which are the behaviour this refactor must preserve exactly
    - .planning/phases/03.8-.../03.8-PATTERNS.md § 2 — the four things to copy verbatim
  </read_first>

  <action>
Pure refactor, zero behaviour change, so the heatmap and the bars cannot later drift apart.

Extract a module-level helper `_monthly_series(returns)` returning the compounded monthly
`pd.Series` indexed by month period, or `None` when there is nothing to compute. Its body is
exactly the five lines currently inside `_add_monthly_returns`: the `returns is None` early
return; `returns.to_pandas().dropna()`; the `series.empty` early return;
`pd.DatetimeIndex(series.index)`; and
`(1.0 + series).groupby(index.to_period("M")).prod() - 1.0`.

All four of those guards are load-bearing and must be copied verbatim rather than
re-derived. The `.dropna()` in particular: without it, `prod()` over `1 + NaN` yields a
month of `0.0` — a fabricated flat month that is indistinguishable at read time from a real
one. Measured: without dropna the January key appears with value 0.0; with it, January is
absent. Keep `to_period("M")`, never a resample alias — the monthly alias was renamed
`M` -> `ME` across pandas versions while `to_period` reads the same in both, and the
existing docstring says so.

Move that reasoning into `_monthly_series`'s own docstring, since it is now the single place
the rule lives, and have `_add_monthly_returns` call it and return early when it gets None
or an empty series. `_add_monthly_returns` keeps its `go.Bar` trace exactly as it is: same
`x=[period.to_timestamp() for period in monthly.index]`, same `y`, same
`name="monthly_return"`, same `hovertemplate`, same `row=3, col=1`.

Do not add a test for the helper in isolation in this task — the three existing monthly
tests already pin the behaviour end to end, and their staying green IS the proof the
refactor is behaviour-preserving. That is the point of doing this as its own commit.
  </action>

  <verify>
    <automated>uv run pytest tests/test_backtest_report.py -q -k monthly</automated>
    <fails_when>exit code 5 with `deselected` and no `passed` in the summary (the three existing monthly tests were renamed or removed), or non-zero exit from a real assertion failure — which here means the refactor changed behaviour</fails_when>
    <automated>uv run pytest tests/test_backtest_report.py -q</automated>
    <fails_when>non-zero exit, or the summary reports any failed test</fails_when>
  </verify>

  <acceptance_criteria>
    - `quantlab/utils/backtest_report.py` contains `def _monthly_series`
    - `grep -c 'to_period("M")' quantlab/utils/backtest_report.py` returns 1 (the rule lives in exactly one place)
    - `grep -c 'dropna()' quantlab/utils/backtest_report.py` returns 1
    - `_add_monthly_returns` no longer contains a `groupby` call of its own
    - `uv run pytest tests/test_backtest_report.py -q -k monthly` exits 0 with 3 passed
    - This task's commit changed no test file: before editing, pin the pre-task SHA into `PRE_TASK_SHA`, then confirm `git diff "$PRE_TASK_SHA"..HEAD -- tests/` prints nothing. An explicit revision range is required — a range-less invocation prints nothing once the change is committed, so it would pass vacuously and could never fail. Write the range with a shell variable rather than an angle-bracket placeholder: the plan-structure R6 check reads the argument span only as far as the first angle bracket, so a bracketed placeholder is indistinguishable from a range-less command. Tests being untouched is what proves the extraction preserved behaviour, so this check has to be able to fail
  </acceptance_criteria>

  <done>`_monthly_series` is the single place the monthly compounding rule lives; `_add_monthly_returns` consumes it; all three existing monthly tests pass unchanged, which is what proves the extraction preserved behaviour.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The heatmap figure, its second div, and the parser that makes it testable</name>
  <files>quantlab/utils/backtest_report.py, tests/test_backtest_report.py</files>

  <read_first>
    - quantlab/utils/backtest_report.py lines 160-226 — how the main figure is built (`make_subplots(rows=3, shared_xaxes=True)`, the height rationale at 205-214) and emitted (`fig.to_html(full_html=False, include_plotlyjs="cdn")` at 222)
    - quantlab/utils/backtest_report.py lines 273-336 — `_add_drawdown_span`, the canonical guard/`.get`/`continue` discipline and its docstring explaining why an exception deletes the whole run
    - quantlab/utils/backtest_report.py lines 523-551 — `_document` and its docstring's rule about plotly fragments being inserted verbatim while everything else is escaped
    - tests/test_backtest_report.py lines 296-308 — `_traces` and `_layout`, both anchored on the FIRST `Plotly.newPlot(`
    - tests/test_backtest_report.py lines 30-61 — the `_value` / `_returns` / `_write` fixture pattern; note `_returns` puts all 12 bars inside ONE calendar month and is depended on by `test_the_figure_draws_exactly_these_five_traces`
    - tests/test_backtest_report.py lines 311-321, 392-444 — `test_every_trace_carries_a_name`, the exactly-five-traces lock and the pixel-budget lock
    - .planning/phases/03.8-.../03.8-RESEARCH.md § Pattern 2 and § Pitfall 6 and § Pitfall 7
  </read_first>

  <behavior>
    - ONE return of +25% in March 2024 -> the heatmap's single non-null cell sits at x label
      `03`. Under the off-by-one (`period.month` instead of `period.month - 1`) it would sit
      at `04` with nothing else differing, which is why the assertion is on the label and not
      on the grid's shape.
    - Two returns in the SAME month of DIFFERENT years (e.g. March 2023 and March 2024) ->
      two year rows, each with its non-null cell at x label `03`.
    - A run starting in November 2023 -> row `2023` has ten leading `None` cells that
      serialize to JSON `null`, never `0.0`.
    - A run ending in February 2025 -> row `2025` has ten trailing `null` cells.
    - `returns=None`, or an all-NaN series, or an empty series -> NO second
      `Plotly.newPlot(` on the page, and the page is still written and still starts with
      `<!DOCTYPE html>`.
    - The heatmap's z values for the months the bar panel also shows equal the bar panel's
      y values (both come from `_monthly_series`).
    - The heatmap trace carries a `name`.
    - The MAIN figure still draws exactly the five existing traces, and the layout still
      satisfies the per-row pixel budget — both unchanged.
  </behavior>

  <action>
Add the heatmap as a SECOND plotly figure and the parser that can see it.

**`_monthly_grid(monthly)`** -> `(years, z)`. `years` is the sorted set of
`period.year` over the series index; `z` is one row per year of 12 cells initialised to
`None`; each `(period, value)` writes `grid[row_of[period.year]][period.month - 1] = float(value)`.
`period.month` is 1-BASED — the `- 1` is the whole correctness question here. Say so in the
docstring, and say why it needs a value-level lock: an off-by-one still renders every cell,
still looks like a heatmap, and raises nothing; the numbers are simply in the wrong month.

**`_monthly_heatmap_div(returns)`** -> an HTML fragment, or the empty string when there is
nothing to draw. Call `_monthly_series(returns)`; if it is None or empty, return `""` —
mirroring the bar panel's early return, and for the same stated reason (this runs inside the
run's staging directory, where an exception costs the whole run). Otherwise build
`go.Figure(go.Heatmap(...))` with `z` from `_monthly_grid`, `x` the twelve two-digit month
labels `01`..`12`, `y` the year strings, a `name` (every trace on the page must carry one —
`test_every_trace_carries_a_name` and the persisted-report parsers both require it), a
diverging colorscale with `zmid=0.0`, and a hovertemplate showing year-month and the value as
a percentage. Reverse the y axis (`autorange="reversed"`) so the earliest year is on top,
the conventional reading order. Give it an explicit height derived from the number of year
rows rather than a bare constant, so a six-year run is not squashed. Return
`fig.to_html(full_html=False, include_plotlyjs=False)`.

`include_plotlyjs=False` is required, not stylistic: the main div already loads plotly.js
via `include_plotlyjs="cdn"`, so this adds ~8 KB instead of a second copy of the library,
and `test_the_report_is_one_self_contained_file_loading_plotly_from_the_cdn` keeps passing
(still exactly one file in the directory, still `cdn.plot.ly` present).

**Do NOT add a fourth subplot row.** The main figure is `make_subplots(rows=3,
shared_xaxes=True)`; measured on plotly 5.24.1, a fourth row makes plotly set
`matches='x4'` on `xaxis`, `xaxis2` and `xaxis3` — binding the equity, drawdown and
monthly-bar datetime axes to the heatmap's CATEGORICAL month axis. `shared_xaxes` is
figure-wide with no per-row opt-out. It would also force re-deriving `height=900` and
rewriting the per-row pixel-budget lock. Put the reason in `_monthly_heatmap_div`'s
docstring so nobody "simplifies" it back into the main figure.

**Threading it through:** `write_backtest_report` computes the second fragment and passes it
to `_document`, which inserts it between the main `div` and `_metrics_section(metrics)` —
picture, picture, numbers, notes. The second fragment is plotly's own and is inserted
VERBATIM, exactly like the first, per `_document`'s docstring rule. If a `<h2>` caption is
added above it, that caption must go through `_escape` like every other non-plotly string on
the page. An empty string from `_monthly_heatmap_div` must insert nothing — no empty heading,
no stray element.

**`_second_figure_traces(html)` in `tests/test_backtest_report.py`** — MANDATORY, and the
reason is the whole test story of this plan. Both existing parsers do
`html.index("[", html.index("Plotly.newPlot("))`, i.e. the FIRST figure. That is deliberate
and is what keeps the exactly-five-traces and pixel-budget locks meaningful and green with
zero edits — and it is exactly why the heatmap is invisible to every current lock. The new
parser finds the SECOND `Plotly.newPlot(` (`html.index("Plotly.newPlot(", first + 1)`), then
decodes the trace array from there, returning traces by name. Give it a docstring saying
this. Without it the chart ships untested.

**Fixtures:** a heatmap test spanning years needs its OWN multi-month/multi-year returns
fixture, built in the same style as `_returns` (`pd.DatetimeIndex` of real dates, values as
an `xr.DataArray` on `timestamp`). Do NOT modify `_returns` — it deliberately puts all 12
bars in one calendar month and `test_the_figure_draws_exactly_these_five_traces` and
`test_a_one_month_run_is_a_single_bar` both depend on that.

Write the tests RED first, one per `<behavior>` bullet, named so `-k heatmap` reaches them,
with the file's full-sentence naming convention and a docstring on each saying what would
break.
  </action>

  <verify>
    <automated>uv run pytest tests/test_backtest_report.py -q -k heatmap</automated>
    <fails_when>exit code 5 with `deselected` and no `passed` in the summary (the new tests were not named to include `heatmap`), or non-zero exit from a real assertion failure</fails_when>
    <automated>uv run pytest tests/test_backtest_report.py -q -k "exactly_these_five_traces or axis_title_room"</automated>
    <fails_when>exit code 5 with `deselected` and no `passed` (one of the two pre-existing locks was renamed or deleted), or non-zero exit — which here means the heatmap was added to the MAIN figure instead of its own div</fails_when>
    <automated>uv run pytest tests/test_backtest_report.py -q</automated>
    <fails_when>non-zero exit, or the summary reports any failed test</fails_when>
  </verify>

  <acceptance_criteria>
    - `quantlab/utils/backtest_report.py` contains `go.Heatmap` and `include_plotlyjs=False`
    - `grep -c 'rows=4' quantlab/utils/backtest_report.py` returns 0
    - `grep -c 'make_subplots' quantlab/utils/backtest_report.py` returns 1 (the main figure only)
    - `quantlab/utils/backtest_report.py` contains `period.month - 1`
    - `grep -c 'try:' quantlab/utils/backtest_report.py` returns 0
    - `grep -c 'import quantlab\|from quantlab' quantlab/utils/backtest_report.py` returns 0
    - `tests/test_backtest_report.py` contains `def _second_figure_traces`
    - `uv run pytest tests/test_backtest_report.py -q -k heatmap` exits 0 with at least 6 passed
    - `uv run pytest tests/test_backtest_report.py -q` exits 0, and the two named pre-existing locks are among the passes
  </acceptance_criteria>

  <done>The report carries a second div holding a year-by-month heatmap whose cells are proved to land in the right month and the right year; uncovered months are null and never fabricated zeros; empty or None returns emit no div and still write the page; the main figure's trace set and pixel budget are untouched; `_second_figure_traces` exists so the chart is reachable by a lock.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the heatmap on a real run, verify the alignment mutation, close the phase</name>
  <files>tests/test_backtest_persistence.py</files>

  <read_first>
    - tests/test_backtest_persistence.py lines 684-745 — `_report_html`, `_report_traces` (also anchored on the FIRST newPlot) and `test_report_has_equity_and_drawdown_and_shades_the_in_sample_range` with its exact five-trace set
    - tests/test_backtest_persistence.py lines 90-103 — `D24_ARTIFACTS`, the exact run-directory file set
    - tests/test_backtest_report.py — `_second_figure_traces` as written in task 2, to mirror it here
    - .planning/phases/03.8-.../03.8-VALIDATION.md § "The two failure modes that would otherwise pass silently", item 2, and § Manual-Only Verifications
  </read_first>

  <action>
Mirror `_second_figure_traces` into `tests/test_backtest_persistence.py` (the two test
files each carry their own parsers by convention; do not import across test modules) and add
one integration lock against the existing `overlap_run` fixture asserting that a REAL run's
`report.html` carries the heatmap in a second div, that its trace carries the expected name,
and that its z grid has 12 columns and one row per year the run spans.

Also assert in the same test that the run directory still holds exactly `D24_ARTIFACTS` —
the heatmap must live INSIDE `report.html` and never as a sibling file. That set is asserted
identically in four files (`tests/test_backtest_persistence.py:97`,
`tests/test_backtest_run.py:123`, `tests/test_backtest_run_cv.py:601`,
`tests/test_universe_filtered_factor.py:936`); adding any run-directory file turns all four
red at once.

**Run the alignment mutation from VALIDATION and record the outcome in the summary:** change
`period.month - 1` to `period.month` in `_monthly_grid`. The single-month alignment test
from task 2 MUST turn red, and ideally little else. If it stays green, that test is asserting
shape rather than position and must be rewritten before this task is done — shape assertions
(12 columns, N rows, the right non-null count) all survive the off-by-one. Restore the source
afterwards.

Then run the phase-closing verification: the seven-suite command and the
`tests/test_universe_filtered_factor.py` cross-check.

**Worktree caveat, stated per the phase gate:** if this phase executed in an isolated
worktree, a green here is NOT the gate. Re-run both commands in the MAIN tree after the merge
and record both results in the summary. A verify command that resolves against the main
repository from inside a worktree tests unmodified code and reads as a false green; every
command in this phase is repo-relative precisely to avoid that.
  </action>

  <verify>
    <automated>uv run pytest tests/test_backtest_persistence.py -q -k heatmap</automated>
    <fails_when>exit code 5 with `deselected` and no `passed` in the summary (the integration test was not named to include `heatmap`), or non-zero exit from a real assertion failure</fails_when>
    <automated>uv run pytest tests/test_backtest_persistence.py tests/test_backtest_run.py tests/test_backtest_run_cv.py tests/test_backtest_metrics.py tests/test_backtest_report.py tests/test_backtest_engine.py tests/test_backtest_contracts.py -q</automated>
    <fails_when>non-zero exit, or the summary reports any failed or errored test, or reports fewer passed than plan 02's recorded total plus this plan's new tests</fails_when>
    <automated>uv run pytest tests/test_universe_filtered_factor.py -q</automated>
    <fails_when>non-zero exit, or the summary reports any failed test, or reports fewer than 84 passed</fails_when>
    <human-check>Open a generated `report.html` in a browser. Confirm the heatmap renders, that its months line up with the monthly bar row above it, that the colour scale is readable in both the positive and negative direction, and that the earliest year is at the top. Tests lock cell values, trace names and pixel budgets; they cannot judge whether the page reads well. This is VALIDATION's one Manual-Only row for item 2. It is recorded as a human check inside this task rather than as a blocking checkpoint: CONTEXT.md scopes the standing "a human must open a report.html" item OUT of this phase, and the user's standing preference is that routine GSD steps run without stopping. Record the outcome in the summary; if the page reads badly, file a follow-up quick task rather than blocking the phase.</human-check>
  </verify>

  <acceptance_criteria>
    - `tests/test_backtest_persistence.py` contains `def _second_figure_traces`
    - A test named to match `heatmap` asserts the second div exists on a real run's `report.html` and that its z grid has 12 columns
    - The same test asserts the run directory contents still equal `D24_ARTIFACTS`
    - The `period.month - 1` -> `period.month` mutation turned the single-month alignment test red, and that is recorded in the summary
    - The seven-suite command exits 0 and `tests/test_universe_filtered_factor.py` exits 0
    - If executed in a worktree, both commands were re-run in the main tree after merge and both results are recorded
  </acceptance_criteria>

  <done>A real run's report is proved to carry the heatmap in a second div with the right grid dimensions and no new run-directory file; the month-alignment mutation was run and turned the alignment test red; the full seven-suite run and the cross-check suite are green, and the main-tree re-run after merge is recorded.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
Symbols this PLAN creates (excluded from drift verification — they are new, not drifted):

| Kind | Name | Location |
|---|---|---|
| function | `_monthly_series(returns)` | `quantlab/utils/backtest_report.py` |
| function | `_monthly_grid(monthly)` | `quantlab/utils/backtest_report.py` |
| function | `_monthly_heatmap_div(returns)` | `quantlab/utils/backtest_report.py` |
| plotly trace name | the heatmap trace's `name` | inside `report.html` |
| test helper | `_second_figure_traces(html)` | `tests/test_backtest_report.py` AND `tests/test_backtest_persistence.py` |
| test fixture | a multi-year monthly returns fixture | `tests/test_backtest_report.py` |

`_document` gains one parameter for the second fragment. No new file, no new
run-directory artifact, no new metrics key, no new dependency.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| returns series -> heatmap grid | Arbitrary-length, possibly all-NaN or empty series drives array indexing |
| report rendering -> staging directory | The report is written inside `.{name}.partial`; ANY exception deletes the completed run's every artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.8-03-01 | Denial of Service | `_monthly_heatmap_div` on empty / None / all-NaN returns | high | mitigate | Early `return ""` when `_monthly_series` yields None or an empty series, mirroring `_add_monthly_returns`. No `try/except`; explicit guards only. Locked by the empty/None behaviour tests |
| T-03.8-03-02 | Denial of Service | `_monthly_grid` row indexing | high | mitigate | Rows are allocated from the series' own year set and each write is indexed by `period.month - 1` into a 12-wide row, so no index can fall outside the allocation. The `- 1` also prevents the December (`month == 12`) IndexError that the off-by-one would raise on exactly one month of the year |
| T-03.8-03-03 | Tampering | heatmap axis labels reaching the page | low | accept | Labels are derived from integer years and months, not from user text, and enter the page inside plotly's own JSON, which plotly escapes. `_document`'s stated rule is that plotly fragments are inserted verbatim by design |
| T-03.8-03-04 | Tampering | a `<h2>` caption added above the second div | medium | mitigate | Any non-plotly string added to the page goes through `_escape`, matching `_summary_section` and `_notes_section` |
| T-03.8-03-05 | Information Disclosure | CDN fetch for plotly.js | low | accept | Unchanged: the second div loads no library of its own (`include_plotlyjs=False`) and the page carries only local numbers |
| T-03.8-SC | Tampering | package-manager installs | n/a | accept | No package installed. `go.Heatmap` is in the already-imported `plotly.graph_objects`; `kaleido` is explicitly NOT installed and is not needed. RESEARCH § Package Legitimacy Audit: "Not applicable" |
</threat_model>

<verification>
- `uv run pytest tests/test_backtest_report.py -q -k heatmap` exits 0 with at least 6 passed
- `uv run pytest tests/test_backtest_report.py -q -k "exactly_these_five_traces or axis_title_room"` exits 0
- `uv run pytest tests/test_backtest_persistence.py -q -k heatmap` exits 0
- The seven-suite command exits 0
- `uv run pytest tests/test_universe_filtered_factor.py -q` exits 0 with 84 passed
- The run-directory file set is unchanged in all four assertion sites
- The month-alignment mutation turned the alignment test red
- The human check on a rendered `report.html` is recorded
</verification>

<success_criteria>
- A year-by-month heatmap renders in a second div, beside the surviving bar row (D-04)
- Month and year alignment are proved by value and label, not by grid shape
- Uncovered months are null, never fabricated zeros
- Empty / None returns emit no div and still write the page
- Both panels consume one shared monthly-series helper
- The main figure's five-trace set and pixel budget are untouched
- No run-directory file was added; the leaf is still a leaf
</success_criteria>

<output>
Create `.planning/phases/03.8-backtest-report-in-out-of-sample-delta-column-monthly-return/03.8-03-SUMMARY.md` when done.
Record: the measured size of the second div; the mutation outcome; the human check's
verdict; and BOTH the worktree-local and the main-tree post-merge results of the
seven-suite command and the cross-check suite.
</output>
