---
phase: 03.6-frequency-keyed-chunking-policy
plan: 06
type: execute
wave: 2
depends_on: ["03.6-05"]
gap_closure: true
files_modified:
  - quantlab/base/data.py
  - quantlab/utils/cli.py
  - quantlab/dataset/backend.py
  - ingest_tiingo.py
  - ingest_us_equity.py
  - tests/test_ingest_conversion_gate.py
  - example/acquisition.md
autonomous: true
requirements: [D-18, PHASE-GOAL-caller-owns-a-stated-cost]

estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "PHASE GOAL, caller-owns-a-stated-cost half: what this repository TELLS a caller about the guards and reports that exist matches what actually exists. A comment asserting a RAM guard runs at every call site is a false safety claim an integrator can act on, and it is not made less false by living in a docstring rather than in code."
    - "D-18's discipline — falsified locked text is annotated IN PLACE with the original wording preserved, not silently rewritten — is applied to the statements phase 03.6 falsified but did not reach: `quantlab/base/data.py:56-58` (the D-11 RAM-guard claim) and `quantlab/utils/cli.py:575, 598-617` (the `_validate_category` reachability argument). `quantlab/acquisition/registry.py:450-472` is the worked example both follow."
    - "Cross-references pointing at code this phase deleted are GONE rather than annotated: `quantlab/dataset/backend.py:444-446` (the deleted calendar planner), `ingest_tiingo.py:228-230` (a deleted sizing block), `ingest_us_equity.py:521-525` and `tests/test_ingest_conversion_gate.py:47, 332` (a deleted test function). A dangling pointer records no decision, so there is nothing to annotate — it is simply wrong."
    - "`example/acquisition.md`'s `--dry-run` transcript is what today's code actually prints, RE-DERIVED by running the documented command and capturing its output — the same treatment `example/chunking.md` already received in this phase — not a hand-patched copy with lines struck out."
    - "No behaviour changes. Every edit in this plan is a comment, a docstring or a documentation transcript; the test suite's passed count is unchanged."
    - "The retirement notes inside `tests/test_volume_guard.py` (`:18-30`, `:1362-1372`) survive untouched. They NAME what phase 03.6 deleted on purpose — that is D-18 working, not a dangling reference — and removing them would erase the record this plan exists to restore elsewhere."
  artifacts:
    - quantlab/base/data.py
    - quantlab/utils/cli.py
    - quantlab/dataset/backend.py
    - ingest_tiingo.py
    - ingest_us_equity.py
    - tests/test_ingest_conversion_gate.py
    - example/acquisition.md
  key_links:
    - "`quantlab/acquisition/registry.py:450-472` (the phase's own D-18 worked example for the D-11 statement) -> the parallel annotation this plan adds at `quantlab/base/data.py:56-58`"
    - "`ingest_us_equity.py:168-187` `_print_estimate` (six `print` calls today) -> the transcript block in `example/acquisition.md` -> a reader's expectation of what `--dry-run` shows"
---

<objective>
Close the six stale-text defects `03.6-VERIFICATION.md` records in its "Anti-Patterns Found"
table as WR-04 through WR-09. Each is a statement in a comment, a docstring or an example
document that describes something phase 03.6 removed. The verifier did not count them
toward any gap — ROADMAP SC-8 enumerates four PLANNING documents, and these are code
comments and an example file — but recorded that they are "the same thing as D-18's
principle that falsified locked text must be annotated in place". The developer has since
put them in scope.

The six:

| Item | Location | What is stale |
|---|---|---|
| WR-04 | `quantlab/utils/cli.py:575, 598-617` | The `_validate_category` override is unreachable on this view, and the comment arguing for it rests on a premise that is now false. |
| WR-05 | `quantlab/base/data.py:56-58` | Still asserts 03.5 D-11's "RAM guard at every call site" for a guard SC-3 deleted. The parallel statement in `quantlab/acquisition/registry.py:457-471` was annotated; this one was missed. |
| WR-06 | `quantlab/dataset/backend.py:444-446` | Describes the deleted calendar planner ("no timestamp axis exists yet"). Plan 03.6-02 rewrote the paragraph; plan 03.6-03 deleted what it pointed at; nobody went back. |
| WR-07 | `ingest_tiingo.py:228-230` | Points at a sizing block this phase deleted. |
| WR-08 | `ingest_us_equity.py:521-525` (+ `tests/test_ingest_conversion_gate.py:47, 332`) | Cites a test function this phase deleted. |
| WR-09 | `example/acquisition.md:424-449` | The `--dry-run` transcript shows eight lines today's code does not print. |

Purpose: the phase's trade — the library offers the ladder and the caller owns the cost —
only holds while what the repository SAYS about its guards is true. A docstring claiming a
RAM guard runs at every call site is a safety claim a new integrator can act on, and it is
currently false.

Output: two D-18-shaped in-place annotations, four deleted dangling pointers, and one
transcript re-derived from a real run.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@quantlab/acquisition/registry.py
@quantlab/base/data.py
@quantlab/utils/cli.py
@ingest_us_equity.py
@example/chunking.md
</context>

<wave_reasoning>
**This plan is wave 2 and depends on `03.6-05` for a mechanical reason, not a judgement
call.** Both plans modify `quantlab/dataset/backend.py` and `quantlab/base/data.py`. Plan
05 edits `_widen_block_rows`'s grid-constraint argument (`quantlab/dataset/backend.py:411-450`)
and `from_raw_data_chunked`'s append call (`quantlab/base/data.py:823`); this plan edits
the closing paragraph of that SAME docstring (`:444-446`, thirteen lines below plan 05's
edit) and that same module's `ConversionResult` docstring (`:56-58`). Same-wave plans must
have zero `files_modified` overlap, so the wave assignment is forced.

The dependency is also substantive in one place: plan 05 Task 3 adds a paragraph to
`_widen_block_rows` about why that path keeps its block floor, and deliberately leaves the
closing calendar-planner paragraph alone, naming this plan as its owner. Reading the file
AFTER plan 05 has landed is the only way to edit the right lines.

Everything else here is independent of plan 05's behaviour: no task below changes code
execution, so nothing in this plan can invalidate plan 05's verification.
</wave_reasoning>

<scope_fence>
**IN SCOPE: WR-04, WR-05, WR-06, WR-07, WR-08, WR-09. Nothing else.**

Explicitly OUT of scope, and NOT to be planned or opportunistically fixed:

- **WR-03** — `tests/test_chunked_ingest.py:501`, the relative-path `rglob` vacuum guard.
  The developer named 04 through 09 and did not include 03. Leave it.
- **The retirement notes in `tests/test_volume_guard.py`** (`:18-30` and `:1362-1372`).
  They name the deleted apparatus deliberately: they ARE the D-18 annotation for it. The
  verifier's review excluded them by name. Touching them would delete the record.
- **Gap #1's grid fix** — that is plan `03.6-05`, wave 1.
- **Re-opening SC-1..SC-8**, all eight verified, and the acquisition-volume guard group,
  which SC-4 pins UNTOUCHED.
- **Any behavioural change whatsoever.** No function is deleted, no branch removed, no
  signature altered. WR-04 in particular does NOT delete the `_validate_category` override
  — see Task 1(b) for why.
</scope_fence>

<tasks>

<task type="auto">
  <name>Task 1: Annotate in place (D-18) the two statements whose PREMISE changed — the RAM-guard claim and the validator-reachability argument</name>

  <reversibility rating="reversible">Docstring and comment text in two files; a single
  `git revert` restores both, and no persistent artifact or contract shape depends on
  them.</reversibility>

  <precondition>The working tree carries no uncommitted change under `quantlab/` when this
  task begins. Plan `03.6-05` is committed — this plan is wave 2 and does not start until
  wave 1 seals — and this is this plan's FIRST task, so the tree is clean by ordering rather
  than by hope. Stated because this task's working-tree criterion is FALSE otherwise. Assert
  with `git status --porcelain -- quantlab/` printing nothing before starting.</precondition>

  <read_first>
    - `quantlab/acquisition/registry.py:450-472` — THE WORKED EXAMPLE. Read it first and
      copy its shape: the falsified bold heading kept, `SUPERSEDED by phase 03.6 (SC-3)`
      stated immediately, the original sentence quoted verbatim under "The original
      sentence read:", an explicit note that the deleted symbol's NAME is not restated
      because the phase's gate asserts it appears nowhere in the tree, and then the new
      truth — that the recorded risk is now UNMITIGATED rather than mitigated-at-the-call-site.
      Every judgement call this task needs has already been made once, right there.
    - `quantlab/base/data.py:40-66` — the `ConversionResult` docstring, especially the
      `peak_window_bytes` / `predicted_peak_bytes` paragraph at `:50-58` whose closing
      sentence ("`convert()` cannot compute it: 03.5 D-11 puts the RAM guard at every call
      site, ...") is the falsified one.
    - `quantlab/utils/cli.py:560-583` — the `_explicit_symbol_catalog` factory docstring,
      whose second paragraph claims this view "overrides only the two steps that are ABOUT
      the roster's provenance", naming `_validate_category` as one of them.
    - `quantlab/utils/cli.py:584-617` — `_ExplicitSymbolCatalog.__init__` and the
      `_validate_category` override, including its comment's stated reason: that
      `_roster_window_profile` "opens with `self._validate_category(category)`".
    - `quantlab/utils/cli.py:618-670` — the `_roster_window_profile` OVERRIDE's body. This
      is the decisive read: the override replaces the base method entirely and calls
      `self._normalize_iso_date` but NOT `self._validate_category`, which is exactly why
      the override above it is unreachable on this view.
    - `quantlab/acquisition/universe.py:1407` (the base `_validate_category`) and its three
      call sites at `:1584`, `:1680`, `:2228` — confirm none of them is reached by this
      synthetic view, whose only entry point on the pricing path is the override at
      `quantlab/utils/cli.py:618`.
    - `.planning/phases/03.6-frequency-keyed-chunking-policy/03.6-CONTEXT.md`, the
      `### Decision-by-decision disposition` table — D-18 "SURVIVES UNCHANGED", and D-11
      "SUPERSEDED BY DELETION".
  </read_first>

  <files>quantlab/base/data.py, quantlab/utils/cli.py</files>

  <behavior>
    - A reader of `ConversionResult`'s docstring learns that the guarantee 03.5 D-11
      recorded is now UNMITIGATED, sees the original sentence it replaced, and does not
      conclude that some call site still runs a RAM guard.
    - A reader of `_ExplicitSymbolCatalog` learns that the `_validate_category` override is
      currently unreachable on this view, WHY (the `_roster_window_profile` override
      replaces the only base path that validated), and why it is retained anyway.
    - The `_validate_category` override still exists and is byte-identical in behaviour;
      only its surrounding prose changes.
    - `uv run pytest -q` reports the same passed count as before this task.
  </behavior>

  <action>
    Prose only, in two files. No executable line changes in either.

    **(a) WR-05 — `quantlab/base/data.py:56-58`.** The closing sentence of the
    `predicted_peak_bytes` paragraph asserts that 03.5 D-11 places a RAM guard at every
    call site, and uses that as the reason `convert()` computes nothing itself. SC-3
    deleted that guard, so the premise is false while the conclusion happens to remain
    true. Annotate exactly as `registry.py:450-472` does for the SAME D-11 statement:
    preserve the original sentence as a quotation, mark it SUPERSEDED by phase 03.6 SC-3,
    and state the surviving truth — `convert()` still computes nothing of its own, but the
    reason is now that there is no guard anywhere rather than that the guard lives
    upstream, and `predicted_peak_bytes` survives as an out-of-repo caller's OWN estimate
    which buys no protection.

    **Do NOT name the deleted guard's method name.** `registry.py:457-465` explicitly
    declines to restate it and says why: `tests/test_chunked_panel_estimate.py` asserts
    those five symbol names appear nowhere in the tree as executable references, and the
    repo's convention after that annotation is to describe the deleted capability rather
    than name it. Follow the same convention here. Cross-reference
    `quantlab/acquisition/registry.py` so a reader can find the fuller treatment.

    **(b) WR-04 — `quantlab/utils/cli.py:575` and `:598-617`.** Two prose sites, one fact.

    The fact, established by reading `:618-670`: `_ExplicitSymbolCatalog` overrides
    `_roster_window_profile` WHOLESALE, and that override does not call
    `self._validate_category`. The base's validator call lives inside base methods this
    view either overrides or never reaches. So the `_validate_category` override is
    currently unreachable, and the reason its comment gives for existing — that
    `_roster_window_profile` opens with a validator call that would reject the sentinel —
    describes the BASE method, not the override that actually runs.

    Correct both prose sites to state what is true: the factory docstring at `:572-579`
    should say this view overrides `_roster_window_profile` wholesale and carries a
    `_validate_category` override that the wholesale override makes unreachable; the
    comment at `:599-616` should state that the validator call it names belongs to the BASE
    `_roster_window_profile`, that this view's override does not make it, and that the
    override is therefore currently dead.

    **KEEP the override.** Do not delete it. Three reasons, and state them in the corrected
    comment so the next reader does not delete it either: (i) this plan makes no
    behavioural change, by its own scope fence; (ii) the override is the guard that makes
    the sentinel safe IF this view's `_roster_window_profile` is ever narrowed to delegate
    to `super()` — which is the drift the class exists to prevent, per the factory
    docstring's own "the explicit path cannot drift away from the category path" argument;
    (iii) deleting it would remove that protection in exchange for nothing measurable.
    What was wrong was the REASON given, not the code.
  </action>

  <acceptance_criteria>
    - `quantlab/base/data.py`'s `ConversionResult` docstring carries a `SUPERSEDED` marker
      naming phase 03.6, quotes the original D-11 sentence verbatim under an explicit
      "original" lead-in, and cross-references `quantlab/acquisition/registry.py`.
    - None of the five names `tests/test_chunked_panel_estimate.py` locks as deleted
      appears in `quantlab/base/data.py` — the existing two-directional deletion lock in
      that test file still passes, unedited.
    - An AST walk of `quantlab/utils/cli.py` confirms the `_roster_window_profile` override
      inside `_explicit_symbol_catalog` contains ZERO calls to `self._validate_category` —
      this is the fact the corrected prose asserts, so the assertion is about the code, not
      about the wording.
    - The `_validate_category` override still exists: an AST walk of
      `quantlab/utils/cli.py` finds a `FunctionDef` named `_validate_category` inside the
      class defined in `_explicit_symbol_catalog`.
    - `uv run pytest -q` reports the same passed count as at the start of this task
      (785 or whatever plan 03.6-05 left it at, whichever is higher) — prose edits change
      no behaviour.
    - `git status --porcelain -- quantlab/` lists exactly two paths — `quantlab/base/data.py`
      and `quantlab/utils/cli.py` — and no third. Deliberately the UNCOMMITTED tree rather
      than a revision range: the criterion is about what this task left behind before it
      commits, and it rests on the `<precondition>` above, which is what makes everything the
      gate sees attributable to THIS task.
  </acceptance_criteria>

  <verify>
    <automated>uv run python -c "
import ast, pathlib
src = pathlib.Path('quantlab/utils/cli.py').read_text()
tree = ast.parse(src)
cls = [n for n in ast.walk(tree) if isinstance(n, ast.ClassDef) and n.name == '_ExplicitSymbolCatalog']
assert len(cls) == 1, f'expected one _ExplicitSymbolCatalog, got {len(cls)}'
names = {n.name for n in cls[0].body if isinstance(n, ast.FunctionDef)}
assert '_validate_category' in names, f'override was deleted; this plan makes no behavioural change: {names}'
prof = [n for n in cls[0].body if isinstance(n, ast.FunctionDef) and n.name == '_roster_window_profile']
assert len(prof) == 1, prof
calls = [n for n in ast.walk(prof[0]) if isinstance(n, ast.Call) and isinstance(n.func, ast.Attribute) and n.func.attr == '_validate_category']
assert not calls, f'the override DOES validate; the corrected comment would be false: {len(calls)} call(s)'
print('validator override present and provably unreachable')
"</automated>
    <fails_when>non-zero exit, or stdout does not contain the line "validator override present and provably unreachable"</fails_when>
    <automated>uv run python -c "
from quantlab.base.data import ConversionResult
doc = ConversionResult.__doc__ or ''
assert 'SUPERSEDED' in doc, 'no in-place supersession marker in ConversionResult docstring'
assert '03.6' in doc, 'the annotation does not name the phase that falsified the claim'
assert 'registry.py' in doc, 'no cross-reference to the worked example'
print('D-18 annotation present on ConversionResult')
"</automated>
    <fails_when>non-zero exit, or stdout does not contain the line "D-18 annotation present on ConversionResult"</fails_when>
    <automated>uv run pytest tests/test_chunked_panel_estimate.py tests/test_volume_guard.py -q</automated>
    <fails_when>non-zero exit, or any line of output starts with "FAILED", or the summary line contains "no tests ran"</fails_when>
    <automated>uv run pytest -q</automated>
    <fails_when>non-zero exit, or any line of output starts with "FAILED", or the passed count in the summary line is below 785</fails_when>
    <automated>git status --porcelain -- quantlab/</automated>
    <fails_when>the output names any path other than quantlab/base/data.py and quantlab/utils/cli.py, or it fails to name both of them</fails_when>
  </verify>

  <done>
    Both falsified premises are annotated in place following the shape
    `quantlab/acquisition/registry.py:450-472` set, the original wordings are preserved,
    the unreachable override is retained with an accurate reason recorded beside it, and
    the suite's passed count is unmoved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete the four pointers that aim at code this phase removed</name>

  <reversibility rating="reversible">Comment deletions in four files; `git revert`
  restores them and nothing reads them at runtime.</reversibility>

  <read_first>
    - `quantlab/dataset/backend.py:411-450` AS PLAN 03.6-05 LEFT IT — the whole
      `_widen_block_rows` docstring. Plan 05 added a paragraph about why this path keeps
      its block floor; the stale sentence this task removes is the LAST clause of the
      closing paragraph at `:440-446`, which asserts the planner is the right tool where
      "no timestamp axis exists yet". Re-read the file rather than trusting these line
      numbers: plan 05's paragraph shifts them.
    - `quantlab/base/chunking.py` — `TimeChunkPlanner` as it now stands. There is exactly
      one planner, `plan_from_timestamps`, and it REQUIRES an observed timestamp axis, which
      is precisely why the stale clause is now false rather than merely dated.
    - `ingest_tiingo.py:185-232` — the surviving acquisition-volume guard call at `:188-214`,
      then the comment at `:228-230` and the `if args.to_zarr:` block it now floats above.
    - `git show 8bdd9e9..HEAD -- ingest_tiingo.py` — the deleted block the comment pointed
      at (a `print_chunk_report(pricing.assert_chunked_panel_fits(...))` call under its own
      `if args.to_zarr:`). Read it to confirm the referent is gone rather than moved.
    - `ingest_tiingo.py:16-26` — the module docstring paragraph phase 03.6 ALREADY added,
      stating that nothing checks the chosen window fits in RAM. This is where the surviving
      half of the deleted comment's claim already lives, which is why deleting the comment
      loses nothing.
    - `ingest_us_equity.py:513-535` — the `--to-zarr` probe comment, whose middle clause
      cites a deleted test function as the record of the reachability lesson.
    - `tests/test_ingest_conversion_gate.py:42-49` and `:320-340` — the same citation, twice
      more, in the file that INHERITED the lesson. `:325` defines
      `test_every_entry_point_that_densifies_refuses_first`, which is the live successor.
    - `tests/test_volume_guard.py:18-30` and `:1362-1372` — the retirement notes. Read them
      so you can tell them apart from the dangling citations: these NAME the deleted
      apparatus on purpose, as its epitaph. They are OUT of scope and must not be touched.
  </read_first>

  <files>quantlab/dataset/backend.py, ingest_tiingo.py, ingest_us_equity.py, tests/test_ingest_conversion_gate.py</files>

  <behavior>
    - No comment anywhere in `quantlab/dataset/backend.py` claims a chunk planner serves a
      case in which no timestamp axis exists yet; the surviving argument for not using the
      planner here (calendar periods cannot bound bytes) is untouched.
    - No comment in `ingest_tiingo.py` refers to a sizing step that is no longer in the
      file.
    - The deleted test function's name appears in `tests/test_volume_guard.py` ONLY, where
      it is the epitaph, and nowhere else in the repository's source.
    - Every file still parses and the suite's passed count is unchanged.
  </behavior>

  <action>
    Four deletions. In each case the referent is gone, so there is no decision to preserve
    and nothing to annotate — a pointer that aims at nothing records nothing.

    **(a) WR-06 — `quantlab/dataset/backend.py`, the closing paragraph of
    `_widen_block_rows`'s docstring.** Its last clause justifies the planner's remaining
    role by a case where no timestamp axis exists yet. That case was the calendar planner's,
    and plan 03.6-03 deleted the calendar planner; the sole survivor plans FROM an observed
    timestamp axis, so the clause is now false rather than merely dated. Remove that clause.
    KEEP the rest of the paragraph — that calendar periods cannot bound BYTES, and that the
    planner is the right tool for planning conversion windows where the calendar is the unit
    of work — because that argument is why this method does not use the planner and is still
    entirely true. Verify against `quantlab/base/chunking.py` that the surviving sentence
    describes the planner that actually exists.

    **(b) WR-07 — `ingest_tiingo.py:228-230`.** Delete the three-line comment. Its subject
    was the RAM sizing block removed from directly beneath it; with that block gone the
    comment floats above an unrelated `if args.to_zarr:` and points at nothing. Its one
    still-true clause — that `convert()` runs no guard of its own — is already carried,
    more prominently and more accurately, by the module docstring paragraph phase 03.6
    added at `:16-26`, so nothing is lost by removing it. Confirm that paragraph is present
    before deleting, and if for any reason it is not, move the surviving clause there rather
    than dropping it.

    **(c) WR-08 — `ingest_us_equity.py:521-525`.** The comment cites a deleted test as the
    record of the "scoped by reachability, not by script name" lesson. Delete the citation
    clause and keep the lesson: the surrounding sentences state the reachability rule in
    their own words and remain correct. Do NOT repoint the citation at the live successor
    `tests/test_ingest_conversion_gate.py::test_every_entry_point_that_densifies_refuses_first`
    unless you have read that test and confirmed it records the same lesson in its own
    docstring — a citation repointed on name similarity is the same defect again.

    **(d) WR-08, continued — `tests/test_ingest_conversion_gate.py:47` and `:332`.** The
    identical dangling citation, twice, in the file that inherited the lesson. Apply the
    same treatment. **These two are included deliberately, and the reason is the lesson
    itself:** a gate scoped to the one file whose defect report arrived is precisely the
    mistake these comments are about. An acceptance criterion that greps only
    `ingest_us_equity.py` would pass while two more copies survived, so the gate below is
    repository-scoped with `tests/test_volume_guard.py` as its single named exclusion. If
    the executor judges these two out of scope, it must say so in the SUMMARY and the
    repository-scoped acceptance criterion below must be narrowed in the same edit — not
    left asserting something the change does not deliver.

    Leave `tests/test_volume_guard.py` alone entirely. Both of its mentions are the D-18
    retirement notes for the deleted apparatus; they are the record, not a reference to it.
  </action>

  <acceptance_criteria>
    - `grep -c "timestamp axis exists yet" quantlab/dataset/backend.py` is `0`. The comment
      filter the usual grep-gate hygiene rule prescribes is DELIBERATELY not applied here:
      the defect IS a comment, so stripping comment lines before counting would vacuum the
      gate into passing by finding nothing.
    - `grep -c "the sizing above" ingest_tiingo.py` is `0`, for the same reason and with the
      same deliberate omission of the comment filter.
    - The deleted test function's name appears in `tests/test_volume_guard.py` and in no
      other `.py` file in the repository — a repository-scoped grep with exactly one named
      exclusion, not a per-file check.
    - `git status --porcelain -- tests/test_volume_guard.py` prints nothing. Path-scoped
      deliberately, so the gate holds whether or not the preceding task has already committed:
      the claim is only that this file was never opened, and no earlier task in this plan
      touches it either, so no commit-boundary assumption is needed here.
    - `quantlab/dataset/backend.py`'s surviving `_widen_block_rows` docstring still contains
      `MAX_WIDEN_BYTES` and `BARS_PER_DAY_BY_FREQUENCY` — the argument that is still true was
      kept, not deleted along with the clause that was not.
    - `ingest_tiingo.py`'s module docstring still states that nothing checks the chosen window
      fits in RAM, so the surviving half of the removed comment's claim is still on record.
    - `uv run pytest -q` reports the same passed count as at the start of this task.
  </acceptance_criteria>

  <verify>
    <automated>uv run python -c "
import pathlib, subprocess
b = pathlib.Path('quantlab/dataset/backend.py').read_text()
assert 'timestamp axis exists yet' not in b, 'WR-06: the stale clause is still in backend.py'
assert 'MAX_WIDEN_BYTES' in b and 'BARS_PER_DAY_BY_FREQUENCY' in b, 'WR-06: the surviving argument was deleted too'
t = pathlib.Path('ingest_tiingo.py').read_text()
assert 'the sizing above' not in t, 'WR-07: the dangling comment is still in ingest_tiingo.py'
name = 'test_every_entry_point_that_densifies_guards_the_dense_panels_ram'
hits = subprocess.run(['git','grep','-l','--',name,'--','*.py'],capture_output=True,text=True).stdout.split()
assert hits == ['tests/test_volume_guard.py'], f'WR-08: expected the epitaph file only, got {hits}'
print('four dangling pointers removed, three surviving records intact')
"</automated>
    <fails_when>non-zero exit, or stdout does not contain the line "four dangling pointers removed, three surviving records intact"</fails_when>
    <automated>uv run python -c "import ast,pathlib; [ast.parse(pathlib.Path(p).read_text()) for p in ('quantlab/dataset/backend.py','ingest_tiingo.py','ingest_us_equity.py','tests/test_ingest_conversion_gate.py')]; print('all four files parse')"</automated>
    <fails_when>non-zero exit, or stdout does not contain the line "all four files parse"</fails_when>
    <automated>uv run pytest -q</automated>
    <fails_when>non-zero exit, or any line of output starts with "FAILED", or the passed count in the summary line is below 785</fails_when>
    <automated>git status --porcelain -- tests/test_volume_guard.py</automated>
    <fails_when>the output is non-empty — the retirement notes were opened, and they are this phase's D-18 record for the deleted apparatus</fails_when>
  </verify>

  <done>
    The four pointers aiming at deleted code are gone, the arguments that were still true
    around them are intact, the deleted test's name survives only in its epitaph, and the
    suite's passed count is unmoved.
  </done>
</task>

<task type="auto">
  <name>Task 3: Re-derive `example/acquisition.md`'s dry-run transcript from a real run</name>

  <reversibility rating="reversible">Documentation transcript in one example file.</reversibility>

  <precondition>`data/data/reference/universe.parquet` exists on the running machine —
  `ingest_us_equity.py --dry-run` resolves its roster from it and exits non-zero without
  it. Confirmed present at plan time; assert it before running the command and halt with
  that path named if it is absent, rather than pasting an error transcript into the guide.</precondition>

  <read_first>
    - `example/acquisition.md:405-460` — the `## 完整例子` section: the invocation, the
      date-stamped "真实输出" lead-in, the fenced transcript, and the bullet list beneath it
      that discusses `raw-data path`, `watermark path` and the coverage report.
    - `example/chunking.md:418-470` — THE WORKED EXAMPLE for this task. The same transcript
      was re-run on 2026-09-12 for that guide: note the re-run date in the lead-in, the
      shortened output, and the paragraph beneath it that states explicitly that the output
      was re-run rather than hand-edited, names the eight lines that disappeared, and
      separates roster drift (symbol count, density) from the deletion's effect. Reproduce
      that structure, not just the numbers.
    - `ingest_us_equity.py:160-187` — `_print_estimate` as it stands: six `print` calls.
      This is the arithmetic that decides how many lines the transcript can legitimately
      contain.
    - `ingest_us_equity.py:188-230` — `_print_coverage`, the source of the `coverage report`
      lines beneath the estimate.
    - `example/README.md` — the directory's own convention, which `example/chunking.md`
      cites: paste output that was actually run.
  </read_first>

  <files>example/acquisition.md</files>

  <behavior>
    - The fenced transcript in `example/acquisition.md` is the verbatim stdout of the
      documented command, run on the machine doing the work with the three credential
      environment variables explicitly unset — byte-equal at the moment of capture, and
      label-for-label equal to a live run on any later day.
    - The lead-in carries the new run's date, replacing the old one.
    - A reader is told the transcript was re-run rather than hand-edited, and which lines
      disappeared and why — the same disclosure `example/chunking.md` carries.
    - No prose elsewhere in `example/acquisition.md` still describes a line the transcript
      no longer contains.
  </behavior>

  <action>
    **Run the command, capture its stdout, paste it. Do not hand-patch lines out of the old
    block.** The command is the one already documented at `example/acquisition.md:421-423`:
    `ingest_us_equity.py --dry-run` with `TIINGO_API_KEY`, `APCA_API_KEY_ID` and
    `APCA_API_SECRET_KEY` explicitly removed from the environment. Removing them is not
    ceremony — the guide's surrounding prose makes the credential-free claim, so the run
    that produces its transcript must actually be credential-free, and CLAUDE.md's
    credentials-only-via-environment-variables constraint means an accidentally-populated
    key could otherwise reach a committed file.

    Replace the fenced block with the captured stdout verbatim. Update the lead-in line
    above it — currently date-stamped 2026-09-09 — to the actual run date, keeping its
    existing sentence shape and its note that the three variables were cleared with
    `env -u`.

    Then add the disclosure paragraph, modelled on `example/chunking.md`'s: state that the
    output was re-run rather than hand-edited, name the lines the old transcript carried
    that today's code does not print, attribute them to the estimator/guard group phase
    03.6 deleted, and — separately — attribute any movement in the surviving numbers
    (symbol count, trading days, observation count, density, coverage classification) to
    roster drift and to the window's end date being today rather than to the deletion.
    Keeping those two causes apart is the whole point of the paragraph: a reader comparing
    the two versions must not conclude the deletion changed the roster.

    **Keep the invocation flagless. Do NOT pin `--start-date` / `--end-date`** the way
    `example/chunking.md`'s copy does. The flagless form is this example's whole pedagogic
    point — the smallest command that answers "what would this fetch", with nothing to
    explain — and the bullet beneath the block reads full-market sparsity (a density of 0.464,
    "全市场日线是稀疏的") off precisely that unbounded window. Pinning one year would take the
    density to roughly 0.9, falsify that surrounding prose, and duplicate the other guide's
    example: a larger edit than WR-09 asks for, aimed at a different guide's job.

    That choice has a consequence, and it is the reason the first gate below has the shape it
    does. An unbounded window ENDS TODAY, so the window line, the trading-day count, the
    observation count, the density and the four coverage counts are every one of them a
    function of the run date. Byte-equality between the committed block and a live run is
    therefore a POINT-IN-TIME claim — true the day it is captured, false the next morning on
    an unchanged tree. So the block is still pasted verbatim from a real run and still carries
    that run's date, and the RE-RUNNABLE gate compares LINE LABELS rather than values: each
    line reduced to the text before its first colon. Labels are exactly what WR-09 is about —
    no line survives in the doc that today's `_print_estimate` and `_print_coverage` cannot
    print, and no line they do print is missing — and labels do not move with the calendar.

    Finally, read the rest of `example/acquisition.md` for any other prose that names a line
    the transcript no longer prints. The bullet list immediately beneath the block discusses
    `raw-data path`, `watermark path` and the coverage report, all of which survive; confirm
    that, and search the whole file for references to the byte-denominated and
    chunk-denominated lines that are gone. Fix any you find, in the same pass.
  </action>

  <acceptance_criteria>
    - The committed block's LINE LABELS — each line reduced to the text before its first
      colon, indentation kept — are equal, IN ORDER, to the line labels of a live `--dry-run`'s
      stdout. This is the re-runnable gate and it decides WR-09 directly: a line the doc
      carries that the code no longer prints, or a line the code prints that the doc lacks,
      moves the label sequence. It is deliberately NOT byte-equality — see the action for why
      an unbounded window makes the VALUES a function of the run date, so a byte gate would
      turn red tomorrow on a tree nobody touched.
    - At the moment of capture the block is byte-equal to the captured stdout, because it was
      pasted verbatim rather than hand-patched. This is a point-in-time claim, exactly as
      `example/chunking.md` records it — "this is what the command printed when this was
      written" — which is why the lead-in must carry the run date. It is the executor's to
      establish while capturing, not a gate a later reader can re-run.
    - Exactly one fenced block in `example/acquisition.md` begins with the dry-run banner
      line, so the label comparison above is unambiguous about which block it checked. The
      gate finds that block by SCANNING fence lines and pairing them in order, not by a
      `re.findall` on a bare-fence pattern: this document opens most of its blocks with a
      language tag and closes every one of them with a bare fence, so a bare-fence pattern
      pairs a preceding block's CLOSING fence with this block's OPENING one and finds zero
      transcripts — verified against the file at plan time.
    - The lead-in above the block names the run's actual date, not 2026-09-09, and not a date
      in the future. The gate reads the last non-empty, non-fence line before the block — the
      lead-in SENTENCE itself — rather than a window of lines, so it is indifferent to where
      the disclosure paragraph lands and cannot be satisfied or broken by the superseded date
      being quoted inside that paragraph, which is what `example/chunking.md` does.
    - `_print_estimate` still makes exactly six `print` calls — counted from the source, as
      the canary that says "re-derive again" if the estimator's shape moves. That the six
      labels it prints all appear in the block is carried by the label-sequence equality
      above, so neither half is asserted against a remembered number.
    - No path in the committed transcript contains a credential-shaped value; the block is
      read before commit and the three credential variable names appear in it only as part
      of the `env -u` invocation, never with a value.
    - `git status --porcelain -- example/` names `example/acquisition.md` and nothing else.
      Path-scoped deliberately, so it holds whether or not the preceding tasks have already
      committed — no task in this plan writes anywhere under `example/` but this one.
  </acceptance_criteria>

  <verify>
    <automated>uv run python -c "
import os, pathlib, subprocess, sys
assert pathlib.Path('data/data/reference/universe.parquet').exists(), 'precondition unmet: data/data/reference/universe.parquet'
env = {k: v for k, v in os.environ.items() if k not in ('TIINGO_API_KEY', 'APCA_API_KEY_ID', 'APCA_API_SECRET_KEY')}
run = subprocess.run([sys.executable, 'ingest_us_equity.py', '--dry-run'], capture_output=True, text=True, env=env)
assert run.returncode == 0, run.stderr[-800:]
FENCE = chr(96) * 3
def fenced(text):
    blocks, cur = [], None
    for line in text.splitlines():
        if line.startswith(FENCE):
            if cur is None:
                cur = []
            else:
                blocks.append(chr(10).join(cur)); cur = None
        elif cur is not None:
            cur.append(line)
    return blocks
doc = pathlib.Path('example/acquisition.md').read_text()
blocks = [b for b in fenced(doc) if b.lstrip().startswith('DRY RUN')]
assert len(blocks) == 1, f'expected exactly one dry-run transcript block, found {len(blocks)}'
labels = lambda text: [line.split(':')[0].rstrip() for line in text.strip().splitlines()]
doc_labels, live_labels = labels(blocks[0]), labels(run.stdout)
stale = [l for l in doc_labels if l not in live_labels]
absent = [l for l in live_labels if l not in doc_labels]
assert doc_labels == live_labels, f'transcript labels differ from what the command prints -- in the doc only: {stale} -- in the live run only: {absent}'
print('transcript line labels match live output')
"</automated>
    <fails_when>non-zero exit, or stdout does not contain the line "transcript line labels match live output"</fails_when>
    <automated>uv run python -c "
import datetime, pathlib, re
src = pathlib.Path('ingest_us_equity.py').read_text()
body = src.split('def _print_estimate')[1].split('\ndef ')[0]
prints = len(re.findall(r'^\s*print\(', body, re.M))
assert prints == 6, f'_print_estimate no longer prints six lines, it prints {prints} -- re-derive the transcript again'
FENCE = chr(96) * 3
doc = pathlib.Path('example/acquisition.md').read_text()
blocks, cur = [], None
for line in doc.splitlines():
    if line.startswith(FENCE):
        if cur is None:
            cur = []
        else:
            blocks.append(chr(10).join(cur)); cur = None
    elif cur is not None:
        cur.append(line)
block = [b for b in blocks if b.lstrip().startswith('DRY RUN')][0]
assert 'TIINGO_API_KEY' not in block, 'a credential variable name leaked into the committed transcript body'
pre = [l for l in doc.split(block)[0].splitlines() if l.strip() and not l.startswith(FENCE)]
lead = pre[-1]
stamps = re.findall(r'20\d\d-\d\d-\d\d', lead)
assert stamps, f'the lead-in sentence above the block carries no run date: {lead!r}'
assert stamps[0] != '2026-09-09', 'the lead-in still carries the superseded run date'
assert stamps[0] <= datetime.date.today().isoformat(), f'the lead-in claims a run date in the future: {stamps[0]}'
print('estimator shape, run date and credential-free block all confirmed')
"</automated>
    <fails_when>non-zero exit, or stdout does not contain the line "estimator shape, run date and credential-free block all confirmed"</fails_when>
    <automated>git -C . status --porcelain -- example/</automated>
    <fails_when>the output names any path other than example/acquisition.md, or the output is empty</fails_when>
  </verify>

  <done>
    `example/acquisition.md`'s dry-run transcript is the captured output of a real,
    credential-free run on the current code, date-stamped, with the disclosure paragraph
    distinguishing the deleted lines from ordinary roster drift, and no surrounding prose
    still names a line the script no longer prints.
  </done>
</task>

</tasks>

<artifacts_this_phase_produces>

No symbol is created, deleted, or changed in signature by this plan. It is prose only.
Listed here because the mandatory section is about what a downstream reader must know
changed, and "nothing executable" is itself the load-bearing fact.

| Symbol / artefact | Location | Disposition |
|---|---|---|
| `ConversionResult` docstring, `predicted_peak_bytes` paragraph | `quantlab/base/data.py:50-58` | **ANNOTATED IN PLACE** (D-18) — original sentence quoted, marked SUPERSEDED by SC-3 |
| `_explicit_symbol_catalog` factory docstring | `quantlab/utils/cli.py:572-579` | **CORRECTED** — states the wholesale `_roster_window_profile` override and the resulting unreachability |
| `_ExplicitSymbolCatalog._validate_category` comment | `quantlab/utils/cli.py:599-616` | **CORRECTED** — the validator call it names belongs to the BASE method |
| `_ExplicitSymbolCatalog._validate_category` (the method) | `quantlab/utils/cli.py:598` | **RETAINED — deliberately**; no behavioural change, drift protection kept |
| `_ExplicitSymbolCatalog._roster_window_profile` (the method) | `quantlab/utils/cli.py:618` | **UNCHANGED** |
| `_widen_block_rows` docstring, closing paragraph | `quantlab/dataset/backend.py:440-446` | **CLAUSE DELETED** — the byte-bounding argument beside it is kept |
| `--to-zarr` guard comment | `ingest_tiingo.py:228-230` | **DELETED** — referent removed by this phase; surviving claim already in the module docstring |
| Reachability-lesson citation | `ingest_us_equity.py:521-525` | **CITATION DELETED**, lesson text kept |
| Reachability-lesson citation, x2 | `tests/test_ingest_conversion_gate.py:47, 332` | **CITATION DELETED**, lesson text kept |
| Dry-run transcript block + lead-in + disclosure paragraph | `example/acquisition.md:424-449` | **RE-DERIVED** from a live run |
| Retirement notes for the deleted RAM-guard apparatus | `tests/test_volume_guard.py:18-30, 1362-1372` | **UNTOUCHED — deliberately**; these ARE the D-18 record |
| `tests/test_chunked_ingest.py:501` relative-path guard (WR-03) | `tests/test_chunked_ingest.py` | **UNTOUCHED — deliberately**; out of scope by developer decision |

</artifacts_this_phase_produces>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository prose -> a future integrator's behaviour | A comment asserting a guard runs is a safety claim. An integrator reading `ConversionResult`'s docstring today is told a RAM guard exists at every call site; acting on that is how an unguarded full-market conversion reaches OOM believing itself protected. |
| local environment -> committed example transcript | `--dry-run` output embeds absolute local filesystem paths and is produced by a script that reads credential environment variables. Anything captured into `example/acquisition.md` is published. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.6-07 | Repudiation | `quantlab/base/data.py:56-58` RAM-guard claim | medium | mitigate | A false safety claim in a docstring is worse than no claim: it retires a recorded risk silently rather than by decision, which is the exact failure `quantlab/acquisition/registry.py:465-471` names when it refuses to let `predicted_peak_bytes` imply protection. Mitigated by Task 1(a)'s in-place D-18 annotation stating the risk is now UNMITIGATED, with the original wording preserved so the change of state is auditable. |
| T-03.6-08 | Information Disclosure | `example/acquisition.md` re-run transcript | high | mitigate | This repository has one real credential leak on record — a hardcoded Tiingo key in `scripts/download_stock_data_from_tiingo.py`, cited in CLAUDE.md as the reason for the environment-variables-only constraint. Task 3 captures script output into a committed file, and that script reads three credential variables. Mitigated on three levels: the run explicitly clears all three variables from the child environment; an acceptance criterion requires the block to be read before commit; and a verify command asserts no credential variable name appears inside the captured block. |
| T-03.6-09 | Tampering | scope of the WR-08 gate | low | mitigate | A gate scoped to the single file whose defect was reported is the exact mistake the deleted test's lesson records, and this repository's STATE.md already logs vacuum-passing guards twice. Mitigated by making the WR-08 acceptance repository-scoped with `tests/test_volume_guard.py` as its one named exclusion, so a surviving copy in a third file fails the gate rather than hiding behind it. |
| T-03.6-SC | Tampering | npm/pip/cargo installs | n/a | n/a | **Not applicable — this plan installs no packages.** No `uv add`, no `pyproject.toml` change, no new import. The package-legitimacy gate is not triggered and no `## Package Legitimacy Audit` is required. |

ASVS level 1; blocking severity `high`. T-03.6-08 is rated `high` on impact (a published
credential is unrecoverable) and its mitigations are executable rather than advisory: the
`env`-clearing is in the verify command itself, and the credential-name assertion runs as
an acceptance gate. No blocking human checkpoint is inserted because the mitigation is
automated and asserted rather than relying on a reviewer's attention.

</threat_model>

<verification>

Run all from the repo root:

1. `uv run pytest -q` — the whole suite, before and after. This plan changes no behaviour,
   so the passed count must be identical. Fails when: non-zero exit, or any line of output
   starts with "FAILED", or the passed count in the summary line is below 785.
2. `uv run pytest tests/test_chunked_panel_estimate.py tests/test_volume_guard.py
   tests/test_ingest_conversion_gate.py tests/test_ingest_shells.py -q` — the four suites
   whose subject matter this plan's text touches, UNEDITED except for
   `tests/test_ingest_conversion_gate.py`'s two comment lines. Fails when: non-zero exit,
   or any line of output starts with "FAILED", or the summary line contains "no tests ran".
3. The three per-task `<automated>` blocks above, each with its stated failing direction.

**Spec-less probe fallback — VISIBLY SKIPPED, recorded here rather than silently omitted.**
`workflow.specless_probe_fallback` is enabled, but phase 03.6 has no `SPEC.md` (so neither
`## Edge Coverage` nor `## Prohibitions` exists) and declares no requirement IDs for the
fallback to derive predicates from — ROADMAP 03.6 records `Requirements: N/A`. The fallback
derives predicates from requirement TEXT, so there was nothing to probe. **No probe-derived
edge or prohibition predicates were generated for this plan.** The `must_haves` above are
derived from the phase GOAL's caller-owns-a-stated-cost half, from D-18's annotation
discipline as recorded in `03.6-CONTEXT.md`, and from the six items in
`03.6-VERIFICATION.md`'s "Anti-Patterns Found" table.

**API-coverage checkpoint — NOT FIRING.** The detector returned
`{"detected": false, "signals": []}` over this phase's scope. This plan edits comments and
one example document; it integrates no external API, SDK or service. No `COVERAGE.md` is
produced.

**Schema-push gate — NOT FIRING.** No Payload / Prisma / Drizzle / Supabase / TypeORM schema
file exists in this repository. No `[BLOCKING]` push task is injected.

**Assumption-delta checkpoint — recorded in plan `03.6-05`.** The detector fires once for
the phase; its decision (`promote`, with the companion invariant test accepted) lives in
`03.6-05-PLAN.md`'s `<assumption_delta_decision>` block. Nothing in this plan changes an
identity model, so no separate decision is recorded here.

</verification>

<success_criteria>

- [ ] WR-04: `_explicit_symbol_catalog`'s two prose sites state the true reachability, the
      override is retained, and an AST assertion proves the fact the prose now claims (D-18)
- [ ] WR-05: `ConversionResult`'s D-11 claim is annotated in place, SUPERSEDED marker
      present, original wording quoted, `registry.py` cross-referenced (D-18)
- [ ] WR-06: the deleted planner's justification clause is gone from `_widen_block_rows`;
      the byte-bounding argument beside it survives
- [ ] WR-07: the dangling sizing comment is gone from `ingest_tiingo.py`; its surviving
      claim is still carried by the module docstring
- [ ] WR-08: the deleted test function's name appears in `tests/test_volume_guard.py` and
      nowhere else in repository source, with the lesson text kept at all three former
      citation sites
- [ ] WR-09: `example/acquisition.md`'s transcript is the verbatim capture of a live
      credential-free run, date-stamped, with the re-run disclosure paragraph — and its line
      labels match a live run's exactly, which is the calendar-stable form of the claim that
      no line survives which today's code cannot print
- [ ] `tests/test_volume_guard.py` and `tests/test_chunked_ingest.py` are absent from this
      plan's diff (the retirement notes and WR-03 are out of scope)
- [ ] `uv run pytest -q` reports the same passed count as before the plan — no behavioural
      change (D-18's annotation discipline applied to code comments and one example file;
      SC-8 is verified, enumerates four PLANNING documents, and is not re-opened here)

</success_criteria>

<output>
Create `.planning/phases/03.6-frequency-keyed-chunking-policy/03.6-06-SUMMARY.md` when done.
</output>

<!-- planner-discipline-allow: timestamp axis exists yet -->
<!-- planner-discipline-allow: the sizing above -->
<!-- planner-discipline-allow: test_every_entry_point_that_densifies_guards_the_dense_panels_ram -->
