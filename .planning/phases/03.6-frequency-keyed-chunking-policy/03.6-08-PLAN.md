---
phase: 03.6-frequency-keyed-chunking-policy
plan: 08
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - quantlab/dataset/backend.py
  - tests/test_chunked_ingest.py
autonomous: true
requirements: [D-18, PHASE-GOAL-caller-owns-a-stated-cost]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "GAP #19, route (b), production half: the sentence `03.6-VERIFICATION.md` measured FALSE -- `widen_and_append`'s unconditional `a store cannot come out of here carrying one grid per axis` -- no longer stands as an unconditional claim. It is preserved verbatim under a `NOT TRUE ANY MORE AS OF 03.6-07` / `SUPERSEDED by` marker (D-18), with the TRUE narrower claim written beneath it: the three things THIS CALL writes read `append_dim_size` from the same `kwargs` entry and therefore agree WITH EACH OTHER; the claim says nothing about variables the store already held."
    - "The accepted cost is named in the production docstring where the false claim used to stand, not only in a planning file: `XrBackend.write()` passes no `encoding`, so a store built by `BaseDataset.save()` / `Factor.save()` never sits on `min(APPEND_DIM_CHUNK, total)`, and `Factor.update()` holds no total-extent source for the filler. The measured consequence at the real default `APPEND_DIM_CHUNK = 512` with no monkeypatch -- `{'close': (1000, 2)}` then `{'close': (1000, 2), 'newvar': (512, 2)}` -- is cited so the next reader does not re-derive it, together with the disposition (root cause WR-02, predates 03.6, independent of the `--chunk` rung, therefore untouched SC-1..SC-8)."
    - "WR-06's remaining half is closed in the same file: `_widen_block_rows`'s transcribed excerpt and `_widen_chunked`'s transcribed excerpt each carry the `NOT TRUE ANY MORE AS OF 03.6-07` prefix over a preserved original, with the correction written as PROSE rather than as a second transcription that can rot in its turn (D-18 forbids deleting the original, which is why WR-06's delete-the-excerpt half is answered this way and not literally)."
    - "The accepted cost has an EXECUTABLE lock, so the day someone pins `encoding` in `write()` the test goes red and forces the ledger, the docstring and plan `03.6-07`'s must-have to be updated together: a new arm builds the store through `write()` -- the path no existing arm exercises, because `_incomplete_store` builds through `append(append_dim_size=9)` -- then adds a variable through `widen_and_append` and asserts TWO grids with the reason named."
    - "No grid BEHAVIOUR changes. `_append_encoding`'s `max(panel.sizes[dim], append_dim_size)`, the `append_dim_size` threading through `_reconcile_new_listings` / `widen_symbol_axis` / `_widen_whole_store` / `_widen_chunked` / `widen_data_vars`, `XrBackend.write()`'s body and the `--chunk` CLI all come out of this plan byte-identical, evidenced by the 797-passed baseline holding and by the executable-code diff being empty."
  artifacts:
    - quantlab/dataset/backend.py
    - tests/test_chunked_ingest.py
  key_links:
    - "`XrBackend.widen_and_append.__doc__` -> the marked-and-preserved false sentence -> the narrowed per-call claim -> the named accepted cost -> `deferred-items.md` (written by plan `03.6-11`) -> `03.6-VERIFICATION.md`'s override (written by plan `03.6-11`, and legitimate only because THIS plan landed first)"
    - "`tests/test_chunked_ingest.py::test_a_store_built_by_write_keeps_two_chunk_grids_accepted_cost` -> `XrBackend.write()` -> Zarr's default grid -> `widen_and_append` filler on `min(APPEND_DIM_CHUNK, stated)` -> the two-member grid set the docstring now names"
---

<objective>
Close the production half of GAP #19 from `03.6-VERIFICATION.md` (21/22, the one FAILED truth),
by **route (b)**, which the developer has locked: narrow the wording and formally retract the
universal. **No behaviour change to the grid path.**

Route (a) -- fix `XrBackend.write()` at the root so it pins `encoding` -- was considered and NOT
chosen. It would change the on-disk chunk grid of every store this project writes from now on,
through a pre-03.6, rung-independent bulk-write path that `03.6-REVIEW-FIX.md` already declined as
out of scope. Do not plan it, do not take a first step toward it, do not add a
`kwargs.setdefault("encoding", ...)` to `write()`.

What makes #19 a gap rather than an advisory is that commit `37e61f2` wrote the falsified
invariant into PRODUCTION code in unconditional form (`quantlab/dataset/backend.py:1029`):
"All three read `append_dim_size` from the SAME `kwargs` entry, so a store cannot come out of here
carrying one grid per axis". Measured false twice, once at the real default
`APPEND_DIM_CHUNK = 512` with no monkeypatch, along the repo's own documented
`Factor.save()` -> `Factor.update()` workflow:

    save_grid    = {'close': (1000, 2)}
    after update = {'close': (1000, 2), 'newvar': (512, 2)}     SINGLETON = False

That is the SECOND violation of this phase's own D-18 discipline in this same file. A phase whose
Wave-4 deliverable was "annotate falsified locked text in place rather than leave it standing"
must not seal while newly shipping an unconditional false safety claim.

This plan is the LEAD plan of the round and its Task 1 occupies the tracer slot. There is no new
end-to-end slice to lead with -- this phase's tracer landed in `03.6-01` and the pipeline it proved
is VERIFIED. Task 1 leads because every other item in scope group A depends on the false production
statement having been marked first: plan `03.6-11`'s `03.6-VERIFICATION.md` override is only
legitimate once this text has landed, and the report says so itself.

Folded in here because they sit in the SAME file and two of them in the SAME docstring region:
WR-06's remaining half (scope group C) -- `_widen_block_rows`'s and `_widen_chunked`'s transcribed
code excerpts, which still describe the pre-03.6-07 mechanism and carry no marker.

Purpose: D-18 is the discipline this whole round enforces. A falsified sentence is allowed to stay
in this codebase -- that is the point of D-18 -- but only under a marker, with the correction
beneath it. An unconditional false safety claim in a docstring the next author reads as a contract
is precisely what D-18 exists to forbid.

Output: `widen_and_append`'s grid paragraph narrowed and its accepted cost named with numbers;
`_widen_block_rows` and `_widen_chunked` excerpts marked; one new test arm pinning the accepted
cost through the `write()` path no existing arm touches.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@quantlab/dataset/backend.py
@tests/test_chunked_ingest.py
</context>

<baseline>
Recorded at plan time against a clean working tree, HEAD `00cd680`.

- **Whole suite:** `uv run pytest -q` -> **797 passed** (101.75s). Independently re-measured by
  the verifier this round. This is the number that must not regress.
- **`tests/test_chunked_ingest.py`:** **65 collected** (re-derived at plan time with
  `uv run pytest tests/test_chunked_ingest.py -q --collect-only`, not assumed).
- **`grep -c 'SUPERSEDED by' quantlab/dataset/backend.py`** -> **4** (lines 57, 234, 588, 1134).
  Note `SUPERSEDED_SUFFIX` at `:277`/`:388` does NOT match this pattern, which is why the pattern
  is `SUPERSEDED by` and not bare `SUPERSEDED`.
- **`grep -c 'NOT TRUE ANY MORE AS OF 03.6-07' quantlab/dataset/backend.py`** -> **0**.

Expected after each task, stated so an UNEXPLAINED change is a failure rather than a shrug:

| After  | `SUPERSEDED by` | `NOT TRUE ANY MORE AS OF 03.6-07` | `test_chunked_ingest.py` | whole suite |
|--------|-----------------|-----------------------------------|--------------------------|-------------|
| Task 1 | 5               | 1                                 | 65                       | 797         |
| Task 2 | 7               | 3                                 | 65                       | 797         |
| Task 3 | 7               | 3                                 | 66                       | 798         |

Line numbers in this plan were read from the live file at plan time and WILL shift as Tasks 1 and 2
insert text. Anchor every edit on the quoted sentence, never on the line number.
</baseline>

<scope_fence>
Byte-identical out of this plan, and each has an executable lock somewhere in the suite:

- `XrBackend.write()`'s body (`quantlab/dataset/backend.py:125-130`). It is the ROOT CAUSE and it
  is DELIBERATELY not fixed. Route (a) is declined.
- `_append_encoding`'s body -- `max(panel.sizes[dim], append_dim_size)` and
  `max(min(self.APPEND_DIM_CHUNK, size), 1)` (VERIFIED truths #13..#18, #20..#22, plus CR-01's
  regression arm).
- The `append_dim_size` threading through `_reconcile_new_listings`, `widen_symbol_axis`,
  `_widen_whole_store`, `_widen_chunked`, `widen_data_vars`.
- `--chunk` and the CLI (`quantlab/utils/cli.py`), locked by
  `tests/test_chunked_ingest.py::test_the_chunk_flag_itself_is_left_exactly_as_it_was`.
- `tests/test_symbol_axis_widening.py`, `tests/test_variable_axis_widening.py`,
  `tests/test_factor_update.py`, `tests/test_widening_fixture_realism.py`,
  `tests/test_dataset_update.py`. VERIFIED truth #21's evidence is literally "these files are not
  in the diff". Adding an arm to any of them falsifies a VERIFIED truth on re-verification. The
  new arm goes in `tests/test_chunked_ingest.py`.
- `quantlab/base/data.py` -- owned by plans `03.6-09` and `03.6-10` this round.

Nothing in this plan deletes text. D-18's whole shape is preserve-and-mark.
</scope_fence>

<coverage_audit>
## Multi-Source Coverage Audit — the whole round, recorded on the lead plan

Four source types audited. Nothing is omitted, nothing is reduced, no item is deferred without the
developer's word for it.

### GOAL — ROADMAP 03.6 phase goal

| Item | Status | Where |
|---|---|---|
| `TimeChunkPlanner` is the single chunking authority; ladder reaches `hour`; caller picks the rung explicitly; dense-panel estimator/guard group deleted | COVERED BY PRIOR WORK | Plans `03.6-01`..`03.6-07`. `03.6-VERIFICATION.md` scores SC-1..SC-8 each VERIFIED and states in its own words that the phase goal is ACHIEVED. This round re-plans NONE of it, and every plan here carries a `<scope_fence>` naming the verified behaviour that must come out byte-identical. |

### REQ — requirement IDs

| Item | Status | Where |
|---|---|---|
| (none) | N/A, RECORDED NOT SILENT | This phase's ROADMAP entry reads `Requirements: N/A -- this phase's contract is its Success Criteria (SC-1..SC-8)`; `init.plan-phase` returns the same string for `phase_req_ids`. There is no requirement ID to distribute, which is why every plan's `requirements` field carries this phase's own decision tags (`D-18`, `WR-03`, `WR-04`, `PHASE-GOAL-caller-owns-a-stated-cost`) the way `03.6-07-PLAN.md` does. DATA-08 is already formally Withdrawn (VERIFIED truth #12) and needs nothing here. |

### RESEARCH — RESEARCH.md features and constraints

| Item | Status | Where |
|---|---|---|
| (no RESEARCH.md for this phase) | N/A | `has_research: false`. `--gaps` skips research by definition. No research feature or constraint exists to leave uncovered. |

### CONTEXT — locked decisions

| Item | Status | Where |
|---|---|---|
| **D-18** — every locked text this phase falsifies gets an in-place `AMENDED`/`SUPERSEDED` annotation, original preserved | COVERED, and it is the governing decision of the entire round | `03.6-08` T1 (`widen_and_append`), T2 (`_widen_block_rows`, `_widen_chunked`); `03.6-09` T2 (`_restore_rebuild_asides`); `03.6-10` T3 (`SUPERSEDED_SUFFIX`); `03.6-11` T1 (`03.6-07` truth #3), T3 (`example/backend.md`, `example/chunking.md`) |
| **D-12** — `day`/`hour` ledger write amplification accepted | COVERED BY PRIOR WORK, untouched here | Named in `03.6-07`'s requirements and VERIFIED. No plan in this round goes near `ChunkLedger`. |
| Every other CONTEXT decision | COVERED BY PRIOR WORK | `03.6-VERIFICATION.md` truths #1..#12 |

### This round's own four scope groups (the actual contract for these four plans)

| Group | Item | Status | Where |
|---|---|---|---|
| A1 | Narrow the false `widen_and_append` sentence, D-18 shape, name the accepted cost | COVERED | `03.6-08` Task 1 |
| A2 | Retract `03.6-07-PLAN.md` `must_haves.truths` #3, visibly | COVERED | `03.6-11` Task 1 |
| A3 | Record the accepted cost in `deferred-items.md` | COVERED | `03.6-11` Task 1 |
| A4 | Override in `03.6-VERIFICATION.md`, `accepted_by: dzr`, AFTER A1 | COVERED | `03.6-11` Task 2, gated on a runnable `<precondition>` |
| A5 | Executable lock pinning the accepted cost on the `write()`-built path | COVERED | `03.6-08` Task 3 |
| B | `example/backend.md:94`, `example/chunking.md:255-264`, snippet to live signature | COVERED | `03.6-11` Task 3 |
| C | WR-06's remaining half: `_widen_block_rows` excerpt, `_widen_chunked` excerpt, `widen_and_append` prefix | COVERED | `03.6-08` Task 2 (first two) and Task 1 (the third, same docstring region as A1) |
| D / WR-03 | Residue crash guard + shared-suffix collision, repro-first | COVERED | `03.6-10` Tasks 1-3 |
| D / WR-04 | Exception masking in `_restore_rebuild_asides`, repro-first | COVERED | `03.6-09` Tasks 1-3 |

**Gaps found: NONE.** Every item in every source is covered by a plan in this round or by already
VERIFIED prior work. No item is dropped, simplified, staged as a "v1", or deferred.

**Exclusions, stated rather than assumed:** route (a) (pinning `encoding` in `XrBackend.write()`)
is NOT an unplanned item -- it is the closure route the developer explicitly did NOT choose, and
route (b) is planned in full instead. `03.6-REVIEW.md`'s `WR-05` and `IN-01`..`IN-05` are outside
the four scope groups the developer set for this round and are NOT silently dropped either: the
verifier recorded WR-05 as fixed-in-passing by CR-01's `max()` and IN-01..IN-05 as Info-level with
no must-have contact. `WR-01`/`WR-02` are the root cause of GAP #19 and are handled as an ACCEPTED
COST with a ledger entry, an override and an executable lock -- not as an unplanned gap.
</coverage_audit>

<probe_fallback>
**Spec-less probe fallback: SKIPPED, recorded visibly rather than silently.** This phase carries
`Requirements: N/A` (its contract is SC-1..SC-8), so the fallback has no requirement text to probe
and no probe-derived edge or prohibition predicates are generated for this round. There is no
SPEC.md, so there is no `## Edge Coverage` / `## Prohibitions` section to lift from either. The
`must_haves` above come from the phase goal and the round's scope groups in the normal way.
</probe_fallback>

<assumption_delta_decision>
The detector fired (`detected: true`) on three `pluralization` / `chosen` signals, all of them
quoting ROADMAP 03.6's SC-1 and SC-5 text about the granularity ladder going from one rung to five
and `day`/`hour` becoming selectable.

- **Noun now primary:** the five-rung `GRANULARITIES` ladder, not any single default rung.
- **Decision: `no-change`.** The promotion already happened, in `03.6-01`: `GRANULARITIES` is the
  five-tuple, `_period_key` has five explicit branches plus a drift-detecting trailing raise, and
  `add_chunk_args` DERIVES its `choices` from `GRANULARITIES` rather than restating them -- which
  is the generalized representation being primary rather than added alongside a still-required
  specific one. VERIFIED truths #1..#5 and #9 measure exactly this.
- Nothing in this round's four scope groups touches the ladder, `_period_key`, or the CLI, so
  there is no new generalization to promote here. Advisory, recorded, non-blocking.
</assumption_delta_decision>

<tasks>

<task type="auto">
  <name>Task 1 (LEAD): narrow `widen_and_append`'s grid paragraph and name the accepted cost, D-18 shape</name>
  <files>quantlab/dataset/backend.py</files>
  <read_first>
    - `quantlab/dataset/backend.py`, the `widen_and_append` docstring, currently `:1024-1032`
      (the paragraph beginning "This method reconciles all three axes AND makes all three of them
      agree on ONE chunk grid"). This is the text being marked.
    - `quantlab/dataset/backend.py:56-93` -- `APPEND_DIM_CHUNK`'s `#:` block. This is the D-18
      shape to MATCH: bolded lede naming what was wrong, then `SUPERSEDED by phase 03.6's ...
      pass; the original wording is quoted here rather than deleted so the correction is legible
      (D-18). It read: "<verbatim>", then why the falsity was load-bearing, then the numbered true
      mechanism, then the measured numbers.
    - `quantlab/dataset/backend.py:233-251` -- `append()`'s docstring paragraph, the second
      already-correct D-18 annotation in this file. The verifier read both of these and will read
      yours.
    - `quantlab/dataset/backend.py:125-130` -- `XrBackend.write()`. Read it to see for yourself
      that it passes no `encoding`; do NOT change it.
    - `quantlab/dataset/backend.py:1101-1196` -- `_append_encoding`, for the exact live signature
      and the `max()` lower-bound comment, so the correction describes the mechanism that is
      actually on disk.
    - `quantlab/base/factor.py:128-210` -- `Factor.save()` and `Factor.update()`, the documented
      workflow whose output the accepted cost describes. `update()` passes no `append_dim_size`
      and has no source for one.
    - `.planning/phases/03.6-frequency-keyed-chunking-policy/03.6-VERIFICATION.md`, the `gaps:`
      block and section 二 -- the measured numbers and the reason this is a gap.
  </read_first>
  <action>
Mark and narrow the grid paragraph in `XrBackend.widen_and_append`'s docstring. Route (b) per the
developer's locked decision; D-18 shape, matching `APPEND_DIM_CHUNK`'s `#:` block and `append()`'s
docstring exactly. DELETE NOTHING.

Produce, in this order, in place of the current single paragraph:

1. A new bolded lede naming what was wrong, immediately followed by the literal marker string
   `NOT TRUE ANY MORE AS OF 03.6-07` and then `SUPERSEDED by phase 03.6's third gap-closure pass
   (plan 03.6-08); the original wording is kept below rather than deleted so the correction is
   legible (D-18).` Use that marker string verbatim, character for character, and keep it on ONE
   source line with no wrapping inside it -- Task 2 and the acceptance criteria count it with
   `grep -c` and with a substring test on `inspect.getdoc(...)`, and a marker split across two
   lines matches neither.

2. The ENTIRE existing paragraph, verbatim and unedited, from "This method reconciles all three
   axes AND makes all three of them agree on ONE chunk grid." through "...so a store cannot come
   out of here carrying one grid per axis." It now sits UNDER the marker instead of standing as
   the method's claim. The sentence about the signature deliberately not growing the parameter is
   still TRUE and stays where it is, outside the marked block.

3. The narrowed, true claim. Scope it to THIS CALL: the three things `widen_and_append` itself
   writes -- the `widen_symbol_axis` rewrite, the `widen_data_vars` filler, and the closing
   `append()` -- all read `append_dim_size` from the same `kwargs` entry, so those three cannot
   disagree WITH EACH OTHER. State explicitly that this says nothing about variables the store
   already held from an earlier write, which is exactly the half the original sentence over-claimed.

4. The accepted cost, named with its mechanism and its numbers. `XrBackend.write()` passes no
   `encoding` at all, so a store created by `BaseDataset.save()` / `Factor.save()` lands on Zarr's
   default grid (the panel's own full shape for a numpy-backed panel) and has NEVER sat on
   `min(APPEND_DIM_CHUNK, total)`. `Factor.update()` holds no source for the store's total extent
   and therefore passes no `append_dim_size`, so the filler takes `min(APPEND_DIM_CHUNK,
   filler_len)`. Cite the measurement verbatim: 2026-09-13, at the REAL default
   `APPEND_DIM_CHUNK = 512` with no monkeypatch, along `Factor.save()` -> `Factor.update()`, a
   1000-row store came back as `{'close': (1000, 2)}` and the update added `'newvar': (512, 2)` --
   two grids in one store. Say the numbers so the next reader does not re-derive them.

5. The disposition, stated plainly: root cause is WR-02 (`write()` never pins `encoding`); it
   predates 03.6; it is INDEPENDENT of the `--chunk` rung (the filler's grid is decided by
   `APPEND_DIM_CHUNK` and the filler's length alone), so it does not touch SC-1..SC-8 or the phase
   goal's caller-owns-a-stated-cost half. It is ACCEPTED, recorded in
   `.planning/phases/03.6-frequency-keyed-chunking-policy/deferred-items.md`, and pinned by
   `tests/test_chunked_ingest.py::test_a_store_built_by_write_keeps_two_chunk_grids_accepted_cost`
   (Task 3 of this plan). Name the route not taken and why: pinning `encoding` in `write()` would
   change the on-disk grid of every store this project writes, on a pre-03.6 bulk-write path
   declined as out of scope in `03.6-REVIEW-FIX.md`.

Prose only. Do not introduce a new transcribed code excerpt into this docstring -- WR-06 is in this
plan precisely because transcribed excerpts rot. Name identifiers inline instead.

Touch no executable statement anywhere in the file. This task's diff is docstring text only.
  </action>
  <verify>
    <automated>uv run python -c "import inspect; from quantlab.dataset.backend import XrBackend; d = inspect.getdoc(XrBackend.widen_and_append); m = d.index('NOT TRUE ANY MORE AS OF 03.6-07'); s = d.index('SUPERSEDED by'); o = d.index('a store cannot come out of here carrying one grid per axis'); assert m < o and s < o, 'the falsified sentence is not under the marker'; assert 'D-18' in d[m:o], 'the marker block does not cite D-18'; assert '(1000, 2)' in d and '(512, 2)' in d, 'the measured numbers are absent'; assert 'deferred-items.md' in d, 'the accepted cost points at no ledger'; assert 'WR-02' in d, 'the root cause is not named'; print('widen_and_append narrowed, D-18 shape, cost named')"</automated>
    <fails_when>non-zero exit, or any AssertionError / ValueError text in the output instead of the final `widen_and_append narrowed` line</fails_when>
    <automated>test "$(grep -c 'SUPERSEDED by' quantlab/dataset/backend.py)" = "5" && test "$(grep -c 'NOT TRUE ANY MORE AS OF 03.6-07' quantlab/dataset/backend.py)" = "1" && echo "marker counts 5/1 ok"</automated>
    <fails_when>non-zero exit, or the `marker counts 5/1 ok` line is absent (the counts are 4/0 if nothing landed, or higher if Task 2 ran out of order)</fails_when>
    <automated>git show 00cd680:quantlab/dataset/backend.py > /tmp/ql_backend_base.py && uv run python -c "import ast, pathlib; strip = lambda p: (lambda t: ([setattr(n, 'body', n.body[1:]) for n in ast.walk(t) if isinstance(n, (ast.Module, ast.ClassDef, ast.FunctionDef, ast.AsyncFunctionDef)) and n.body and isinstance(n.body[0], ast.Expr) and isinstance(n.body[0].value, ast.Constant) and isinstance(n.body[0].value.value, str)], ast.dump(t))[1])(ast.parse(pathlib.Path(p).read_text())); base = strip('/tmp/ql_backend_base.py'); now = strip('quantlab/dataset/backend.py'); assert base == now, 'the executable AST changed -- THIS task edited an executable statement, not only docstring text'; open('/tmp/ql_ast_task1.txt', 'w').write(now); print('executable AST identical to 00cd680; snapshot captured for Task 2')"</automated>
    <fails_when>non-zero exit, or the message `the executable AST changed`, or the final `executable AST identical to 00cd680` line is absent. The baseline is read from the COMMIT OBJECT `00cd680`, not from a snapshot this task took of its own output, so this gate machine-checks THIS task's no-executable-change claim rather than only the next task's. Ordering is a non-issue: `git show` reads an immutable blob, so it is a genuine pre-edit baseline whenever it runs</fails_when>
    <automated>uv run pytest tests/test_chunked_ingest.py -q</automated>
    <fails_when>non-zero exit, or a "failed"/"error" count in the summary line, or a passed count other than 65</fails_when>
  </verify>
  <acceptance_criteria>
    - `inspect.getdoc(XrBackend.widen_and_append)` contains the literal
      `a store cannot come out of here carrying one grid per axis` EXACTLY ONCE, and its index is
      greater than the index of `NOT TRUE ANY MORE AS OF 03.6-07` and greater than the index of
      `SUPERSEDED by`. (Preserve-and-mark, not delete.)
    - The text between the marker and the preserved sentence contains `D-18`.
    - The same docstring contains all of: `(1000, 2)`, `(512, 2)`, `WR-02`, `deferred-items.md`,
      and `test_a_store_built_by_write_keeps_two_chunk_grids_accepted_cost`.
    - `grep -c 'SUPERSEDED by' quantlab/dataset/backend.py` outputs exactly `5`.
    - `grep -c 'NOT TRUE ANY MORE AS OF 03.6-07' quantlab/dataset/backend.py` outputs exactly `1`.
    - The docstring-stripped AST of `quantlab/dataset/backend.py` is byte-identical to the
      docstring-stripped AST of `git show 00cd680:quantlab/dataset/backend.py`, both produced by the
      SAME `strip` routine in one command. This is THIS task's own machine-checked
      no-executable-change proof -- Task 1 is the task that edits a docstring region adjacent to
      live statements, so it does not get to rest on a snapshot taken of its own output and checked
      only by Task 2.
    - `git diff 00cd680 -- quantlab/dataset/backend.py` shows changes inside the
      `widen_and_append` docstring only; no `+`/`-` line outside a triple-quoted string. The eyeball
      form is kept ALONGSIDE the AST criterion above, not instead of it, because a reviewer reads
      the diff and the AST check cannot tell a docstring edit from a comment edit.
    - `uv run pytest tests/test_chunked_ingest.py -q` reports `65 passed`.
  </acceptance_criteria>
  <reversibility rating="reversible">Docstring text under D-18 preserve-and-mark. Nothing is deleted and no executable statement moves, so the edit reverts with `git revert` and costs nothing on disk.</reversibility>
  <done>The sentence `03.6-VERIFICATION.md` measured FALSE no longer stands as an unconditional claim: it sits verbatim under a `NOT TRUE ANY MORE AS OF 03.6-07` / `SUPERSEDED by` marker with the narrowed per-call claim, the named accepted cost and the measured `(1000, 2)` / `(512, 2)` numbers beneath it. Grid behaviour is unchanged and `tests/test_chunked_ingest.py` still reports 65 passed.</done>
</task>

<task type="auto">
  <name>Task 2: mark the two remaining stale code excerpts -- WR-06's other half (scope group C)</name>
  <files>quantlab/dataset/backend.py</files>
  <read_first>
    - `quantlab/dataset/backend.py:555-625` -- `_widen_block_rows`. The target is the opening
      section's transcribed excerpt: "The FIRST block is written with
      `encoding=self._append_encoding(append_dim, data=first_block)` ... so the block's own length
      is what decides the store's on-disk append-dim chunk". Note that the LATER half of this same
      docstring (`:577-608`) already corrects this and already carries a `SUPERSEDED by` at `:588`
      -- that one is VERIFIED truth #20 and is NOT what this task edits. The excerpt at the top is
      the unmarked one.
    - `quantlab/dataset/backend.py:735-800` -- `_widen_chunked`. The target is "The FIRST block
      creates the sidecar with `mode="w"` and `encoding=self._append_encoding(append_dim,
      data=block)`, so the chunk rule stays single-sourced and the block-size constraint in
      `_widen_block_rows` is what makes the resulting grid match the whole-store path's."
    - `quantlab/dataset/backend.py:785-800` -- `_widen_chunked`'s LIVE first-block write, which
      now passes `append_dim_size=append_dim_size` as well as `data=block`. This is the mechanism
      the correction must describe.
    - `quantlab/dataset/backend.py:1101-1112` -- `_append_encoding`'s live signature
      (`append_dim`, `data=None`, keyword-only `append_dim_size=None`), so the corrected prose
      names the real parameter set.
    - The Task 1 diff in this same file, so the two edits do not fight. Task 1 owns the
      `widen_and_append` docstring; this task owns `_widen_block_rows` and `_widen_chunked` only.
    - `.planning/phases/03.6-frequency-keyed-chunking-policy/03.6-REVIEW.md`, `### WR-06` -- the
      full finding.
  </read_first>
  <action>
Apply the same D-18 shape Task 1 used to the two remaining stale excerpts WR-06 names. Two edits,
both docstring text, both preserve-and-mark.

In BOTH edits, keep the marker string `NOT TRUE ANY MORE AS OF 03.6-07` on ONE source line, exactly
as Task 1 did -- the counts and the `inspect.getdoc(...)` substring tests below match nothing if it
wraps.

Edit A -- `_widen_block_rows`, the opening section's transcribed excerpt. Insert the literal marker
`NOT TRUE ANY MORE AS OF 03.6-07` plus a `SUPERSEDED by phase 03.6's third gap-closure pass (plan
03.6-08); the original wording is kept below rather than deleted so the correction is legible
(D-18).` lede immediately BEFORE the transcribed sentence, leave the transcription and its
conclusion verbatim, and write the correction beneath it as PROSE: the first block's write now also
passes the caller's stated `append_dim_size`, so what decides the store's on-disk append-dim chunk
is `min(APPEND_DIM_CHUNK, max(block length, stated extent))` -- the block's own length is only the
answer when the caller states nothing. Name `_append_encoding` and its keyword-only
`append_dim_size` inline; do NOT transcribe the call's argument list again. Leave the measured
2026-09-08 600-timestamp figures and the `max(APPEND_DIM_CHUNK, ...)` floor argument exactly as
they are -- the floor is still live and the figures are still what they measured.

Edit B -- `_widen_chunked`, the transcribed first-block sentence. Same marker, same lede, original
preserved verbatim, correction beneath as prose: the two strategies now agree on the grid because
BOTH read the same `append_dim_size` keyword, not because `_widen_block_rows`'s block-size
constraint makes them coincide. State that the block-size constraint is still live and still does
its own job (bounding BYTES under `MAX_WIDEN_BYTES`, which no append-dimension length can express)
-- it simply is no longer the thing that aligns the grids. Point at `_widen_block_rows`'s own
already-correct floor-versus-`append_dim_size` paragraph rather than restating its argument.

DEVIATION FROM WR-06, RECORDED DELIBERATELY: WR-06's suggested fix is to DELETE the transcribed
excerpts ("a snippet always rots, prose does not"). This phase's D-18 forbids deleting falsified
text -- preserve-and-mark is the whole discipline, and it is the discipline this entire round
enforces. The half of WR-06 that is adopted is the marker prefix; the half that is declined is the
deletion. The rot concern is answered by writing every CORRECTION as prose, so no new transcription
enters the file. Write that reasoning into Edit A's annotation in one sentence, so the next reader
sees the excerpt was kept on purpose rather than missed.

Touch no executable statement. This task's diff is docstring text only.
  </action>
  <verify>
    <automated>uv run python -c "import inspect; from quantlab.dataset.backend import XrBackend; M='NOT TRUE ANY MORE AS OF 03.6-07'; a=inspect.getdoc(XrBackend._widen_block_rows); b=inspect.getdoc(XrBackend._widen_chunked); assert M in a and M in b, 'a marker is missing'; assert a.index(M) < a.index(\"the block's own length is what decides\"), 'the _widen_block_rows excerpt is not under its marker'; assert b.index(M) < b.index('block-size constraint in'), 'the _widen_chunked excerpt is not under its marker'; assert 'append_dim_size' in a[a.index(M):] and 'append_dim_size' in b[b.index(M):], 'a correction does not name the keyword'; assert 'MAX_WIDEN_BYTES' in b, 'the byte-bounding job of the floor is not restated'; print('WR-06 remaining half marked')"</automated>
    <fails_when>non-zero exit, or any AssertionError text instead of the final `WR-06 remaining half marked` line</fails_when>
    <automated>test "$(grep -c 'NOT TRUE ANY MORE AS OF 03.6-07' quantlab/dataset/backend.py)" = "3" && test "$(grep -c 'SUPERSEDED by' quantlab/dataset/backend.py)" = "7" && echo "marker counts 3/7 ok"</automated>
    <fails_when>non-zero exit, or the `marker counts 3/7 ok` line is absent (1/5 means only Task 1 landed)</fails_when>
    <automated>git show 00cd680:quantlab/dataset/backend.py > /tmp/ql_backend_base.py && uv run python -c "import ast, pathlib; strip = lambda p: (lambda t: ([setattr(n, 'body', n.body[1:]) for n in ast.walk(t) if isinstance(n, (ast.Module, ast.ClassDef, ast.FunctionDef, ast.AsyncFunctionDef)) and n.body and isinstance(n.body[0], ast.Expr) and isinstance(n.body[0].value, ast.Constant) and isinstance(n.body[0].value.value, str)], ast.dump(t))[1])(ast.parse(pathlib.Path(p).read_text())); base = strip('/tmp/ql_backend_base.py'); now = strip('quantlab/dataset/backend.py'); assert base == now, 'the executable AST changed -- a docstring-only task edited code'; assert open('/tmp/ql_ast_task1.txt').read() == now, 'the AST differs from Task 1 snapshot -- Task 1 did not run its gate, or ran against a different tree'; print('executable AST identical to 00cd680 and to Task 1')"</automated>
    <fails_when>non-zero exit, or the message `the executable AST changed`, or the message `the AST differs from Task 1 snapshot`, or a FileNotFoundError for `/tmp/ql_ast_task1.txt` (Task 1 did not run its capture step). Compares against the immutable `00cd680` blob with the IDENTICAL `strip` routine Task 1 used -- same lambda, same walk, same node classes -- rather than against a snapshot alone, so the two tasks are checked against one fixed reference instead of a chain</fails_when>
    <automated>uv run pytest tests/test_chunked_ingest.py tests/test_symbol_axis_widening.py tests/test_variable_axis_widening.py -q</automated>
    <fails_when>non-zero exit, or a "failed"/"error" count in the summary line</fails_when>
  </verify>
  <acceptance_criteria>
    - `inspect.getdoc(XrBackend._widen_block_rows)` contains `NOT TRUE ANY MORE AS OF 03.6-07` at
      an index LOWER than the index of `the block's own length is what decides`, and that original
      sentence is still present verbatim.
    - `inspect.getdoc(XrBackend._widen_chunked)` contains `NOT TRUE ANY MORE AS OF 03.6-07` at an
      index LOWER than the index of `block-size constraint in`, and that original sentence is still
      present verbatim.
    - Both corrections name `append_dim_size`; `_widen_chunked`'s correction still names
      `MAX_WIDEN_BYTES` as the floor's surviving job.
    - `_widen_block_rows`'s annotation contains the word `D-18` and states in one sentence why the
      excerpt was kept rather than deleted.
    - `grep -c 'NOT TRUE ANY MORE AS OF 03.6-07' quantlab/dataset/backend.py` outputs exactly `3`.
    - `grep -c 'SUPERSEDED by' quantlab/dataset/backend.py` outputs exactly `7`.
    - The docstring-stripped AST of `quantlab.dataset.backend` is byte-identical to the one
      captured at the end of Task 1.
    - `uv run pytest tests/test_chunked_ingest.py tests/test_symbol_axis_widening.py
      tests/test_variable_axis_widening.py -q` reports zero failures, and
      `git diff --name-only 00cd680 -- tests/test_symbol_axis_widening.py tests/test_variable_axis_widening.py tests/test_factor_update.py tests/test_widening_fixture_realism.py tests/test_dataset_update.py` produces NO output.
  </acceptance_criteria>
  <reversibility rating="reversible">Docstring text only, preserve-and-mark. Reverts cleanly.</reversibility>
  <done>WR-06's remaining half is closed: both transcribed excerpts carry the `NOT TRUE ANY MORE AS OF 03.6-07` prefix over preserved originals with prose corrections beneath, the marker counts are 3 and 7, and the file's executable AST is unchanged since Task 1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the executable lock for the accepted cost -- a `write()`-built store keeps TWO grids</name>
  <files>tests/test_chunked_ingest.py</files>
  <read_first>
    - `tests/test_chunked_ingest.py:1387-1454` -- `_incomplete_store` and
      `test_a_variable_widen_leaves_one_chunk_grid_not_two`. READ THIS FIRST and understand WHY
      existing coverage is blind to the gap: `_incomplete_store` builds its store with
      `append(append_dim_size=9)`, i.e. a store already ON the correct grid, so no existing arm in
      this family ever goes through `write()` / `save()`. The new arm goes IMMEDIATELY AFTER
      `test_a_variable_widen_leaves_one_chunk_grid_not_two`.
    - `tests/test_chunked_ingest.py:164-180` -- `_chunk_grid`, the module's ONE read idiom for the
      on-disk grid. Reuse it; do not add a second way to read chunks.
    - `tests/test_chunked_ingest.py` -- `_small_panel` (the 3-line `close`-only panel helper the
      sibling arms use) and `_panel`.
    - `quantlab/dataset/backend.py:125-130` -- `XrBackend.write()`, the path under test.
    - `quantlab/dataset/backend.py:1101-1196` -- `_append_encoding`, for the filler's arithmetic.
    - The `widen_and_append` docstring as Task 1 left it, so the test's docstring and the
      production docstring tell the same story with the same numbers.
  </read_first>
  <behavior>
    - Under `monkeypatch.setattr(XrBackend, "APPEND_DIM_CHUNK", 4)`, a store built by
      `XrBackend().to_internal(panel).write(path)` from a 10-row x 2-symbol `close` panel comes
      back on Zarr's default grid: `_chunk_grid(path) == {"close": (10, 2)}`. NOT
      `(min(APPEND_DIM_CHUNK, 10), 2)`.
    - Adding a new variable through `widen_and_append(path, append_dim_size=15)` lands that
      variable on `(min(APPEND_DIM_CHUNK, 15), 2) == (4, 2)` while `close` stays `(10, 2)`.
    - Therefore `len(set(_chunk_grid(path).values())) == 2` -- the accepted cost, pinned.
    - The pre-existing variable's grid is UNCHANGED by the widen (Zarr cannot revise it), so the
      test also proves the cost is a creation-time property, not something `widen_and_append`
      introduced.
  </behavior>
  <action>
Add ONE new test function to `tests/test_chunked_ingest.py`, named exactly
`test_a_store_built_by_write_keeps_two_chunk_grids_accepted_cost`, placed immediately after
`test_a_variable_widen_leaves_one_chunk_grid_not_two`.

This arm PINS AN ACCEPTED COST. It is not a red-to-green repro and the behaviour it measures is
deliberately NOT being fixed this round (route (b)). Say that in the test docstring, plainly, and
say what must happen the day it goes red: `XrBackend.write()` has started pinning `encoding`, and
when that happens `deferred-items.md`, `widen_and_append`'s docstring and plan `03.6-07`'s
`must_haves.truths` #3 must all be updated in the same change. A test whose failure instruction is
"update these three artifacts together" is the point of writing it.

Shape: monkeypatch `XrBackend.APPEND_DIM_CHUNK` to 4 (the module's established idiom, and it makes
the two grids differ by a wide margin). Build a 10-timestamp x 2-symbol `close` panel with
`_small_panel` and write it with `XrBackend().to_internal(panel).write(path)` -- `write()`, NOT
`append()`, and NOT `save()`, because `Factor.save()` is just `write()` plus a Timer and an error
wrapper and this module has no Factor fixtures. Assert the creating grid is `{"close": (10, 2)}`
with a message naming `write()` passing no `encoding` as the reason. Then build a later,
non-overlapping 3-timestamp panel on the same symbols, add a `newvar` data variable to it, and call
`widen_and_append(path, append_dim_size=15)`. Assert `close` is still `(10, 2)`, `newvar` is
`(min(XrBackend.APPEND_DIM_CHUNK, 15), 2)`, and `len(set(_chunk_grid(path).values())) == 2` -- each
with a message that names the mechanism, not just the numbers.

In the docstring cite BOTH measurements from `03.6-VERIFICATION.md` so a reader knows this is not a
monkeypatch artifact: the `(10, 2)` / `(4, 2)` pair this arm reproduces, and the decisive
no-monkeypatch pair at the real default `APPEND_DIM_CHUNK = 512` -- `{'close': (1000, 2)}` then
`{'close': (1000, 2), 'newvar': (512, 2)}` -- measured along `Factor.save()` -> `Factor.update()`.
Name the root cause as WR-02 and state that it predates 03.6 and is independent of the `--chunk`
rung, which is why it leaves SC-1..SC-8 and the phase goal intact.

Do not touch any other test in the file. Do not touch `_incomplete_store` or
`test_a_variable_widen_leaves_one_chunk_grid_not_two` -- the latter is the arm that asserts a
SINGLETON on the `append(append_dim_size=...)`-built store, and both it and the new arm are correct
at the same time; that pair IS the narrowed claim made executable.
  </action>
  <verify>
    <automated>uv run pytest tests/test_chunked_ingest.py::test_a_store_built_by_write_keeps_two_chunk_grids_accepted_cost -q</automated>
    <fails_when>non-zero exit, or `no tests ran` / `ERROR ... not found` (the name does not match), or a "failed" count in the summary line</fails_when>
    <automated>uv run pytest tests/test_chunked_ingest.py -q</automated>
    <fails_when>non-zero exit, or a passed count other than 66, or any "failed"/"error" count in the summary line</fails_when>
    <automated>uv run python -c "import inspect, tests.test_chunked_ingest as t; d = inspect.getdoc(t.test_a_store_built_by_write_keeps_two_chunk_grids_accepted_cost); assert 'WR-02' in d, 'the root cause is not named'; assert '(1000, 2)' in d and '(512, 2)' in d, 'the no-monkeypatch default measurement is not cited'; assert 'deferred-items.md' in d and '03.6-07' in d, 'the go-red instruction does not name the artifacts to update together'; src = inspect.getsource(t.test_a_store_built_by_write_keeps_two_chunk_grids_accepted_cost); assert '.write(' in src, 'the arm does not build its store through write()'; assert 'append_dim_size=15' in src, 'the widen does not state an extent'; print('accepted-cost lock in place')"</automated>
    <fails_when>non-zero exit, or any AssertionError text instead of the final `accepted-cost lock in place` line, or a ModuleNotFoundError for `tests.test_chunked_ingest` (run it with `uv run python -c` from the repo root so `tests/` is importable; if it is not, substitute an `importlib`-by-path load rather than changing the assertions)</fails_when>
    <automated>uv run pytest -q</automated>
    <fails_when>non-zero exit, or a passed count below 798, or any "failed"/"error" count in the summary line</fails_when>
    <automated>git diff --name-only 00cd680 -- tests/test_symbol_axis_widening.py tests/test_variable_axis_widening.py tests/test_factor_update.py tests/test_widening_fixture_realism.py tests/test_dataset_update.py</automated>
    <fails_when>ANY output line at all. Each of those five files is VERIFIED truth #21's evidence ("these files are not in the diff"); one appearing here falsifies a truth that is already passing. Scoped to those five paths deliberately rather than asserting the whole `tests/` diff is one file: plan `03.6-09` runs in the SAME wave and adds `tests/test_rebuild_aside_recovery.py`, and this project's worktree isolation is known to degrade to a shared checkout, so a global diff assertion would false-positive on a sibling plan's legitimate work</fails_when>
    <automated>git diff --stat 00cd680 -- quantlab/dataset/backend.py tests/test_chunked_ingest.py</automated>
    <fails_when>empty output -- this plan's own two files show no change, meaning nothing landed</fails_when>
  </verify>
  <acceptance_criteria>
    - `tests/test_chunked_ingest.py::test_a_store_built_by_write_keeps_two_chunk_grids_accepted_cost`
      exists and passes.
    - Its source builds the store with `.write(` and widens with `append_dim_size=15`.
    - Its docstring names `WR-02`, cites `(1000, 2)` and `(512, 2)`, and names both
      `deferred-items.md` and `03.6-07` as artifacts to update together when it goes red.
    - The test asserts all three of: `close` on `(10, 2)`, `newvar` on
      `(min(XrBackend.APPEND_DIM_CHUNK, 15), 2)`, and a grid set of size exactly 2.
    - `uv run pytest tests/test_chunked_ingest.py -q` reports exactly `66 passed`.
    - `uv run pytest -q` reports at least `798 passed` and zero failures.
    - `git diff --name-only 00cd680 -- tests/test_symbol_axis_widening.py tests/test_variable_axis_widening.py tests/test_factor_update.py tests/test_widening_fixture_realism.py tests/test_dataset_update.py`
      produces NO output, and `git diff --stat 00cd680 -- tests/test_chunked_ingest.py` is
      non-empty. Scoped to those five fenced paths -- the SAME scoping this task's `<verify>`
      command uses, and for the same stated reason -- rather than asserting the whole `tests/`
      diff is one file: plan `03.6-09` runs in the SAME wave and legitimately adds
      `tests/test_rebuild_aside_recovery.py`, and this project's worktree isolation is known to
      degrade to a shared checkout, so a whole-`tests/` assertion would read as a failure whenever
      `03.6-09` lands first.
  </acceptance_criteria>
  <reversibility rating="reversible">A new test function in an existing module. Deleting it restores the prior state exactly; it adds no production behaviour.</reversibility>
  <done>The accepted cost has an executable lock on the `write()`-built path no existing arm touched. `tests/test_chunked_ingest.py` reports 66 passed, the whole suite reports at least 798 passed with zero failures, and no fenced widen/update test module appears in the diff.</done>
</task>

</tasks>

<artifacts_this_plan_produces>
New symbols and files created by this plan (the source-grounding pass must EXCLUDE these from drift
verification -- they do not exist before this plan runs):

- `tests/test_chunked_ingest.py::test_a_store_built_by_write_keeps_two_chunk_grids_accepted_cost`
  (new test function)
- `/tmp/ql_backend_base.py` (transient copy of `git show 00cd680:quantlab/dataset/backend.py`,
  rewritten identically by Task 1's and Task 2's verify; not committed, not a deliverable)
- `/tmp/ql_ast_task1.txt` (transient AST snapshot written by Task 1's verify and read by Task 2's;
  not committed, not a deliverable)

No new production symbols. No new files under `quantlab/`. Every production identifier this plan
names -- `XrBackend.write`, `XrBackend.widen_and_append`, `XrBackend._widen_block_rows`,
`XrBackend._widen_chunked`, `XrBackend._append_encoding`, `XrBackend.APPEND_DIM_CHUNK`,
`Factor.save`, `Factor.update` -- already exists on the live tree and was read at plan time.
</artifacts_this_plan_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docstring -> next author | A production docstring is read as a CONTRACT by the next author and by the phase verifier. A false unconditional claim there is an untrusted input crossing into design decisions. |
| test suite -> seal decision | A green suite is the evidence a phase seals on. A cost with no arm asserting it is invisible at the boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.6-08-01 | Tampering | `XrBackend.widen_and_append` docstring | medium | mitigate | The falsified unconditional claim is marked `NOT TRUE ANY MORE AS OF 03.6-07` / `SUPERSEDED by` with the narrowed true claim beneath (Task 1). A future author cannot read the over-claim without reading the marker first, because the marker precedes it in the text. |
| T-03.6-08-02 | Information disclosure (of a false safety property) | `_widen_block_rows` / `_widen_chunked` transcribed excerpts | low | mitigate | Both excerpts marked and corrected in prose (Task 2); no NEW transcription is introduced, so the class of defect does not recur in this change. |
| T-03.6-08-03 | Repudiation | the accepted cost | medium | mitigate | An executable arm pins the two-grid outcome and its docstring names the three artifacts that must change together when it goes red (Task 3). The cost cannot later be denied or silently "fixed" without a red test. |
| T-03.6-08-04 | Tampering | `XrBackend.write()` / `_append_encoding` / `--chunk` | medium | accept | Route (a) is DECLINED by developer decision: pinning `encoding` in `write()` would change the on-disk grid of every store this project writes, on a pre-03.6 bulk-write path. The residual risk (a two-grid store on the `save()` -> `update()` workflow) is data-layout inefficiency, not data loss or corruption -- values and coordinates are unaffected. Accepted, recorded in `deferred-items.md` by plan `03.6-11`, pinned by T-03.6-08-03's arm. **Severity is `medium`, not `high`, and the re-rating changes the LABEL only -- not one word of the acceptance above.** Severity is impact x likelihood: the likelihood is certain (every `save()` -> `update()` run on this repo), but the impact is the layout inefficiency the cell already states, on a single-operator local research codebase with no multi-tenant or network surface. `high` was the wrong band for that product and would make a future ASVS gate stall on a cost this round formally overrides in `03.6-VERIFICATION.md` (plan `03.6-11` Task 2) -- a gate stalling on an already-signed override is noise, not safety. |
| T-03.6-08-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs NO packages and adds no dependency. `pyproject.toml` and `uv.lock` are outside `files_modified`, so the package-legitimacy gate has nothing to audit. Recorded rather than omitted so the absence is a visible choice. |

ASVS level 1, block on `high`. NO row in this register is `high`. The one `accept` row that carries
a real residual risk is `T-03.6-08-04`, rated `medium` on the reasoning in its own cell: a certain
occurrence with a layout-only impact on a local single-operator research codebase. Its acceptance is
the developer's locked route-(b) decision and is recorded with its residual-risk analysis rather
than waved through; the `medium` rating re-states that risk at its measured size, it does not
soften it, and route (a) remains available in `deferred-items.md` if it is ever picked up.
</threat_model>

<verification>
1. `uv run pytest -q` -> at least 798 passed, zero failed. The 797 baseline must not regress.
2. `grep -c 'NOT TRUE ANY MORE AS OF 03.6-07' quantlab/dataset/backend.py` -> `3`;
   `grep -c 'SUPERSEDED by' quantlab/dataset/backend.py` -> `7`.
3. The docstring-stripped AST of `quantlab/dataset/backend.py` equals that of
   `git show 00cd680:quantlab/dataset/backend.py` at the END OF TASK 1 and again at the end of
   Task 2, both via the same `strip` routine -- proof that group A (Task 1) and group C (Task 2)
   EACH changed no executable statement, measured against one immutable reference rather than
   against each other.
4. `git diff --name-only 00cd680 -- tests/test_symbol_axis_widening.py tests/test_variable_axis_widening.py tests/test_factor_update.py tests/test_widening_fixture_realism.py tests/test_dataset_update.py` is empty, and
   `git diff --stat 00cd680 -- quantlab/dataset/backend.py tests/test_chunked_ingest.py` is
   non-empty. Scoped rather than a whole-tree listing: plan `03.6-09` runs in the SAME wave and
   legitimately adds `tests/test_rebuild_aside_recovery.py`.
5. `uv run pytest tests/test_chunked_ingest.py -k "grid or narrow_stated" -q` -> 12 passed before
   Task 3, 12 passed after (the new arm's name contains `grids`, so confirm the count the verifier
   measured either holds at 12 or rises to 13 for a reason you can state; do not adjust the filter
   to hide a change).
</verification>

<success_criteria>
- `XrBackend.widen_and_append`'s docstring no longer carries an unconditional claim that
  `03.6-VERIFICATION.md` measured false; the original sits under a marker with the narrowed claim,
  the named accepted cost and the measured numbers beneath it.
- `_widen_block_rows` and `_widen_chunked` carry the same marker over their stale excerpts.
- A test arm builds a store through `write()` and pins the two-grid outcome with the reason and the
  go-red instruction in its docstring.
- Zero production behaviour change: same AST, 797 -> 798 passed with the delta fully explained by
  the one new arm.
</success_criteria>

<output>
Create `.planning/phases/03.6-frequency-keyed-chunking-policy/03.6-08-SUMMARY.md` when done.
Record in it: the exact marker counts after each task, the whole-suite count, and the fact that
plan `03.6-11`'s `03.6-VERIFICATION.md` override is now unblocked (its precondition is this plan's
Task 1 having landed).
</output>
