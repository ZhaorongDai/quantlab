---
phase: 03.6-frequency-keyed-chunking-policy
plan: 11
type: execute
wave: 2
depends_on: ["03.6-08"]
gap_closure: true
files_modified:
  - .planning/phases/03.6-frequency-keyed-chunking-policy/03.6-07-PLAN.md
  - .planning/phases/03.6-frequency-keyed-chunking-policy/deferred-items.md
  - .planning/phases/03.6-frequency-keyed-chunking-policy/03.6-VERIFICATION.md
  - example/backend.md
  - example/chunking.md
autonomous: true
requirements: [D-18, PHASE-GOAL-caller-owns-a-stated-cost]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "GAP #19, route (b), paperwork half: plan `03.6-07`'s `must_haves.truths` #3 no longer asserts a universal that was measured false. Its scope now matches the narrowed production claim plan `03.6-08` landed -- per-call consistency, not `for every store this project writes` -- and the retraction is VISIBLE rather than a silent frontmatter edit: the original wording is preserved in the same entry under a `SUPERSEDED` marker (the same D-18 shape used in the code), with a one-line pointer to `03.6-VERIFICATION.md` truth #19 and to plan `03.6-08`. The file's other five truths, and every other key in it, are untouched, and the frontmatter still parses as YAML."
    - "The accepted cost is in the phase's own ledger, matching the shape of the entry already in `deferred-items.md`: `XrBackend.write()` pins no `encoding`; stores built by `save()` are off the `min(APPEND_DIM_CHUNK, total)` grid; `Factor.update()` has no total-extent source for the filler; the consequence is a two-grid store on the documented `save()` -> `update()` workflow; root cause WR-02 / WR-01, predates 03.6, independent of the `--chunk` rung and therefore untouching SC-1..SC-8 and the phase goal; route (a) remains available if it is ever picked up."
    - "`03.6-VERIFICATION.md` carries the override in the shape the report itself specified, with `accepted_by: dzr`, and it was written AFTER plan `03.6-08`'s text change landed -- never before. The report says so in its own words: adding the override while leaving the false statement standing would be using an override to cover a D-18 violation, which is not what that override is for. The gap record, the score and the status are left intact as the audit trail; only `overrides` and `overrides_applied` change."
    - "The two now-false statements in `example/` are corrected with the same D-18 in-place shape: `example/backend.md`'s and `example/chunking.md`'s claims that the chunk grid is pinned once at creation time are preserved under a marker with the correction beneath naming `widen_symbol_axis`'s `mode=\"w\"` rewrite as the path that RE-PINS it, and the shown `_append_encoding` snippet is brought up to the LIVE signature read from source rather than copied from `03.6-PATTERNS.md`, which is known stale."
    - "No production code and no test changes in this plan. Its whole diff is planning artifacts and `example/` prose, so the suite count coming out equals the count going in."
  artifacts:
    - .planning/phases/03.6-frequency-keyed-chunking-policy/03.6-07-PLAN.md
    - .planning/phases/03.6-frequency-keyed-chunking-policy/deferred-items.md
    - .planning/phases/03.6-frequency-keyed-chunking-policy/03.6-VERIFICATION.md
    - example/backend.md
    - example/chunking.md
  key_links:
    - "plan `03.6-08`'s narrowed `widen_and_append` docstring (the precondition) -> `03.6-07-PLAN.md` truth #3's retraction -> `deferred-items.md`'s accepted-cost entry -> `03.6-VERIFICATION.md`'s `overrides` block. All four state the SAME narrowed claim and the SAME accepted cost; a reader who finds one finds the rest."
    - "`XrBackend.widen_symbol_axis`'s `mode=\"w\"` rewrite -> the grid is RE-PINNED, not preserved -> `example/backend.md:94` and `example/chunking.md:264` corrected -> `example/` stops teaching the falsified rule to the next reader"
---

<objective>
Complete GAP #19's route (b). Plan `03.6-08` landed the production half -- the false unconditional
sentence is marked and narrowed in `quantlab/dataset/backend.py`. This plan lands the paperwork
half, which is what "formally retract" actually means, and folds in the `example/` advisory the
verifier raised alongside it.

Three of the four items here are the retraction itself:

- **A2** -- plan `03.6-07`'s `must_haves.truths` #3 is the FAILED truth. It says
  `set(_chunk_grid(store).values())` is a singleton "for every store this project writes". That
  universal is false and was measured false twice. It is retracted, visibly, in the same D-18
  preserve-and-mark shape the code uses. This is the ONE deliberate edit to an executed plan this
  round; plans 01-07 are otherwise off limits.
- **A3** -- the accepted cost goes into the phase's own ledger, so the next reader finds it where
  the phase's other accepted costs live rather than only inside a docstring.
- **A4** -- the override goes into `03.6-VERIFICATION.md`, in the shape that report specified for
  itself. Its precondition is stated by the report in its own words: the override is only
  legitimate once A1's text change has landed. That is why this plan `depends_on: ["03.6-08"]` and
  why Task 2 asserts the precondition before writing anything.

The fourth is **B**, an advisory the verifier recorded twice: `example/backend.md:94` and
`example/chunking.md:255-264` still teach that the chunk grid is pinned once, at creation time, and
never revised. Plan `03.6-07` falsified that -- `widen_symbol_axis`'s `mode="w"` rewrite RE-PINS the
grid -- and plan `03.6-07` explicitly fenced `example/` out of its scope, which is exactly why the
false text is still standing. SC-8's list is item-by-item and does not include `example/`, so this
was correctly an advisory rather than a gap; the developer has now pulled it in.

Wave 2, blocked on `03.6-08`, for the reason A4 states: the override must be written AFTER the
text change, never before. It runs in parallel with `03.6-10`, which shares none of its files.

Purpose: a retraction that lives only in a docstring is half a retraction. The must-have that was
measured false sits in a plan file, the cost sits in a ledger, and the verdict sits in a
verification report -- all three have to say the same thing, or the next verifier re-derives the
same gap from the same unretracted text.

Output: truth #3 narrowed with its original preserved; the accepted cost in `deferred-items.md`;
the override in `03.6-VERIFICATION.md`; `example/`'s two false claims marked and corrected against
the live signature.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/03.6-frequency-keyed-chunking-policy/03.6-VERIFICATION.md
@.planning/phases/03.6-frequency-keyed-chunking-policy/03.6-07-PLAN.md
@.planning/phases/03.6-frequency-keyed-chunking-policy/deferred-items.md
</context>

<baseline>
Clean tree at plan time, HEAD `00cd680`. This plan changes NO production code and NO tests, so its
expected suite delta is zero.

- `03.6-07-PLAN.md` frontmatter has `must_haves.truths` with SIX entries. #3 (index 2) is the
  target. Its current text begins "The second symptom of the same root cause is closed:" and
  contains the universal "is a SINGLETON for every store this project writes".
- `deferred-items.md` currently holds ONE entry, about the untracked `example/chunk_grid_demo.py`,
  with the field shape: a bullet headline, then `status:`, then `**What:**`, `**Discovered
  during:**`, `**Why deferred:**`, `**Note:**`, `**Next step:**`, `**Resolution ...:**`. Match that
  shape; the new entry needs no `**Resolution**` because it is not resolved.
- `03.6-VERIFICATION.md` frontmatter currently has `overrides_applied: 0` and NO `overrides:` key.
  `status: gaps_found`, `score: 21/22 must-haves verified`. The `gaps:` block is the audit trail.
- `example/backend.md:85` shows `kwargs.setdefault("encoding", self._append_encoding(append_dim))`
  -- the live `append()` passes `append_dim_size=append_dim_size` as well.
- `example/backend.md:94` claims the grid is pinned at the first write, and points at
  `quantlab/dataset/backend.py:34` for `APPEND_DIM_CHUNK` -- the live line is `93`.
- `example/chunking.md:255` is the `### 首次写入：XrBackend.APPEND_DIM_CHUNK = 512` heading;
  `:264` is "而且网格已经在创建时钉好了。"

Line numbers were read live at plan time and shift as this plan inserts text. Anchor on the quoted
sentence, never on the number.
</baseline>

<scope_fence>
- Plans `03.6-01` through `03.6-06` are EXECUTED and off limits entirely. `03.6-07` is edited at
  exactly ONE place: `must_haves.truths` entry #3. Nothing else in that file moves -- not its other
  five truths, not `artifacts`, not `key_links`, not the objective, not a single task body.
- `03.6-VERIFICATION.md`: only the frontmatter keys `overrides` (added) and `overrides_applied`
  (0 -> 1). `status`, `score`, `gaps`, `advisory`, `re_verification`, `covered_files`,
  `covered_digest` and the entire markdown body stay byte-identical. Do NOT flip `status` to
  anything -- re-verification derives that, not this plan.
- No file under `quantlab/` and no file under `tests/` is touched. `03.6-10` owns
  `quantlab/dataset/backend.py` and `quantlab/base/data.py` this wave.
- `example/acquisition.md` is NOT in scope -- plan `03.6-06` already re-derived it from a live run.
- `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md` are NOT in scope; SC-8's edits there are
  VERIFIED truth #12 and must not be disturbed.
</scope_fence>

<probe_fallback>
**Spec-less probe fallback: SKIPPED, recorded visibly.** This phase carries `Requirements: N/A`
(its contract is SC-1..SC-8), so the fallback has no requirement text to probe and no probe-derived
edge or prohibition predicates exist for this round. There is no SPEC.md and therefore no
`## Edge Coverage` / `## Prohibitions` section to lift from. `must_haves` above come from the
round's scope groups A2/A3/A4 and B in the normal way.
</probe_fallback>

<tasks>

<task type="auto">
  <name>Task 1: retract truth #3 in place, and record the accepted cost in the phase ledger</name>
  <files>.planning/phases/03.6-frequency-keyed-chunking-policy/03.6-07-PLAN.md, .planning/phases/03.6-frequency-keyed-chunking-policy/deferred-items.md</files>
  <read_first>
    - `.planning/phases/03.6-frequency-keyed-chunking-policy/03.6-07-PLAN.md`, the whole
      frontmatter block. Entry #3 of `must_haves.truths` is the target; read all six so the edit
      lands on the right one and so its wording matches the register of its siblings.
    - `.planning/phases/03.6-frequency-keyed-chunking-policy/03.6-VERIFICATION.md`, the `gaps:`
      block and section 二 ("#19 为什么记为 gap 而不是 advisory"). This is the source of the
      retraction's reasoning and of the measured numbers.
    - `.planning/phases/03.6-frequency-keyed-chunking-policy/deferred-items.md` -- the ONE existing
      entry, in full, for the field shape the new entry must match.
    - `quantlab/dataset/backend.py`'s `widen_and_append` docstring AS PLAN `03.6-08` LEFT IT. The
      retraction's narrowed claim and the ledger entry must say the SAME thing that docstring now
      says, in the same terms. Read it rather than paraphrasing this plan.
    - `quantlab/dataset/backend.py:56-93` -- the `APPEND_DIM_CHUNK` `#:` block, for the D-18
      preserve-and-mark shape.
  </read_first>
  <action>
Two edits, both planning artifacts, neither touching code.

**Edit A -- `03.6-07-PLAN.md`, `must_haves.truths` entry #3 only.**

Rewrite that ONE list entry so its scope matches the narrowed claim plan `03.6-08` landed in
`widen_and_append`'s docstring, and make the retraction auditable rather than silent. Content, in
this order inside the single entry:

1. The word `SUPERSEDED` plus a one-line pointer naming `03.6-VERIFICATION.md` truth #19 and plan
   `03.6-08` as the retraction, so a reader lands on the evidence and the fix from here.
2. The ORIGINAL wording, preserved verbatim, clearly quoted as the superseded text -- including the
   universal "is a SINGLETON for every store this project writes" and the measured-red
   `close (4, 2)` beside `newvar (3, 2)` line. Delete nothing.
3. The narrowed replacement claim: within a single `widen_and_append` call, the symbol-axis widen,
   the `widen_data_vars` filler and the closing `append()` all read `append_dim_size` from the same
   `kwargs` entry and therefore agree WITH EACH OTHER. It says nothing about variables the store
   already held from an earlier write.
4. One sentence naming the accepted cost and its root cause: `XrBackend.write()` pins no
   `encoding`, so a `save()`-built store is off the `min(APPEND_DIM_CHUNK, total)` grid (WR-02);
   measured at the real default `APPEND_DIM_CHUNK = 512` as `{'close': (1000, 2)}` then
   `{'close': (1000, 2), 'newvar': (512, 2)}`; pinned by
   `tests/test_chunked_ingest.py::test_a_store_built_by_write_keeps_two_chunk_grids_accepted_cost`.

YAML mechanics, because this entry now contains quotes and colons: write it as a `>-` folded block
scalar rather than as a double-quoted scalar. Its siblings stay double-quoted -- mixing scalar
styles in one YAML sequence is valid and is the least invasive option. Re-parse the frontmatter
with `yaml.safe_load` after the edit; a plan file whose frontmatter no longer parses is worse than
the universal it was fixing.

Change nothing else in that file. Not truths #1, #2, #4, #5, #6; not `artifacts`; not `key_links`;
not `requirements`; not a single character of the body.

**Edit B -- append one entry to `deferred-items.md`**, under the existing `## Deferred Items`
heading and matching the existing entry's field shape exactly (bullet headline, `status:`,
`**What:**`, `**Discovered during:**`, `**Why deferred:**`, `**Next step:**`). Use
`status: accepted` -- this is an accepted cost, not a resolved item and not an open question, and
the file's own rule is that `resolved` is reserved for something explicitly committed, deleted or
archived.

The entry must state, concretely: `XrBackend.write()` passes no `encoding` at all, so every store
built by `BaseDataset.save()` / `Factor.save()` lands on Zarr's default grid and has never sat on
`min(APPEND_DIM_CHUNK, total)`; `Factor.update()` holds no source for the store's total extent and
so passes no `append_dim_size`, leaving the filler on `min(APPEND_DIM_CHUNK, filler_len)`; the
consequence on the repo's own documented `save()` -> `update()` workflow is a two-grid store,
measured at the real default 512 with no monkeypatch as `{'close': (1000, 2)}` then
`{'close': (1000, 2), 'newvar': (512, 2)}`; the root cause is review findings WR-02 / WR-01 and it
PREDATES 03.6; it is independent of the `--chunk` rung, which is why it leaves SC-1..SC-8 and the
phase goal's caller-owns-a-stated-cost half intact (the verifier's PROBE1 measured all four rungs
landing identically). Discovered during: the third-round verification, `03.6-VERIFICATION.md` truth
#19. Next step: route (a) remains available if it is ever picked up -- pin `encoding` in `write()`
on the `mode="w"` branch and give `Factor.update()` a path to the store's total extent -- and the
day that happens, this entry, `widen_and_append`'s docstring and `03.6-07-PLAN.md` truth #3 must be
updated together, which is what the accepted-cost test arm forces.
  </action>
  <verify>
    <automated>uv run python -c "import yaml, pathlib; p = pathlib.Path('.planning/phases/03.6-frequency-keyed-chunking-policy/03.6-07-PLAN.md'); lines = p.read_text().splitlines(); end = lines.index('---', 1); d = yaml.safe_load('\n'.join(lines[1:end])); t = d['must_haves']['truths']; assert len(t) == 6, f'truth count changed to {len(t)}'; e = t[2]; assert 'SUPERSEDED' in e, 'truth #3 carries no SUPERSEDED marker'; assert 'for every store this project writes' in e, 'the original universal was deleted instead of preserved'; assert '03.6-VERIFICATION.md' in e and '03.6-08' in e, 'no pointer to the verification truth or to the retracting plan'; assert 'WR-02' in e and '(1000, 2)' in e and '(512, 2)' in e, 'the accepted cost and its numbers are not stated'; assert t[0].startswith('VERIFICATION truth #18'), 'truth #1 was disturbed'; assert t[3].startswith('The three reading points'), 'truth #4 was disturbed'; print('truth #3 retracted in place, five siblings intact')"</automated>
    <fails_when>non-zero exit, or any AssertionError / yaml ScannerError text instead of the final `truth #3 retracted in place` line</fails_when>
    <automated>git diff --stat 00cd680 -- .planning/phases/03.6-frequency-keyed-chunking-policy/03.6-07-PLAN.md</automated>
    <fails_when>empty output (nothing landed), or a stat showing more than roughly 20 changed lines -- entry #3 is one list item and a retraction of it should not ripple through the file</fails_when>
    <automated>git diff 00cd680 -- .planning/phases/03.6-frequency-keyed-chunking-policy/03.6-07-PLAN.md > /tmp/ql_p07.diff && grep -cE '^-[^-]' /tmp/ql_p07.diff</automated>
    <fails_when>a count greater than 1 -- exactly ONE original line should be removed (truth #3's single-line entry), and more than that means a sibling truth or another key was rewritten</fails_when>
    <automated>grep -c 'status:' .planning/phases/03.6-frequency-keyed-chunking-policy/deferred-items.md</automated>
    <fails_when>a value other than `2` -- the file held one entry with one `status:` line and this task adds exactly one more</fails_when>
    <automated>grep -E 'WR-02|WR-01|1000, 2|512, 2|write\(\)|Factor.update\(\)' .planning/phases/03.6-frequency-keyed-chunking-policy/deferred-items.md</automated>
    <fails_when>non-zero exit, or fewer than four distinct matching lines in the output -- the ledger entry must carry the root cause, the measured numbers and both named methods, not a one-line summary</fails_when>
  </verify>
  <acceptance_criteria>
    - `03.6-07-PLAN.md`'s frontmatter parses with `yaml.safe_load` and still has exactly 6 truths.
    - Truth #3 contains all of: `SUPERSEDED`, the preserved literal
      `for every store this project writes`, `03.6-VERIFICATION.md`, `03.6-08`, `WR-02`,
      `(1000, 2)`, `(512, 2)`.
    - Truths #1 and #4 begin with their original text, proving no sibling was disturbed.
    - The diff of `03.6-07-PLAN.md` removes exactly ONE line.
    - `deferred-items.md` has exactly 2 `status:` lines, and the new entry names `WR-02`/`WR-01`,
      `(1000, 2)`, `(512, 2)`, `write()` and `Factor.update()`.
    - `git status --porcelain -- quantlab/ tests/` produces NO output when run before this task's
      commit.
  </acceptance_criteria>
  <reversibility rating="reversible">Planning-artifact text under D-18 preserve-and-mark. No code, no test, no on-disk artifact. Reverts cleanly.</reversibility>
  <done>The universal that `03.6-VERIFICATION.md` measured false is formally retracted where it was written -- visibly, with its original preserved and pointers to the evidence and the fix -- and the accepted cost is recorded in the phase's own ledger in the ledger's own shape.</done>
</task>

<task type="auto">
  <name>Task 2: write the override into `03.6-VERIFICATION.md` -- AFTER the text change, never before</name>
  <files>.planning/phases/03.6-frequency-keyed-chunking-policy/03.6-VERIFICATION.md</files>
  <precondition>Plan `03.6-08`'s Task 1 has landed: `inspect.getdoc(XrBackend.widen_and_append)` contains `NOT TRUE ANY MORE AS OF 03.6-07` at an index lower than the index of `a store cannot come out of here carrying one grid per axis`. `03.6-VERIFICATION.md` states this precondition in its own words -- adding the override while leaving the false statement standing would be using an override to cover a D-18 violation, which is not what that override is for. Assert it before writing anything; if it does not hold, HALT and report that `03.6-08` did not land rather than writing the override anyway.</precondition>
  <read_first>
    - `.planning/phases/03.6-frequency-keyed-chunking-policy/03.6-VERIFICATION.md` frontmatter in
      full, and the "### 这看起来可能是有意为之" section, which specifies the override's exact
      shape and states its precondition in the report's own words.
    - `quantlab/dataset/backend.py`'s `widen_and_append` docstring AS PLAN `03.6-08` LEFT IT --
      this is the precondition, and it must be checked, not assumed.
    - `03.6-07-PLAN.md` truth #3 as Task 1 of THIS plan left it, so the override's `must_have`
      field quotes the text that was actually retracted.
  </read_first>
  <action>
Assert the precondition FIRST, with the runnable check in the `<verify>` block below, and halt if
it does not hold.

Then add an `overrides:` block to `03.6-VERIFICATION.md`'s frontmatter, in the shape the report
specified for itself (it is written out verbatim in its "### 这看起来可能是有意为之" section):
a list with one entry carrying `must_have`, `reason`, `accepted_by` and `accepted_at`.

- `must_have`: quote plan `03.6-07` `must_haves.truths` #3's universal -- the
  `set(_chunk_grid(store).values())` singleton claim "for every store this project writes" -- so
  the override names exactly the assertion being waived and nothing wider.
- `reason`: the root cause is `write()` never pinning `encoding` (WR-02), which predates 03.6 and
  is unrelated to this phase's rung contract; the universal was over-broad wording and has been
  narrowed per route (b) to "the three things `widen_and_append` writes in one call agree with each
  other"; the cost is recorded in `deferred-items.md` and pinned by
  `tests/test_chunked_ingest.py::test_a_store_built_by_write_keeps_two_chunk_grids_accepted_cost`.
- `accepted_by`: `dzr`.
- `accepted_at`: the ISO-8601 UTC timestamp at which you write it, in the same format the file's
  other timestamps use (`verified:` is the pattern to match).

Also set `overrides_applied` from `0` to `1`, because the report declares that counter and an
override with the counter at zero leaves the file contradicting itself.

Change NOTHING else. `status` stays `gaps_found`, `score` stays `21/22 must-haves verified`, the
`gaps:` block stays exactly as written, and the entire markdown body is untouched. Those are the
audit trail; re-verification derives the new verdict from the tree, not from this edit. Do not be
tempted to "finish the job" by flipping `status` -- an override that also rewrites the verdict it
is attached to is indistinguishable from a verdict that was never rendered.

Re-parse the frontmatter with `yaml.safe_load` afterwards.
  </action>
  <verify>
    <automated>uv run python -c "import inspect; from quantlab.dataset.backend import XrBackend; d = inspect.getdoc(XrBackend.widen_and_append); m = d.index('NOT TRUE ANY MORE AS OF 03.6-07'); o = d.index('a store cannot come out of here carrying one grid per axis'); assert m < o, 'PRECONDITION UNMET: plan 03.6-08 has not landed; the override may not be written'; print('precondition met: 03.6-08 landed')"</automated>
    <fails_when>non-zero exit, a `ValueError: substring not found`, or the text `PRECONDITION UNMET` -- in any of those cases HALT the task and report that `03.6-08` did not land, rather than writing the override</fails_when>
    <automated>uv run python -c "import yaml, pathlib, re; p = pathlib.Path('.planning/phases/03.6-frequency-keyed-chunking-policy/03.6-VERIFICATION.md'); lines = p.read_text().splitlines(); end = lines.index('---', 1); d = yaml.safe_load('\n'.join(lines[1:end])); assert d['overrides_applied'] == 1, f'overrides_applied is {d[\"overrides_applied\"]!r}'; o = d['overrides']; assert len(o) == 1, f'{len(o)} overrides, expected 1'; e = o[0]; assert e['accepted_by'] == 'dzr', f'accepted_by is {e[\"accepted_by\"]!r}'; assert re.match(r'^\d{4}-\d{2}-\d{2}T', str(e['accepted_at'])), 'accepted_at is not an ISO timestamp'; assert '_chunk_grid' in e['must_have'], 'the override does not name the waived assertion'; assert 'WR-02' in e['reason'] and 'deferred-items.md' in e['reason'], 'the reason states no root cause or no ledger pointer'; assert d['status'] == 'gaps_found', 'status was altered'; assert d['score'] == '21/22 must-haves verified', 'score was altered'; assert len(d['gaps']) == 1, 'the gap record was altered'; print('override written, verdict intact')"</automated>
    <fails_when>non-zero exit, or any AssertionError / yaml error text instead of the final `override written, verdict intact` line</fails_when>
    <automated>git diff 00cd680 -- .planning/phases/03.6-frequency-keyed-chunking-policy/03.6-VERIFICATION.md > /tmp/ql_verif.diff && { grep -cE '^-[^-]' /tmp/ql_verif.diff || echo 0; }</automated>
    <fails_when>a count greater than `1` -- the only line this task may REMOVE is `overrides_applied: 0`. Anything more means `status`, `score`, `gaps` or the body was rewritten, which this task is forbidden to do</fails_when>
    <automated>git status --porcelain -- quantlab/ tests/</automated>
    <fails_when>ANY output line at all. Deliberately an UNCOMMITTED-tree check and deliberately not a `00cd680`-based diff: plans `03.6-08`, `03.6-09` and `03.6-10` legitimately change `quantlab/` and `tests/` this round, so a round-base diff would false-positive on their committed work. Run this BEFORE this task's own commit -- it is asking "did THIS task leave an edit under `quantlab/` or `tests/`", which is the only form of the question this plan can answer on its own</fails_when>
  </verify>
  <acceptance_criteria>
    - The precondition check printed `precondition met: 03.6-08 landed` before any edit was made.
    - `03.6-VERIFICATION.md`'s frontmatter parses with `yaml.safe_load` and has
      `overrides_applied: 1` and exactly one entry under `overrides`.
    - That entry has `accepted_by: dzr`, an ISO-8601 `accepted_at`, a `must_have` naming
      `_chunk_grid`, and a `reason` naming both `WR-02` and `deferred-items.md`.
    - `status` is still `gaps_found`, `score` is still `21/22 must-haves verified`, and `gaps` still
      holds exactly one entry.
    - The diff of `03.6-VERIFICATION.md` removes at most ONE line.
    - `git status --porcelain -- quantlab/ tests/` produces no output when run before this task's
      commit.
  </acceptance_criteria>
  <reversibility rating="costly">An override is a FORMAL acceptance recorded in a verification report and signed to a person. Reverting it is a git revert with no on-disk or runtime consequence, so it is not one-way -- but a future reader treats a signed override as a settled question rather than an open one, and re-opening it costs the re-litigation this round exists to end. Rated `costly` for that, and gated behind a runnable precondition rather than a trusted assumption, because an override written before the text change would be exactly the misuse the report warns against.</reversibility>
  <done>`03.6-VERIFICATION.md` carries the override in the shape it specified for itself, signed `dzr`, written only after the precondition check confirmed plan `03.6-08` landed, with the gap record, score and status left intact as the audit trail.</done>
</task>

<task type="auto">
  <name>Task 3: de-stale `example/` -- the grid is NOT pinned once at creation (scope group B)</name>
  <files>example/backend.md, example/chunking.md</files>
  <read_first>
    - `example/backend.md`, the `### append()：沿 append_dim 延长` section: the code excerpt at
      `:85` showing `kwargs.setdefault("encoding", self._append_encoding(append_dim))`, and the
      claim at `:94` beginning `**chunk 网格是在第一次写的时候被钉死的。**`, which also points at
      `quantlab/dataset/backend.py:34` for `APPEND_DIM_CHUNK`.
    - `example/chunking.md`, the `### 首次写入：XrBackend.APPEND_DIM_CHUNK = 512` section at
      `:255-264`, ending `而且网格已经在创建时钉好了。`
    - `quantlab/dataset/backend.py`, the LIVE `append()` creating branch and the LIVE
      `_append_encoding` signature -- read them from SOURCE. `03.6-PATTERNS.md` is KNOWN STALE
      (its excerpts predate the CONTEXT decisions), so do not copy a signature from it.
    - `quantlab/dataset/backend.py`'s `widen_symbol_axis` docstring paragraph beginning
      `**append_dim_size` decides the REWRITTEN store's chunk grid.**` -- this is the mechanism the
      correction describes, and the correction must agree with it word for word on the mechanism.
    - `quantlab/dataset/backend.py:56-93` -- the `APPEND_DIM_CHUNK` `#:` block, whose numbered
      three-paths list is the canonical statement `example/` must stop contradicting.
    - `example/acquisition.md` -- read only, as the example of how plan `03.6-06` handled an
      `example/` correction in this phase. Do NOT edit it; it is out of scope.
  </read_first>
  <action>
Two documents, same D-18 in-place shape used everywhere else this round: the falsified sentence
stays, marked, with the correction beneath it naming the mechanism. These files are in Chinese --
write the annotations in Chinese to match, keeping the marker string and the identifier names in
Latin script as the rest of the file already does.

**`example/backend.md`.**

The claim at `:94` -- that the chunk grid is nailed down at the first write -- is now false in its
unconditional form. Preserve it verbatim, mark it, and write beneath it: a `mode="w"` rewrite
RE-PINS the grid, and `XrBackend.widen_symbol_axis` is exactly such a rewrite, so "pinned at
creation and never revised" holds only until the first symbol-axis widen. Name the three paths the
`APPEND_DIM_CHUNK` `#:` block enumerates -- the whole-range creating write, the incremental chunked
append with a stated `append_dim_size`, and the `mode="w"` widen rewrite -- rather than inventing a
fourth framing. State what IS still true and is the point the section was making: without an
explicit `encoding` the first window's own length becomes the store's layout, which is why the
creating write pins it deliberately.

Bring the `:85` excerpt up to the LIVE signature read from source: the creating branch now passes
`append_dim_size=append_dim_size` alongside `append_dim`. Update the excerpt itself rather than
annotating it -- an `example/` file is a teaching document whose snippets are meant to be copied,
and a knowingly-wrong snippet there is a different failure mode from a falsified CLAIM in a
docstring; D-18's preserve-and-mark applies to the CLAIM at `:94`, not to a mechanical
transcription of a signature. Say so in one sentence beside the excerpt so the asymmetry is
deliberate and visible.

Fix the stale pointer in the same line: `APPEND_DIM_CHUNK` is at `quantlab/dataset/backend.py:93`
on the live tree, not `:34`. Verify the number against the live file before writing it, and prefer
naming the symbol over the line number where the sentence reads just as well without it, since a
line pointer is the thing that rots fastest.

**`example/chunking.md`.**

`:264`'s `而且网格已经在创建时钉好了。` is the same falsified claim in its shortest form. Preserve
it, mark it, and correct beneath: the reason `append()` drops `encoding` on the append branch is
narrower than "the grid is already settled" -- xarray REJECTS `encoding` on an append, so THIS
METHOD cannot revise the grid; the grid itself is revisable, and `widen_symbol_axis` revises it.
Point the reader at `example/backend.md`'s corrected section rather than restating the whole
mechanism twice.

Leave the rest of both files alone -- the `_assert_append_compatible` sections, the measured
transcripts, the per-window cleaning cost paragraph, all of it. Touch no file under `quantlab/` or
`tests/`.
  </action>
  <verify>
    <automated>uv run python -c "import pathlib; b = pathlib.Path('example/backend.md').read_text(); c = pathlib.Path('example/chunking.md').read_text(); assert 'chunk 网格是在第一次写的时候被钉死的' in b, 'the original backend.md claim was deleted rather than preserved'; assert '网格已经在创建时钉好了' in c, 'the original chunking.md claim was deleted rather than preserved'; assert 'widen_symbol_axis' in b and 'widen_symbol_axis' in c, 'neither correction names the rewrite that re-pins the grid'; assert 'append_dim_size' in b, 'backend.md does not show the live signature'; assert 'backend.py:34' not in b, 'the stale line pointer survives'; print('example/ corrected, originals preserved')"</automated>
    <fails_when>non-zero exit, or any AssertionError text instead of the final `example/ corrected` line</fails_when>
    <automated>uv run python -c "import inspect, pathlib; from quantlab.dataset.backend import XrBackend; sig = str(inspect.signature(XrBackend._append_encoding)); assert 'append_dim_size' in sig, 'read the live signature before trusting this check'; b = pathlib.Path('example/backend.md').read_text(); i = b.index('_append_encoding('); seg = b[i:i+200]; assert 'append_dim_size' in seg, f'the shown snippet still omits the keyword: {seg[:120]!r}'; print('shown snippet matches the live signature')"</automated>
    <fails_when>non-zero exit, or any AssertionError text instead of the final `shown snippet matches the live signature` line</fails_when>
    <automated>grep -n 'APPEND_DIM_CHUNK = 512' quantlab/dataset/backend.py</automated>
    <fails_when>the reported line number is anything other than the one written into `example/backend.md` -- re-read it and correct the document rather than the assertion</fails_when>
    <automated>git diff --name-only 00cd680 -- example/acquisition.md .planning/ROADMAP.md .planning/REQUIREMENTS.md quantlab/base/chunking.py quantlab/utils/cli.py quantlab/acquisition/</automated>
    <fails_when>ANY output line at all. These paths are touched by NO plan this round, so a round-base diff over them is a true round-wide gate rather than a sibling-plan false positive: `example/acquisition.md` was re-derived by plan `03.6-06`, ROADMAP and REQUIREMENTS carry SC-8's VERIFIED truth #12, and the chunking ladder, the CLI and the acquisition layer are all fenced</fails_when>
    <automated>git status --porcelain -- quantlab/ tests/</automated>
    <fails_when>ANY output line at all -- an uncommitted-tree check, run before this task's own commit, asking whether THIS task left an edit under `quantlab/` or `tests/`</fails_when>
    <automated>uv run pytest -q</automated>
    <fails_when>non-zero exit, or any "failed"/"error" count in the summary line, or a passed count different from the one measured at the start of this plan -- a documentation plan that moves the suite count has touched something it should not have</fails_when>
  </verify>
  <acceptance_criteria>
    - Both original Chinese claims are still present verbatim in their files, each preceded by a
      marker, with a correction beneath naming `widen_symbol_axis` and the `mode="w"` rewrite.
    - `example/backend.md`'s shown `_append_encoding` call includes `append_dim_size`, matching the
      live signature read from source.
    - The literal `backend.py:34` no longer appears in `example/backend.md`; any line pointer that
      remains matches the live file.
    - `example/backend.md` names the three grid-pinning paths the `APPEND_DIM_CHUNK` `#:` block
      enumerates, and states in one sentence why the CLAIM is preserved-and-marked while the
      SNIPPET is updated in place.
    - `git diff --name-only 00cd680` scoped to `example/acquisition.md`, `.planning/ROADMAP.md`,
      `.planning/REQUIREMENTS.md`, `quantlab/base/chunking.py`, `quantlab/utils/cli.py` and
      `quantlab/acquisition/` produces NO output, and `git status --porcelain -- quantlab/ tests/`
      produces no output before this task's commit.
    - `uv run pytest -q` reports the same passed count as at the start of this plan, zero failures.
  </acceptance_criteria>
  <reversibility rating="reversible">Documentation prose under D-18 preserve-and-mark, plus one snippet brought to the live signature. No code, no test, no on-disk artifact.</reversibility>
  <done>`example/`'s two now-false claims that the chunk grid is pinned once at creation are marked with their originals preserved and corrected beneath, the shown `_append_encoding` snippet matches the live signature read from source rather than from the stale PATTERNS file, the rotted line pointer is gone, and the suite count is unchanged.</done>
</task>

</tasks>

<artifacts_this_plan_produces>
This plan creates NO new symbols and NO new files. Every path and identifier it names already
exists on the live tree and was read at plan time:

- `.planning/phases/03.6-frequency-keyed-chunking-policy/03.6-07-PLAN.md` (edited: one
  `must_haves.truths` entry)
- `.planning/phases/03.6-frequency-keyed-chunking-policy/deferred-items.md` (edited: one entry
  appended under the existing `## Deferred Items` heading)
- `.planning/phases/03.6-frequency-keyed-chunking-policy/03.6-VERIFICATION.md` (edited: two
  frontmatter keys)
- `example/backend.md`, `example/chunking.md` (edited: prose and one snippet)

The only NEW frontmatter key introduced anywhere is `overrides:` in `03.6-VERIFICATION.md`, whose
shape is specified by that report itself. It references
`tests/test_chunked_ingest.py::test_a_store_built_by_write_keeps_two_chunk_grids_accepted_cost`,
which is created by plan `03.6-08` and must be excluded from drift verification on that plan's
behalf, not this one's.
</artifacts_this_plan_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning artifact -> next verifier | A verifier re-derives gaps from must-have text. An unretracted universal in a plan file regenerates the same gap on every future round regardless of what the code says. |
| `example/` -> next author | These are teaching documents whose snippets are meant to be copied. A wrong snippet propagates into real code; a wrong CLAIM propagates into a wrong mental model. |
| override -> seal decision | An override waives a measured failure. If it can be written before the corresponding fix, it becomes a way to seal past any finding. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.6-11-01 | Repudiation | `03.6-07-PLAN.md` truth #3 | medium | mitigate | The retraction PRESERVES the original wording under a `SUPERSEDED` marker with pointers to `03.6-VERIFICATION.md` truth #19 and plan `03.6-08` (Task 1), so the history of the claim survives and the edit cannot be mistaken for a claim that was never made. A silent frontmatter rewrite is what this specifically prevents. |
| T-03.6-11-02 | Elevation of privilege | the `overrides` mechanism | high | mitigate | The override is gated on a RUNNABLE precondition asserting plan `03.6-08`'s text landed (Task 2's `<precondition>` and its first verify command), and the task halts if it does not hold. The report names this misuse itself: an override with the false statement still standing would be covering a D-18 violation. `status`, `score` and `gaps` are left intact so the override annotates the verdict rather than replacing it, and a diff gate caps removed lines at one. |
| T-03.6-11-03 | Tampering | `example/backend.md` snippet | medium | mitigate | The snippet is brought to the LIVE signature read from `inspect.signature`, not copied from `03.6-PATTERNS.md`, which is known stale; a verify command compares the document against the live signature rather than against a literal written into this plan. |
| T-03.6-11-04 | Information disclosure (of a false mental model) | `example/` grid claims | medium | mitigate | Both falsified claims are preserved-and-marked with corrections naming `widen_symbol_axis`'s `mode="w"` rewrite, and pointed at the `APPEND_DIM_CHUNK` `#:` block's canonical three-path enumeration so `example/` and the source cannot drift into two different stories. |
| T-03.6-11-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs NO packages, adds no dependency, and touches no file under `quantlab/` or `tests/`. Recorded so the absence is a visible choice. |

ASVS level 1, block on `high`. The one `high` row is `mitigate`, and its mitigation is a runnable
halt condition rather than an instruction to be careful.
</threat_model>

<verification>
1. `03.6-07-PLAN.md` frontmatter parses, still has six truths, and truth #3 contains `SUPERSEDED`,
   the preserved `for every store this project writes`, and pointers to `03.6-VERIFICATION.md` and
   `03.6-08`. Its diff removes exactly one line.
2. `deferred-items.md` has exactly two `status:` lines, the new one naming `WR-02`/`WR-01`, the
   measured `(1000, 2)` / `(512, 2)` numbers, `write()` and `Factor.update()`.
3. `03.6-VERIFICATION.md` has `overrides_applied: 1` and one `overrides` entry signed `dzr`, while
   `status`, `score` and `gaps` are unchanged; and the precondition check passed BEFORE that edit.
4. `example/backend.md` and `example/chunking.md` each preserve their original claim under a marker
   with a correction naming `widen_symbol_axis`; the shown `_append_encoding` snippet matches
   `inspect.signature(XrBackend._append_encoding)`; `backend.py:34` is gone.
5. `git diff --name-only 00cd680` over the round-wide fenced paths (`example/acquisition.md`,
   `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`, `quantlab/base/chunking.py`,
   `quantlab/utils/cli.py`, `quantlab/acquisition/`) is empty, and `git status --porcelain --
   quantlab/ tests/` is empty before each of this plan's commits.
6. `uv run pytest -q` reports the same count as at the start of this plan, zero failures.
</verification>

<success_criteria>
- The universal that was measured false is retracted where it was written, visibly, with its
  original preserved and the evidence and fix pointed to.
- The accepted cost is in the phase's ledger in the ledger's own shape.
- The override is in the verification report in the shape the report specified, signed `dzr`,
  written only after its precondition was checked at runtime.
- `example/`'s two false claims are marked and corrected against the live source.
- Zero production and zero test change; the suite count is unmoved.
</success_criteria>

<output>
Create `.planning/phases/03.6-frequency-keyed-chunking-policy/03.6-11-SUMMARY.md` when done.
It MUST carry: the precondition check's output proving `03.6-08` landed before the override was
written, the `accepted_at` timestamp used, the removed-line counts for both edited planning
artifacts, and a statement that `status` / `score` / `gaps` in `03.6-VERIFICATION.md` were left
intact deliberately so re-verification derives the new verdict from the tree.
</output>