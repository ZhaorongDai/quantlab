---
phase: 03.6-frequency-keyed-chunking-policy
verified: 2026-09-13T19:17:59Z
status: gaps_found
score: 21/22 must-haves verified
covered_files:
  - ".planning/REQUIREMENTS.md"
  - ".planning/ROADMAP.md"
  - ".planning/phases/03.6-frequency-keyed-chunking-policy/03.6-01-PLAN.md"
  - ".planning/phases/03.6-frequency-keyed-chunking-policy/03.6-01-SUMMARY.md"
  - ".planning/phases/03.6-frequency-keyed-chunking-policy/03.6-02-PLAN.md"
  - ".planning/phases/03.6-frequency-keyed-chunking-policy/03.6-02-SUMMARY.md"
  - ".planning/phases/03.6-frequency-keyed-chunking-policy/03.6-03-PLAN.md"
  - ".planning/phases/03.6-frequency-keyed-chunking-policy/03.6-03-SUMMARY.md"
  - ".planning/phases/03.6-frequency-keyed-chunking-policy/03.6-04-PLAN.md"
  - ".planning/phases/03.6-frequency-keyed-chunking-policy/03.6-04-SUMMARY.md"
  - ".planning/phases/03.6-frequency-keyed-chunking-policy/03.6-05-PLAN.md"
  - ".planning/phases/03.6-frequency-keyed-chunking-policy/03.6-05-SUMMARY.md"
  - ".planning/phases/03.6-frequency-keyed-chunking-policy/03.6-06-PLAN.md"
  - ".planning/phases/03.6-frequency-keyed-chunking-policy/03.6-06-SUMMARY.md"
  - ".planning/phases/03.6-frequency-keyed-chunking-policy/03.6-07-PLAN.md"
  - ".planning/phases/03.6-frequency-keyed-chunking-policy/03.6-07-SUMMARY.md"
  - ".planning/phases/03.6-frequency-keyed-chunking-policy/03.6-REVIEW-FIX.md"
  - ".planning/phases/03.6-frequency-keyed-chunking-policy/03.6-REVIEW.md"
  - ".planning/phases/03.6-frequency-keyed-chunking-policy/deferred-items.md"
  - "example/acquisition.md"
  - "example/backend.md"
  - "example/chunking.md"
  - "ingest_alpaca.py"
  - "ingest_tiingo.py"
  - "ingest_us_equity.py"
  - "quantlab/acquisition/registry.py"
  - "quantlab/acquisition/universe.py"
  - "quantlab/base/chunking.py"
  - "quantlab/base/data.py"
  - "quantlab/dataset/backend.py"
  - "quantlab/utils/cli.py"
  - "tests/test_chunked_ingest.py"
  - "tests/test_chunked_panel_estimate.py"
  - "tests/test_ingest_conversion_gate.py"
  - "tests/test_ingest_shells.py"
  - "tests/test_universe.py"
  - "tests/test_volume_guard.py"
covered_digest: "v1:sha256:083c2716daf21b1e704ab709c20bece1b49742222a838603412c4ff106f69d48"
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 16/18
  gaps_closed:
    - "阶段目标 caller-owns-a-stated-cost 的落盘格网一半:`append_dim_size` 已贯通到 widen 重写路径的四个站点,`from_raw_data_chunked` 的 widen + crash-resume + roster 增长在 year / quarter / month / day 四档上全部落在 `(min(APPEND_DIM_CHUNK, 9), 3)`,格网不再随 rung 变化(旧报告 truth #17 / #18)"
    - "上一轮点名的两处新写入的无条件假陈述(`APPEND_DIM_CHUNK` 的 `#:` 块、`append()` docstring `:189-192`)已按 D-18 就地更正:原句保留并标 SUPERSEDED,新句明写「widen 重写会 RE-PIN 格网」并列出三条路径"
    - "CR-01(本轮新引入的反向缺陷:窄 config 窗口通过 `len(timestamps)` 把已完成 store 的格网不可逆改小)已修:`_append_encoding` 取 `max(panel.sizes[dim], append_dim_size)`,陈述 extent 降级为下界,`min(APPEND_DIM_CHUNK, size)` 天花板不变"
  gaps_remaining: []
  regressions:
    - "plan `03.6-07` 新写入的 `widen_and_append` docstring(`quantlab/dataset/backend.py:1027-1030`,commit `37e61f2`)再一次把一条可被证伪的不变式写成无条件成立:「a store cannot come out of here carrying one grid per axis」。已独立复现为假,且在 `APPEND_DIM_CHUNK` 的真实默认值 512 下、不打任何 monkeypatch 就为假。这与上一轮记录的 regression 属于同一类,是同一个 D-18 纪律在同一个文件里第二次被违反。"
gaps:
  - truth: "plan `03.6-07` must_haves.truths #3 逐字:`set(_chunk_grid(store).values())` 对本项目写出的 EVERY store 都是单元素集;`widen_data_vars` 的 filler 与 store 里已有变量落在同一格网上。"
    status: failed
    reason: >-
      已独立复现两次,其中一次在 `APPEND_DIM_CHUNK` 的真实默认值 512 下、不打任何 monkeypatch:

        save_grid   = {'close': (1000, 2)}
        after update= {'close': (1000, 2), 'newvar': (512, 2)}   SINGLETON = False

      第二次(monkeypatch `APPEND_DIM_CHUNK=4`,10 行 store,`append_dim_size=15`):
      `{'close': (10, 2), 'newvar': (4, 2)}` —— 与评审 WR-01 给出的数字逐位一致。

      根因是 WR-02:`XrBackend.write()`(`backend.py:125-130`)**完全不传 `encoding`**,
      所以 `BaseDataset.save()` / `Factor.save()` 建出来的 store 走 Zarr 默认格网,从来没有
      落在 `min(APPEND_DIM_CHUNK, total)` 上。`append()` / `widen_symbol_axis` /
      `widen_data_vars` 三条路都在维护这条不变式,最常用的批量写路径不维护。

      可达性不是理论上的:`Factor.save()` → `Factor.update()`(`factor.py:128` / `:174` →
      `:202` `widen_and_append`)是本仓文档明确推荐的工作流,且 `update()` 根本不传
      `append_dim_size`,filler 因此拿 `min(512, filler_len)`,与 save() 留下的整段长度格网
      必然不同。

      与阶段目标的关系,说清楚而不含糊:**这一条不falsify阶段目标本身**。它不随 rung 变化
      (probe 已确认 filler 的格网只由 `APPEND_DIM_CHUNK` 与 filler 长度决定,与
      `--chunk` 档位无关),所以旧报告 truth #17 / #18 —— 也就是「调用方选细粒度 rung 不
      承担落盘格网退化」这一阶段目标半句 —— 是真的、本轮已验过。失败的是 plan `03.6-07`
      **自己写下的、比阶段目标更宽的那条 must-have 断言**,以及它在代码里留下的对应陈述。

      为什么仍然记为 gap 而不是 advisory:该断言不是评审提出的新范围,而是 plan `03.6-07`
      frontmatter 里第三条 must-have 的逐字内容;与之配套的无条件陈述由本阶段的 commit
      `37e61f2` 新写入生产代码。一条被本阶段引入、又被本阶段的评审证伪、且仍以无条件形式
      留在生产 docstring 里的不变式,正是本阶段 D-18 纪律要禁止的东西。
    artifacts:
      - path: "quantlab/dataset/backend.py"
        issue: "`write()`(:125-130)不传 `encoding`,`save()` 建出来的 store 从不落在 `min(APPEND_DIM_CHUNK, total)` 上(WR-02,单一根因)。"
      - path: "quantlab/dataset/backend.py"
        issue: "`widen_and_append` docstring(:1023-1032)「All three read `append_dim_size` from the SAME `kwargs` entry, so a store cannot come out of here carrying one grid per axis」为假:它只覆盖本次调用写的三样东西,不覆盖 store 里更早写入的变量。实测该方法返回后 store 内就是两个格网。"
      - path: "quantlab/base/factor.py"
        issue: "`update()`(:202)调 `widen_and_append` 时不传 `append_dim_size`,filler 只能拿 `min(APPEND_DIM_CHUNK, filler_len)`;`save()` 那条路上没有任何一方持有 store 的总 extent。"
      - path: "tests/test_chunked_ingest.py"
        issue: "`test_a_variable_widen_leaves_one_chunk_grid_not_two`(:1406)的 store 由 `_incomplete_store` 用 `append(append_dim_size=9)` 建出,即 store 本身已在正确格网上;没有任何一条臂用 `write()` / `save()` 建库,所以该缺口对绿灯不可见。"
    missing:
      - "二选一(评审 WR-01 已给出两条,推荐第一条)。(a) 从根因修:`write()` 在 `mode=\"w\"` 时 `kwargs.setdefault(\"encoding\", self._append_encoding(append_dim))`,并补一条断言 `save()` 与 `from_raw_data_chunked()` 落在同一格网的测试;同时给 `Factor.update()` 一条把 store 总 extent 传下去的路径,或在 `widen_data_vars` 里对格网不一致 `logger.warning`。"
      - "(b) 若决定不修行为:把 `widen_and_append` docstring(:1027-1030)那句降级为「本次调用写入的三样东西彼此一致」,把 plan `03.6-07` must-have truth #3 里「for every store this project writes」这一 universal 收回,并把「`save()` 建出的 store 不在该格网上」显式列入已接受成本 —— 不能让一条已被实测证伪的不变式以无条件形式留在生产 docstring 里(D-18)。"
      - "补一条测试臂:`write()` 建库 → `widen_and_append` 加新变量 → 断言 `len(set(_chunk_grid(path).values())) == 1`。在修复前该臂应红在 `{'close': (10, 2), 'newvar': (4, 2)}`(与本报告复现一致)。"
deferred: []
advisory:
  - finding: "WR-03:`widen_symbol_axis` 的 crash guard 不处理 `superseded.exists() AND target.exists()`,残留的 `.superseded.tmp` 会让收尾的 `os.replace` 在整份 sidecar 写完之后才抛 ENOTEMPTY;且 `BaseDataset.SUPERSEDED_SUFFIX` 与 `XrBackend.SUPERSEDED_SUFFIX` 是同一字符串拼在同一 store path 后面。"
    category: architectural
    reason: "健壮性缺陷,与本阶段任何一条 SC 或 must-have 无关;评审已完整记录并给出修复形状。开发者本轮明确只做 CR-01。"
    evidence_status: "not independently reproduced (需要构造 SIGKILL 残留)"
  - finding: "WR-04:`_restore_rebuild_asides` 可能在 `except BaseException` 处理器内部抛 OSError,替换掉原始异常。"
    category: architectural
    reason: "同上,新范围、非本阶段交付物。"
    evidence_status: "not independently reproduced"
  - finding: "WR-06 残留半条:`_widen_block_rows` 首段(`backend.py:559-566`)的代码片段抄录仍在说「the block's own length is what decides the store's on-disk append-dim chunk」,同一 docstring 后段已把它更正;`_widen_chunked:755-758` 与 `widen_and_append:1026-1031` 未加「NOT TRUE ANY MORE AS OF 03.6-07」前缀。"
    category: other
    reason: "plan `03.6-07` must-have truth #4 点名的三处阅读点(`APPEND_DIM_CHUNK` `#:` 块、`append()` docstring、`_widen_block_rows` 的 floor-vs-`append_dim_size` 论证)都已按 D-18 更正到位,truth #4 因此判 VERIFIED;WR-06 抱怨的是同一 docstring 里更细的抄录段落与前缀格式,属评审建议而非 must-have。"
    evidence_status: "independently reproduced (通读源码确认)"
  - finding: "`example/backend.md:94` 与 `example/chunking.md:255-262` 仍在说「chunk 网格是在第一次写的时候被钉死的」「网格已经在创建时钉好了」,并展示不带 `append_dim_size` 的 `_append_encoding(append_dim)`。第二句现已为假 —— `widen_symbol_axis` 就会 RE-PIN。"
    category: other
    reason: "SC-8 的清单是逐项列举的(ROADMAP 03.5 的 SC-5 / D-05 / D-10 / D-11 半句、REQUIREMENTS 的 DATA-08),不含 `example/`;plan `03.6-07` 也显式把这两处 fence 到范围外。记为 advisory 而非 gap,但下一轮碰 `example/` 时应一并更正。"
    evidence_status: "independently reproduced (读原文确认)"
  - finding: "IN-01..IN-05(未使用的 fixture 参数、三个消费者只断言两个、测试辅助泄漏 zarr 句柄、`_reconcile_new_listings` 的 `on_new_listing` 注解为假、`append()` 白算一遍 encoding)。"
    category: other
    reason: "评审 Info 级,均不触及任何 must-have。其中 IN-02 与本轮 gap 无关:`test_the_chunk_grid_reaches_three_consumers_from_one_read` 用 `popped == []` 覆盖了第三个消费者,只是不以 `in receivers` 的形式。"
    evidence_status: "partially reproduced (读源码确认 IN-02 的实际形状)"
---

# Phase 03.6: Sub-Daily Chunk Granularity & Panel-Estimator Removal 验证报告(第三轮,plan 07 + CR-01 修复后)

**Phase Goal:** `TimeChunkPlanner` becomes the SINGLE chunking authority, its granularity ladder
reaches `hour`, and the caller chooses a rung by passing it explicitly. The dense-panel
estimator/guard group is DELETED from `UniverseCatalog` — both its estimating and its refusing
halves — so a roster catalogue goes back to answering only "who is in the pool, and when".

**Requirements:** N/A —— 本阶段的契约是它自己的 SC-1..SC-8(ROADMAP.md:613-618 起)。
**Verified:** 2026-09-13T19:17:59Z
**Status:** gaps_found
**Re-verification:** 是。`f7c1109` 记录的 16/18 之后,plan `03.6-07`(6 个 commit)与
`8c7ff94` 的 CR-01 修复已落地。**本报告整份替换旧报告**;二十二条 must-have 全部对着盘上的
代码重新验过,旧报告的 16/18 结论没有被直接继承 —— 尤其是 #17 / #18,是用我自己写的探针在
生产入口上重新测的,不是读 SUMMARY 得出的。
**HEAD:** `1f33a46`,工作树干净。
**Whole suite:** `uv run pytest -q` → **797 passed**(101.75s,主 checkout,本轮亲自跑完一次)。

---

## Goal Achievement

### Observable Truths

Must-have 来源:ROADMAP SC-1..SC-8(契约,不可缩减)+ 上一轮报告已确立的 gap-closure 条目
(#13..#16)+ plan `03.6-07` frontmatter 的六条 truths(#17..#22,其中 #17 / #18 与上一轮
同号同文)。

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | SC-1 梯子算术:`GRANULARITIES` 五元组,`_period_key` 五分支,返回注解仍是 `tuple[int, int]`,`day`=`(year, dayofyear)`,`hour`=`(year, dayofyear*24+hour)` | ✓ VERIFIED | 本轮重新运行时取值(`2024-12-31 23:30`):`(2024,0)/(2024,4)/(2024,12)/(2024,366)/(2024,8807)`;`inspect.signature` 确认 `-> tuple[int, int]` |
| 2 | SC-1 tracer:`day` 级分块转换端到端写进真实 Zarr,与非分块 store 逐变量同格网 | ✓ VERIFIED | `test_a_day_granularity_conversion_reaches_zarr_identical_to_the_unchunked_store` 绿;`:1143-1150` 逐变量比对 `_chunk_grid()` 并断言 `shape[0] == min(APPEND_DIM_CHUNK, 9)` |
| 3 | SC-1:`hour` 级在真实分钟轴上每个钟点一个窗口 | ✓ VERIFIED | `test_plan_from_timestamps_cuts_one_window_per_clock_hour` 绿(全量 797 内) |
| 4 | SC-1:加 token 而不教 `_period_key` 会 raise | ✓ VERIFIED | `chunking.py` 尾部 `raise ValueError`;`test_a_granularity_period_key_does_not_handle_refuses_instead_of_guessing` 绿 |
| 5 | SC-2:只暴露一个 planner,`plan_calendar` 已删 | ✓ VERIFIED | 运行时 `hasattr(T,"plan_calendar") is False`;`[n for n in dir(T) if n.startswith("plan")] == ['plan_from_timestamps']` |
| 6 | SC-2:模块/类 docstring 不再宣称两个 planner,不再用「共享 period 规则防漂移」论证 | ✓ VERIFIED | 通读 `chunking.py` docstring,明写 "There is ONE planner" |
| 7 | SC-3:五个 dense-panel 成员从 `UniverseCatalog` 全部消失 | ✓ VERIFIED | 五个 `hasattr` 全 False(本轮重跑) |
| 8 | SC-4:acquisition-volume guard 组 14 个成员全在,算术未变 | ✓ VERIFIED | `UniverseCatalog` 5 个 + `utils/cli` 3 个 `hasattr` 全 True;`tests/test_volume_guard.py` 不在 `f7c1109..HEAD` 的 diff 里,全量内通过 |
| 9 | SC-5:无按频率键控的 chunk granularity 常量表;`choices` 仍从 `GRANULARITIES` 派生;`--chunk` 的 `required`/`default`/`type`/`choices` 未被本阶段改动 | ✓ VERIFIED | `grep -rn "CHUNK_GRANULARITY_BY\|GRANULARITY_BY_FREQ\|chunk_granularity" quantlab/ ingest_*.py` 零命中;运行时读 argparse action:`choices=['year','quarter','month','day','hour']`,`default='year'`,`required=False`,`type=str` |
| 10 | SC-6:只为已删估算器服务的东西一并删除 | ✓ VERIFIED | `hasattr(cli,"print_chunk_report") is False`;`volume_pricing`/`add_volume_guard_args`/`print_volume_estimate` 三者保留 |
| 11 | SC-7:三个美股 shell 摘除已删函数调用点,`--chunk` 管线不变 | ✓ VERIFIED | 三个 shell 各 `add_chunk_args` 1 次 + `args.chunk` 2 次,`estimate_chunked_panel` / `assert_chunked_panel_fits` / `print_chunk_report` / `assert_dense_panel_fits` 零命中 |
| 12 | SC-8:被证伪的锁定文本就地标注,DATA-08 正式撤回且覆盖算术自洽 | ✓ VERIFIED | ROADMAP 三处 SUPERSEDED(`:499/:532/:548`);REQUIREMENTS `[~] DATA-08` 原文保留于 `:28`、`:30` 起为 WITHDRAWN 注记、traceability `:144` 为 Withdrawn、`[~]` 恰好 1 个、`:164` 计 Withdrawn: 1 |
| 13 | gap-closure(建库写):分块转换建库写的落盘格网是 store 总 extent 的属性,与 rung 无关,resume / rebuild 两条非零迭代路径同样成立 | ✓ VERIFIED | 我自己的探针 PROBE1 在 `APPEND_DIM_CHUNK=4`、整段 9 行、崩溃后 resume 的条件下,四个 rung 的建库写全部留下 `(4, 2)`;仓内四条臂同时全绿 |
| 14 | gap-closure:SC-1 的等价性证据覆盖 `encoding`,不再只靠 `assert_identical` | ✓ VERIFIED | tracer `:1143-1150` 读 `zarr.open_group(...).chunks`,逐变量比 + 断言 `shape[0] == min(APPEND_DIM_CHUNK, 9)` |
| 15 | gap-closure:三个显式传 `data=` 的 `_append_encoding` 调用点输出不变,姊妹 widen 测试套未被编辑 | ✓ VERIFIED | 新关键字默认 `None`,`:1177-1187` 仅在 `append_dim_size is not None` 时替换;四个 widen 测试文件不在 `f7c1109..HEAD` diff 内 |
| 16 | gap-closure(06):D-18 纪律施加于清单内的六条陈旧文本,无行为变更 | ✓ VERIFIED | 相关文件均不在 `f7c1109..HEAD` diff 内,上一轮结论无回归;全量 797(基线 791 + plan 07 的 5 条新臂 + CR-01 的 1 条)自洽 |
| 17 | **plan 07 truth #2 逐字**:分块转换所建 store 的落盘 append 维 chunk 在 **EVERY rung** 上都是 `min(APPEND_DIM_CHUNK, 整段轴长)`,且在 `on_new_listing='widen'` 的 `mode='w'` 重写之后**仍然如此**,包括 store 相对该 extent 不完整(崩溃 resume + roster 增长)时 | ✓ VERIFIED | **我自己写的探针,走生产入口,rung 参数化到四档**(见下方「一、独立复现」)。四档全部 `after_widen = {(4, 3)}`,`sizes = {timestamp: 9, symbol: 3}`。上一轮实测的 `year → (3,3)` / `day → (1,3)` 已消失 |
| 18 | **plan 07 truth #1 / 阶段目标 caller-owns-a-stated-cost 一半**:调用方选细粒度 rung 只承担已陈述的代价,**不含**落盘格网的不可逆退化,**在 store 能到达的任何路径上**,widen 重写包括在内 | ✓ VERIFIED | 同上四档实测格网**完全不随 rung 变化**;再加 PROBE3(CR-01 方向):窄 config 窗口的 widen 之后格网仍是 `(4, 3)`,不会被改小 |
| 19 | **plan 07 truth #3 逐字**:`set(_chunk_grid(store).values())` 对本项目写出的 EVERY store 都是单元素集;`widen_data_vars` 的 filler 与 store 已有变量同格网 | ✗ FAILED | 独立复现两次,其中一次在真实默认 `APPEND_DIM_CHUNK=512` 下不打 monkeypatch:`save()` 建库 `{'close': (1000,2)}` → `widen_and_append` 加新变量后 `{'close': (1000,2), 'newvar': (512,2)}`,SINGLETON=False。根因 WR-02:`write()` 不传 `encoding` |
| 20 | **plan 07 truth #4**:三处阅读点(`APPEND_DIM_CHUNK` 的 `#:`、`append()` 关于既有 store 的段落、`_widen_block_rows` 的 floor-vs-`append_dim_size` 论证)描述已修复的机制,点名 widen 重写为一条路径,原句按 D-18 就地保留 | ✓ VERIFIED | 三处逐段通读:`#:` 块列出三条路径并保留「原来只数了两条」的 SUPERSEDED 原文;`append()` `:233-251` 保留「the grid was pinned irreversibly ... nothing left to decide」原句并给出正确理由;`_widen_block_rows` `:577-608` 保留「keeps its floor rather than adopting `append_dim_size`」原论证并逐条驳回 |
| 21 | **plan 07 truth #5**:关键字对不传它的调用方完全 inert,四个 widen / update 测试套 UNEDITED 且绿 | ✓ VERIFIED | `git diff --name-only f7c1109..HEAD` 不含这四个文件(也不含 `test_dataset_update.py`);五者合跑 **96 passed** |
| 22 | **plan 07 truth #6**:两条 widen 策略仍携带同一签名,router 可按名选择并同样调用 | ✓ VERIFIED | `inspect.signature` 逐字相同:`(self, stored, widening, requested, *, dim, append_dim, fills, block_rows, append_dim_size=None) -> None` |

**Score:** 21/22 truths verified(0 present-but-behavior-unverified,0 override)

---

## 一、独立复现(不采信 SUMMARY,也不只跑仓内测试)

我把三个探针写成临时 pytest 文件放进 `tests/`,跑完后删除(`git status` 已确认干净)。
探针复用仓内的 `_GrowingRoster` / `_FailsOnSecondWindow` fixture,但**断言与参数化是我自己写的**
——特别是把 rung 从仓内那条臂的 `["year","day"]` 扩到四档。

### PROBE1 —— 生产入口 `from_raw_data_chunked`,崩溃 resume + roster 增长,四档 rung

`APPEND_DIM_CHUNK` monkeypatch 到 4,整段 9 行,第一次运行在第二个窗口崩溃,随后新 listing `C`
出现,resume 时 `on_new_listing="widen"`:

```
PROBE1 rung=year    after_crash={(4, 2)}  after_widen={(4, 3)}  sizes={'timestamp': 9, 'symbol': 3}
PROBE1 rung=quarter after_crash={(4, 2)}  after_widen={(4, 3)}  sizes={'timestamp': 9, 'symbol': 3}
PROBE1 rung=month   after_crash={(4, 2)}  after_widen={(4, 3)}  sizes={'timestamp': 9, 'symbol': 3}
PROBE1 rung=day     after_crash={(4, 2)}  after_widen={(4, 3)}  sizes={'timestamp': 9, 'symbol': 3}
```

**这张表是 #17 / #18 判 VERIFIED 的支点。** 上一轮同一 fixture 上实测的是
`year → (3, 3)`、`day → (1, 3)`;现在四档同为 `(4, 3) = (min(4, 9), 3)`。
`min(4, 9)` 的三个候选规则在这里给出 1 / 3 / 4 三个不同答案,只有「调用方陈述的总 extent
决定」给 4,所以这是机制而不是 fixture 巧合。我把 rung 扩到 `quarter` / `month` 是为了排除
「只对两个被测档位打了补丁」这种可能。

### PROBE3 —— CR-01 的反方向:窄 config 窗口不得把已完成 store 的格网改小

先用整段 9 行建完整 store(`{(4, 2)}`),再把 `config.start_date`/`end_date` 收窄到 2024 年
(3 个观测时间戳)并以 `on_new_listing="widen"` 重跑:

```
PROBE3 before={(4, 2)}  after_narrow_widen={(4, 3)}
```

`max(panel.sizes[dim], append_dim_size)` 生效,窄陈述 extent 变成惰性下界。CR-01 已闭合。

### PROBE2 —— WR-01 / plan 07 truth #3:一库两格网,**在真实默认值下**

两次测量,第一次复现评审的数字,第二次是我加的、也是决定性的那次:

```
# monkeypatch APPEND_DIM_CHUNK=4,10 行 save() 建库,widen_and_append(append_dim_size=15)
PROBE2 save_grid={'close': (10, 2)}  after_widen_and_append={'close': (10, 2), 'newvar': (4, 2)}  SINGLETON=False

# 不打任何 monkeypatch,APPEND_DIM_CHUNK=512,1000 行 save() 建库,widen_and_append 不传 append_dim_size
#   —— 即 Factor.save() → Factor.update() 的真实形状
PROBE-DEFAULT APPEND_DIM_CHUNK=512 save_grid={'close': (1000, 2)}
              after_update={'close': (1000, 2), 'newvar': (512, 2)}  SINGLETON=False
```

第二次很重要:它证明这不是 monkeypatch 制造的实验室现象,而是**本仓默认配置下、
文档推荐的 `save()` → `update()` 工作流的常态输出**。

顺带测到的第三个数:`save()` 建的 `(1000, 2)` store 一旦被 symbol widen,格网被静默改成
`(512, 3)`。方向上它是**向不变式靠拢**(`min(512, 1000) = 512`),所以不构成 #18 意义上的
退化,但它同样是一次不可逆的 `mode="w"` 重钉,操作者得不到任何提示。

---

## 二、#19 为什么记为 gap 而不是 advisory —— 也说明它没有 falsify 阶段目标

我把这两件事分开讲,因为把它们混在一起会得出两个都错的结论。

**它没有 falsify 阶段目标。** 阶段目标那半句是「the caller chooses a rung by passing it
explicitly」,配套的成本承诺(plan 07 truth #1 / 旧 #18)是「选细粒度 rung 不额外承担落盘
格网退化」。PROBE1 的四档同值、PROBE2 里 filler 格网只由 `APPEND_DIM_CHUNK` 与 filler 长度
决定(与 `--chunk` 档位无关)—— 两者合起来说明 #19 的失败**与 rung 无关**。SC-1..SC-8 逐条
为真。所以「阶段目标达成了吗」这个问题的答案是:**达成了**。

**但它仍然是 gap,不是 advisory。** 判据是来源,不是严重度:

1. 它是 plan `03.6-07` frontmatter 里 **must_haves.truths 的第三条**,逐字。不是评审提出的
   新范围,是本阶段自己承诺的东西。
2. 与它配套的无条件陈述由本阶段 commit `37e61f2` **新写入生产代码**
   (`backend.py:1027-1030`)。`git log -S` 已确认。
3. 上一轮报告的 `regressions` 记录的正是同一类事:本阶段一边执行 D-18(「保留被推翻的
   原文,让更正可读」),一边在同一个文件里新写下无条件的假安全陈述。这是第二次。一条已被
   本阶段自己的评审实测证伪的不变式,不应该以无条件形式留在下一个作者会当契约读的 docstring 里。

评审 `WR-01` / `WR-02` 被开发者本轮**显式声明为 out of scope**(见 `03.6-REVIEW-FIX.md`),
这是一个正当的排期决定,但排期决定不等于把 must-have 收回。要让本阶段收口,需要在
`missing` 的 (a) 与 (b) 之间选一条 —— (b) 只改文字,成本很低。

### 这看起来可能是有意为之

若判定「行为不改,措辞收窄」即为可接受的收口(即 `missing` 的 (b)),可在本文件 frontmatter
加 override 直接放行:

```yaml
overrides:
  - must_have: "plan 03.6-07 must_haves.truths #3:set(_chunk_grid(store).values()) 对本项目写出的 EVERY store 都是单元素集"
    reason: "根因是 write() 从不钉 encoding(WR-02),早于 03.6 且与本阶段的 rung 契约无关;universal 措辞过宽,已按 (b) 收窄为「widen_and_append 本次调用写入的三样东西彼此一致」"
    accepted_by: "dzr"
    accepted_at: "2026-09-13T..."
```

注意:接受 override 的**前提**是 (b) 的文字修改真的落地。仅加 override 而把
`backend.py:1027-1030` 的假陈述留在原处,等于用 override 掩盖 D-18 违规,那不是这条 override
的用途。

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `quantlab/dataset/backend.py` | `append_dim_size` 贯通 append / widen 两族;`_append_encoding` 以 `max()` 取下界 | ✓ VERIFIED | 存在、实质、已接线、数据流通(PROBE1/3)。**但 :1027-1030 携带一条实测为假的无条件陈述** |
| `quantlab/base/data.py` | `from_raw_data_chunked` → `_reconcile_new_listings(append_dim_size=len(timestamps))` → widen 分支 | ✓ VERIFIED | `:677`、`:870` 传入;`:1246` 收下;`:1315` 转发给 `widen_symbol_axis` |
| `tests/test_chunked_ingest.py` | 第五条 widen 臂 + 三条 companion 臂 + CR-01 回归臂 | ✓ VERIFIED | `-k "grid or narrow_stated"` → **12 passed**;`test_the_chunk_grid_survives_a_widen` 在 `year`/`day` 两档参数化 |
| `quantlab/base/chunking.py` | 五级梯子、单 planner、缺分支即 raise | ✓ VERIFIED | 运行时逐级取值 + `dir()` 检查 |
| `quantlab/acquisition/universe.py` | 五个 dense-panel 成员已删,volume guard 组保留 | ✓ VERIFIED | `hasattr` 逐项 |
| `quantlab/utils/cli.py` | `print_chunk_report` 已删,`--chunk` 逐字不变 | ✓ VERIFIED | 运行时读 argparse action |
| `quantlab/base/factor.py` | `Factor.update()` 路由 | ⚠️ HOLLOW(对 #19 而言) | `:202` 调 `widen_and_append` 但不持有 store 总 extent,filler 因此无法与 `save()` 格网对齐 |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `BaseDataset.from_raw_data_chunked` | `_reconcile_new_listings` | `append_dim_size=len(timestamps)` | ✓ WIRED | `data.py:677`、`:870` |
| `_reconcile_new_listings` | `XrBackend.widen_symbol_axis` | `append_dim_size=append_dim_size` | ✓ WIRED | `data.py:1315` |
| `widen_symbol_axis` router | `_widen_whole_store` / `_widen_chunked` | `append_dim_size=` 转发 | ✓ WIRED | `backend.py:490`;两策略签名逐字相同 |
| `_widen_whole_store` / `_widen_chunked` | `_append_encoding` | `append_dim_size=` | ✓ WIRED | `backend.py:731`、`:790` |
| `widen_and_append` `**kwargs` | `widen_symbol_axis` + `widen_data_vars` + 收尾 `append()` | `kwargs.get`(不 pop) | ✓ WIRED | `backend.py:1041`、`:1084`、`:1096`;`test_the_chunk_grid_reaches_three_consumers_from_one_read` AST 锁 |
| `XrBackend.write()`(`save()` 落盘路径) | `_append_encoding` | —— | ✗ NOT_WIRED | `backend.py:125-130` 不传 `encoding`。**这是 #19 的唯一根因** |
| `Factor.update()` | `widen_and_append(append_dim_size=…)` | —— | ✗ NOT_WIRED | `factor.py:202` 不传;`update()` 侧无 store 总 extent 来源 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| `_append_encoding` | `size` | `max(panel.sizes[dim], append_dim_size)` | ✓(PROBE1/2/3 实测三种输入三种输出) | ✓ FLOWING |
| `_widen_whole_store` | 重写后的 `chunks` | `_append_encoding(append_dim, data=widened, append_dim_size=…)` | ✓ `(4, 3)` 四档同值 | ✓ FLOWING |
| `widen_data_vars` filler | 新变量 `chunks` | `_append_encoding(..., append_dim_size=…)` | ⚠️ 只在调用方给了 extent 时对齐;`update()`/`save()` 路上给不出 | ⚠️ STATIC(对 #19 而言) |
| `XrBackend.write()` | 建库 `chunks` | Zarr 默认(无 encoding) | ✗ 永不落在 `min(APPEND_DIM_CHUNK, total)` 上 | ✗ DISCONNECTED |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| 梯子五档运行时取值 | `uv run python -c "…_period_key…"` | `(2024,0)/(2024,4)/(2024,12)/(2024,366)/(2024,8807)` | ✓ PASS |
| `--chunk` argparse 形状 | `uv run python -c "…add_chunk_args…"` | `choices=[year,quarter,month,day,hour] default=year required=False type=str` | ✓ PASS |
| 生产 widen 路径格网(四档) | 自写探针 PROBE1 | 四档全部 `{(4, 3)}` | ✓ PASS |
| 窄窗口不改小格网 | 自写探针 PROBE3 | `(4, 2)` → `(4, 3)` | ✓ PASS |
| 一库单格网(`save()` 建库) | 自写探针 PROBE2 / PROBE-DEFAULT | `{'close': (1000,2), 'newvar': (512,2)}` | ✗ FAIL |
| 格网相关仓内测试 | `uv run pytest tests/test_chunked_ingest.py -k "grid or narrow_stated"` | 12 passed | ✓ PASS |
| 惰性(四个 widen 套 + dataset_update) | `uv run pytest tests/test_symbol_axis_widening.py tests/test_variable_axis_widening.py tests/test_factor_update.py tests/test_widening_fixture_realism.py tests/test_dataset_update.py -q` | 96 passed | ✓ PASS |
| 全量 | `uv run pytest -q` | **797 passed**,101.75s | ✓ PASS |

### Probe Execution

本仓无 `scripts/*/tests/probe-*.sh` 约定,PLAN / SUMMARY 也未声明 shell 探针。
Step 7c: SKIPPED(no conventional probe scripts);等价的可执行证据以上表的自写 pytest 探针形式给出。

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| N/A | ROADMAP 03.6 | 本阶段 `Requirements: N/A`,契约是 SC-1..SC-8 | ✓ SATISFIED | SC-1..SC-8 = truths #1..#12,全部 VERIFIED |
| DATA-08 | 03.6-03(SC-8) | 「调用方可在内存分配前问出预测峰值」—— 本阶段 SC-3 删掉了该能力 | ✓ SATISFIED(以 Withdrawn 形式) | REQUIREMENTS `:28` 原文保留 + `:30` WITHDRAWN 注记 + `:144` traceability + `:164` 计数 1 |
| `PHASE-GOAL-caller-owns-a-stated-cost` / `D-12` / `D-18` | 03.6-07 frontmatter | 非 REQUIREMENTS.md ID,是本阶段自定的决策标签 | ⚠️ 部分 | 前两者 → truths #17/#18 ✓;**D-18 在 `backend.py:1027-1030` 上不成立**(见 gap) |

**孤儿需求检查:** `grep -E "Phase 03.6" .planning/REQUIREMENTS.md` 未映射任何额外 ID 到本阶段;
无 ORPHANED。

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `quantlab/dataset/backend.py` | 1027-1030 | 无条件的假安全陈述(「a store cannot come out of here carrying one grid per axis」) | 🛑 Blocker | 见 gap #19;违反本阶段自己的 D-18 |
| `quantlab/dataset/backend.py` | 559-566 | 已过期的代码片段抄录(同 docstring 后段已更正,但抄录段未标注) | ⚠️ Warning | 评审 WR-06;plan 07 truth #4 点名的三处已到位,这是第四处 |
| `example/backend.md` | 94 | 「chunk 网格是在第一次写的时候被钉死的」+ 不带 `append_dim_size` 的片段 | ⚠️ Warning | 阶段外文本,SC-8 清单不含,plan 07 显式 fence |
| `example/chunking.md` | 255-262 | 「网格已经在创建时钉好了」现已为假 | ⚠️ Warning | 同上 |
| 三个改动文件 | — | `TBD` / `FIXME` / `XXX` / `TODO` / `HACK` / `PLACEHOLDER` | ℹ️ Info | 全树 grep **零命中**,debt marker gate 通过 |

### Human Verification Required

无。二十二条 must-have 全部可编程判定,不存在 present-but-behavior-unverified 条目
—— #17 / #18 这两条行为依赖型 truth 都由我自己写的、跑通的探针给出行为证据,而不是靠符号存在性。

### Gaps Summary

**一句话:阶段目标达成了,plan `03.6-07` 自己多承诺的那一条没达成。**

上一轮那个真正的 gap —— 「落盘 chunk 格网随 `--chunk` 档位不可逆退化」—— 已经闭合,而且是
按最强的形式闭合的:我在生产入口上把 rung 扩到四档重测,`year` / `quarter` / `month` / `day`
全部落在 `(min(APPEND_DIM_CHUNK, 整段轴长), 3)`。plan `03.6-07` 中途自己引入的反向缺陷
(CR-01,窄 config 窗口把已完成 store 的格网改小)也已修复并有回归臂守住方向。SC-1..SC-8
逐条为真,全量 797 passed。

留下的一条,是 plan `03.6-07` 在 must-have 里把不变式写得比阶段目标更宽:
「`set(_chunk_grid(store).values())` 对本项目写出的 **EVERY** store 都是单元素集」。
这条 universal 为假,根因是 `XrBackend.write()` 从来不钉 `encoding` —— 一个早于 03.6、
与 rung 无关的批量写路径缺陷(评审 WR-02)。我在真实默认 `APPEND_DIM_CHUNK=512` 下、
不打任何 monkeypatch、沿本仓文档推荐的 `save()` → `update()` 工作流复现了它:
`{'close': (1000, 2), 'newvar': (512, 2)}`。

让它成为 gap 而不是 advisory 的,是第三件事:本阶段 commit `37e61f2` 把这条已被证伪的
不变式以**无条件形式**写进了生产 docstring。上一轮报告在同一个文件里记过同一类 regression。
一个执行 D-18(「保留被推翻的原文,让更正可读」)的阶段,不应该在收尾时新增一条不可读的
假保证。

闭合路径有两条,其中一条只改文字:要么从根因修 `write()`(顺带一条 `save()` 与
`from_raw_data_chunked()` 同格网的断言),要么把措辞收窄到「本次调用写入的三样东西彼此一致」
并把 universal 从 plan 的 must-have 里正式收回、把「`save()` 建的 store 不在该格网上」列入
已接受成本。两条都可接受;把假陈述原样留下不可接受。

---

_Verified: 2026-09-13T19:17:59Z_
_Verifier: Claude (gsd-verifier)_
_HEAD: `1f33a46`_
