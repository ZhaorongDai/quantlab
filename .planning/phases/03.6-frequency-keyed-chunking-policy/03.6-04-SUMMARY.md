---
phase: 03.6-frequency-keyed-chunking-policy
plan: 04
subsystem: infra
tags: [documentation, decision-record, requirements, annotation, d-18, roadmap]

# Dependency graph
requires:
  - phase: 03.6-frequency-keyed-chunking-policy
    provides: "03.6-01 的五级 ladder 与 `--chunk` 三把回归锁 —— 本计划 SC-5 修正案要点名的那把锁"
  - phase: 03.6-frequency-keyed-chunking-policy
    provides: "03.6-02 删掉的五个 dense-panel 成员，以及接手 roster 算术的 `_roster_window_profile`"
  - phase: 03.6-frequency-keyed-chunking-policy
    provides: "03.6-03 删掉的 `plan_calendar` 与重写后的 drift 段落 —— `example/chunking.md` 逐字引用"
provides:
  - "ROADMAP 03.5 的 Goal / SC-4 / SC-5 三处原位 SUPERSEDED 标注，原文一字未删"
  - "ROADMAP 03.6 自身 SC-5 的原位 AMENDED 标注：把 `--chunk` \"stays REQUIRED\" 这句错误转述对照 live argparse 改正，且不改代码"
  - "quick task 260906-13w D-05 的唯一权威修正块，加上八处 restatement 的指针行"
  - "03.5-CONTEXT D-10 / D-11 的 SUPERSEDED 与 SUPERSEDED IN PART 标注，并给出算术的 forwarding address"
  - "REQUIREMENTS.md DATA-08 正式 WITHDRAWN，原中文措辞逐字保留，v1 覆盖率算术按文件实际行数重算"
  - "`example/chunking.md` / `example/acquisition.md` 去陈旧，前者新增一节记录守卫删除及其代价"
affects: [03.6 phase gates, quantlab-console CVT-02, future milestone audit]

actuals:
  tokens: 8743
  tasks: 3
  commits: 3
plan_head_before: 0bdee4ab1884abd7b3b5b6cf9d4a56d3aef0e2bb

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "一处权威修正 + N 处指针：一个被推翻的决定只在一个地方完整说明发生了什么，其余 restatement 只放一行指针 —— 复述 N 遍正是 phase 03.4 那类文档缺陷复发的方式"
    - "算术重算而非增减一：总数被质疑时从文件实际行数重新数，而不是把旧数字减一 —— 后者会把旧错误一路带下去"
    - "描述性指代：当一处合规注记要提到某个被闸门断言为零匹配的标识符时，改用描述性说法并显式说明为何不复述（沿用 03.6-02 在 registry.py 的手法）"
    - "贴出的输出只能重跑，不能手改：删掉一行不再打印的输出等于伪造整份 transcript"

key-files:
  created:
    - .planning/phases/03.6-frequency-keyed-chunking-policy/03.6-04-SUMMARY.md
  modified:
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/03.5-registry-level-raw-to-zarr-conversion-entry-point/03.5-CONTEXT.md
    - .planning/quick/260906-13w-k-zarr-16gib-oom/260906-13w-PLAN.md
    - .planning/quick/260906-13w-k-zarr-16gib-oom/260906-13w-SUMMARY.md
    - example/chunking.md
    - example/acquisition.md

key-decisions:
  - "v1 覆盖率算术按文件实际行数重算为 28 = 27 active + 1 withdrawn，而不是按计划写的 26 = 25 + 1。那个 26 在本次撤回之前就已经陈旧了 —— 它早于 2026-09-11 新增的 DATA-07/DATA-08，从未被递增过。减一会把旧错误带下去并让总数与文件本身不符"
  - "撤回状态的 checkbox 用 `[~]`：本文件此前只用过 `[ ]`（待办）与 `[x]`（完成），两者对 DATA-08 都不诚实 —— 它从未交付所以不是完成，也不是待办工作所以不是待办"
  - "D-05 的 restatement 站点由新 grep 重新推导得到 9 处（计划枚举的是 5 处），全部标注；其中一处是 YAML 标量，没有\"底下\"可言，指针以同行后缀形式追加并在文中说明原因"
  - "`example/chunking.md` 的 dry-run transcript 用真实重跑替换，不做手改 —— 有八行今天已经完全不打印了，手删等于伪造"
  - "`example/acquisition.md` 的 transcript 不需要重跑：它本来就只展示 volume estimate 与凭证报错，没有任何一行来自被删的守卫，今天重跑仍会产出同样的形状"

patterns-established:
  - "撤回而非删除：被撤回的 requirement 保留 bullet 与 traceability 行，并在\"曾经定义过\"的总数里继续计数 —— 消失的行无法告诉后来者\"这条是被有意退役的\""
  - "修正契约而不是修正代码：当一条 success criterion 与 live 代码不符时，先确认哪一边才是决定本身；错在转述就改转述，并在标注里点名那条会变红的锁"

requirements-completed: [SC-5, SC-8]

coverage:
  - id: D1
    description: "ROADMAP 03.5 的 SC-5 携带原位 SUPERSEDED 标注，点名 phase 03.6 SC-3，原句仍在"
    requirement: "SC-8"
    verification:
      - kind: other
        ref: "uv run python -c \"annotation-presence + '03.6' citation over four files\" -> 'decision records annotated in place'"
        status: pass
      - kind: other
        ref: "uv run python -c \"pinned-literal gate incl. 'A guard refusal names the remedy that would fit'\" -> 'every annotated original is still present verbatim'"
        status: pass
    human_judgment: false
  - id: D2
    description: "ROADMAP 03.6 自身 SC-5 携带 AMENDED 2026-09-12 标注，点名 live default / required 从未传入 / choices 派生自 GRANULARITIES / 03.6-01 的锁；且 `--chunk` 仍是 required=False、default='year'、type=str"
    requirement: "SC-5"
    verification:
      - kind: other
        ref: "uv run python -c \"SC-5 slice token gate + argparse introspection of add_chunk_args\" -> 'SC-5 annotated in place and still describes the live argument'"
        status: pass
      - kind: unit
        ref: "tests/test_chunked_ingest.py#test_the_chunk_flag_itself_is_left_exactly_as_it_was"
        status: pass
    human_judgment: false
  - id: D3
    description: "quick task 260906-13w 的 D-05 在 PLAN.md 里恰好有一个完整修正块，其余八处 restatement 各带一行指针"
    requirement: "SC-8"
    verification:
      - kind: other
        ref: "uv run python -c \"count lines containing both AMENDED and 03.6 in 260906-13w-PLAN.md\" -> 1"
        status: pass
      - kind: other
        ref: "grep -n 'D-05|assert_dense_panel_fits|MAX_DENSE_PANEL_BYTES' over both files -> 9 restatement sites, all annotated"
        status: pass
    human_judgment: false
  - id: D4
    description: "03.5-CONTEXT D-10 同时保有它原有的 2026-09-11 AMENDED 块与新的 2026-09-12 SUPERSEDED 块，顺序在后；D-11 的 SUPERSEDED IN PART 明确区分仍为真的一半"
    requirement: "SC-8"
    verification:
      - kind: other
        ref: "uv run python -c \"assert c.index('AMENDED 2026-09-11') < c.rindex('SUPERSEDED')\" -> 'decision records annotated in place'"
        status: pass
    human_judgment: false
  - id: D5
    description: "DATA-08 原中文措辞逐字保留于其 WITHDRAWN 标注之上；traceability 行读作 Withdrawn；DATA-07 未被触碰；无后继 requirement 被写入"
    requirement: "SC-8"
    verification:
      - kind: other
        ref: "uv run python -c \"byte-identity vs git show HEAD + row check + successor-ID absence\" -> 'DATA-08 withdrawn in place; DATA-07 untouched; no DATA-09'"
        status: pass
      - kind: other
        ref: "uv run python -c \"absolute committed-literal pin of the Chinese sentence\" -> 'DATA-08 original wording preserved verbatim beneath its withdrawal'"
        status: pass
      - kind: other
        ref: "uv run python -c \"requirement bullet count vs HEAD\" -> 'requirement bullet count unchanged'"
        status: pass
    human_judgment: false
  - id: D6
    description: "v1 覆盖率算术自洽：28 曾定义 = 27 active + 1 withdrawn，28 mapped，0 unmapped，且与文件实际的 28 个 bullet / 28 个 traceability 行一致"
    requirement: "SC-8"
    verification:
      - kind: other
        ref: "grep -c '^- \\[' (28) and grep -cE '^\\| [A-Z]+-[0-9]+ \\|' (28) over .planning/REQUIREMENTS.md"
        status: pass
    human_judgment: false
  - id: D7
    description: "`example/` 下没有任何文档把本阶段删掉的符号当作现存 API 描述；`example/chunking.md` 携带 2026-09-12 的守卫删除小节，并把格算术转指 `_roster_window_profile`"
    requirement: "SC-8"
    verification:
      - kind: other
        ref: "! grep -rn -e MAX_DENSE_PANEL_BYTES -e estimate_dense_panel -e assert_dense_panel_fits -e estimate_chunked_panel -e assert_chunked_panel_fits -e plan_calendar example/ -> zero matches"
        status: pass
      - kind: other
        ref: "uv run python -c \"dated-section + forwarding-address + five-rung + import-every-named-attribute gate\" -> 'example guides describe only live API'"
        status: pass
    human_judgment: false
  - id: D8
    description: "`example/acquisition.md` 两句改写后仍陈述「护栏在任何 client 被构造之前跑完」「凭证只来自环境变量」「命令行无处传 key」，且保留 `assert_acquisition_volume_fits` 之名"
    requirement: "SC-8"
    verification:
      - kind: other
        ref: "uv run python -c \"four committed-literal absolute pins\" -> 'acquisition guide keeps every surviving point'"
        status: pass
    human_judgment: false
  - id: D9
    description: "四个决策记录里删除的行数不超过 5 —— 每一处改动都是新增而非替换"
    requirement: "SC-8"
    verification:
      - kind: other
        ref: "uv run python -c \"git diff HEAD --numstat over the four decision records\" -> 'deleted_lines=1'"
        status: pass
    human_judgment: false
  - id: D10
    description: "冷读者到 03.5-CONTEXT.md 或 260906-13w 仍能重建当初决定了什么、何时改变、为何改变 —— 标注读起来像一段有日期的历史，而不是盖在矛盾之上的补丁（plan backstop，verification 步骤 6）"
    verification: []
    human_judgment: true
    rationale: "这是关于散文连贯性的判断：断言只能证明原文还在、新块在其下方、日期与出处被点名，证明不了「读起来是一段历史而不是一块补丁」。需要人把 D-10 与 D-11 两段从头读到尾。"
  - id: D11
    description: "`uv run pytest -q` 仍为 785 passed —— 本计划不碰任何源码，红即说明越界"
    verification:
      - kind: integration
        ref: "uv run pytest -q -> 785 passed, 452 warnings in 102.48s"
        status: pass
    human_judgment: false

# Metrics
duration: 19 min
completed: 2026-09-13
status: complete
---

# Phase 03.6 Plan 04: SC-8 的原位标注与 DATA-08 撤回 Summary

**把本阶段证伪的每一处锁定文本按 D-18 原位标注（原文一字不删，标注写在其下方）：ROADMAP 03.5 的 Goal/SC-4/SC-5、quick task 260906-13w 的 D-05（一处权威修正块 + 八处指针）、03.5 的 D-10/D-11；正式撤回 REQUIREMENTS.md 的 DATA-08 并按文件实际行数重算 v1 覆盖率；顺带把两份 `example/` 指南去陈旧——其中 `example/chunking.md` 的 dry-run transcript 是真实重跑出来的，不是手改的。**

## Performance

- **Duration:** 19 min
- **Started:** 2026-09-13T02:12:59Z
- **Completed:** 2026-09-13T02:32:10Z
- **Tasks:** 3
- **Files modified:** 7
- **Net:** +334 / −76（其中 76 行删除里 64 行在 `example/chunking.md`——参考文档按计划直接改写；四个决策记录合计只删了 **1** 行）

## Accomplishments

- **SC-8 的五个决策记录全部原位标注，删除行数总计 1。** 那一行是 quick SUMMARY 的 YAML 标量（见下文），其余全部是纯新增。计划设的上限是 5。
- **本阶段自己的契约被修正，而代码一个字节没动。** ROADMAP 03.6 SC-5 写着 `--chunk` "stays REQUIRED"，live argparse 从来不是必填。修正的是句子，不是旗标——`quantlab/utils/cli.py` 全程未被打开编辑，闸门用 argparse introspection 正面证明 `required is False`、`default == "year"`、`type is str`。
- **DATA-08 正式撤回，中文原文逐字保留。** checkbox `[ ]` → `[~]`，traceability 行 `Pending` → `Withdrawn`（行保留不删），并明确写出撤回不等于全损：acquisition-volume 守卫仍在（量的是钱和时间），算术搬到了 `_roster_window_profile`，粒度阶梯反而更细。
- **v1 覆盖率算术重算而不是减一**——见下文「算术重算」一节，这是本计划唯一一处实质性判断分歧。
- **`example/` 零匹配，且 transcript 是真跑的。** `example/chunking.md` 的 `--dry-run` 输出用 `env -u` 清掉三个凭证变量后重新执行，八行不再打印的内容随真实输出一起消失；没有任何一行是手工删改出来的。

## Task Commits

1. **Task 1: 四个决策记录的原位标注 + SC-5 契约修正** — `9e8ecbe` (docs)
2. **Task 2: DATA-08 正式撤回** — `a9f3886` (docs)
3. **Task 3: 两份 example 指南去陈旧** — `7f0039d` (docs)

`commits: 3` 由 `git rev-list --count 0bdee4a..HEAD` 实测，不是叙述值；`plan_head_before` 取自落盘的 plan ledger。

## Files Created/Modified

- `.planning/ROADMAP.md` — 03.5 的 Goal（SUPERSEDED IN PART）、SC-4、SC-5 三处 SUPERSEDED 标注；03.6 自身 SC-5 的 AMENDED 标注（+72/−0）
- `.planning/REQUIREMENTS.md` — DATA-08 撤回、traceability 行改判、覆盖率算术重算、footer 更新（+48/−5）
- `.planning/phases/03.5-.../03.5-CONTEXT.md` — D-10 的 SUPERSEDED 块（追加在 2026-09-11 AMENDED 块之下）、D-11 的 SUPERSEDED IN PART 块（+45/−0）
- `.planning/quick/260906-13w-.../260906-13w-PLAN.md` — 一个完整 D-05 修正块 + 四处指针（+34/−0）
- `.planning/quick/260906-13w-.../260906-13w-SUMMARY.md` — 四处指针（+4/−1）
- `example/chunking.md` — 单 planner 表、五级阶梯、重画的调用流图、重跑的 transcript、改写的两条常见坑、新增的 2026-09-12 小节（+121/−64）
- `example/acquisition.md` — `:489` 与 `:514` 两句改写（+10/−6）

## 计划 `<output>` 要求逐条记录的事项

### 1. 驱动 D-05 站点列表的 grep 输出（证据，不是继承）

```
$ grep -n "D-05\|assert_dense_panel_fits\|MAX_DENSE_PANEL_BYTES" 260906-13w-PLAN.md 260906-13w-SUMMARY.md
260906-13w-SUMMARY.md:9   （frontmatter provides，列举上游产物，不是 D-05 的 restatement）
260906-13w-SUMMARY.md:54  key-decisions：「...are untouched...(D-05)」          → 指针（新增同级列表项）
260906-13w-SUMMARY.md:130 coverage D5 description：「...(D-05)」                → 指针（YAML 标量，同行后缀）
260906-13w-SUMMARY.md:181 Accomplishments：「the guard that reported it is intact」→ 指针
260906-13w-SUMMARY.md:336 Self-check：「CONFIRMED by test」                     → 指针
260906-13w-PLAN.md:41   must_haves truths：「...(D-05)」                        → 指针（新增同级列表项）
260906-13w-PLAN.md:75   Decision IDs 段落：D-05 本体                            → **唯一完整修正块**
260906-13w-PLAN.md:207  read_first：「keep all three」                          → 指针
260906-13w-PLAN.md:210  behavior：描述当时新建的 chunked 守卫行为               → 不加指针（见下）
260906-13w-PLAN.md:212  behavior：同上                                          → 不加指针
260906-13w-PLAN.md:214  behavior：「still exist with their current signatures」  → 指针
260906-13w-PLAN.md:222  action：实现说明                                        → 不加指针
260906-13w-PLAN.md:226  action：调用点改写说明                                  → 不加指针
260906-13w-PLAN.md:302  success_criteria：「...(D-05)」                         → 指针
```

计划枚举的是 5 处（PLAN 41/75/302 + SUMMARY 54/130）。重新推导得到 **9 处**需要标注：多出的四处是 PLAN:207、PLAN:214、SUMMARY:181、SUMMARY:336，它们同样以陈述句断言那两个成员被保留。四处 `210/212/222/226` 刻意不加指针：它们描述的是当时**新建**的 chunked 守卫的实现细节（属于 action/behavior 的施工说明），不是「D-05 这个决定成立」的 restatement；它们描述的对象同样已被删除，而唯一那个修正块已经明说「两半都删了」，逐句复述只会把一处权威变成十三处噪音。

**PLAN.md 中同时含 `AMENDED` 与 `03.6` 的行恰好 1 条**（验收标准要求），指针行刻意只写 `03.6` 不写 `AMENDED`，以免把指针误判成第二个修正块。

### 2. ROADMAP 03.5 里 SC-5 之外还标注了哪些，为什么

| 位置 | 判定 | 理由 |
|---|---|---|
| **Goal 段落** | 标注 SUPERSEDED **IN PART** | 它的后半句「can ask what peak RAM that conversion predicts BEFORE anything is allocated」与 SC-4 被同一次删除证伪；前半句（通过 registry 转换）完好无损，所以是 IN PART 而不是整条 |
| **SC-4** | 标注 SUPERSEDED | 「The caller can ask what peak a given (roster, window) conversion predicts, and get an answer WITHOUT starting the conversion」——这正是被删的那个能力本身，与 SC-5 同源。标注里把 D-11「convert() 不跑守卫」那一半明确划为**未变且仍为真**，只有「ANSWERABLE before the run」这一半失去了回答者 |
| **SC-5** | 标注 SUPERSEDED | 计划指定 |
| **SC-6** | **不标注** | 读过：它讲的是三个 shell 通过统一入口做转换、以及第四个 shell 明确不加入。与预测峰值能力无关，本阶段也没碰这件事。计划明令「Do NOT annotate criteria that merely mention chunking」 |

### 3. 撤回状态选用的 checkbox 标记

`[~]`。先查过：`.planning/REQUIREMENTS.md` 此前只出现过 `[ ]` 与 `[x]`，没有第三种标记，所以按计划的指示使用 `[~]` 并在此说明。理由写进了文件本身：DATA-08 从未交付所以不能标 `[x]`，也不是待办工作所以不能留 `[ ]`。`grep -c '^- \['` 与 HEAD 一致（28），bullet 数未变。

### 4. SC-5 修正案的文本，以及为写它而从源码读到的 `add_chunk_args` 取值

从 `quantlab/utils/cli.py` 实读（只读，未编辑）：

```python
def add_chunk_args(parser, *, default: str = "year") -> argparse.ArgumentParser:
    parser.add_argument(
        "--chunk",
        type=str,
        choices=list(TimeChunkPlanner.GRANULARITIES),
        default=default,
        help=...,
    )
```

即：`type=str`、`choices=list(TimeChunkPlanner.GRANULARITIES)`、`default=default` 而工厂关键字是 `default: str = "year"`、**`required` 根本没有传**，因此 argparse 保持 `False`。argparse introspection 交叉验证：`required is False`、`default == 'year'`、`type is str`。

写入 ROADMAP 03.6 SC-5 之下的修正块（摘其骨架，全文见 `.planning/ROADMAP.md`）：

> **AMENDED 2026-09-12 — "REQUIRED" describes the intent, not the argument.** 这项修正针对的是一句错误转述，而不是一个被改变的世界：上面那句话在写下的那一刻就不准确，而它描述的那部分代码正是本阶段**刻意不动**的。
> 1. live 参数究竟是什么 —— `type=str`、`choices=list(TimeChunkPlanner.GRANULARITIES)`、`default=default`（工厂关键字 `default: str = "year"`），`required` 从未传入，argparse 留它为 `False`；不传 `--chunk` 得到 `year`。
> 2. 本阶段刻意让这个旗标保持**逐字节不变**；两个新 rung 之所以自动到达 CLI，恰恰**因为** `choices` 是派生的。plan `03.6-01` Task 2 带着那把可执行的锁（`tests/test_chunked_ingest.py::test_the_chunk_flag_itself_is_left_exactly_as_it_was`）——有人「照着措辞去修旗标」时会变红的就是它。
> 3. SC-5 实际断言的是：(i) `quantlab/` 下不存在任何按频率键控的 chunk 粒度常量表；(ii) `add_chunk_args` 继续从 `TimeChunkPlanner.GRANULARITIES` 派生 `choices`。两半都已被 plan `03.6-01` Task 2 钉住（AST 扫描 + monkeypatch 臂）。
>
> 为什么记录而不是悄悄改掉：下游 verifier 照字面读 SC-5，要么判它未达成（旗标不是必填），要么「照着措辞去修旗标」——后者是由一处文档瑕疵引入的、面向操作者的真实回归。

### 5. `example/` transcript：重跑了哪一份、哪一份不需要

| 文件 | 处置 | 说明 |
|---|---|---|
| `example/chunking.md` 例一 `--dry-run` | **重跑替换** | `env -u TIINGO_API_KEY -u APCA_API_KEY_ID -u APCA_API_SECRET_KEY uv run python ingest_us_equity.py --start-date 2025-09-07 --end-date 2026-09-06 --dry-run`，exit 0。真实输出里 `dense grid cells` / `dense float64` / `observed float64` / `chunk granularity` / `chunk count` / `whole-range total` / `largest chunk` / `per-chunk budget` **八行全部不再打印**。顺带发生的真实漂移也照实记在文档里：标的数 8,159 → 8,157、密度 0.890 → 0.895（roster 在这几天动了）、预览十个符号变化（排序前十而非随机十个）——这些与本次删除无关，写清楚以免读者误以为是删除的副作用 |
| `example/acquisition.md` 例二 | **不需要重跑** | 它贴的是 `Pre-flight volume estimate` 九行加一段凭证 `RuntimeError`，没有任何一行来自被删的守卫；今天重跑仍会产出同样的形状。改动只在其上下两段散文 |

凭证安全（T-03.6-15）：唯一一次执行是上表那条，三个凭证环境变量全部用 `env -u` 显式清掉，输出里只出现本机路径，无任何 key。

## 算术重算（计划要求在任务笔记里写明前后总数）

| | 之前 | 之后 |
|---|---|---|
| v1 requirements 曾定义 | **26**（文件里写的） | **28**（按文件实际行数重算） |
| 其中 withdrawn | — | 1（DATA-08） |
| active | 26 | 27 |
| Mapped to phases | 26 | 28 |
| Unmapped | 0 | 0 |
| 实际 `- [` bullet 数 | 28 | 28（未变） |
| 实际 traceability 行数 | 28 | 28（未变） |

计划写的是「26 = 25 active + 1 withdrawn」。按文件实际行数数出来是 **28**：`grep -c '^- \['` 得 28，`grep -cE '^\| [A-Z]+-[0-9]+ \|'` 也得 28。那个 26 在本次撤回之前**就已经陈旧**——它早于 2026-09-11 新增的 DATA-07 与 DATA-08（footer 自己记着这次新增），而覆盖率块从未被递增。

把 26 减一得到 25 会同时做错两件事：把旧误差原样带下去，并让「总数」与文件本身对不上（25 active 对 28 个 bullet）。因此按实测重算为 28 = 27 + 1，并在文件里留下一段「算术注记」明写旧值 26 为何是错的——旧数字本身也是一条值得保留的事实，不能悄悄抹掉。

## Decisions Made

1. **修正契约，不修正代码。** `quantlab/utils/cli.py` 只读打开，从未编辑；闸门正面证明旗标未变。这是 part (e) 的全部要点。
2. **一处权威 + N 处指针。** D-05 的完整说明只写一遍，其余八处各一行指针。复述 N 遍正是 phase 03.4 那类文档缺陷复发的机制。
3. **YAML 标量没有「底下」。** quick SUMMARY 的 coverage `description` 是一个 YAML 标量，无法在其「下方」插入散文而不引入新键（新键会污染 coverage schema）。指针以同行后缀追加，原文逐字在前，并在指针里显式说明「YAML 标量没有 beneath」。这是四个决策记录里唯一一行删除。
4. **描述性指代，沿用 03.6-02 的手法。** DATA-08 的撤回注记本要写明「没有写后继 requirement」，但直接写出那个标识符会触发计划自己的零匹配闸门。改为描述性说法并显式说明为何不复述——与 03.6-02 在 `registry.py` 对 D-11 做的处理同源。
5. **`example/` 的守卫删除单独成节。** 「文档里干脆不提」会让读者以为守卫从未存在或是漏写。这一节明写：删了什么、代价是什么（直接 OOM）、操作者还剩什么（更细的 rung、acquisition-volume 守卫、自己算），以及**哪些守卫没被删**（`XrBackend.append()` 的坐标/dtype 守卫、`ChunkLedger.assert_consistent()`）——防止读者把两类守卫混为一谈。

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] 计划给出的覆盖率算术（26 = 25 + 1）与文件实际行数不符**

- **Found during:** Task 2
- **Issue:** 计划指示把覆盖率重述为「26 曾定义 = 25 active + 1 withdrawn」。实测文件里有 28 个 v1 requirement bullet 和 28 条 traceability 行。那个 26 早于 2026-09-11 的 DATA-07/DATA-08 新增，从未被递增。照计划写会让「总数」与文件本身相差 3（25 active vs 28 bullets），且验收标准「total = active + withdrawn 且内部自洽」只在数字彼此自洽、同时也与文件自洽时才真正成立。
- **Fix:** 按文件实际行数重算为 28 = 27 active + 1 withdrawn，mapped 28、unmapped 0，并在文件里加一段「算术注记」写明旧值 26 为何陈旧、以及为何不用「减一」的办法。
- **Files modified:** `.planning/REQUIREMENTS.md`
- **Verification:** `grep -c '^- \['` → 28；`grep -cE '^\| [A-Z]+-[0-9]+ \|'` → 28；bullet 数与 HEAD 一致
- **Committed in:** `a9f3886`
- **注：** 本次派单提示里的 `<arithmetic_care>` 明确要求「从文件实际行数重算，而不是把某个数字减一，并在任务笔记里写明前后总数」，本处即按该指示执行。ROADMAP 的 `artifacts_this_phase_produces` 表里写的「26 defined = 25 active + 1 withdrawn」同样基于那个陈旧数字，以本 SUMMARY 的实测为准。

**2. [Rule 3 - Blocking] 撤回注记写出后继 requirement 的标识符会触发计划自己的闸门**

- **Found during:** Task 2
- **Issue:** 按 D-18 的精神，撤回注记应当说明「没有写后继 requirement、以及为什么」。第一版直接写出了那个标识符，随即让 Task 2 的第一条 `<verify>`（`assert 'DATA-09' not in cur`）变红——合规注记本身被判成违规。
- **Fix:** 改为描述性说法：「NO successor requirement was written… That drafted successor's ID is deliberately NOT restated here — an executable gate asserts the identifier appears nowhere in this file」。指令含义与可追溯性完整保留，只是不复述字面量。手法与 03.6-02 在 `quantlab/acquisition/registry.py` 处理 D-11 的方式一致。
- **Files modified:** `.planning/REQUIREMENTS.md`
- **Verification:** Task 2 第一条 `<verify>` 通过
- **Committed in:** `a9f3886`

**3. [Rule 2 - Missing Critical] D-05 的 restatement 站点比计划枚举的多四处**

- **Found during:** Task 1
- **Issue:** 计划枚举 5 处并要求「re-derive 而不是照搬」。新 grep 得到的实际断言性 restatement 是 9 处：多出 `PLAN.md:207`、`PLAN.md:214`、`SUMMARY.md:181`、`SUMMARY.md:336`。漏掉它们会留下四处仍在声明「守卫被保留」的文本——这正是 T-03.6-12 与 phase 03.4 gap 2（行号继承错误）要防的失效模式。
- **Fix:** 九处全部处理：`PLAN.md:75` 放唯一完整修正块，其余八处各加一行指针。指针刻意不含 `AMENDED` 字样，以满足「恰好一个完整修正块」的验收标准。
- **Files modified:** 两个 quick task 文档
- **Verification:** 含 `AMENDED` 且含 `03.6` 的行数 = 1；新 grep 复查九处均已标注
- **Committed in:** `9e8ecbe`

---

**Total deviations:** 3 auto-fixed（1 bug、1 blocking、1 missing critical）
**Impact on plan:** 没有扩大范围。偏差 1 是计划文本与文件现实不符，且派单提示的 `<arithmetic_care>` 已预见到；偏差 2 是计划自己的闸门与 D-18 的完整性要求相撞，用 03.6-02 已确立的手法化解；偏差 3 是计划明令「re-derive」之后必然出现的扩充。三处都只改文档，未触碰任何源码、测试或 shell。

## Issues Encountered

- **`example/chunking.md` 的 dry-run 重跑产生了与删除无关的真实漂移**（标的数 8,159 → 8,157、密度 0.890 → 0.895、预览符号变化）。没有为了「看起来只有删除的影响」而挑数字：按 `example/README.md` 的约定贴真实输出，并在文档里写清这些漂移来自 roster 本身的变化。
- 计划正文给出的行号（`example/chunking.md:5/25/34/54/57/61/78/82/86/107/173-176/644-646/675` 等）与实际基本吻合，按计划指示仍以新 grep 为准执行。

## Verification Results

| # | Command | Result |
|---|---------|--------|
| 1 | `! grep -rn -e MAX_DENSE_PANEL_BYTES -e estimate_dense_panel -e assert_dense_panel_fits -e estimate_chunked_panel -e assert_chunked_panel_fits -e plan_calendar example/` | PASS，零匹配（grep exit=1） |
| 2 | DATA-08 byte-identity + 撤回检查 | `DATA-08 withdrawn in place; DATA-07 untouched; no DATA-09` |
| 3 | DATA-08 中文原文绝对 pin | `DATA-08 original wording preserved verbatim beneath its withdrawal` |
| 4 | requirement bullet 计数 vs HEAD | `requirement bullet count unchanged`（28） |
| 5 | 四文件标注存在性 + 03.6 引用 + D-10 顺序 | `decision records annotated in place` |
| 6 | 四文件 heading 结构 vs HEAD | `heading structure preserved in all four` |
| 7 | ROADMAP 03.6 SC-5 切片 token 闸门 + argparse introspection | `SC-5 annotated in place and still describes the live argument` |
| 8 | 五条 committed-literal 绝对 pin（原文仍在） | `every annotated original is still present verbatim` |
| 9 | 四个决策记录的删除行数上限 | `deleted_lines=1`（上限 5） |
| 10 | `example/chunking.md` 日期节 + forwarding address + 五级阶梯 + 逐属性导入 | `example guides describe only live API` |
| 11 | `example/acquisition.md` 四条绝对 pin | `acquisition guide keeps every surviving point` |
| 12 | `example/acquisition.md` 行数下限 vs HEAD | `acquisition guide rewritten rather than gutted` |
| 13 | 两个 quick task 文档的 YAML frontmatter 仍可解析 | 均 `yaml ok dict`（指针以合法同级列表项 / 同行后缀写入） |
| 14 | `uv run pytest -q` | **785 passed**，与 wave 3 收尾同数 |
| 15 | `PLAN.md` 中同含 `AMENDED` 与 `03.6` 的行数 | 1（恰好一个权威修正块） |
| 16 | 每次提交的 `git diff --diff-filter=D` | 三次提交均无文件删除 |

**验证步骤 6（backstop，by-eye）：** D-10 与 D-11 两段已端到端读过。D-10 现在按时间顺序携带三层：原决定 → 2026-09-11 的 `bars_per_day` 修正 → 2026-09-12 的 SUPERSEDED，并明确区分「被推翻的是字节那一半」与「2026-09-11 的修正是**对的**、其算术搬到了 `_roster_window_profile`」——有转发地址，不是一条断头的删除注记。D-11 的标注先说仍为真的一半（`convert()` 不跑守卫），再说变假的一半（shell/console 在 `convert()` 之前调用守卫），最后指出记录在案的风险从**有条件**（忘了问）变成**无条件**（无可问）——这是类别变化而不只是程度变化，也是该决定「Revisit if that failure is observed」那一句现在独自承重的原因。两段读起来是有日期的历史，不是盖在矛盾上的补丁。

**Spec-less probe fallback — VISIBLY SKIPPED.** 与计划所记一致：phase 03.6 无 `SPEC.md`，`Requirements: N/A`，本计划未生成 probe 派生的 edge / prohibition 谓词。上表全部来自计划自带的 `<verify>` 命令。

**actuals 的量具说明：** `tokens: 8743` = 本计划实际 diff 净荷（`git diff 0bdee4a..HEAD` 的 `+`/`-` 行，34,972 字符）除以 4。同口径下计划的 `estimate.tokens: 46000` 是一次显著高估——这是一个纯文档计划，散文写入量远低于按「七个文件、五份决策记录」形成的预期。没有为了贴近估算而调整数字；作为参考，被改动七个文件的**全文**字符数为 270,134（÷4 ≈ 67,534），两种口径差异很大，因此在此写明用的是前者。

## Known Stubs

None —— 本计划未引入 stub、未跳过任何测试、未留下未运行的 `<verify>`。所有 `<verify>` 命令均按原文执行并记录于上表。

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- **phase 03.6 的四个计划全部完成**，SC-1..SC-8 各自有落地物与闸门。可进入 phase gates / `/gsd-verify-work 03.6`。
- **给验证者的两条提示：**
  1. ROADMAP 03.6 SC-5 的字面措辞「`--chunk` stays REQUIRED」**仍然在原位**（D-18 要求保留原文），它下方的 AMENDED 块才是该准则的操作性内容。请照修正块判定，**不要**把旗标改成必填——那正是 `tests/test_chunked_ingest.py::test_the_chunk_flag_itself_is_left_exactly_as_it_was` 会变红的回归。
  2. 对被删的五个 dense-panel 符号与 `plan_calendar` 做负向断言时，`.planning/` 与 `example/` 下仍有刻意保留的 D-18 标注散文提及它们（`example/` 除外——该目录已实测零匹配）。源码树的零匹配由 03.6-03 建立并仍成立。
- **一项待人工确认（承自 03.6-02 coverage D7）：** `ingest_us_equity.py --dry-run` 去掉 dense-byte 行后的输出可读性。本计划已把真实的新输出贴进 `example/chunking.md` 例一，可直接对着它判断。
- 无阻塞项。

---
*Phase: 03.6-frequency-keyed-chunking-policy*
*Completed: 2026-09-13*

## Self-Check: PASSED

- 磁盘存在：`.planning/ROADMAP.md`、`.planning/REQUIREMENTS.md`、`.planning/phases/03.5-.../03.5-CONTEXT.md`、两个 quick task 文档、`example/chunking.md`、`example/acquisition.md`、本 SUMMARY —— 全部 FOUND。
- 提交可达：`9e8ecbe`、`a9f3886`、`7f0039d` —— 全部可在 `git log --all` 中查到。
- `commits: 3` 由 `git rev-list --count 0bdee4a..HEAD` 实测，非叙述值。
- `uv run pytest -q` → **785 passed**，与本计划开工时的基线一致；本计划未触碰任何源码。
- `quantlab/utils/cli.py` 全程未被编辑（`git diff --name-only 0bdee4a..HEAD` 中不含该文件）。
