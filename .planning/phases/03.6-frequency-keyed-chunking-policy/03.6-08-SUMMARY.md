---
phase: 03.6-frequency-keyed-chunking-policy
plan: 08
subsystem: database
tags: [zarr, xarray, chunking, docstring, D-18, gap-closure]

requires:
  - phase: 03.6-frequency-keyed-chunking-policy
    provides: "plans 03.6-01..03.6-07 -- the TimeChunkPlanner ladder, append_dim_size threading and the D-18 annotations this plan extends"
provides:
  - "`XrBackend.widen_and_append`'s falsified unconditional grid claim marked `NOT TRUE ANY MORE AS OF 03.6-07` / `SUPERSEDED by`, original preserved, narrowed per-call claim written beneath it"
  - "The accepted cost (a `write()`-built store keeps two chunk grids) named in production with its measured numbers, its root cause WR-02, its ledger pointer and its executable lock"
  - "WR-06's remaining half closed: `_widen_block_rows` and `_widen_chunked` transcribed excerpts marked, corrections written as prose"
  - "`tests/test_chunked_ingest.py::test_a_store_built_by_write_keeps_two_chunk_grids_accepted_cost` -- the executable lock on the `write()`-built path no existing arm exercised"
  - "Plan 03.6-11's `03.6-VERIFICATION.md` override is now UNBLOCKED -- its precondition was this plan's Task 1 having landed"
affects: [03.6-11, 03.6-VERIFICATION, deferred-items, chunking-docs]

actuals:
  tokens: 3548
  tasks: 3
  commits: 3
plan_head_before: c0db5f97cc01621ec512fc85043e446e58604eb8

tech-stack:
  added: []
  patterns:
    - "D-18 preserve-and-mark with explicit `[BEGIN preserved original -- D-18]` / `[END preserved original]` fences, so a quoted falsified paragraph is unambiguously bounded and can be kept byte-identical (its critical sentences stay on their original source lines, which is what the `inspect.getdoc(...)` substring gates depend on)"
    - "Corrections to a transcribed code excerpt are written as PROSE, never as a second transcription -- WR-06's rot concern answered without deleting the original"
    - "Characterization lock: a test that pins an ACCEPTED COST rather than a fixed defect, whose docstring names the artifacts that must be updated together the day it goes red"

key-files:
  created: []
  modified:
    - quantlab/dataset/backend.py
    - tests/test_chunked_ingest.py

key-decisions:
  - "Route (b) executed as locked: the wording was narrowed and the universal retracted. `XrBackend.write()` was NOT changed -- no `kwargs.setdefault(\"encoding\", ...)`, no first step toward route (a)."
  - "WR-06's suggested fix (DELETE the transcribed excerpts) was deliberately declined and the decline recorded in `_widen_block_rows`'s own annotation: D-18 forbids deleting falsified text. The adopted half is the marker prefix; the rot concern is answered by writing every correction as prose."
  - "The preserved originals keep their ORIGINAL line wrapping rather than being re-flowed, because `a store cannot come out of here carrying one grid per axis` and `the block's own length is what decides` must each stay on ONE source line for the acceptance gates to find them in `inspect.getdoc(...)`."
  - "Task 3's arm is green on landing and that is correct, not a skipped RED: route (b) leaves the behaviour alone by design, so the arm characterizes an accepted cost instead of repro-ing a defect. Its RED condition is a FUTURE change (route (a) landing), and its docstring states the three artifacts that must move with it."

patterns-established:
  - "Pattern 1: a falsified production claim is narrowed to the scope that IS true (this call's three writes agree with each other) rather than deleted or left standing"
  - "Pattern 2: an accepted cost is named in production code with its measured numbers, its root cause ID, its ledger path and its pinning test -- four pointers, so no reader has to re-derive it"

requirements-completed: [D-18, PHASE-GOAL-caller-owns-a-stated-cost]

coverage:
  - id: D1
    description: "`XrBackend.widen_and_append`'s unconditional grid claim no longer stands: the original sits verbatim under a `NOT TRUE ANY MORE AS OF 03.6-07` / `SUPERSEDED by` marker with the narrowed per-call claim beneath it (D-18 shape)"
    requirement: "D-18"
    verification:
      - kind: other
        ref: "uv run python -c \"...inspect.getdoc(XrBackend.widen_and_append)... marker index < falsified-sentence index, D-18 cited between them\""
        status: pass
      - kind: other
        ref: "grep -c 'SUPERSEDED by' quantlab/dataset/backend.py == 5 && grep -c 'NOT TRUE ANY MORE AS OF 03.6-07' == 1 (after Task 1)"
        status: pass
    human_judgment: false
  - id: D2
    description: "The accepted cost is named in the production docstring with its measured numbers ({'close': (1000, 2)} then 'newvar': (512, 2) at the real default APPEND_DIM_CHUNK = 512), its root cause WR-02, its ledger path and its pinning test"
    requirement: "PHASE-GOAL-caller-owns-a-stated-cost"
    verification:
      - kind: other
        ref: "inspect.getdoc(XrBackend.widen_and_append) contains (1000, 2), (512, 2), WR-02, deferred-items.md, test_a_store_built_by_write_keeps_two_chunk_grids_accepted_cost"
        status: pass
    human_judgment: false
  - id: D3
    description: "WR-06's remaining half closed: `_widen_block_rows` and `_widen_chunked` transcribed excerpts each carry the marker over a preserved original, with the correction written as prose"
    requirement: "D-18"
    verification:
      - kind: other
        ref: "uv run python -c \"...inspect.getdoc(_widen_block_rows)/(_widen_chunked)... marker index < excerpt index, corrections name append_dim_size, MAX_WIDEN_BYTES restated\""
        status: pass
      - kind: other
        ref: "grep -c 'NOT TRUE ANY MORE AS OF 03.6-07' quantlab/dataset/backend.py == 3 && grep -c 'SUPERSEDED by' == 7"
        status: pass
    human_judgment: false
  - id: D4
    description: "The accepted cost has an executable lock on the `write()`-built path no existing arm exercised"
    requirement: "PHASE-GOAL-caller-owns-a-stated-cost"
    verification:
      - kind: unit
        ref: "tests/test_chunked_ingest.py#test_a_store_built_by_write_keeps_two_chunk_grids_accepted_cost"
        status: pass
      - kind: unit
        ref: "uv run pytest tests/test_chunked_ingest.py -q => 66 passed"
        status: pass
    human_judgment: false
  - id: D5
    description: "Zero production behaviour change -- the grid path comes out of this plan byte-identical"
    verification:
      - kind: other
        ref: "docstring-stripped AST of quantlab/dataset/backend.py == that of `git show 00cd680:quantlab/dataset/backend.py` (same strip routine), checked at the end of Task 1 AND Task 2"
        status: pass
      - kind: unit
        ref: "uv run pytest -q => 798 passed, 0 failed (797 baseline + the one new arm)"
        status: pass
    human_judgment: false

duration: 22 min
completed: 2026-09-13
status: complete
---

# Phase 03.6 Plan 08: Narrow the Falsified Grid Claim and Pin the Accepted Cost Summary

**GAP #19's production half closed by route (b): `widen_and_append`'s unconditional "a store cannot come out of here carrying one grid per axis" now sits preserved under a D-18 marker with the narrowed per-call claim, the named accepted cost and its measured `(1000, 2)` / `(512, 2)` numbers beneath it, backed by a new test arm that builds through `write()` and asserts TWO grids — zero behaviour change, AST-identical to `00cd680`.**

## Performance

- **Duration:** 22 min
- **Started:** 2026-09-13T20:23:32Z (approx, at HEAD `c0db5f9`)
- **Completed:** 2026-09-13T20:45:52Z
- **Tasks:** 3 of 3
- **Files modified:** 2

## Accomplishments

- **Task 1 (LEAD, group A1):** The sentence `03.6-VERIFICATION.md` measured FALSE no longer stands as an unconditional claim. It is preserved verbatim between `[BEGIN preserved original -- D-18]` / `[END preserved original]` fences under a one-line `NOT TRUE ANY MORE AS OF 03.6-07 -- SUPERSEDED by phase 03.6's third gap-closure pass (plan 03.6-08)` marker. Beneath it: the narrowed true claim (the three things THIS CALL writes read `append_dim_size` from the same `kwargs` entry and so cannot disagree with EACH OTHER; nothing is claimed about variables the store already held), the accepted cost with its mechanism and numbers, and the disposition naming WR-02, the ledger and the route not taken.
- **Task 2 (group C, WR-06's remaining half):** `_widen_block_rows`'s opening transcribed excerpt and `_widen_chunked`'s transcribed first-block sentence each carry the same marker over a preserved original, with the correction written as PROSE. `_widen_block_rows`'s annotation additionally records, in one sentence, why the excerpt was kept rather than deleted as WR-06 proposed.
- **Task 3 (group A5):** `test_a_store_built_by_write_keeps_two_chunk_grids_accepted_cost` builds the store through `XrBackend().to_internal(panel).write(path)` — the path no existing arm touched, because `_incomplete_store` builds through `append(append_dim_size=9)` — asserts the creating grid is `{'close': (10, 2)}`, adds a variable through `widen_and_append(path, append_dim_size=15)`, and asserts `close` still `(10, 2)`, `newvar` on `(4, 2)` and a grid set of size exactly 2.
- **Plan 03.6-11 unblocked:** its `03.6-VERIFICATION.md` override was gated on a runnable `<precondition>` that this plan's Task 1 had landed. Task 1 is committed at `a024fea`; the precondition is now satisfiable.

## Measured Counters (recorded as the plan required)

| After  | `SUPERSEDED by` | `NOT TRUE ANY MORE AS OF 03.6-07` | `test_chunked_ingest.py` | whole suite |
|--------|-----------------|-----------------------------------|--------------------------|-------------|
| baseline (`00cd680`) | 4 | 0 | 65 | 797 |
| Task 1 | **5** | **1** | **65** | not re-run (scoped run only) |
| Task 2 | **7** | **3** | **65** | not re-run (scoped run only) |
| Task 3 | 7 | 3 | **66** | **798 passed, 0 failed** |

Every number matches the plan's stated expectation exactly. `uv run pytest -q` → **798 passed** in 104.91s; the +1 over the 797 baseline is fully explained by the single new arm.

`uv run pytest tests/test_chunked_ingest.py -k "grid or narrow_stated" -q` → **13 passed** (was 12). The rise is explained and expected: the new arm's name contains `grids`, so the existing filter picks it up. The filter was NOT adjusted to hide the change.

## Task Commits

1. **Task 1: narrow `widen_and_append`'s grid paragraph, name the accepted cost** — `a024fea` (docs)
2. **Task 2: mark the two remaining stale code excerpts (WR-06's other half)** — `a75e967` (docs)
3. **Task 3: the executable lock for the accepted cost** — `d4589a1` (test)

**Plan metadata:** see the `docs(03.6-08)` commit that carries this SUMMARY.

## Files Created/Modified

- `quantlab/dataset/backend.py` — three docstring regions annotated under D-18 (`widen_and_append`, `_widen_block_rows`, `_widen_chunked`). **Docstring text only**: the docstring-stripped AST is byte-identical to `git show 00cd680:quantlab/dataset/backend.py`, machine-checked with the same `strip` routine at the end of Task 1 AND again at the end of Task 2, against the immutable commit blob rather than against a self-snapshot.
- `tests/test_chunked_ingest.py` — one new test function inserted immediately after `test_a_variable_widen_leaves_one_chunk_grid_not_two`. No other test in the file was touched; `_incomplete_store` and the singleton arm are unchanged, and that pair (singleton on the `append(append_dim_size=...)`-built store, two grids on the `write()`-built one) IS the narrowed claim made executable.

## Scope Fence Held

- `XrBackend.write()`'s body: **untouched**. No `encoding`, no `kwargs.setdefault`, no first step toward route (a).
- `_append_encoding`'s `max(panel.sizes[dim], append_dim_size)` and `max(min(APPEND_DIM_CHUNK, size), 1)`: untouched.
- The `append_dim_size` threading, `--chunk` and the CLI: untouched.
- `git diff --name-only 00cd680 -- tests/test_symbol_axis_widening.py tests/test_variable_axis_widening.py tests/test_factor_update.py tests/test_widening_fixture_realism.py tests/test_dataset_update.py` → **no output**, so VERIFIED truth #21's evidence ("these files are not in the diff") still holds.
- Nothing was deleted anywhere in this plan.

## Decisions Made

- **Route (b), exactly as locked.** The wording was narrowed and the universal retracted; the root cause was not fixed. Route (a) is named in the docstring as the route not taken, with the reason (it would change the on-disk grid of every store this project writes, on a pre-03.6 bulk-write path `03.6-REVIEW-FIX.md` already declined).
- **WR-06's delete-the-excerpt half declined on the record.** D-18 forbids deleting falsified text. The marker-prefix half was adopted; the rot concern was answered by writing every correction as prose so no new transcription entered the file. That reasoning is written into `_widen_block_rows`'s annotation itself, so the next reader sees the excerpt was kept on purpose rather than missed.
- **Preserved originals keep their original line wrapping.** Re-flowing them to fit a deeper quote indent would have split `a store cannot come out of here carrying one grid per axis` and `the block's own length is what decides` across source lines, which `inspect.getdoc(...)` renders with an embedded newline and which the acceptance gates then cannot find. Explicit `[BEGIN ...]` / `[END ...]` fences at the same indent give the reader the boundary instead.
- **Task 3's arm is green on landing, deliberately.** Its docstring says so in the first paragraph and states its future RED condition (`write()` starts pinning `encoding`) together with the three artifacts that must then be updated in the same change.

## Deviations from Plan

None — plan executed exactly as written.

Two things worth recording that are NOT deviations, because the plan anticipated both in writing:

- The `-k "grid or narrow_stated"` count rose 12 → 13. The plan's verification step 5 explicitly permits this "for a reason you can state": the new arm's name contains `grids`.
- Task 3 carries `tdd="true"` but its arm passes on first run. This is the plan's own design (`"This arm PINS AN ACCEPTED COST. It is not a red-to-green repro"`), so there is no RED phase to evidence; `TDD_MODE=false` for this run, so no orchestrator-level RED gate applied either. It shipped as a single `test(03.6-08)` commit.

## Issues Encountered

None. Every acceptance criterion passed on first evaluation for all three tasks; the only in-task correction was adding the second blank line between the new test function and its successor (PEP 8 spacing), caught before the task's verify ran.

## Threat Flags

None. No new network endpoint, auth path, file-access pattern or schema change at a trust boundary. The plan's `T-03.6-08-04` (`accept`) is unchanged in substance: the residual two-grid layout cost is now named in production, ledgered by plan 03.6-11 and pinned by `T-03.6-08-03`'s arm.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- **Plan 03.6-11 is unblocked.** Its Task 2 `<precondition>` was "plan 03.6-08's Task 1 has landed"; Task 1 is committed at `a024fea` and its marker/number/pointer gates all pass, so 03.6-11's `03.6-VERIFICATION.md` override (`accepted_by: dzr`) is now legitimate to write. Plan 03.6-11 still owns writing `deferred-items.md` — **the accepted-cost entry that both `widen_and_append`'s docstring and the new test arm point at does not exist on disk yet**. Those two pointers are forward references until 03.6-11 lands; that is the planned sequencing, not a dangling link introduced here.
- Plan 03.6-11 Task 1 also still owns retracting `03.6-07-PLAN.md`'s `must_haves.truths` #3 (group A2) and Task 3 owns `example/backend.md` / `example/chunking.md` (group B).
- Plans 03.6-09 (WR-04) and 03.6-10 (WR-03) are untouched by this plan; `quantlab/base/data.py` was not modified here.
- Whole-suite baseline for the next plan in this round: **798 passed**, `SUPERSEDED by` = 7, `NOT TRUE ANY MORE AS OF 03.6-07` = 3.

---
*Phase: 03.6-frequency-keyed-chunking-policy*
*Completed: 2026-09-13*

## Self-Check: PASSED

- Files verified on disk: `quantlab/dataset/backend.py`, `tests/test_chunked_ingest.py`, `.planning/phases/03.6-frequency-keyed-chunking-policy/03.6-08-SUMMARY.md`
- Commits verified in history: `a024fea`, `a75e967`, `d4589a1`, `4b6192f`
- Plan `<verification>` re-run: 798 passed / 0 failed; marker counts 3 and 7; AST identical to `00cd680`; the five fenced widen/update test modules absent from the diff.
