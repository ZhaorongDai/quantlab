{
  "command": "uv run pytest tests/test_chunked_ingest.py -v --tb=line -k chunk_grid  (with `append_dim_size=len(timestamps)` temporarily removed from quantlab/base/data.py -- the pre-fix tree)",
  "exitCode": 1,
  "targetTest": "test_the_chunk_grid_is_the_stores_extent_not_the_first_windows",
  "targetFile": "tests/test_chunked_ingest.py",
  "output": "not ok 1 - test_the_chunk_grid_is_the_stores_extent_not_the_first_windows\nnot ok 2 - test_the_chunk_grid_is_identical_at_every_rung[year]\nnot ok 3 - test_the_chunk_grid_is_identical_at_every_rung[month]\nnot ok 4 - test_the_chunk_grid_is_identical_at_every_rung[day]\nnot ok 5 - test_the_chunk_grid_survives_a_crash_and_resume\nnot ok 6 - test_the_chunk_grid_survives_a_rebuild\n# tests 6\n# pass 0\n# fail 6\n",
  "expected": "every data variable's append-dim chunk is min(APPEND_DIM_CHUNK=4, 9) == 4 at every rung, on a resumed run and on a rebuild run alike",
  "actual": "ceiling arm {(1, 3)} != {(4, 3)}; rung arm year {(3, 3)} / month {(1, 3)} / day {(1, 3)} -- the three rungs disagree; resume arm {(3, 3)} and rebuild arm {(3, 3)} both fail on the ABSOLUTE assertion, while their clean-run cross-check would have passed (both runs land on 3 together)",
  "note": "pytest project: the TAP block is a faithful projection of the real run captured at scratchpad/red2.out. RED was produced by temporarily reverting the Task 1 production edit in the working tree and restoring it immediately after; no git history was rewritten and quantlab/ is clean."
}
