---
phase: 03.6-frequency-keyed-chunking-policy
plan: 03
subsystem: infra
tags: [chunking, deletion, docstring, ast-gate, pandas]

# Dependency graph
requires:
  - phase: 03.6-frequency-keyed-chunking-policy
    provides: "03.6-01's five-rung `GRANULARITIES` and the trailing `raise` in `_period_key` — the drift guard the rewritten class docstring now points at"
  - phase: 03.6-frequency-keyed-chunking-policy
    provides: "03.6-02's removal of `UniverseCatalog.estimate_chunked_panel`, `plan_calendar`'s only production caller — the precondition this plan verified before starting"
provides:
  - "`TimeChunkPlanner` with exactly ONE planner, `plan_from_timestamps` — every window edge any caller can obtain is an OBSERVED timestamp"
  - "A class docstring that describes a single-planner class and names the `GRANULARITIES`/`_period_key` drift plus the trailing raise that detects it"
  - "A two-directional deletion lock (`not hasattr` on the deleted planner + positive `hasattr` on the four survivors), both arms mutation-checked live"
  - "The FULL-TREE dense-panel absence claim, satisfiable for the first time in the phase now that `chunking.py:164` is gone"
affects: [03.6-04, chunked conversion]

actuals:
  tokens: 25274
  tasks: 2
  commits: 2
plan_head_before: 492dd622cf9d3a2107097776b6425ad1b057fcca

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Negative constraints on deleted symbols are asserted by AST walk, never by text grep — prose retirement annotations (D-18) and a deletion lock's own string argument are not call sites"
    - "A total-accounting gate: classify EVERY textual occurrence as executable / docstring prose / deletion-lock literal, and fail on anything unclassifiable — strictly stronger than a node-subset walk"
    - "Retired coverage is re-expressed against the surviving subject, not dropped (D-18)"

key-files:
  created: []
  modified:
    - quantlab/base/chunking.py
    - tests/test_chunked_ingest.py

key-decisions:
  - "The full-tree dense-panel gate and the `plan_calendar` gate were both run AST-scoped rather than as the literal text greps the plan wrote — the plan's own acceptance criteria are jointly unsatisfiable under a text grep, and 03.6-02 established the AST instrument for this exact claim"
  - "`_period_key`'s docstring and its trailing-raise comment were edited despite the scope fence naming `_period_key` untouchable — both asserted the two-planner defence Task 1 removed, so leaving them was leaving the exact class of vacuous safety claim T-03.6-10 exists to eliminate"
  - "The rewritten class docstring stops enumerating the granularity tokens and defers to `GRANULARITIES`, so the summary line that went stale in 03.6-01 cannot go stale a third time"
  - "The two new planner tests were placed in the `TimeChunkPlanner` section rather than at the retired tests' old location, which sat misfiled under the SC-5 shells header"

patterns-established:
  - "Total-occurrence accounting: a negative gate that classifies every hit rather than sampling a node subset, so an occurrence it cannot explain is a failure by default"

requirements-completed: [SC-2]

coverage:
  - id: D1
    description: "`TimeChunkPlanner` exposes exactly one planner — `plan_calendar` does not exist and the four surviving members do"
    requirement: "SC-2"
    verification:
      - kind: unit
        ref: "tests/test_chunked_ingest.py#test_the_calendar_sizing_planner_is_deleted_and_the_observed_one_is_not"
        status: pass
      - kind: other
        ref: "uv run python -c \"... assert not hasattr(T,'plan_calendar') ...\" -> 'one planner'"
        status: pass
    human_judgment: false
  - id: D2
    description: "Neither the module docstring, the class docstring, `plan_from_timestamps`'s docstring nor `_period_key`'s docstring claims two planners or defends against inter-planner drift; the class docstring names the `GRANULARITIES`/`_period_key` drift and the trailing raise"
    requirement: "SC-2"
    verification:
      - kind: other
        ref: "uv run python -c \"d=T.__doc__; assert 'plan_calendar' not in d; assert 'GRANULARITIES' in d and '_period_key' in d\" -> 'single planner; docstring rewritten'"
        status: pass
    human_judgment: true
    rationale: "The automated arm proves the deleted name is absent and the two drift terms are present; that the paragraph READS as a coherent single-planner description rather than an edited two-planner one is plan verification step 5's explicit by-eye backstop and no test asserts prose coherence."
  - id: D3
    description: "No executable reference to the deleted calendar planner survives in `quantlab/`, `tests/`, the three shells or `test.py`; every residual textual occurrence is classified (1 deletion-lock literal, 2 D-18 docstring annotations)"
    requirement: "SC-2"
    verification:
      - kind: other
        ref: "AST total-accounting gate over 114 files -> 'no executable reference; 1 deletion lock, 2 D-18 prose lines'"
        status: pass
    human_judgment: false
  - id: D4
    description: "The FULL-TREE dense-panel absence gate — all five deleted symbols across `quantlab/`, `tests/`, the shells and `test.py`, no exclusion — passes for the first time in the phase"
    requirement: "SC-3"
    verification:
      - kind: other
        ref: "AST gate over 110 files -> 'no executable dense-panel reference in 110 files'"
        status: pass
      - kind: other
        ref: "! grep -rn -e MAX_DENSE_PANEL_BYTES -e estimate_dense_panel -e assert_dense_panel_fits -e estimate_chunked_panel -e assert_chunked_panel_fits quantlab/ ingest_*.py test.py -> zero matches"
        status: pass
    human_judgment: false
  - id: D5
    description: "The retired tests' still-true assertions survive against the observed planner: 3 monthly windows over Jan-Mar, 4 quarterly over a year, and 2006..2026 == 21 yearly windows"
    verification:
      - kind: unit
        ref: "tests/test_chunked_ingest.py#test_plan_from_timestamps_is_granularity_sensitive"
        status: pass
      - kind: unit
        ref: "tests/test_chunked_ingest.py#test_the_full_backfill_range_is_twenty_one_yearly_windows"
        status: pass
    human_judgment: false
  - id: D6
    description: "`quantlab/base/chunking.py` is still a LEAF — import surface a subset of the seven allowed modules, asserted absolutely rather than as a delta"
    verification:
      - kind: other
        ref: "uv run python -c \"ast walk of chunking.py imports\" -> 'leaf import surface unchanged'"
        status: pass
    human_judgment: false
  - id: D7
    description: "The chunked conversion path is behaviourally unchanged — same windows, same store, same ledger"
    verification:
      - kind: integration
        ref: "tests/test_chunked_ingest.py#test_a_day_granularity_conversion_reaches_zarr_identical_to_the_unchunked_store"
        status: pass
      - kind: other
        ref: "uv run pytest -q -> 785 passed"
        status: pass
    human_judgment: false

# Metrics
duration: 21 min
completed: 2026-09-13
status: complete
---

# Phase 03.6 Plan 03: 单一 planner 收束 Summary

**`TimeChunkPlanner.plan_calendar` 连同它的 `pd.date_range(..., freq="D")` 日级展开一起删除，`TimeChunkPlanner` 只剩 `plan_from_timestamps` 一个 planner——调用方能拿到的每一个窗口边界都是真实观测到的 timestamp；三处仍在宣称"两个 planner 共享一条周期规则、因此漂移在结构上不可能"的 docstring 被重写为它现在真正防的那种漂移（`GRANULARITIES` 与 `_period_key`）以及 03.6-01 加的那个尾部 `raise`。**

## Performance

- **Duration:** 21 min
- **Started:** 2026-09-13T01:47:00Z
- **Completed:** 2026-09-13T02:08:02Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- **`plan_calendar` 整体删除（36 行）**：方法体、docstring、`days = pd.date_range(start, end, freq="D")` 的逐日展开，以及上方那段"之所以分组逐日范围而不用 `pd.period_range`，是为了让两个 planner 无法漂移"的四行注释。该论证在只剩一个 planner 后不再论证任何东西。
- **import 块一字未动**，如计划预期：`ChunkLedger` 仍然同时使用 `pd.` 与 `xr.`，`Iterable`/`Optional`/`Sequence` 三者也都仍有使用点。`pd.date_range` 在文件中已零出现。
- **类 docstring 重写**为单 planner 陈述，并停止枚举粒度 token、改为指向 `GRANULARITIES`——03.6-01 已经因为这句枚举陈旧而做过一次 Rule 1 修补，改为 defer 让它不会陈旧第三次。
- **模块 docstring** 的"OBSERVED timestamp axis **或** calendar date range"二选一 bullet 收为只剩 observed 一半；`ChunkLedger` bullet 与整段 LEAF 声明按计划一字未动。
- **`plan_from_timestamps` docstring** 去掉"This is the planner the write loop uses"这个暗示存在兄弟方法的措辞，四句契约（交易日不等于日历日、每个边界都是观测值、窗口有序不重叠、恰好覆盖去重轴一次）逐字保留。
- **两个 calendar 测试退休，但断言没有丢**：跨粒度规则（Jan–Mar 三个月窗、整年四个季窗）折进 `test_plan_from_timestamps_is_granularity_sensitive`，用合成日频轴重新表达；2006..2026 = 21 个年窗变成 `test_the_full_backfill_range_is_twenty_one_yearly_windows`，跑在每年一个观测点的轴上。
- **双向删除锁**，形状照抄 03.6-02 在 `tests/test_chunked_panel_estimate.py` 立的那个。两条臂都做了真实变异检查：把 `plan_calendar` 加回去 → 锁变红；把 `plan_from_timestamps` 删掉 → 正向臂变红。
- **relocated 的全树 dense-panel gate 首次成立**：`quantlab/base/chunking.py:164`（`plan_calendar` docstring 里那句 `UniverseCatalog.estimate_dense_panel`）是十三个文件中最后一个，03.6-02 的 scope fence 不许它动。删掉方法就带走了这一行，所以 `quantlab/` + 三个 shell + `test.py` 的**纯文本** grep 现在也返回零匹配。

## 重写后的 drift 段落（03.6-04 引用用，逐字）

供 plan 03.6-04 在修订 `example/chunking.md` 时准确引用。类 docstring 的最后一段：

> The drift this class does have to defend against is between
> `GRANULARITIES` and `_period_key`. A new granularity is added in two
> places, the tuple and a `_period_key` branch, and adding it to only the
> first used to fall through to month-sized windows under another name --
> a wrong window size is a wrong memory bound, and it failed open.
> `_period_key` now ends in a `raise ValueError` naming BOTH lists. That
> raise is unreachable while the two agree, because `__init__` already
> refuses any token absent from `GRANULARITIES`; being unreachable is
> exactly what makes it a drift detector rather than dead code.

前一段（记录删除的动机，`example/chunking.md` 目前仍大篇幅描述 `plan_calendar`，03.6-04 需要它）：

> A second, SIZING-time planner stood beside it until phase 03.6. It ran
> BEFORE the download, when no timestamp axis existed yet, so its edges came
> from calendar arithmetic; its only production consumer was the
> pre-download dense-panel RAM estimator that phase 03.6 deleted. Calendar
> edges routinely name days on which nothing traded, which is why that
> method had to open its own docstring with a warning never to hand its
> windows to a densifier -- and that warning was the only thing enforcing
> it. One entry point removes the warning by removing the hazard.

## Task Commits

1. **Task 1: delete the calendar planner, rewrite the two-planner docstrings** — `114796f` (refactor)
2. **Task 2: retire the calendar-planner tests, lock the deletion both ways** — `18fe2f5` (test)

## Files Created/Modified

- `quantlab/base/chunking.py` — 删除 `plan_calendar`（36 行）；重写模块 docstring bullet、类 docstring、`plan_from_timestamps` docstring 首句；Rule 1 修正 `_period_key` docstring 与其尾部 raise 上方的注释。import 块未动。
- `tests/test_chunked_ingest.py` — 删除两个 calendar 测试（32 行）；`test_plan_from_timestamps_is_granularity_sensitive` 增加两段跨粒度断言与说明其来源的 docstring；新增 `test_the_full_backfill_range_is_twenty_one_yearly_windows` 与 `test_the_calendar_sizing_planner_is_deleted_and_the_observed_one_is_not`；Rule 1 修正漂移测试 docstring 里对已删除类 docstring 措辞的引用。

## Decisions Made

- **全树 gate 用 AST 而不是文本 grep，两处都是。** 这是本计划最实质的一次判断，理由分两层：
  - **dense-panel 五个名字**：`tests/` 里有约 24 行 D-18 退休注释在**散文中**刻意点名这五个符号（03.6-02 的交接事实），文本 grep 会全部误报。03.6-02 已经为同一主张确立了 AST 工具，本计划沿用并把范围从 `tests/` 扩到计划要求的全树（110 个文件）。
  - **`plan_calendar`**：计划自己的两条验收标准**互相矛盾**——既要求 `grep -rn plan_calendar` 零匹配，又要求存在一个 `not hasattr(TimeChunkPlanner, "plan_calendar")` 的删除锁，而后者不可能不写出这个名字。文本 grep 在这里问错了问题。
- **`plan_calendar` 的 gate 做成"全量占位清点"而不是节点子集扫描。** 单纯扫 `Attribute`/`Name`/import 会漏掉 `getattr(T, "plan_calendar")` 这种字符串式可执行引用。改为：枚举扫描树内**每一处**文本出现，逐一归类为 executable（判负）/ docstring 散文（D-18 允许）/ 删除锁字符串字面量（恰好一个，且必须存在——为零说明锁没了），**无法归类者一律判负**。这比计划写的文本 grep 更严，不是更松。
- **越过 scope fence 修了 `_period_key` 的 docstring。** 计划明令"do not touch `_period_key`"。但它的 docstring 写着"Both planners group by this ... 使 sizing 窗口与 write 窗口之间的漂移在结构上不可能"——这正是 T-03.6-10 定义的那类**空洞安全声明**，而本计划存在的理由就是消除它。留着它等于在同一个文件里既删掉又保留同一句假话。判为 Rule 1，改动仅限散文，分支与 raise 文案一字未动。
- **新测试放进 `TimeChunkPlanner` 小节**，而不是原两个 calendar 测试所在的位置——后者位于"The ladder's reach into the three shells (03.6 SC-5)"标题之下，本来就是错位的。

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] 全树 dense-panel gate 的文本 grep 形式不可能通过**

- **Found during:** Task 1（验收前基线测量）
- **Issue:** 计划的 `<verify>` 第三条与 `<verification>` 第三步都写作 `! grep -rn -e MAX_DENSE_PANEL_BYTES ... quantlab/ tests/ ingest_*.py test.py`。实测在 `tests/` 下命中 24 行，全部是 03.6-02 按 D-18 刻意写下的退休注释散文（`tests/test_volume_guard.py`、`tests/test_universe.py`、`tests/test_ingest_shells.py`、`tests/test_chunked_panel_estimate.py`），其中还包括 `test_chunked_panel_estimate.py:44-48` 那个删除锁自己的五元组字符串常量。工具选错了：主张是"没有可执行引用"，文本 grep 答的是"这些字符没出现过"。
- **Fix:** 改用 AST 扫描（`Attribute` / `Name` / `Import` / `ImportFrom` / def 名），范围严格按计划的全树（`quantlab/` + `tests/` + 三个 shell + `test.py`，共 110 个文件，无任何排除），并另外保留**纯文本** grep 作用于 `quantlab/` + 三个 shell + `test.py`——那部分源码树里没有点名这些字面量的 D-18 散文（`registry.py:458-471` 的 SUPERSEDED 注释刻意以描述性说法指代，见 03.6-02 交接），所以文本形式在那里是有意义且更强的。两条臂都通过。
- **Files modified:** 无（gate 是只读探针）
- **Verification:** `no executable dense-panel reference in 110 files`；`PASS: zero text matches in quantlab/ + shells + test.py`
- **Committed in:** 探针未入库（一次性验证脚本，放在会话 scratchpad）；使其成立的代码改动在 `114796f`

**2. [Rule 3 - Blocking] `plan_calendar` 的文本 grep 与删除锁验收标准互斥**

- **Found during:** Task 2
- **Issue:** Task 2 同时要求 `grep -rn plan_calendar ...` 零匹配，和"一个断言 `not hasattr` 的测试"。后者必须写出 `"plan_calendar"` 这个字符串，因此前者永远不可能通过。此外 D-18 要求退休注释点名被退休的测试，又产生两行散文命中。
- **Fix:** 同样换成 AST 工具，但做成全量占位清点（见上文 Decisions）：三处残留全部被归类——1 处删除锁字面量（验收标准本身要求的）、2 处 docstring 散文（D-18 要求的），零处可执行引用。gate 同时断言删除锁字面量**恰好存在一个**，所以锁被误删也会判负。
- **Files modified:** 无（gate 是只读探针）
- **Verification:** `no executable reference; 1 deletion lock, 2 D-18 prose lines`，并逐条列出位置
- **Committed in:** 探针未入库；使其成立的代码改动在 `18fe2f5`

**3. [Rule 1 - Bug] `_period_key` 的 docstring 与尾部注释断言了本任务刚删掉的两-planner 防线**

- **Found during:** Task 1
- **Issue:** `_period_key` 的 docstring 写着 "Both planners group by this and nothing else, which is what makes drift between the sizing windows and the write windows structurally impossible rather than merely unlikely."——删掉 `plan_calendar` 后这句话直接为假，而且它正是 T-03.6-10 点名的"空洞安全声明"。尾部 raise 上方的注释也以"The class docstring's claim that both planners inherit a new granularity..."指向一句已被删除的表述。
- **Fix:** docstring 改为 `_group_by_period()` 单一分组者的表述；注释改为指向重写后的类 docstring 措辞（"a new granularity is added in `GRANULARITIES` plus a branch here"）。计划的 scope fence 说不要动 `_period_key`，但明确保护的是它的**分支**与 `GRANULARITIES`；本次改动仅限散文，五个分支、raise 的文案、返回注解全部一字未动。
- **Files modified:** `quantlab/base/chunking.py`
- **Verification:** `uv run pytest -q`（785 passed）；LEAF AST 探针通过；`plan_from_timestamps` 签名与 HEAD 逐字节相同
- **Committed in:** `114796f`

**4. [Rule 1 - Bug] 漂移测试的 docstring 引用了已删除的类 docstring 措辞**

- **Found during:** Task 2
- **Issue:** `test_a_granularity_period_key_does_not_handle_refuses_instead_of_guessing` 的 docstring 结尾写着"reaching it is what makes the class docstring's \"both planners inherit it\" claim true rather than accidental"。Task 1 删掉了那句 claim，引用悬空。
- **Fix:** 改为指向重写后的措辞——"a new granularity is added in two places" 这条程序由 raise 变为 by construction 成立。
- **Files modified:** `tests/test_chunked_ingest.py`
- **Verification:** `uv run pytest tests/test_chunked_ingest.py -q`（53 passed）
- **Committed in:** `18fe2f5`

---

**Total deviations:** 4 auto-fixed（2 blocking 工具替换，2 bug 文档修正）
**Impact on plan:** 两处 blocking 都是**工具替换而非主张放宽**——替换后的 gate 严格强于计划写的文本形式（AST 版本额外断言删除锁存在，全量清点版本对无法归类的出现判负）。两处 bug 都是本次改动直接造成的陈述失真，改动仅限散文，且都在计划的 write surface（两个文件）之内。计划明令保留的构造——`GRANULARITIES`、`__init__`、`_period_key` 的五个分支与 raise 文案、`_group_by_period`、`ChunkLedger`、`plan_from_timestamps` 的签名与四句契约、模块 docstring 的 LEAF 段落、import 块——全部未动，并逐条验证。`example/` 与 `.planning/` 一字未碰（03.6-04 的范围）。

## Issues Encountered

- 计划里的行号与实际文件偏移约 30 行（`plan_calendar` 实际在 `:154-187` 而非 `:124-157`，类 docstring 在 `:34-51`）——03.6-01 给 `_period_key` 加的尾部 raise 与 docstring 说明推高了后续行号。按计划自己的指示"work from the live grep rather than from the list here"处理，无影响。
- 计划 Task 2 的 `read_first` 要求确认 `tests/test_chunked_panel_estimate.py` 的三处 `plan_calendar` 引用已被 03.6-02 清除——已确认清除（`grep -rn plan_calendar tests/` 只剩本模块的三处）。

## Verification Results

| # | Command | Result |
|---|---------|--------|
| 1 | Precondition: `not hasattr(UniverseCatalog, 'estimate_chunked_panel')` | `ok` |
| 2 | `uv run pytest -q` | **785 passed**，无 failed / errored（与 wave 2 收尾同数：删 2 增 2） |
| 3 | Task 1 gate: 单 planner + docstring 重写 | `single planner; docstring rewritten` |
| 4 | `! grep -n plan_calendar quantlab/base/chunking.py` | PASS，零匹配 |
| 5 | **Relocated 全树 dense-panel gate（AST，110 文件，无排除）** | `no executable dense-panel reference in 110 files` |
| 6 | 同上的纯文本形式，作用于 `quantlab/` + 三 shell + `test.py` | PASS，零匹配（本阶段首次成立） |
| 7 | LEAF import AST 探针（绝对子集断言） | `leaf import surface unchanged` |
| 8 | `plan_from_timestamps` 签名 vs HEAD | 逐字节相同 |
| 9 | Task 1 expected-red gate | `expected_red=['test_plan_calendar_and_plan_from_timestamps_share_one_period_rule', 'test_plan_calendar_covers_the_full_backfill_range']`，无其他红 |
| 10 | `plan_calendar` 全量占位清点 gate | `no executable reference; 1 deletion lock, 2 D-18 prose lines` |
| 11 | `uv run pytest tests/test_chunked_ingest.py -q -k "granularity or window or period or planner or calendar"` | 27 passed（≥5 选中） |
| 12 | 变异：把 `plan_calendar` 加回类上 | 删除锁如期 FAILED |
| 13 | 变异：删掉 `plan_from_timestamps` | 正向臂如期 FAILED |
| 14 | `uv run python -c "... assert not hasattr(T,'plan_calendar') ..."` | `one planner` |

**验证步骤 5（by-eye backstop）：** 重写后的类 docstring 已在本文档上方逐字记录。它按"这个类是什么 → 只有一个 planner → 曾经的第二个为什么消失 → 现在真正要防的漂移是什么、由什么检测"的顺序展开，不是在两-planner 文本上做删改的残留。drift 段落对尾部 raise 的描述与 03.6-01 的实现一致：raise 的文案确实同时点名 `GRANULARITIES` 与 `_period_key`，且确实只能由漂移到达（`__init__` 先拒绝不在 `GRANULARITIES` 里的 token）。

**Spec-less probe fallback — VISIBLY SKIPPED.** 与计划所记一致：phase 03.6 无 `SPEC.md`，`Requirements: N/A`，本计划未生成 probe 派生的 edge/prohibition 谓词。上表全部来自计划自带的 `<verify>` 命令或其工具替换版本。

## Known Stubs

None — 本计划未引入 stub、未跳过任何测试、未留下未运行的 `<verify>`。两处 `<verify>` 命令以更强的工具形式执行并记录（见 Deviations 1 与 2），不是未运行。

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- SC-2 完成并被双向锁钉住；SC-3 的全树主张在本计划首次成立（十三个文件中最后一个已清）。
- **给 03.6-04 的交接事实：**
  - `example/chunking.md` 仍大篇幅记述 `plan_calendar`（本计划的 scope fence 明令不动），是 03.6-04 的工作面。重写后可直接引用本文档"重写后的 drift 段落"一节的两段逐字文本。
  - `.planning/` 下若干文档同样仍点名 `plan_calendar`，同属 03.6-04。
  - `tests/test_chunked_ingest.py` 现有三处 `plan_calendar` 文本出现，**都是刻意保留的**：`:390` 是删除锁的字符串实参（验收标准要求），`:194` 与 `:356` 是 D-18 退休注释。任何后续的"清理"若移除它们，就是移除删除锁本身或移除 D-18 要求的可追溯性。
  - 对这五个 dense-panel 名字与 `plan_calendar` 的负向主张，**不要用文本 grep 断言**——本计划与 03.6-02 都已证明该工具在这里会误报。可执行引用用 AST。
- 无阻塞项。

---
*Phase: 03.6-frequency-keyed-chunking-policy*
*Completed: 2026-09-13*

## Self-Check: PASSED

- Files on disk: `quantlab/base/chunking.py`, `tests/test_chunked_ingest.py` — both present.
- Commits reachable: `114796f`, `18fe2f5` — both present in `git log --all`.
- `commits: 2` measured with `git rev-list --count 492dd62..HEAD`, not narrated; `plan_head_before` recorded from the on-disk plan ledger.
