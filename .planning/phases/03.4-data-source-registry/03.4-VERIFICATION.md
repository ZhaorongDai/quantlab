---
phase: 03.4-data-source-registry
verified: 2026-09-09T16:40:00Z
status: human_needed
score: 6/6 ROADMAP success criteria verified; plan 09/10/11 must-haves verified; 0 gaps
covered_files:
  - ".planning/REQUIREMENTS.md"
  - ".planning/ROADMAP.md"
  - ".planning/STATE.md"
  - ".planning/WINDOWS.md"
  - ".planning/phases/03.4-data-source-registry/03.4-01-PLAN.md"
  - ".planning/phases/03.4-data-source-registry/03.4-01-SUMMARY.md"
  - ".planning/phases/03.4-data-source-registry/03.4-02-PLAN.md"
  - ".planning/phases/03.4-data-source-registry/03.4-02-SUMMARY.md"
  - ".planning/phases/03.4-data-source-registry/03.4-03-PLAN.md"
  - ".planning/phases/03.4-data-source-registry/03.4-03-SUMMARY.md"
  - ".planning/phases/03.4-data-source-registry/03.4-04-PLAN.md"
  - ".planning/phases/03.4-data-source-registry/03.4-04-SUMMARY.md"
  - ".planning/phases/03.4-data-source-registry/03.4-05-PLAN.md"
  - ".planning/phases/03.4-data-source-registry/03.4-05-SUMMARY.md"
  - ".planning/phases/03.4-data-source-registry/03.4-06-PLAN.md"
  - ".planning/phases/03.4-data-source-registry/03.4-06-SUMMARY.md"
  - ".planning/phases/03.4-data-source-registry/03.4-07-PLAN.md"
  - ".planning/phases/03.4-data-source-registry/03.4-07-SUMMARY.md"
  - ".planning/phases/03.4-data-source-registry/03.4-08-PLAN.md"
  - ".planning/phases/03.4-data-source-registry/03.4-08-SUMMARY.md"
  - ".planning/phases/03.4-data-source-registry/03.4-09-PLAN.md"
  - ".planning/phases/03.4-data-source-registry/03.4-09-SUMMARY.md"
  - ".planning/phases/03.4-data-source-registry/03.4-10-PLAN.md"
  - ".planning/phases/03.4-data-source-registry/03.4-10-SUMMARY.md"
  - ".planning/phases/03.4-data-source-registry/03.4-11-PLAN.md"
  - ".planning/phases/03.4-data-source-registry/03.4-11-SUMMARY.md"
  - ".planning/phases/03.4-data-source-registry/03.4-CONTEXT.md"
  - ".planning/phases/03.4-data-source-registry/03.4-DISCUSSION-LOG.md"
  - ".planning/phases/03.4-data-source-registry/03.4-PATTERNS.md"
  - ".planning/phases/03.4-data-source-registry/03.4-RESEARCH.md"
  - ".planning/phases/03.4-data-source-registry/03.4-REVIEW.md"
  - ".planning/phases/03.4-data-source-registry/03.4-VALIDATION.md"
  - ".planning/phases/03.4-data-source-registry/COVERAGE.md"
  - ".planning/phases/03.4-data-source-registry/manifest-sentence-ledger.tsv"
  - ".planning/phases/03.4-data-source-registry/manifest-sentence-retired.tsv"
  - ".planning/phases/03.4-data-source-registry/manifest_sentence_audit.py"
  - ".planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/260909-174-PLAN.md"
  - ".planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/260909-174-SUMMARY.md"
  - ".planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/manifest_semantics_scan.py"
  - ".planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/prose_only_check.py"
  - ".planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/scan-allowlist.txt"
  - "example/acquisition.md"
  - "example/pageledger.md"
  - "example/registry.md"
  - "ingest_alpaca.py"
  - "ingest_tiingo.py"
  - "ingest_us_equity.py"
  - "quantlab/acquisition/alpaca.py"
  - "quantlab/acquisition/inspector.py"
  - "quantlab/acquisition/registry.py"
  - "quantlab/acquisition/tiingo.py"
  - "quantlab/base/acquisition.py"
  - "quantlab/base/chunking.py"
  - "quantlab/base/coverage.py"
  - "quantlab/base/pageledger.py"
  - "quantlab/base/progress.py"
  - "quantlab/utils/atomic.py"
  - "tests/conftest.py"
  - "tests/test_acquisition_progress.py"
  - "tests/test_alpaca_acquisition.py"
  - "tests/test_atomic_sidecars.py"
  - "tests/test_ingest_shells.py"
  - "tests/test_page_ledger.py"
  - "tests/test_source_inspector.py"
  - "tests/test_source_registry.py"
  - "tests/test_tiingo_quota.py"
covered_digest: "v1:sha256:d88f40fc6070e52c469ca6968c26efcc0513b02e1207836ee58df02f260732ed"
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: "6/6 ROADMAP SC; 1 plan must-have FAILED（D-18 文档漂移，第 4 次复发）"
  gaps_closed:
    - "五个站点全部改对，且与 HEAD 代码行为一致、与同文件其它段落自洽：`quantlab/base/coverage.py:260` 与 `quantlab/acquisition/inspector.py:161` 的 docstring 摘要行现为**逐字相同**的「Every symbol `_failures.json` records as failing, accumulated across runs.」；`example/registry.md:253` 表格单元现为「当前已知仍在失败……跨 run 累积，可能含本轮根本没请求过的符号」，与同文件 556 行一致；`quantlab/base/acquisition.py:1899` 与 `tests/test_tiingo_quota.py:293` 的「lie about what the last run did」已换成不预设 run 作用域的「park a never-failing symbol in the durable cross-run failure manifest」。"
    - "prohibition 2（一句陈述不得与它自己的反面并排站着）现在有了**穷举性的**证据，而不再是「我恰好没看到」：`manifest_sentence_audit.py` 枚举出 55 句同时命中 manifest 语义类与 run-scope 语义类的候选句（14 个文件），我逐句读完全部 55 行判读，**没有一行我会判得和账本不同**。"
    - "新 gate 是能红的，不是回执。我在独立 worktree 里做了 13 个 mutation：五个站点各改回退役原文 → 5/5 转红（`UNADJUDICATED` + `VANISHED` 双臂）；翻一行判读 `OK`→`STALE` → 转红；整句删除 → `VANISHED` 转红；注入 7 句全新造的假话 → 4 句转红。**并且历史上它真的在未经破坏的仓库上红过**：`git show a4a5040` 的账本有 5 条 `STALE`，我 checkout 该 commit 实跑 `--check` 得到 `AUDIT RED: 5 STALE` / exit 1，五条正是那五个站点。"
    - "`manifest-sentence-retired.tsv` 的 5 条 `replacement_digest` 全部在账本第 1 列真实存在，且各自指向**同一文件**里改写后的那句。对账链完整（plan-checker 曾把它断裂列为 blocker）。`NOT-A-CANDIDATE` 逃生舱（全表上限 1 行）**一次都没用**。"
    - "`quantlab/` 与 `tests/` 逐字未变行为：acbc7b9→b2a6e2e 之间非 `.planning` 改动只有 4 个 .py + 1 个 .md；我做的「剥 docstring 后 AST 比对」对 4 个 .py 全部 IDENTICAL，`tests/test_acquisition_progress.py` 是**逐字节相同**（本轮根本没动它）。断言条件与断言消息都在 AST 内，AST 相同即两者都未变。全量套件 734 passed / 0 failed。"
    - "IN-04（`inspector.py` 的 “FIRST in-repo reader” 过时序数）与本轮改动的摘要行在同一段 docstring 里，**逐字保留**，`grep -c` = 1。WR-01..WR-05、IN-03 同样未被顺手带走。"
  gaps_remaining: []
  regressions: []
gaps: []
deferred: []
advisory:
  - finding: "AU-01（新）：`manifest_sentence_audit.py` 的 `RUNSCOPE_TERMS` 在中英之间不对称 —— 有中文 `最近`，却没有英文 `most recent` / `recent` / `current run`。我实测出 3 条能整个绕过候选网的新造假话：「The failure manifest describes only the most recent invocation.」「The manifest holds the most recent run and nothing else.」「`_failures.json` describes the current run only.」三句都是 `--check` 绿。"
    category: other
    reason: "**不是 must-have 失败**：plan 10 的 must-have 只要求词表是「语义类」而非「已知坏句子字面量清单」（成立），plan 11 的 must-have #2 更是逐字声明「它证明的不是语义类求交是完备的」。但这个洞恰好落在复发过 4 次的那个语义类的**英文形式**上，而它的中文孪生词已经在表里 —— 修复成本是两行。建议在 `RUNSCOPE_TERMS` 补 `r\"most recent\"`、`r\"current run\"`、`r\"each time\"`，补完后按本阶段自己定的顺序把新产生的候选句判读进账本。留给用户裁决。"
    evidence_status: "reproducible — 见「行为抽查与 mutation」表 I-1..I-3"
  - finding: "AU-02（新）：`manifest_sentence_audit.py` 声明了 `VERDICTS = (\"OK\", \"NA\", \"STALE\")` 却从不校验它。`load_ledger()` 只对 `entry[0] == \"STALE\"` 取真，因此一个拼错的判读（实测把 `OK` 改成 `0K`）**静默通过**，gate 保持绿。这是 fail-open。"
    category: other
    reason: "低概率（要把 `STALE` 拼错成别的东西才会漏），但修复是一行：`if entry[0] not in VERDICTS: 计入 UNADJUDICATED`。留给用户裁决。"
    evidence_status: "reproducible — 见 mutation V-2"
  - finding: "AU-03（记录）：`scoped_files()` 用 `git ls-files`，所以**未被 git 跟踪**的新文件对 gate 不可见（实测：同一段假话放在未跟踪文件里绿，`git add -N` 之后立刻红）。实务上任何要提交的文件都会被跟踪，因此不构成实际缺口，记录以免将来有人误判。"
    category: other
    reason: "设计使然，非缺陷。"
    evidence_status: "reproducible"
  - finding: "WR-01：合并保留的条目没有清理路径；`example/acquisition.md` 的四态表（边车/清单）在「手工 stamp 后仍留有旧条目」或「符号永久离开 roster」时不再互斥"
    category: architectural
    reason: "用户已明确 defer（原样保留承继）。不证伪任何 ROADMAP SC。"
    evidence_status: "none provided"
  - finding: "WR-02：`_write_failure_manifest` 的 WARNING 日志把折回条目播报成「failed and were skipped ... the next run retries them」，两个分句对折回条目都可能为假"
    category: other
    reason: "用户已明确 defer（原样保留承继）。本轮实测该措辞仍在 `quantlab/base/acquisition.py`（`grep -c 'failed and were skipped'` = 1），未被动过。"
    evidence_status: "none provided"
  - finding: "WR-03：清单的 read-modify-write 不是原子的；03.4-08 之后每一条退出路径都走这段 RMW，并发 run 的竞争面被放大"
    category: security
    reason: "用户已明确 defer（原样保留承继）。D-20 把并发交给 console 的任务队列；D-15 的 thin shell 跑在队列之外，D-20 自己也标了 flagged risk。"
    evidence_status: "none provided"
  - finding: "WR-04：`tests/test_acquisition_progress.py` 里 `assert ticker != permanent` 守卫按构造不可能触发，且 `_attempt_batch` 的 `except Exception` 会吞掉 `AssertionError`"
    category: other
    reason: "用户已明确 defer（原样保留承继）。本轮实测该行仍在（`grep -c` = 1），文件逐字节未动。"
    evidence_status: "none provided"
  - finding: "WR-05：合并/写入外层没有 try/finally；`wait_for_quota` 的 `_sleep` 期间 Ctrl-C 会丢掉本轮的失败记录"
    category: other
    reason: "用户已明确 defer（原样保留承继）。不证伪 SC-5：可续跑性由逐批原子写的 watermark 边车驱动。"
    evidence_status: "none provided"
  - finding: "IN-03（inspector 重复构造 ledger）／IN-04（`inspector.py:175` 的 “FIRST in-repo reader” 过时序数）"
    category: other
    reason: "用户已明确 defer（原样保留承继）。本轮特别核实 IN-04 **没有**被顺手带走：它与新写的摘要行在同一段 docstring 内，`grep -c 'FIRST in-repo reader' quantlab/acquisition/inspector.py` = 1。"
    evidence_status: "none provided"
  - finding: "`.planning/ROADMAP.md` 里的 `TBD` 占位（未来阶段 03.3、04..07 的 Requirements / Plans）"
    category: other
    reason: "预先存在的 roadmap 规划占位符，不是本阶段代码里的债务标记；前两版验证同样未列为 blocker。本轮改动的 8 个文件上 `TBD|FIXME|XXX` 全部零命中。"
    evidence_status: "none provided"
behavior_unverified_items: []
coincidental_reliance_items: []
human_verification:
  - test: "对真实 Tiingo / Alpaca 跑一次实际回填，观察配额、404 与进度行为"
    expected: "批次边界取消、配额中止、失败记录的行为与 mock 套件断言的一致"
    why_human: "本阶段每一个 acquisition 测试都跑在 FakeTiingoClient 或 socket tripwire 之上；这套测试从未发出过一次真实厂商请求。承继自上一版，原样保留。"
  - test: "对真实 store 执行 `ingest_us_equity.py --stamp-legacy-watermarks <date>`"
    expected: "watermark 被打上；随后的 run 跳过这些符号"
    why_human: "WINDOWS.md #11 仍为 open。写入路径在仓库内未被驱动过，仍是 quick task 260906-26o 传下来的阻塞性人工检查点。承继自上一版，原样保留。"
  - test: "用一个解析结果为空的 roster 调用 shell"
    expected: "在构造任何 acquisition 之前 `parser.error` 拒绝"
    why_human: "WINDOWS.md #10 仍为 open。分支存在且顺序正确（`ingest_us_equity.py:470`，在唯一那个 `run(SOURCE, ...)` 之前），但没有测试驱动它。承继自上一版，原样保留。"
---

# Phase 03.4: Data Source Registry（运维面基座）验证报告 — 第四次验证（第三次复验）

**阶段目标：** 每一个 quantlab 能获取的数据源都由**一个**已注册的描述符描述，使得 quantlab
自己的代码和仓库外的运维控制台从同一份定义枚举同一批数据源 —— 而加一个厂商是注册一个描述符，
不是改五个调用点。

**验证时间：** 2026-09-09T16:40:00Z
**HEAD：** `b2a6e2e`
**状态：** human_needed（gap 全部关闭；仅剩 3 项承继的人工检查点）
**复验：** 是 —— 覆写 2026-09-09T14:24:48Z（`6342a7c`）那一版

---

## 一段话结论

**这一轮的 gap 关掉了，而且是用一种前三轮没用过的方式关的。**
五个站点全部改对、与代码对得上、与同文件其它段落自洽；`quantlab/` 与 `tests/` 逐字未变
（AST 剥 docstring 后 4 个文件全部 IDENTICAL，`test_acquisition_progress.py` 逐字节相同）。

**新交付的 `manifest_sentence_audit.py` 是一条真能红的 gate，不是精致的回执 —— 这一条我用
13 个 mutation 独立证过，而且它在提交历史上真的红过。** 它的三条判定臂（`STALE` /
`UNADJUDICATED` / `VANISHED`）我各自打中；把五个站点分别改回退役原文，5/5 转红；把整句
删掉也转红（「让发现消失」不是廉价的绿）。最有说服力的一条不是我造的：`git checkout a4a5040`
（改写**之前**的那个 commit）实跑 `--check`，输出 `AUDIT RED: 5 STALE` / exit 1，五条 STALE
正是那五个站点。**这条 gate 在未经人为破坏的真实仓库文本上红过，然后被改绿。**

**判读账本是人读的产物 —— 这一点我用「我自己也读一遍」来替代无法验证的provenance声明。**
账本没有理由列，所以「是不是逐句人读的」在artefact层面本就不可判定。我改为验证它**判得对不对**：
55 行我逐句读完，其中 4 条 `NA` 是一个脚本很难做出的区分（一条是 ROADMAP 里描述本 gap 本身的
计划勾选行；一条是被切句规则揉在一起的 f-string 日志块；一条是 Alpaca 测试里讲「真 secret 可能被
写进日志/清单/CI 记录」的注释；一条讲的是 **PageLedger** 的 manifest，另一个对象）。
**没有一行我会判得和账本不同。**

**唯一值得记的新发现是词表的中英不对称（AU-01）。** `RUNSCOPE_TERMS` 里有中文 `最近`，
却没有英文 `most recent` / `current run`。我实测出 3 句能整个绕过候选网的新造假话，
其中「The failure manifest describes only the most recent invocation.」正是复发四次的那个
语义类的英文形式。**但这不构成 must-have 失败** —— plan 10 只要求词表是语义类（成立），
plan 11 更是逐字声明「本计划证明的不是语义类求交是完备的」。所以我把它记成 advisory 而不是
gap：修复是两行，但要不要修由用户决定。

---

## 目标达成情况

### 可观察真相（ROADMAP Success Criteria）

| # | 真相 | 状态 | 本次独立复验的证据 |
|---|------|------|------|
| SC-1 | 每个可获取的数据源都能从**一个**登记表枚举出来，调用点不出现厂商名；每个描述符携带能力、凭证环境变量**名**、采集类 | ✓ VERIFIED | 冷 import `quantlab.acquisition.registry` 实测枚举出 `alpaca`（4 caps，`('APCA_API_KEY_ID','APCA_API_SECRET_KEY')`）与 `tiingo`（1 cap，`('TIINGO_API_KEY',)`）；`acquisition_cls` 是类对象（`AlpacaAcquisition` / `TiingoAcquisition`），`required_env` 是名字元组；`sys.modules` 里两个 vendor 模块都被拉起。三个 shell 里 `TiingoAcquisition|AlpacaAcquisition` 只命中 2 条注释，零代码引用 |
| SC-2 | 枚举数据源与读取凭证状态永不返回凭证**值**，也不要求凭证存在 | ✓ VERIFIED | 植入三个哨兵值后，把两个描述符的 `repr`/`str`/`asdict`/`required_env`/每个 `Capability` 的 `repr`/`str` 全部拼串搜索 → `LEAKS: []` |
| SC-3 | 读侧面在**无凭证**机器上回答清单、覆盖、失败原因与行级浏览，且发出**零**厂商请求 | ✓ VERIFIED | `env -u TIINGO_API_KEY -u APCA_API_KEY_ID -u APCA_API_SECRET_KEY uv run python ingest_us_equity.py --dry-run` 实跑出真实覆盖报告（13729 符号、11 chunk、`would fetch: 13729/13729`）；单独 import `inspector` 后 `sys.modules` 里 vendor/net 模块为 `[]` |
| SC-4 | 覆盖判定由真实采集跑用的**同一份代码**计算，不是第二份实现 | ✓ VERIFIED | 全仓一处定义 `quantlab/base/coverage.py:502`，两处委托（`inspector.py:148`、`acquisition.py:1813`） |
| SC-5 | 调用方能以编程方式启动采集、观察结构化进度、在批次边界取消并留下可续跑的 store 与 watermark、拿到列明成败的结果对象 | ✓ VERIFIED（行为性） | 这是一条 cancellation/ordering 不变量，不能靠符号存在判定。单跑命名测试：`test_cancel_leaves_a_resumable_store`、`test_result_and_manifest_agree`、`test_the_manifest_survives_a_quota_abort_on_the_default_path`、`test_a_disjoint_rerun_does_not_inherit_earlier_failures`、`test_the_quota_condition_never_lands_in_the_failure_manifest` 全绿。相关代码自上一版 AST 未变（见下节），上一版的 M1/M2 mutation 结论承继 |
| SC-6 | 仓库内的 ingest 入口经由登记表到达数据源，使登记表在本仓库有消费者 | ✓ VERIFIED | 三个 shell 各 1 处 `DataSourceRegistry.get(...)`（`ingest_tiingo.py:58`、`ingest_alpaca.py:141`、`ingest_us_equity.py:130`）+ 1 处 `run(SOURCE, ...)`（`:182` / `:378` / `:528`） |

**得分：6/6 ROADMAP 成功标准通过**（0 个 present-but-behavior-unverified，0 个 override）。

**阶段级 plan must-have：** 上一版唯一 FAILED 的 plan 09 must-have 7 + prohibition 2 现已通过
（见下两节）。plan 10 与 plan 11 的 must-have 逐条核实结果见「新 gate 的逐条核实」。

---

## 1. 五个站点是否真的与 HEAD 代码一致，且与同文件其它段落自洽

| # | 站点 | HEAD 上的表述 | 与代码是否一致 | 同文件自洽性 |
|---|---|---|---|---|
| 1 | `quantlab/base/coverage.py:260` `read_failure_manifest` 摘要行 | 「Every symbol `_failures.json` records as failing, accumulated across runs.」+「A CROSS-RUN record rather than a report on the most recent run: the manifest can name symbols no recent run requested at all, because the writer folds unattempted entries forward before every overwrite.」 | ✓ 方法就是把盘上文件原样读回；文件由 `_merge_unattempted_failures` → `_write_failure_manifest` 写出，跨 run 累积 | ✓ 与同段 5 行后的「folds the previous run's entries for symbols THIS run never attempted back in」**同向**，不再互为反面 |
| 2 | `quantlab/acquisition/inspector.py:161` `SourceInspector.failures` 摘要行 | **与 1 逐字相同**的摘要行；下文「Because the manifest accumulates across runs, the console can be shown a symbol the most recent run never requested」，并点名 `test_the_manifest_survives_a_quota_abort_on_the_default_path` | ✓ 我读了该测试第 1028 行附近：`reported = SourceInspector().failures(second_config)` 后断言 `reported.get(permanent) == recorded_reason`，而上方断言 `mock_tiingo_client.calls` 从没请求过 `permanent`。**docstring 点名的可执行证据是真的，且单跑绿** | ✓ 摘要行与 1 逐字一致，docstring 自己也把这条一致性写成了要求（「kept verbatim in sync」） |
| 3 | `example/registry.md:253` API 表格 | 「`_failures.json` 里**当前已知仍在失败**的全部符号（跨 run 累积，可能含本轮根本没请求过的符号），`{symbol: 原因}`」 | ✓ 同上 | ✓ 与同文件 556 行「`_failures.json` 是跨 run 累积的运维记录，可能还留着别的 run 遗留、本轮根本没请求过的条目」**同向** |
| 4 | `quantlab/base/acquisition.py:1899` `_attempt_batch` quota 分支 | 「…would defame a perfectly good symbol and park a never-failing symbol in the durable cross-run failure manifest, where the fold-forward keeps it alive until someone clears it by hand.」 | ✓ 实质主张不变且更准（配额确实被排除在清单外，`test_the_quota_condition_never_lands_in_the_failure_manifest` 绿），措辞不再预设「最近一次 run 的报告」 | ✓ 与同文件 `_write_failure_manifest` / `_merge_unattempted_failures` 的 docstring 同向 |
| 5 | `tests/test_tiingo_quota.py:293` 测试 docstring | 同 4 的措辞 | ✓ | ✓ |

两条摘要行**逐字节相同**（`grep -c "records as failing, accumulated across runs"` 两文件各 1），
这正是它们自己声明要维持的性质。

---

## 2. `manifest_sentence_audit.py` 是能红的检查，还是精致的回执

结论：**是能红的检查。** 我在 `git worktree` 里做了 13 个 mutation，每次用 `git checkout --` 确定性恢复。

### 2a. `--check` 在当前 HEAD 上退出 0，且账本里每一句都有判读

```
$ uv run python .planning/phases/03.4-data-source-registry/manifest_sentence_audit.py --check
AUDIT GREEN: 55 candidates, 55 adjudicated, 0 STALE
EXIT=0
```

`0 UNADJUDICATED` + `0 VANISHED` 同时成立，意味着**候选集与账本 digest 集合完全相等** ——
既没有未判读的句子，也没有指向已消失句子的僵尸行。账本 55 行、全部 4 列、无缺列。

### 2b. 我自己做的 mutation（不看 SUMMARY 的记录）

| # | Mutation | 结果 |
|---|---|---|
| 基线 | 无（fresh worktree at `b2a6e2e`） | `AUDIT GREEN: 55 candidates, 55 adjudicated, 0 STALE` / exit 0 |
| **H-1** | **`git checkout a4a5040`（改写之前的真实提交），不做任何人为破坏** | **`AUDIT RED: 5 STALE, 0 UNADJUDICATED, 0 VANISHED` / exit 1，五条 STALE 正是那五个站点** |
| M-1 | `coverage.py` 摘要行改回 “The last run's …” | **RED** — `UNADJUDICATED ad6635a64b4e` + `VANISHED 4e7254d4bb5a` |
| M-2 | `inspector.py` 摘要行改回 | **RED** — 同双臂 |
| M-3 | `registry.md` 表格单元改回「上一次 run 的」 | **RED** — 同双臂 |
| M-4 | `acquisition.py` 改回 “lie about what the last run did” | **RED** — 同双臂 |
| M-5 | `test_tiingo_quota.py` 改回 | **RED** — 同双臂 |
| V-1 | 把账本一行判读从 `OK` 翻成 `STALE` | **RED** — `STALE 4e7254d4bb5a`（第三条判定臂） |
| **V-2** | **把同一行判读拼错成 `0K`** | **GREEN（fail-open）** — 见 advisory AU-02 |
| D-1 | 把 `coverage.py` 的候选句整句删掉换成「Returns a mapping.」 | **RED** — `VANISHED`（「让发现消失」不是廉价的绿） |
| I-1..I-7 | 在 `quantlab/acquisition/registry.py` 注入 7 句全新造的假话 | **4 红 3 绿** — 见 AU-01 |
| S-1 | 把切句规则里半角 `.` 的「后随空白」限定拿掉 | 候选集 55→39 句，`registry.md` **归零**，`AUDIT RED: 21 UNADJUDICATED, 37 VANISHED` |
| T-1 | 把同一句假话放进**未跟踪**新文件 / `git add -N` 后 | 未跟踪：**GREEN**；跟踪后：**RED** — 见 AU-03 |

三条判定臂（`STALE` / `UNADJUDICATED` / `VANISHED`）我全部独立打中。

### 2c. 判读账本是逐句人读还是脚本推断

**artefact 层面不可判定** —— 账本只有 `digest / verdict / path / sentence` 四列，没有理由列，
所以「人读」这个 provenance 声明本身没有物证。我改为验证**判得对不对**，逐句读完全部 55 行：

- **51 条 `OK`：我逐条核对过，没有一条我会判成 `STALE`。** 其中包括几条容易误判的：
  `example/acquisition.md` 的「`_failures.json` 是跨 run 累积的运维记录，每次 run 覆盖重写」——
  两个分句都对（先折回、再整文件覆写）；`acquisition.py` 的「`_write_failure_manifest`
  OVERWRITES, and `_run`'s `failures` starts empty on every call」—— 这是在讲机制、不是在
  声称清单只含本轮。
- **4 条 `NA` 是脚本很难做出的区分**，这一点反而是「有人读过」的正面证据：
  `.planning/ROADMAP.md` 里描述本 gap 本身的计划勾选行；`acquisition.py` 里被切句规则揉成
  一坨的 f-string 日志块；`test_alpaca_acquisition.py` 里讲「真 secret 可能被写进日志/清单/
  CI 记录」的注释；`test_page_ledger.py` 里讲 **PageLedger** 的 manifest（另一个对象）。
- 账本演化也与「先判读、后改写」的顺序一致：`a4a5040` 53 行（3 NA / 45 OK / **5 STALE**）
  → `6890d53` 55 行（3 NA / 52 OK / 0 STALE）→ `f00f648` 55 行（4 NA / 51 OK）。
  最后那次 OK→NA 是因为 ROADMAP 的 `- [ ]` 变成 `- [x]`，digest 变了、被重新判读。

**结论：账本内容正确。**「是不是人读的」无法从 artefact 判定，但**如果它是脚本推断的，
那 4 条 `NA` 就推不出来**；而且无论产生方式如何，我的独立复读没有发现任何一条错判。

### 2d. `NOT-A-CANDIDATE` 逃生舱是否被滥用

**一次都没用。** `cut -f4 manifest-sentence-retired.tsv | tail -n +2 | grep -c '^NOT-A-CANDIDATE$'`
为 `0`（计划上限 1）。五条退役行的 `replacement_digest` 全部填了真实 digest。
账本侧的 `NA` 判读 4 条，**没有一条落在这五个站点上**。

### 2e. `replacement_digest` 对账链

五条全部在账本第 1 列真实存在，且各自指向**同一文件**里改写后的句子：

| retired 站点 | replacement_digest | 账本里对应的行 |
|---|---|---|
| `quantlab/base/coverage.py` | `4e7254d4bb5a` | ✓ `OK  quantlab/base/coverage.py  … Every symbol \`_failures.json\` records as failing, accumulated across runs.` |
| `quantlab/acquisition/inspector.py` | `7813d82ff088` | ✓ `OK  quantlab/acquisition/inspector.py  …（同一句）` |
| `example/registry.md` | `1657182a79f0` | ✓ `OK  example/registry.md  \`_failures.json\` 里**当前已知仍在失败**…` |
| `quantlab/base/acquisition.py` | `d81f1f5fa1ec` | ✓ `OK  quantlab/base/acquisition.py  … park a never-failing symbol in the durable cross-run failure manifest …` |
| `tests/test_tiingo_quota.py` | `53eb522cf07f` | ✓ `OK  tests/test_tiingo_quota.py  …（同上措辞）` |

对账链完整。（`--check` 的 `0 VANISHED` 已经蕴含「账本每个 digest 都对应一句活着的候选句」，
所以这五个 digest 不可能是伪造的。）

---

## 3. 切句规则是否稳，以及那五处的候选身份押在哪

计划规定：全角 `。！？；` 无条件断句；半角 `.!?;` **仅当**后随空白或串尾。
脚本 docstring 说明了理由：否则 `` `_failures.json` `` 会被自己的点切成两半，
`_failures\.json`（`MANIFEST_TERMS` 里最重要的一条）就到处都匹配不上。

**这条限定确实是承重的（S-1 实测）：** 把它拿掉后候选集 55→39 句，`example/registry.md`
**归零**，`--check` 变成 `21 UNADJUDICATED / 37 VANISHED`。

**但 executor 报告里那句归因不完全准确。** 我逐句枚举了五处的实际命中词：

| 站点 | 命中的 MANIFEST 类词 | 命中的 RUNSCOPE 类词 | 是否 100% 押在半角限定上 |
|---|---|---|---|
| `coverage.py` | `_failures\.json`、`manifest`、`read_failure_manifest`（**三个**） | `accumulat` | **否** —— 方法名 `read_failure_manifest` 本身就带 `manifest` |
| `inspector.py` | `_failures\.json`（**唯一**） | `accumulat` | **是** |
| `example/registry.md` | `_failures\.json`（**唯一**） | `本轮`、`跨 run`、`累积` | **是** |
| `acquisition.py` | `failure manifest`、`manifest` | `cross-run`、`durable` | 否 |
| `test_tiingo_quota.py` | `failure manifest`、`manifest` | `cross-run`、`durable` | 否 |

即：押在半角限定上的是 **`inspector.py` + `example/registry.md`**，不是 executor 说的
`coverage.py` + `example/registry.md`。**实质结论不变**（半角限定确实承重，且这两处确实
100% 依赖它），只是站点归属报错了一个。这是 SUMMARY 里的一处事实误述，不影响交付物本身。

---

## 4. `quantlab/` 行为是否逐字未变

`acbc7b9`（上一版验证的 HEAD）→ `b2a6e2e`（现 HEAD）之间，非 `.planning` 的改动全部落在
一个 commit（`6890d53`）里，共 5 个文件、31 insertions / 8 deletions。

| 文件 | 剥掉 docstring 后的 AST | 结论 |
|---|---|---|
| `quantlab/acquisition/inspector.py` | **IDENTICAL** | 纯 docstring |
| `quantlab/base/acquisition.py` | **IDENTICAL** | 纯 docstring |
| `quantlab/base/coverage.py` | **IDENTICAL** | 纯 docstring |
| `tests/test_tiingo_quota.py` | **IDENTICAL** | 纯 docstring |
| **`tests/test_acquisition_progress.py`** | **BYTE-IDENTICAL（本轮根本没动）** | 断言条件与断言消息都逐字未变 |

**关于「断言条件是否被弱化」：** 上一轮（`e1ef28b`）动过这个文件的两条断言**消息字符串**，
所以这一轮我把它列为第一优先项。结果是**它一个字节都没动** —— 比「条件未变」更强的结论。
另外，断言的 `msg` 节点本身就在 AST 里，所以对其余 4 个文件「剥 docstring 后 AST 相同」
同时蕴含了**断言条件与断言消息都未变**。

全量套件：`uv run pytest -q` → **734 passed, 0 failed**（48.50s），与交给我的基线一致。

已 defer 项的原样保留（逐条实测）：

| 条目 | 检查 | 结果 |
|---|---|---|
| IN-04 | `grep -c 'FIRST in-repo reader' quantlab/acquisition/inspector.py` | **1**（`:175`，就在新摘要行下方 3 行，未被带走） |
| WR-02 | `grep -c 'failed and were skipped' quantlab/base/acquisition.py` | 1（未动） |
| WR-04 | `grep -c 'ticker != permanent' tests/test_acquisition_progress.py` | 1（未动） |

---

## 5. executor 自报的三处偏差 —— 核实结果

### 偏差 1：`03.4-11` Task 1 的「工作树干净」门槛改为按路径求交（Rule 3 deviation）

**判定：正当，且没有削弱 mutation 的可信度。**

- 事实成立：`cal.py`、`pyproject.toml`、`test.py`、`uv.lock` 在本阶段开工前就已修改
  （会话起始的 `git status` 快照即为这四个），逐字执行「`git status --porcelain` 为空」
  会让该验收**永远为假**，与它想守的性质（mutation 有没有残留）无关。
- 替代形式守的是**同一个性质**：对三条 mutation 路径求 `git status --porcelain -- <paths>`
  要求为空，外加三轮前后各跑一次全树 status 确认那 4 个无关文件的集合逐字不变、没有第 5 个冒出来。
- **更重要的是：这个偏差对我的结论完全不构成依赖。** 我没有复用 executor 的 mutation 记录，
  而是在一棵**全新的 `git worktree`**（detached at `b2a6e2e`）里从头做了 13 个 mutation，
  每次用 `git checkout -- .` 恢复，最后 `git worktree remove --force`。主工作树在我全部
  探测结束后仍然只有那 4 个无关文件（已实测）。

### 偏差 2：`scan-allowlist.txt` 重新生成后逐字节相同、git 无 diff

**判定：解释成立，且我用两条独立证据排除了「无 diff = 没做」与「无 diff = 蒙对了」。**

- **证据 A（无新命中确实是因为树已清干净）：** 我在 HEAD 上实跑扩到 16 条 pattern 的
  `manifest_semantics_scan.py`，输出**恰好是那 7 行**，双向 `grep -Fxv` diff 两个方向都为空，
  与 allowlist `sort` 后 `diff` 报 IDENTICAL。P11..P15 在 HEAD 上零命中，因为
  `03.4-10` 已把对应措辞从树上清掉，而退役原文只活在被 `.planning/phases/` 排除的
  `manifest-sentence-retired.tsv` 里。
- **证据 B（新 pattern 不是死正则）：** 我把五条退役原文全部粘回 `example/registry.md`，
  扫描立刻多出 `example/registry.md|P11|1` 到 `|P15|1` **五行**，双向 diff 转红。
  **五条新 pattern 条条是活的。**
- provenance 比例也对得上 plan 11 的 must-have：`grep -c '# derived:'` = **3**（P11..P13，
  逐条来自 retired.tsv），`grep -c '# named-by:'` = **2**（P14..P15，由上一版 VERIFICATION
  的 `missing` 第 4 条逐字点名）。

### 偏差 3：`03.4-11` 的 STATE.md 新条目未产生新候选句

**判定：核实成立。** 我把 `f00f648..b2a6e2e` 的 STATE.md 新增行喂给 `segment()` 与两组词表：
新增的那条决策条目切出 3 句命中 `manifest` / `失败清单`，但**没有任何一句命中 RUNSCOPE 类词**，
因此 `is_candidate()` 全为 False。账本在 `f00f648 → b2a6e2e` 之间 diff 为空，与之吻合。

**另外我独立复现了 plan 11 的「窄/宽对照」，而且用的是我自己造的句子**（不是计划挑的那句）：
把 `The manifest is a per-run report.` 注入 `quantlab/acquisition/registry.py` 后 ——
字面量扫描双向 diff **GREEN**，句子级审计 `--check` **RED (exit 1)**。同一棵树、两条命令、两个退出码。

---

## 6. 新 gate 的逐条 must-have 核实（plan 10 / plan 11）

| 计划 | must-have | 状态 | 证据 |
|---|---|---|---|
| 10 | `--report` 对语义类求交的每个句子输出一行，匹配语义类而非已知坏句子字面量 | ✓ | `MANIFEST_TERMS`(8) × `RUNSCOPE_TERMS`(27) 均为语义类词；HEAD 上 55 句 / 14 文件 |
| 10 | 候选集每一句在账本有判读，verdict ∈ {OK, NA, STALE}，无未判读剩余 | ✓ | 55/55；`0 UNADJUDICATED`；实测 55 行全部 4 列、verdict 只有 OK/NA |
| 10 | `--check` 在**未经修改的 HEAD** 上退出非零并打印命名 coverage.py / inspector.py 的 STALE 行 | ✓ | `git checkout a4a5040` 实跑：`AUDIT RED: 5 STALE` / exit 1，其中含这两行 |
| 10 | 五个站点改写、自洽 | ✓ | 见第 1 节 |
| 10 | 每处替换文本仍在候选集内、各有一行 `OK` | ✓ | 五个 digest 全部在账本、全部 `OK`（第 2e 节） |
| 10 | 候选集只增不减：Task 2 之后计数不低于 Task 1，五个路径全部仍在 `--report` 里 | ✓ | 53 → 55；per-file：coverage.py 3、inspector.py 3、registry.md 3、acquisition.py 11、test_tiingo_quota.py 1 |
| 10 | retired.tsv 每行 `replacement_digest` 指向账本真实行，或逐字 `NOT-A-CANDIDATE`（全表 ≤1） | ✓ | 5/5 指向真实行；`NOT-A-CANDIDATE` 出现 **0** 次 |
| 10 | 代码行为逐字不变；734 passed | ✓ | 第 4 节 |
| 10 | prohibition：不得从已知坏站点清单倒推词表 | ✓ RESOLVED | 提交历史可见顺序：`a4a5040`（枚举+判读，含 5 STALE）→ `6890d53`（改写）→ `cbb884e`（**才**扩词表，且逐条注明 provenance） |
| 10 | prohibition：一句陈述不得与它自己的反面并排 | ✓ RESOLVED | 55 句候选我逐句读完，无一处正反并排；`coverage.py` 与 `registry.md` 的两组「原并排点」现已同向 |
| 10 | prohibition：不得弱化 `tests/` 断言条件 | ✓ RESOLVED | `test_acquisition_progress.py` 逐字节未动；`test_tiingo_quota.py` AST-minus-docstring IDENTICAL |
| 10 | prohibition：不得有行为变更 | ✓ RESOLVED | 4 个 .py AST IDENTICAL；734 passed |
| 10 | prohibition：不得顺手修 WR-01..05 / IN-03 / IN-04 | ✓ RESOLVED | 逐条 grep 实测保留（第 4 节表） |
| 11 | `--check` 是**证明过能红**的命令，三条判定臂各被一个 mutation 打中 | ✓ | 我独立打中三臂（M-1..M-5 / I-* / V-1）；另有 D-1 打中 `VANISHED` 单臂 |
| 11 | 窄/宽对照在一个真实样本上成立，且 SUMMARY 写明句子是被选中的、命中哪条词 | ✓ | 我用**自己造的**句子复现了对照；SUMMARY 确实标注了自选性与命中词 |
| 11 | PATTERNS 11→16，P11..P13 派生自 retired.tsv，P14..P15 由 VERIFICATION 逐字点名，docstring 写明来源 | ✓ | 16 条；`# derived:` 3 / `# named-by:` 2；docstring 明写 P00..P10 是抄来的且这本身是缺陷 |
| 11 | `manifest_semantics_scan.py` 的 docstring 写明它**从属**于句子级审计 | ✓ | 「The rule from here on: the only legitimate input to this list is `manifest-sentence-retired.tsv`」 |
| 11 | allowlist 重新生成、双向 diff 绿 | ✓（无净变化，已核实原因） | 见偏差 2 |
| 11 | 扩词表之后字面量扫描仍能红 | ✓ | 粘回退役原文 → P11..P15 **五行**全部转红 |
| 11 | 顺序被写进 `.planning/STATE.md` | ✓ | Phase 03.4 决策段新增一条，含 `manifest-sentence-retired.tsv` 与 `manifest_sentence_audit.py` 路径 |
| 11 | 写完 STATE.md 之后 `--check` 仍退出 0 | ✓ | HEAD 上 exit 0；新增文本零候选句（偏差 3） |
| 11 | prohibition：新 pattern 只有 (D)/(V) 两类来源且逐条注明 | ✓ RESOLVED | 5 条新 pattern 全部带行尾 provenance 注释 |
| 11 | prohibition：allowlist 不得当橡皮图章 | ✓ RESOLVED | 7 行内容与扩词表前完全相同，且我实测这 7 行确实是 HEAD 上的真实命中（非塞进去让 gate 变绿的） |
| 11 | prohibition：mutation 后用确定性手段恢复 | ✓ RESOLVED | 我自己的 13 个 mutation 全部 `git checkout --` 恢复；主工作树探测前后一致 |
| 11 | prohibition：`quantlab/` 与 `tests/` 一个字节不改 | ✓ RESOLVED | `cbb884e..b2a6e2e` 非 `.planning` 改动为空 |

---

## 需求追溯（D-01..D-20）

十一份 PLAN 的 `requirements:` 并集覆盖 **D-01..D-20 全部 20 条，无遗漏、无孤儿**
（`03.4-10` 与 `03.4-11` 均声明 `[D-18]`）。`.planning/REQUIREMENTS.md` 中
`grep -cE '^\*\*REQ-'` = **0**，也没有任何条目映射到 Phase 03.4 —— 本阶段需求面即
`03.4-CONTEXT.md` 的 D-01..D-20，**无孤儿需求**。

| 需求 | 状态 | 本次证据 |
|---|---|---|
| D-01..D-17、D-19、D-20 | ✓ SATISFIED（承继） | 相关代码自上一版 AST 未变（本轮为纯 docstring 改动），SC-1..SC-6 探针全部重跑通过。D-20 附 WR-03 advisory |
| **D-18** 结果对象 + 失败清单 | **✓ SATISFIED（代码 + 文档）** | 代码：`manifest = dict(failures)` 与 `AcquisitionResult(failures=dict(failures))` 是两个独立值，merge 无条件覆盖五条出口（上一版 AST 证 + M1/M2 mutation 证，本轮代码未变）。文档：五个站点改对（第 1 节），55 句候选全部判读为非 STALE 且我独立复读一致（第 2c 节）。**这是上一版唯一的 gap，现已关闭** |

---

## 关键连接验证

| From | To | Via | 状态 | 细节 |
|---|---|---|---|---|
| 三个 ingest shell | `quantlab/acquisition/registry.py` | `DataSourceRegistry.get()` + `run(SOURCE, ...)` | ✓ WIRED | 各 1 处 get、1 处 run |
| `_run` 的每一条退出路径 | `_merge_unattempted_failures` | 循环外、写盘前的唯一一次无条件调用 | ✓ WIRED | 承继上一版 AST 证据（`_run.body[12]`，零嵌套，紧邻 `body[13]` 的写盘）；本轮代码未变 |
| 盘上的 `_failures.json` | `SourceInspector.failures` | `CoverageLedger.read_failure_manifest` | ✓ WIRED **且文档已对齐** | 上一版此行标注「代码 WIRED / 文档失真」；本轮两个读者的摘要行逐字相同且与代码一致 |
| `SourceInspector.coverage` + `Acquisition._run` | `CoverageLedger.partition_by_coverage` | 同一函数对象 | ✓ WIRED | 一处定义（`coverage.py:502`）、两处委托 |
| `manifest-sentence-retired.tsv` | `manifest-sentence-ledger.tsv` | `replacement_digest` → 账本第 1 列 | ✓ WIRED | 5/5 解析成功且同文件（第 2e 节） |
| `manifest-sentence-retired.tsv` | `manifest_semantics_scan.py` P11..P13 | 逐条派生 + 行尾 provenance 注释 | ✓ WIRED | `# derived:` 3 条 |
| `quantlab/`、`example/`、`tests/`、ROADMAP、STATE 的全部句子 | `manifest-sentence-ledger.tsv` | `manifest_sentence_audit.py` 语义类求交 | ✓ WIRED（有已知洞，见 AU-01） | 55 候选句 / 14 文件，双向对账（`0 UNADJUDICATED` + `0 VANISHED`） |

---

## 数据流追踪（Level 4）

| 产物 | 数据变量 | 来源 | 产出真实数据 | 状态 |
|---|---|---|---|---|
| `ingest_us_equity.py --dry-run` 覆盖报告 | `already covered / would fetch` | `CoverageLedger.partition_by_coverage` 读真实 watermark 边车树 | 是（本次无凭证实跑 13729/13729） | ✓ FLOWING |
| `SourceInspector.failures()` | 清单 dict | `CoverageLedger.read_failure_manifest` 读盘 | 是（命名测试经检视器从盘上再读一次并断言，绿） | ✓ FLOWING（docstring 现已如实描述） |
| `DataSourceRegistry.all()` | 描述符元组 | `@register_source` + registry 底部 vendor import | 是（冷 import 实测 2 vendor、4+1 caps） | ✓ FLOWING |
| `AcquisitionResult.failures` | 本轮 `failures` dict | `_run_once` 逐 pass 累积，**不含**盘上折回条目 | 是（`disjoint_rerun` 断言 `== {}`，绿） | ✓ FLOWING |
| `manifest_sentence_audit.py --report` | 候选句列表 | `git ls-files` → 真实文件内容切句 → 语义类求交 | 是（55 句，改一句就变） | ✓ FLOWING |

---

## 行为抽查与 mutation 验证（汇总）

| # | 检查 | 结果 |
|---|---|---|
| 基线 | `uv run pytest -q` | **734 passed, 0 failed**（48.50s） |
| 定向 | 5 条命名测试单跑 | 全绿（cancel_leaves_a_resumable_store / result_and_manifest_agree / manifest_survives_a_quota_abort / disjoint_rerun / quota_never_lands） |
| 探针 | 三哨兵凭证 × 描述符全部对外表面 | `LEAKS: []`（SC-2） |
| 探针 | 清空三凭证跑 `--dry-run` | 真实覆盖报告（SC-3） |
| 探针 | 冷 import registry / 单独 import inspector | 2 vendor / 无 vendor 模块（SC-1、D-07、D-08） |
| AST | 剥 docstring 比对 5 个文件 | 4 IDENTICAL + 1 BYTE-IDENTICAL（第 4 节） |
| H-1 | `--check` @ `a4a5040`（真实历史，未破坏） | **AUDIT RED: 5 STALE / exit 1** |
| M-1..M-5 | 五站点各改回退役原文 | **5/5 RED**（`UNADJUDICATED` + `VANISHED`） |
| V-1 | 判读 `OK` → `STALE` | **RED** |
| V-2 | 判读 `OK` → `0K`（拼错） | **GREEN** ← fail-open，AU-02 |
| D-1 | 整句删除 | **RED**（`VANISHED`） |
| I-1 | 注入「The failure manifest describes only the most recent invocation.」 | **GREEN** ← AU-01 |
| I-2 | 注入「The manifest holds the most recent run and nothing else.」 | **GREEN** ← AU-01 |
| I-3 | 注入「`_failures.json` describes the current run only.」 | **GREEN** ← AU-01 |
| I-4 | 注入「The manifest reflects what just happened, nothing older.」 | GREEN |
| I-5 | 注入「The manifest is rewritten wholesale each time the ingester finishes.」 | GREEN |
| I-6 | 注入「`_failures.json` is a snapshot of the last attempt.」 | **RED** |
| I-7 | 注入「The manifest is emptied at the start of every run.」/「失败清单只记录最近那次跑的结果。」/「The manifest is a per-run report.」/「…the most recent run's fetch produced.」 | **4/4 RED** |
| S-1 | 半角 `.` 断句限定去掉 | 候选 55→39，`registry.md` 归零，`21 UNADJUDICATED / 37 VANISHED` |
| T-1 | 同一假话放未跟踪文件 / `git add -N` 后 | GREEN / **RED**（AU-03） |
| 对照 | 我自造句「The manifest is a per-run report.」：字面量扫描 vs 句子级审计 | **GREEN vs RED（exit 1）** |
| 对账 | 粘回五条退役原文 | 字面量扫描新增 `P11..P15` **五行**，双向 diff 转红 |

---

## 反模式扫描

本轮改动的 8 个文件（`inspector.py`、`acquisition.py`、`coverage.py`、`test_tiingo_quota.py`、
`example/registry.md`、`manifest_sentence_audit.py`、`manifest_semantics_scan.py`、`STATE.md`）
上 `TBD|FIXME|XXX` **全部零命中**。唯一命中项仍是 `.planning/ROADMAP.md` 里未来阶段的
规划占位 `TBD`（预先存在，前两版同样未列为 blocker），已记入 advisory。

---

## 状态判定

按 gates 决策树：

1. **Rule 1（gaps_found）？** 否 —— 无 FAILED 真相、无 MISSING/STUB 产物、无 NOT_WIRED 关键连接、
   无 blocker 反模式、无未引用的债务标记。上一版唯一的 gap 已关闭。
2. **Rule 2（human_needed）？** **是** —— Step 8 产出 3 项人工检查项（全部为**承继保留**，
   按指示未重新判定）。
3. → **status: human_needed**

AU-01 / AU-02 / AU-03 是我本轮新发现的、**新范围**的问题。按 verifier-evidence-gate：
它们不是承继 gap；`manifest_sentence_audit.py` 确实是本轮新增文件，所以它们**有资格**阻塞。
但我判定它们**不是 Blocker**：AU-01 不违反任何 must-have（plan 10 只要求「语义类」，
plan 11 逐字声明「不主张完备」），AU-02/AU-03 同理。因此按 📋 Advisory 记录，交用户裁决。

---

## Gaps Summary

**没有 gap。**

同一形状的 D-18 文档漂移在本阶段复发四次，第四次的闭环换掉了**验收方式本身**：
从「一份抄来的坏句子字面量清单」换成「语义类求交枚举候选集 → 逐句判读 → 只有 STALE 才有资格
反哺词表」。这个顺序在提交历史上是可见的（`a4a5040` 枚举判读含 5 STALE → `6890d53` 改写 →
`cbb884e` 才扩词表），并且被写进了 `.planning/STATE.md` 的决策段。

**它比前三轮强在哪（都是我实测的，不是 SUMMARY 的说法）：**

1. **它在未被破坏的真实仓库上红过。** 前三轮的验收脚本都是「修完之后才写、写完就是绿」的；
   这一条我 checkout 改写前的 commit 实跑，`AUDIT RED: 5 STALE` / exit 1。
2. **55 句候选被 digest 钉死。** 动其中任何一句——改写、改回、删掉——都同时触发
   `UNADJUDICATED` 与 `VANISHED` 两条臂。「让发现消失」不再是廉价的绿（D-1 实测）。
3. **对账链闭合。** 5 条退役行的 `replacement_digest` 全部落在账本的真实行上，
   而 `--check` 的 `0 VANISHED` 保证账本每个 digest 都对应一句活着的句子 —— 伪造不了。
4. **词表反哺被降级且有出处。** 新增 5 条 pattern 条条带 provenance 注释（3 derived / 2 named-by），
   而且我实测它们全是活的（粘回退役原文 → 五行全红）。

**它仍然做不到、也没有声称做到的：** 词表不是穷举。我造了 10 句全新的假话，4 句能整个绕过。
其中 3 句（AU-01）走的是同一个洞：`RUNSCOPE_TERMS` 有中文 `最近`，没有英文 `most recent` /
`current run`。这是中英不对称，不是「语义类这个思路不成立」——补两三个词就能堵上，
而且补完之后按本阶段自己定的顺序把新候选句判读进账本即可。**我把它记成 advisory 而不是 gap，
因为两份计划都逐字声明过「本轮不主张完备性」；但如果用户想让这条 gate 再紧一格，
AU-01 是最便宜的那一格。**

---

_Verified: 2026-09-09T16:40:00Z_
_Verifier: Claude (gsd-verifier) — 第四次验证 / 第三次复验_
