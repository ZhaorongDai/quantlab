---
phase: 03.4-data-source-registry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_source_registry.py
  - tests/test_source_inspector.py
  - tests/test_acquisition_progress.py
  - tests/test_ingest_shells.py
  - tests/test_atomic_sidecars.py
autonomous: true
requirements: [D-01, D-04, D-05, D-06, D-09, D-11, D-15, D-16, D-17, D-20]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every one of the five new test files exits 0 with at least one test COLLECTED and PASSED — none of them exits 5 (\"no tests ran\"), which reads as green."
    - "`no_credentials` removes TIINGO_API_KEY, APCA_API_KEY_ID and APCA_API_SECRET_KEY from os.environ for the duration of a test and restores the pre-test environment afterwards, whether or not each name was set beforehand."
    - "`isolated_registry` imports quantlab inside the fixture body, so tests/conftest.py still imports no quantlab module at collection time."
    - "`isolated_registry` yields with DataSourceRegistry.SOURCES snapshotted and restores the exact pre-test tuple afterwards, so a test that registers a fake source leaks nothing into a later test."
    - statement: "Requesting `isolated_registry` before quantlab/acquisition/registry.py exists fails the requesting test with a clear ImportError rather than silently yielding None."
      verification: backstop
    - "`_scan_source_count` returns 5 for polars' abbreviated `[first.pqt, ... 4 other sources]` plan rendering and 2 for an explicitly-listed two-file plan — both asserted in the same test, so the helper cannot rot into a tautology."
    - "`_attempt_batch` returns exactly a 3-element tuple, asserted by length rather than by unpacking, so the L-6 arity constraint is red before any later plan can widen it."
    - "`PageLedger._flush` leaves no `*.tmp` file in the sidecar directory after a successful write, and the written file parses as JSON — the precedent the atomic-write plan copies is proven before it is copied."
  prohibitions:
    - statement: "A Wave-0 scaffold file must not ship a placeholder, a bare `pass`, an `xfail`, or a `pytest.skip` as its only test — a file whose tests do not run is indistinguishable from a file whose tests pass."
      status: unresolved
      reason: "spec-less fallback: recalled in-prompt, no wired check descriptor authored"
    - statement: "A `-k` selector name must not be attached to a scaffold self-test that does not honestly cover that selector's behaviour — a name that matches without testing is the same green-on-nothing lie as the exit-5 trap (03.2 precedent, STATE.md)."
      status: unresolved
      reason: "spec-less fallback: recalled in-prompt, no wired check descriptor authored"
  artifacts:
    - path: tests/conftest.py
      provides: "isolated_registry and no_credentials fixtures"
      contains: "def no_credentials"
    - path: tests/test_source_registry.py
      provides: "D-01..D-07 / SC-1 / SC-2 test home, with fixture-contract self-tests"
      contains: "def test_"
    - path: tests/test_source_inspector.py
      provides: "D-08..D-11 / SC-3 / SC-4 test home, with the hive-tree and _scan_source_count self-tests"
      contains: "_scan_source_count"
    - path: tests/test_acquisition_progress.py
      provides: "D-16..D-18 / SC-5 test home, with the _attempt_batch arity lock"
      contains: "_attempt_batch"
    - path: tests/test_ingest_shells.py
      provides: "D-15 / SC-6 test home, with the AST import-resolver self-test"
      contains: "_resolved_imports"
    - path: tests/test_atomic_sidecars.py
      provides: "D-20 test home, with the PageLedger._flush precedent self-test"
      contains: "PageLedger"
  key_links:
    - from: tests/conftest.py
      to: quantlab/acquisition/registry.py
      via: "isolated_registry imports DataSourceRegistry INSIDE the fixture body (lazy), never at module scope"
      pattern: "from quantlab.acquisition.registry import DataSourceRegistry"
    - from: tests/test_source_inspector.py
      to: tests/test_raw_hive_layout.py
      via: "_scan_source_count is copied with its negative control, parsing polars' abbreviated source list"
      pattern: "other sources?"
---

<objective>
Build the Wave-0 validation scaffolding for Phase 03.4: two shared fixtures and the five new test
files the rest of the phase fills in, each shipping at least one REAL infrastructure self-test.

Purpose: this repository has already burned itself on the failure this plan exists to prevent. A
pytest file with zero tests exits **5** ("no tests ran"), and every per-file command in every later
plan would read that as green (03.2 precedent, recorded in STATE.md). A second recorded incident is
sharper still: a `-k` selector whose only covering test was named outside the selector left
`-k fingerprint` green *after the mechanism was deleted*. Wave 0 therefore ships tests that assert
the exact fixture and arity contracts the later plans depend on, so each file has value on its own
and fails loudly if that contract drifts.

Output: `tests/conftest.py` gains `isolated_registry` and `no_credentials`; `tests/test_source_registry.py`,
`tests/test_source_inspector.py`, `tests/test_acquisition_progress.py`, `tests/test_ingest_shells.py`
and `tests/test_atomic_sidecars.py` exist and pass.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.4-data-source-registry/03.4-CONTEXT.md
@.planning/phases/03.4-data-source-registry/03.4-VALIDATION.md
@.planning/phases/03.4-data-source-registry/03.4-PATTERNS.md
</context>

## Multi-Source Coverage Audit

Audit covers the whole 03.4 plan set (plans 01–07); it lives here, in the first plan, so it is read
before execution begins.

```
SOURCE    | ID    | Feature / Requirement                                                   | Plan     | Status  | Notes
--------- | ----- | ---------------------------------------------------------------------- | -------- | ------- | -----
GOAL      | —     | One registered descriptor per source; both quantlab and an out-of-repo | 02,04,   | COVERED | SC-1..SC-6 split across the tracer and its expansions
          |       | console enumerate from the same definition                              | 05,06    |         |
REQ       | SC-1  | Every acquirable source enumerable from ONE registry, no vendor class  | 02,06    | COVERED | Registry + purity tests on all three shells
          |       | at the call site; descriptor carries capabilities/env names/class      |          |         |
REQ       | SC-2  | Enumeration + credential status never return a VALUE, never require one| 02       | COVERED | is_configured / credential_status return bools only
REQ       | SC-3  | Read surface answers inventory/coverage/failures/browsing with NO      | 04       | COVERED | SourceInspector + socket tripwire + no_credentials
          |       | credentials and zero vendor requests                                    |          |         |
REQ       | SC-4  | Coverage judgement computed by the same code the real run uses         | 04       | COVERED | CoverageLedger extraction, both callers share it
REQ       | SC-5  | Start programmatically, observe progress, cancel at a batch boundary   | 02,03,05 | COVERED | run() + reporter + cancel token + atomic sidecars
          |       | leaving the store resumable, receive a result object                    |          |         |
REQ       | SC-6  | In-repo ingest entry points reach their source through the registry    | 02,06    | COVERED | ingest_tiingo (02); ingest_alpaca + ingest_us_equity (06)
RESEARCH  | —     | Vendor imports at the BOTTOM of registry.py; __init__.py stays empty   | 02       | COVERED | L-3 resolution 1, confirmed by D-07 amendment
RESEARCH  | —     | CoverageLedger extraction (Pattern 6 Option A), Acquisition delegates  | 04       | COVERED | ~40 existing call sites keep working via delegation
RESEARCH  | —     | Cancel check reuses _attempt_batch's FIRST statement; _run_once widens,| 05       | COVERED | L-6 arity constraint locked in Wave 0
          |       | _attempt_batch's 3-tuple arity does not                                  |          |         |
RESEARCH  | —     | Atomic sidecar writes via ONE shared helper, not a third copy          | 03       | COVERED | L-4 + Don't-Hand-Roll
RESEARCH  | —     | All three shells kept; ingest_us_equity is NOT redundant (7 ways)      | 06       | COVERED | L-1
RESEARCH  | —     | example/acquisition.md goes stale; example/registry.md expected        | 07       | COVERED | L-8 + A9
RESEARCH  | —     | ROADMAP boundary-contract bullet still states the superseded rule      | 07       | COVERED | D-12 ACTION REQUIRED
RESEARCH  | —     | Q3 coverage event AND keep log lines; Q4 descriptors beside vendor     | 02,05    | COVERED | orchestrator ruling: take RESEARCH's recommendations
          |       | class; Q5 universe categories advisory; Q6 inventory reports tiers      | 04       |         |
          |       | separately                                                              |          |         |
CONTEXT   | D-01  | ONE descriptor per vendor, capability list inside                      | 02       | COVERED |
CONTEXT   | D-02  | Capabilities are small dataclasses, extensible                         | 02       | COVERED |
CONTEXT   | D-03  | Direct class reference, not a dotted path                              | 02       | COVERED |
CONTEXT   | D-04  | Descriptor declares env var NAMES; ONE shared is_configured            | 02       | COVERED |
CONTEXT   | D-05  | Descriptors are dataclass INSTANCES in a class-level registry tuple    | 02       | COVERED |
CONTEXT   | D-06  | @register_source decorator; isolation fixture; enumeration order       | 01,02    | COVERED |
CONTEXT   | D-07  | Cold-import completeness (AMENDED: bottom of registry.py)              | 02       | COVERED |
CONTEXT   | D-08  | New read-only inspector, no credentials                                | 04       | COVERED |
CONTEXT   | D-09  | Inspector must NOT re-implement coverage judgement                     | 04       | COVERED |
CONTEXT   | D-10  | Row-level browsing returns a LAZY object                               | 04       | COVERED |
CONTEXT   | D-11  | symbols AND a date range are REQUIRED on the lazy read                 | 04       | COVERED |
CONTEXT   | D-12  | Writes run IN-PROCESS (supersedes the subprocess contract)             | 02,07    | COVERED | code (02) + ROADMAP rewrite (07)
CONTEXT   | D-13  | Long-task isolation moves ENTIRELY to quantlab-console                 | 05,07    | COVERED | 05 adds no pool; 07 records the boundary
CONTEXT   | D-14  | Programmatic run(); AMENDED acquisition-only, no Zarr conversion       | 02       | COVERED |
CONTEXT   | D-15  | Three ingest scripts reduced to thin shells, kept not deleted          | 02,06    | COVERED |
CONTEXT   | D-16  | Progress as event objects through a pluggable reporter                 | 05       | COVERED |
CONTEXT   | D-17  | Separate cancel token checked at batch boundaries                      | 05       | COVERED |
CONTEXT   | D-18  | Result object AND _failures.json, kept consistent                      | 02,05    | COVERED |
CONTEXT   | D-19  | Logging stays as it is; console installs its own sink                  | 05,07    | COVERED |
CONTEXT   | D-20  | No lock; AMENDED atomic sidecar writes IN SCOPE                        | 03       | COVERED |
```

All rows COVERED. No `⚠ MISSING`. Excluded as non-gaps: CONTEXT `## Deferred Ideas` (thin-shell
deletion, a same-source file lock, `[project.scripts]` entry points) and the ROADMAP's explicit
out-of-scope list (Textual TUI, service layer, scheduler, run history, download form, CSV export,
quality-check rendering, HTTP API, web frontend).

## Artifacts this phase produces

Every symbol below is CREATED by phase 03.4 and does not exist before it — exclude these from
source-grounding drift verification.

- `quantlab/utils/atomic.py`: `write_json_atomically()`
- `quantlab/base/coverage.py`: `CoverageLedger`, `CoverageLedger.for_config()`, `.watermark_root`,
  `.watermark_path()`, `.read_sidecar()`, `.read_watermark()`, `.read_coverage()`, `.legacy_policy()`,
  `.coverage_status()`, `.classify_coverage()`, `.covers()`, `.partition_by_coverage()`,
  `.validate_symbols()`
- `quantlab/base/progress.py`: `ProgressEvent`, `ProgressReporter`, `NullProgressReporter`,
  `TqdmProgressReporter`, `CallbackProgressReporter`, `CancelToken`
- `quantlab/base/acquisition.py` (new members on an existing class): `AcquisitionResult`,
  `Acquisition.last_result`, `Acquisition._coverage`, `Acquisition.attach()`,
  `Acquisition._reporter`, `Acquisition._cancel_token`, `Acquisition._should_stop()`,
  `Acquisition._emit()`
- `quantlab/acquisition/registry.py`: `Capability`, `SourceDescriptor`, `SourceDescriptor.supports()`,
  `DataSourceRegistry`, `DataSourceRegistry.SOURCES`, `.all()`, `.get()`, `register_source()`,
  `is_configured()`, `credential_status()`, `run()`
- `quantlab/acquisition/inspector.py`: `SourceInspector`, `.inventory()`, `.coverage()`,
  `.failures()`, `.browse_raw()`, `.browse_zarr()`, `_RawTierReader`
- `quantlab/acquisition/tiingo.py`: `TIINGO_SOURCE`
- `quantlab/acquisition/alpaca.py`: `ALPACA_SOURCE`
- Tests: `tests/test_source_registry.py`, `tests/test_source_inspector.py`,
  `tests/test_acquisition_progress.py`, `tests/test_ingest_shells.py`,
  `tests/test_atomic_sidecars.py`; fixtures `isolated_registry`, `no_credentials`
- Docs: `example/registry.md`
- CLI: no new flags. `ingest_us_equity.py --dry-run` gains a credential-free coverage report where
  it previously printed a skip line.

<flagged_assumptions>
The spec-less edge probe returned these rows as `unclassified` / `unresolved` for requirements this
plan set owns. They are surfaced here rather than dropped; each is a planner assumption a reviewer
may overturn.

- **D-06 (unclassified is not among them, but the fixture design is an assumption).** `isolated_registry`
  isolates by `monkeypatch.setattr` on the class attribute, which is correct ONLY because `SOURCES`
  is rebound (`+=` on a tuple) and never mutated in place. Plan 02 pins that invariant with
  `registration_tuple_shape`; if a later change switches to `list.append`, this fixture silently
  stops isolating.
- The remaining `unclassified` probe rows (D-02, D-05, D-09, D-10, D-12, D-13, D-14, D-16, D-18,
  D-19) are surfaced in the plans that own those decisions: 02 (D-02, D-05, D-12, D-14, D-18),
  04 (D-09, D-10), 05 (D-13, D-16, D-19).
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Add the isolated_registry and no_credentials fixtures to tests/conftest.py</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py — read `_reset_data_root_override` at `:38-57` (the autouse
      snapshot/restore idiom to mirror, including its rationale-first docstring and its
      import-quantlab-INSIDE-the-body convention) and `acquisition_config` at `:1105`
    - quantlab/acquisition/tiingo.py — `KEY_ENV` at `:16`, `CREDENTIAL_ENV_VARS` at `:73`
    - quantlab/acquisition/alpaca.py — `KEY_ENV`/`SECRET_ENV` at `:22-23`,
      `CREDENTIAL_ENV_VARS` at `:520`
    - .planning/phases/03.4-data-source-registry/03.4-PATTERNS.md — section
      "`tests/conftest.py` — `isolated_registry`, `no_credentials`"
  </read_first>
  <action>
    Add two fixtures to `tests/conftest.py`, both NON-autouse (a test opts in), both importing
    `quantlab.*` inside the fixture body so the module keeps its zero-import-at-collection-time
    promise — the convention `_reset_data_root_override` already follows.

    `isolated_registry(monkeypatch)`: import `DataSourceRegistry` from
    `quantlab.acquisition.registry` inside the body, then
    `monkeypatch.setattr(DataSourceRegistry, "SOURCES", tuple(DataSourceRegistry.SOURCES), raising=True)`
    and `yield DataSourceRegistry`. Docstring must state the load-bearing precondition: this
    isolates ONLY because `SOURCES` is rebound with `+=` on a tuple and never mutated in place, so
    the day someone switches to `list.append` the isolation silently stops working — and name
    `test_registration_tuple_shape` (plan 02) as the test that keeps that honest. The import will
    raise `ImportError` until plan 02 lands; that is correct and must not be softened with a
    try/except or a `pytest.importorskip`, because a silently-skipped isolation fixture is how a
    fake source leaks into every later test in the session.

    `no_credentials(monkeypatch)`: delete `TIINGO_API_KEY`, `APCA_API_KEY_ID` and
    `APCA_API_SECRET_KEY` from the environment with `monkeypatch.delenv(name, raising=False)`, then
    yield the tuple of names. Docstring must say why the three names are written as literals here
    rather than read from `CREDENTIAL_ENV_VARS`: the fixture is what proves a surface needs no
    credential, so sourcing its list from the same declaration the surface uses would make the
    proof circular. `raising=False` is required because a developer machine may have none of them
    set. `monkeypatch` restores the pre-test environment on teardown, so no manual restore is needed.
  </action>
  <verify>
    <automated>uv run pytest --collect-only -q 2>&1 | tail -5</automated>
    <fails_when>non-zero exit, or the output contains "error" / "ERROR" / a traceback (a collection error means conftest.py imports quantlab at module scope or has a syntax error)</fails_when>
    <automated>uv run pytest -q</automated>
    <fails_when>non-zero exit, or the summary line reports fewer than 649 passed (the 2026-09-08 baseline) — a new conftest fixture must not disturb any existing test</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def isolated_registry' tests/conftest.py` is 1 and `grep -c 'def no_credentials' tests/conftest.py` is 1
    - Neither fixture is decorated `autouse=True`
    - `tests/conftest.py` still contains no module-scope `import quantlab` / `from quantlab` for the
      registry: `awk '/^def |^@pytest/{exit} /^(import|from) quantlab/{print}' tests/conftest.py`
      prints nothing new for `quantlab.acquisition.registry`
    - `uv run pytest -q` reports at least 649 passed and 0 failed
    - `no_credentials` names exactly the three literals `TIINGO_API_KEY`, `APCA_API_KEY_ID`,
      `APCA_API_SECRET_KEY` and calls `delenv` with `raising=False`
  </acceptance_criteria>
  <done>Both fixtures exist, are opt-in, import lazily, and the full suite is still green at its 649-test baseline.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold tests/test_source_registry.py and tests/test_source_inspector.py with real self-tests</name>
  <files>tests/test_source_registry.py, tests/test_source_inspector.py</files>
  <read_first>
    - tests/conftest.py — `acquisition_config` at `:1105` (vendor-terminated raw root + sibling
      watermark root), `hive_raw_tree` at `:1046`, `stock_zarr` at `:760`
    - tests/test_raw_hive_layout.py — `_OTHER_SOURCES` at `:161`, `_scan_source_count` at `:164`,
      and its own self-test at `:204-205` (the negative-control habit to copy)
    - tests/test_extensibility_contract.py — `:1-50`, the module-docstring + file-list +
      forbidden-substring idiom, including the docstring's statement of what the scan CANNOT catch
    - quantlab/acquisition/tiingo.py — `:16`, `:68`, `:73`
    - quantlab/acquisition/alpaca.py — `:22-23`, `:229`, `:520`
    - .planning/phases/03.4-data-source-registry/03.4-VALIDATION.md — the D-01..D-11 rows, so every
      later `-k` selector named there has a home in the right file
  </read_first>
  <action>
    Create both files. Each opens with a module docstring naming the ROADMAP success criteria it
    will prove (SC-1/SC-2 for the registry file; SC-3/SC-4 for the inspector file), the D-IDs it
    covers, and — per the 03.2 precedent recorded in STATE.md — an explicit statement that every
    test in the file must be a real assertion, because a file with zero tests exits 5 and reads as
    green.

    `tests/test_source_registry.py` ships these self-tests, all of which pass against the code as it
    stands today:
    - `test_the_acquisition_config_fixture_serves_both_vendors_with_a_terminated_raw_root`:
      build `acquisition_config(vendor="tiingo")` and `acquisition_config(vendor="alpaca")`; assert
      for each that `Path(cfg.raw_data_dir_path).name == cfg.vendor`, that
      `Path(cfg.watermark_path).parent == Path(cfg.raw_data_dir_path).parent.parent / "_watermarks"`
      is expressible from the fixture's own layout (assert the watermark root is NOT inside the raw
      root, which is the invariant a `.json` inside a scanned tree would break), and that the two
      vendors' raw roots are different directories.
    - `test_vendor_credential_env_names_are_module_level_constants_not_client_attributes`:
      assert `quantlab.acquisition.tiingo.KEY_ENV == "TIINGO_API_KEY"`,
      `quantlab.acquisition.alpaca.KEY_ENV == "APCA_API_KEY_ID"`,
      `quantlab.acquisition.alpaca.SECRET_ENV == "APCA_API_SECRET_KEY"`, and that each class's
      `CREDENTIAL_ENV_VARS` is exactly the tuple of its module-level constants. This is the pin the
      descriptor's `required_env` will be checked against in plan 02, so it must be true first.
    - `test_the_no_credentials_fixture_empties_every_declared_name`: request `no_credentials`, then
      assert all three names are absent from `os.environ` and that the fixture returned exactly those
      three names.

    `tests/test_source_inspector.py` ships these self-tests:
    - Copy `_OTHER_SOURCES` and `_scan_source_count` verbatim from `tests/test_raw_hive_layout.py`
      (with a comment naming that file as the origin and the reason a copy rather than a cross-test
      import is used) plus its negative control:
      `test_scan_source_count_reads_both_polars_plan_renderings` asserting 5 for the abbreviated
      form and 2 for the explicit form, in ONE test.
    - `test_the_hive_raw_tree_fixture_writes_vendor_namespaced_shards`: call `hive_raw_tree` for
      two vendors under one root; assert each vendor's shards live under
      `{root}/{vendor}/{hive_key}=.../` and that no shard of one vendor is reachable from the other
      vendor's root, since that separation is what `browse_raw` will rely on.
    - `test_the_stock_zarr_fixture_opens_and_carries_a_symbol_index`: open the fixture's store with
      `xr.open_zarr`, assert `symbol` and `timestamp` are both indexes and that
      `.sel(symbol=[<one real symbol>])` succeeds — the Zarr-tier contract `browse_zarr` builds on.

    Do NOT add any test named for a plan-02/plan-04 `-k` selector (`one_descriptor_per_vendor`,
    `capabilities`, `direct_class_reference`, `env_names_are_exactly_what_gates_construction`,
    `never_returns_a_credential_value`, `registration_tuple_shape`, `decorator_registers`,
    `enumeration_order`, `enumeration_is_complete_from_a_cold_import`, `without_credentials`,
    `zero_vendor_requests`, `inspector_binds_no_client`, `coverage_is_the_same_code`,
    `browse_requires_symbols_and_window`, `browse_prunes`). Those selectors must match ZERO tests
    until the behaviour exists; a name that matches without testing the behaviour is the same
    green-on-nothing lie as the exit-5 trap. Record that rule in each file's module docstring.
  </action>
  <verify>
    <automated>uv run pytest tests/test_source_registry.py tests/test_source_inspector.py -q</automated>
    <fails_when>non-zero exit, exit code 5, or the summary line reports fewer than 6 passed (three self-tests per file) — fewer means a scaffold shipped a placeholder instead of an assertion</fails_when>
    <automated>uv run pytest tests/test_source_registry.py tests/test_source_inspector.py --collect-only -q 2>&1 | tail -3</automated>
    <fails_when>the trailing count line reports 0 tests collected, or the output contains "no tests ran"</fails_when>
  </verify>
  <acceptance_criteria>
    - Both files exist and `uv run pytest tests/test_source_registry.py -q` and
      `uv run pytest tests/test_source_inspector.py -q` each exit 0 with at least 3 passed
    - `uv run pytest tests/test_source_registry.py tests/test_source_inspector.py -k "one_descriptor_per_vendor or coverage_is_the_same_code or browse_prunes" -q 2>&1 | grep -q "no tests ran"`
      succeeds — those selectors match nothing yet, and each later plan is responsible for making
      its own selector match
    - Neither file contains `pytest.skip`, `pytest.mark.xfail`, or a test body that is only `pass`
    - `_scan_source_count` in `tests/test_source_inspector.py` is asserted against BOTH plan
      renderings in one test
    - `uv run pytest -q` still reports 0 failed
  </acceptance_criteria>
  <done>Both scaffold files exist, each with three passing real self-tests, and none of the phase's later `-k` selectors matches anything yet.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold the progress, ingest-shell and atomic-sidecar test files with real self-tests</name>
  <files>tests/test_acquisition_progress.py, tests/test_ingest_shells.py, tests/test_atomic_sidecars.py</files>
  <read_first>
    - quantlab/base/acquisition.py — `_attempt_batch` at `:1605` and its first-statement abort
      check at `:1635`; `_run_once` at `:1276` and its tqdm block at `:1326-1352`; `_knob` at `:466`
    - tests/test_acquisition_batching.py — the two existing `_attempt_batch` unpackers (the
      positional call and the 3-tuple unpack) that L-6 says fix its arity
    - tests/test_volume_guard.py — `_no_network` at `:153`, `_RESOLVER_TOKEN` at `:703`,
      `_FORBIDDEN_ACQUISITION_MODULES` at `:708`, `_resolved_imports` at `:717`
    - quantlab/base/pageledger.py — `_flush` (the atomic temp-file + fsync + os.replace precedent)
    - tests/conftest.py — `acquisition_config` at `:1105`, `mock_tiingo_client` at `:165`
  </read_first>
  <action>
    Create three files, each with a module docstring naming its D-IDs, its success criterion, and
    the zero-tests-exits-5 rule.

    `tests/test_acquisition_progress.py` (D-16..D-18, SC-5) ships:
    - `test_attempt_batch_returns_exactly_three_elements`: construct a Tiingo acquisition through
      `mock_tiingo_client` + `acquisition_config`, set the quota abort so the call returns on its
      first statement without issuing anything, and assert `len(result) == 3` **by length**, not by
      unpacking. Docstring states this is the L-6 constraint: two existing tests in
      `tests/test_acquisition_batching.py` fix this arity, so the cancel work must reuse the same
      first-statement seam and the same `"skipped"` status rather than widening it — while
      `_run_once`'s arity is pinned by nothing and is the one that may widen.
    - `test_the_progress_knob_is_read_through_kwargs_and_defaults_on`: assert
      `acq._knob("progress", True)` is `True` for a config with no `kwargs`, and `False` for one
      built with `kwargs={"progress": False}`. This is the knob `TqdmProgressReporter` must keep
      honouring.

    `tests/test_ingest_shells.py` (D-15, SC-6, SC-1) ships:
    - Copy `_resolved_imports` verbatim from `tests/test_volume_guard.py:717-745` together with a
      token constant of this file's own (a marker string appearing in exactly one file, so a failure
      is attributable to THIS arm), and a `SHELL_FILES` tuple naming the three repo-root scripts.
    - `test_the_import_resolver_sees_absolute_and_relative_spellings`: run `_resolved_imports` over
      a small `ast.parse`-able source written into `tmp_path` containing an absolute import, a
      `from X import y` and a relative `from ..pkg.mod import Z`, and assert the resolved set
      contains all three fully-qualified names. This is the helper the purity test depends on; a
      substring scan would pass on a docstring mention, which is exactly what this resolver exists
      to avoid.
    - `test_every_shell_file_exists_and_is_git_tracked`: assert each of the three scripts exists and
      that `git ls-files -- <path>` prints it, so a shell cannot be quietly deleted rather than
      thinned (D-15 keeps all three).

    `tests/test_atomic_sidecars.py` (D-20) ships:
    - `test_page_ledger_flush_leaves_no_temp_file_and_writes_parseable_json`: drive a `PageLedger`
      through a real flush into `tmp_path`, then assert the sidecar parses as JSON and that
      `list(directory.glob("*.tmp"))` is empty. This proves the in-repo precedent plan 03 copies,
      before it is copied.
    - `test_a_half_written_watermark_sidecar_reads_back_as_absent`: hand-write a truncated JSON file
      at the path `_watermark_path` yields for a symbol, then assert `acq._read_sidecar(symbol)` is
      `None` rather than raising. This is the blast-radius bound L-4 relies on and it must hold both
      before and after the atomic-write change.
  </action>
  <verify>
    <automated>uv run pytest tests/test_acquisition_progress.py tests/test_ingest_shells.py tests/test_atomic_sidecars.py -q</automated>
    <fails_when>non-zero exit, exit code 5, or the summary line reports fewer than 6 passed — fewer means one of the three files shipped a placeholder</fails_when>
    <automated>for f in tests/test_source_registry.py tests/test_source_inspector.py tests/test_acquisition_progress.py tests/test_ingest_shells.py tests/test_atomic_sidecars.py; do uv run pytest "$f" -q > /dev/null 2>/dev/null || { echo "RED $f"; exit 1; }; done; echo scaffolds_green</automated>
    <fails_when>the line "scaffolds_green" is absent, or any "RED &lt;path&gt;" line is printed</fails_when>
    <automated>uv run pytest -q</automated>
    <fails_when>non-zero exit, or the summary line reports any failed/errored test, or fewer than 649 passed</fails_when>
  </verify>
  <acceptance_criteria>
    - All three files exist; each exits 0 with at least 2 passed when run alone
    - `uv run pytest tests/test_acquisition_progress.py -k "cancel_leaves_a_resumable_store or emits_one_event_per_batch or result_and_manifest_agree" -q 2>&1 | grep -q "no tests ran"` succeeds
    - `uv run pytest tests/test_ingest_shells.py -k "no_shell_names_a_vendor_class or us_equity_keeps" -q 2>&1 | grep -q "no tests ran"` succeeds
    - No file contains `pytest.skip`, `pytest.mark.xfail`, or a body that is only `pass`
    - `uv run pytest -q` reports 0 failed and at least 649 + 11 passed
  </acceptance_criteria>
  <done>All five Wave-0 files exist with real passing self-tests, every later `-k` selector still matches nothing, and the full suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → OS environment | Fixtures read and delete real credential env vars on a developer machine |
| test process → filesystem | Fixtures write hive trees, Zarr stores and sidecars under `tmp_path` |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.4-01-01 | Information Disclosure | `no_credentials` fixture | medium | mitigate | The fixture deletes names; it never reads, returns, prints or asserts on a VALUE. Its return is the tuple of NAMES only. |
| T-03.4-01-02 | Tampering | `no_credentials` restoring the environment | medium | mitigate | Deletion goes through `monkeypatch.delenv(..., raising=False)` so pytest restores the pre-test environment on teardown; no manual `os.environ` assignment anywhere in the fixture. |
| T-03.4-01-03 | Tampering | `isolated_registry` failing open | high | mitigate | The lazy import is left to raise `ImportError`. No `try/except`, no `importorskip` — a silently-skipped isolation fixture lets a fake descriptor leak into every later test in the session. |
| T-03.4-01-04 | Repudiation | a scaffold file with zero tests | high | mitigate | Every file ships ≥2 real assertions; the per-file verify asserts a minimum passed count, so exit 5 cannot read as green. |
| T-03.4-01-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs nothing. `03.4-RESEARCH.md` § Package Legitimacy Audit records zero packages added for the whole phase, so there is no slopsquat surface and no `[ASSUMED]`/`[SUS]` entry to gate. |
</threat_model>

<verification>
- `uv run pytest -q` is green with at least 660 passed (649 baseline + 11 new self-tests)
- Each of the five new files exits 0, never 5, when run alone
- Every phase `-k` selector listed in `03.4-VALIDATION.md` that belongs to a later plan still
  reports "no tests ran"
- `tests/conftest.py` imports no quantlab module at collection time
</verification>

<success_criteria>
Wave 0 is complete: the fixtures and the five test files exist, each file carries at least one real
infrastructure self-test that would fail if the contract it asserts drifted, and the pre-existing
649-test baseline is untouched.
</success_criteria>

<output>
Create `.planning/phases/03.4-data-source-registry/03.4-01-SUMMARY.md` when done
</output>
