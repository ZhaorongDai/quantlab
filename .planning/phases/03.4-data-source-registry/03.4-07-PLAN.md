---
phase: 03.4-data-source-registry
plan: 07
type: execute
wave: 7
depends_on: ["03.4-05", "03.4-06"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/phases/03.4-data-source-registry/03.4-RESEARCH.md
  - example/registry.md
  - example/acquisition.md
  - example/pageledger.md
  - example/README.md
  - README.md
autonomous: true
requirements: [D-12, D-13, D-15, D-19, D-20]

estimate:
  tokens: 54000
  raw_tokens: 54000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The ROADMAP's Phase 03.4 boundary-contract bullet no longer states that writes run as a subprocess CLI invocation; it states the in-process contract, names the date and the decision that superseded the old rule, and records what was given up (process isolation and the exported-command guarantee) (D-12 ACTION REQUIRED)."
    - "The ROADMAP no longer says concurrency needs a decision pending a check of whether sidecar writes are atomic; it records that they were not, that they now are, and that quantlab still adds no lock (D-20)."
    - "The ROADMAP's `DataSourceRegistry` in-scope bullet no longer says `quantlab/acquisition/__init__.py` imports every vendor module; it states that the vendor imports sit at the bottom of `registry.py`, that the package `__init__.py` stays empty, and why — while keeping the unchanged intent that enumeration is complete from a cold import (D-07 AMENDED)."
    - "The ROADMAP's programmatic-entry-point bullet no longer calls `_failures.json` a resume input; it states that nothing under `quantlab/` reads the manifest, that resume is driven entirely by watermark-sidecar presence, and that the manifest is kept as the crash-durable record (D-18 FACTUAL CORRECTION)."
    - "`03.4-RESEARCH.md`'s `## Open Questions` heading is marked `(RESOLVED)` and each of Q1-Q6 carries a `**Resolved:**` line naming the decision or plan that closed it, with the question text itself unedited; the two assumption rows still marked `needs user confirmation` (A4, A5) are recorded as settled in an inserted paragraph."
    - "The ROADMAP's out-of-scope list still names long-task isolation, the log sink and same-source concurrency control as the console's, and nothing in this phase contradicts that (D-13 / D-19)."
    - "`example/registry.md` exists, is written in Chinese like every sibling, is linked from `example/README.md`'s reading order, and documents the descriptor, the capability list, `is_configured`, `run()`, the reporter, the cancel token and the inspector."
    - "Every runnable example in `example/registry.md` either was actually executed with its real output pasted, or is explicitly marked as not run — the convention `example/README.md` states for the whole directory."
    - "`example/acquisition.md`'s flow section describes the reporter-and-cancel-aware `_run` → `_run_once` → `_attempt_batch` chain rather than the tqdm bar it describes today."
    - "`example/acquisition.md`'s core-concepts section states that the failure manifest has no reader inside quantlab, that resume is driven entirely by watermark-sidecar presence, and that the manifest is kept as the crash-durable operator record which the inspector now reads."
    - "`example/acquisition.md`'s two no-credential worked examples point at the inspector as the better answer to the question they pose, and the `--dry-run` example no longer shows the coverage report being skipped for want of a key."
    - "`example/acquisition.md`'s new-vendor section says that adding a vendor now means registering a descriptor beside the class, and states where the vendor import goes and why it is not in the package `__init__`."
    - "`example/pageledger.md`'s sidecar-tree listing still describes the failure manifest correctly after the atomic-write change, and names the shared atomic writer."
    - "`README.md`'s entry-point section describes the three ingest scripts as thin shells over the registry, and its environment-variable section still names each credential by NAME only, with no value anywhere."
    - statement: "No documentation file contains a credential value, and every credential mention is a variable NAME."
      verification: backstop
  prohibitions:
    - statement: "Documentation must not be left standing in contradiction with the decision that superseded it. A stale boundary contract in the ROADMAP is worse than no contract, because the next reader will implement the rule that is written down."
      status: unresolved
      reason: "spec-less fallback: recalled in-prompt, no wired check descriptor authored"
    - statement: "A worked example must not paste an output that was never produced. This directory's stated convention is that unrun examples are labelled as unrun, and fabricating one output makes every other output in the directory unreliable."
      status: unresolved
      reason: "spec-less fallback: recalled in-prompt, no wired check descriptor authored"
    - statement: "Documentation must not print, echo, or illustrate a credential value, even a fake-looking one. An example key in a document is the shape a real key gets pasted into."
      status: unresolved
      reason: "spec-less fallback: recalled in-prompt, no wired check descriptor authored"
  artifacts:
    - path: example/registry.md
      provides: "the registry layer's Chinese guide — descriptor, capabilities, credential status, run(), reporter, cancel token, inspector"
      contains: "DataSourceRegistry"
    - path: .planning/ROADMAP.md
      provides: "a Phase 03.4 boundary contract that states the in-process rule instead of the superseded subprocess one"
      contains: "IN-PROCESS"
    - path: example/acquisition.md
      provides: "flow, core-concepts, worked-example and new-vendor sections updated for the registry, the reporter and the cancel token"
      contains: "registry"
    - path: .planning/phases/03.4-data-source-registry/03.4-RESEARCH.md
      provides: "an Open Questions section marked resolved, each question annotated with its closer"
      contains: "Open Questions (RESOLVED)"
  key_links:
    - from: example/README.md
      to: example/registry.md
      via: "the reading-order table gains a registry row in the first step, beside acquisition.md"
      pattern: "registry.md"
    - from: example/acquisition.md
      to: example/registry.md
      via: "the new-vendor section points at the registry guide for descriptor registration"
      pattern: "registry.md"
---

<objective>
Close the documentation debt this phase creates: rewrite every ROADMAP statement a CONTEXT.md
amendment reversed, mark `03.4-RESEARCH.md`'s open questions resolved, add `example/registry.md`,
and update the sections of `example/acquisition.md` that this phase makes stale.

Purpose: CONTEXT.md marks the ROADMAP rewrite `ACTION REQUIRED` explicitly — the roadmap still tells
a reader that writes go through a subprocess CLI invocation, which D-12 replaced, and leaving both
statements standing means the next person implements whichever they read first. `example/acquisition.md`
is 683 lines of Chinese documentation of exactly the layer this phase changed, and two of its worked
examples are specifically about not having credentials — the question the inspector now answers
better. The repo's convention is one `example/*.md` per layer with no exception, so the registry
needs its own.

Three of the ROADMAP's own statements were reversed by later amendments, not just one: the
subprocess write rule (D-12), the `quantlab/acquisition/__init__.py` enumeration mechanism (D-07
AMENDED) and the claim that `_failures.json` is resume input (D-18 FACTUAL CORRECTION). All three
are in the same section and all three are fixed here. `03.4-RESEARCH.md` likewise still presents six
questions as open when every one of them was answered before planning finished.

Output: a corrected ROADMAP, an annotated RESEARCH.md, `example/registry.md`, and updated
`example/acquisition.md`, `example/pageledger.md`, `example/README.md` and `README.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.4-data-source-registry/03.4-CONTEXT.md
@.planning/phases/03.4-data-source-registry/03.4-05-SUMMARY.md
@.planning/phases/03.4-data-source-registry/03.4-06-SUMMARY.md
</context>

## Artifacts this phase produces

See `03.4-01-PLAN.md` → "Artifacts this phase produces". This plan creates `example/registry.md` and
updates existing documentation; it adds no code symbol.

<flagged_assumptions>
- **D-12, D-13, D-19 (unclassified probe rows).** These three are decisions ABOUT another
  repository's responsibilities, so no in-repo test can prove them. Planner assumption: the
  obligation this phase carries for all three is documentary — say what moved, say what was given
  up, and do not leave a superseded statement standing. That is what this plan delivers, and it is
  the honest ceiling for a cross-repo contract.
- **Documentation language.** `example/*.md` is Chinese by convention (`example/README.md` says so);
  `README.md` and `.planning/*` are English. `example/registry.md` is therefore written in Chinese.
- **Why the RESEARCH.md annotation lands here rather than in planning.** `03.4-RESEARCH.md` is a
  research artifact, and its `## Open Questions` section is stale for exactly the reason the ROADMAP
  bullets are: a decision closed it and nothing wrote the closure down. Folding it into this plan
  keeps every documentation correction for this phase in one commit with a verify attached, rather
  than having the planner edit the artifact silently.
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the ROADMAP Phase 03.4 boundary contract and its concurrency note</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md — the whole `### Phase 03.4:` section, especially the four bullets under
      "Boundary contract with the console (revised 2026-09-08 -- supersedes the 2026-09-07 lock)",
      the in-scope bullet about the programmatic write entry point, the in-scope bullet about
      retiring the hardcoded vendor dispatch, and the explicitly-out-of-scope paragraph
    - .planning/phases/03.4-data-source-registry/03.4-CONTEXT.md — D-12's `ACTION REQUIRED` note,
      D-13, D-19 and D-20's amended block
    - .planning/phases/03.4-data-source-registry/03.4-05-SUMMARY.md and 03.4-06-SUMMARY.md — what
      actually shipped, so the roadmap describes the built system rather than the planned one
  </read_first>
  <action>
    Edit `.planning/ROADMAP.md` with SCOPED replacements only — never rewrite the whole file, and
    never touch any phase section other than 03.4.

    Rewrite the boundary-contract bullet that still describes writes as a subprocess CLI invocation.
    The replacement states: the console depends on quantlab and quantlab never depends on the
    console; READS run in-process through a direct import; WRITES also run in-process through the
    registry's programmatic entry point; and this reverses the 2026-09-07 decision as of
    2026-09-08 (D-12). Keep the honest accounting of what that cost — process isolation, so keeping
    a multi-hour backfill from taking the console down is the console's own job, which is why
    quantlab now provides a cancel token and progress events; and the exported-command guarantee,
    which was true by construction only while the exported command WAS what the console ran, so any
    such export is now a reconstruction and has to be tested as one. Mark the bullet as SETTLED
    rather than leaving it phrased as a pending revision.

    Sentinel wording, pinned here so you do not have to guess it. If the replaced rule is referred to
    at all, refer to it ONLY through the exact phrase `the subprocess-CLI rule it supersedes`
    (capitalisation is free, the wording is not). That phrase is the single labelled form the verify
    tolerates, and it must be the ONLY occurrence of the word `subprocess` anywhere in the Phase 03.4
    section — including the out-of-scope paragraph, the plan list, and the D-07 bullet rewritten
    below, whose cold-import test is a natural second place to reach for it. Write the honest prose
    around the sentinel as "the console launched writes by invoking quantlab's CLI in a child
    process", and describe D-07's test as running "in a child process" too. The gate exists to stop a
    superseded INSTRUCTION standing unlabelled, and this wording lets you describe both mechanisms
    accurately without tripping it. There is exactly one occurrence in the section today, in the
    bullet you are replacing.

    Rewrite the concurrency bullet. It currently ends by asking whether watermark/sidecar writes are
    already atomic before deciding whether a lock is needed. Replace that with the answer and the
    outcome: they were not — both were plain writes while the two ledgers beside them already used
    temp file plus fsync plus rename — they now are, through one shared writer, and quantlab still
    adds no lock. Keep the residual risk visible: the thin-shell scripts run outside any console task
    queue, so a shell and the console can still overlap on one source; the atomic write bounds what
    that overlap can do rather than preventing it.

    Update the in-scope bullet about the programmatic entry point to record D-14's amendment — the
    entry point is acquisition-only and stops at the raw parquet tier, with conversion left to the
    entry points because the three of them convert in three different modes with three
    differently-sized RAM guards.

    Update the in-scope bullet about retiring the hardcoded vendor dispatch to record the answer to
    the question it asks: `ingest_us_equity.py` is NOT redundant with `ingest_tiingo.py --universe
    us_all` — they differ in roster mode, storage subdirectory and watermark tree, conversion mode,
    dataset symbols, and three flags that exist on only one of them — so all three shells are kept.

    Rewrite the two remaining sentences in this section that a CONTEXT.md amendment reversed. They
    are as stale as the boundary contract and fail for the same reason — the next reader implements
    whichever statement they read first:

    - The `DataSourceRegistry` in-scope bullet still pairs the registration decorator with
      `quantlab/acquisition/__init__.py` importing every vendor module. D-07's AMENDED block reversed
      that MECHANISM while keeping the INTENT: the vendor imports go at the BOTTOM of
      `quantlab/acquisition/registry.py` and the package `__init__.py` stays empty, because a
      non-empty one runs the vendor imports on every `import quantlab.acquisition.universe` — the one
      module whose whole guarantee is that no acquisition client can be constructed there — and it
      would do so while `tests/test_volume_guard.py` stayed green, since that test's structural arm
      is an AST scan of `universe.py`'s own source and cannot see a transitive import. State that
      enumeration completeness from a cold import is unchanged and is proven by an import test that
      runs in a CHILD PROCESS (say it that way — the other word for it is the one the sentinel rule
      below reserves), that bottom placement is also what lets descriptors be defined beside their
      vendor class, and that the repo's empty-`__init__.py` convention is therefore not broken after
      all.
    - The programmatic-entry-point bullet still calls `_failures.json` "both the crash-durable record
      and resume input". D-18's FACTUAL CORRECTION establishes that nothing under `quantlab/` reads
      the manifest and that resume is driven entirely by watermark-sidecar presence. Replace the
      clause with that, keep the manifest as the crash-durable operator record the inspector reads,
      and do not leave a resume-from-manifest mechanism described that does not exist.

    Do not weaken or expand the explicitly-out-of-scope paragraph: long-task isolation, the log sink
    and same-source concurrency control stay the console's (D-13, D-19, D-20). Leave every other
    phase section, the overview, the phase list and the progress table untouched.
  </action>
  <verify>
    <automated>uv run python -c "import pathlib,re; s=pathlib.Path('.planning/ROADMAP.md').read_text(); sec=s.split('### Phase 03.4:')[1].split('### Phase 4:')[0]; assert 'IN-PROCESS' in sec.upper(), 'the boundary contract does not state the in-process rule'; assert 'SUBPROCESS' not in sec.upper().replace('SUBPROCESS-CLI RULE IT SUPERSEDES',''), 'a subprocess rule statement survives'; print('boundary_rewritten')"</automated>
    <fails_when>the line "boundary_rewritten" is absent, or an AssertionError fires — "the boundary contract does not state the in-process rule" means the replacement text was never written; "a subprocess rule statement survives" means the word appears somewhere outside the one sentinel phrase the action pins, i.e. either a superseded INSTRUCTION is still standing unlabelled, or the prose reached for that word instead of the child-process wording the action specifies</fails_when>
    <automated>uv run python -c "import pathlib; s=pathlib.Path('.planning/ROADMAP.md').read_text(); assert s.count('### Phase ') >= 11, 'phase sections were lost'; assert '### Phase 7: Testing & Code Quality' in s; print('roadmap_intact')"</automated>
    <fails_when>the line "roadmap_intact" is absent, or an AssertionError reporting lost phase sections — a whole-file rewrite instead of a scoped edit</fails_when>
    <automated>git diff --numstat -- .planning/ROADMAP.md</automated>
    <fails_when>the output names any path other than .planning/ROADMAP.md, or insertions plus deletions (the first two fields summed) exceeds 140 — the whole Phase 03.4 section is 137 lines of a 466-line file, so a larger churn means the edit reached outside the section rather than rewriting five bullets inside it</fails_when>
  </verify>
  <acceptance_criteria>
    - The Phase 03.4 boundary-contract bullet states the in-process write rule and is marked settled
    - No sentence in the Phase 03.4 section still instructs a reader that the console launches writes
      as a subprocess, except where it is explicitly labelled as the superseded rule
    - The concurrency bullet states that the sidecar writes were not atomic, now are, and that no
      lock was added, and keeps the shell-versus-console overlap visible as a residual risk
    - The programmatic-entry-point bullet records the acquisition-only amendment, and no longer
      calls `_failures.json` a resume input
    - The `DataSourceRegistry` bullet states that the vendor imports live at the bottom of
      `registry.py` and that `quantlab/acquisition/__init__.py` stays empty (D-07 AMENDED)
    - The vendor-dispatch bullet records that all three shells are kept and why
    - The word `subprocess` appears in the Phase 03.4 section at most once, inside the sentinel
      phrase `the subprocess-CLI rule it supersedes`
    - Every other phase section is byte-unchanged: `git diff -- .planning/ROADMAP.md` touches only
      the Phase 03.4 section and the 03.4 plan list
  </acceptance_criteria>
  <done>The roadmap describes the boundary contract that was actually built, with no superseded rule left standing and no other phase section disturbed.</done>
</task>

<task type="auto">
  <name>Task 2: Write example/registry.md and update the stale acquisition documentation</name>
  <files>example/registry.md, example/acquisition.md, example/pageledger.md, example/README.md, README.md</files>
  <read_first>
    - example/README.md — the reading-order tables, the stated convention that every pasted output
      was really produced or is explicitly marked as not run, and the "想直接上手扩展" list
    - example/acquisition.md — `## 核心概念` (the failure-manifest description around `:180-190`),
      `## 它是怎么工作的` and its flow diagram (`:217-320`), the four worked examples (`:322-545`,
      of which 例 1 and 例 2 are specifically about having no credentials), and `## 怎么接入一个新厂商`
      (`:546-613`)
    - example/pageledger.md — the sidecar-tree listing around `:120-135` that names the failure
      manifest
    - example/factor.md or example/backend.md — a sibling to copy the document's shape and tone from
    - README.md — the entry-point list around `:253-265` and the environment-variable section around
      `:315-335`
    - ingest_us_equity.py — the `_print_coverage` helper only, to confirm the `coverage report:`
      header line plan `03.4-06` task 2 added is present; that literal is what this task's
      PHASE_GATE_OK verify asserts on, and nothing in this plan writes it
    - quantlab/acquisition/registry.py, quantlab/acquisition/inspector.py,
      quantlab/base/progress.py — the shipped code the guide must describe accurately
  </read_first>
  <action>
    Write `example/registry.md` in Chinese, following the shape of the sibling guides: a one-sentence
    summary, a "不用它会怎样" section grounded in what this repository actually did before (a vendor
    class hardcoded at every call site, seven vendor-class reads per script at argparse-definition
    time, a coverage report skipped for want of a key that the computation never needed), a core
    concepts section (descriptor per VENDOR with a capability LIST inside — not a cross-product,
    because Alpaca's tick is us_equity-only and splits into quotes and trades — plus the direct class
    reference, the credential NAMES, and the decorator registration), a how-it-works section (where
    the descriptors are defined, why the vendor imports sit at the bottom of `registry.py` and not
    in the package `__init__`, and why enumeration is sorted rather than import-ordered), a
    programmatic-usage section covering `run()`, the reporter, the cancel token and the result
    object, an inspector section covering coverage, failures, inventory and both browse methods with
    their required arguments, and an extension section showing how to add a third vendor by
    registering a descriptor.

    Run the runnable examples for real and paste the real output. Anything not run — anything needing
    a credential, a live vendor, or data that is not on this machine — must be labelled as not run,
    exactly as the sibling documents do. Never paste a credential value, and never illustrate one:
    every credential mention is a variable NAME.

    Update `example/acquisition.md`:
    - `## 核心概念`: correct the failure-manifest description. It has no reader inside quantlab —
      resume is driven entirely by watermark-sidecar presence — and it is kept because it is the
      crash-durable operator record, which the inspector now reads. Add that its write is now atomic.
    - `## 它是怎么工作的`: update the flow so it describes the reporter and the cancel token — where
      progress events are emitted, that the cancel check is `_attempt_batch`'s first statement and
      why that position rather than the input generator, and that a cancel is not a quota abort.
    - 例 1 and 例 2: these are the no-credential examples. Point them at the inspector as the better
      answer, and update the dry-run walkthrough so it no longer shows the coverage report being
      skipped for want of a key.
    - `## 怎么接入一个新厂商`: adding a vendor now also means registering a descriptor beside the
      class, and the section says where the vendor import goes and why it is not in the package
      `__init__`. Link to `example/registry.md`.

    Update `example/pageledger.md`'s sidecar-tree listing so its description of the failure manifest
    is still accurate and names the shared atomic writer. Add a registry row to `example/README.md`'s
    first-step reading table and a line to its extension list ("新增一个数据源 → registry.md").
    Update `README.md`'s entry-point section to describe the three ingest scripts as thin shells over
    the registry, keeping the environment-variable section's NAME-only discipline intact.
  </action>
  <verify>
    <automated>uv run python -c "import pathlib; p=pathlib.Path('example/registry.md'); assert p.exists() and len(p.read_text().splitlines()) > 120, 'registry.md is missing or a stub'; idx=pathlib.Path('example/README.md').read_text(); assert 'registry.md' in idx, 'registry.md is not linked from the index'; print('registry_doc_ok')"</automated>
    <fails_when>the line "registry_doc_ok" is absent, or an AssertionError reporting a missing, stub, or unlinked document</fails_when>
    <automated>uv run python -c "import pathlib,re; docs=[p for p in pathlib.Path('example').glob('*.md')]+[pathlib.Path('README.md')]; bad=[str(p) for p in docs for pat in (r'sk-[A-Za-z0-9]{16,}', r'api_key\s*[=:]\s*[\"\x27][A-Za-z0-9]{16,}') if re.search(pat, p.read_text())]; assert not bad, bad; print('no_credential_values_in_docs')"</automated>
    <fails_when>the line "no_credential_values_in_docs" is absent, or an AssertionError listing a document that contains something shaped like a key</fails_when>
    <automated>uv run pytest -q</automated>
    <fails_when>non-zero exit, or any failed/errored test — documentation edits must not disturb the suite</fails_when>
    <automated>OUT=$(env -u TIINGO_API_KEY -u APCA_API_KEY_ID -u APCA_API_SECRET_KEY uv run python ingest_us_equity.py --dry-run 2>&amp;1); printf '%s\n' "$OUT" | grep -q "coverage report" &amp;&amp; echo PHASE_GATE_OK</automated>
    <fails_when>the line PHASE_GATE_OK is absent — the phase's headline credential-free operator path no longer works, so the documentation describing it would be false</fails_when>
  </verify>
  <acceptance_criteria>
    - `example/registry.md` exists, is over 120 lines, is written in Chinese, and covers the
      descriptor, the capability list, credential status, `run()`, the reporter, the cancel token,
      the inspector and how to add a third vendor
    - `example/README.md` links `registry.md` from both the reading-order table and the extension list
    - `example/acquisition.md`'s core-concepts, flow, 例 1 / 例 2 and new-vendor sections all reflect
      the shipped behaviour
    - Every example output in `example/registry.md` is either real or explicitly labelled as not run
    - No document under `example/` or `README.md` contains anything shaped like a credential value
    - `README.md` describes the three ingest scripts as thin shells over the registry
    - `uv run pytest -q` reports 0 failed and the credential-free dry run still prints a coverage report
  </acceptance_criteria>
  <done>The registry layer has its own guide, every stale section of the acquisition documentation reflects what was built, and no document contains a credential value.</done>
</task>

<task type="auto">
  <name>Task 3: Mark 03.4-RESEARCH.md's open questions resolved</name>
  <files>.planning/phases/03.4-data-source-registry/03.4-RESEARCH.md</files>
  <read_first>
    - .planning/phases/03.4-data-source-registry/03.4-RESEARCH.md — the whole `## Open Questions`
      section (Q1-Q6) at `:1625`, and the `## Assumptions` table rows A4 and A5, both of which still
      end in "needs user confirmation"
    - .planning/phases/03.4-data-source-registry/03.4-CONTEXT.md — D-14's AMENDED block, which is
      what closed Q1 and A4
    - .planning/phases/03.4-data-source-registry/03.4-06-PLAN.md — the `<flagged_assumptions>` block,
      which records the SC-1 class-versus-string ruling that closed A5
  </read_first>
  <action>
    Annotate in place. Do NOT rewrite the questions, do not shorten them, do not delete them and do
    not renumber them: a research artifact records what was unknown at research time, and erasing
    that erases the reason the decision reads the way it does.

    Change the heading `## Open Questions` to `## Open Questions (RESOLVED)` and add one lead-in
    sentence beneath it — every question below was closed before planning finished, and each names
    the decision or plan that closed it.

    Append a `**Resolved:**` line as the last line of each question:

    - Q1 (does `run()` include the raw-to-Zarr conversion, or stop at raw) — closed by D-14's AMENDED
      block of 2026-09-08, developer-confirmed. `run()` is acquisition-only and stops at the raw
      parquet tier; the "confirm with the developer before planning" instruction was carried out. The
      conversion stays in the shells, and a separate registry-level call is the route if the console
      ever needs it. Implemented by plan `03.4-02`.
    - Q2 (how the config factory attaches to the descriptor) — closed by plan `03.4-02`, which takes
      the recommendation: a `functools.partial` over `stock_acquisition_config` with the vendor
      pinned, carried on the descriptor as `config_factory`, leaving the descriptor a pure frozen
      dataclass.
    - Q3 (do `_report_coverage`'s four log lines become events, stay logging, or both) — closed by
      plan `03.4-05`, which takes the recommendation: emit a `coverage` progress event AND keep all
      four log lines, so the console gets structured counts while a shell run's stderr is unchanged.
    - Q4 (where the descriptors are defined) — closed by plan `03.4-02`: beside their vendor class,
      which is why the vendor imports sit at the bottom of `registry.py`. Note that D-07's AMENDED
      block requires the same placement for an independent reason.
    - Q5 (`supported universe categories` semantics) — closed by plan `03.4-02`, which takes the
      recommendation: the field is advisory and defaults to all four categories rather than encoding
      a per-vendor distinction that does not exist yet.
    - Q6 (does `inventory()` traverse the raw tier, the Zarr tier, or both) — closed by plan
      `03.4-04`: both, reported separately, because they are different artefacts with different
      lifecycles.

    Then INSERT one paragraph immediately after the `## Assumptions` table — inserted beneath it, not
    edited into its cells — recording that its two rows marked "needs user confirmation" are settled.
    A4 is closed by D-14's AMENDED block (`run()` acquisition-only, developer-confirmed). A5 is closed
    by the ruling that SC-1's "no vendor named at the call site" forbids naming a vendor CLASS, not a
    vendor STRING, so `DataSourceRegistry.get("alpaca")` at the top of `ingest_alpaca.py` is in bounds
    and no `--source` flag is added — that being the merged CLI D-15 forbids. Point at
    `03.4-06-PLAN.md`'s `<flagged_assumptions>` block, where the ruling is recorded for reviewers.

    Every edit in this task is an insertion except the single heading line, and the diff is checked
    against that.
  </action>
  <verify>
    <automated>uv run python -c "import pathlib,re; s=pathlib.Path('.planning/phases/03.4-data-source-registry/03.4-RESEARCH.md').read_text(); assert '## Open Questions (RESOLVED)' in s, 'the Open Questions heading is not marked resolved'; sec=s.split('## Open Questions (RESOLVED)')[1].split('\n## ')[0]; n=len(re.findall(r'\*\*Resolved:\*\*', sec)); assert n == 6, f'expected one resolution marker per question, found {n}'; print('open_questions_closed')"</automated>
    <fails_when>the line "open_questions_closed" is absent, or an AssertionError fires — the heading was left unmarked, or the resolution-marker count is anything but 6, meaning a question was skipped or a marker landed outside the section</fails_when>
    <automated>git diff --numstat -- .planning/phases/03.4-data-source-registry/03.4-RESEARCH.md</automated>
    <fails_when>the output names any path other than 03.4-RESEARCH.md, or its deletions column (the second field) is greater than 1 — more than the single heading line was removed, so question text was rewritten or dropped instead of annotated</fails_when>
  </verify>
  <acceptance_criteria>
    - The section heading reads `## Open Questions (RESOLVED)` and carries a one-sentence lead-in
    - Each of Q1-Q6 ends with a `**Resolved:**` line naming the decision or plan that closed it
    - Q1 names D-14's AMENDED block, Q3 names plan 05, Q6 names plan 04, and Q2, Q4 and Q5 name plan 02
    - A paragraph beneath the `## Assumptions` table records A4 and A5 as settled, with A5 stating the
      vendor-CLASS-not-vendor-STRING reading of SC-1
    - The question text itself is untouched: the whole diff for this file has exactly one deleted line
  </acceptance_criteria>
  <done>The research artifact carries no unresolved open question, and every question names the decision or plan that closed it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → reader | A wrong instruction in a document becomes an implementation |
| documentation → published repository | Anything pasted into a document is public |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.4-07-01 | Information Disclosure | a credential value pasted into a worked example | high | mitigate | Every credential reference is a NAME; a verify command scans every `example/*.md` and `README.md` for key-shaped literals. This repository has a real leaked-key incident, and a document is as public as a source file. |
| T-03.4-07-02 | Repudiation | a fabricated example output | medium | mitigate | The directory's stated convention is enforced: run it and paste the real output, or label it as not run. |
| T-03.4-07-03 | Tampering | a whole-file ROADMAP rewrite destroying other phase sections | high | mitigate | Scoped edits only, with a verify command asserting the phase-section count and a diffstat bound. |
| T-03.4-07-04 | Repudiation | a superseded boundary rule left standing beside its replacement | high | mitigate | The verify command asserts the in-process rule is present and that no unlabelled subprocess instruction survives in the Phase 03.4 section; the same task also replaces the two other statements later amendments reversed (the `__init__.py` enumeration mechanism, the `_failures.json` resume-input claim). |
| T-03.4-07-05 | Repudiation | a research artifact rewritten rather than annotated | medium | mitigate | Task 3's edits are insertions except one heading line, bounded by a `git diff --numstat` deletions check — erasing what was unknown at research time erases the reason a decision reads the way it does. |
| T-03.4-07-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task in this plan; the phase adds zero packages. |
</threat_model>

<verification>
- The ROADMAP's Phase 03.4 boundary contract states the in-process rule, no superseded instruction
  survives unlabelled, and no other phase section changed
- The ROADMAP's `__init__.py` enumeration sentence and its `_failures.json` resume-input claim are
  both replaced by what the amendments established
- `03.4-RESEARCH.md`'s open questions are marked resolved, each naming its closer, with exactly one
  deleted line in the diff
- `example/registry.md` exists, is linked from the index, and covers the whole registry surface
- `example/acquisition.md`'s four stale sections reflect the shipped behaviour
- No document contains a credential value
- `uv run pytest -q` reports 0 failed and the credential-free dry run still prints a coverage report
</verification>

<success_criteria>
The documentation debt this phase created is closed: the roadmap describes the system that was built
rather than the three superseded statements it replaced, no research question is left presented as
open when a decision or a plan already closed it, the registry layer has the per-layer guide this
repo's convention requires, and every section of the acquisition guide that the phase invalidated has
been corrected.
</success_criteria>

<output>
Create `.planning/phases/03.4-data-source-registry/03.4-07-SUMMARY.md` when done
</output>
