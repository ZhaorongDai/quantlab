---
status: complete
phase: 03.4-data-source-registry
source: [03.4-VERIFICATION.md]
started: 2026-09-09T16:55:00Z
updated: 2026-09-09T15:25:00Z
---

## Current Test

[testing complete]

## Tests

### 1. 对真实 Tiingo / Alpaca 跑一次实际回填，观察配额、404 与进度行为
expected: 批次边界取消、配额中止、失败记录的行为与 mock 套件断言的一致
why_human: 本阶段每一个 acquisition 测试都跑在 FakeTiingoClient 或 socket tripwire 之上；这套测试从未发出过一次真实厂商请求
result: pass
retested: "用户确认重测通过（quick task 260909-idh 修复后）"
prior_result: issue
prior_reported: "Tiingo 侧 20 符号与 2000 符号 (--wait-for-quota) 两次真实回填均成功，no-data 标记、watermark 跳过、_failures.json 行为与 mock 断言一致。Alpaca 侧 uv run python ingest_alpaca.py --data-dir $SMOKE --symbols AAPL,MSFT,NVDA 报 0 succeeded / 3 failed (401 Unauthorized，凭证未导出)，随后脚本仍无条件进入 Zarr 转换并抛出未捕获 traceback: ValueError: StockDataset: no raw data for vendor 'alpaca' at frequency '1d' (ingest_alpaca.py:405)"
prior_severity: major

### 2. 对真实 store 执行 `ingest_us_equity.py --stamp-legacy-watermarks <date>`
expected: watermark 被打上；随后的 run 跳过这些符号
why_human: WINDOWS.md #11 仍为 open。写入路径在仓库内未被驱动过，仍是 quick task 260906-26o 传下来的阻塞性人工检查点
result: pass
evidence: |
  3 个被人为降级（删去 start_date）的 sidecar 被 stamp 为 start_date=2024-01-01；
  已记录 start 的 sidecar 保持 2016-01-01 未被覆盖；no_data: true 标记原样带过；零价格请求。
  跳过半边由零请求 --dry-run 的覆盖分类确认：
  窗口 2024-01-01..2024-03-31 -> legacy 归零、already covered 40 (would be skipped)；
  窗口 2016-01-01..           -> 那 3 个判为 re-fetch, widened: 3（stamp 的 start 晚于请求起点），
  即 stamp 不谎报比它声明更宽的覆盖。

### 3. 用一个解析结果为空的 roster 调用 shell
expected: 在构造任何 acquisition 之前 `parser.error` 拒绝
why_human: WINDOWS.md #10 仍为 open。分支存在且顺序正确（`ingest_us_equity.py:470`，在唯一那个 `run(SOURCE, ...)` 之前），但没有测试驱动它
result: pass
evidence: |
  `uv run python ingest_us_equity.py --limit 0` -> exit=2，
  "us_all resolved to zero symbols over 2016-01-01..2026-09-09"，
  在任何 acquisition 构造与 run(SOURCE, ...) 之前退出。

### 4. `--limit N` 的 roster 解析是否可复现（UAT 期间发现）
expected: 同一组参数每次解析出同一批符号，因而第二次 run 能跳过第一次已覆盖的符号
result: pass
retested: "用户确认重测通过（quick task 260909-idh 修复后）"
evidence: |
  三次同参数 --dry-run --limit 20 返回完全一致的 preview ['A','AA','AAAP','AAC-U',...]。
  真取一次后重跑同一命令：'Resume: skipping 20/20 symbols already covering
  2024-01-01..2024-03-31; 0 remaining.'，进度条未出现，0 succeeded / 0 failed，零请求。
  另有 6 个 'queried, no data' 标记被正确区分于 _failures.json 并同样跳过。"
prior_result: issue
prior_reported: "重复执行 `--limit 20 --start-date 2024-01-01 --end-date 2024-03-31` 从不跳过任何符号。dry-run 显示这 20 个 already covered: 0 / would fetch 20/20，且 DFDV/GROM/PNW/CMPR/DH 均无 sidecar；而磁盘上 100 个 last_date=2024-03-31 的 sidecar 属于 ABT/AIFF/ALCYF 等另一批符号。连续两次相同参数的 dry-run 返回完全不同的 preview。"
prior_severity: major

## Summary

total: 4
passed: 4
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- gap_id: G-03.4-1
  status_note: "RECONCILED MANUALLY. No phase *-PLAN.md carries this gap_id — the fix shipped
    through quick task 260909-idh, which lives in .planning/quick/ and is invisible to
    verify-work's automatic gap reconciliation. Evidence: 260909-idh-SUMMARY.md, commits
    1d71521 (guard + --to-zarr opt-in on all three shells) and cf215bc (the guard was placed
    one line after the construction that densifies, so it was unreachable; moved ahead of it,
    mutation-verified test added). Orchestrator re-ran the original reproduction: exits 1 with
    the guard's message, no traceback. Full suite 745 passed. Awaiting human re-confirmation
    via test 1."
  resolved_by: "quick/260909-idh"
  resolved_at: 2026-09-09
  truth: "真实厂商回填下，批次边界取消、配额中止、失败记录的行为与 mock 套件断言一致"
  status: resolved
  reason: "User reported: Tiingo 侧全部符合预期；Alpaca 侧在全部符号取数失败 (401 Unauthorized) 后，ingest_alpaca.py 仍无条件执行 Zarr 转换并抛出未捕获 ValueError traceback，而非干净退出。"
  severity: major
  test: 1
  root_cause: "ingest_alpaca.py:403-405 的非 tick 分支无条件执行 StockDataset(ds_config).from_raw_data().save()，对 result.succeeded 为空没有任何守卫。全部符号失败时 vendor 根目录下没有任何 raw shard，quantlab/dataset/stock.py:304 的 absent-root 分支抛 ValueError，以未捕获 traceback 形式冒出。对照 ingest_us_equity.py，那里 Zarr 转换是 --to-zarr 显式 opt-in，因而不会走到这条路径；ingest_alpaca.py 与 ingest_tiingo.py 都缺少同等开关与 zero-success 守卫。凭证 401 本身不是缺陷——失败记录行为（写 _failures.json、不落 watermark、下次重试）是正确的。"
  artifacts:
    - path: "ingest_alpaca.py"
      issue: "第 403-405 行无条件转换 Zarr，缺 zero-success 守卫，且无 --to-zarr opt-in"
    - path: "ingest_tiingo.py"
      issue: "第 192 行同一未加守卫的形状，同一潜在缺陷"
    - path: "quantlab/dataset/stock.py"
      issue: "第 304 行 absent-root ValueError 是正确的下层信号，但没有上层调用点把它翻译成干净退出"
  missing:
    - "在 ingest_alpaca.py / ingest_tiingo.py 的转换分支前加 zero-success 守卫：result.succeeded 为空且 raw 根目录无数据时，打印清晰说明并以非零码退出，不进入转换"
    - "考虑将 Zarr 转换改为与 ingest_us_equity.py 一致的 --to-zarr opt-in，消除三个 ingest 脚本之间的行为分歧"
  debug_session: ""

- gap_id: G-03.4-2
  status_note: "RECONCILED MANUALLY — same reason as G-03.4-1. Evidence: commit eebf2b3 sorts
    both UniverseCatalog queries after .unique(), so roster order is a function of the
    membership set rather than the parquet layout. Orchestrator re-ran the reproduction: three
    consecutive identical --limit 20 dry-runs returned the identical sorted preview
    ['A','AA','AAAP',...]. Awaiting human re-confirmation via test 4."
  resolved_by: "quick/260909-idh"
  resolved_at: 2026-09-09
  truth: "同一组 CLI 参数每次解析出同一批符号；带 --limit 的第二次 run 跳过第一次已覆盖的符号"
  status: resolved
  reason: "User-observed: 重复执行 --limit 20 从不跳过任何符号。调查确认每次解析出的是不同的任意 20 个符号，因而 watermark 永远对不上。"
  severity: major
  test: 4
  root_cause: "quantlab/acquisition/universe.py:1587 (get_symbols_in_range) 与 :2355 (get_symbols_as_of) 都以 `matched.select(\"symbol\").unique().collect()[\"symbol\"].to_list()` 收尾。Polars 的 .unique() 默认 maintain_order=False，在多线程 collect 下每次返回顺序不同，因此 quantlab/utils/cli.py:resolve_symbols 里的 `symbols[:limit]` 每次截取的是任意的另一批符号。--limit 的 help 文本承诺的是 \"the first N resolved symbols\"，而这个稳定顺序并不存在。项目内已有 maintain_order=True 的用法 (quantlab/base/acquisition.py:1099)，这两处只是漏了。"
  artifacts:
    - path: "quantlab/acquisition/universe.py"
      issue: "第 1587 行 get_symbols_in_range 的 .unique() 未固定顺序"
    - path: "quantlab/acquisition/universe.py"
      issue: "第 2355 行 get_symbols_as_of 的 .unique() 同一构造，同一问题"
    - path: "quantlab/utils/cli.py"
      issue: "resolve_symbols 的 symbols[:limit] 依赖一个不存在的稳定顺序；--limit help 文本承诺 \"first N\""
  missing:
    - "两处 .unique() 改为顺序确定的形式（maintain_order=True，或收尾加 sort），使同一 config 每次解析出同一 roster —— CLAUDE.md 的可复现性硬约束"
    - "加一个测试：同一参数两次解析 roster，断言结果逐元素相等"
  影响面: "全量回填（不带 --limit）内容正确，集合相同仅顺序不定；带 --limit 时 resume/skip 失效，冒烟测试每次都在下载新符号。Test 2 的 pass 不受影响 —— 那次验证走的是全量 roster 的 dry-run。"
  debug_session: ""
