---
phase: 03.4-data-source-registry
plan: 09
type: execute
wave: 9
depends_on: ["03.4-08"]
gap_closure: true
files_modified:
  - .planning/ROADMAP.md
  - .planning/WINDOWS.md
  - example/acquisition.md
  - example/registry.md
autonomous: true
requirements: [D-18]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The reader set written into every document is the set `grep -rn \"read_failure_manifest()\" quantlab/` returns at the time of writing, enumerated from source in this plan rather than carried forward from the sentence being replaced. Gap 2 exists because a count was inherited on faith; a plan that inherits it again re-creates the gap (D-18)."
    - "`.planning/ROADMAP.md`'s Phase 03.4 programmatic-entry-point bullet no longer asserts that no module beneath `quantlab/` reads the failure manifest. It states that the manifest is not RESUME input — resume is driven entirely by watermark-sidecar presence — and names its in-repo readers, both reaching it through the single tolerant reader `CoverageLedger.read_failure_manifest` (D-18 FACTUAL CORRECTION)."
    - "`example/acquisition.md`'s failure-manifest passage in `## 核心概念` makes the same correction, in Chinese, and no longer follows a no-reader claim with the naming of a reader two lines later."
    - "`example/registry.md`'s pitfall 11 makes the same correction. REVIEW WR-06 names it alongside `example/acquisition.md`, and it is a document this phase created, so leaving it would restate the falsehood gap 2 is about in the guide the phase added."
    - "Every passage describing the pre-write merge says it runs on every exit path, not on the cancel path — plan 03.4-08 moved the call, and a document describing the old call site would be a fresh instance of the same defect. There are FOUR such passages, not three: `example/acquisition.md` carries the claim twice, once in its `## 核心概念` failure-manifest bullet and again as a parenthetical on step ⑤ of the `## 它是怎么工作的` flow diagram."
    - "All three documents record what the result-object / manifest equality actually is: both sides are built from one dict at one point, so it is a receipt of joint assembly rather than a check, and the property that protects the operator is the manifest-contents check plan 03.4-08 pinned."
    - "REVIEW CR-01 is recorded in `.planning/WINDOWS.md`. The verifier found it lived only in the committed review document, which is not the tracking surface; it is entered against `quantlab/base/acquisition.py` and marked resolved by plan 03.4-08."
    - "Every other phase section, the roadmap overview, the phase list and the progress table are byte-unchanged — the ROADMAP is edited with scoped replacements, never rewritten whole."
    - statement: "No document contains a credential value; every credential mention is a variable NAME."
      verification: backstop
  prohibitions:
    - statement: "A reader count must not be written into a document unless a command run in this plan produced it. This is the exact defect being closed: plan 07's must-have specified a count that the same phase had already falsified, and the executor faithfully wrote it down."
      status: unresolved
      reason: "spec-less fallback: no SPEC.md and no REQ-ID probe for this phase; recalled from VERIFICATION.md gap 2, no wired check descriptor authored"
    - statement: "A statement must not be left standing beside its own contradiction. Two documents currently deny that any in-repo code reads the manifest and then name a reader two lines later; a partial fix that corrects one clause and leaves the other is the same defect in a smaller font."
      status: unresolved
      reason: "spec-less fallback: recalled in-prompt, no wired check descriptor authored"
    - statement: "Documentation must not print, echo or illustrate a credential value, even a fake-looking one. An example key in a document is the shape a real key gets pasted into, and this repository has a real leaked-key incident."
      status: unresolved
      reason: "spec-less fallback: recalled in-prompt, no wired check descriptor authored"
  artifacts:
    - path: .planning/ROADMAP.md
      provides: "a Phase 03.4 programmatic-entry-point bullet whose manifest clause matches the code, naming both in-repo readers"
      contains: "_merge_unattempted_failures"
    - path: example/acquisition.md
      provides: "a 核心概念 failure-manifest passage that states the reader set correctly, plus a flow diagram whose merge step is no longer annotated as cancel-branch-only"
      contains: "_merge_unattempted_failures"
    - path: example/registry.md
      provides: "a pitfall 11 that states the reader set correctly instead of denying that any in-repo code reads the manifest"
      contains: "_merge_unattempted_failures"
    - path: .planning/WINDOWS.md
      provides: "a durable ledger entry for REVIEW CR-01, resolved by plan 03.4-08"
      contains: "CR-01"
  key_links:
    - from: "quantlab/ source (the call sites of read_failure_manifest)"
      to: ".planning/ROADMAP.md, example/acquisition.md, example/registry.md"
      via: "an enumeration command run in task 1 whose output is what the documents state, checked by a verify that re-runs it and compares"
      pattern: "read_failure_manifest"
    - from: "example/acquisition.md and example/registry.md"
      to: "quantlab/base/coverage.py `CoverageLedger.read_failure_manifest`"
      via: "both guides name the single tolerant reader every call site goes through"
      pattern: "read_failure_manifest"
---

<objective>
Close VERIFICATION gap 2: stop three documents asserting a falsehood that this phase itself created,
and make the corrected statement checkable against source rather than asserted on faith.

Purpose: `.planning/ROADMAP.md` and `example/acquisition.md` both deny that anything beneath
`quantlab/` reads `_failures.json`, then name a reader two lines later. Two `quantlab/` modules read
it today and this phase added both — `SourceInspector.failures` and
`Acquisition._merge_unattempted_failures`, both through `CoverageLedger.read_failure_manifest`,
whose own docstring says so. `example/registry.md`'s pitfall 11 carries the same sentence; REVIEW
WR-06 names it beside `acquisition.md`, and it is a document this phase created. A fourth site sits
inside `example/acquisition.md` itself: step ⑤ of its flow diagram annotates the merge as
cancel-branch-only, which plan 03.4-08 falsifies.

The instructive part is HOW the defect got in: plan 07's own must-have specified the contradiction
verbatim, so the executor wrote exactly what it was asked to write. D-18's FACTUAL CORRECTION of
2026-09-08 was true when written, and plan 03.4-05 falsified it a few hours later by adding the
second reader. This plan therefore does not restate a count from any prior document. Task 1
enumerates the readers from source first and treats that output as authoritative, and the verify
re-runs the same enumeration and checks the documents against it — so the claim cannot go stale
silently a second time.

Plan 03.4-08 also moved the merge out of the cancel branch, so every passage describing a
cancel-path merge is stale for a second, independent reason. That is why this plan is sequenced
after it rather than beside it.

Output: a corrected ROADMAP bullet, corrected passages in both Chinese guides, and a WINDOWS.md
entry for CR-01 so the finding outlives the review document.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.4-data-source-registry/03.4-CONTEXT.md
@.planning/phases/03.4-data-source-registry/03.4-VERIFICATION.md
@.planning/phases/03.4-data-source-registry/03.4-REVIEW.md
@.planning/phases/03.4-data-source-registry/03.4-07-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols and files this plan CREATES or RENAMES. Everything else it touches already exists and is
listed in `files_modified`.

| Kind | Name | Location |
|------|------|----------|
| ledger entry (new) | a `.planning/WINDOWS.md` row for REVIEW CR-01, marked resolved by 03.4-08 | `.planning/WINDOWS.md` |

No new file, no new code symbol, no new test. Every path this plan writes to already exists and is
git-tracked. `SourceInspector.failures`, `Acquisition._merge_unattempted_failures` and
`CoverageLedger.read_failure_manifest` are pre-existing symbols the documents must NAME, not create.

<specless_probe_note>
**Recorded, visible skip.** The spec-less edge probe produced NO predicates for this plan, for the
same reason recorded in `03.4-08-PLAN.md`: phase 03.4 has no `REQUIREMENTS.md` REQ-IDs (its
requirements live as `03.4-CONTEXT.md` decisions D-01..D-20, and the ROADMAP's `**Requirements**:`
line is prose), and the phase has no `SPEC.md`, so there is no `## Edge Coverage` or
`## Prohibitions` section to lift and nothing to derive predicates from. `must_haves` are derived
from `03.4-VERIFICATION.md`'s gap 2 and from CONTEXT.md D-18. The `prohibitions` entries are
recorded `unresolved`: recalled in-prompt, not backed by a wired check descriptor.
</specless_probe_note>

<assumption_delta_decision>
- **Primary noun:** the failure manifest's in-repo reader (documented as singular today —
  "the one `SourceInspector` reads").
- **Decision:** `add-alongside`.
- **Rationale:** this is the one place in this run where the detector's singular-to-plural signal is
  real, though it fires on the DOCUMENTS rather than on the architecture. The code went from one
  reader to two when plan 03.4-05 added `_merge_unattempted_failures`; the documents never followed.
  The correction adds the second reader alongside the first and names the single tolerant reader
  both go through — it does not promote either to primary, and it changes no code. The advisory
  detector's own matched snippets ("not a second implementation", "bottom placement is also what
  lets…") remain false positives and drive nothing here; the plural is taken from the source
  enumeration in task 1, not from the detector.
</assumption_delta_decision>

<flagged_assumptions>
- **`example/registry.md` is included although VERIFICATION gap 2 names only two artifacts.**
  Gap 2's stated truth is that the manifest's reader set is stated correctly; REVIEW WR-06
  independently names `example/registry.md:656-658` as carrying the identical sentence, and that
  file was created by plan 03.4-07 in this same phase. Correcting two of the three sites would
  leave the phase shipping the falsehood in the guide it added, and any grep-based check of the
  corrected claim would find it. Scope judgement: in bounds as work strictly required to make gap
  2's closure verifiable, not as an adjacent improvement.
- **`.planning/phases/03.4-data-source-registry/03.4-CONTEXT.md` is NOT edited.** D-18's FACTUAL
  CORRECTION contains the sentence that later became false. It was accurate on 2026-09-08 and plan
  03.4-05 falsified it. It stays as the dated decision record, for the same reason plan 07
  annotated `03.4-RESEARCH.md`'s questions rather than rewriting them: erasing what was believed at
  decision time erases the reason the decision reads the way it does. The durable corrected
  statement is what this plan writes into the ROADMAP and the two guides.
- **`03.4-REVIEW.md` and `03.4-VERIFICATION.md` are NOT edited.** They are dated findings reports,
  and their descriptions of the defect are what makes this plan legible. WR-06's own suggested
  Chinese replacement text is a useful starting point for task 2 but is not binding — it predates
  plan 03.4-08's relocation and describes the merge as cancel-path, which task 2 must not repeat.
- **Documentation language.** `example/*.md` is Chinese by the convention `example/README.md`
  states; `.planning/*` and `README.md` are English. Task 2's replacements are written in Chinese to
  match their files.
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate the manifest's readers from source, correct the ROADMAP bullet, and file CR-01 in the defect ledger</name>
  <files>.planning/ROADMAP.md, .planning/WINDOWS.md</files>
  <precondition>`grep -rn "read_failure_manifest()" quantlab/` returns exactly two call sites — `quantlab/acquisition/inspector.py` inside `SourceInspector.failures`, and `quantlab/base/acquisition.py` inside `Acquisition._merge_unattempted_failures`. Plan ordering does not guarantee this: plan 03.4-08 edits the second call site's surroundings. If the command returns any other set, halt before editing any document and report what it returned — the reader set is the thing this task writes down, so a wrong precondition makes every edit below wrong.</precondition>
  <reversibility rating="reversible">Scoped text replacements in two `.planning/` files plus one ledger row; revertible by `git revert` with no effect on code or on-disk data.</reversibility>
  <read_first>
    - .planning/ROADMAP.md `:305-322` — the whole in-scope bullet beginning "A programmatic write
      entry point, and progress/cancellation to go with it", whose manifest clause is the defective
      site named by gap 2; and `:386-425`, the Phase 03.4 plan list, so the scoped edit can be
      distinguished from the plan-list rows already added by the planner
    - quantlab/acquisition/inspector.py `:160-190` — `SourceInspector.failures`, the first call site,
      as it stands after plan 03.4-08's docstring correction
    - quantlab/base/acquisition.py `:2027-2065` — `_merge_unattempted_failures`, the second call
      site, and `:1465-1490` — the relocated merge and the write it now precedes, so the document
      describes the call site as plan 03.4-08 left it
    - quantlab/base/coverage.py `:249-280` — `failure_manifest_path` and `read_failure_manifest`, the
      single tolerant reader both call sites go through, and the reasoning for why there is one
    - .planning/phases/03.4-data-source-registry/03.4-CONTEXT.md `:178-190` — D-18 and its FACTUAL
      CORRECTION, the decision this correction serves
    - .planning/phases/03.4-data-source-registry/03.4-VERIFICATION.md — gap 2 in the frontmatter
      `gaps:` list and the two paragraphs about it in `## Gaps Summary`
    - .planning/phases/03.4-data-source-registry/03.4-REVIEW.md — CR-01's full text, for the ledger
      entry's description
    - .planning/WINDOWS.md `:1-20` and its last rows — the frontmatter counters, the table shape and
      the ids already in use (1-12)
  </read_first>
  <action>
    ENUMERATE FIRST, EDIT SECOND. Run `grep -rn "read_failure_manifest()" quantlab/` and treat its
    output as the authoritative reader set. Do not write any reader count into any document that
    this command did not produce, and do not carry a count forward from the sentence being replaced
    or from `03.4-REVIEW.md`. That inheritance is precisely how this gap was created: plan 07's
    must-have named a count the same phase had already falsified, and the executor wrote it down
    faithfully. Record the command's output verbatim in the SUMMARY.

    Then edit `.planning/ROADMAP.md` with SCOPED replacements only — never rewrite the whole file,
    and touch no phase section other than 03.4.

    In the Phase 03.4 in-scope bullet about the programmatic write entry point, replace the clause
    asserting that no module beneath `quantlab/` reads the manifest, together with the clause after
    it that names a single reader. The replacement states three things, in this order:
    the manifest is NOT resume input, and resume is driven entirely by watermark-sidecar presence;
    it has the number of in-repo readers the enumeration returned, named — `SourceInspector.failures()`
    (the credential-free operator view) and `Acquisition._merge_unattempted_failures` (the pre-write
    merge that restores entries the run had no news about) — both reaching the file through the
    single tolerant reader `CoverageLedger.read_failure_manifest`; and it is kept because it is the
    crash-durable operator record, which is the part of the existing sentence that was always true
    and must survive. Mark the correction with the date and the closing plan, in the same style the
    section already uses for its other amendments.

    In the same bullet, the sentence naming the equality between `result.failures` and the manifest
    as the consistency this phase pins: keep it, and add what it is. `_run` builds both sides from
    one dict at one point, so the equality is a receipt that the two were assembled together rather
    than a check on either — it read True during verification directly on top of a manifest that had
    just been emptied. Name what plan 03.4-08 pinned instead: the manifest's contents surviving a
    quota abort on the default path, asserted by reading the file back from disk.

    Do not weaken, expand or reorder anything else in the section. Leave the boundary contract, the
    concurrency bullet, the `DataSourceRegistry` bullet, the vendor-dispatch bullet, the
    out-of-scope paragraph, the plan list and every other phase section alone.

    Finally, file REVIEW CR-01 in the defect ledger. The verifier recorded that it exists only in
    the committed review document, which is not the tracking surface. Append it with
    `gsd-tools windows append` against `quantlab/base/acquisition.py`, kind `deviation`, phase
    `03.4`, describing the defect as it was found — the merge was wired into the cancel branch only,
    so both quota-abort exits overwrote the previous run's manifest, and the D-18 agreement
    invariant could not catch it because both sides come from one dict — then mark it fixed with
    `gsd-tools windows fixed <id>`, using the id the append reported. Do not touch ids 1-12.
  </action>
  <verify>
    <automated>uv run python -c "import pathlib,subprocess; out=subprocess.run(['grep','-rn','read_failure_manifest()','quantlab/'],capture_output=True,text=True).stdout.splitlines(); assert len(out)==2, out; s=pathlib.Path('.planning/ROADMAP.md').read_text(); sec=s.split('### Phase 03.4:')[1].split('### Phase 4:')[0].split('**Plans:**')[0]; assert 'nothing under' not in sec, 'the no-reader claim survives in the Phase 03.4 section'; assert 'SourceInspector.failures' in sec, 'the ROADMAP does not name the first reader'; assert '_merge_unattempted_failures' in sec, 'the ROADMAP does not name the second reader'; assert 'CoverageLedger.read_failure_manifest' in sec, 'the ROADMAP does not name the single tolerant reader both go through'; print('roadmap_reader_set_correct')"</automated>
    <fails_when>the line "roadmap_reader_set_correct" is absent, or an AssertionError fires — a list of other than two grep lines means the reader set changed and the text written here is already wrong; "the no-reader claim survives" means the defective clause was edited around rather than replaced; a missing name means the corrected sentence does not actually enumerate what the grep returned</fails_when>
    <automated>uv run python -c "import pathlib; s=pathlib.Path('.planning/ROADMAP.md').read_text(); n=s.count('### Phase '); assert n == 11, f'phase section count is {n}, expected 11'; assert '### Phase 7: Testing' in s, 'the Phase 7 heading is gone'; print('roadmap_intact')"</automated>
    <fails_when>the line "roadmap_intact" is absent, or an AssertionError fires — a phase count other than 11 or a missing Phase 7 heading means a whole-file rewrite replaced the scoped edit and destroyed sections outside 03.4</fails_when>
    <automated>git diff --numstat -- .planning/ROADMAP.md</automated>
    <fails_when>the output names any path other than .planning/ROADMAP.md, or insertions plus deletions (the first two fields summed) exceeds 45 — this task rewrites one clause and one sentence inside a single bullet, so a larger churn means the edit reached outside the bullet</fails_when>
    <automated>uv run python -c "import pathlib,re; s=pathlib.Path('.planning/WINDOWS.md').read_text(); rows=[l for l in s.splitlines() if 'CR-01' in l]; assert rows, 'CR-01 is not in the ledger'; assert any('fixed' in r for r in rows), f'CR-01 is present but not marked fixed: {rows}'; print('cr01_filed')"</automated>
    <fails_when>the line "cr01_filed" is absent, or an AssertionError fires — a missing row means the finding still lives only in the review document; a row present but not marked fixed means the ledger claims an open defect that plan 03.4-08 already closed</fails_when>
  </verify>
  <acceptance_criteria>
    - The SUMMARY records the verbatim output of `grep -rn "read_failure_manifest()" quantlab/`, run
      in this task, and the reader count written into the ROADMAP equals the number of lines it
      returned
    - The Phase 03.4 section does not contain the substring `nothing under`
    - The Phase 03.4 section names `SourceInspector.failures`, `_merge_unattempted_failures` and
      `CoverageLedger.read_failure_manifest`
    - The Phase 03.4 section states that the manifest is not resume input and that resume is driven
      by watermark-sidecar presence
    - The Phase 03.4 section states that the result/manifest equality is built from one dict at one
      point, and names the manifest-contents check plan 03.4-08 pinned instead
    - The correction carries a date and names plan 03.4-09, in the section's existing amendment style
    - `.planning/ROADMAP.md` still contains exactly 11 `### Phase ` headings and the
      `### Phase 7: Testing & Code Quality` heading
    - `git diff -- .planning/ROADMAP.md` touches only the programmatic-entry-point bullet
    - `.planning/WINDOWS.md` contains a row naming CR-01, against
      `quantlab/base/acquisition.py`, phase `03.4`, status `fixed`, describing the cancel-branch-only
      wiring and the tautological invariant
    - Rows 1-12 of `.planning/WINDOWS.md` are byte-unchanged
  </acceptance_criteria>
  <done>The ROADMAP's manifest clause names the reader set a command run in this task produced, and CR-01 exists on the tracking surface rather than only in the review document.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the same statement in both Chinese guides</name>
  <files>example/acquisition.md, example/registry.md</files>
  <reversibility rating="reversible">Prose replacements in two documentation files; revertible by `git revert` with no code effect.</reversibility>
  <read_first>
    - example/acquisition.md `:180-196` — the whole failure-manifest bullet in `## 核心概念`,
      including the paragraph that denies an in-repo reader, the sentence naming one, the sentence
      describing the pre-write merge as belonging to the cancel path, and the atomic-write paragraph
      after them (which is correct and must survive)
    - example/acquisition.md `:258-280` — the ASCII flow diagram in `## 它是怎么工作的`, whose step ⑤
      is `_merge_unattempted_failures()` carrying a parenthetical that restricts it to the cancel
      branch. This is a FOURTH site with the same defect, in the same file, and plan 03.4-08's
      relocation falsifies it directly. Note that step ⑤ is already drawn AFTER the loop box, so its
      POSITION is already correct and only the parenthetical is wrong
    - example/registry.md `:645-660` — pitfall 11, which carries the same denial; and `:548-556` —
      the sentence asserting the result/manifest equality holds on every exit path including cancel
    - example/README.md — the stated convention that a pasted output was really produced or is
      explicitly labelled as not run, so no output is invented here
    - the verbatim `grep -rn "read_failure_manifest()" quantlab/` output recorded by task 1 — the
      authoritative reader set both guides must state
    - quantlab/base/acquisition.py `:1465-1490` — the relocated merge and the write it precedes, as
      plan 03.4-08 left them, so the guides describe the call site that exists
    - .planning/phases/03.4-data-source-registry/03.4-REVIEW.md WR-06 — its suggested Chinese
      replacement, useful as a starting point but written before the relocation, so its description
      of the merge as cancel-path must not be copied
  </read_first>
  <action>
    Both files are Chinese by the convention `example/README.md` states; the replacements are written
    in Chinese to match, in the surrounding passages' voice. Add no runnable example and paste no
    output — this task only corrects prose that is already there.

    In `example/acquisition.md`'s failure-manifest bullet under `## 核心概念`, replace the paragraph
    that denies any code beneath `quantlab/` reads the file, together with the sentence after it
    claiming the repository has a single reader. The two sit two lines apart and contradict each
    other, so replacing one and leaving the other reproduces the defect. The replacement states: the
    manifest is not resume input, and resume is driven entirely by watermark-sidecar presence; the
    repository has the readers task 1's enumeration returned — `SourceInspector.failures()` and
    `Acquisition._merge_unattempted_failures` — both reaching it through the single tolerant reader
    `CoverageLedger.read_failure_manifest`, because two tolerant readers would be two copies of the
    failure policy free to drift; and it is kept because it is the crash-durable operator record,
    since a dead process returns no `AcquisitionResult`.

    In the same bullet, correct the sentence attributing the pre-write merge to the cancel path.
    After plan 03.4-08 the merge is unconditional: it runs immediately before the write and covers
    every exit of the resume loop — cancel, an empty pending list, a completed run, `wait_for_quota`
    off, and `quota_max_waits` exhausted. Say what it restores (entries for symbols this run had no
    news about) and say what the resulting equality is: both sides are built from one dict at one
    point, so it is a receipt that the two were assembled together, not proof that either is right.
    Name the property that is the real protection and where it is pinned: after a quota abort on the
    default path the previous run's entries are still in the file, asserted by reading it back from
    disk in `tests/test_acquisition_progress.py`. Leave the atomic-write paragraph that follows
    untouched — it is correct and it is D-20's record.

    Then correct the SAME file's ASCII flow diagram in `## 它是怎么工作的`. Its step ⑤ names the merge
    and carries a parenthetical restricting it to the cancel branch; that parenthetical is what plan
    03.4-08 falsified. Do NOT move step ⑤ — it is already drawn after the loop box, between step ④
    and the manifest write at step ⑥, which is exactly where the call now lives. Replace only the
    parenthetical, with a note that the step runs on every exit of the loop. Change nothing else in
    the diagram: step ④'s description of the cancel break happening before the quota branch is still
    accurate and is D-17's record.

    In `example/registry.md`'s pitfall 11, apply the same correction in the pitfall's shorter form:
    keep its true point that the manifest is not resume input, replace the denial that any in-repo
    code reads it with the two named readers and the single tolerant reader they share, and keep the
    crash-durability rationale. Then update the sentence around `:553` asserting the equality holds
    on every exit path including cancel: it is still true and is now true for a better reason (the
    merge is unconditional), but add the same one-line note that it is a receipt of joint assembly
    rather than a check, and point at the manifest-contents regression.

    Never paste a credential value and never illustrate one: every credential mention in both files
    stays a variable NAME.
  </action>
  <verify>
    <automated>uv run python -c "import pathlib,subprocess; out=subprocess.run(['grep','-rn','read_failure_manifest()','quantlab/'],capture_output=True,text=True).stdout.splitlines(); assert len(out)==2, out; bad=[]; missing=[]; [ (bad.append(p) if '没有任何代码读' in pathlib.Path(p).read_text() else None) for p in ('example/acquisition.md','example/registry.md') ]; [ (missing.append(p) if not all(k in pathlib.Path(p).read_text() for k in ('SourceInspector.failures','_merge_unattempted_failures','CoverageLedger.read_failure_manifest')) else None) for p in ('example/acquisition.md','example/registry.md') ]; assert not bad, f'the no-reader claim survives in: {bad}'; assert not missing, f'the reader set is not fully named in: {missing}'; print('guides_reader_set_correct')"</automated>
    <fails_when>the line "guides_reader_set_correct" is absent, or an AssertionError fires — a grep result other than two lines means the reader set changed under the documents; "the no-reader claim survives" names a file where the denial was edited around rather than replaced; "the reader set is not fully named" names a file that omits one of the two readers or the shared tolerant reader. NOTE the asymmetry, so this gate is not read as stronger than it is: `example/acquisition.md` already contains all three identifiers today (two in the very passage being corrected, one in the flow diagram), so for that file only the negative arm discriminates; the positive arm is load-bearing for `example/registry.md`, where all three are currently absent</fails_when>
    <automated>uv run python -c "import pathlib; s=pathlib.Path('example/acquisition.md').read_text(); assert '仅取消分支内' not in s, 'the flow diagram still restricts the merge step to the cancel branch'; i=s.index('_merge_unattempted_failures()'); j=s.index('_write_failure_manifest()'); assert i in range(0,j), 'the merge step no longer precedes the manifest write in the flow diagram'; print('flow_diagram_corrected')"</automated>
    <fails_when>the line "flow_diagram_corrected" is absent, or an AssertionError fires — "still restricts the merge step" means the parenthetical plan 03.4-08 falsified survives in the diagram; "no longer precedes" means step ⑤ was moved, which it must not be (it was already in the right place)</fails_when>
    <automated>uv run python -c "import pathlib,re; docs=list(pathlib.Path('example').glob('*.md'))+[pathlib.Path('README.md')]; bad=[str(p) for p in docs for pat in (r'sk-[A-Za-z0-9]{16,}', r'api_key\s*[=:]\s*[\"\x27][A-Za-z0-9]{16,}') if re.search(pat, p.read_text())]; assert not bad, bad; print('no_credential_values_in_docs')"</automated>
    <fails_when>the line "no_credential_values_in_docs" is absent, or an AssertionError listing a document that contains something shaped like a key</fails_when>
    <automated>git diff --numstat -- example/acquisition.md example/registry.md</automated>
    <fails_when>the output names any path other than example/acquisition.md and example/registry.md, or either file's insertions plus deletions summed exceeds 55 — this task rewrites one bullet in each file plus two lines of `example/acquisition.md`'s flow diagram, so a larger churn means the edit reached beyond the failure-manifest passages</fails_when>
    <automated>uv run pytest -q</automated>
    <fails_when>non-zero exit; or the summary reports any failed or errored count; or fewer than 732 passed — documentation edits must not disturb the suite</fails_when>
  </verify>
  <acceptance_criteria>
    - Neither `example/acquisition.md` nor `example/registry.md` contains the Chinese clause denying
      that any code reads the manifest
    - Both files name `SourceInspector.failures`, `_merge_unattempted_failures` and
      `CoverageLedger.read_failure_manifest`
    - Both files state that the manifest is not resume input and that resume is driven by
      watermark-sidecar presence
    - `example/acquisition.md` describes the merge as running before the write on every exit of the
      resume loop, and names all five exits
    - `example/acquisition.md`'s flow-diagram step ⑤ no longer carries a parenthetical restricting
      the merge to the cancel branch, and still sits between step ④ and the manifest write at ⑥
    - The flow diagram's step ④ description of the cancel break preceding the quota branch is
      byte-unchanged
    - Both files state that the result/manifest equality is built from one dict at one point, and
      point at the manifest-contents regression in `tests/test_acquisition_progress.py` as the check
    - `example/acquisition.md`'s atomic-write paragraph is byte-unchanged
    - Both files remain in Chinese, matching their surrounding prose
    - No new runnable example and no pasted output is added by this task
    - No document under `example/` or `README.md` contains anything shaped like a credential value
    - `uv run pytest -q` reports 0 failed, 0 errored and at least 732 passed
  </acceptance_criteria>
  <done>Both Chinese guides state the manifest's reader set as the source enumeration returned it, and describe the merge as the unconditional pre-write step plan 03.4-08 made it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → reader | A wrong instruction in a document becomes an implementation; the ROADMAP is the contract the out-of-repo console is built against |
| documentation → published repository | Anything pasted into a document is as public as a source file |
| source enumeration → documentation | A count carried into a document without being measured goes stale silently — the mechanism that produced this gap |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.4-09-01 | Information Disclosure | a credential value pasted into a corrected passage or an illustrative manifest excerpt | high | mitigate | Every credential reference stays a variable NAME; no example output is added by this plan at all, and a verify command scans every `example/*.md` and `README.md` for key-shaped literals. This repository has a real leaked-key incident. |
| T-03.4-09-02 | Information Disclosure | a vendor-identifying or endpoint-revealing string introduced while describing the manifest's contents | medium | mitigate | The corrected passages name method and module identifiers only. No URL, no request shape, no manifest sample is added; `_scrub` remains the sole egress choke point and this plan adds no path to it. |
| T-03.4-09-03 | Repudiation | a reader count restated on faith and stale again | high | mitigate | Task 1 enumerates from source before editing and records the output in the SUMMARY; both tasks' verify commands re-run the enumeration and assert the documents name exactly what it returned, so the claim is checked against code rather than asserted. |
| T-03.4-09-04 | Repudiation | a false statement left standing beside its replacement | high | mitigate | Each verify negative-greps the defective clause and positively asserts all three identifiers, so a partial correction that adds the truth without removing the falsehood fails. |
| T-03.4-09-05 | Tampering | a whole-file ROADMAP rewrite destroying other phase sections | high | mitigate | Scoped `Edit` replacements only, with a phase-heading count assertion, a Phase 7 heading assertion, and a diffstat bound of 45. |
| T-03.4-09-06 | Repudiation | a defect that exists only in a findings document and is never tracked | medium | mitigate | CR-01 is appended to `.planning/WINDOWS.md` and marked fixed, so it survives the review document that currently holds it alone. |
| T-03.4-09-SC | Tampering | npm/pip/cargo installs | high | accept | No package-manager install task in this plan; it adds zero packages and does not touch `pyproject.toml` or `uv.lock`. |
</threat_model>

<verification>
- `grep -rn "read_failure_manifest()" quantlab/` returns exactly two call sites, and all three
  documents name exactly those two readers plus the single tolerant reader they share
- No document still denies that an in-repo module reads the manifest
- All three documents describe the merge as the unconditional pre-write step, not a cancel-path one
- All three documents record that the result/manifest equality is a receipt of joint assembly, and
  point at the manifest-contents regression as the check
- `.planning/ROADMAP.md` still has 11 phase sections and its Phase 7 heading; only the
  programmatic-entry-point bullet changed
- `.planning/WINDOWS.md` carries CR-01, marked fixed, with rows 1-12 unchanged
- No document contains a credential value
- `uv run pytest -q` reports 0 failed, 0 errored and at least 732 passed
</verification>

<success_criteria>
Every document in this repository that describes who reads `_failures.json` states the set that
`grep -rn "read_failure_manifest()" quantlab/` returns, measured in this plan rather than inherited,
and describes the pre-write merge as plan 03.4-08 left it. No sentence denies a reader it then names
two lines later, and the finding that produced this correction is recorded on the defect ledger
rather than only in a review document.
</success_criteria>

<output>
Create `.planning/phases/03.4-data-source-registry/03.4-09-SUMMARY.md` when done
</output>
