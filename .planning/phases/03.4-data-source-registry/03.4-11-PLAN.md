---
phase: 03.4-data-source-registry
plan: 11
type: execute
wave: 11
depends_on: ["03.4-10"]
gap_closure: true
files_modified:
  - .planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/manifest_semantics_scan.py
  - .planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/scan-allowlist.txt
  - .planning/phases/03.4-data-source-registry/manifest-sentence-ledger.tsv
  - .planning/STATE.md
autonomous: true
requirements: [D-18]

estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`manifest_sentence_audit.py --check` 是一条**证明过能红**的 verify 命令，不是一张回执：三个 mutation 分别打中它的三条判定臂 —— 把一句已改写的话换回退役原文（`UNADJUDICATED` + `VANISHED`）、写进一句词表从没见过的新措辞（`UNADJUDICATED`）、把一行判读从 `OK` 翻成 `STALE`（`STALE`）。每个 mutation 之后仓库都恢复原状（D-18）。"
    - "三个 mutation 里的第二个同时证明了**为什么换工具**：那句全新措辞在 `manifest_semantics_scan.py` 的字面量词表上是**绿**的，在句子级候选集枚举上是**红**的。这不是推论，是同一棵工作树上两条命令的两个退出码。"
    - "`manifest_semantics_scan.py` 的 PATTERNS 从 11 条扩到 16 条，新增的 P11..P13 逐条从 `manifest-sentence-retired.tsv` 的 `retired_sentence` 列**派生**，P14..P15 由 03.4-VERIFICATION.md 的 `missing` 逐字点名。模块 docstring 写明了每一条的来源，以及 P00..P10 是抄来的这件事本身就是缺陷（D-18）。"
    - "`manifest_semantics_scan.py` 的模块 docstring 写明它是**从属**于 `manifest_sentence_audit.py` 的快速 tripwire：候选集枚举是权威，字面量列表只是让已知退役措辞的重现变便宜。它的 PATTERNS 只能从退役记录派生，不能从任何人手写的坏句子清单派生。"
    - "`scan-allowlist.txt` 在扩词表之后重新生成，且双向 diff 在修好的树上是绿的 —— 扫描输出与 allowlist 逐行相等，两个方向都没有多余项。"
    - "扩词表之后的字面量扫描仍然**能红**：把退役记录里的一条原文粘回 `example/registry.md`，双向 diff 转红并点名新增的那条 pattern。"
    - "「先句子级扫描拿候选集 → 再从候选集定词表」这个顺序被写进 `.planning/STATE.md`，作为本阶段的决策记录留在下一次有人碰这块文档时会读到的地方（D-18）。"
    - "写完 STATE.md 之后 `--check` 仍然退出 0：新写进范围内文件的任何句子，如果成了候选句就必须在同一个任务里被判读。gate 是活的，不是一次性的。"
    - statement: "`quantlab/` 下没有任何改动；全量套件仍是 734 passed / 0 failed。"
      verification: backstop
  prohibitions:
    - statement: "新增的 pattern 不得从「这次发现的五个站点」直接抄写。合法输入只有一个：`manifest-sentence-retired.tsv` 的 `retired_sentence` 列 —— 也就是**上一份计划逐句判读的产物**。这条区分不是形式主义：前三次闭环漏掉的，恰恰是「没人想到过的措辞」，而一份从已知清单倒推出来的词表按定义看不见它。"
      status: unresolved
      reason: "spec-less fallback：本阶段在 REQUIREMENTS.md 中无 REQ-ID，需求面是 CONTEXT.md 的 D-01..D-20；本条由 03.4-VERIFICATION.md 的 gap 抬升，未编写 wired check descriptor"
    - statement: "`scan-allowlist.txt` 不得被当成橡皮图章。它只能由**修好之后**的树上实跑的扫描输出生成，且生成后必须逐行读一遍：allowlist 里的每一行都是一条「我知道它在那儿、而且它是对的」的声明。把一条真命中塞进 allowlist 让 gate 变绿，等于把 gate 关掉。"
      status: unresolved
      reason: "spec-less fallback：由 03.4-VERIFICATION.md 对上一轮 allowlist 的评述抬升，未编写 wired check descriptor"
    - statement: "mutation 之后必须把工作树恢复原状，且恢复要用 `git checkout --` 之类的确定性手段，不得靠手工把改动改回去。一个手工改回去的 mutation 会在字符层面留下差异，让下一条 gate 的红绿失去意义。"
      status: unresolved
      reason: "spec-less fallback：由 planning context 的 failing_direction_contract 抬升，未编写 wired check descriptor"
    - statement: "不得发生任何行为变更，`quantlab/` 与 `tests/` 下一个字节都不改。本计划只碰两个验收脚本、一份 allowlist、一份判读账本和 `.planning/STATE.md`。"
      status: unresolved
      reason: "spec-less fallback：由 planning context 的 out_of_scope 抬升；由全量套件与 `git status --porcelain` 的洁净树门槛承接"
    - statement: "不得顺手修用户已明确 defer 的 WR-01..WR-05、IN-03、IN-04，也不得改动 03.4-10 已经改好的五处表述。"
      status: unresolved
      reason: "spec-less fallback：由 planning context 的 out_of_scope 抬升，未编写 wired check descriptor"
  artifacts:
    - path: .planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/manifest_semantics_scan.py
      provides: "扩到 16 条的字面量 tripwire，模块 docstring 记录每条 pattern 的来源与它对句子级审计的从属关系"
      contains: "manifest_sentence_audit"
    - path: .planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/scan-allowlist.txt
      provides: "扩词表之后在修好的树上重新生成的双向 diff 基准"
      contains: "|P"
    - path: .planning/STATE.md
      provides: "把「先候选集、后词表」的顺序记成本阶段的决策，写在下一次碰这块文档的人会读到的地方"
      contains: "manifest-sentence-retired.tsv"
    - path: .planning/phases/03.4-data-source-registry/manifest-sentence-ledger.tsv
      provides: "把 STATE.md 新写入文本引入的候选句（若有）判读进账本，使 `--check` 保持绿"
      contains: "OK"
  key_links:
    - from: ".planning/phases/03.4-data-source-registry/manifest-sentence-retired.tsv"
      to: "manifest_semantics_scan.py 的 PATTERNS P11..P13"
      via: "逐条派生 —— 退役记录的 retired_sentence 列是新增 pattern 的唯一合法输入"
      pattern: "retired_sentence"
    - from: "manifest_semantics_scan.py"
      to: "manifest_sentence_audit.py"
      via: "模块 docstring 声明前者是后者的从属 tripwire，权威在候选集枚举一侧"
      pattern: "manifest_sentence_audit"
    - from: "扩词表之后的扫描输出"
      to: "scan-allowlist.txt"
      via: "双向 grep -Fxv diff，两个方向都必须为空"
      pattern: "scan-allowlist"
---

<!-- planner-discipline-allow: lie about what the last run -->
<!-- planner-discipline-allow: 上一次 run 的 -->

<objective>
把 03.4-10 交付的那条 gate 从「看起来能红」变成「证明过能红」，并把字面量扫描降级成一条
**从属**的快速 tripwire —— 它的词表从此只能从逐句判读的产物派生，不能从任何人手写的
坏句子清单派生。

Purpose：03.4-VERIFICATION.md 的 `missing` 里有两条是关于工具而不是关于文本的：
扩 `manifest_semantics_scan.py` 的 PATTERNS、同步 `scan-allowlist.txt`。这两条如果单独做，
就是这个缺陷第 5 次复发的标准姿势 —— 前三次闭环每一次都扩过词表，每一次都漏掉了
「没人想到过的措辞」。所以本计划把它们排在 03.4-10 之后，并且把输入锁死在
`manifest-sentence-retired.tsv` 上：新增的 pattern 逐条来自**上一份计划逐句判读出来的**
退役句子，这个派生方向在文件系统上是可读的。

本计划最有说服力的一步是 Task 1 的第二个 mutation：往 `example/registry.md` 里写一句
词表从没见过的、断言 per-run 语义的新话，然后在**同一棵工作树**上跑两条命令 ——
字面量扫描绿、句子级审计红。这不是论证，是两个退出码。它是「为什么必须换工具」的
唯一一份可执行证据，也是这个 gap 第 4 次复发的根因说明（D-18）。

Output：三个 mutation 的红绿记录、一份扩到 16 条且写明来源的 pattern 列表、一份重新生成的
allowlist，以及一条写进 `.planning/STATE.md` 的方法论决策。
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03.4-data-source-registry/03.4-VERIFICATION.md
@.planning/phases/03.4-data-source-registry/03.4-10-PLAN.md
@.planning/phases/03.4-data-source-registry/03.4-10-SUMMARY.md
@.planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/manifest_semantics_scan.py
</context>

## Artifacts this phase produces

| 路径 | 新建 / 改写 | 符号 / 内容 |
|---|---|---|
| `.planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/manifest_semantics_scan.py` | 改写 | `PATTERNS` 列表由 11 条扩到 16 条（新增 `P11`..`P15`）；模块 docstring 新增一段「Where these patterns come from」与一段「Subordinate to the sentence-level audit」 |
| `.planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/scan-allowlist.txt` | 改写 | 在修好的树上用扩词表后的扫描器重新生成的全量输出 |
| `.planning/STATE.md` | 追加一条 | Phase 03.4 的方法论决策条目：候选集在前、词表在后 |
| `.planning/phases/03.4-data-source-registry/manifest-sentence-ledger.tsv` | 改写 | 为 STATE.md 新增文本引入的候选句（若有）追加判读行 |

<tasks>

<task type="auto">
  <name>Task 1: 三个 mutation 证明句子级 gate 真能红，并当场证明字面量扫描看不见其中一个</name>

  <read_first>
  - `.planning/phases/03.4-data-source-registry/manifest_sentence_audit.py` —— `--check` 的三条判定臂（`UNADJUDICATED` / `VANISHED` / `STALE`）就是本任务要逐个打中的靶子
  - `.planning/phases/03.4-data-source-registry/manifest-sentence-retired.tsv` —— mutation A 的输入：`path` 为 `quantlab/base/coverage.py` 那一行的 `retired_sentence` 列
  - `.planning/phases/03.4-data-source-registry/manifest-sentence-ledger.tsv` —— mutation C 要翻的那一行判读
  - `.planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/manifest_semantics_scan.py` 的 `PATTERNS` 列表（11 条）—— mutation B 的对照组必须跑的是**未扩词表前**的这一版
  </read_first>

  <files>无文件被永久改动 —— 本任务只做三轮「改一下 / 跑 gate / git checkout 恢复」，交付物是记录在 SUMMARY 里的六个退出码</files>

  <action>
  三个 mutation 逐个做，每个做完立刻用 `git checkout -- <path>` 恢复，再做下一个。每一轮都
  先记下 `git status --porcelain` 为空，做完恢复后再确认它重新为空 —— 否则后面那些 gate 的
  红绿就是在一棵被污染的树上跑出来的，什么也证明不了。

  **Mutation A —— 把一句已改写的话换回退役原文。**
  从 `manifest-sentence-retired.tsv` 里取 `path` 为 `quantlab/base/coverage.py` 的那一行，
  用它的 `retired_sentence` 列的内容，把 `quantlab/base/coverage.py` 里
  `CoverageLedger.read_failure_manifest` 的 docstring 摘要行整行换回去。
  用退役记录做输入、而不是手打，本身就是在验证退役记录可用 —— 这是它存在的理由之一。
  跑 `--check`：必须退出 1，必须同时出现一条命名 `quantlab/base/coverage.py` 的
  `UNADJUDICATED` 行（换回去的旧句子在账本里已经没有行了）和一条 `VANISHED` 行
  （改写后那句话的 digest 不再被枚举出来）。
  `git checkout -- quantlab/base/coverage.py` 恢复。

  **Mutation B —— 写一句词表从没见过的新措辞（本任务的要害）。**
  在 `example/registry.md` 的 `SourceInspector` 一节末尾追加一句独立的新话，逐字为：
  `注意：\`_failures.json\` 在每次 run 结束后只保留这一轮的结果。`
  这句话在语义上是**假**的（它正是被 CR-01 作废的那个说法），但它的措辞不在
  `manifest_semantics_scan.py` 的 11 条 pattern 里、也不在 `manifest-sentence-retired.tsv` 的
  任何一行里 —— 它是「没人想到过的措辞」的一个真实样本。
  在**同一棵工作树**上跑两条命令，两个结果都要记进 SUMMARY：
  ① 未扩词表的 `manifest_semantics_scan.py` + 双向 allowlist diff → **绿**（它看不见）；
  ② `manifest_sentence_audit.py --check` → **红**，出现一条命名 `example/registry.md` 的
  `UNADJUDICATED` 行（它看得见）。
  这两个退出码就是「为什么把验收方式从字面量匹配换成候选集枚举」的全部论证。
  `git checkout -- example/registry.md` 恢复。

  **Mutation C —— 把一行判读从 `OK` 翻成 `STALE`。**
  在 `manifest-sentence-ledger.tsv` 里任选一行 verdict 为 `OK` 的，改成 `STALE`。
  跑 `--check`：必须退出 1 并打印一条 `STALE` 行。
  `git checkout -- .planning/phases/03.4-data-source-registry/manifest-sentence-ledger.tsv` 恢复。

  三轮做完后再跑一次 `--check` 确认回到绿，并确认 `git status --porcelain` 为空。
  本任务不留下任何文件改动 —— 它的交付物是 SUMMARY 里那张六个退出码的表。
  </action>

  <acceptance_criteria>
  - Mutation A 期间 `--check` 退出码为 1，stdout 同时含以 `UNADJUDICATED` 开头且第 3 列为 `quantlab/base/coverage.py` 的行、以及以 `VANISHED` 开头且第 3 列为 `quantlab/base/coverage.py` 的行
  - Mutation B 期间：`manifest_semantics_scan.py` 的双向 allowlist diff 两个方向输出均为空（绿）；同一时刻 `--check` 退出码为 1 且 stdout 含以 `UNADJUDICATED` 开头、第 3 列为 `example/registry.md` 的行
  - Mutation C 期间 `--check` 退出码为 1，stdout 含以 `STALE` 开头的行
  - 三轮全部恢复后：`git status --porcelain` 输出为空，`--check` 退出 0 且 stdout 唯一一行以 `AUDIT GREEN:` 开头
  - 全程 `quantlab/` 下无永久改动（提交前后都成立的检查）：`grep -c "CROSS-RUN record, not a report on the most recent run" quantlab/base/coverage.py quantlab/acquisition/inspector.py` 两个文件各为 1
  </acceptance_criteria>

  <verify>
    <automated>git status --porcelain; echo "TREE_MUST_BE_CLEAN_ABOVE"</automated>
    <fails_when>`TREE_MUST_BE_CLEAN_ABOVE` 之前出现任何一行 —— 每一行都是一个没被 `git checkout --` 恢复干净的 mutation 残留</fails_when>
  </verify>
  <verify>
    <automated>uv run python .planning/phases/03.4-data-source-registry/manifest_sentence_audit.py --check; echo "EXIT=$?"</automated>
    <fails_when>输出里出现 `EXIT=1` 或 `AUDIT RED:`，或缺少以 `AUDIT GREEN:` 开头的行 —— 三个 mutation 之后 gate 必须回到绿，否则说明恢复不完整</fails_when>
  </verify>
  <verify>
    <automated>uv run pytest -q 2>&1 | tail -3</automated>
    <fails_when>输出里没有 `734 passed`，或出现 `failed`/`error` 字样</fails_when>
  </verify>

  <done>三个 mutation 各自打中一条判定臂并留下退出码记录；Mutation B 的「字面量绿 / 候选集红」对照在同一棵工作树上取得；三轮之后工作树干净、gate 回到 `AUDIT GREEN:`。</done>
</task>

<task type="auto">
  <name>Task 2: 从退役记录派生 P11..P15，重生成 allowlist，并证明扩词表后的 tripwire 仍能红</name>

  <read_first>
  - `.planning/phases/03.4-data-source-registry/manifest-sentence-retired.tsv` —— 新增 pattern 的**唯一合法输入**，逐行读它的 `retired_sentence` 列
  - `.planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/manifest_semantics_scan.py` —— 现有模块 docstring 里「Order is the contract: the index IS the `Pnn` label」那条约定必须遵守：只许追加，不许重排
  - `.planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/scan-allowlist.txt` —— 现有 7 行；扩词表后必然变化，本任务重新生成它
  - `.planning/phases/03.4-data-source-registry/03.4-VERIFICATION.md` 的 `gaps[].missing` 第 4 条 —— 逐字点名了 P14/P15 这两条 prophylactic pattern
  </read_first>

  <files>.planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/manifest_semantics_scan.py, .planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/scan-allowlist.txt</files>

  <action>
  **① 追加五条 pattern。** 在 `PATTERNS` 列表末尾按顺序追加（索引接着 P10 往下，不重排既有项）：
  `[Tt]he last run's` 作为 P11、`上一次\s*run\s*的` 作为 P12、
  `lie about what the last run` 作为 P13、`上一轮的\s*_failures` 作为 P14、
  `describes? the last run` 作为 P15。每条后面用行尾注释标出它的**来源**：
  P11 派生自退役记录里 `quantlab/base/coverage.py` 与 `quantlab/acquisition/inspector.py` 两行，
  P12 派生自 `example/registry.md` 那一行，
  P13 派生自 `quantlab/base/acquisition.py` 与 `tests/test_tiingo_quota.py` 两行，
  P14 与 P15 不派生自任何退役行，是 03.4-VERIFICATION.md 的 `missing` 逐字点名的预防性条目。

  **② 改模块 docstring，把这份词表的身份写清楚。** 新增两段：
  一段标题为 `Where these patterns come from`，说明 P00..P10 是从上一版 VERIFICATION 报告里
  列的 6 个变体**原样抄过来**的、而那 6 个从来不是穷举 —— 这件事本身就是这个缺陷第 4 次复发的
  根因，写在这里是为了让下一个想「再加两条」的人先停一下；P11..P13 派生自
  `.planning/phases/03.4-data-source-registry/manifest-sentence-retired.tsv`；
  P14..P15 是 VERIFICATION 点名的预防性条目。并写明规则：以后新增 pattern 的合法输入
  只有退役记录，也就是逐句判读的产物。
  另一段标题为 `Subordinate to the sentence-level audit`，说明本脚本是快速 tripwire，
  权威在 `.planning/phases/03.4-data-source-registry/manifest_sentence_audit.py` 的
  候选集枚举一侧；本脚本绿而事实为假是**可能且已经发生过**的（03.4-11 Task 1 的
  Mutation B 在同一棵工作树上同时取得了这两个退出码），所以它绿不构成任何完备性主张。

  **③ 重生成 `scan-allowlist.txt`。** 在修好的树上跑扩词表后的扫描器，把全量输出写进
  allowlist。写完**逐行读一遍**：allowlist 里的每一行都是一条「我知道它在那儿、而且它是对的」
  的声明。如果某一行你说不出它为什么该被放行，那它就不该被放行 —— 去修那处文本，
  而不是把它留在 allowlist 里。

  **④ 证明扩词表后的 tripwire 仍能红。** 从 `manifest-sentence-retired.tsv` 取
  `path` 为 `example/registry.md` 那一行的 `retired_sentence`，把它作为一句新话粘进
  `example/registry.md`，跑双向 allowlist diff：必须转红，且红出来的那一行的 `Pnn` 是新增的
  P12。然后 `git checkout -- example/registry.md` 恢复，再跑一次双向 diff 确认回到绿。
  </action>

  <acceptance_criteria>
  - `grep -c '^\s*r"' .planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/manifest_semantics_scan.py` 为 16（原 11 条 + 新增 5 条）
  - 该文件的模块 docstring 里出现 `manifest_sentence_audit` 与 `manifest-sentence-retired.tsv` 两个字符串各至少 1 次
  - 该文件里出现 `P11`..`P15` 的来源注释，且 `grep -c 'manifest-sentence-retired' ` 该文件 ≥ 2
  - 扫描器仍能跑：`uv run python ... manifest_semantics_scan.py` 退出 0
  - 双向 diff 在修好的树上两个方向输出均为空
  - Mutation（把退役原文粘回 `example/registry.md`）期间双向 diff 非空，且非空的那一行含 `|P12|`
  - Mutation 恢复后 `git status --porcelain` 只列出本任务真正要改的两个文件
  - `.planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/scan-allowlist.txt` 的每一行都是 `path|Pnn|count` 形状（`grep -cv '^[^|]\+|P[0-9][0-9]|[0-9]\+$'` 为 0）
  </acceptance_criteria>

  <verify>
    <automated>uv run python .planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/manifest_semantics_scan.py | grep -Fxv -f .planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/scan-allowlist.txt; echo "EXTRA_ABOVE_MUST_BE_EMPTY"</automated>
    <fails_when>`EXTRA_ABOVE_MUST_BE_EMPTY` 之前出现任何 `path|Pnn|count` 行 —— 每一行都是一处 allowlist 没有认领的命中</fails_when>
  </verify>
  <verify>
    <automated>uv run python .planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/manifest_semantics_scan.py | grep -Fxv -f - .planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/scan-allowlist.txt; echo "VANISHED_ABOVE_MUST_BE_EMPTY"</automated>
    <fails_when>`VANISHED_ABOVE_MUST_BE_EMPTY` 之前出现任何行 —— 每一行都是 allowlist 声称存在、实际已消失的命中（反方向 diff 失效意味着「悄悄删掉一条退役声明」不会被发现）</fails_when>
  </verify>
  <verify>
    <automated>grep -c '^\s*r"' .planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/manifest_semantics_scan.py; grep -c 'manifest-sentence-retired' .planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/manifest_semantics_scan.py; grep -c 'manifest_sentence_audit' .planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/manifest_semantics_scan.py</automated>
    <fails_when>第一行不是 `16`（11 表示 pattern 根本没加，其它数表示加漏或加多），或第二行小于 `2`（派生来源没写进注释与 docstring），或第三行为 `0`（从属关系没声明，下一个人会把这份词表当成权威）</fails_when>
  </verify>
  <verify>
    <automated>grep -cv '^[^|]\+|P[0-9][0-9]|[0-9]\+$' .planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/scan-allowlist.txt</automated>
    <fails_when>输出不是 `0` —— 非零表示 allowlist 里混进了不是 `path|Pnn|count` 形状的行（注释、空行、手写说明），而双向 `grep -Fxv` diff 会把它们当成永远对不上的项</fails_when>
  </verify>

  <done>PATTERNS 为 16 条且每条新增项标注了来源；模块 docstring 声明了派生规则与对句子级审计的从属关系；allowlist 在修好的树上重新生成且双向 diff 为空；粘回退役原文能让双向 diff 在 P12 上转红，恢复后回绿。</done>
</task>

<task type="auto">
  <name>Task 3: 把「候选集在前、词表在后」写进 STATE.md，并让 gate 在写完之后仍然绿</name>

  <read_first>
  - `.planning/STATE.md` 的 Phase 03.4 决策条目段（约 195-215 行）—— 体例是每条一行、破折号后接理由；新条目照这个体例写
  - `.planning/phases/03.4-data-source-registry/03.4-10-SUMMARY.md` —— 取实际的候选句数目与文件数写进条目，不要沿用规划阶段的探针数字（55 / 14）除非它们逐字相同
  - `.planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/manifest_semantics_scan.py` 扩词表后的 `PATTERNS` —— 新写的 STATE.md 文本不得命中其中任何一条
  </read_first>

  <files>.planning/STATE.md, .planning/phases/03.4-data-source-registry/manifest-sentence-ledger.tsv</files>

  <action>
  在 `.planning/STATE.md` 的 Phase 03.4 决策条目段末尾追加一条，内容为：
  失败清单的文档契约改用**句子级语义求交**验收 —— 语义类词表只负责撒网，判定权在
  `manifest-sentence-ledger.tsv` 的逐句判读里；理由是同一形状在本阶段复发四次，
  前三次每一次都是从一份已知坏句子清单倒推 pattern 列表，而漏掉的恰恰是没人想到过的措辞；
  第四次改成先枚举候选集（manifest 语义类词 × run-scope 语义类词，实测 N 句 / M 个文件，
  N 与 M 取 03.4-10-SUMMARY 里的实际数字）再逐句判读，一次就找齐了三轮没找齐的三处；
  字面量扫描 `manifest_semantics_scan.py` 从此是从属的快速 tripwire，其 pattern 只能从
  `manifest-sentence-retired.tsv` 派生。条目里要点名
  `manifest_sentence_audit.py` 与 `manifest-sentence-retired.tsv` 两个路径，
  这样下一个碰这块文档的人能直接找到工具。

  写的时候有一条约束：这段文本本身**不得命中**扩词表后的 16 条 pattern 中的任何一条 ——
  否则它会自己制造一处 allowlist 命中。写完立刻跑一次扫描器的双向 diff 确认没有新增行。

  然后跑 `manifest_sentence_audit.py --check`。`.planning/STATE.md` 在这个工具的扫描范围里，
  所以新写的文本如果同时命中 manifest 类词与 run-scope 类词，就会成为一条新候选句、被打成
  `UNADJUDICATED`。如果发生了：读那一句，判读它，以 verdict=`OK` 追加进
  `manifest-sentence-ledger.tsv`，再跑一次 `--check` 到绿。
  如果没发生（新文本没同时命中两类词），也不要为了「演示」硬造一句 ——
  在 SUMMARY 里如实写「本次 STATE.md 追加未产生新候选句」即可。
  这一步的意义在于 gate 是**活的**：范围内文件新写进去的任何句子都要经过同一道判读，
  而不是这道 gate 只对 2026-09-09 那一天的文本有效。
  </action>

  <acceptance_criteria>
  - `grep -c 'manifest_sentence_audit.py' .planning/STATE.md` ≥ 1 且 `grep -c 'manifest-sentence-retired.tsv' .planning/STATE.md` ≥ 1
  - 新增条目以 `- [Phase 03.4]:` 开头，与该段既有条目体例一致
  - 扫描器双向 diff 在两个方向上均为空（新写的 STATE.md 文本没有制造新的字面量命中）
  - `uv run python ... manifest_sentence_audit.py --check` 退出 0，stdout 唯一一行以 `AUDIT GREEN:` 开头并以 `0 STALE` 结尾
  - `--report | wc -l` 不低于 03.4-10-SUMMARY 记录的候选句数
  - 03.4-10 改好的五处表述一处未动（提交前后都成立的检查）：`grep -c "CROSS-RUN record, not a report on the most recent run" quantlab/base/coverage.py quantlab/acquisition/inspector.py` 两个文件各为 1，`grep -c "当前已知仍在失败" example/registry.md` 为 1
  </acceptance_criteria>

  <verify>
    <automated>uv run python .planning/phases/03.4-data-source-registry/manifest_sentence_audit.py --check; echo "EXIT=$?"</automated>
    <fails_when>输出里出现 `EXIT=1`、`AUDIT RED:`，或任何以 `UNADJUDICATED`/`VANISHED`/`STALE` 开头的行 —— 若出现 `UNADJUDICATED` 且第 3 列为 `.planning/STATE.md`，说明新写的条目引入了候选句但没被判读进账本</fails_when>
  </verify>
  <verify>
    <automated>uv run python .planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/manifest_semantics_scan.py | grep -Fxv -f .planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/scan-allowlist.txt; uv run python .planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/manifest_semantics_scan.py | grep -Fxv -f - .planning/quick/260909-174-fix-six-stale-documentation-and-docstrin/scan-allowlist.txt; echo "BOTH_DIFFS_ABOVE_MUST_BE_EMPTY"</automated>
    <fails_when>`BOTH_DIFFS_ABOVE_MUST_BE_EMPTY` 之前出现任何 `path|Pnn|count` 行 —— 出现 `.planning/STATE.md|Pnn|` 说明新写的条目自己命中了一条退役措辞的 pattern</fails_when>
  </verify>
  <verify>
    <automated>grep -c 'manifest_sentence_audit.py' .planning/STATE.md; grep -c 'manifest-sentence-retired.tsv' .planning/STATE.md</automated>
    <fails_when>任一行输出为 `0` —— 条目没有点名工具路径，下一个读到它的人得靠翻 git log 才能找到这两个文件</fails_when>
  </verify>
  <verify>
    <automated>grep -c "CROSS-RUN record, not a report on the most recent run" quantlab/base/coverage.py quantlab/acquisition/inspector.py; grep -c "当前已知仍在失败" example/registry.md</automated>
    <fails_when>前两行不是 `quantlab/base/coverage.py:1` 与 `quantlab/acquisition/inspector.py:1`，或第三行不是 `1` —— 说明 03.4-10 改好的表述在本计划里被动过。这条检查在提交前后都成立，不像未固定 revision range 的工作树比对那样在提交后变空</fails_when>
  </verify>
  <verify>
    <automated>uv run pytest -q 2>&1 | tail -3</automated>
    <fails_when>输出里没有 `734 passed`，或出现 `failed`/`error` 字样</fails_when>
  </verify>

  <done>STATE.md 多出一条点名两个工具路径、体例一致、且不命中任何一条字面量 pattern 的方法论条目；两条 gate（字面量双向 diff、句子级 `--check`）在写完之后都是绿的；`quantlab/`、`example/`、`tests/` 下一个字节未改。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 验收 gate → 后续所有关于「文档与代码一致」的判断 | 一条能绿但不能红的 gate 比没有 gate 更危险：它把「没检查」伪装成「检查过了」。本计划三个 mutation 存在的全部理由就是守住这条边界 |
| `manifest_semantics_scan.py` → `git ls-files` 子进程 | 既有执行面，本计划只改它的 PATTERNS 与 docstring，不改子进程调用形状 |
| `scan-allowlist.txt` → gate 的判定结果 | allowlist 是唯一能让一条真命中变绿的地方，也就是这条 gate 唯一的被绕过点 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.4-11-01 | Repudiation | `scan-allowlist.txt` 被当成橡皮图章 | medium | mitigate | allowlist 只许由修好之后的树上实跑输出生成（Task 2 ③），生成后逐行读一遍；行形状被 `grep -cv '^[^\|]\+\|P[0-9][0-9]\|[0-9]\+$'` 为 0 钉住，杜绝混进手写说明行；反方向 diff 让「悄悄删掉一条声明」也转红 |
| T-03.4-11-02 | Tampering | mutation 之后的工作树残留 | medium | mitigate | 每个 mutation 用 `git checkout -- <path>` 恢复，三轮之后以 `git status --porcelain` 为空作为门槛；Task 1 的第一条 verify 就是这个 |
| T-03.4-11-03 | Spoofing | 一条只能绿、不能红的 gate 冒充有效检查 | high | mitigate | 三个 mutation 分别打中 `--check` 的三条判定臂并记录退出码；Mutation B 额外在同一棵树上取得「字面量绿 / 候选集红」的对照，使从属关系有可执行证据而不是声明 |
| T-03.4-11-04 | Tampering | `manifest_semantics_scan.py` 的 `PATTERNS` 索引即标签 | low | mitigate | 既有 docstring 的「Order is the contract，append only」约定在 Task 2 ① 被逐字遵守；重排会让 allowlist 里所有 `Pnn` 标签整体错位，把一份有效 allowlist 变成噪声 |
| T-03.4-11-05 | Information Disclosure | STATE.md 新增条目 | low | mitigate | 条目只写方法论与路径，不粘贴任何失败 reason 样例、不出现任何凭证值 |
| T-03.4-11-SC | Tampering | npm/pip/cargo 供应链 | low | accept | 本计划没有任何 package-manager 安装任务，也不新增依赖；扩的是既有脚本的正则常量。Package Legitimacy Gate 无输入 |

**为什么这一份的威胁面比 03.4-10 高一档：** 03.4-10 改的是文本，03.4-11 改的是**判断文本对错的东西**。
T-03.4-11-03 定为 high 是因为它的失效模式正是本阶段已经发生过三次的那一种 ——
一条绿着的检查，覆盖面比所有人以为的窄。security_block_on = high，所以这条威胁的缓解
（三个 mutation 的退出码记录）是本计划的阻塞项，不是可选项。
</threat_model>

<verification>
1. Task 1 的六个退出码在 SUMMARY 里成表：Mutation A（`UNADJUDICATED` + `VANISHED`）、Mutation B（字面量绿 / 候选集红）、Mutation C（`STALE`）
2. `grep -c '^\s*r"' .../manifest_semantics_scan.py` 为 16
3. 双向 allowlist diff 两个方向均为空
4. `manifest_sentence_audit.py --check` 退出 0，唯一一行以 `AUDIT GREEN:` 开头、以 `0 STALE` 结尾
5. `uv run pytest -q` → `734 passed`，0 failed
6. 03.4-10 改好的五处表述一处未动：`grep -c "CROSS-RUN record, not a report on the most recent run" quantlab/base/coverage.py quantlab/acquisition/inspector.py` 两个文件各为 1，`grep -c "当前已知仍在失败" example/registry.md` 为 1
</verification>

<success_criteria>
- 句子级 gate 的三条判定臂各有一个 mutation 打中并留下退出码
- 「为什么换工具」有一份同树对照证据（字面量绿 / 候选集红），而不是一句论断
- 字面量词表扩到 16 条，新增项逐条标出来源，派生方向锁死在退役记录上
- allowlist 在修好的树上重新生成，双向 diff 为空，且能被退役原文重新粘回而转红
- 方法论顺序写进 STATE.md，且新写的文本自己通过了这两道 gate
</success_criteria>

<output>
Create `.planning/phases/03.4-data-source-registry/03.4-11-SUMMARY.md` when done.

SUMMARY 必须含一张 mutation 表，逐行记：mutation 名、改了什么、跑了哪条命令、退出码、
打印出的判定臂。Mutation B 那一行要把两条命令的两个退出码并排放在一起 ——
它是本阶段第 4 次复发的根因说明，也是下一次有人想「再加两条 pattern 就够了」时唯一拦得住他的东西。
</output>
