---
phase: 03.4-data-source-registry
plan: 07
subsystem: docs
tags: [documentation, roadmap, research, chinese, registry, inspector, credentials]

requires:
  - phase: 03.4-data-source-registry
    provides: "03.4-02's DataSourceRegistry/SourceDescriptor/Capability/run() and the bottom-of-registry.py vendor imports; 03.4-03's quantlab/utils/atomic.py:write_json_atomically; 03.4-04's CoverageLedger + credential-free SourceInspector; 03.4-05's ProgressEvent/ProgressReporter/CancelToken and the widened run() signature; 03.4-06's three thin shells and the literal `  coverage report:` header"
  - phase: 03.2-multi-source-acquisition
    provides: "the Acquisition engine example/acquisition.md documents — _run/_run_once/_attempt_batch, the watermark sidecars, the failure manifest, the quota and rate-limit policy"
provides:
  - "example/registry.md — the registry layer's Chinese guide (658 lines): descriptor, capability list, credential status, run(), reporter, cancel token, SourceInspector, and how to register a third vendor"
  - ".planning/ROADMAP.md Phase 03.4 with all three superseded statements replaced — the subprocess write rule (D-12), the `quantlab/acquisition/__init__.py` enumeration mechanism (D-07 AMENDED), the `_failures.json`-as-resume-input claim (D-18) — plus the concurrency answer (D-20) and the D-14 acquisition-only amendment"
  - "03.4-RESEARCH.md's `## Open Questions (RESOLVED)` — Q1-Q6 each annotated with its closer, A4/A5 recorded as settled, question text untouched"
  - "example/acquisition.md's core-concepts, flow, 例 1 / 例 2 and new-vendor sections rewritten for the registry, the reporter and the cancel token"
  - "example/pageledger.md's sidecar-tree note naming the shared atomic writer and the four writers that delegate to it"
  - "README.md's entry-point section describing the three ingest scripts as thin shells over the registry, with ingest_us_equity.py listed for the first time"
affects: [03.3, 04, quantlab-console]

actuals:
  tokens: 19540
  tasks: 3
  commits: 3
plan_head_before: 1cd6b8f67be764b3d105d87ac36a4dcfc39d4197

tech-stack:
  added: []
  patterns:
    - "A superseded rule is referred to through ONE labelled sentinel phrase, and the mechanism it describes is otherwise worded so the sentinel gate cannot fire on accurate prose"
    - "A pasted example output is re-RUN and replaced WHOLE, never patched line-by-line — a block mixing an old run with one new line is the fabrication the directory's convention forbids"
    - "A grep-shaped acceptance criterion is checked for whether it DISCRIMINATES before it is trusted; where it does not, the finding is recorded rather than worked around silently"

key-files:
  created:
    - example/registry.md
  modified:
    - .planning/ROADMAP.md
    - .planning/phases/03.4-data-source-registry/03.4-RESEARCH.md
    - example/acquisition.md
    - example/pageledger.md
    - example/chunking.md
    - example/README.md
    - README.md
    - quantlab/acquisition/inspector.py

key-decisions:
  - "The retired write contract is referred to ONLY through the labelled phrase `the subprocess-CLI rule it supersedes`; both mechanisms are otherwise described as running `in a child process`, so the sentinel gate catches an unlabelled superseded INSTRUCTION without firing on accurate description"
  - "Seven of example/registry.md's eight worked examples were really executed with all three credential env vars unset; the eighth (console-side run() with a live cancel) is explicitly labelled as not run"
  - "example/acquisition.md's dry-run block was re-run and replaced WHOLE rather than having its last line patched — patching would have produced a block mixing a 2026-09-07 run with one 2026-09-09 line"
  - "Two stale statements outside the plan's files_modified were FIXED, not logged: quantlab/acquisition/inspector.py's module docstring and example/chunking.md's pasted dry-run output both asserted the deleted no-key skip line as current behaviour"
  - "Task 3's `**Resolved:**` count is non-discriminating (it counts the lead-in sentence's own mention); the lead-in was reworded so the plan's command passes as written, and a discriminating list-item form was additionally run and recorded"

patterns-established:
  - "Every ROADMAP statement a CONTEXT.md amendment reversed is rewritten in the same commit as the amendment's own closing plan, with a verify asserting both the replacement's presence and the superseded rule's absence"
  - "A research artifact's open questions are ANNOTATED, never rewritten: the whole-file diff carries exactly one deleted line (the heading), bounded by a `git diff --numstat` deletions check"

requirements-completed: [D-12, D-13, D-15, D-19, D-20]

coverage:
  - id: D1
    description: "The ROADMAP's Phase 03.4 boundary contract states the IN-PROCESS write rule, is marked SETTLED, and refers to the retired rule only through the single labelled sentinel phrase — with the honest accounting of what was given up (process isolation, the exported-command guarantee) kept"
    requirement: "D-12"
    verification:
      - kind: other
        ref: "uv run python -c '<section split; assert IN-PROCESS present; assert SUBPROCESS absent once the sentinel phrase is stripped>' -> boundary_rewritten"
        status: pass
      - kind: other
        ref: "regex count over the Phase 03.4 section: subprocess=1, sentinel=1 — the single occurrence IS the sentinel"
        status: pass
    human_judgment: false
  - id: D2
    description: "No other phase section was disturbed: 11 phase headings survive, `### Phase 7: Testing & Code Quality` is intact, and every diff hunk lands inside the Phase 03.4 section"
    verification:
      - kind: other
        ref: "uv run python -c '<assert s.count(\"### Phase \") >= 11 and Phase 7 heading present>' -> roadmap_intact"
        status: pass
      - kind: other
        ref: "git diff --numstat -- .planning/ROADMAP.md -> 57 insertions + 18 deletions = 75 (plan bound: <= 140); git diff -U0 hunk headers all within old lines 289-348, i.e. inside 250-390"
        status: pass
    human_judgment: false
  - id: D3
    description: "The concurrency bullet records that the two sidecar writes were NOT atomic, now are through one shared writer, that no lock was added, and keeps the shell-versus-console overlap visible as a bounded residual risk"
    requirement: "D-20"
    verification:
      - kind: other
        ref: "section scan: 'quantlab/utils/atomic.py:write_json_atomically' present, 'adds NO lock' present, 'Check whether watermark/sidecar writes are already atomic' absent"
        status: pass
    human_judgment: false
  - id: D4
    description: "The `DataSourceRegistry` bullet states that the vendor imports live at the bottom of `registry.py` and that `quantlab/acquisition/__init__.py` STAYS EMPTY, with the volume-guard erosion reason and the child-process completeness test — while keeping the unchanged cold-import intent (D-07 AMENDED)"
    verification:
      - kind: other
        ref: "uv run python -c '<assert \"importing every vendor module so\" absent and \"STAYS EMPTY\" present>' -> amendments_propagated"
        status: pass
    human_judgment: false
  - id: D5
    description: "The programmatic-entry-point bullet records D-14's acquisition-only amendment and no longer calls `_failures.json` a resume input; it states that nothing under `quantlab/` reads the manifest and that resume is watermark-sidecar driven"
    verification:
      - kind: other
        ref: "uv run python -c '<assert \"both the crash-durable record and resume input\" absent and \"NOT resume input\" present>' -> amendments_propagated"
        status: pass
    human_judgment: false
  - id: D6
    description: "The vendor-dispatch bullet records that `ingest_us_equity.py` is NOT redundant with `ingest_tiingo.py --universe us_all`, names the five axes they differ on, and states that all three shells are kept and all three are thin (including `ingest_tiingo.py`, rerouted in Wave 6)"
    requirement: "D-15"
    verification:
      - kind: other
        ref: "section scan: 'is NOT redundant' + 'All\\nTHREE shells are therefore KEPT' present; five differences enumerated"
        status: pass
    human_judgment: false
  - id: D7
    description: "The explicitly-out-of-scope paragraph is byte-unchanged: long-task isolation, the log sink and same-source concurrency control all still belong to the console (D-13 / D-19)"
    requirement: "D-13"
    verification:
      - kind: other
        ref: "git diff -U0 -- .planning/ROADMAP.md — no hunk touches the out-of-scope paragraph (old lines 326-331)"
        status: pass
    human_judgment: false
  - id: D8
    description: "`03.4-RESEARCH.md`'s Open Questions heading is marked `(RESOLVED)`, each of Q1-Q6 carries a `**Resolved:**` line naming its closer, and the question text is untouched — exactly one deleted line in the whole-file diff"
    verification:
      - kind: other
        ref: "uv run python -c '<assert heading marked; assert 6 **Resolved:** markers in section>' -> open_questions_closed"
        status: pass
      - kind: other
        ref: "discriminating form: 6 markers matching '^\\s+- \\*\\*Resolved:\\*\\* ' against 6 questions matching '^\\d+\\. \\*\\*' -> one_resolution_list_item_per_question"
        status: pass
      - kind: other
        ref: "git diff --numstat -> 51 insertions, 1 deletion (the heading line only)"
        status: pass
    human_judgment: false
  - id: D9
    description: "A paragraph beneath the Assumptions table records A4 and A5 as settled, A5 stating the vendor-CLASS-not-vendor-STRING reading of SC-1 and pointing at 03.4-06-PLAN.md's flagged_assumptions block"
    verification:
      - kind: other
        ref: "file scan: 'The two rows still marked \"needs user confirmation\" (A4, A5) are SETTLED.' present, followed by both bullets"
        status: pass
    human_judgment: false
  - id: D10
    description: "`example/registry.md` exists, is 658 lines of Chinese (4,553 CJK characters), is linked from `example/README.md`'s reading-order table AND its extension list, and covers the descriptor, the capability list, credential status, `run()`, the reporter, the cancel token, the inspector and how to add a third vendor"
    verification:
      - kind: other
        ref: "uv run python -c '<assert exists and > 120 lines and linked from index>' -> registry_doc_ok"
        status: pass
      - kind: other
        ref: "uv run python -c '<CJK count; assert every surface keyword present>' -> surface_covered (CJK chars: 4553, lines: 658)"
        status: pass
    human_judgment: false
  - id: D11
    description: "Seven of registry.md's eight worked examples were really executed on this machine with TIINGO_API_KEY / APCA_API_KEY_ID / APCA_API_SECRET_KEY all explicitly unset, with their real output pasted; the eighth is labelled 此例未实际运行"
    verification:
      - kind: other
        ref: "each of 例 1-7 run via `env -u TIINGO_API_KEY -u APCA_API_KEY_ID -u APCA_API_SECRET_KEY uv run python ...` and its stdout pasted verbatim; two elided tracebacks are annotated as elided"
        status: pass
    human_judgment: true
    rationale: "That each pasted block IS the output of the command printed above it is a property of how this plan was executed, not something any in-repo assertion re-checks. A reviewer can re-run all seven — every one is credential-free and offline — but nothing pins them against drift, so a later code change could silently make a paste stale exactly as it did to example/chunking.md's."
  - id: D12
    description: "No document under `example/` or `README.md` contains anything shaped like a credential value; every credential mention is a variable NAME"
    verification:
      - kind: other
        ref: "uv run python -c '<regex sweep for sk-[A-Za-z0-9]{16,} and api_key=\"...\" over example/*.md + README.md>' -> no_credential_values_in_docs"
        status: pass
    human_judgment: false
  - id: D13
    description: "`example/acquisition.md`'s core-concepts section states the failure manifest has no reader inside quantlab, that resume is watermark-sidecar driven, that it is kept as the crash-durable operator record the inspector reads through the one tolerant reader, and that its write is now atomic"
    requirement: "D-19"
    verification:
      - kind: other
        ref: "section scan: '它不是续跑输入' + 'CoverageLedger.read_failure_manifest' + 'write_json_atomically' present in 核心概念"
        status: pass
    human_judgment: false
  - id: D14
    description: "`example/acquisition.md`'s flow section describes the reporter-and-cancel-aware chain — where each event is emitted, that `_should_stop()` is `_attempt_batch`'s first statement and why that position rather than the input generator, and that a cancel is not a quota abort"
    verification:
      - kind: other
        ref: "flow diagram now emits run_started / batch_completed / quota_exhausted / cancelled / run_finished, shows the cancel break BEFORE the quota branch, and the _attempt_batch box reads `if self._should_stop()` with the OR spelled out"
        status: pass
      - kind: other
        ref: "the 并发/失败隔离/断点 table's 进度条 row became a 进度 row plus a new 取消 row"
        status: pass
    human_judgment: false
  - id: D15
    description: "例 1 and 例 2 point at the inspector as the better answer to the no-credential question they pose, and 例 1's dry-run walkthrough shows a real coverage report instead of the skip line"
    requirement: "D-15"
    verification:
      - kind: e2e
        ref: "env -u TIINGO_API_KEY -u APCA_API_KEY_ID -u APCA_API_SECRET_KEY uv run python ingest_us_equity.py --dry-run -> PHASE_GATE_OK; whole block re-pasted verbatim (13,729 symbols, coverage report printed)"
        status: pass
      - kind: other
        ref: "grep 'skipped (export' over example/ -> no match remains"
        status: pass
    human_judgment: false
  - id: D16
    description: "`example/acquisition.md`'s new-vendor section says adding a vendor now also means registering a descriptor beside the class, states that the vendor import goes at the BOTTOM of registry.py and not in the package __init__, and links example/registry.md"
    verification:
      - kind: other
        ref: "section renamed 还要做的两件配套事 -> 三件, with the register_source snippet, the bottom-import instruction and its reason, and a registry.md link"
        status: pass
    human_judgment: false
  - id: D17
    description: "`example/pageledger.md`'s sidecar-tree listing still describes the failure manifest correctly after the atomic-write change and names the shared atomic writer plus all four writers that delegate to it"
    requirement: "D-20"
    verification:
      - kind: other
        ref: "file scan: 'quantlab/utils/atomic.py:write_json_atomically' + '四个写入方' + '它不是续跑输入' present beneath the tree"
        status: pass
    human_judgment: false
  - id: D18
    description: "`README.md`'s entry-point section describes the three ingest scripts as thin shells over the registry (and lists ingest_us_equity.py for the first time), while its Environment Variables section still names each credential by NAME only"
    requirement: "D-15"
    verification:
      - kind: other
        ref: "file scan: 'THIN SHELLS over the data source registry' + 'ingest_us_equity.py --' present; Environment Variables section byte-unchanged in the diff"
        status: pass
      - kind: other
        ref: "the credential regex sweep (D12) covers README.md"
        status: pass
    human_judgment: false
  - id: D19
    description: "Whole-suite regression: the 732-test Wave-6 baseline is untouched"
    verification:
      - kind: integration
        ref: "uv run pytest -q -> 732 passed, 0 failed (run three times across the plan)"
        status: pass
    human_judgment: false
  - id: D20
    description: "D-12 / D-13 / D-19 as cross-repo contract statements: what moved, what was given up, and nothing superseded left standing"
    requirement: "D-12"
    verification: []
    human_judgment: true
    rationale: "These three are decisions about ANOTHER repository's responsibilities, so no in-repo test can prove them — the plan's own flagged_assumptions says so. The obligation this phase carries for all three is documentary, and D1/D3/D7 above prove the documents now say the right thing. Whether the quantlab-console repo actually assumes those responsibilities is unobservable from here; the first console integration is what closes it."

duration: 22 min
completed: 2026-09-09
status: complete
---

# Phase 03.4 Plan 07: Documentation Debt Closed Summary

**Every statement this phase invalidated is now rewritten rather than left standing beside its replacement — the ROADMAP's three superseded claims (the subprocess write rule, the `__init__.py` enumeration mechanism, the `_failures.json` resume-input claim) plus the concurrency answer and the D-14 amendment; `03.4-RESEARCH.md`'s six open questions annotated with their closers and its two `needs user confirmation` assumptions recorded as settled; and a new 658-line Chinese `example/registry.md` whose seven runnable examples were actually run with all three credential env vars unset.**

## Performance

- **Duration:** 22 min
- **Started:** 2026-09-09T01:44:02Z
- **Completed:** 2026-09-09T02:06:00Z
- **Tasks:** 3
- **Files modified:** 9 (1 created, 8 modified)

## Accomplishments

- **The ROADMAP now describes the system that was built, not the three statements later amendments reversed.** All three sat in the same section and all three fail the same way — the next reader implements whichever they read first. The boundary contract states the IN-PROCESS write rule and is marked SETTLED; the `DataSourceRegistry` bullet states that the vendor imports live at the bottom of `registry.py` and the package `__init__.py` stays empty; the programmatic-entry-point bullet stops calling `_failures.json` a resume input and records that nothing under `quantlab/` reads it. Two further bullets gained the answers they had been left asking for: the sidecar writes were not atomic, now are through one shared writer, and no lock was added (D-20); and `ingest_us_equity.py` is **not** redundant with `ingest_tiingo.py --universe us_all`, so all three shells stay.
- **The sentinel rule was honoured without weakening the prose.** The word `subprocess` appears exactly once in the whole Phase 03.4 section, inside `the subprocess-CLI rule it supersedes`, and that occurrence *is* the sentinel. Both mechanisms — the retired write rule and D-07's cold-import completeness test — are otherwise described as running "in a child process". The gate exists to catch a superseded INSTRUCTION standing unlabelled; a bare-word ban cannot tell an instruction from an accurate description of what the old rule was, and this wording lets both be true at once.
- **The edit stayed scoped.** 57 insertions and 18 deletions in a 466-line file (plan bound: 140 combined), every hunk inside old lines 289-348 — the Phase 03.4 section runs 250-390. The out-of-scope paragraph that keeps long-task isolation, the log sink and same-source concurrency control with the console (D-13 / D-19 / D-20) is byte-unchanged, as required.
- **`example/registry.md` is real, in Chinese, and its examples were really run.** 658 lines, 4,553 CJK characters, eight worked examples of which **seven were executed on this machine with `TIINGO_API_KEY`, `APCA_API_KEY_ID` and `APCA_API_SECRET_KEY` all explicitly unset** — enumeration and credential status, capability queries and the `get()` error message, cold-import completeness in a real subprocess, `config_factory` path derivation, the inspector's inventory/coverage/failures against the repo's real Alpaca 1m tree, a real `browse_raw` collect, and the reporter/cancel-token surface. The eighth (the console-side `run()` with a live cancel timer) is labelled `此例未实际运行` because it needs a Tiingo credential.
- **A pasted output that this phase had silently falsified was caught before it shipped.** Patching only the last line of `example/acquisition.md`'s dry-run block would have produced a block whose 20 upper lines came from a 2026-09-07 run and whose bottom four came from today's — the exact fabrication the directory's stated convention forbids. Both `example/acquisition.md`'s and `example/chunking.md`'s dry-run blocks were re-run and replaced **whole**. `example/chunking.md` was not in the plan's file list; see Deviations.
- **`03.4-RESEARCH.md` is annotated, not rewritten.** The whole-file diff is 51 insertions and **exactly one** deletion — the heading line. Every question's text, including the parts that turned out to be wrong, survives verbatim, because erasing what was unknown at research time erases the reason the decision reads the way it does.

## Task Commits

1. **Task 1: rewrite the ROADMAP statements later amendments reversed** — `2a3f7e7` (docs)
2. **Task 2: `example/registry.md` + the stale acquisition documentation** — `a3c2ca2` (docs)
3. **Task 3: mark `03.4-RESEARCH.md`'s open questions resolved** — `7834044` (docs)

**Commits measured:** `git rev-list --count 1cd6b8f..HEAD` = 3 (plus this metadata commit).

## Files Created/Modified

- `example/registry.md` *(new, 658 lines)* — 一句话 / 不用它会怎样 (5 grounded failure modes) / 核心概念 / 它是怎么工作的 (where descriptors live, why the vendor imports are at the bottom, why `all()` sorts, how the shells consume it) / `run()` with the reporter and the cancel token / `SourceInspector` / 8 worked examples / 扩展 (register a third vendor, run for real in a throwaway module) / 11 常见坑.
- `.planning/ROADMAP.md` — five scoped bullet replacements inside Phase 03.4 only.
- `.planning/phases/03.4-data-source-registry/03.4-RESEARCH.md` — heading marked `(RESOLVED)` + lead-in; a `**Resolved:**` line on each of Q1-Q6; a settled-assumptions paragraph beneath the Assumptions table.
- `example/acquisition.md` — 核心概念's failure-manifest bullet; the `_run` flow diagram (events, the cancel break before the quota branch, the merge, the atomic write, the result object) plus three prose points on reporter/cancel; the `_attempt_batch` box now reads `_should_stop()`; the 并发/失败隔离/断点 table gained a 取消 row; 例 1 re-run credential-free and both 例 1 / 例 2 pointed at the inspector; 怎么接入一个新厂商 gained the descriptor-registration step; 常见坑 5 gained the not-resume-input clause.
- `example/pageledger.md` — a note beneath the sidecar tree distinguishing the failure manifest from the page ledger, stating it is not resume input, and naming `write_json_atomically` and the four writers delegating to it.
- `example/chunking.md` — its dry-run paste re-run credential-free (see Deviations).
- `example/README.md` — `registry.md` in the first-step reading table and in the extension list; the "这四篇" count corrected to five.
- `README.md` — a new lead bullet describing the three ingest scripts as thin shells over the registry, `ingest_us_equity.py` listed for the first time, `ingest_tiingo.py`'s vendor-class mention replaced. The Environment Variables section is untouched and still NAME-only.
- `quantlab/acquisition/inspector.py` — a module-docstring tense fix (see Deviations).

## Decisions Made

- **One labelled sentinel, and child-process wording everywhere else.** Recorded above; it is what lets D-07's cold-import test be described accurately in the same section that forbids the bare word.
- **Re-run and replace the WHOLE output block, never patch a line.** A block mixing two runs is worse than a stale one: it reads as a single real execution and every number in it is now unverifiable.
- **Two out-of-file-list stale statements were fixed rather than deferred.** A plan whose entire purpose is "no superseded statement left standing" that leaves two standing because they sit outside a `files_modified` list has not done its job.
- **`registry.md` documents the inspector's `nautilus_trader` cold-import cost as a stated trade, not a defect** — matching Wave 4's module docstring, which chose `StockDataset._scan_raw` over a second parquet scan precisely because that scan carries four separately-measured fixes.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] `quantlab/acquisition/inspector.py`'s module docstring asserted deleted behaviour as current**

- **Found during:** Task 2 (reading the shipped code the guide must describe accurately)
- **Issue:** The docstring reads "That is why `ingest_us_equity.py` **today** prints ``coverage report: skipped (export TIINGO_API_KEY to see it)``". Plan 06 deleted exactly that branch — along with the module's only `os.environ` read and its `import os`. Plan 06 fixed the analogous docstring in `tests/test_source_inspector.py` for this very reason but did not sweep for others. A docstring stating a shipped behaviour that no longer exists is how a reviewer re-derives a wrong mental model from the source, and it is the same class of defect this whole plan exists to close.
- **Fix:** rewritten to past tense ("USED TO print"), with the deletion dated to `03.4-06` and a pointer to `tests/test_ingest_shells.py::test_the_dry_run_needs_no_credential`, the lock that keeps it history.
- **Files modified:** `quantlab/acquisition/inspector.py`
- **Verification:** `uv run pytest -q` → 732 passed; `grep -rn "skipped (export"` now matches only `example/`-free locations, i.e. `tests/test_ingest_shells.py` where both hits are the deliberate history statement and the negative assertion.
- **Committed in:** `a3c2ca2`

**2. [Rule 1 - Bug] `example/chunking.md` pasted a dry-run output containing the deleted skip line**

- **Found during:** Task 2 (sweeping for the stale literal)
- **Issue:** `example/chunking.md:435` pastes an `ingest_us_equity.py --dry-run` output ending in `coverage report:   skipped (export TIINGO_API_KEY to see it; ...)`. That line cannot be produced any more. The file is not in this plan's `files_modified`, but it is the same defect as `example/acquisition.md:355`, which is — and leaving one of two identical stale pastes standing would have been arbitrary.
- **Fix:** the command re-run credential-free and the block replaced **whole** (not patched), so the entire paste is one real execution. Its lead-in now records the `env -u` invocation and the 2026-09-09 date, and three bullets were added noting that the coverage report needs no credential and that `re-fetch, widened: 6924` is exactly the widening case that document is about.
- **Files modified:** `example/chunking.md`
- **Verification:** the pasted block is byte-identical to the captured stdout of the command printed above it; `grep -rn "skipped (export" example/` → no match.
- **Committed in:** `a3c2ca2`

**3. [Rule 1 - Bug] Patching `example/acquisition.md`'s dry-run block line-by-line produced a fabricated output, caught before commit**

- **Found during:** Task 2 (immediately after the first edit pass)
- **Issue:** The scripted edit replaced only the `coverage report:` line inside 例 1's output block. The surrounding 20 lines were the 2026-09-07 run: `window: ... 2026-09-07`, `trading days (~): 2693`, `dense grid cells: 36,972,197` and a different `preview` list. Today's real run gives `2026-09-08`, `2694` and `36,985,926`. The result would have been a single block presenting itself as one execution while being two — precisely the "a worked example must not paste an output that was never produced" prohibition this plan carries.
- **Fix:** the whole block located by its fence boundaries and replaced with the verbatim captured stdout of a fresh credential-free run; the lead-in and the command line updated to show the `env -u` invocation and the new date.
- **Files modified:** `example/acquisition.md`
- **Verification:** block diffed against the captured stdout file; `PHASE_GATE_OK` from the same command.
- **Committed in:** `a3c2ca2`

**4. [Rule 1 - Bug] Task 3's `**Resolved:**` count criterion is non-discriminating**

- **Found during:** Task 3 (first verify run)
- **Issue:** The plan's command counts every `**Resolved:**` occurrence in the section and asserts `== 6`. The acceptance criteria also require a lead-in sentence, and the natural lead-in explains what the marker is — so it contains the literal. The first run returned **7 against fully correct content**. This is the phase's recurring trap in its third form (plan 01's `no tests ran`, which pytest never emits; plan 05's `src.count('Parallel(')`, which counted comments and was already 3 at HEAD).
- **Fix:** the lead-in reworded to "each now ends with a resolution line naming the decision or the plan that closed it", so the plan's command passes exactly as written; **and** a discriminating form was run alongside it — 6 markers matching `^\s+- \*\*Resolved:\*\* ` against 6 questions matching `^\d+\. \*\*`, which a prose mention cannot satisfy. Recorded in `.planning/WINDOWS.md` (entry 12) so the finding survives out of this SUMMARY's context.
- **Files modified:** `.planning/phases/03.4-data-source-registry/03.4-RESEARCH.md`
- **Verification:** both forms print their token; `git diff --numstat` → 51 insertions, 1 deletion.
- **Committed in:** `7834044`

**5. [Rule 3 - Blocking] `example/acquisition.md` overclaimed byte-identical stderr**

- **Found during:** Task 2 (reviewing the flow prose against 03.4-05's evidence)
- **Issue:** The first draft said "shell 跑法的 stderr 是逐字节不变的" of the progress bar. 03.4-05 asserts the tqdm reporter's **constructor kwargs**, explicitly *not* captured stderr, because a bar's rendered width depends on the terminal. Only `_report_coverage`'s four log lines are literally unchanged.
- **Fix:** split into the two claims that are actually true — constructor arguments are what is pinned, and the four coverage log lines are the byte-identical part with the `coverage` event additive on top.
- **Files modified:** `example/acquisition.md`
- **Verification:** re-read against `tests/test_acquisition_progress.py::test_tqdm_default_bar_arguments_are_unchanged` and `test_logging_is_unchanged`.
- **Committed in:** `a3c2ca2`

**6. [Rule 3 - Blocking] An untracked runtime lock file appeared in the working tree**

- **Found during:** Task 2 (pre-commit `git status --short`)
- **Issue:** `.planning/milestone.lock` — a JSON file carrying this session's id and pid, written by the orchestrator — was untracked. Committing session/pid state would put a machine-local artefact into shared history; leaving it untracked violates the executor's own "never leave generated files untracked".
- **Fix:** added to `.gitignore` beside the existing `.planning/state.json` and `.gsd/` entries, with the same style of comment.
- **Files modified:** `.gitignore`
- **Verification:** `git status --short` shows no untracked entry.
- **Committed in:** this plan's metadata commit

---

**Total deviations:** 6 auto-fixed (4 bugs — two stale statements outside the file list, one fabricated-output near-miss, one non-discriminating verify; 2 blocking — an overclaim and an untracked runtime file)
**Impact on plan:** No scope creep. Three of the four bugs are exactly the defect class this plan exists to close, found one file outside the plan's list; the fourth is the phase's third grep-shaped-criterion failure and is recorded as such rather than quietly worked around.

## Issues Encountered

**Committing to `main`.** Same condition every prior wave recorded, and explicitly authorised by the orchestrator for this run: `git.branching_strategy` is `"none"`, all prior plans in this repo committed to `main`, this plan was dispatched sequentially onto the main working tree with worktree isolation disabled (#683 fork-base degrade), and no concurrent agent exists. **No self-recovery of any kind was performed** — no `git update-ref`, no force-push, no history rewrite, no `git stash`, no `git clean`; only additive commits. Four pre-existing dirty files (`cal.py`, `pyproject.toml`, `test.py`, `uv.lock`) were present at plan start and were left untouched — none was staged.

**Grep-shaped criteria: which ones actually discriminate.** The upstream handoff asked for this explicitly, so here is the audit of every text-scan criterion this plan carries:

| Criterion | Discriminates? | Note |
|---|---|---|
| `SUBPROCESS` absent after stripping the sentinel | **Yes** | Verified both ways: the section contains exactly one `subprocess` and exactly one sentinel occurrence, so the strip is what makes it pass. Writing the word anywhere else would fail it. |
| `IN-PROCESS` present | Weak but adequate | The section already contained `IN-PROCESS` on the READS bullet before this plan, so it would have passed unchanged. Its partner assertion is what carries the weight. |
| `### Phase ` count ≥ 11 + Phase 7 heading | **Yes** | A whole-file rewrite is what it is aimed at, and it would catch one. |
| `git diff --numstat` ≤ 140 | **Yes** | Measured 75. |
| `**Resolved:**` count == 6 | **No** — see Deviation 4 | Counts a prose mention. Passed only after the lead-in was reworded; a discriminating list-item form was run alongside. |
| RESEARCH deletions ≤ 1 | **Yes** | Directly measures the annotate-don't-rewrite property. Measured 1. |
| `registry.md` > 120 lines + linked | Weak | Length is not content. `surface_covered` (every documented symbol present) was added as a partner check; neither proves the prose is *right*, which is what D11's `human_judgment` records. |
| Credential-shaped literal sweep | Partial | It catches `sk-`-prefixed keys and quoted `api_key=` assignments. A Tiingo key is a bare 40-char hex string and would NOT match. The real control is that every credential mention in these documents is a variable name, which a reviewer can see; the regex is a backstop, not the guarantee. |
| `grep -q "coverage report"` (PHASE_GATE_OK) | **Yes**, per the Wave 6 handoff | Asserted on the surviving `  coverage report:` header, not the deleted `skipped (export` line — which is now a correct negative. Asserting the old line would have failed against correct code. |

**`requirements-completed` IDs are phase-local.** `D-12`/`D-13`/`D-15`/`D-19`/`D-20` come from `03.4-CONTEXT.md`, not from `.planning/REQUIREMENTS.md` (keyed on `DATA-*`/`FACTOR-*`). Copied verbatim per the template; `requirements ready-ids`/`mark-complete` has nothing matching to check off, the same situation every plan in this phase has been in.

## Known Stubs

None. This plan added no code path; its one source change is a docstring tense correction.

Two things are deliberately **unproven rather than stubbed**, and both are recorded above:

- **D11 (the pasted example outputs).** Every one of the seven was really run, but nothing in the repository pins them against future drift — which is exactly how `example/chunking.md`'s paste went stale under plan 06 without anyone noticing. Carried as `human_judgment: true`.
- **D20 (D-12 / D-13 / D-19 as cross-repo contracts).** Decisions about `quantlab-console`'s responsibilities, unobservable from this repository. The documentary obligation is discharged; the rest is the first console integration's to prove.

`.planning/WINDOWS.md`'s three existing 03.4 entries (the `test_tiingo_quota.py` coverage gap from Wave 5, and Wave 6's two `unmet-truth` entries for the empty-roster refusal and the unexercised `stamp_watermarks` write) are **untouched and still open**. This plan neither closed nor papered over any of them; entry 12 was appended for Deviation 4.

## Threat Flags

None beyond the plan's own register, all mitigated as planned:

- **T-03.4-07-01** (a credential value pasted into a worked example) — every credential mention in `example/registry.md`, `example/acquisition.md`, `example/chunking.md` and `README.md` is a variable NAME. The regex sweep passes; see the discrimination audit above for what that sweep does and does not catch. Seven of eight examples were run with all three variables *unset*, so there was no value in the environment to leak into an output.
- **T-03.4-07-02** (a fabricated example output) — caught in the act; see Deviation 3.
- **T-03.4-07-03** (a whole-file ROADMAP rewrite) — 75 lines of churn against a 140 bound, 11 phase headings intact, every hunk inside the 03.4 section.
- **T-03.4-07-04** (a superseded boundary rule left standing) — one sentinel occurrence, verified two ways; the two sibling amended statements replaced in the same commit.
- **T-03.4-07-05** (a research artifact rewritten rather than annotated) — exactly one deleted line.
- **T-03.4-07-SC** — no package-manager install; `pyproject.toml` untouched by this plan (its pre-existing dirty state predates it).

## User Setup Required

None — no external service configuration required. Every command in this plan ran offline with `TIINGO_API_KEY`, `APCA_API_KEY_ID` and `APCA_API_SECRET_KEY` explicitly unset.

## Next Phase Readiness

**Phase 03.4 is complete — 7 of 7 plans executed.** What the next reader inherits:

1. **The ROADMAP's Phase 03.4 section is now the accurate contract.** Writes are in-process, `run()` is acquisition-only, the vendor imports are at the bottom of `registry.py`, `_failures.json` is not resume input, the sidecar writes are atomic and no lock exists. Anything quoting the old subprocess rule is quoting a superseded document.
2. **`example/registry.md` is the per-layer guide for anything registry-shaped**, and `example/README.md`'s reading order now puts it beside `acquisition.md` in step one. A new data source is documented there, not in `acquisition.md`.
3. **Three `.planning/WINDOWS.md` items remain open for 03.4** and are the honest state of the phase: the `test_tiingo_quota.py` first-statement gap (pre-existing, surfaced by Wave 5's mutation M6), the untested empty-roster refusal, and the unexercised `--stamp-legacy-watermarks` write, which is still the open blocking-human checkpoint from quick task `260906-26o`.
4. **The pasted-output convention now has a demonstrated failure mode.** `example/chunking.md`'s paste went stale under a plan that never touched that file. Any future change to `ingest_us_equity.py`'s dry-run output must sweep `example/` for pastes of it — there is no test that will catch it.

Baseline for later phases: **732 passed** (unchanged by this plan).

## Self-Check: PASSED

- All 9 key files verified present on disk with `[ -f ]`.
- All 3 task commits verified present via `git log --oneline --all | grep`: `2a3f7e7`, `a3c2ca2`, `7834044`.
- Every task's `<acceptance_criteria>` re-run and logged (Task 1: 8/8, Task 2: 7/7, Task 3: 5/5 — the fifth, `**Resolved:**` == 6, only after Deviation 4's rewording, with a discriminating partner check added).
- Plan-level `<verification>` re-run in full: `boundary_rewritten`, `roadmap_intact`, `amendments_propagated`, `open_questions_closed`, `one_resolution_list_item_per_question`, `registry_doc_ok`, `surface_covered`, `no_credential_values_in_docs`, `PHASE_GATE_OK`, and `uv run pytest -q` → **732 passed, 0 failed**.
- `commits: 3` MEASURED via `git rev-list --count 1cd6b8f..HEAD`, not narrated; `plan_head_before` read from the on-disk ledger written before the first commit.
- `actuals.tokens` = 19,540, measured as chars/4 over the realized diff (78,162 chars across the nine files), on the same scale as the plan's `estimate.tokens: 54000` — a genuine 64% under, because five of the seven changed documents needed a paragraph rather than a section.
- `git diff --diff-filter=D` on each task commit: **no deletions** in any of the three.

---
*Phase: 03.4-data-source-registry*
*Completed: 2026-09-09*
